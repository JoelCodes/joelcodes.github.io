---
interface Props {
  title: string;
  slug: string;
  pubDate: Date;
  description: string;
  featuredImage: string;
  tags: string[];
  readingTime: number;
}

const { title, slug, pubDate, description, featuredImage, tags, readingTime } = Astro.props;

// Format date
const formattedDate = pubDate.toLocaleDateString('en-US', {
  month: 'short',
  day: 'numeric',
  year: 'numeric',
});

// Truncate description to 160 chars
const excerpt = description.length > 160 ? description.slice(0, 157) + '...' : description;
---

<article class="blog-card show" data-tags={tags.join(',')}>
  <a
    href={`/blog/${slug}`}
    class="block group transition-all duration-300 hover:-translate-y-2 rounded-lg overflow-hidden border-[4px] border-text-light dark:border-text-dark bg-bg-light dark:bg-bg-dark shadow-neo-turquoise hover:shadow-neo-turquoise-hover focus-visible:outline-none focus-visible:shadow-[0_0_0_2px_var(--color-bg-light),0_0_0_6px_var(--color-turquoise)] dark:focus-visible:shadow-[0_0_0_2px_var(--color-bg-dark),0_0_0_6px_var(--color-turquoise-dark)]"
  >
    <!-- Featured image with 16:9 aspect ratio -->
    <div class="relative w-full bg-text-muted-light/10 dark:bg-text-muted-dark/10 overflow-hidden" style="aspect-ratio: 16/9;">
      <img
        src={featuredImage}
        alt={title}
        class="absolute inset-0 w-full h-full object-cover"
        width="640"
        height="360"
        loading="lazy"
        onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"
      />
      <!-- Placeholder fallback (hidden by default, shown on error) -->
      <div class="absolute inset-0 items-center justify-center hidden">
        <svg class="w-16 h-16 text-text-muted-light/30 dark:text-text-muted-dark/30" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
        </svg>
      </div>
    </div>

    <!-- Card content -->
    <div class="p-4">
      <!-- Title -->
      <h3 class="font-heading text-xl font-bold mb-2 group-hover:text-turquoise dark:group-hover:text-turquoise-dark transition-colors">
        {title}
      </h3>

      <!-- Date and reading time -->
      <div class="flex items-center gap-3 text-sm text-text-muted-light dark:text-text-muted-dark mb-3">
        <time datetime={pubDate.toISOString()}>{formattedDate}</time>
        <span class="text-text-muted-light/50 dark:text-text-muted-dark/50">|</span>
        <span>{readingTime} min read</span>
      </div>

      <!-- Excerpt -->
      <p class="text-sm text-text-muted-light dark:text-text-muted-dark mb-3 line-clamp-3">
        {excerpt}
      </p>

      <!-- Tags -->
      {tags.length > 0 && (
        <div class="flex flex-wrap gap-2">
          {tags.map((tag) => (
            <span class="text-xs px-2 py-1 rounded-full border-2 border-turquoise/50 dark:border-turquoise-dark/50 bg-turquoise/10 dark:bg-turquoise-dark/10 text-text-light dark:text-text-dark">
              {tag}
            </span>
          ))}
        </div>
      )}
    </div>
  </a>
</article>
