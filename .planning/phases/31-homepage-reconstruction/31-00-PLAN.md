---
id: "31-00"
phase: 31-homepage-reconstruction
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md
autonomous: true
requirements:
  - PAGE-01
must_haves:
  truths:
    - "Phase 31-01 page-frame composition has a content lookup table for Hero/Services/Process/About/ContactSection/Header v1.3 verbatim copy (D-98 chain)"
    - "Phase 31-01 has a documented Section/Header (G0wNOc) descendants-override path for Joel's 4-link nav per D-162 (Path B HIDE+REPLACE vs Path C children-whole-replacement)"
    - "Phase 31-01 has a documented Primitive/Button/Secondary (hIWuC) descendants-override property path for OPEN-25-01 white-stroke + white-label per D-157"
    - "Phase 31-01 has per-ID role assignments for ujMLJ Hero subtree (0veF5 / Wx9kx / 7QsZc / HuBKK / LqPtn / ggx3v) + identified Hero illustration leaf node ID for D-155 LOCK note target"
    - "Baseline regression confirmed: 21 component IDs intact, 9 page-frame IDs intact, 107 user-facing tokens intact"
  artifacts:
    - path: ".planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md"
      provides: "Content lookup table + Header override path decision + hIWuC override IDs + ujMLJ Hero subtree role map + baseline regression evidence"
      contains: "Task 0 / Task 1 / Task 2 / Task 3 / Task 4 sections"
  key_links:
    - from: "31-00-SUMMARY.md Task 0 content lookup"
      to: "31-01-PLAN.md Tasks 3-7 v1.3 verbatim content overrides"
      via: "downstream lookup table consumed by Plan 31-01 executor"
    - from: "31-00-SUMMARY.md Task 1 Header override path"
      to: "31-01-PLAN.md Task 2 Section/Header descendants override"
      via: "Path B (HIDE+REPLACE) or Path C (children-whole-replacement) selected by probe outcome"
    - from: "31-00-SUMMARY.md Task 2 hIWuC override IDs"
      to: "31-01-PLAN.md Task 3 Hero Secondary Button descendants override"
      via: "stroke child ID + label child ID values plugged into Hero CTA override"
    - from: "31-00-SUMMARY.md Task 3 ujMLJ Hero subtree role map"
      to: "31-01-PLAN.md Task 10 calibration gate § 3.4 Hero side-by-side"
      via: "ujMLJ Hero subtree node ID used as get_screenshot pairing target"
---

<objective>
Phase 31 foundation — probe-only plan that gathers content + structural intel Phase 31-01 needs to compose the Homepage page frame. No library mutations, no calibration gate at close (per D-86/D-102/D-124/D-139/D-152/D-165 chain).

Purpose: De-risk Plan 31-01 by resolving 3 sources of MEDIUM-confidence ambiguity at plan-time:
1. Joel's v1.3 verbatim content (Hero H1 / Services categories / Process steps / About bio / Header nav labels) — D-98 chain enforcement
2. Section/Header (G0wNOc) descendants-override path for D-162 4-link nav (Path B vs Path C decision)
3. Primitive/Button/Secondary (hIWuC) stroke + label child IDs for OPEN-25-01 white-stroke carve-out per D-157
4. ujMLJ Hero subtree role assignment per-ID (PEN-INVENTORY line 121 enumeration) + Hero illustration leaf node for D-155 LOCK note target

Output: `31-00-SUMMARY.md` with 5 dense sections (T0 content table + T1 Header path + T2 hIWuC IDs + T3 ujMLJ Hero map + T4 baseline regression evidence) consumed by Plan 31-01 executor.

This plan ships ZERO Pencil mutations + ZERO new tokens + ZERO library changes. Library count 19 → 19. Token surface 107 → 107.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-homepage-reconstruction/31-CONTEXT.md
@.planning/phases/31-homepage-reconstruction/31-RESEARCH.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/PITFALLS.md
@.planning/phases/30-design-system-reference-reconstruction/30-00-SUMMARY.md
@.planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/23-audit-token-foundation/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: v1.3 content extraction → content lookup table</name>
  <read_first>
    - src/pages/index.astro (entry; confirms 5-component composition order: Hero → Services → Process → About → ContactSection)
    - src/components/Hero.astro (lines 71-73 + 91-93 H1; lines 79-81 + 116-118 CTA copy; lines 13-49 outcome tiles NOT inherited per D-157)
    - src/components/Services.astro (3 service category blocks; section H2; per-category title + body)
    - src/components/Process.astro (5 step blocks; section H2; per-step number + title + body)
    - src/components/About.astro (H2; bio paragraph(s); credibility stats; CTA copy + href)
    - src/components/homepage/ContactSection.astro (D-161 sibling-note documentation source ONLY; NOT instanced; extract headline + body + form intent for sibling note text)
    - src/components/layout/Header.astro (4 nav-link labels Blog/Projects/FAQ/Contact + hrefs; "Joel Shinness" wordmark for D-163 sibling note)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md § "Established Patterns" + D-157/-158/-159/-160/-161/-162/-163 content authority lines
  </read_first>
  <action>
    Read the 7 v1.3 source files listed above using the Read tool (these are .astro source — read directly, NOT via Pencil MCP).

    For each, extract the literal text content (NOT structure, NOT styling) and write a dense lookup table to 31-00-SUMMARY.md under heading `## Task 0 — v1.3 Content Lookup Table` with the following exact sub-sections:

    1. **Hero (Hero.astro)**
       - H1 headline (full quoted string from lines 71-73 + 91-93; if any are conditional, capture the canonical display string)
       - Subtitle (the "Ready to get started?" line — exact string from CTA region)
       - Primary CTA label (e.g., "Let's talk!" — exact string from lines 79-81 / 116-118)
       - Primary CTA href (likely /contact)
       - Secondary CTA candidates — list any explicit v1.3 secondary CTA (default: NONE; if absent, document "no v1.3 secondary CTA" and note that Plan 31-01 Task 3 default lean per D-Discretion is "See how I work" / "View my projects" linking to /projects)

    2. **Services (Services.astro)**
       - Section H2 (exact string — "Solutions That Fit" or whatever ships)
       - Per-category title × 3 (likely AI Development / Automation / Web Apps — exact strings)
       - Per-category body × 3 (the descriptive paragraph for each — exact strings, preserve sentence boundaries)
       - Per-category CTA if present (e.g., "Learn more" or per-category link); document "no per-category CTA" if absent
       - Note: Joel-brand isometric illustrations NOT inherited per D-158 — image-slot is STUB

    3. **Process (Process.astro)**
       - Section H2 (exact string — "How We Work Together" or whatever ships)
       - Per-step block × 5 (number 1-5 + title + body — exact strings preserving sentence boundaries; v1.3 names likely Discovery Call / Prototype / Proposal / Building / Handoff & Support)
       - Note: Joel-brand isometric illustrations NOT inherited per D-159 — step image-slot is STUB

    4. **About (About.astro)**
       - Section H2 (exact string — "About Me" or whatever ships)
       - Bio paragraphs (full text — exact strings, preserve all sentence boundaries; capture all paragraphs)
       - Credibility stats (e.g., "15+ Years experience" / "200+ Students" — exact strings)
       - CTA label (e.g., "Let's talk") + href (likely /contact)
       - Note: Joel headshot NOT inherited per D-160 — headshot-slot is STUB

    5. **ContactSection (ContactSection.astro)**
       - Section headline (exact string — e.g., "Ready to get started?" or "Let's talk")
       - Body text (the 48-hour body line + any other framing)
       - Note: full 8-field form NOT instanced on Homepage per D-161 — Section/CTA Hs5rc closing instance replaces with Primary Button to /contact n0QqTd; this content lookup feeds D-161 sibling-note text + Section/CTA headline-slot + body-slot overrides

    6. **Header (Header.astro)**
       - Nav links × 4 with label + href (exact strings — likely Blog / Projects / FAQ / Contact)
       - Logo wordmark text (exact string — likely "Joel Shinness")
       - Note: 4-link nav drives D-162 descendants override; "Joel Shinness" wordmark NOT inherited per D-163 — Crito placeholder stays + sibling note documents

    7. **Section/SecondaryLink emerging-consumer evaluation (D-164 + Phase 30 D-145 reference)**
       - Audit Hero + Services + About + closing-CTA across content extracted above for any inline-text-link-with-iconLeading pattern (would be a 3rd consumer after Phase 27 D-99 Thank-you "Return to homepage" + Phase 29 D-133 Project detail back-nav)
       - Default expected outcome: NO third consumer (Hero CTAs are dual Buttons; Services footer-actions uses Button; About CTA uses Button; closing-CTA uses Button)
       - Document outcome: DEFER Section/SecondaryLink factoring to future phase / code milestone

    8. **Token surface escalation evaluation (D-72/D-106/D-135 mid-plan gate trigger)**
       - Confirm no v1.3 content surfaces a NEW token need (e.g., heading-5/-6 long-tail per OPEN-23-10, Poppins per OPEN-23-09, radius-semantic-pill per OPEN-23-12)
       - Default expected outcome: existing 107-token surface sufficient — Hero PJS 70 → type-semantic-display + Inter 18 → type-semantic-body-sm + heading-1 + prose-paragraph + Card / Button / Section tokens all already in surface

    Do NOT use the Pencil MCP for this task — `.astro` files are plain TypeScript, read with the Read tool.

    Sectional output convention: each sub-section uses `### Task 0.N — <Source file>` with the extracted strings in fenced code blocks (preserve whitespace, newlines, special chars).
  </action>
  <acceptance_criteria>
    - 31-00-SUMMARY.md contains a `## Task 0 — v1.3 Content Lookup Table` section with 8 sub-sections (Hero / Services / Process / About / ContactSection / Header / SecondaryLink eval / token escalation eval) populated with exact strings from v1.3 source files
    - Hero H1 string starts with `Your business needs more than software` (verifiable by grep)
    - Services contains exactly 3 categories with title + body each (count assertion)
    - Process contains exactly 5 steps with title + body each (count assertion)
    - Header contains exactly 4 nav-link labels (count assertion)
    - SecondaryLink eval explicitly documents DEFER outcome (or surfaces 3rd consumer with citation)
    - Token escalation eval explicitly documents 0 new tokens default (or surfaces escalation with citation)
    - Zero Pencil MCP calls made during Task 0 (verifiable in plan-execution log)
  </acceptance_criteria>
  <verify>
    grep -c "^### Task 0\." .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns 8
    grep "Your business needs more than software" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns at least 1 line
  </verify>
  <done>
    Plan 31-01 Tasks 3-7 have a single source of truth for every overridden text-slot — no executor needs to re-read .astro files.
  </done>
</task>

<task type="auto">
  <name>Task 1: Probe Section/Header (G0wNOc) descendants-override path</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md (D-162 SCOPED Header descendants override; Claude's Discretion "Header descendants-override path for nav items")
    - .planning/phases/31-homepage-reconstruction/31-RESEARCH.md § Focus 2 (Path B HIDE+REPLACE vs Path C children-whole-replacement) + OPEN-30-07 ref-composition constraint
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (Section/Header G0wNOc structure; 6 Crito-source nav labels Home/About/Services/Blog/FAQ/Contact; CTA at right; logo at left in g9oRa5)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md D-111 (TagFilter active-pill descendants-override pattern — proven precedent)
    - .planning/research/PITFALLS.md Pitfall O5/O6 (single-consumer rule — NO new components from this probe; Header instance override only)
  </read_first>
  <action>
    First, run `mcp__pencil__get_editor_state({ include_schema: false })` and assert `active_editor` ends with `Crito.pen`. Halt + surface to user on mismatch (D-166).

    Then call `mcp__pencil__batch_get({ nodeIds: ["G0wNOc"], readDepth: 3 })` to retrieve the Section/Header component subtree (Header in g9oRa5).

    From the returned tree, identify and record in 31-00-SUMMARY.md under `## Task 1 — Section/Header (G0wNOc) Override Path`:

    1. **Nav container node** — the direct child frame of G0wNOc that holds the 6 nav-item children (likely a horizontal-layout frame). Record its node ID + name.

    2. **Per-nav-item children × 6** — enumerate each nav-item child with:
       - Node ID
       - Display label text (the actual text node inside, with current Crito-source labels Home / About / Services / Blog / FAQ / Contact)
       - Node type (frame? group? text?) — required for descendants-override property path validity

    3. **Path B decision (default candidate per D-162 + § Discretion)** — HIDE Home + About + Services nav-items via `descendants.<homeNavItemId>.enabled = false`, `descendants.<aboutNavItemId>.enabled = false`, `descendants.<servicesNavItemId>.enabled = false`; REPLACE one hidden-slot's text with "Projects" via `descendants.<replacedSlotTextId>.text = "Projects"` AND `descendants.<replacedSlotId>.enabled = true`. Result: 4 visible nav-items (Blog / Projects / FAQ / Contact). Cite which of Home / About / Services becomes the "Projects" slot (default lean: REPLACE "Home" text with "Projects" since it's the leftmost/first in tab order).

    4. **Path C alternative (OPEN-30-07 ref-composition constraint considered)** — `descendants.<navContainerId>.children = [<new array of 4 nav-item children>]` whole-replacement. Document whether Pencil 2.13 schema allows whole-children-array replacement on a ref-instance descendants override (per § Focus 2 OPEN-30-07 said ref-composition cannot accept ARBITRARY children-array mutations; Path C MAY NOT be valid).

    5. **Decision** — select Path B (default candidate) OR Path C (if probe surfaces evidence Path B has a structural blocker). Document the exact property path map that Plan 31-01 Task 2 will pass to `batch_design Update` on the Homepage Header instance:
       ```
       descendants: {
         "<homeNavItemId>": { enabled: false },
         "<aboutNavItemId>": { enabled: false },
         "<servicesNavItemId>": { enabled: false },
         "<replacedSlotTextId>": { text: "Projects" },
         "<replacedSlotId>": { enabled: true }
       }
       ```
       (substitute exact IDs from probe; "Projects" hrefs are code-milestone wiring — D-163 chain.)

    6. **Logo slot ID** — identify the logo slot child node ID for D-163 sibling-note Pencil note target at Plan 31-01 Task 2 (the note attaches as a sibling of the Header instance, NOT the component — so this is informational; sibling note placement is on the Homepage Header instance, NOT the G0wNOc component itself per OPEN-30-07).

    DO NOT mutate G0wNOc or any other component. This task is read-only `batch_get` probe.
  </action>
  <acceptance_criteria>
    - mcp__pencil__get_editor_state returned active editor ending with Crito.pen BEFORE batch_get (logged in plan execution)
    - mcp__pencil__batch_get({ nodeIds: ["G0wNOc"], readDepth: 3 }) returned a tree with at least 6 nav-item children identifiable by labels Home/About/Services/Blog/FAQ/Contact
    - 31-00-SUMMARY.md `## Task 1 — Section/Header (G0wNOc) Override Path` section contains:
      - Nav container node ID + name
      - Exactly 6 nav-item children enumerated with IDs + labels + types
      - Path B candidate property-path map with exact IDs substituted
      - Path C feasibility note (per OPEN-30-07)
      - DECISION: Path B (default) OR Path C (with justification)
      - Logo slot node ID
    - Zero mutations to G0wNOc subtree (verifiable by re-running batch_get and confirming child IDs + text values unchanged)
  </acceptance_criteria>
  <verify>
    Re-run `mcp__pencil__batch_get({ nodeIds: ["G0wNOc"], readDepth: 3 })` after task — child IDs and text values match initial probe (zero mutations).
    grep -E "(homeNavItemId|aboutNavItemId|servicesNavItemId)" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns at least 3 lines with actual ID substitutions (not literal "homeNavItemId" placeholder text).
  </verify>
  <done>
    Plan 31-01 Task 2 has exact `descendants: {...}` property-path map ready to paste into batch_design Update call for the Homepage Header instance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Probe Primitive/Button/Secondary (hIWuC) descendants-override path for OPEN-25-01 white-stroke carve-out</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md (D-157 Hero Secondary Button white-stroke override on dark-bg; OPEN-25-01 RESOLVES at Phase 31)
    - .planning/phases/31-homepage-reconstruction/31-RESEARCH.md § Focus 3 (hIWuC stroke + label child ID identification)
    - .planning/phases/25-section-compound-components/25-CONTEXT.md D-43 (Secondary Button ships navy stroke + navy label for light-bg consumers; OPEN-25-01 carve-out reserved for Phase 31 Hero dark-bg consumer)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (hIWuC build inside avgor; structure)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (primitive IDs reference: M7eUr Button/Default + hIWuC Button/Secondary)
    - .planning/research/PEN-INVENTORY.md (OPEN-25-01 carve-out documentation)
  </read_first>
  <action>
    Run `mcp__pencil__get_editor_state({ include_schema: false })` + assert active editor (D-166).

    Then call `mcp__pencil__batch_get({ nodeIds: ["hIWuC"], readDepth: 3 })` to retrieve the Primitive/Button/Secondary component subtree.

    From the returned tree, identify and record in 31-00-SUMMARY.md under `## Task 2 — Primitive/Button/Secondary (hIWuC) White-Stroke Override Path`:

    1. **Stroke-bearing node** — identify the child node (likely the outer button frame or a rectangle child) that carries the navy stroke property. Record:
       - Node ID
       - Current stroke value (expected: navy literal `#141f39ff` or token reference `color-semantic-bg-dark` per Phase 25 D-43)
       - Stroke property name (likely `stroke` or `borderColor` — confirmed by schema)
       - Stroke width property + value (if present — likely 1 or 2)

    2. **Label text node** — identify the child text node that carries the navy fill property. Record:
       - Node ID
       - Current fill value (expected: navy literal or token reference)
       - Fill property name (likely `fill` for text)
       - Current text content (likely placeholder like "Button" or "Label")

    3. **Optional icon nodes** — if hIWuC ships with iconLeading/iconTrailing slot children, identify their IDs (these may also need fill override on dark-bg per OPEN-25-01 — Plan 31-01 Task 3 plan-execution decides whether to override icon fill alongside label fill).

    4. **Descendants-override property-path map** — write exact map Plan 31-01 Task 3 will pass to `batch_design Insert` (when inserting the Hero Secondary CTA instance):
       ```
       descendants: {
         "<strokeBearingNodeId>": { stroke: "#ffffffff" },
         "<labelTextNodeId>": { fill: "#ffffffff" }
         // optional: "<iconLeadingNodeId>": { fill: "#ffffffff" }
         // optional: "<iconTrailingNodeId>": { fill: "#ffffffff" }
       }
       ```
       Substitute exact IDs from probe. The literal `#ffffffff` value (NOT a token reference) is the documented OPEN-25-01 carve-out per Phase 25 D-43 — Crito-source-literal white restoration on dark-bg Hero CTA.

    5. **Schema feasibility note** — confirm that descendants-override property keys `stroke` and `fill` are valid for the node types involved (per the schema retrieved at task start via get_editor_state include_schema=false; if validity is unclear, re-call get_editor_state with include_schema=true and document the relevant schema fragment).

    DO NOT mutate hIWuC or any other component. This task is read-only `batch_get` probe.
  </action>
  <acceptance_criteria>
    - mcp__pencil__get_editor_state returned active editor ending with Crito.pen BEFORE batch_get
    - mcp__pencil__batch_get({ nodeIds: ["hIWuC"], readDepth: 3 }) returned a tree
    - 31-00-SUMMARY.md `## Task 2 — Primitive/Button/Secondary (hIWuC) White-Stroke Override Path` contains:
      - Stroke-bearing node ID + current navy stroke value + stroke property name
      - Label text node ID + current navy fill value + current text content
      - Optional icon node IDs (or explicit "no icon slots in default hIWuC build" note)
      - Exact descendants-override property-path map with substituted IDs (NOT literal placeholder text "strokeBearingNodeId")
      - Schema feasibility note (with citation if include_schema=true was needed)
    - Zero mutations to hIWuC subtree
  </acceptance_criteria>
  <verify>
    Re-run `mcp__pencil__batch_get({ nodeIds: ["hIWuC"], readDepth: 3 })` after task — child IDs and properties match initial probe.
    grep -E "stroke.*#ffffffff" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns at least 1 line in the Task 2 section.
  </verify>
  <done>
    Plan 31-01 Task 3 has exact `descendants: { ... }` property-path map for OPEN-25-01 white-stroke carve-out — ready to paste into batch_design Insert call for Hero Secondary CTA instance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Probe ujMLJ Hero subtree role assignment + identify Hero illustration leaf node</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md (D-155 HYBRID + D-157 Hero structure source; D-167 LOCK target Hero illustration leaf)
    - .planning/phases/31-homepage-reconstruction/31-RESEARCH.md § Focus 4 (ujMLJ Hero subtree probe deliverables; 6 IDs 0veF5 / Wx9kx / 7QsZc / HuBKK / LqPtn / ggx3v per PEN-INVENTORY line 121)
    - .planning/research/PEN-INVENTORY.md line 65 (Home Page ujMLJ — IN-SCOPE, 10 editable children, factored:8 + partial:2, reconstruction_priority high)
    - .planning/research/PEN-INVENTORY.md line 121 (Hero subtree enumerated IDs)
    - .planning/research/PEN-INVENTORY.md line 129 (coral ellipse 286×225 — Crito Hero decorative anchor; Phase 31 D-157 reuses color-semantic-decorative-coral via Pencil-native ellipse per § Discretion)
    - .planning/research/PEN-INVENTORY.md lines 155-156 (PJS 70 mining evidence; type-semantic-display token)
    - .planning/phases/23-audit-token-foundation/23-01-SUMMARY.md (ujMLJ Hero subtree node IDs as observed at Phase 23 audit)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.3 sub-section-raster sub-rule (LOCK action target identification)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.4 step 4 (Hero side-by-side pairing screenshot target — ujMLJ Hero subtree id)
  </read_first>
  <action>
    Run `mcp__pencil__get_editor_state({ include_schema: false })` + assert active editor.

    Then call `mcp__pencil__batch_get({ nodeIds: ["ujMLJ"], readDepth: 3 })` to retrieve the Home Page frame with depth-3 children.

    From the returned tree, identify Hero-subtree relevant nodes and record in 31-00-SUMMARY.md under `## Task 3 — ujMLJ Hero Subtree Role Map`:

    1. **Hero-section container node** — the direct child of ujMLJ that contains the Hero subtree (NOT ujMLJ itself; one of its 10 group children). Record its node ID + name + dimensions (width × height).

    2. **Per-ID role assignment for the 6 enumerated IDs** (per PEN-INVENTORY line 121: 0veF5 / Wx9kx / 7QsZc / HuBKK / LqPtn / ggx3v). For each:
       - Node ID (confirmed against probe return)
       - Node type (frame / group / text / shape / image)
       - Functional role — one of: `display-headline` (PJS 70 type) / `subtitle` (Inter 18 type) / `primary-cta` / `secondary-cta` / `decorative-anchor` (coral ellipse) / `hero-illustration` (raster) / `content-column-frame` / `right-anchor-frame` / `other`
       - Current text content (if text node) — capture the Crito-source headline + subtitle text for cross-reference (NOT for content authority — Joel's v1.3 content wins per D-157, but this confirms our role assignment is correct)
       - Position / dimensions for layout-target fidelity per D-157

    3. **Hero illustration leaf node** — identify the deepest leaf node (or the raster-bearing leaf) that holds the Crito Hero illustration raster. Record:
       - Node ID
       - Node type (likely image / rect with fill imageRef)
       - Parent node ID (this is the LOCK note attachment target per § 3.3 sub-section-raster sub-rule — the sibling note attaches as a sibling, i.e., child of this parent)
       - Current `enabled` value (expected: `true` — must STAY true post-LOCK per Pitfall 4 + § 3.3; LOCK means a sibling note ONLY, NOT enabled:false)

    4. **Coral ellipse identification** — confirm the decorative coral ellipse (286×225 per PEN-INVENTORY line 129) is present in the Hero subtree:
       - Node ID
       - Fill value (expected: coral literal `#f88d6dff` or similar; token reuse via color-semantic-decorative-coral per Phase 28 D-114 banner candidate)
       - Dimensions verification (286 × 225 or close)
       - Pattern A reconstruction lean per § Discretion: Plan 31-01 reconstructs as Pencil-native ellipse (low cost — single shape node); record this as the Plan 31-01 target.

    5. **Calibration pairing target** — record the exact `nodeId` Plan 31-01 Task 10 calibration gate will pass to `mcp__pencil__get_screenshot({ nodeId: "<HeroSubtreeContainerId>" })` for § 3.4 step 4 Hero side-by-side. This is typically the Hero-section container node from item 1.

    6. **Layout-target fidelity capture** — for D-157 visual-target fidelity, record:
       - Hero section width / height
       - Internal layout direction (horizontal vs vertical — Crito Hero ujMLJ is likely horizontal with content-column left + decorative-anchor right per § Discretion)
       - Key spacing values (gap between content + anchor; padding) for Plan 31-01 inline composition

    DO NOT mutate ujMLJ or any of its descendants. This task is read-only `batch_get` probe. The LOCK action (sibling Pencil note on the Hero illustration parent) is a POST-APPROVE Plan 31-01 Task 11 action, NOT a Plan 31-00 action.
  </action>
  <acceptance_criteria>
    - mcp__pencil__get_editor_state returned active editor ending with Crito.pen BEFORE batch_get
    - mcp__pencil__batch_get({ nodeIds: ["ujMLJ"], readDepth: 3 }) returned a tree
    - 31-00-SUMMARY.md `## Task 3 — ujMLJ Hero Subtree Role Map` contains:
      - Hero-section container node ID + dimensions
      - Per-ID role assignments for at least 6 enumerated IDs from PEN-INVENTORY line 121 (each with type + role + text/value)
      - Hero illustration leaf node ID + parent ID + current enabled value confirmed `true`
      - Coral ellipse node ID + fill value + dimensions confirmed near 286×225
      - Calibration pairing nodeId recorded
      - Layout-target fidelity capture (width / height / direction / gap / padding)
    - Zero mutations to ujMLJ subtree
  </acceptance_criteria>
  <verify>
    Re-run `mcp__pencil__batch_get({ nodeIds: ["ujMLJ"], readDepth: 3 })` — child IDs and structure unchanged.
    grep -E "^[[:space:]]*-[[:space:]]*(0veF5|Wx9kx|7QsZc|HuBKK|LqPtn|ggx3v)" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns at least 6 lines (per-ID role rows).
  </verify>
  <done>
    Plan 31-01 Task 3 (Hero composition) has exact role assignments for layout fidelity; Plan 31-01 Task 10 calibration has exact nodeId for § 3.4 Hero side-by-side; Plan 31-01 Task 11 post-APPROVE LOCK has exact parent nodeId for sibling Pencil note attachment.
  </done>
</task>

<task type="auto">
  <name>Task 4: Baseline regression verification + 31-00-SUMMARY.md commit</name>
  <read_first>
    - .planning/research/PEN-INVENTORY.md (21 component IDs across Primitives / Compounds / Sections + 9 reconstructed page-frame IDs from Phase 26-30; 107 user-facing tokens)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (primitive IDs: M7eUr Button/Default, hIWuC Button/Secondary, nwJk7 Input/Default, j0FxQZ Badge/Default, kJQmJ Badge/Outline, u7NmaS Icon/24 + 16/20/32 variants)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (G0wNOc Section/Header)
    - .planning/phases/25-section-compound-components/25-02-SUMMARY.md (Xs0Hs Section/Footer)
    - .planning/phases/25-section-compound-components/25-03-SUMMARY.md (t40xct Compound/Card)
    - .planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md (Hs5rc Section/CTA + N1jo3i Section/NavBack + b7Hgy FAQ frame + csXky 404 frame)
    - .planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md + 27-01-SUMMARY.md + 27-02-SUMMARY.md (XsDab Thank-you frame + n0QqTd Contact frame + Phase 27 primitives Textarea/Select/Checkbox/CheckboxGroup)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/*-SUMMARY.md (EDAf1 Blog index + aQ8FL Blog Post + Tag frame + Section/TagFilter + Section/RelatedPosts + Card metadata-caption + Badge/Outline)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-*-SUMMARY.md (Y2isa Projects index + cYlRH Project detail + Compound/ProjectCard + Section/ResultsMetrics + Section/RelatedProjects)
    - .planning/phases/30-design-system-reference-reconstruction/30-*-SUMMARY.md (a0gRv Design system frame + Section/TokenSwatchGrid + Section/TypeSpecimen + Section/ComponentShowcase)
  </read_first>
  <action>
    Run `mcp__pencil__get_editor_state({ include_schema: false })` + assert active editor.

    Run `mcp__pencil__get_variables({})` and assert the return contains exactly 107 user-facing tokens (Phase 30 baseline). Record the count in 31-00-SUMMARY.md `## Task 4 — Baseline Regression`. If the count is NOT 107, halt and surface to user — schema/baseline drift since Phase 30 close.

    Construct a `nodeIds` array of all reconstructed-component + reconstructed-page-frame IDs from Phase 23-30 SUMMARYs. Target set:

    **Components (21 expected):**
    - Phase 24 Primitives: M7eUr (Button/Default), hIWuC (Button/Secondary), nwJk7 (Input/Default), j0FxQZ (Badge/Default), u7NmaS (Icon/24) + any other Icon/16/20/32 IDs from 24-05-SUMMARY
    - Phase 25 Sections + Compound: G0wNOc (Section/Header), Xs0Hs (Section/Footer), t40xct (Compound/Card)
    - Phase 26 Sections: Hs5rc (Section/CTA), N1jo3i (Section/NavBack)
    - Phase 27 Primitives + Compound: Textarea variant, Primitive/Select, Primitive/Checkbox, Compound/CheckboxGroup
    - Phase 28 Sections + Compound: Section/TagFilter, Section/RelatedPosts, Card title-slot extension (vGH3A), kJQmJ (Badge/Outline)
    - Phase 29 Compound + Sections: Compound/ProjectCard, Section/ResultsMetrics, Section/RelatedProjects
    - Phase 30 Sections: Section/TokenSwatchGrid, Section/TypeSpecimen, Section/ComponentShowcase

    **Page frames (9 expected — reconstructed; NOT Crito-source):**
    - b7Hgy (FAQ), csXky (404), XsDab (Thank-you), n0QqTd (Contact), EDAf1 (Blog index), aQ8FL (Blog Post), Tag frame from Phase 28, Y2isa (Projects index — visual proxy; the reconstructed Projects index has its own ID), cYlRH (Project detail visual proxy; reconstructed has own ID), a0gRv (Design system)

    NOTE: PEN-INVENTORY is the source of truth for the actual IDs — read PEN-INVENTORY before constructing the list, and use the IDs documented there. If any ID is unclear, batch_get a known-good ID first to confirm shape.

    Call `mcp__pencil__batch_get({ nodeIds: [<full list>], readDepth: 0 })` and verify every requested ID returns a valid node (NOT 404-equivalent). Record:
    - Count of IDs requested
    - Count of IDs returned with valid nodes
    - Any IDs that failed to return (likely a baseline corruption — halt + surface)

    Run `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root. Expected result: `"No layout problems."`. If text-clipping false-positives surface, document them as benign per Pitfall 6 + Phase 24-30 precedent.

    Write final consolidation section in 31-00-SUMMARY.md under `## Task 4 — Baseline Regression`:
    1. Token surface: 107 confirmed (or actual count if drift surfaced)
    2. Component/frame IDs: N/N requested+returned (with any failures listed)
    3. snapshot_layout result (with documented false-positives if any)
    4. **Overall verdict**: BASELINE INTACT → Plan 31-01 cleared to proceed, or BASELINE DRIFT → halt + surface

    Then write the SUMMARY frontmatter at the top of 31-00-SUMMARY.md following the project's summary template (phase / plan / status / completed / subsystem / affects / tech-stack / patterns / key files / decisions). Include:
    - subsystem: design-system (Crito.pen)
    - affects: ["31-01"] (downstream Plan 31-01 consumes T0/T1/T2/T3 outputs)
    - tech-stack.added: [] (zero new tech)
    - tech-stack.removed: []
    - patterns.added: [] (probe-only foundation; reuses Phase 28 D-111 + Phase 30 D-151 + Phase 27 D-101 patterns)
    - key-files: [".planning/research/PEN-INVENTORY.md (unchanged this plan)"]
    - decisions: [] (no new decisions; consumes D-155 through D-167)

    Final SUMMARY conclusion paragraph: state that Phase 31-00 is foundation-only, zero mutations to Crito.pen, library count unchanged (19), token surface unchanged (107), Plan 31-01 cleared with full content + Header override + hIWuC override + ujMLJ Hero map intel ready.
  </action>
  <acceptance_criteria>
    - mcp__pencil__get_variables returned exactly 107 user-facing tokens (or halt + surface logged on mismatch)
    - mcp__pencil__batch_get on the constructed nodeIds array returned valid nodes for every requested ID (or halt + surface on any miss)
    - mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true }) returned "No layout problems." (or text-clipping false-positives documented as benign)
    - 31-00-SUMMARY.md `## Task 4 — Baseline Regression` section contains: token count, component/frame ID hit count, snapshot_layout result, overall verdict (INTACT or DRIFT)
    - 31-00-SUMMARY.md has SUMMARY frontmatter at top following project template
    - 31-00-SUMMARY.md final paragraph confirms Plan 31-01 cleared to proceed
    - File committed to git on a feature branch
    - Zero mutations to Crito.pen across entire Plan 31-00 (verifiable: 0 batch_design calls in plan-execution log)
  </acceptance_criteria>
  <verify>
    test -f .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md
    grep -E "^## Task [0-4]" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns exactly 5 lines (Task 0 / 1 / 2 / 3 / 4)
    grep -c "107" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns at least 1 (token count confirmation)
    grep "BASELINE INTACT" .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md returns at least 1 line (or BASELINE DRIFT documented)
    git log --oneline -1 returns a commit referencing Phase 31-00
  </verify>
  <done>
    Plan 31-01 has all foundation intel: content table (T0) + Header override path (T1) + hIWuC override IDs (T2) + ujMLJ Hero map (T3) + baseline regression evidence (T4). Library + token surface unchanged. Plan 31-00 closes WITHOUT user calibration gate per D-86/D-102/D-124/D-139/D-152/D-165 chain.
  </done>
</task>

</tasks>

<verification>
Overall plan checks at close:
- 31-00-SUMMARY.md exists with 5 dense task sections + frontmatter
- get_variables == 107 (unchanged)
- batch_get on 21 components + 9 page frames returns all valid (baseline intact)
- snapshot_layout returns "No layout problems." (or documented benign false-positives)
- ZERO batch_design calls made (verifiable in plan execution log)
- ZERO new tokens, ZERO new library components, ZERO frame mutations
- No user calibration gate at plan close (D-165 default)
</verification>

<success_criteria>
Phase 31-00 complete when:
- [ ] 31-00-SUMMARY.md committed to git with all 5 task sections populated
- [ ] Task 0 content lookup table contains exact v1.3 strings for Hero/Services/Process/About/ContactSection/Header (verifiable by grep)
- [ ] Task 1 has Path B (default) or Path C decision with exact `descendants: {...}` property-path map for Section/Header (G0wNOc) 4-link override
- [ ] Task 2 has exact `descendants: {...}` property-path map for hIWuC OPEN-25-01 white-stroke + white-label override
- [ ] Task 3 has per-ID role assignments for 6 ujMLJ Hero subtree IDs + Hero illustration leaf node parent ID for LOCK note + coral ellipse ID + calibration pairing nodeId
- [ ] Task 4 confirms baseline intact (107 tokens + 21 components + 9 page frames + snapshot_layout clean)
- [ ] SUMMARY frontmatter populated per project template
- [ ] Plan 31-01 is unblocked with full intel package
</success_criteria>

<output>
After completion, ensure `.planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md` exists and contains all required sections. Commit on a feature branch (per CLAUDE.md git workflow — do NOT commit directly to main).
</output>
