# Phase 31: Homepage Reconstruction - Context

**Gathered:** 2026-06-11
**Status:** Ready for planning

<domain>
## Phase Boundary

A new top-level page frame — `Homepage` — is added to `design/Crito.pen` as an editable composition of Phase 23–30 tokens / primitives / compounds / sections. Joel-shape 5-section narrative backbone (Hero → Services → Process → About → CTA close), Crito-vocab tokens + library components. Final per-page phase in v2.0; closes every remaining cross-phase deferral chain (Header nav 4-link override per Phase 25 D-38 / 26 D-77; OPEN-25-01 Secondary Button white-stroke override on dark-bg Hero CTAs; logo-slot Joel-brand decision).

Branch assignment per CALIBRATION-PROTOCOL.md § 2 branch decision tree:
- **Homepage = HYBRID joel-only-with-Hero-pairing-exception (D-155 NEW sub-branch).** PEN-INVENTORY line 65 reports `Home Page` (`ujMLJ`) as IN-SCOPE with 10 editable group children — the ONLY editable Crito IN-SCOPE source in v2.0 (every prior crito-source phase reconstructed a flat raster). User decision: **topology is Joel-shape**, NOT Crito-shape — Joel's v1.3 5-section narrative is the backbone; Crito's 10-section ujMLJ structure becomes token-mining + vibe reference. This makes the standard § 3 `crito-source-present` branch a structural mismatch by construction (Joel-shape reconstructed frame cannot side-by-side against Crito-shape ujMLJ frame as a whole). Resolution: **default to § 4 joel-only branch protocol** (token-usage check against `_Tokens & Foundations` RpGbe per D-62), **WITH a Hero-section exception** that pairs the reconstructed Hero against ujMLJ Hero subtree per § 3.4 step 4 (Hero is the one section where Crito-shape == Joel-shape per D-157 selection). PAGE-11 status: **LOCK (not hide)** for the Crito Hero illustration raster inside ujMLJ per § 3.3 sub-section-raster sub-rule (raster is one section among many in an editable Crito frame). Other 9 ujMLJ sections (partner-logo strip / "We help to grow" / Dashboard / "Why will you choose" / "How to grow" / Testimonial / "Performance is the key" / "Ready to use our app" / Footer) stay editable in place as token-mining + vibe carry-forward per OPEN-23-04 — NOT reconstructed.

**Phase 31 closes every remaining v2.0 per-page-phase deferral.** After Phase 31 APPROVES, the last placeholder mappings in PEN-INVENTORY Frames Inventory are resolved: line 65 `Home Page` (ujMLJ) status_counts update (Hero illustration LOCK; partial:2 → partial:1 + locked:1); new top-level `Homepage` frame row added. After this, only `04_About` (WDGxc, IN-SCOPE token-mining-only) remains as a Phase 32 milestone-close disposition row.

**Out of scope (already decided):**
- Per-page reconstruction for any other page (Phase 32 milestone-close sweep)
- Mobile breakpoint reconstruction (PAGE-09 — desktop only for v2.0)
- Dark mode (TOKEN-07 + Phase 23 D-01)
- A new CALIBRATION-PROTOCOL revision — D-155 NEW hybrid sub-branch is documented in this CONTEXT, not a protocol amendment (Phase 31 is the only consumer of this sub-branch in v2.0 by construction; Phase 32 sweep evaluates whether it warrants protocol promotion)
- Joel's v1.3 neobrutalist visual chrome (yellow/turquoise/magenta OKLCH palette + Bricolage Grotesque + DM Sans + shadow-to-glow + isometric illustrations + 3-layer button technique + iso-shadow/iso-glow/iso-rotate utilities) — explicitly OUT-OF-SCOPE for v2.0 per PROJECT.md "Joel-brand visual chrome — still planned to be replaced when code milestones run on top of v2.0"
- Joel's v1.3 Hero bento-grid asymmetric layout (D-157 user decision: Crito Hero layout wins over Joel's v1.3 bento)
- Joel's v1.3 isometric Process illustrations (Joel-brand asset; D-159 STUB treatment)
- Joel's v1.3 About headshot (Joel-brand asset; D-160 STUB treatment)
- Joel's v1.3 Services isometric illustrations (Joel-brand asset; Card image-slot STUB)
- Joel's v1.3 8-field ContactSection.astro homepage section (D-161 — Section/CTA close replaces; full form lives at /contact n0QqTd per Phase 27 + sibling note documents v1.3 ContactSection on-homepage status for code milestone)
- Header nav 4-link override propagation to ALL prior-page Header instances (D-162 — override applies to Homepage frame instance ONLY; other reconstructed pages keep Crito-source 6-link defaults per Phase 25 D-77 carry-forward; Phase 32 sweep optionally back-propagates if Joel wants cross-page consistency)
- Joel-brand wordmark / logo asset on Section/Header logo slot (D-163 — Crito-source placeholder stays + sibling Pencil note documents v1.3 "Joel Shinness" wordmark for code milestone; PROJECT.md "Joel-brand visual chrome" Out of Scope honored)
- Crito's 9 non-Hero ujMLJ sections (partner-logo strip / "We help to grow" / Dashboard / "Why will you choose" / "How to grow" / Testimonial / "Performance is the key" / "Ready to use our app" / Footer) as Phase 31 reconstructed sections (D-156 — Joel-shape backbone wins; these stay token-mining-only references in ujMLJ per OPEN-23-04)
- Joel's v1.3 src code (v2.0 milestone scope — `.pen` file only)
- New Section components inventing Joel-specific patterns (D-158 + D-159 inline composition; Pitfall O5/O6 single-consumer rule + Phase 26 D-79 / Phase 28 D-116 / Phase 29 D-134 narrow-scoping carry-forward); EXCEPTION already shipped by Phase 30 D-149 for showcase patterns

</domain>

<decisions>
## Implementation Decisions

### Section Topology — Joel-Shape, Crito-Vocab (Backbone Architecture)

- **D-155:** **HYBRID joel-only-with-Hero-pairing-exception calibration sub-branch.** Phase 31 introduces a NEW calibration sub-branch — Phase 31 is the FIRST and ONLY v2.0 phase where the Crito source frame is structurally editable (every prior crito-source phase reconstructed a flat raster). User decision per Area 1 Q1 picks **Joel-shape, Crito-vocab** topology, which makes the standard § 3 crito-source-present whole-frame side-by-side a structural mismatch by construction (Joel's 5-section reconstructed frame cannot pair against Crito's 10-section ujMLJ frame as a whole). Resolution: default to § 4 joel-only branch (token-usage check vs RpGbe per D-62), with a Hero-section exception that pairs the reconstructed Hero section against ujMLJ Hero subtree per § 3.4 step 4. Plan 31-01 calibration AskUserQuestion gate carries BOTH protocol artifacts: § 4.5 joel-only token-usage description + § 3.4 step 5 Hero side-by-side image pair. PAGE-11 status: **LOCK (sibling Pencil note)** for the ujMLJ Hero illustration raster per § 3.3 sub-section-raster sub-rule — raster stays in place for cross-reference because the source frame as a whole is editable, NOT hidden via enabled:false. Phase 32 sweep evaluates whether this hybrid sub-branch warrants CALIBRATION-PROTOCOL.md protocol promotion as a third codified branch alongside crito-source + joel-only — for now it's a Phase 31-only mechanism documented in this CONTEXT.

- **D-156:** **Joel-shape 5-section backbone, Crito-vocab tokens + library.** Per Area 1 Q1 user selection — Joel's v1.3 5-section narrative (Hero / Services / Process / About / closing) is the page topology. Crito's 10-section ujMLJ structure becomes (a) token-mining reference (already mined in Phase 23 — heading-1 + body sizes + Plus Jakarta Sans + Inter), (b) vibe carry-forward for sectional cadence + dark-bg/light-bg rhythm, and (c) Hero-section structural reference (the one section where Crito and Joel align — see D-157). The other 9 ujMLJ sections (partner-logo strip / "We help to grow" / Dashboard / "Why will you choose" / "How to grow" / Testimonial / "Performance is the key" / "Ready to use our app" / Footer) are NOT reconstructed as Phase 31 sections — they remain editable in place inside ujMLJ as token-mining surface per OPEN-23-04 carry-forward. Honors Phase 29 D-129 "Crito-shape Joel-content" SPIRIT (visual fidelity to Crito where it applies) but adapts the LETTER to the editable-source case where Joel's narrative is well-established (v1.3 ships 5 sections + ContactSection per `src/pages/index.astro` lines 11-15). v1.3 content authority dominates (matches D-82/D-98/D-123 verbatim-content extraction chain).

### Hero Section

- **D-157:** **Crito Hero layout (ujMLJ Hero subtree), Joel content.** Per Area 1 Q2 user selection — Hero structure follows Crito's `0veF5 / Wx9kx / 7QsZc / HuBKK / LqPtn / ggx3v` subtree pattern: large display-tier headline (Plus Jakarta Sans 70/700 → `type-semantic-display` token + `type-primitive-size-70` per Phase 23 23-03-SUMMARY mining evidence) + subtitle (Inter 18 opacity 0.7 → `type-semantic-body-sm` or `type-semantic-prose-paragraph` per token reuse) + DUAL CTAs (Primary `Button/Default` M7eUr + Secondary `Button/Secondary` hIWuC) + decorative shape anchor (coral ellipse 286×225 per PEN-INVENTORY line 129 token-mining evidence; STUB visual rendering). Hero content authority: v1.3 verbatim per D-82/D-98/D-123 chain — H1 from `src/components/Hero.astro` lines 71-73 + 91-93 ("Your business needs more than software, it needs..."), subtitle from v1.3 bento `cta-subtext` ("Ready to get started?"), Primary CTA label "Let's talk!" (v1.3 `cta-text` line 80 + 117), Secondary CTA label TBD at plan-execution (default candidates: "See how I work" / "View my projects"; Plan 31-01 surfaces user gate IF dual-CTA copy needs Joel input). **Triggers OPEN-25-01 resolution at instance time:** Crito Hero has dark navy bg `#141f39ff` (color-semantic-bg-dark token); Secondary Button hIWuC default is navy-stroke + navy-label for light-bg consumer surfaces (Phase 25 D-43); Phase 31 Hero CTA descendants override per Phase 25 OPEN-25-01 carve-out: `{strokeId:{stroke:"#ffffffff"},labelId:{fill:"#ffffffff"}}` to restore Crito-literal whites on dark-bg. Plan 31-00 foundation probes the descendants-override path; Plan 31-01 instances. **Hero illustration treatment:** v1.3 ships no Hero illustration (bento outcomes are the visual); Crito Hero ships decorative coral ellipse + side illustration. Plan 31-01 STUB Hero illustration as image-fill rectangle with sibling Pencil note documenting Crito source + Joel-asset-pending for code milestone. **Hero is the calibration pairing target per D-155** — Plan 31-01 calibration gate inline-renders both the reconstructed Hero (Pencil get_screenshot) AND ujMLJ Hero subtree screenshot for side-by-side per § 3.4 step 4.

### Services Section

- **D-158:** **3-column Compound/Card grid, inline composition (no new section component).** Per Area 1 Q3 user selection. 3 Compound/Card (t40xct) instances in a 3-up grid with horizontal-layout per Phase 28 D-114 / Phase 29 D-114 grid pattern. Section H2 "Solutions" or "Services" (Plan 31-01 plan-execution selects from v1.3 wording per `src/components/Services.astro` content extraction at Plan 31-00 Task 0) bound to `type-semantic-heading-1` per Phase 23 + Phase 26 D-72 chain (size 48 — matches Crito section heading size per PEN-INVENTORY line 170 mining evidence). Card descendants overrides per category: image-slot STUB (Joel-brand isometric illustrations OUT-OF-SCOPE per PROJECT.md; image-fill rectangle placeholder + sibling Pencil note documenting v1.3 illustration source), title-slot = category name (e.g., "AI Development" / "Automation" / "Web Apps" per v1.3 Services.astro), body-slot = category description (v1.3 verbatim), footer-actions-slot = Secondary Button "Learn more" or single link (Plan 31-01 plan-execution decides; default Secondary). NO new Section component per Pitfall O5/O6 single-consumer rule + Phase 26 D-79 / Phase 28 D-116 / Phase 29 D-134 narrow-scoping precedent (Services is a Homepage-only consumer; Card is already the canonical 3-up container across Phase 28 BlogCard + Phase 29 ProjectCard + Phase 30 Card gallery). Per-section fidelity label: layout EXACT, illustrations STUB.

### Process Section

- **D-159:** **5-step vertical timeline, inline composition (no new section component).** Per Area 1 Q4 user selection. Vertical-stack of 5 step blocks under section H2 "How we work" or "Our process" (Plan 31-01 plan-execution selects from v1.3 wording per `src/components/Process.astro` content extraction at Plan 31-00 Task 0). Each step block: number marker + step title + step body (3-part vertical or 2-column horizontal — Plan 31-01 plan-execution decides default vertical for clean linear scroll; horizontal-grid 2x3 acceptable if visual density warrants — single calibration AskUserQuestion at plan close covers either path). Step content v1.3 verbatim per D-98 chain (Discovery / Design / Build / Test / Launch headings + 1-2-sentence body per `Process.astro`). Step illustrations STUB — v1.3 ships 5 isometric illustrations (`@lucide/astro` icon-based per v1.2); Joel-brand asset OUT-OF-SCOPE for v2.0. Step image-slot = STUB image-fill rectangle + sibling Pencil note documenting v1.3 isometric pattern for code milestone. NO new Section component per Pitfall O5/O6 + narrow-scoping chain (Process is a Homepage-only consumer). Per-section fidelity label: layout EXACT, illustrations STUB.

### About Section

- **D-160:** **2-column About — headshot + bio + CTA, inline composition (no new section component).** Per Area 1 Q4 user selection. 2-column horizontal-layout: left column = headshot image-slot (Crito-vocab Card-like frame or image-fill rectangle; Joel headshot STUB — Joel-brand asset OUT-OF-SCOPE), right column = H2 "About" + body paragraph + Primary Button "Let's talk" or "Get in touch" (link target /contact per Phase 27 n0QqTd). H2 + body content v1.3 verbatim per D-98 chain (from `src/components/About.astro` content extraction at Plan 31-00 Task 0). NO new Section component per Pitfall O5/O6 + narrow-scoping (About is a Homepage-only consumer; only About-on-homepage page per PROJECT.md key decision "Homepage About section — single-page feel, no separate page"). Per-section fidelity label: layout EXACT, headshot STUB. Honors PROJECT.md Key Decision row "Homepage About section — single-page feel, no separate page" + carries forward as v2.0 Pencil composition.

### Bottom Close — Section/CTA

- **D-161:** **Section/CTA Hs5rc instance at bottom; full ContactSection deferred to /contact route.** Per Area 1 Q5 user selection. Instance Phase 26 Section/CTA (Hs5rc — Phase 26 D-78 broad-scoping component; Plan 31-01 is the SECOND cross-phase consumer after Phase 27 Plan 27-01 Thank-you) at the bottom of the Homepage frame. Descendants overrides: headline-slot = "Ready to get started?" or "Let's talk" (Plan 31-01 plan-execution selects from v1.3 ContactSection.astro content; default "Ready to get started?" per Hero CTA copy carry-forward per D-157), body-slot = brief lead-magnet line (v1.3 verbatim per D-98), actions-slot = Primary Button "Let's talk" linking to /contact (Phase 27 n0QqTd). **Sibling Pencil note on Section/CTA Homepage instance** per D-93 belt-and-suspenders chain: "v1.3 `src/components/homepage/ContactSection.astro` ships full 8-field form on-homepage as `#contact` anchor section; code milestone decides whether to extract to /contact route (matching Phase 27 n0QqTd reconstructed Contact frame) OR keep as homepage section." Honors Phase 27 PEN-INVENTORY note about code-milestone-time architecture decision. v1.3 8-field form NOT instanced on Homepage frame — single-source-of-truth lives at /contact frame n0QqTd (Phase 27); homepage offers marketing close + CTA hand-off. Per-section fidelity label: EXACT layout, EXACT content (Section/CTA is fully-factored library component from Phase 26).

### Joel-Brand Chrome — Header + Logo

- **D-162:** **Header nav 4-link override applied to Phase 31 Homepage instance ONLY via descendants overrides.** Per Area 2 Q1 user selection. Phase 25 D-38 + Phase 26 D-77 + Phase 27 + Phase 28 + Phase 29 + Phase 30 carry-forward deferral chain CLOSES here. Section/Header (G0wNOc inside g9oRa5) currently ships 6 Crito-source nav labels (Home / About / Services / Blog / FAQ / Contact); Joel's v1.3 nav per `src/components/layout/Header.astro` is 4 links (Blog / Projects / FAQ / Contact). Phase 31 Homepage Section/Header instance applies descendants override: hide "Home" + "About" + "Services" nav-item children via `{<homeNavItemId>:{enabled:false},<aboutNavItemId>:{enabled:false},<servicesNavItemId>:{enabled:false}}`, leave "Blog" + "FAQ" + "Contact" labels intact, add "Projects" nav-item child via Insert sub-operation (or replace one of the hidden slots' text). Plan 31-00 foundation probes the override path (descendants Update on a Phase 25 component instance was tested at Phase 28 D-111 TagFilter active-pill override — pattern proven). **Scope of override: Homepage frame instance ONLY.** Other already-reconstructed pages (FAQ b7Hgy / 404 csXky / Thank-you XsDab / Contact n0QqTd / Blog EDAf1 / Blog Post aQ8FL / Tag XXX / Projects SCcln / Project s5k41l / Design system a0gRv) retain Phase 25 Crito-source 6-link defaults via Phase 25 D-77 carry-forward. Acknowledges that cross-page Header nav consistency is a code-milestone-time decision: code milestone wires the real 4 links at runtime via component-level configuration. Phase 32 milestone-close sweep may optionally back-propagate the override to other pages if Joel wants Pencil-side consistency before code milestone (NOT default — adds 9 frames of descendants-override surface; deferred to Phase 32 Joel-decision gate IF surfaced).

- **D-163:** **Logo slot retains Crito-source placeholder + sibling Pencil note for code milestone.** Per Area 2 Q2 user selection. Section/Header (G0wNOc) logo slot currently renders Crito-source logo placeholder (Phase 25 D-40 deferred Joel-brand logo). Phase 31 Homepage Section/Header instance DOES NOT override the logo slot — Crito placeholder stays as carry-forward. **Sibling Pencil note on Phase 31 Homepage Section/Header instance** per D-93 belt-and-suspenders chain: "v1.3 `src/components/layout/Header.astro` ships 'Joel Shinness' wordmark in logo slot (Bricolage Grotesque heading family + uppercase + neobrutalist styling); code milestone wires Joel-brand wordmark + Joel-brand fonts at runtime. v2.0 .pen retains Crito-source logo placeholder per Phase 25 D-40 + PROJECT.md 'Joel-brand visual chrome — replaced when code milestones run on top of v2.0'." Honors PROJECT.md Out of Scope ("Joel-brand visual chrome") + Phase 25 D-40 deferral discipline. NO Joel-brand asset introduction.

### Plan Structure + Calibration

- **D-164:** **Phase 31 ships 2 plans default: foundation-first per chain.** Mirrors Phase 27 D-101 + Phase 28 D-122 + Phase 29 D-138 + Phase 30 D-151 foundation-first chain. Per Area 4 Q1 user selection.
  - **Plan 31-00 — Foundation (no user gate at close per D-86 + D-102 + D-124 + D-139 + D-152 chain):**
    - **Task 0:** Content extraction from `src/components/Hero.astro` + `src/components/Services.astro` + `src/components/Process.astro` + `src/components/About.astro` + `src/components/homepage/ContactSection.astro` for v1.3 verbatim content sourcing per D-98 chain
    - **Task 1:** Probe Phase 25 Section/Header (G0wNOc inside g9oRa5) descendants-override path for 4-link nav (D-162) — confirms Update sub-operation succeeds on nav-item children's `enabled` property; pattern reuse from Phase 28 D-111 TagFilter active-pill descendants override (proven). Probe outcome documented in 31-00-SUMMARY.md.
    - **Task 2:** Probe Phase 24 Primitive/Button/Secondary (hIWuC) descendants-override path for OPEN-25-01 white-stroke + white-label override on dark-bg Hero CTA per Phase 25 OPEN-25-01 carve-out. Confirms `{strokeId:{stroke:"#ffffffff"},labelId:{fill:"#ffffffff"}}` descendants override is structurally valid on hIWuC component. Probe outcome documented in 31-00-SUMMARY.md.
    - **Task 3:** Probe ujMLJ Hero subtree structure via `batch_get(nodeIds:["ujMLJ"], readDepth:3)` to confirm Hero subtree element IDs (0veF5 / Wx9kx / 7QsZc / HuBKK / LqPtn / ggx3v per PEN-INVENTORY line 121 enumeration) + measure key spacing/sizing for D-157 visual-target fidelity. Probe outcome documented in 31-00-SUMMARY.md.
    - **Possible new tokens** per mid-plan gate per D-72 / D-106 / D-135 / D-151 precedent (Phase 31 likely consumes existing token surface from Phase 23 + 26 + 28 — Plus Jakarta Sans 70 already in heading-1 + new heading-3/-4 from Phase 28; Inter 18 already in primitives; coral decorative + navy dark-bg in color-semantic). Default: 0 new tokens.
    - **Section/SecondaryLink consideration:** Phase 27 D-99 + Phase 29 D-133 are 2 prior consumers of the inline-text-link-with-iconLeading pattern (Thank-you "Return to homepage" + Project detail back-nav). Phase 30 D-145 noted "if Phase 31 Homepage adds a third, factor then per emerging-consumer rule." Plan 31-00 Task 0 evaluates Joel's v1.3 surface — v1.3 Hero doesn't ship a secondary text link; Services may not need one (footer-actions-slot Secondary Button suffices). Default: DEFER Section/SecondaryLink factoring to a future phase or milestone. Plan-execution mid-plan gate IF a third consumer surfaces in v1.3 audit.
    - **NO new Section component for Services / Process / About / closing-CTA** per D-158 + D-159 + D-160 + D-161 narrow-scoping carry-forward chain. Plan 31-00 ships ZERO new library components; foundation work is purely probe-and-prep.
    - Library count unchanged: 19 → 19 (Phase 30 baseline).
    - Token surface unchanged default: 107 → 107.
  - **Plan 31-01 — `Homepage` page-frame composition + calibration (single user gate at close per D-155 hybrid joel-only-with-Hero-pairing-exception):** top-level `Homepage` frame at 1440 width via `find_empty_space_on_canvas` with `nodeId: a0gRv` (Phase 30 Design system frame) anchor per CALIBRATION-PROTOCOL § 10.4 nodeId-anchor pattern. Page-frame `layout: vertical`, `gap: 0`, `padding: 0`, `alignItems: center`, `fill: color-semantic-bg-page` (Crito-vocab default `#ffffffff` per D-58 carry-forward). Content-width 1200 sub-frames center at 1440 per Phase 26-30 chain convention. Instances:
    - Section/Header (G0wNOc) WITH descendants override per D-162 (4-link nav: Blog / Projects / FAQ / Contact; hide Home + About + Services) + sibling Pencil note re logo slot per D-163
    - Hero section per D-157 (inline composition — display-headline + subtitle + dual CTAs primary+Secondary with OPEN-25-01 white-stroke descendants override + decorative shape STUB)
    - Services section per D-158 (inline composition — section H2 + 3-Card horizontal grid; Card image-slot STUB, title/body/footer-actions overrides per v1.3 Services.astro extraction)
    - Process section per D-159 (inline composition — section H2 + 5-step vertical timeline; step image-slot STUB)
    - About section per D-160 (inline composition — section H2 + 2-column headshot-left/bio-right with Primary CTA; headshot STUB)
    - Section/CTA (Hs5rc) instance per D-161 (descendants overrides for headline + body + Primary Button; sibling note re v1.3 ContactSection on-homepage architecture for code milestone)
    - Section/Footer (Xs0Hs) no override per D-77 carry-forward
    - **Plan-close calibration gate** per D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch:
      - § 4.5 joel-only token-usage description format covers ALL sections (token-binding check vs `_Tokens & Foundations` RpGbe per D-62 — confirms heading-1 + prose-paragraph + display + body + Card + Button + Section components bind correctly)
      - § 3.4 step 4 Hero side-by-side image pair: `get_screenshot(<Hero-section-id>)` paired against `get_screenshot(<ujMLJ Hero subtree id>)` for visual fidelity check
      - PAGE-11 LOCK action per § 3.3 sub-section-raster sub-rule: on APPROVE → sibling Pencil note added on ujMLJ Hero illustration raster marking it "LOCKED — cross-reference for Phase 31 Homepage Hero per D-155"; raster NOT hidden (enabled stays true)
      - Description identifier conventions per § 3.5 (crito-source Hero pairing) + § 4.6 (joel-only token-usage): `31-homepage-hero--side-by-side.png` + `31-homepage--token-usage.png` — IDENTIFIERS ONLY, NOT disk-write paths (OPEN-23-01 substitution)
    - **PEN-INVENTORY status_counts** for ujMLJ updates: `partial:2 → partial:1, locked:1` (or `factored:8, partial:2 → factored:8, partial:1, locked:1`) per Phase 31 Plan 31-01 close — schema TBD at plan-execution per PEN-INVENTORY extension pattern per D-154 carry-forward.
    - **NEW `Homepage` frame row** added to PEN-INVENTORY Frames Inventory per D-154 carry-forward: `Homepage | <frameId> | reconstructed-PHASE-31 | Homepage (per Plan 31-01) | 6 | factored:6 | n/a (Phase 31 plan 31-01 — reconstructed; FIRST D-155 HYBRID joel-only-with-Hero-pairing-exception branch use in v2.0; LAST per-page reconstruction; Header descendants-override 4-link nav per D-162; SECOND cross-phase consumer of Section/CTA Hs5rc per D-161 + D-78 — first was Phase 27 Plan 27-01 Thank-you; OPEN-25-01 RESOLVED via Hero Secondary Button white-stroke descendants override per D-157)`.

- **D-165:** **Per-plan calibration gates — single AskUserQuestion at plan close per D-155 HYBRID sub-branch.**
  - **Plan 31-00 closes WITHOUT a user gate** at default — pure foundation probe work (no library or page-frame mutations). Mid-plan gate ONLY if (a) token escalation surfaces per D-72/D-106/D-135 precedent (default: NO new tokens — existing surface sufficient), (b) Section/SecondaryLink third consumer surfaces in v1.3 audit per Phase 30 D-145 reference (default: DEFER), or (c) Hero illustration content authority surfaces a decision (Crito-vocab vs Joel-vibe STUB style at illustration-slot level).
  - **Plan 31-01 closes WITH a single calibration gate** per D-155 HYBRID:
    - § 4.5 joel-only token-usage description format header
    - § 3.4 step 5 Hero side-by-side inline image pair (reconstructed Hero vs ujMLJ Hero subtree)
    - Per-section fidelity proposals: Hero EXACT (token bindings) + APPROXIMATE (Crito-source Hero subtree pairing — declared decorative-shape STUB + illustration STUB deviations); Services EXACT layout + STUB illustrations; Process EXACT layout + STUB illustrations; About EXACT layout + STUB headshot; closing-CTA EXACT (Section/CTA fully-factored library component); Header overridden-instance EXACT (4-link descendants override); Footer EXACT (no override)
    - **NO MORE THAN ONE gate per plan** per Phase 26 D-65 / Phase 27 D-102 / Phase 28 D-124 / Phase 29 D-139 / Phase 30 D-152 chain — even with hybrid sub-branch, the gate description carries both protocol artifacts in a single AskUserQuestion.

### Cross-Cutting / Carry-Forward

- **D-166:** **Pre-flight `mcp__pencil__get_editor_state` enforcement carries forward** from Phase 23 OPEN-23-14 → Phase 24 D-35 → Phase 25 D-54 → Phase 26 D-87 → Phase 27 D-103 → Phase 28 D-125 → Phase 29 D-140 → Phase 30 D-153. Every Phase 31 plan that calls `batch_design`, `find_empty_space_on_canvas`, or any Pencil-mutating tool first calls `mcp__pencil__get_editor_state({ include_schema: false })` and asserts active editor == `design/Crito.pen`. Halt and surface to user on mismatch. No exceptions.

- **D-167:** **PEN-INVENTORY extension pattern carries forward** from Phase 23 D-18 / Phase 24 D-23 / Phase 25 D-56 / Phase 26 D-88 / Phase 27 D-104 / Phase 28 D-126 / Phase 29 D-141 / Phase 30 D-154. Phase 31 adds:
  - **NEW `Homepage` frame row** per D-164 Plan 31-01 close (with frame ID from `find_empty_space_on_canvas` + `batch_design` returns)
  - **UPDATE to `Home Page` (ujMLJ) line 65** per D-155 LOCK action: `status_counts: flat:0, partial:2, factored:8` → `status_counts: flat:0, partial:1, factored:8, locked:1` (Hero illustration locked + sibling note added; remaining partial section count adjusted per plan-execution audit); reconstruction_priority `high` → `n/a (Phase 31 Plan 31-01 — Hero subtree consumed as visual pairing target via D-155 HYBRID; remaining 9 sections retained as token-mining only per OPEN-23-04 carry-forward)`
  - **Variant Evidence rows for Phase 31** covering: Hero descendants override on Primitive/Button/Secondary (hIWuC) per OPEN-25-01 resolution + D-157; Section/Header descendants override for 4-link nav per D-162; Section/CTA Hs5rc instance as SECOND cross-phase consumer per D-161 (first was Phase 27 Plan 27-01 Thank-you); possible Section/SecondaryLink emerging-consumer evaluation per D-164 Plan 31-00 (default DEFER)
  - **New section `## Open Flags — Phase 31 (OPEN-31-NN)`** populated by D-159 Process illustration STUB + D-160 About headshot STUB + D-158 Services illustration STUB labeling + any plan-surfaced flags (likely candidates: OPEN-23-09 Poppins revisit per Phase 23 OPEN-23-09 "Phase 31 Homepage rebuild may revisit if Crito visual fidelity needs Poppins" — default position: NO Poppins introduction per Crito-vocab-utilitarian D-156 + Joel-shape lean; Plan 31-00 Task 0 v1.3 audit confirms Joel doesn't need Poppins; flag stays unchanged)
  - **Updates to:** OPEN-25-01 RESOLVED via Plan 31-01 Hero Secondary Button white-stroke + white-label descendants override per D-157; OPEN-25-07 final consumer count (Phase 31 Services instances 3 Compound/Card refs — Card consumer count: Phase 27 sidebar 1 + Phase 28 RelatedPosts 3 + Phase 29 ProjectCard sibling-component pattern via Compound/ProjectCard + Phase 30 Card gallery 1 + Phase 31 Services 3 = total cross-phase Card consumers documented for Phase 32 close); OPEN-23-09 Poppins revisit RESOLVED via Plan 31-00 Task 0 audit (Joel doesn't need Poppins per Crito-vocab + Joel-shape lean — Plus Jakarta Sans + Inter sufficient); OPEN-23-04 LAST-VISIT (Phase 31 is the final per-page consumer of Home Page token-mining context; partial-resolution chain closes)
  - **PAGE-11 status:** LOCK (sibling note, raster NOT hidden) per § 3.3 sub-section-raster sub-rule for ujMLJ Hero illustration

### Claude's Discretion

- **Auto-layout vs absolute positioning at the page-frame level** — Phase 26/27/28/29/30 carry-forward: auto-layout vertical-stack at page level. Phase 31 inherits.
- **Hero section internal layout** — Crito ujMLJ Hero subtree pattern: horizontal-layout (content-column left + decorative-anchor right) OR vertical-stack (centered display + subtitle + CTAs above a hero illustration). Plan 31-01 plan-execution defaults to Crito source-evidenced default after Plan 31-00 Task 3 probe; if probe surfaces multiple Hero layouts in ujMLJ subtree, mid-plan gate per D-72 chain.
- **Hero Secondary Button label** — D-157 leaves "TBD at plan-execution" with default candidates ("See how I work" / "View my projects"). Plan 31-01 plan-execution selects from Plan 31-00 Task 0 v1.3 audit; default lean: link to Projects index (Phase 29 SCcln) since that's the strongest secondary destination for a lead-gen homepage. Mid-plan gate IF Joel input required on copy.
- **Services H2 wording** — D-158 leaves "Solutions" or "Services" decision to plan-execution. Default: v1.3 verbatim per `Services.astro` content extraction at Plan 31-00 Task 0. Likely "Solutions" or "What I do" per v1.3 narrative.
- **Process section H2 wording + step orientation** — D-159 leaves vertical-stack (default) vs horizontal-grid 2x3 (alternative) decision to plan-execution per visual density evaluation. Default: vertical-stack for clean linear scroll; horizontal-grid acceptable if visual density warrants.
- **About bio length + CTA copy** — D-160 leaves bio paragraph length to v1.3 verbatim per `About.astro` extraction (Plan 31-00 Task 0); CTA copy "Let's talk" or "Get in touch" default candidates; Plan 31-01 plan-execution selects.
- **Closing-CTA copy** — D-161 leaves headline + body + button label to plan-execution; default candidates per v1.3 ContactSection + Hero "Let's talk!" CTA copy carry-forward.
- **Header descendants-override path for nav items** — D-162 specifies HIDE Home + About + Services + ADD Projects. Plan 31-00 Task 1 probe confirms whether Add via Insert sub-operation works at descendants-override level, OR whether the cleaner path is to REPLACE one hidden slot's text with "Projects" (in-place type override). Default: probe outcome decides.
- **OPEN-25-01 white-stroke descendants-override exact properties** — D-157 specifies `{strokeId:{stroke:"#ffffffff"},labelId:{fill:"#ffffffff"}}`. Plan 31-00 Task 2 probe identifies the actual hIWuC child IDs for stroke + label; substitution at instance time.
- **Crito Hero decorative shape treatment** — Crito Hero has coral ellipse (286×225) + Hero illustration (likely partially-flat). Plan 31-01 plan-execution decides whether to reconstruct the coral ellipse as a Pencil-native ellipse shape (color-semantic-decorative-coral token reuse — Phase 28 D-114 banner used this) OR STUB as a sibling note. Default: Pencil-native ellipse for visual fidelity (low cost — single shape node).
- **About headshot vs illustration vs decorative-image-slot** — D-160 specifies image-slot STUB. Plan 31-01 plan-execution decides STUB visual: gradient-fill rectangle (visually neutral) vs solid-fill rectangle (placeholder-honest) vs image-placeholder-with-camera-icon (semantic STUB). Default: solid-fill rectangle with sibling Pencil note documenting Joel-headshot-pending status.
- **Snapshot_layout discipline at plan close** — matches Phase 24/25/26/27/28/29/30 chain. Default: yes, runs at every plan close, documented in 31-NN-SUMMARY.md with text-clipping false-positive caveat per phase precedent (Plan 31-01 likely fewer instances than Plan 30-01's ~58 but more SECTIONS than any prior plan — text-clipping likely on Hero headline + Services Card bodies + Process step bodies + About bio).
- **Cross-row stale-cache `get_screenshot` quirk on the new page frame** — CALIBRATION-PROTOCOL § 6.4 Tier-1/Tier-2 workarounds available; Phase 31 inherits fallback path per Phase 26-30 production precedent.
- **Spacing rhythm between sections** — Phase 26-30 chain uses `space-semantic-section-y` token. Phase 31 inherits as default; plan-execution may surface `space-semantic-section-y-large` (or similar) if Homepage cadence warrants more breathing room than utilitarian-docs Phase 30 — mid-plan gate per D-72 chain IF surfaced.
- **Plan 31-01 calibration gate description ordering** — D-155 HYBRID carries 2 protocol artifacts (§ 4.5 token-usage + § 3.4 Hero side-by-side). Plan 31-01 plan-execution decides description ordering: section-by-section (top-to-bottom Header → Hero → Services → Process → About → close → Footer) OR protocol-by-protocol (token-usage block first, Hero side-by-side second). Default: section-by-section for visual scanning ease.
- **Cross-phase consistency call-out at calibration gate** — Plan 31-01 description should call out the cross-page consistency proof per ROADMAP success criterion 3 (Phase 30 was the first; Phase 31 is the second user-facing consumer of Section/Header + Section/Footer). User can spot regression vs Phase 30 Design system frame.
- **PEN-INVENTORY locked vs hidden vs partial status_counts schema choice** — D-167 leaves the actual status_counts key choice ambiguous (`partial:2` becomes `partial:1, locked:1` OR `partial:2, locked:1` per how PEN-INVENTORY § 2 audit logic treats LOCK semantics). Plan 31-01 plan-execution decides at PEN-INVENTORY extension write-time per Phase 32 sweep readability — default lean: add `locked:N` as a new status_counts key + document in PEN-INVENTORY § 2 status_counts logic block.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase scope + requirements (must-read)
- `.planning/PROJECT.md` — v2.0 milestone goal, v1.4 abandonment context, single-file strategy, desktop-only scope, "content untouched" boundary, key decision "Homepage About section — single-page feel, no separate page" (informs D-160), key decisions "OKLCH color system / shadow-to-glow / 3-layer button technique / Bricolage Grotesque / DM Sans / isometric illustrations" Out-of-Scope for v2.0 (informs D-157 + D-158 + D-159 + D-160 STUB treatments + D-163 logo deferral).
- `.planning/REQUIREMENTS.md` — Phase 31 requirements: PAGE-01 (Homepage frame reconstructed — zero `[FLAT]` markers across hero / services / process / about / any other sections), PAGE-09 (desktop-only), PAGE-11 (LOCK sub-section-raster sub-rule per § 3.3 — Hero illustration inside editable ujMLJ retained as cross-reference, NOT hidden), VALID-01 (per-section fidelity labels — D-83 carry-forward), VALID-02 (calibration artifact — D-155 HYBRID branch-redefined), VALID-03 (gap declaration — OPEN-31-NN flags).
- `.planning/ROADMAP.md` § "Phase 31" — Goal, Depends on (Phase 30), Success Criteria. **Critical reading of success criteria 3:** "The reconstructed homepage uses `Section / Header` and `Section / Footer` instances at top and bottom (proving cross-page consistency with Phase 30's design-system page)" — D-162 Header descendants override per D-162 SCOPES the override to Homepage instance only, NOT to the Section/Header component itself, preserving cross-page consistency proof. **Critical reading of success criteria 4:** "highest scrutiny because this is the most-trafficked surface" — D-155 HYBRID calibration with Hero side-by-side pairing exception elevates Hero-section calibration depth beyond the joel-only-baseline.
- `.planning/STATE.md` — current position: Phase 30 COMPLETE (2026-06-10), Phase 31 ready to plan, Phase 31 is the LAST per-page reconstruction phase before milestone close (Phase 32).

### CALIBRATION-PROTOCOL.md (definition-of-done framework — must-read at every per-page plan)
- `.planning/research/CALIBRATION-PROTOCOL.md` — Phase 26 Plan 26-03 codify-what-worked.
  - **§ 2 Branch Decision Tree** — Phase 31 introduces D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch (not in the original tree; documented in this CONTEXT only — Phase 32 sweep evaluates protocol promotion). PEN-INVENTORY `Home Page` line 65 is IN-SCOPE with 10 editable group children — UNIQUE among v2.0 IN-SCOPE pages (every prior was flat raster).
  - **§ 3.3 PAGE-11 status (crito-source) sub-section-raster sub-rule** — "If the raster is one section among many in an editable Crito frame (e.g., a Hero illustration inside `ujMLJ`), **lock** (mark with a sibling Pencil note) and leave in place for cross-reference." This is the literal Phase 31 case — Phase 31 D-155 inherits LOCK action for ujMLJ Hero illustration raster post-APPROVE.
  - **§ 3.4 Per-step script (crito-source branch) step 4** — Hero-section side-by-side pairing target: `get_screenshot(<reconstructed-Hero-id>)` + `get_screenshot(<ujMLJ Hero subtree id>)`. Inherited as Hero-section calibration artifact per D-155 HYBRID exception.
  - **§ 4 joel-only branch protocol** — § 4.1 VALID-02 (token-usage check against `_Tokens & Foundations` RpGbe); § 4.2 VALID-01 fidelity definitions (EXACT token bindings; APPROXIMATE Crito-source Hero pairing; STUB Joel-brand asset placeholders — illustrations + headshot); § 4.4 per-step script default for non-Hero sections; § 4.5 AskUserQuestion format default for token-usage gate; § 4.6 description identifier convention.
  - **§ 6.4 OPEN-26-02 stale-cache workaround tiering** — Tier-1 (cross-row Update) + Tier-2 (user editor verification fallback). Phase 31 page frame moderate height (5-section + Header + Footer ≈ 7 children); lower stale-cache risk than Phase 30's ~58-instance density; Tier-1 expected sufficient; Tier-2 fallback inherited.
  - **§ 10.4 FindEmptySpace `nodeId` anchor pattern** — Plan 31-01 anchors on `nodeId: a0gRv` (Phase 30 Design system frame). Without anchor, FindEmptySpace may pick library-row Y; with anchor, placement falls on page-frame row y ≈ −4111.

### Phase 30 carry-forward (Phase 31 inherits these decisions directly)
- `.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md` — Phase 30 decisions D-142 through D-154. Especially **D-145** (Section/SidebarNav DROPPED + sibling note for code milestone — Phase 31 inherits "no sticky sidebar" architecture for Homepage; sibling note pattern for code-milestone documentation), **D-148** (full v2.0 library coverage in Phase 30 Component gallery — confirms what's available for Phase 31 to instance: 4 primitives + 4 compounds + 8 sections, all proven cross-page consumable), **D-149** (intentional departure from narrow-scoping for showcase patterns — Phase 31 does NOT depart, narrow-scoping carries forward per Pitfall O5/O6 + Phase 26 D-79 / Phase 28 D-116 / Phase 29 D-134), **D-151** (foundation-first plan ordering — D-164 mirrors), **D-152** (per-plan calibration gates with foundation-no-gate exception — D-165 mirrors), **D-153** (pre-flight active-editor — D-166 carry-forward), **D-154** (PEN-INVENTORY extension pattern — D-167 extends), **deferred Section/SidebarNav + Section/SecondaryLink consolidation** (D-145 line 327-328 — Phase 31 evaluates emerging-consumer per D-164 Plan 31-00 Task 0; default DEFER).
- `.planning/phases/30-design-system-reference-reconstruction/30-00-SUMMARY.md` — Plan 30-00 foundation pattern reference (3 new section components shipped without user gate). Phase 31 Plan 31-00 mirrors structure but ships ZERO new components (probe-only foundation).
- `.planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md` — Plan 30-01 page-frame composition + joel-only-branch calibration pattern. Plan 31-01 inherits page-frame architecture conventions + Plan 31-01 `nodeId: a0gRv` anchor (Phase 30 Design system frame id per D-167 PEN-INVENTORY line 77 update).

### Phase 29 carry-forward (D-129 Crito-shape Joel-content origin; in-place reclassification; new section component decline rationale)
- `.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md` — Phase 29 decisions D-127 through D-141. Especially **D-129** (Crito-shape Joel-content hybrid for crito-source pages — Phase 31 D-156 ADAPTS the SPIRIT to the editable-source case where Joel's narrative authority dominates topology choice; the LETTER of D-129 was flat-raster-context only), **D-133** (back-nav inline composition + Section/SecondaryLink second consumer — D-164 Plan 31-00 references for emerging-consumer rule evaluation), **D-134** (narrow-scoping precedent for new section components — D-158 + D-159 + D-160 + D-161 inherit; D-164 Plan 31-00 ships ZERO new library components), **D-138** (foundation-first plan ordering — D-164 mirrors), **D-139** (per-plan calibration gates with foundation-no-gate exception — D-165 mirrors), **D-140** (pre-flight active-editor — D-166 carry-forward), **D-141** (PEN-INVENTORY extension pattern — D-167 extends).
- `.planning/phases/29-projects-reconstruction-index-project-detail/29-00-SUMMARY.md` + `29-01-SUMMARY.md` + `29-02-SUMMARY.md` — Phase 29 plan patterns (foundation + 2-page-frame composition pattern). Plan 31-00 mirrors foundation discipline.

### Phase 28 carry-forward (D-111 TagFilter descendants-override pattern; broad-scoping section component pattern)
- `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` — Phase 28 decisions D-105 through D-126. Especially **D-111** (descendants override on Phase 25 Section/Header component — Phase 28 used this for nav-link override probe; Phase 31 D-162 Plan 31-00 Task 1 reuses pattern), **D-114** (3-up Card grid pattern at Phase 28 Blog index — Phase 31 D-158 Services 3-up grid mirrors), **D-122** (foundation-first plan ordering — D-164 mirrors), **D-124** (per-plan calibration gates — D-165 mirrors), **D-125** (pre-flight active-editor — D-166 carry-forward), **D-126** (PEN-INVENTORY extension pattern — D-167 extends).

### Phase 27 carry-forward (D-98 v1.3 verbatim content extraction; D-99 Section/SecondaryLink first consumer; Contact frame architecture)
- `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` — Phase 27 decisions D-89 through D-104. Especially **D-93** (sibling-note belt-and-suspenders convention — D-161 + D-163 + D-167 inherit), **D-98** (v1.3 verbatim content extraction discipline — D-157 + D-158 + D-159 + D-160 + D-161 inherit; Plan 31-00 Task 0 v1.3 audit pattern mirrors Phase 27 D-98 lookup pattern), **D-99** (Section/SecondaryLink inline-text-link-with-iconLeading first consumer for Thank-you "Return to homepage" — D-164 Plan 31-00 references for emerging-consumer rule evaluation), **D-101** (foundation-first plan ordering — D-164 mirrors), **D-102** (per-plan calibration gates — D-165 mirrors), **D-103** (pre-flight active-editor — D-166 carry-forward), **D-104** (PEN-INVENTORY extension pattern — D-167 extends).
- **Phase 27 Plan 27-02 Contact frame architecture context** — `n0QqTd` (top-level Contact frame at /contact route per PEN-INVENTORY line 81) houses Joel's 8-field form. D-161 SECOND cross-phase Section/CTA consumer (first was Plan 27-01 Thank-you) targets `/contact` route (n0QqTd) — closes the homepage marketing flow into Joel's lead-gen surface. v1.3 ContactSection.astro on-homepage status documented as sibling note per D-93 + D-161 — code milestone decides architecture (extract vs preserve).

### Phase 26 carry-forward (CALIBRATION-PROTOCOL § 4 joel-only ancestry; Section/CTA broad-scoping; D-77 Header no-override carry-forward)
- `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` — Phase 26 decisions D-58 through D-88. Especially **D-58** (fresh-design-in-Crito-vocab — Phase 31 D-156 carries forward Crito-vocab token authority; departs at the Joel-shape topology layer per D-129 spirit adaptation), **D-65** (single calibration gate per plan — D-165 carry-forward), **D-72** (mid-plan user gate precedent for token-value escalation — D-164 Plan 31-00 inherits IF Hero subtree spacing or new token surfaces escalation), **D-77** (Section/Header instances stay no-override default — D-162 INTENTIONALLY DEPARTS for Homepage frame ONLY per cross-phase deferral chain closure; other reconstructed pages retain D-77 carry-forward), **D-78** (Section/CTA broad-scoping precedent — D-161 SECOND cross-phase consumer validates broad-scoping prediction), **D-82** (v1.3 content verbatim — D-157 + D-158 + D-159 + D-160 + D-161 inherit), **D-83** (per-section fidelity labels — D-157 + D-158 + D-159 + D-160 + D-161 + D-165 inherit), **D-86** (foundation plans close without user gate — D-165 carry-forward), **D-87** (pre-flight active-editor — D-166 carry-forward), **D-88** (PEN-INVENTORY extension pattern — D-167 extends).

### Phase 25 component library (Phase 31 INSTANCES + D-162/D-163 closes deferral chain; D-43 Secondary Button + OPEN-25-01 Hero white-stroke override consumer)
- `.planning/phases/25-section-compound-components/25-CONTEXT.md` — Phase 25 decisions D-38 through D-57. Especially **D-38** (Crito-source nav labels stay literal default; deferred override to Phase 31 — D-162 CLOSES the deferral chain via Homepage-instance descendants override for Joel's 4-link nav), **D-40** (Joel-brand logo deferred to a later phase; Phase 31 inherits — D-163 retains Crito-source placeholder + sibling note for code milestone per PROJECT.md Out of Scope), **D-43** (Primitive/Button/Secondary hIWuC ships with navy stroke + navy label for light-bg consumer surfaces; OPEN-25-01 Hero dark-bg consumer descendants override deferred to Phase 31 — D-157 + D-164 Plan 31-00 Task 2 RESOLVES per OPEN-25-01 carve-out: `{strokeId:{stroke:"#ffffffff"},labelId:{fill:"#ffffffff"}}`), **D-49** (single Card serves all card types via slot content — D-158 Services 3-up grid validates Card cross-page consumer count), **D-52** (Pencil 2.13 typed-slot suggestion-only + sibling-note belt-and-suspenders — D-161 + D-163 + D-167 inherit), **D-77** (Section/Header instances stay no-override default — D-162 SCOPED departure documented per D-77 carry-forward).
- `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` (Section/Header G0wNOc + Secondary Button hIWuC build) — Plan 31-00 Task 1 probes G0wNOc descendants-override path; Task 2 probes hIWuC descendants-override path.
- `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` (Section/Footer Xs0Hs build) — Plan 31-01 instances Xs0Hs no-override per D-77 carry-forward.
- `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` (Compound/Card t40xct build + slot signature) — Plan 31-01 Services 3-up grid instances t40xct × 3 with descendants overrides per D-158.

### Phase 26 Section/CTA Hs5rc component (Phase 31 D-161 SECOND cross-phase consumer)
- `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` (Section/CTA Hs5rc + Section/NavBack N1jo3i build) — Plan 31-01 D-161 instances Hs5rc with descendants overrides for closing-CTA. Slot signature: headline + body + actions per Phase 26 D-78 broad-scoping.

### Phase 24 primitive library (Phase 31 INSTANCES + D-157 Hero Secondary Button override)
- `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` — Phase 24 decisions D-21 through D-37. Especially **D-22** (Default/Focus/Error compositional minimum forward states — Phase 31 instances Default + Secondary; no Focus/Error consumers on Homepage Hero CTAs at v2.0 since interactivity is code-milestone scope), **D-29** (Button iconLeading/iconTrailing slot pattern — Plan 31-01 plan-execution decides whether Hero Primary CTA carries an arrow-right per Phase 25 D-43 sibling iconTrailing pattern; default lean: yes per Phase 25 D-43 Header CTA + Phase 27 D-99 Thank-you Section/CTA precedent), **D-44** (lucide-native Pattern A glyph swap — Hero arrow-right candidate inherits Phase 25 D-43 pattern).
- `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` — primitive ids: Button/Default (M7eUr), Button/Secondary (hIWuC), Badge/Default (j0FxQZ), Badge/Outline (kJQmJ per Phase 28 addition), Input/Default (nwJk7), Icon/24 (u7NmaS plus 16/20/32 variants). Phase 31 D-157 + D-158 + D-160 + D-161 references.

### Phase 23 token foundation (PRIMARY calibration target via _Tokens & Foundations RpGbe per § 4 joel-only branch)
- `.planning/phases/23-audit-token-foundation/23-CONTEXT.md` — token-naming convention (D-12 flat-dash), components-reference-semantic-only (D-14 — D-158 + D-159 + D-160 + D-161 inherit), OPEN-flag policy (D-09 — informs all OPEN-31-NN additions), PEN-INVENTORY plain-markdown discipline (D-18 — carry-forward chain), Crito .fig fallback (D-04 — informs D-157 Hero subtree probe at Plan 31-00 Task 3 — IF ujMLJ subtree mining is insufficient for Hero structural fidelity).
- `.planning/phases/23-audit-token-foundation/23-01-SUMMARY.md` (Plan 23-01 audit — `ujMLJ` Hero subtree node IDs `0veF5 / Wx9kx / 7QsZc / HuBKK / LqPtn / ggx3v`; status_counts logic) — Plan 31-00 Task 3 probes these IDs for Hero subtree structural detail.
- `.planning/phases/23-audit-token-foundation/23-05-SUMMARY.md` — `_Tokens & Foundations` reference frame RpGbe at top-of-canvas — THIS IS THE PRIMARY CALIBRATION TARGET for Plan 31-01 § 4.5 joel-only token-usage check per D-62 + D-155.
- Phase 23 OPEN flags Phase 31 may resolve / address:
  - **OPEN-23-04** — Home Page ujMLJ is the primary IN-SCOPE token-mining source; Phase 31 is the LAST per-page consumer of this token-mining context. Phase 31 D-167 documents the partial-resolution close (status_counts update to ujMLJ line 65; Phase 31 is the final consumer that uses ujMLJ as both token-source AND visual-pairing target via D-155 HYBRID).
  - **OPEN-23-09** — Poppins token decision deferred to Phase 31. D-167 Plan 31-00 Task 0 v1.3 audit confirms Joel doesn't need Poppins (Crito-vocab + Joel-shape lean — Plus Jakarta Sans + Inter sufficient); OPEN-23-09 RESOLVED at Plan 31-00 close.
  - **OPEN-23-10** — heading-5 + heading-6 long-tail. D-159 Process step number markers + D-160 About bio likely don't need heading-5/-6 (display + heading-1 + body suffice). Default leaves OPEN-23-10 unchanged through Phase 31.

### Phase 25 OPEN flags Phase 31 resolves or extends
- **OPEN-25-01** — Primitive/Button/Secondary hIWuC navy-stroke deviation from Crito-source-literal white-stroke (Hero `mkw8g` dark-bg context). D-157 + D-164 Plan 31-00 Task 2 RESOLVES via Plan 31-01 Hero Secondary Button white-stroke + white-label descendants override per OPEN-25-01 carve-out documented in Phase 25 D-43 closure.
- **OPEN-25-07** — Card consumer count (Phase 28 + Phase 29 partial-resolution via BlogCard + ProjectCard sibling-component pattern; Phase 30 Component gallery 1 instance). Phase 31 Services adds 3 Compound/Card consumers (D-158); final cross-phase Card consumer count documented for Phase 32 close.

### Phase 26 + 27 + 28 + 29 + 30 OPEN flags Phase 31 may resolve or extend
- **OPEN-26-02** — stale-cache get_screenshot quirk; CALIBRATION-PROTOCOL § 6.4 Tier-1/Tier-2 workarounds available. Phase 31 page frame moderate height (5 content sections + Header + Footer); Tier-1 expected sufficient; Tier-2 fallback inherited per Phase 26-30 production precedent.
- **OPEN-28-NN** — Phase 28 prose-paragraph 'provisional' flag verification chain. Phase 30 was SECONDARY validation surface for `type-semantic-prose-code-block`. Phase 31 likely doesn't use prose-code-block (Homepage marketing surface — no code blocks); prose-paragraph used in About bio + body paragraphs across sections — third validation surface for prose-paragraph (after Plan 28-02 Blog Post PRIMARY + Plan 29-02 Project detail SECONDARY). 'Provisional' flag final disposition: Phase 32 milestone-close evaluation.
- **OPEN-29-NN** — Phase 29 deferrals (Screenshots / Testimonial / Built With for Project detail). Phase 31 doesn't address these; remains for future Joel-content phase / code milestone.
- **OPEN-30-NN** — Phase 30 Utilities STUB labeling (iso-shadow APPROXIMATE / iso-glow + iso-rotate STUB). Phase 31 doesn't address these; remains as Phase 32 milestone-close evaluation.

### Audit / inventory source (read for every plan in Phase 31)
- `.planning/research/PEN-INVENTORY.md` — single source of truth. Phase 31 plans read:
  - Frame classifications: `Home Page` (ujMLJ) line 65 — IN-SCOPE, 10 children, factored:8 + partial:2, reconstruction_priority high → Phase 31 D-155 + D-156 + D-167 read this row + UPDATE per D-155 LOCK action + ADD new `Homepage` reconstructed row
  - Token surface (107 tokens after Phase 30; Phase 31 adds 0 new tokens default per D-164 plan-execution evaluation)
  - Phase 23/24/25/26/27/28/29/30 Open Flags Phase 31 may resolve at calibration gates (listed above)
  - Phase 31 adds OPEN-31-NN rows per D-167 (illustration STUB labels + headshot STUB + any plan-surfaced flags)
- `.planning/research/exports/v2.0/end-of-phase-30/id-inventory.json` (when published post-Phase 30 execution) — structural snapshot of `Crito.pen` after Phase 30. Plan 31-00 reads this to know primitive + section + compound + page frame ids to instance + baseline ids that must not be mutated. Includes Phase 30 Design system frame `a0gRv` id for D-164 + § 10.4 FindEmptySpace anchor on Plan 31-01.

### Ground-truth source files (Phase 31 reads these via Pencil MCP or direct file I/O)
- `design/Crito.pen` — the work surface (Pencil MCP only). Phase 31 mutates:
  - NEW: `Homepage` page frame per D-164 Plan 31-01 (top-level frame; descendants per D-157 + D-158 + D-159 + D-160 + D-161 + D-162 + D-163)
  - UPDATE: `Home Page` (ujMLJ) Hero illustration raster — sibling Pencil note added per D-155 LOCK (raster NOT hidden, stays enabled:true)
  - Existing frames (Phase 23-30 components + page frames + library parents + 14 Crito-source page frames + RpGbe `_Tokens & Foundations`) are read-only inputs (with the LOCK note addition above).
- `src/pages/index.astro` (lines 1-16) — Joel's v1.3 homepage entry. Content extraction at Plan 31-00 Task 0:
  - Hero component imported (line 3) — content source per D-157
  - Services component imported (line 4) — content source per D-158
  - Process component imported (line 5) — content source per D-159
  - About component imported (line 6) — content source per D-160
  - ContactSection component imported (line 7) — content source per D-161 sibling note (NOT instanced on Homepage frame)
- `src/components/Hero.astro` (lines 1-497) — v1.3 Hero. Content extraction for D-157:
  - H1 headline: lines 71-73 + 91-93 "Your business needs more than software, it needs..."
  - CTA copy: lines 79-81 + 116-118 "Ready to get started? / Let's talk! / →"
  - 5 outcome tile content (lines 13-49) — STUB on Phase 31 (bento-grid topology declined per D-157 Area 1 Q2 selection)
  - Visual style (Bricolage Grotesque + uppercase + neobrutalist 3-layer shadow + yellow/turquoise/magenta variant colors) explicitly NOT inherited per D-58 + D-157 carry-forward
- `src/components/Services.astro` — v1.3 Services. Content extraction for D-158:
  - 3 service category title + description (AI Development / Automation / Web Apps — exact wording per content)
  - Section H2 wording (likely "Solutions" or "Services")
  - Lucide isometric illustration per category — NOT inherited per D-158 illustration-slot STUB; sibling note documents
- `src/components/Process.astro` — v1.3 Process. Content extraction for D-159:
  - 5 step title + body (Discovery / Design / Build / Test / Launch per v1.2 + v1.3 + likely revised in v1.3)
  - Section H2 wording (likely "How we work" or "Our process")
  - 5 isometric illustrations — NOT inherited per D-159 illustration-slot STUB; sibling note documents
- `src/components/About.astro` — v1.3 About. Content extraction for D-160:
  - H2 wording (likely "About me" or "About Joel")
  - Bio paragraph
  - CTA copy + target (likely "Let's talk" → /contact)
  - Headshot — NOT inherited per D-160 headshot-slot STUB; sibling note documents
- `src/components/homepage/ContactSection.astro` — v1.3 on-homepage Contact form. NOT reconstructed on Homepage frame per D-161 (full form lives at /contact n0QqTd per Phase 27); sibling note on Section/CTA closing instance documents v1.3 on-homepage architecture for code milestone.
- `src/components/layout/Header.astro` — v1.3 Header. Content extraction for D-162:
  - 4 nav links: Blog / Projects / FAQ / Contact (exact href + label wording)
  - Logo: "Joel Shinness" wordmark (Bricolage Grotesque + uppercase) — NOT inherited per D-163; sibling note documents

### v2.0 research (cross-cutting context)
- `.planning/research/SUMMARY.md` — themes T1 (variables-first — Phase 31 adds 0 new tokens default per D-164; primarily INSTANCES the surface), T5 (don't repeat v1.4 — calibration gates enforce per-section labels per § 4.2 + § 3.2 hybrid), T6 (single-file strategy — Homepage frame inside `Crito.pen`), T8 (component variants on-demand only — Phase 31 INSTANCES existing variants; no new variants added).
- `.planning/research/STACK.md` — Pencil MCP tool catalogue. `find_empty_space_on_canvas` with `nodeId` anchor (D-164 + § 10.4), `get_screenshot` (calibration artifacts — inline-only per OPEN-23-01 substitution), `batch_design` (Plan 31-01 1 page frame + sibling Pencil note on ujMLJ Hero illustration per D-155 LOCK), `batch_get` (Plan 31-00 Task 3 ujMLJ Hero subtree probe + Plan 31-01 baseline ID verification).
- `.planning/research/PITFALLS.md` — Pitfall 1 (no inventing primitives without source — D-164 ships ZERO new tokens default; only IF Hero subtree probe surfaces real escalation), Pitfall 4 (PAGE-11 NEVER hide raster before APPROVE — D-155 LOCK is post-APPROVE; raster STAYS enabled:true per § 3.3 sub-section-raster sub-rule), Pitfall 6 (snapshot_layout text-clipping false-positive — documented per phase precedent), Pitfall 7 (no premature component variants — D-164 ships ZERO new variants; OPEN-25-01 white-stroke is a Phase 25-anticipated CARVE-OUT, not a new variant), Pitfall O5/O6 (single-consumer component anti-pattern — D-158 + D-159 + D-160 + D-161 honor; Phase 30 D-149 departure NOT inherited).

### Pencil MCP guidance (consulted during execution, not pre-read at planning)
- `mcp__pencil__get_editor_state({ include_schema: false })` — call FIRST in every 31-NN plan per D-166.
- `mcp__pencil__get_guidelines({ topic: "design-system" })` — call at start of Plan 31-00 (component instance + descendants override mechanics).
- `mcp__pencil__get_variables({})` — call to verify 107-token surface intact at Plan 31-00 start; re-call at every plan close to verify drift expectations (Phase 31 ships 0 new tokens default per D-164).
- `mcp__pencil__batch_get` — verify baseline IDs intact (Phase 23-30 components + page frames + library parents + RpGbe + ujMLJ + 13 Crito source page frames).
- `mcp__pencil__batch_design` — Plan 31-01 (Homepage page frame + Hero/Services/Process/About inline composition + Section/Header + Section/CTA + Section/Footer instances + Header descendants override + Hero Secondary Button descendants override + sibling notes; possible Insert on ujMLJ Hero illustration sibling-note per D-155 LOCK).
- `mcp__pencil__find_empty_space_on_canvas` — Plan 31-01 page-frame placement per D-164 + § 10.4 (nodeId anchor pattern on `a0gRv` Phase 30 Design system frame).
- `mcp__pencil__get_screenshot` — calibration artifacts per CALIBRATION-PROTOCOL § 4.5 step 9 + § 3.4 step 4 Hero side-by-side (inline-only, NOT disk-written per OPEN-23-01 substitution).
- `mcp__pencil__snapshot_layout({ problemsOnly: true })` — per-plan close per Phase 24-30 precedent.

### Outputs this phase produces (referenced by Phase 32)
- 1 new page frame in `design/Crito.pen`: `Homepage` (Plan 31-01)
- 1 sibling Pencil note added on ujMLJ Hero illustration raster per D-155 LOCK (raster NOT hidden — stays enabled:true)
- 0 new library entries (Phase 31 Plan 31-00 is probe-only; library count unchanged 19 → 19)
- 0 new tokens default per D-164 plan-execution evaluation
- `.planning/research/PEN-INVENTORY.md` extensions per D-167 (new `Homepage` reconstructed row + UPDATE to `Home Page` ujMLJ line 65 status_counts + Variant Evidence rows for Phase 31 + Open Flags Phase 31 — OPEN-31-NN section + RESOLVED labels for OPEN-25-01 + OPEN-23-09)
- `.planning/research/exports/v2.0/end-of-phase-31/id-inventory.json` — structural snapshot per OPEN-23-01 substitution pattern (matches Phase 23-30 precedent)

</canonical_refs>

<code_context>
## Existing Code Insights

**Phase 31 makes NO `src/` code changes.** v2.0 is `.pen`-file-only (per PROJECT.md + REQUIREMENTS.md Out of Scope). The code-side observations below are content-extraction context only — they do NOT shape Phase 31 visual decisions, which follow the D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch per D-156 + D-157 + D-158 + D-159 + D-160 + D-161 + D-162 + D-163 (Joel-shape backbone, Crito-vocab tokens + library, 1440 page-frame conventions).

### Reusable Assets (content-only references)
- `src/pages/index.astro` (lines 1-16) — v1.3 homepage entry. Imports 5 components in order: Hero / Services / Process / About / ContactSection. Section ordering preserved by Phase 31 D-156 Joel-shape backbone (Hero / Services / Process / About / closing-CTA — ContactSection replaced by Section/CTA Hs5rc + sibling note per D-161).
- `src/components/Hero.astro` (lines 1-497) — v1.3 Hero. Bento-grid asymmetric layout NOT inherited per D-157 (Crito Hero structure wins). Content lifted: H1 "Your business needs more than software, it needs..." + CTA copy "Let's talk!" + subtitle "Ready to get started?" per Plan 31-00 Task 0 + D-157.
- `src/components/Services.astro` — v1.3 Services. 3-category content lifted: AI Development / Automation / Web Apps (exact wording per Plan 31-00 Task 0). Isometric illustrations NOT inherited per D-158 illustration-slot STUB; sibling note documents.
- `src/components/Process.astro` — v1.3 Process. 5-step content lifted: Discovery / Design / Build / Test / Launch (exact wording per Plan 31-00 Task 0). Isometric illustrations NOT inherited per D-159; sibling note documents.
- `src/components/About.astro` — v1.3 About. H2 + bio + CTA copy lifted per Plan 31-00 Task 0 + D-160. Headshot NOT inherited per D-160; sibling note documents.
- `src/components/homepage/ContactSection.astro` — v1.3 on-homepage Contact form. NOT instanced on Homepage frame per D-161. Sibling Pencil note on Section/CTA closing instance documents v1.3 on-homepage architecture for code-milestone decision (extract to /contact n0QqTd vs preserve on homepage).
- `src/components/layout/Header.astro` — v1.3 Header. 4-link nav (Blog / Projects / FAQ / Contact) extracted for D-162 Section/Header descendants-override target labels. Logo "Joel Shinness" wordmark NOT inherited per D-163; sibling note documents.

### Established Patterns (Pencil-side, ARE Phase 31 inputs)
- **Variables-first → primitives → compounds → sections → page frames** — Phase 23 tokens, Phase 24 primitives, Phase 25 sections + compounds, Phase 26-30 per-page reconstructions, Phase 31 eighth per-page phase (LAST per-page phase — Phase 32 is milestone-close sweep + handoff).
- **Single-file strategy** — everything in `design/Crito.pen`. Phase 31 page frame placed at the page-frame row (y ≈ −4111) via FindEmptySpace nodeId anchor pattern from CALIBRATION-PROTOCOL § 10.4 (Plan 31-01 anchors on Phase 30 Design system frame `a0gRv`).
- **OPEN-flag system** — Phase 31 extends with `OPEN-31-NN` rows per D-167 (illustration STUB + headshot STUB labels). OPEN-25-01 + OPEN-23-09 RESOLVED at Plan 31-00 + Plan 31-01 close.
- **Pre-flight active-editor assertion** — D-166 carries forward from Phase 30 D-153 / Phase 29 D-140 / Phase 28 D-125 / Phase 27 D-103 / Phase 26 D-87 / Phase 25 D-54 / Phase 24 D-35 / Phase 23 OPEN-23-14.
- **Plain-markdown audit trails** — Phase 23 D-18 / Phase 24 D-23 / Phase 25 D-56 / Phase 26 D-88 / Phase 27 D-104 / Phase 28 D-126 / Phase 29 D-141 / Phase 30 D-154 pattern. Phase 31 D-167 extends.
- **Probe-first within plans** — Phase 24-30 established. Phase 31 Plan 31-00 probes Header descendants-override (Task 1) + Secondary Button descendants-override (Task 2) + ujMLJ Hero subtree (Task 3).
- **Belt-and-suspenders sibling-note documentation** — Phase 25 D-52 / Phase 26 D-78 / Phase 27 D-93 / Phase 28 D-111 / Phase 29 D-132 / Phase 30 D-143 + D-144 + D-145 pattern. D-155 (ujMLJ Hero illustration LOCK note) + D-161 (v1.3 ContactSection on-homepage status note) + D-163 (Joel-brand logo deferral note) all inherit.
- **Per-section fidelity labels with LAYOUT/TEXT split** — D-83 (Phase 26) precedent. D-157 + D-158 + D-159 + D-160 + D-161 inherit (Hero APPROXIMATE pairing; Services + Process + About EXACT layout + STUB illustrations/headshot; closing-CTA EXACT library component instance; Header overridden-EXACT; Footer EXACT).
- **CALIBRATION-PROTOCOL joel-only-no-crito-ref branch** — Phase 26 Plans 26-01 + 26-02 + Phase 27 Plan 27-01 + Phase 28 Plan 28-03 + Phase 30 Plan 30-01 production-proven. Phase 31 Plan 31-01 is the SIXTH production use of the joel-only branch (with D-155 HYBRID Hero exception).
- **CALIBRATION-PROTOCOL crito-source-present branch § 3.4 Hero side-by-side** — Phase 27 Plan 27-02 + Phase 28 Plans 28-01/28-02 + Phase 29 Plans 29-01/29-02 production-proven (against flat-raster sources). Phase 31 Plan 31-01 is the FIRST § 3.4 production use against an editable Crito source (ujMLJ Hero subtree).
- **Anti-over-factoring discipline carry-forward** — Phase 26 D-79 + Phase 28 D-116 + Phase 29 D-134 narrow-scoping precedent. Phase 31 D-158 + D-159 + D-160 + D-161 INHERIT (no new section components). Phase 30 D-149 showcase-pattern departure NOT inherited — Phase 31 stays at the narrow-scoping baseline.
- **Cross-phase deferral chain closure** — Phase 25 D-38 (Header nav override) + Phase 25 D-40 (Joel logo) + Phase 25 OPEN-25-01 (Hero Secondary white-stroke) + Phase 23 OPEN-23-04 (Home Page token-mining final consumer) + Phase 23 OPEN-23-09 (Poppins revisit) CLOSED at Phase 31 per D-157 + D-162 + D-163 + D-167. Phase 31 is the final v2.0 per-page consumer of these chains.

### Integration Points
- **CALIBRATION-PROTOCOL.md is the definition-of-done framework** — Plan 31-01 consults at plan-time (D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch documented in this CONTEXT; § 4 joel-only branch protocol + § 3.4 step 4 Hero side-by-side artifact + § 3.3 sub-section-raster LOCK sub-rule).
- **`_Tokens & Foundations` reference frame RpGbe is the PRIMARY calibration target for non-Hero sections** — per D-62 + § 4.1 VALID-02. Plan 31-01 calibration AskUserQuestion description structures itself around: "Does the Homepage page frame's Services + Process + About + closing-CTA + Header overridden-instance + Footer bindings match the canonical surface declared in RpGbe?"
- **ujMLJ Hero subtree is the SECONDARY calibration target for Hero section** — per D-155 HYBRID + § 3.4 step 4. Plan 31-01 calibration AskUserQuestion description carries Hero side-by-side image pair: reconstructed Hero `get_screenshot(<reconstructed-Hero-id>)` + ujMLJ Hero subtree `get_screenshot(<ujMLJ Hero subtree id>)`.
- **Phase 30 was the FIRST cross-page consistency proof for Section/Header + Section/Footer; Phase 31 is the SECOND** — per ROADMAP Phase 31 success criterion 3. Phase 31 D-162 Header descendants-override SCOPED to Homepage instance preserves this consistency at the component level.
- **Phase 31 IS the LAST per-page reconstruction phase in v2.0** — D-156 + D-167 + ROADMAP. After Plan 31-01 APPROVE, Phase 32 milestone-close sweep + handoff begins; no further per-page work.
- **PEN-INVENTORY Frames Inventory table** — Phase 31 ADDS 1 new reconstructed `Homepage` row + UPDATES 1 existing `Home Page` ujMLJ line 65 row (status_counts LOCK) per D-167 + D-154 carry-forward. Closes the IN-SCOPE reconstructed-Joel-page chain.

</code_context>

<specifics>
## Specific Ideas

- **The D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch is Phase 31's novel mechanism** — CALIBRATION-PROTOCOL was designed for two pure branches (§ 3 crito-source-present + § 4 joel-only-no-crito-ref); Phase 31 is the first case where the topology decision (Joel-shape per D-156) decouples from the source availability (Crito source editable for Hero subtree). Resolution: § 4 default + § 3.4 step 4 Hero exception, layered. The mechanism is documented in this CONTEXT (not in CALIBRATION-PROTOCOL.md per Phase 31 D-155); Phase 32 milestone-close sweep evaluates whether to promote it to a third codified branch. Plan 31-01 calibration gate carries both protocol artifacts in a single AskUserQuestion description.
- **The Joel-shape topology lock at D-156 is the most deliberate Phase 31 reading** — Phase 29 D-129 established "Crito-shape Joel-content hybrid" for crito-source pages, but every prior crito-source consumer was a flat raster, so "Crito-shape" was just "follow the raster's visible layout." Phase 31 is the first case where the Crito source has REAL structural ground truth (ujMLJ has 10 editable group children), so the choice is meaningful. User selection: Joel-shape wins because (a) v1.3 ships 5 sections (Hero / Services / Process / About / ContactSection) that map to Joel's narrative, (b) Crito's 10 sections include template-marketplace patterns (partner-logo strip, dashboard showcase) that Joel doesn't have, (c) PROJECT.md "content untouched in v2.0" supports content authority resting with Joel's existing source.
- **The Hero section is the one section where Crito-shape == Joel-shape per D-157** — Joel's v1.3 Hero (bento grid) doesn't match Crito's Hero structurally, but Joel's CONTENT authority (H1 headline) does map 1:1 to Crito's Hero content slot. Joel's bento-grid layout (5 outcome tiles + center headline + bottom CTA) is a v1.3 stylistic choice that explicitly carries Joel-brand neobrutalist chrome (Bricolage Grotesque + uppercase + 3-layer shadow + yellow/turquoise/magenta variants) — all OUT-OF-SCOPE for v2.0. Crito Hero structure (PJS 70 + Inter 18 + dual CTAs + decorative anchor) is the natural Crito-vocab Hero that v2.0 can ship without dragging in OUT-OF-SCOPE Joel-brand chrome. The choice is honest: Joel content + Crito visual register, the cleanest combination available.
- **OPEN-25-01 finally RESOLVES at Phase 31 Hero** — Phase 25 D-43 anticipated this carve-out: "Phase 31 Hero may override stroke + label fill to white at instance time via descendants to restore source-literal dark-bg appearance." Phase 31 D-157 + D-164 Plan 31-00 Task 2 + Plan 31-01 instance-time descendants override RESOLVE the carve-out. The Secondary Button (hIWuC) component-level navy stroke + navy label serves Header + About + other light-bg consumers; the descendants override at instance time restores Crito-literal whites for the Hero dark-bg consumer.
- **The D-162 Header descendants-override is SCOPED to Homepage instance only** — Phase 25 D-77 carry-forward chain said Section/Header instances stay no-override default for cross-page consistency. Phase 31 D-162 INTENTIONALLY DEPARTS for ONE instance (Homepage) per the cross-phase deferral chain closure mandate from Phase 25 D-38 + Phase 26 D-77 + Phase 27 + Phase 28 + Phase 29 + Phase 30 carry-forward. Other reconstructed pages retain Crito-source 6-link defaults. Phase 32 milestone-close sweep evaluates whether to back-propagate the override to all 9 other pages (optional; Joel-decision gate IF surfaced).
- **The D-163 logo deferral REMAINS deferred to code milestone** — Phase 25 D-40 deferred Joel-brand logo to a later phase. Phase 31 inherits the deferral per PROJECT.md "Joel-brand visual chrome — still planned to be replaced when code milestones run on top of v2.0" Out of Scope. Sibling Pencil note on Section/Header logo slot documents v1.3 wordmark for code milestone. This is honest: v2.0 is .pen-file-only + Joel-brand chrome is code-milestone scope.
- **Phase 31 page frame architecture is moderate-density compared to Phase 30** — Plan 30-01 had ~58 visual artifacts (token gallery + component gallery + utilities); Plan 31-01 has 7 children (Header + Hero + Services [3 Cards] + Process [5 steps] + About + closing-CTA + Footer) ≈ 15-20 visual artifacts in primary instance count. Lower stale-cache risk than Phase 30; Tier-1 fallback expected sufficient.
- **The closing-CTA Section/CTA Hs5rc instance is the SECOND cross-phase consumer of the broad-scoping pattern from Phase 26 D-78** — first was Phase 27 Plan 27-01 Thank-you. Phase 31 D-161 validates Phase 26 D-78 broad-scoping prediction (Section/CTA serves multiple consumers; broad-scoping warranted at Phase 26 time). Component count breakdown: 2 cross-phase consumers (Thank-you + Homepage). Lower than Section/Header / Section/Footer cross-page consumer count (every per-page consumer instances them) but solid validation.
- **Phase 31 closes the v2.0 per-page reconstruction chain** — After Plan 31-01 APPROVE: every IN-SCOPE Joel page in {Homepage, Projects index + detail, Blog index + post + tag, FAQ, Contact, Thank-you, Design system, 404} is reconstructed. Only `04_About` (WDGxc, IN-SCOPE token-mining-only per D-07) remains as the long-tail Phase 32 milestone-close disposition row. v2.0 is at 9/10 phases complete after Phase 31 (90%).

</specifics>

<deferred>
## Deferred Ideas

- **Crito 10-section topology adoption** — Crito's `ujMLJ` 10 sections (partner-logo strip / "We help to grow" / Dashboard / "Why will you choose" / "How to grow" / Testimonial / "Performance is the key" / "Ready to use our app" / Footer + Hero) NOT inherited per D-156 Joel-shape backbone. If a future Joel-content milestone wants to add a Testimonials section or partner-logo strip (PROJECT.md Future enhancements: "Testimonials section with client quotes"), those become new homepage sections in a future per-page phase or a homepage v2 milestone. v2.0 leaves them as token-mining + vibe-reference only.
- **Joel's v1.3 Hero bento-grid layout** — Bento-grid asymmetric layout (5 outcome tiles + center headline + bottom CTA) NOT inherited per D-157. Joel-brand visual chrome (Bricolage Grotesque + uppercase + 3-layer shadow + variant colors) OUT-OF-SCOPE per PROJECT.md. Code milestone may rebuild the bento UI on top of the Crito-vocab Hero structure.
- **Joel-brand isometric illustrations (Services + Process + About headshot)** — Joel-brand assets OUT-OF-SCOPE for v2.0 per PROJECT.md "Joel-brand visual chrome." STUB image-fill slots in Phase 31; code milestone wires illustrations + headshot at runtime per sibling Pencil notes.
- **Joel-brand wordmark / logo asset** — D-163 + Phase 25 D-40 carry-forward. Crito-source placeholder stays on Phase 31 Homepage Header instance; sibling note documents v1.3 wordmark for code milestone.
- **Joel-brand fonts (Bricolage Grotesque + DM Sans)** — PROJECT.md "still planned to be replaced when code milestones run on top of v2.0" Out of Scope. Phase 31 honors Crito-vocab (Plus Jakarta Sans + Inter) per D-58 + D-156 + D-157 chain.
- **ContactSection.astro full 8-field form on Homepage frame** — D-161 declines. Section/CTA close + link to /contact n0QqTd is the v2.0 Pencil treatment. Code milestone decides architecture (extract to /contact route OR preserve on homepage); sibling note documents.
- **Header nav 4-link override propagation to other reconstructed pages (FAQ / 404 / Thank-you / Contact / Blog / Tag / Projects / Project / Design system)** — D-162 scopes to Homepage instance only. Phase 32 milestone-close sweep optionally back-propagates (adds 9 frames of descendants-override surface; Joel-decision gate IF surfaced). Default: defer to code milestone where runtime configuration is cleaner.
- **Sticky positioning behavior in Pencil-rendered frames** — D-92 / D-59 / D-145 static-design carry-forward. Code milestone wires sticky behavior; .pen represents structural READING state only.
- **Bottom contact preview (mini form: Name + Email + Send)** — Area 3 Q1 declined in favor of Section/CTA marketing-close. If future Joel-content milestone wants a homepage lead-magnet preview, factor then per emerging-consumer rule.
- **OPEN-23-09 Poppins token addition** — D-167 documents RESOLVED at Plan 31-00 Task 0 v1.3 audit (Joel doesn't need Poppins per Crito-vocab + Joel-shape lean — PJS + Inter sufficient). If future Joel-content milestone wants Poppins for visual fidelity, add then with explicit consumer evidence per Pitfall 1.
- **`type-semantic-heading-5` + `type-semantic-heading-6` token additions** — OPEN-23-10 long-tail. Phase 31 doesn't add per D-164 Plan 31-00 evaluation (display + heading-1 + body + caption surface sufficient for Hero + Services + Process + About + closing-CTA composition). Phase 32 milestone-close evaluation if any cross-phase pattern surfaces.
- **`radius-semantic-pill` token addition** — OPEN-23-12 / Phase 28 D-120 / Phase 29 D-141 / Phase 30 D-167 deferred to instance-time `9999` cornerRadius override. Phase 31 doesn't surface new pill consumers (Hero + Services + Process + About + closing-CTA use Card / Button at radius-semantic-button or radius-semantic-card defaults). Phase 32 sweep retains final-evaluation authority.
- **Crito Hero illustration as Pencil-native composition** — D-155 LOCK sub-section-raster sub-rule retains the Crito Hero illustration raster in place. Phase 31 STUB-pattern at Plan 31-01 Hero illustration slot is an image-fill rectangle placeholder; code milestone reconstructs as full illustration. Crito-source Hero illustration raster (inside ujMLJ) stays as cross-reference.
- **`Section / SidebarNav` factoring** — Phase 30 D-145 noted potential factoring IF Phase 31 Homepage or future v3 milestone needs a sidebar pattern. Phase 31 Homepage doesn't need a sidebar (full-width linear scroll per D-156 Joel-shape); declined. Future v3 milestone may surface.
- **`Section / SecondaryLink` consolidation** — Phase 27 D-99 + Phase 29 D-133 are 2 consumers; Phase 30 D-145 noted potential factoring IF Phase 31 adds a third. D-164 Plan 31-00 Task 0 v1.3 audit confirms Phase 31 doesn't add a third (Hero CTAs are dual Primary+Secondary Buttons not text-links; Services footer-actions-slot uses Button; closing-CTA uses Button). Default: defer to future phase or code milestone.
- **Phase 31 calibration sub-branch (D-155 HYBRID) protocol promotion** — D-155 documents the sub-branch in this CONTEXT; Phase 32 milestone-close sweep evaluates whether to promote it to a third codified branch in CALIBRATION-PROTOCOL.md alongside § 3 crito-source-present + § 4 joel-only-no-crito-ref. Default: Phase 31 is the only consumer in v2.0; CALIBRATION-PROTOCOL.md stays as-is unless future Joel-content phase consumes a similar pattern.
- **Joel-brand neobrutalist chrome reintroduction** — yellow/turquoise/magenta OKLCH palette + Bricolage Grotesque + DM Sans + shadow-to-glow + 3-layer button technique + iso-shadow/iso-glow/iso-rotate utilities — all "still planned to be replaced when code milestones run on top of v2.0" per PROJECT.md. Phase 31 honors Crito-vocab per D-58 + D-156 + D-157 + D-158 + D-159 + D-160 chain.
- **Code-milestone homepage rebuild** — out of v2.0 scope. v1.3 route keeps rendering on v1.3 code throughout v2.0. Code milestone must refactor `src/pages/index.astro` + companion components (Hero / Services / Process / About / ContactSection / Header / Footer) to consume Phase 31's Pencil-native section composition + token bindings.

</deferred>

---

*Phase: 31-homepage-reconstruction*
*Context gathered: 2026-06-11*
