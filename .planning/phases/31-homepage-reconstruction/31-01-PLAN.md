---
id: "31-01"
phase: 31-homepage-reconstruction
plan: "01"
type: execute
wave: 2
depends_on: ["31-00"]
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
  - .planning/phases/31-homepage-reconstruction/31-01-SUMMARY.md
autonomous: false
requirements:
  - PAGE-01
must_haves:
  truths:
    - "ROADMAP success criterion 1: NEW Homepage page frame exists in design/Crito.pen with 7 child sections (Header + Hero + Services + Process + About + closing-CTA + Footer); ZERO [FLAT] markers in the reconstructed frame; Hero illustration STUB is INSIDE the new frame; ujMLJ Hero illustration raster stays in ujMLJ as LOCK reference per D-155 + § 3.3"
    - "ROADMAP success criterion 2: every section is either a Section component instance (Header / closing-CTA / Footer = 3 instances) OR rule-justified inline layout frame (Hero / Services / Process / About = 4 inline per D-156/-157/-158/-159/-160 + Pitfall O5/O6 narrow-scoping chain); duplication NOT silent (sibling Pencil notes per D-93 belt-and-suspenders carry-forward)"
    - "ROADMAP success criterion 3: reconstructed Homepage uses Section/Header (G0wNOc) + Section/Footer (Xs0Hs) instances at top + bottom — Phase 30 a0gRv was FIRST cross-page consistency consumer; Phase 31 is SECOND"
    - "ROADMAP success criterion 4: per-section fidelity labels for 7 sections (Header EXACT overridden / Hero APPROXIMATE / Services EXACT layout + STUB illustrations / Process EXACT layout + STUB illustrations / About EXACT layout + STUB headshot / closing-CTA EXACT / Footer EXACT) + side-by-side calibration artifact via D-155 HYBRID single AskUserQuestion + user spot-check before phase close"
  artifacts:
    - path: "design/Crito.pen :: Homepage frame (NEW, top-level)"
      provides: "7-child Homepage composition: Header instance + Hero inline + Services inline + Process inline + About inline + Section/CTA instance + Footer instance"
      exports: ["Homepage frame ID (TBD at find_empty_space_on_canvas time)"]
    - path: "design/Crito.pen :: ujMLJ Hero illustration parent (UPDATE — sibling Pencil note added)"
      provides: "LOCK action per § 3.3 sub-section-raster sub-rule; raster stays enabled:true; sibling note documents cross-reference"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "NEW Homepage row + UPDATE ujMLJ line 65 status_counts + Variant Evidence Phase 31 rows + Open Flags — Phase 31 (OPEN-31-NN) section + OPEN-25-01/-23-09 RESOLVED + OPEN-23-04 LAST-VISIT"
    - path: ".planning/phases/31-homepage-reconstruction/31-01-SUMMARY.md"
      provides: "Plan-close summary with frontmatter, per-section fidelity labels, calibration gate transcript reference, PEN-INVENTORY diff summary"
  key_links:
    - from: "Homepage frame"
      to: "Section/Header instance with D-162 4-link descendants override"
      via: "batch_design Insert with descendants: {<nav-item-IDs from 31-00 T1>}"
    - from: "Hero inline composition"
      to: "Primitive/Button/Secondary (hIWuC) instance with OPEN-25-01 white-stroke override"
      via: "batch_design Insert with descendants: {<stroke + label IDs from 31-00 T2>: {stroke/fill: #ffffffff}}"
    - from: "Services inline composition"
      to: "3 × Compound/Card (t40xct) instances with per-card descendants overrides"
      via: "horizontal-layout 3-up grid; image-slot STUB image-fill rect; title/body/footer-actions slot overrides per v1.3 content from 31-00 T0"
    - from: "Section/CTA (Hs5rc) instance"
      to: "/contact route (Phase 27 n0QqTd) via Primary Button"
      via: "actions-slot descendants override Button label + href; sibling Pencil note re v1.3 ContactSection on-homepage architecture"
    - from: "Calibration gate AskUserQuestion (Task 10)"
      to: "user APPROVE/REVISE/GAP decision"
      via: "single AskUserQuestion carrying both § 4.5 token-usage description + § 3.4 Hero side-by-side inline image pair"
    - from: "Post-APPROVE LOCK action (Task 11)"
      to: "ujMLJ Hero illustration parent — sibling Pencil note"
      via: "batch_design Insert sibling note on parent (NOT enabled:false on raster) per § 3.3 + Pitfall 4"
---

<objective>
Compose the `Homepage` page frame in `design/Crito.pen` as a 7-child editable composition of Phase 23-30 tokens / primitives / compounds / sections per D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch. Run single calibration AskUserQuestion gate carrying BOTH § 4.5 joel-only token-usage description AND § 3.4 step 4 Hero side-by-side inline image pair. On APPROVE: attach sibling Pencil note on ujMLJ Hero illustration parent per § 3.3 LOCK action; extend PEN-INVENTORY per D-167. Closes v2.0 per-page reconstruction chain (last per-page phase; Phase 32 = milestone-close sweep).

Purpose:
- Satisfy PAGE-01 (Homepage frame reconstructed — zero `[FLAT]` markers across hero/services/process/about/closing/Header/Footer)
- Satisfy ROADMAP success criteria 1-4 with highest-scrutiny calibration per "most-trafficked surface"
- CLOSE cross-phase deferral chain: Phase 25 D-38 Header nav override + Phase 25 D-40 Joel-brand logo + Phase 25 OPEN-25-01 Hero Secondary white-stroke + Phase 23 OPEN-23-04 Home Page token-mining last consumer + Phase 23 OPEN-23-09 Poppins decision
- Validate Section/CTA Hs5rc as SECOND cross-phase consumer (after Phase 27 Plan 27-01 Thank-you) per Phase 26 D-78 broad-scoping prediction

Output:
- 1 NEW top-level Homepage page frame
- 1 sibling Pencil note added on ujMLJ Hero illustration parent (raster NOT hidden — stays enabled:true)
- 0 new library entries (composition-only)
- 0 new tokens default (Plan 31-00 T0 audit confirms existing 107-token surface sufficient)
- PEN-INVENTORY extensions per D-167
- 31-01-SUMMARY.md with per-section fidelity labels + calibration transcript reference
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-homepage-reconstruction/31-CONTEXT.md
@.planning/phases/31-homepage-reconstruction/31-RESEARCH.md
@.planning/phases/31-homepage-reconstruction/31-VALIDATION.md
@.planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/PITFALLS.md
@.planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight + create top-level Homepage page frame</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md (Task 4 baseline regression confirms intact starting state)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md (D-164 Plan 31-01 page-frame conventions: 1440 width, vertical layout, gap 0, padding 0, alignItems center, fit_content height, fill color-semantic-bg-page or #ffffffff per D-58)
    - .planning/research/CALIBRATION-PROTOCOL.md § 10.4 (FindEmptySpace nodeId anchor pattern; anchor on Phase 30 Design system frame a0gRv)
    - .planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md (a0gRv Design system frame ID confirmation + page-frame row y ≈ −4111 convention)
    - .planning/research/PITFALLS.md (Pitfall O5/O6 narrow-scoping single-consumer rule — 4 inline sections per D-158/-159/-160 honored; NO new section components in this plan)
  </read_first>
  <action>
    Run `mcp__pencil__get_editor_state({ include_schema: false })` and assert `active_editor` ends with `Crito.pen`. Halt + surface to user on mismatch (D-166 pre-flight).

    Run `mcp__pencil__get_variables({})` and assert returns exactly 107 user-facing tokens (Phase 31-00 baseline). Halt + surface on drift.

    Run `mcp__pencil__find_empty_space_on_canvas({ nodeId: "a0gRv" })` per § 10.4 — anchor on Phase 30 Design system frame to land on the page-frame row (y ≈ −4111) NOT the library row. Record the returned `{ x, y }` coordinates.

    Run `mcp__pencil__batch_design` with a single Create operation to add the top-level Homepage frame:
    - Type: frame (top-level page frame)
    - Position: returned { x, y } from find_empty_space_on_canvas
    - Width: 1440 (page-frame convention per Phase 26-30 chain)
    - Height: fit_content (vertical-stack will determine final height)
    - Name: "Homepage"
    - Layout: vertical (auto-layout vertical-stack)
    - Gap: 0 (sections own their own internal padding/gap per Phase 26-30 chain)
    - Padding: 0 (sections own their own internal padding)
    - AlignItems: center (content-width 1200 sub-frames center at 1440)
    - Fill: `color-semantic-bg-page` token reference (fallback to literal `#ffffffff` per D-58 carry-forward IF token reference fails)

    Capture the returned frame ID and record it in 31-01-SUMMARY.md under `## Task 1 — Homepage Frame Created`. This is the parent for all subsequent Insert operations (Tasks 2-8). Refer to this ID as `<HomepageId>` in subsequent task actions.

    Run `mcp__pencil__batch_get({ nodeIds: [<HomepageId>], readDepth: 0 })` to verify the frame was created with the expected properties (width 1440, layout vertical, 0 children initially). Record the verification result.
  </action>
  <acceptance_criteria>
    - get_editor_state confirmed active editor == Crito.pen BEFORE any mutation (D-166)
    - get_variables returned 107 (or halt on drift)
    - find_empty_space_on_canvas with nodeId:a0gRv returned valid {x, y} on the page-frame row (y in range matching Phase 30 a0gRv neighbor row)
    - batch_design Create returned a frame ID for the new Homepage frame
    - batch_get verification confirms frame exists with width:1440, layout:vertical, 0 children
    - 31-01-SUMMARY.md `## Task 1` section records the Homepage frame ID + placement coordinates
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<HomepageId>"], readDepth: 0 }) returns name:"Homepage", width:1440, layout:vertical
  </verify>
  <done>
    Empty Homepage frame exists at the page-frame row; ready for Section/Header Insert at Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header (G0wNOc) with D-162 4-link descendants override + D-163 logo sibling note</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 1 — Section/Header (G0wNOc) Override Path` (exact descendants property-path map with substituted nav-item IDs)
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 0` Header sub-section (4 nav-link labels Blog/Projects/FAQ/Contact + hrefs from v1.3)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-162 (SCOPED descendants override — Homepage instance only; other reconstructed pages keep Crito-source 6-link defaults per Phase 25 D-77)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-163 (Crito-placeholder logo + sibling Pencil note for code milestone)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (Section/Header G0wNOc structure)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.3 sub-section-raster sub-rule
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md D-111 (TagFilter descendants override pattern — proven precedent)
  </read_first>
  <action>
    Pre-flight: `mcp__pencil__get_editor_state({ include_schema: false })` (D-166).

    Read 31-00-SUMMARY.md Task 1 section for the exact descendants property-path map. The map will look like (with actual IDs substituted from probe):
    ```
    descendants: {
      "<homeNavItemId>": { enabled: false },
      "<aboutNavItemId>": { enabled: false },
      "<servicesNavItemId>": { enabled: false },
      "<replacedSlotTextId>": { text: "Projects" },
      "<replacedSlotId>": { enabled: true }
    }
    ```
    (If 31-00 Task 1 selected Path C children-whole-replacement, use that map instead. Path B is the documented default.)

    Run `mcp__pencil__batch_design` with a single Insert operation:
    - Component reference: G0wNOc (Section/Header)
    - Parent: <HomepageId>
    - Position: first child (top of vertical-stack)
    - descendants: the property-path map from 31-00 T1
    - Sibling Pencil note ALSO INSERTED in same batch_design call:
      - Type: note (sticky note as sibling of the Header instance, NOT inside it — placed next to Homepage frame at the same y as Header instance)
      - Text: "v1.3 src/components/layout/Header.astro ships 'Joel Shinness' wordmark in logo slot (Bricolage Grotesque + uppercase + neobrutalist styling); code milestone wires Joel-brand wordmark + Joel-brand fonts at runtime. v2.0 .pen retains Crito-source logo placeholder per Phase 25 D-40 + PROJECT.md 'Joel-brand visual chrome — replaced when code milestones run on top of v2.0'. D-163."
    - SECOND sibling Pencil note for cross-page consistency call-out:
      - Text: "Header descendants override SCOPED to Homepage instance per D-162 — other reconstructed pages (FAQ b7Hgy / 404 csXky / Thank-you XsDab / Contact n0QqTd / Blog EDAf1 / Blog Post aQ8FL / Tag XXX / Projects (Y2isa proxy) / Project (cYlRH proxy) / Design system a0gRv) retain Crito-source 6-link nav per Phase 25 D-77 carry-forward. Cross-page consistency at COMPONENT level preserved — instance-level override only. Phase 30 a0gRv was FIRST cross-page consumer of Section/Header; Phase 31 is SECOND per ROADMAP success criterion 3."

    Verify via `mcp__pencil__batch_get({ nodeIds: [<HomepageId>, <HeaderInstanceId>], readDepth: 2 })`:
    - Homepage has 1 child (the Header instance) — count assertion
    - Header instance descendants includes the property-path overrides (visible in returned descendants object)
    - Header instance's effective rendered nav-items count = 4 (Blog / Projects / FAQ / Contact) post-override

    Capture the inserted Header instance ID and sibling note IDs into 31-01-SUMMARY.md `## Task 2`.
  </action>
  <acceptance_criteria>
    - get_editor_state confirmed active editor BEFORE mutation
    - batch_design Insert returned a valid Header instance ID
    - batch_get on Header instance shows descendants property with the override map applied
    - batch_get on Homepage shows 1 child (Header instance) at top of vertical-stack
    - 2 sibling Pencil notes created with the documented text (verifiable by reading sibling notes via batch_get)
    - 31-01-SUMMARY.md `## Task 2` records Header instance ID + 2 sibling-note IDs
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<HeaderInstanceId>"], readDepth: 2 }) shows descendants with at least 4 keys matching the property-path map; effective nav-item count is 4 post-override
  </verify>
  <done>
    Header instance is at top of Homepage with 4-link Joel nav + D-163 + cross-page consistency sibling notes attached; D-162 + D-163 + ROADMAP success criterion 3 satisfied at this layer.
  </done>
</task>

<task type="auto">
  <name>Task 3: Inline Hero composition per D-157 — display H1 + subtitle + dual CTAs with OPEN-25-01 white-stroke + decorative coral ellipse + Hero illustration STUB</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 0` Hero sub-section (H1 + subtitle + primary CTA + secondary CTA copy)
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 2 — hIWuC White-Stroke Override Path` (exact stroke + label IDs + descendants map for OPEN-25-01)
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 3 — ujMLJ Hero Subtree Role Map` (coral ellipse fill value + dimensions + Hero illustration leaf for STUB sizing)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-157 (Crito Hero layout + Joel content; dark navy bg #141f39ff or color-semantic-bg-dark; PJS 70 → type-semantic-display; Inter 18 → type-semantic-body-sm or prose-paragraph; dual CTAs Primary M7eUr + Secondary hIWuC with white-stroke override)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (M7eUr Primary Button + hIWuC Secondary Button IDs)
    - .planning/research/PEN-INVENTORY.md line 129 (coral ellipse 286×225)
    - .planning/research/PEN-INVENTORY.md lines 155-156 (PJS 70 mining + type-semantic-display token confirmation)
  </read_first>
  <action>
    Pre-flight: get_editor_state + active editor assertion (D-166).

    Run `mcp__pencil__batch_design` with a single Insert operation creating the Hero section as an inline frame (NOT a component instance — narrow-scoping per D-158 + Pitfall O5/O6):

    1. **Hero section parent frame** (Insert into <HomepageId>):
       - Type: frame
       - Position: second child of Homepage (after Header)
       - Width: 1440 (full-bleed page width)
       - Height: fit_content
       - Layout: horizontal (Crito Hero pattern: content-column left + decorative-anchor right per D-157 + 31-00 T3 layout-target fidelity)
       - Padding: derive from 31-00 T3 layout-target capture (default lean: space-semantic-section-y vertical + space-semantic-container-x horizontal)
       - Gap: derive from 31-00 T3 (default lean: space-semantic-stack-lg or larger between content + anchor)
       - AlignItems: center (vertical centering of content vs anchor)
       - Fill: `color-semantic-bg-dark` token reference (Crito dark navy #141f39ff per D-157)

    2. **Content column (left child of Hero parent)**:
       - Type: frame
       - Width: fit_content (or fixed 600 if D-157 visual-target fidelity warrants)
       - Layout: vertical
       - Gap: `space-semantic-stack-md` token (between H1, subtitle, CTA row)
       - Children:
         - **H1 text node**: text = (exact H1 string from 31-00 T0 Hero — "Your business needs more than software, it needs..."); typeStyle = `type-semantic-display` token (PJS 70 per Phase 23 + Phase 26 D-72 chain); fill = `color-semantic-text-on-dark` (white on dark-navy) or literal `#ffffffff`
         - **Subtitle text node**: text = (exact subtitle from 31-00 T0 — likely "Ready to get started?"); typeStyle = `type-semantic-body-sm` or `type-semantic-prose-paragraph` token (Inter 18 per D-157); fill = `color-semantic-text-on-dark` (with opacity 0.7 per D-157 Crito reference if supported by Pencil typeStyle; otherwise literal RGBA)
         - **CTA row frame**: horizontal layout, gap `space-semantic-inline-md`, containing two Button instance children:
           - **Primary CTA** = Insert M7eUr (Primitive/Button/Default) with descendants override `{ <labelTextId>: { text: "<Primary CTA label from 31-00 T0 — likely 'Let's talk!'>" } }` + (optional per § Discretion + Phase 25 D-43 + Phase 27 D-99 iconTrailing arrow-right precedent) descendants override on iconTrailing slot to lucide arrow-right Pattern A
           - **Secondary CTA** = Insert hIWuC (Primitive/Button/Secondary) with descendants override from 31-00 T2 exact map: `{ <strokeBearingId>: { stroke: "#ffffffff" }, <labelTextId>: { fill: "#ffffffff", text: "<Secondary CTA label — default 'View my projects' or '/projects' link per § Discretion if 31-00 T0 lacks v1.3 source>" } }` + optional icon fill override per 31-00 T2

    3. **Decorative anchor column (right child of Hero parent)**:
       - Type: frame
       - Width: fit_content
       - Layout: stack (overlapping or vertical with overlap — simple: just two layered children)
       - Children:
         - **Coral ellipse**: Pencil-native ellipse shape per § Discretion default + Phase 28 D-114 banner precedent; width 286, height 225; fill = `color-semantic-decorative-coral` token if available, else literal `#f88d6dff` from 31-00 T3 (REUSE existing token surface per D-164 zero new tokens default)
         - **Hero illustration STUB**: image-fill rectangle; width matching Crito Hero illustration sizing from 31-00 T3 layout-target capture (default fallback: 400 × 400); fill = solid placeholder color (e.g., `color-semantic-bg-surface` neutral) OR image-fill with placeholder pattern; sibling Pencil note attached: "Hero illustration STUB per D-157 — v1.3 ships no Hero illustration (bento-grid outcomes are the visual); Crito ujMLJ Hero illustration raster locked in place at ujMLJ per § 3.3 + D-155 LOCK (Task 11); code milestone wires Joel-brand asset at runtime per PROJECT.md Joel-brand visual chrome Out of Scope. Flagged as OPEN-31-04 Hero illustration STUB labeling per D-167."

    4. **Sibling Pencil note on Hero section parent**:
       - Text: "Hero section inline composition per D-157 (NOT a Section/Hero component — narrow-scoping per Pitfall O5/O6 + D-158/-159/-160 chain). Crito Hero layout (ujMLJ Hero subtree pattern) + Joel content (v1.3 verbatim per D-98 chain). Dark-bg navy color-semantic-bg-dark + dual CTAs Primary M7eUr + Secondary hIWuC with OPEN-25-01 white-stroke + white-label descendants override carve-out (Phase 25 D-43 closure). v1.3 Hero bento-grid asymmetric layout NOT inherited — bento is Joel-brand visual chrome per PROJECT.md Out of Scope. Per-section fidelity label: APPROXIMATE (declared deviations re Hero illustration STUB + ujMLJ Hero illustration raster locked NOT migrated; coral ellipse EXACT 286×225 fill token-reused). Pairing target: ujMLJ Hero subtree per § 3.4 step 4 (Plan 31-01 Task 10 calibration gate)."

    Verify via batch_get on Hero section parent (readDepth:3): 2 column children + content column has H1 + subtitle + CTA row; CTA row has 2 Button instances; right column has ellipse + STUB rect + sibling note. Capture all IDs in 31-01-SUMMARY.md `## Task 3`.
  </action>
  <acceptance_criteria>
    - Hero section parent inserted as second child of Homepage frame (vertical position 2/7)
    - Content column contains text H1 string from 31-00 T0 (grep-verifiable on summary)
    - Primary CTA M7eUr instance present with label override per 31-00 T0
    - Secondary CTA hIWuC instance present with descendants override applying `#ffffffff` stroke + label fill per 31-00 T2 — verifiable by batch_get on the instance showing descendants object with those property-path keys
    - Coral ellipse present with width/height near 286×225 + fill referencing coral token or literal #f88d6dff
    - Hero illustration STUB rect present with sibling Pencil note containing "OPEN-31-04" identifier
    - Hero section sibling Pencil note attached containing "D-157" identifier + APPROXIMATE fidelity label keyword
    - 31-01-SUMMARY.md `## Task 3` records all created IDs + per-section fidelity label = APPROXIMATE
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<HeroSectionParentId>"], readDepth: 3 }) returns: 2 column frames + content column has 3 children (H1 text + subtitle text + CTA row); CTA row has 2 button instances; Secondary button descendants property contains exactly the override map from 31-00 T2 (stroke "#ffffffff" + label fill "#ffffffff")
  </verify>
  <done>
    Hero section is composed inline with Joel content + Crito layout + dual CTAs + OPEN-25-01 white-stroke RESOLVED at instance time + STUB Hero illustration + LOCK target documented for Task 11.
  </done>
</task>

<task type="auto">
  <name>Task 4: Inline Services composition per D-158 — section H2 + 3-up Compound/Card grid with per-card descendants overrides</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 0` Services sub-section (H2 + 3 category titles + 3 bodies + per-category CTAs)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-158 (3-column Compound/Card grid inline composition; layout EXACT + illustrations STUB)
    - .planning/phases/25-section-compound-components/25-03-SUMMARY.md (Compound/Card t40xct + 4-slot signature: image / title / body / footer-actions)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md D-114 (3-up Card grid pattern at Phase 28 Blog index — proven precedent)
    - .planning/research/PEN-INVENTORY.md line 170 (heading size mining — Crito section heading-1 size 48)
  </read_first>
  <action>
    Pre-flight: get_editor_state (D-166).

    Run `mcp__pencil__batch_design` Insert into <HomepageId> (third child position):

    1. **Services section parent frame**:
       - Width: 1440 (or 1200 content-width centered — Plan 31-01 plan-execution decides; default 1440 with internal content-width 1200 padding-row)
       - Layout: vertical
       - Gap: `space-semantic-stack-lg`
       - Padding: `space-semantic-section-y` vertical + `space-semantic-container-x` horizontal
       - AlignItems: center
       - Fill: `color-semantic-bg-page` (white default — Crito section rhythm dark/light alternation per D-156 vibe carry-forward)

    2. **Section H2 text node** (first child):
       - Text: (exact H2 from 31-00 T0 Services — likely "Solutions That Fit" or "Solutions")
       - typeStyle: `type-semantic-heading-1` token (size 48 per Phase 26 D-72 + PEN-INVENTORY line 170 mining)
       - Fill: `color-semantic-text-default` token
       - AlignText: center

    3. **3-up Card grid frame** (second child):
       - Layout: horizontal
       - Gap: `space-semantic-stack-md` between cards
       - Width: 1200 (content-width per Phase 28 D-114 + Phase 29 ProjectCard grid precedent)
       - AlignItems: stretch (equal heights)
       - Children: 3 × Compound/Card (t40xct) Insert with per-card descendants overrides:
         - **Card 1 — AI Development** (or v1.3 category 1 exact title from 31-00 T0):
           - descendants override map (with actual slot child IDs from 25-03-SUMMARY):
             - image-slot: enabled true with STUB image-fill rectangle (placeholder color via color-semantic-bg-surface OR neutral fill) + sibling Pencil note inside card "Services illustration STUB per D-158 — v1.3 ships isometric illustrations; Joel-brand asset OUT-OF-SCOPE for v2.0 per PROJECT.md; code milestone wires at runtime. Flagged as OPEN-31-01 per D-167."
             - title-slot text: (category 1 title from 31-00 T0)
             - body-slot text: (category 1 body from 31-00 T0)
             - footer-actions-slot: M7eUr Primary Button with descendants override `{ <labelTextId>: { text: "Discuss Your Project" } }` (or per-category CTA from 31-00 T0 if 31-00 T0 surfaces per-category CTAs)
         - **Card 2 — Automation** (or v1.3 category 2): same structure as Card 1 with category 2 content from 31-00 T0
         - **Card 3 — Web Apps** (or v1.3 category 3): same structure as Card 1 with category 3 content from 31-00 T0

    4. **Sibling Pencil note on Services section parent**:
       - Text: "Services section inline composition per D-158 (NOT a Section/Services component — narrow-scoping per Pitfall O5/O6 + D-159/-160/-161 chain; Services is Homepage-only consumer). 3-up Compound/Card t40xct grid per D-158 + Phase 28 D-114 + Phase 29 ProjectCard grid precedent; Card is canonical 3-up container across Phase 28 BlogCard + Phase 29 ProjectCard + Phase 30 Card gallery + Phase 31 Services. v1.3 isometric illustrations NOT inherited — Joel-brand asset Out of Scope. Per-section fidelity label: layout EXACT + illustrations STUB. Three OPEN-31-01 Services illustration STUB flags raised at PEN-INVENTORY per D-167."

    Verify via batch_get on Services section parent (readDepth:3): H2 + 3-Card grid; each Card has title-slot + body-slot text content matching 31-00 T0 categories; image-slot is STUB with sibling note; footer-actions-slot has Button instance. Capture all IDs in 31-01-SUMMARY.md `## Task 4`.
  </action>
  <acceptance_criteria>
    - Services section is third child of Homepage
    - Section H2 text matches 31-00 T0 Services H2 string
    - Exactly 3 Compound/Card (t40xct) instances in horizontal-layout grid
    - Each Card has title-slot + body-slot overrides with v1.3 content from 31-00 T0
    - Each Card has image-slot STUB sibling Pencil note containing "OPEN-31-01"
    - Services section sibling note contains "D-158" + "layout EXACT" + "illustrations STUB" keywords
    - 31-01-SUMMARY.md `## Task 4` records Services section ID + 3 Card instance IDs + per-section fidelity label
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<ServicesSectionId>"], readDepth: 3 }) returns: H2 + grid frame with 3 Card instance children; each Card descendants override has at least title-slot + body-slot + image-slot + footer-actions-slot keys
  </verify>
  <done>
    Services section composed inline with 3-up Card grid validating Phase 25 D-49 single-Card claim at fourth cross-phase consumer (Services 3 + Phase 27 sidebar 1 + Phase 28 RelatedPosts 3 + Phase 29 ProjectCard sibling pattern + Phase 30 gallery 1).
  </done>
</task>

<task type="auto">
  <name>Task 5: Inline Process composition per D-159 — section H2 + 5-step vertical timeline</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 0` Process sub-section (H2 + 5 step titles + 5 step bodies)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-159 (5-step vertical timeline; layout EXACT + illustrations STUB; vertical-stack default per § Discretion)
    - .planning/research/PEN-INVENTORY.md line 170 (heading size mining)
  </read_first>
  <action>
    Pre-flight: get_editor_state (D-166).

    Run `mcp__pencil__batch_design` Insert into <HomepageId> (fourth child position):

    1. **Process section parent frame**:
       - Width: 1440 with content-width 1200 padding-row
       - Layout: vertical
       - Gap: `space-semantic-stack-lg`
       - Padding: `space-semantic-section-y` vertical + `space-semantic-container-x` horizontal
       - AlignItems: center
       - Fill: `color-semantic-bg-surface` (alternate light-bg from Services — section rhythm) OR `color-semantic-bg-page` per Plan 31-01 plan-execution visual-density decision

    2. **Section H2 text node** (first child):
       - Text: (exact H2 from 31-00 T0 Process — likely "How We Work Together")
       - typeStyle: `type-semantic-heading-1` token
       - AlignText: center

    3. **5-step timeline frame** (second child):
       - Layout: vertical (default per § Discretion clean linear scroll; horizontal-grid 2x3 acceptable alternative per § Discretion — vertical is default)
       - Gap: `space-semantic-stack-lg` between step blocks
       - Width: 1200
       - Children: 5 × step block frames (one per step):
         - **Step block** (each):
           - Layout: horizontal (number marker LEFT + step content RIGHT) OR vertical with number above title; default lean: horizontal
           - Gap: `space-semantic-inline-md`
           - Children:
             - **Number marker text/frame**: text = (step number 1-5); typeStyle = `type-semantic-display` token OR styled differently (Plan 31-01 plan-execution decides; default = numeric digit in display tier for visual weight)
             - **Step content column**: vertical layout, gap `space-semantic-stack-sm`, with:
               - Step title text: exact title from 31-00 T0; typeStyle `type-semantic-heading-3` token (Phase 28 D-105 + Phase 26 D-72 chain heading-3)
               - Step body text: exact body from 31-00 T0; typeStyle `type-semantic-prose-paragraph` token
               - Step illustration STUB: image-fill rectangle placeholder + sibling Pencil note "Process step illustration STUB per D-159 — v1.3 ships isometric illustrations; Joel-brand asset OUT-OF-SCOPE for v2.0; code milestone wires at runtime. Flagged as OPEN-31-02 per D-167."

    4. **Sibling Pencil note on Process section parent**:
       - Text: "Process section inline composition per D-159 (NOT a Section/Process component — narrow-scoping per Pitfall O5/O6 + D-158/-160/-161 chain; Process is Homepage-only consumer). 5-step vertical timeline pattern per § Discretion default (clean linear scroll); horizontal-grid 2x3 not adopted at default. v1.3 isometric illustrations NOT inherited — Joel-brand asset Out of Scope. Per-section fidelity label: layout EXACT + illustrations STUB. Five OPEN-31-02 Process illustration STUB flags raised at PEN-INVENTORY per D-167."

    Verify via batch_get: H2 + 5 step blocks; each step has number marker + title + body + STUB illustration. Capture all IDs in 31-01-SUMMARY.md `## Task 5`.
  </action>
  <acceptance_criteria>
    - Process section is fourth child of Homepage
    - Section H2 text matches 31-00 T0 Process H2
    - Exactly 5 step blocks in vertical timeline (count assertion via batch_get on timeline frame children length == 5)
    - Each step has number marker + title + body + STUB illustration with sibling note containing "OPEN-31-02"
    - Process section sibling note contains "D-159" + "layout EXACT" + "illustrations STUB"
    - 31-01-SUMMARY.md `## Task 5` records Process section ID + 5 step block IDs + per-section fidelity label
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<ProcessTimelineFrameId>"], readDepth: 0 }) returns children.length == 5
  </verify>
  <done>
    Process section composed inline with 5-step vertical timeline validating D-159 + § Discretion default; all 5 step illustrations OPEN-31-02 flagged for PEN-INVENTORY at Task 12.
  </done>
</task>

<task type="auto">
  <name>Task 6: Inline About composition per D-160 — 2-column headshot + bio + CTA</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 0` About sub-section (H2 + bio paragraphs + credibility stats + CTA copy + CTA href)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-160 (2-column About; headshot STUB; About-on-homepage no separate page per PROJECT.md)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (M7eUr Primary Button)
  </read_first>
  <action>
    Pre-flight: get_editor_state (D-166).

    Run `mcp__pencil__batch_design` Insert into <HomepageId> (fifth child position):

    1. **About section parent frame**:
       - Width: 1440 with content-width 1200 padding-row
       - Layout: vertical
       - Gap: 0 (the 2-column content row owns its own gap)
       - Padding: `space-semantic-section-y` vertical + `space-semantic-container-x` horizontal
       - AlignItems: center
       - Fill: alternate light/dark per § Discretion section rhythm (default: `color-semantic-bg-page`)

    2. **2-column content row** (only child):
       - Layout: horizontal
       - Width: 1200
       - Gap: `space-semantic-stack-lg` (or `space-semantic-section-gap-lg` if surfaced) between left + right
       - AlignItems: center
       - Children:
         - **Headshot column** (LEFT):
           - Type: frame
           - Width: 480 (default candidate per § Discretion)
           - Height: 360 (default candidate per § Discretion)
           - Fill: solid-fill rectangle (default per § Discretion — placeholder-honest; visually neutral surface color via `color-semantic-bg-surface` token)
           - Sibling Pencil note: "About headshot STUB per D-160 — v1.3 ships Joel headshot; Joel-brand asset OUT-OF-SCOPE for v2.0 per PROJECT.md; code milestone wires Joel headshot at runtime. Flagged as OPEN-31-03 per D-167."
         - **Content column** (RIGHT):
           - Type: frame
           - Layout: vertical
           - Gap: `space-semantic-stack-md`
           - Children:
             - **H2 text**: text = (exact H2 from 31-00 T0 About — likely "About Me"); typeStyle = `type-semantic-heading-1` token
             - **Bio paragraphs** (one or more text nodes per 31-00 T0): typeStyle = `type-semantic-prose-paragraph` token; fill = `color-semantic-text-default` token
             - **Credibility stats** (inline composition — 2-column horizontal frame with stat × 2 from 31-00 T0 — likely "15+ Years" + "200+ Students"):
               - Layout: horizontal
               - Gap: `space-semantic-stack-md`
               - Per-stat frame: vertical with large-number text (typeStyle type-semantic-display or type-semantic-heading-1) + label text (typeStyle type-semantic-body-sm)
             - **CTA Primary Button** (Insert M7eUr with descendants override):
               - Label override text: (exact CTA copy from 31-00 T0 — likely "Let's talk")
               - href documented in sibling note: (exact href from 31-00 T0 — likely /contact pointing at Phase 27 n0QqTd)

    3. **Sibling Pencil note on About section parent**:
       - Text: "About section inline composition per D-160 (NOT a Section/About component — narrow-scoping per Pitfall O5/O6 + D-158/-159/-161 chain; About is Homepage-only consumer per PROJECT.md 'Homepage About section — single-page feel, no separate page' Key Decision). 2-column horizontal-layout: headshot STUB LEFT (480×360 placeholder per § Discretion) + content column RIGHT (H2 + bio + credibility stats inline + Primary CTA to /contact n0QqTd). v1.3 Joel headshot NOT inherited — Joel-brand asset Out of Scope. Per-section fidelity label: layout EXACT + headshot STUB. OPEN-31-03 About headshot STUB flag raised at PEN-INVENTORY per D-167."

    Verify via batch_get: 2-column row with headshot LEFT + content column RIGHT; content column has H2 + bio + stats inline + Primary Button. Capture all IDs in 31-01-SUMMARY.md `## Task 6`.
  </action>
  <acceptance_criteria>
    - About section is fifth child of Homepage
    - 2-column horizontal row with exactly 2 column children
    - Headshot rectangle dimensions match plan-execution defaults (480 × 360) or 31-00 T0 surfaces alternative
    - Headshot has sibling Pencil note containing "OPEN-31-03"
    - Content column has H2 + bio paragraph(s) + credibility stats inline composition + Primary Button instance
    - About section sibling note contains "D-160" + "layout EXACT" + "headshot STUB" + "n0QqTd"
    - 31-01-SUMMARY.md `## Task 6` records About section ID + headshot ID + content column children IDs + Button instance ID + per-section fidelity label
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<About2ColRowId>"], readDepth: 0 }) returns children.length == 2 (headshot + content column)
  </verify>
  <done>
    About section composed inline with 2-column + headshot STUB + Primary CTA wired to /contact; PROJECT.md "Homepage About section — single-page feel, no separate page" honored as v2.0 Pencil composition.
  </done>
</task>

<task type="auto">
  <name>Task 7: Insert Section/CTA (Hs5rc) instance per D-161 — SECOND cross-phase consumer (Phase 27 Plan 27-01 Thank-you was first)</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 0` ContactSection sub-section (headline + body + Section/CTA descendants override copy)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-161 (Section/CTA Hs5rc at bottom; v1.3 ContactSection NOT instanced; sibling note documents code-milestone decision)
    - .planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md (Section/CTA Hs5rc 3-slot signature: headline / body / actions per D-78)
    - .planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md (Phase 27 Plan 27-01 FIRST cross-phase Section/CTA consumer — pattern reference)
  </read_first>
  <action>
    Pre-flight: get_editor_state (D-166).

    Run `mcp__pencil__batch_design` Insert into <HomepageId> (sixth child position):

    1. **Section/CTA (Hs5rc) instance** with descendants overrides per D-161 + Phase 26 D-78 3-slot signature:
       - Component reference: Hs5rc
       - Parent: <HomepageId>
       - Position: sixth child (after About, before Footer)
       - descendants override map (slot child IDs from 26-00-SUMMARY):
         - headline-slot text: (exact headline from 31-00 T0 ContactSection or v1.3 chain — default lean: "Ready to get started?" per D-161 Hero CTA copy carry-forward; alternative: "Let's talk")
         - body-slot text: (exact body from 31-00 T0 ContactSection — 48-hour body line)
         - actions-slot Primary Button label override: "Let's talk" (or v1.3 CTA copy from 31-00 T0)
         - actions-slot Primary Button href documented in sibling note: /contact (Phase 27 n0QqTd)

    2. **Sibling Pencil note on Section/CTA instance** (per D-93 belt-and-suspenders chain + D-161):
       - Text: "Section/CTA Hs5rc instance per D-161 — SECOND cross-phase consumer (FIRST was Phase 27 Plan 27-01 Thank-you; validates Phase 26 D-78 broad-scoping prediction). Replaces v1.3 src/components/homepage/ContactSection.astro full 8-field form on-homepage section — full form lives at /contact frame n0QqTd (Phase 27 Plan 27-02 reconstructed Contact). Code milestone decides architecture: extract v1.3 ContactSection to /contact route (matching Phase 27 n0QqTd reconstructed Contact frame) OR preserve as homepage section. Primary CTA wired to /contact n0QqTd. Per-section fidelity label: EXACT (Section/CTA is fully-factored library component from Phase 26)."

    Verify via batch_get on Section/CTA instance (readDepth:2): descendants property contains headline-slot + body-slot + actions-slot override keys with content matching plan. Capture IDs in 31-01-SUMMARY.md `## Task 7`.
  </action>
  <acceptance_criteria>
    - Section/CTA Hs5rc instance is sixth child of Homepage
    - Descendants property contains headline-slot + body-slot + actions-slot override keys (verifiable in batch_get descendants object)
    - Sibling note contains "D-161" + "SECOND cross-phase consumer" + "Phase 27 Plan 27-01 Thank-you" + "n0QqTd" keywords
    - 31-01-SUMMARY.md `## Task 7` records Section/CTA instance ID + per-section fidelity label = EXACT
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<SectionCTAInstanceId>"], readDepth: 2 }) returns descendants object with at least 3 keys (headline/body/actions slots)
  </verify>
  <done>
    Section/CTA closing instance is in place; Phase 26 D-78 broad-scoping prediction validated at 2 cross-phase consumers; D-161 v1.3 ContactSection architecture decision documented for code milestone.
  </done>
</task>

<task type="auto">
  <name>Task 8: Insert Section/Footer (Xs0Hs) — no override per D-77 carry-forward</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-77 carry-forward (Section/Footer instances stay no-override default)
    - .planning/phases/25-section-compound-components/25-02-SUMMARY.md (Section/Footer Xs0Hs structure — 3 link columns + Instagram + Substack)
  </read_first>
  <action>
    Pre-flight: get_editor_state (D-166).

    Run `mcp__pencil__batch_design` Insert into <HomepageId> (seventh / final child position):

    1. **Section/Footer (Xs0Hs) instance** — NO descendants override per D-77 carry-forward:
       - Component reference: Xs0Hs
       - Parent: <HomepageId>
       - Position: seventh child (last in vertical-stack)
       - descendants: (none — defaults inherited from component)

    Verify via batch_get: Homepage now has exactly 7 children in order Header → Hero → Services → Process → About → Section/CTA → Footer. Capture Footer instance ID in 31-01-SUMMARY.md `## Task 8`.
  </action>
  <acceptance_criteria>
    - Section/Footer Xs0Hs instance is seventh and final child of Homepage
    - No descendants override applied
    - mcp__pencil__batch_get({ nodeIds: ["<HomepageId>"], readDepth: 1 }) returns children.length == 7
    - 31-01-SUMMARY.md `## Task 8` records Footer instance ID + per-section fidelity label = EXACT
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<HomepageId>"], readDepth: 1 }) returns 7 children in order Header / Hero / Services / Process / About / Section-CTA / Footer
  </verify>
  <done>
    Homepage frame fully composed with 7 children; ROADMAP success criterion 3 satisfied (Section/Header + Section/Footer instances at top + bottom — second cross-page consumer after Phase 30 a0gRv).
  </done>
</task>

<task type="auto">
  <name>Task 9: snapshot_layout regression at document root</name>
  <read_first>
    - .planning/research/PITFALLS.md Pitfall 6 (snapshot_layout text-clipping false-positive — documented benign per Phase 24-30 precedent)
    - .planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md (Phase 30 text-clipping false-positives baseline reference)
  </read_first>
  <action>
    Run `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root.

    Expected outcome (per Phase 24-30 chain precedent): `"No layout problems."` OR a list of text-clipping false-positives concentrated in the new Hero / Services / Process / About sections (PJS 70 H1 + heading-1 H2s + prose-paragraph bodies likely to trigger Pitfall 6 false-positives).

    Document the snapshot_layout return verbatim in 31-01-SUMMARY.md `## Task 9 — snapshot_layout`. For each reported problem:
    - Categorize as text-clipping false-positive (Pitfall 6 benign) OR real structural issue
    - If real structural issue surfaces (overflow, overlap, broken auto-layout): halt + surface to user + diagnose; DO NOT proceed to calibration gate (Task 10)
    - If all reported problems are text-clipping false-positives: document each with node ID + reason categorization + benign citation

    The "No layout problems." case is the green-path outcome; document as a single line.
  </action>
  <acceptance_criteria>
    - snapshot_layout return captured verbatim in 31-01-SUMMARY.md `## Task 9`
    - Each reported problem categorized as benign (text-clipping false-positive per Pitfall 6) OR real (halt path)
    - If green path: Plan 31-01 cleared to proceed to Task 10 calibration gate
    - If real issue: plan halts at Task 9, no Task 10 attempt until resolved
  </acceptance_criteria>
  <verify>
    grep "snapshot_layout" .planning/phases/31-homepage-reconstruction/31-01-SUMMARY.md returns at least 1 line; if problems reported, each has a categorization label
  </verify>
  <done>
    Homepage frame structurally clean (or only Pitfall-6-benign text-clipping false-positives reported); calibration gate cleared to proceed.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 10: Single calibration AskUserQuestion gate per D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch</name>
  <read_first>
    - .planning/research/CALIBRATION-PROTOCOL.md § 4.5 (joel-only branch AskUserQuestion format — token-usage description default)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.4 step 4 (crito-source branch Hero side-by-side per-step script)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.5 (crito-source description identifier convention)
    - .planning/research/CALIBRATION-PROTOCOL.md § 4.6 (joel-only description identifier convention)
    - .planning/research/CALIBRATION-PROTOCOL.md § 6.4 (Tier-1/Tier-2 stale-cache fallback)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-155 (HYBRID sub-branch — two artifacts in single AskUserQuestion)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-165 (single gate at plan close)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md § Claude's Discretion "Plan 31-01 calibration gate description ordering" (section-by-section default)
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 3` (ujMLJ Hero subtree calibration pairing nodeId)
    - .planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md (Phase 30 joel-only calibration precedent — FIFTH production use; Phase 31 is SIXTH)
  </read_first>
  <what-built>
    7-section Homepage page frame in design/Crito.pen:
    - Header instance with D-162 4-link descendants override (Blog / Projects / FAQ / Contact)
    - Hero inline composition per D-157 (Crito Hero layout + Joel content + dual CTAs with OPEN-25-01 white-stroke override + coral ellipse + Hero illustration STUB)
    - Services inline composition per D-158 (3-up Compound/Card grid + STUB illustrations)
    - Process inline composition per D-159 (5-step vertical timeline + STUB illustrations)
    - About inline composition per D-160 (2-column headshot STUB + bio + credibility stats + Primary CTA to /contact)
    - Section/CTA instance per D-161 (SECOND cross-phase consumer; replaces v1.3 ContactSection; Primary CTA to /contact n0QqTd)
    - Footer instance per D-77 carry-forward (no override)
    Library count unchanged (19); token surface unchanged (107). Zero [FLAT] markers in reconstructed frame.
  </what-built>
  <how-to-verify>
    Call `mcp__pencil__get_screenshot({ nodeId: "<HomepageId>" })` to capture the full Homepage frame for inline review. If the tall page frame triggers stale-cache misbehavior per OPEN-26-02, use § 6.4 Tier-1 (cross-row Update — bump a sibling note's position by 1px) then re-fetch; if still misbehaving, fall back to Tier-2 (ask user to verify in their editor).

    Call `mcp__pencil__get_screenshot({ nodeId: "<HeroSectionParentId>" })` to capture reconstructed Hero subtree.

    Call `mcp__pencil__get_screenshot({ nodeId: "<ujMLJ Hero subtree id from 31-00 T3>" })` to capture Crito Hero pairing target per § 3.4 step 4.

    Present an AskUserQuestion to the user with the following structure (description ordering = section-by-section per § Discretion default):

    ---

    **Phase 31 Plan 31-01 — Homepage Page Frame Calibration (D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch — SIXTH joel-only production use + FIRST D-155 HYBRID consumer in v2.0)**

    Reviewing 7 sections in scroll order. Per-section fidelity proposals + paired evidence below.

    **§ 4.5 joel-only token-usage description (covers all 7 sections — token bindings vs `_Tokens & Foundations` RpGbe per D-62):**

    1. **Header (overridden instance) — fidelity proposal EXACT**
       Token bindings: Section/Header (G0wNOc) baseline + D-162 4-link descendants override (Home/About/Services HIDE; replace slot text "Projects"); logo slot Crito-placeholder retained per D-163 + sibling note. Cross-page consistency proof: SECOND cross-page consumer of Section/Header after Phase 30 a0gRv per ROADMAP success criterion 3.

    2. **Hero (inline composition) — fidelity proposal APPROXIMATE**
       (Declared deviations: Hero illustration STUB per D-157; coral ellipse EXACT 286×225.)
       Token bindings: type-semantic-display (PJS 70 H1) + type-semantic-body-sm (Inter 18 subtitle) + color-semantic-bg-dark (navy bg) + color-semantic-decorative-coral (ellipse) + space-semantic-section-y/container-x; Primary M7eUr + Secondary hIWuC with OPEN-25-01 white-stroke + white-label descendants override (#ffffffff literal per Phase 25 D-43 carve-out closure).

    3. **§ 3.4 step 4 Hero side-by-side image pair** (inline below this description):
       - **Reconstructed Hero** [identifier: `31-homepage-hero--reconstructed`]
       - **ujMLJ Hero subtree** [identifier: `31-homepage-hero--side-by-side`]
       Pairing for visual fidelity check per D-155 HYBRID Hero exception.

    4. **Services (inline composition) — fidelity proposal layout EXACT + illustrations STUB**
       Token bindings: type-semantic-heading-1 (H2 size 48) + Compound/Card (t40xct) × 3 with descendants overrides + image-slot STUB (3 OPEN-31-01 flags) + footer-actions-slot M7eUr Primary Button. Card cross-phase consumer count after this plan: Phase 27 sidebar 1 + Phase 28 RelatedPosts 3 + Phase 29 ProjectCard sibling pattern + Phase 30 gallery 1 + Phase 31 Services 3 = robust validation of Phase 25 D-49 single-Card claim.

    5. **Process (inline composition) — fidelity proposal layout EXACT + illustrations STUB**
       Token bindings: type-semantic-heading-1 (H2) + 5 step blocks with number marker (type-semantic-display) + title (type-semantic-heading-3) + body (type-semantic-prose-paragraph) + step illustration STUB (5 OPEN-31-02 flags).

    6. **About (inline composition) — fidelity proposal layout EXACT + headshot STUB**
       Token bindings: type-semantic-heading-1 (H2 "About Me") + type-semantic-prose-paragraph (bio) + credibility stats inline (type-semantic-display + type-semantic-body-sm) + M7eUr Primary Button → /contact n0QqTd; headshot 480×360 STUB rect (1 OPEN-31-03 flag).

    7. **closing-CTA (Section/CTA Hs5rc instance) — fidelity proposal EXACT**
       Section/CTA fully-factored library component (Phase 26 D-78 broad-scoping). SECOND cross-phase consumer after Phase 27 Plan 27-01 Thank-you per D-161. Primary CTA → /contact n0QqTd. v1.3 ContactSection on-homepage architecture sibling-noted for code milestone.

    8. **Footer (Section/Footer Xs0Hs instance) — fidelity proposal EXACT** (no override per D-77 carry-forward)

    **Token surface drift:** 0 (107 unchanged from Phase 30 close).
    **Library count drift:** 0 (19 unchanged from Phase 30 close).
    **PEN-INVENTORY extensions ready post-APPROVE:** new Homepage row + ujMLJ line 65 status_counts LOCK + Variant Evidence Phase 31 + new OPEN-31-NN section + OPEN-25-01/-23-09 RESOLVED + OPEN-23-04 LAST-VISIT.

    **POST-APPROVE LOCK action (Task 11):** sibling Pencil note ONLY on ujMLJ Hero illustration parent — raster stays enabled:true per § 3.3 sub-section-raster sub-rule + Pitfall 4 (NEVER hide before APPROVE).

    **Options:**
    - **APPROVE** — fidelity labels accepted; Plan 31-01 closes; proceed to Task 11 LOCK + Task 12 PEN-INVENTORY extensions
    - **REVISE** — surface what to change (which section / what fidelity label / what override) and Plan 31-01 cycles back
    - **GAP** — surface a missing piece that needs to be addressed before approval

    ---

    description identifiers per § 3.5 + § 4.6: `31-homepage--token-usage` + `31-homepage-hero--side-by-side` (IDENTIFIERS ONLY, NOT disk-write paths per OPEN-23-01 substitution).
  </how-to-verify>
  <resume-signal>
    User selects APPROVE / REVISE / GAP. If APPROVE: proceed to Task 11. If REVISE: cycle Plan 31-01 with surfaced changes. If GAP: document gap as OPEN-31-NN and decide whether to address in-plan or defer.
  </resume-signal>
  <acceptance_criteria>
    - get_screenshot called for Homepage frame + Hero subtree + ujMLJ Hero subtree (3 calls)
    - AskUserQuestion presented to user with single description block carrying BOTH § 4.5 token-usage format AND § 3.4 step 4 Hero side-by-side image pair (D-155 HYBRID)
    - Description includes per-section fidelity proposals for 7 sections (Header EXACT + Hero APPROXIMATE + Services EXACT layout + STUB illustrations + Process EXACT layout + STUB illustrations + About EXACT layout + STUB headshot + closing-CTA EXACT + Footer EXACT)
    - Description identifiers logged per § 3.5 + § 4.6 conventions (NOT disk paths per OPEN-23-01)
    - Cross-page consistency proof call-out included per § Claude's Discretion (Phase 30 was FIRST; Phase 31 is SECOND Section/Header + Section/Footer consumer)
    - 31-01-SUMMARY.md `## Task 10` records AskUserQuestion transcript reference (including user response) + selected outcome (APPROVE / REVISE / GAP)
  </acceptance_criteria>
  <done>
    Calibration gate complete; user APPROVE/REVISE/GAP outcome captured; on APPROVE → proceed to Task 11.
  </done>
</task>

<task type="auto">
  <name>Task 11 (post-APPROVE only): LOCK action — sibling Pencil note on ujMLJ Hero illustration parent</name>
  <read_first>
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.3 sub-section-raster sub-rule (LOCK = sibling Pencil note, NOT enabled:false)
    - .planning/research/PITFALLS.md Pitfall 4 (NEVER hide raster before APPROVE — applies here; Task 10 APPROVE is the gate)
    - .planning/phases/31-homepage-reconstruction/31-00-SUMMARY.md `## Task 3` (Hero illustration leaf node parent ID for sibling note attachment)
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-155 + D-167 (LOCK action operational sequence)
  </read_first>
  <action>
    GATE CHECK: this task ONLY runs if Task 10 returned APPROVE. If REVISE or GAP, halt this task — Task 11 does not execute until APPROVE is reached.

    Pre-flight: get_editor_state (D-166).

    Run `mcp__pencil__batch_design` with a single Insert sibling Pencil note operation:
    - Type: note (sticky note as sibling of Hero illustration leaf node — i.e., child of <HeroIllustrationParentId> from 31-00 T3)
    - Parent: <HeroIllustrationParentId> (from 31-00 T3 Hero subtree role map)
    - Position: appended as last child of parent (alongside the existing Hero illustration leaf raster node)
    - Text: "LOCKED — cross-reference for Phase 31 Homepage Hero per D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch. Raster stays enabled:true (NEVER hidden per § 3.3 sub-section-raster sub-rule + Pitfall 4). Reconstructed Hero lives at top-level Homepage frame <HomepageId> per Plan 31-01 Task 3 inline composition (D-157 Crito-Hero-layout + Joel content). Hero illustration STUB inside reconstructed Homepage Hero is image-fill rectangle placeholder per OPEN-31-04 — code milestone wires Joel-brand or Crito-aligned asset at runtime. This raster sibling note is the LOCK action per CALIBRATION-PROTOCOL § 3.3 (raster preserved in-place for cross-reference; NOT migrated to reconstructed frame; NOT hidden)."

    CRITICAL: do NOT pass enabled:false on the Hero illustration leaf raster. The raster's `enabled` property MUST stay `true`. The LOCK action is exclusively a sibling-note addition.

    Verify via `mcp__pencil__batch_get({ nodeIds: [<HeroIllustrationLeafId>, <HeroIllustrationParentId>], readDepth: 1 })`:
    - <HeroIllustrationLeafId>.enabled == true (unchanged — assertion match)
    - <HeroIllustrationParentId>.children now includes the new sibling note (length +1 from baseline at 31-00 T3 capture)
    - Sibling note text matches (grep-verifiable: "LOCKED — cross-reference for Phase 31")

    Capture sibling note ID in 31-01-SUMMARY.md `## Task 11`.
  </action>
  <acceptance_criteria>
    - This task executes ONLY if Task 10 returned APPROVE
    - get_editor_state confirmed active editor BEFORE mutation
    - batch_design Insert created sibling Pencil note as child of <HeroIllustrationParentId>
    - Note text contains "LOCKED" + "Phase 31" + "D-155" + "§ 3.3" identifiers
    - <HeroIllustrationLeafId>.enabled remains true (verified by batch_get post-mutation)
    - 31-01-SUMMARY.md `## Task 11` records sibling note ID + confirmation that raster.enabled is true (NOT hidden)
  </acceptance_criteria>
  <verify>
    mcp__pencil__batch_get({ nodeIds: ["<HeroIllustrationLeafId>"], readDepth: 0 }) returns enabled:true (UNCHANGED from 31-00 T3 baseline)
  </verify>
  <done>
    LOCK action complete per § 3.3 sub-section-raster sub-rule; raster preserved as cross-reference; Pitfall 4 honored (raster never hidden).
  </done>
</task>

<task type="auto">
  <name>Task 12: PEN-INVENTORY extensions per D-167</name>
  <read_first>
    - .planning/phases/31-homepage-reconstruction/31-CONTEXT.md D-167 (full extension specification)
    - .planning/research/PEN-INVENTORY.md (current state — Phase 30 close baseline; line 65 ujMLJ row + line 77 Design system Phase 30 reclassified)
    - .planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md (Phase 30 D-154 in-place reclassification pattern reference — Phase 31 mirrors)
    - All Task 1-11 SUMMARY-section IDs (Homepage frame ID + section IDs + Hero subtree LOCK note ID + per-section OPEN-31-NN flags)
  </read_first>
  <action>
    GATE CHECK: this task runs after Task 11 (post-APPROVE). If Task 10 was REVISE or GAP, Task 12 does NOT execute until Plan 31-01 re-runs through APPROVE.

    Read current `.planning/research/PEN-INVENTORY.md`. Apply the following extensions (use Edit tool for surgical changes per Phase 28-30 carry-forward — do NOT rewrite the whole file):

    1. **NEW `Homepage` row in Frames Inventory table** (per D-154 + D-167):
       Row format matching Phase 26-30 carry-forward convention:
       ```
       | Homepage | <HomepageId from Task 1> | reconstructed-PHASE-31 | Homepage (per Plan 31-01) | 7 | factored:7 | n/a (Phase 31 Plan 31-01 — reconstructed; FIRST D-155 HYBRID joel-only-with-Hero-pairing-exception branch use in v2.0; LAST per-page reconstruction; Header descendants-override 4-link nav per D-162; SECOND cross-phase consumer of Section/CTA Hs5rc per D-161 + D-78 — first was Phase 27 Plan 27-01 Thank-you; OPEN-25-01 RESOLVED via Hero Secondary Button white-stroke descendants override per D-157 + Phase 25 D-43 closure) |
       ```

    2. **UPDATE `Home Page` (ujMLJ) line 65 row** (per D-155 LOCK action):
       - Change status_counts from current `flat:0, partial:2, factored:8` (or equivalent baseline from PEN-INVENTORY at Plan 31-00 T4 capture) to `flat:0, partial:1, factored:8, locked:1`
       - Add NEW status_counts key `locked:1` per § Discretion default lean
       - Change reconstruction_priority from `high` → `n/a (Phase 31 Plan 31-01 — Hero subtree consumed as visual pairing target via D-155 HYBRID; Hero illustration LOCKED via sibling Pencil note per § 3.3 sub-section-raster sub-rule; remaining 9 non-Hero sections retained as token-mining only per OPEN-23-04 carry-forward; final partial section count adjusted per Plan 31-01 execution audit)`

    3. **Document new `locked:N` status_counts key in PEN-INVENTORY § 2 status_counts logic block** (per § Discretion):
       Add a brief logic block entry explaining `locked:N` semantics:
       ```
       - **locked:N** — N children are editable Crito-source nodes preserved IN-PLACE per CALIBRATION-PROTOCOL § 3.3 sub-section-raster sub-rule LOCK action; raster enabled stays true; sibling Pencil note documents cross-reference. Introduced Phase 31 Plan 31-01 (ujMLJ Hero illustration).
       ```

    4. **Variant Evidence rows for Phase 31** (under existing Variant Evidence sub-section):
       - Header descendants override for D-162 4-link nav (Homepage instance only — SCOPED departure from D-77; instance ID + override property-path map summary)
       - Hero Secondary Button (hIWuC) descendants override for OPEN-25-01 white-stroke + white-label carve-out (instance ID + property-path map summary)
       - Section/CTA (Hs5rc) Homepage instance as SECOND cross-phase consumer (instance ID + headline/body/actions override summary)
       - Section/SecondaryLink emerging-consumer evaluation outcome from 31-00 T0 (default DEFER per § Discretion + Phase 30 D-145 reference)

    5. **NEW `## Open Flags — Phase 31 (OPEN-31-NN)` section** populated with:
       - **OPEN-31-01** Services illustration STUB labeling (3 instances — Card 1 + Card 2 + Card 3 image-slot STUB; Joel-brand asset Out of Scope; code milestone wires)
       - **OPEN-31-02** Process illustration STUB labeling (5 instances — step 1-5 illustration STUB)
       - **OPEN-31-03** About headshot STUB labeling (1 instance — headshot rectangle STUB)
       - **OPEN-31-04** Hero illustration STUB labeling (1 instance inside reconstructed Homepage Hero — image-fill rectangle placeholder; separate from ujMLJ Hero illustration raster which is LOCKED in-place per § 3.3)
       (Any additional plan-surfaced OPEN-31-NN flags from Task 10 GAP outcome.)

    6. **Cross-phase OPEN-flag status updates**:
       - **OPEN-25-01 → RESOLVED** at Plan 31-01 Hero Secondary Button white-stroke + white-label descendants override per D-157 + Phase 25 D-43 closure (Plan 31-00 T2 probe + Plan 31-01 T3 instance-time override)
       - **OPEN-23-09 → RESOLVED** via Plan 31-00 T0 v1.3 audit (Joel doesn't need Poppins per Crito-vocab + Joel-shape lean — PJS + Inter sufficient; per D-167 + § Discretion default)
       - **OPEN-23-04 → LAST-VISIT** (Phase 31 is the final per-page consumer of Home Page ujMLJ token-mining context per D-167; partial-resolution chain closes)
       - **OPEN-25-07** — Card cross-phase consumer count documentation update (Phase 27 sidebar 1 + Phase 28 RelatedPosts 3 + Phase 29 ProjectCard sibling pattern + Phase 30 gallery 1 + Phase 31 Services 3); document for Phase 32 close evaluation
       - **OPEN-26-02 stale-cache** — document Phase 31 Plan 31-01 observed behavior at Task 10 (Tier-1 sufficed OR Tier-2 fallback needed); contributes to Phase 32 disposition decision
       - **OPEN-23-10 heading-5/-6 long-tail** — unchanged (Phase 31 didn't surface a need)

    7. **Update Frames Inventory progress tracking** (if present in PEN-INVENTORY):
       - IN-SCOPE Joel pages reconstructed: now 10/10 (was 9/10 after Phase 30; Homepage closes the chain). Only `04_About` (WDGxc, IN-SCOPE token-mining-only per OPEN-23-04 + D-07) remains as Phase 32 milestone-close disposition row.

    Verify via git diff that PEN-INVENTORY changes are surgical (no unintended deletes; sections added or row-replaced; logic block extended) and committed to a feature branch.

    Then write 31-01-SUMMARY.md final consolidation:
    - SUMMARY frontmatter per project template:
      - subsystem: design-system (Crito.pen)
      - affects: [] (Phase 32 milestone-close sweep is downstream but no specific plan ID yet)
      - tech-stack.added: [] / .removed: []
      - patterns.added: ["D-155 HYBRID joel-only-with-Hero-pairing-exception sub-branch (Phase 31-only consumer in v2.0; Phase 32 evaluates protocol promotion)"]
      - key-files: ["design/Crito.pen :: Homepage frame (NEW)", "design/Crito.pen :: ujMLJ Hero illustration parent (LOCK note added)", ".planning/research/PEN-INVENTORY.md (extended)"]
      - decisions: [] (no new decisions; this plan executes D-155 through D-167)
    - Per-section fidelity labels table (7 rows: Header EXACT-overridden / Hero APPROXIMATE / Services layout-EXACT + illustrations-STUB / Process layout-EXACT + illustrations-STUB / About layout-EXACT + headshot-STUB / closing-CTA EXACT / Footer EXACT)
    - Calibration gate transcript reference (Task 10 outcome captured)
    - PEN-INVENTORY diff summary
    - Final paragraph: Phase 31 closes v2.0 per-page reconstruction chain (10/10 IN-SCOPE Joel pages reconstructed); Phase 32 milestone-close sweep + handoff is next.

    Commit on a feature branch per CLAUDE.md git workflow.
  </action>
  <acceptance_criteria>
    - PEN-INVENTORY contains NEW Homepage row (grep "Homepage" + grep "reconstructed-PHASE-31")
    - PEN-INVENTORY line 65 ujMLJ row reflects status_counts with `locked:1` (or selected schema choice from § Discretion) + reconstruction_priority changed from `high` to `n/a (Phase 31 Plan 31-01...)`
    - PEN-INVENTORY § 2 status_counts logic block has new `locked:N` definition entry
    - Variant Evidence Phase 31 sub-section has at least 4 rows (Header override + Hero Secondary override + Section/CTA Homepage instance + Section/SecondaryLink eval outcome)
    - NEW `## Open Flags — Phase 31 (OPEN-31-NN)` section with at least 4 entries (OPEN-31-01 through OPEN-31-04)
    - OPEN-25-01 marked RESOLVED + OPEN-23-09 marked RESOLVED + OPEN-23-04 marked LAST-VISIT
    - 31-01-SUMMARY.md committed with frontmatter + 7-row fidelity labels table + calibration reference + PEN-INVENTORY diff summary
    - Commit on feature branch (verifiable via git log)
  </acceptance_criteria>
  <verify>
    grep "^| Homepage |" .planning/research/PEN-INVENTORY.md returns at least 1 line
    grep "locked:1" .planning/research/PEN-INVENTORY.md returns at least 1 line
    grep "OPEN-31-01" .planning/research/PEN-INVENTORY.md returns at least 1 line
    grep "OPEN-25-01.*RESOLVED" .planning/research/PEN-INVENTORY.md returns at least 1 line
    test -f .planning/phases/31-homepage-reconstruction/31-01-SUMMARY.md
    git log --oneline -1 | grep -E "(31-01|Phase 31)" returns at least 1 line
  </verify>
  <done>
    Phase 31 closes with PEN-INVENTORY extended per D-167 + SUMMARY committed + v2.0 per-page reconstruction chain at 10/10 IN-SCOPE Joel pages.
  </done>
</task>

</tasks>

<verification>
Overall plan checks at close (post-APPROVE):
- design/Crito.pen contains new Homepage frame with exactly 7 children in order Header / Hero / Services / Process / About / Section-CTA / Footer
- Header instance descendants override applies 4-link nav (Blog / Projects / FAQ / Contact); Home + About + Services hidden
- Hero Secondary Button (hIWuC) instance descendants override applies #ffffffff stroke + #ffffffff label fill per OPEN-25-01 carve-out
- Services has 3 Compound/Card (t40xct) instances with per-card descendants overrides + STUB illustrations (3 OPEN-31-01)
- Process has 5 step blocks vertical-stack with STUB illustrations (5 OPEN-31-02)
- About has 2-column composition with STUB headshot (1 OPEN-31-03)
- Section/CTA (Hs5rc) is sixth child with overrides + sibling note for v1.3 ContactSection
- Section/Footer (Xs0Hs) is seventh child no-override per D-77
- ujMLJ Hero illustration parent has new sibling Pencil LOCK note; ujMLJ Hero illustration leaf enabled stays true (never hidden — Pitfall 4 honored)
- get_variables returns 107 (unchanged from Phase 30 close)
- snapshot_layout returns "No layout problems." OR only text-clipping false-positives (Pitfall 6 benign)
- PEN-INVENTORY extended with Homepage row + ujMLJ line 65 LOCK update + Variant Evidence Phase 31 + Open Flags Phase 31 + OPEN-25-01/-23-09 RESOLVED + OPEN-23-04 LAST-VISIT
- 31-01-SUMMARY.md committed with 7-row fidelity labels table + calibration transcript reference + PEN-INVENTORY diff summary
- ROADMAP success criteria 1-4 satisfied
</verification>

<success_criteria>
Phase 31-01 complete when:
- [ ] Homepage frame exists in design/Crito.pen with exactly 7 children (Header / Hero / Services / Process / About / Section-CTA / Footer)
- [ ] Hero CTAs include OPEN-25-01 white-stroke + white-label descendants override on hIWuC Secondary Button
- [ ] D-162 Header 4-link descendants override applied (SCOPED to Homepage instance only per D-162)
- [ ] D-163 logo sibling Pencil note attached to Homepage Header instance
- [ ] 3 × Compound/Card (t40xct) instances in Services with per-card descendants overrides + 3 OPEN-31-01 STUB illustration flags
- [ ] 5-step vertical Process timeline with 5 OPEN-31-02 STUB illustration flags
- [ ] About 2-column with STUB headshot + 1 OPEN-31-03 flag + Primary CTA to /contact n0QqTd
- [ ] Section/CTA Hs5rc instance as SECOND cross-phase consumer with v1.3 ContactSection sibling note for code milestone
- [ ] Section/Footer Xs0Hs no-override per D-77
- [ ] snapshot_layout document-root regression clean (or only Pitfall-6-benign text-clipping false-positives)
- [ ] Single calibration AskUserQuestion gate per D-155 HYBRID — § 4.5 token-usage + § 3.4 Hero side-by-side in ONE description; user APPROVE captured
- [ ] Post-APPROVE LOCK action: sibling Pencil note on ujMLJ Hero illustration parent; raster.enabled remains true (Pitfall 4 honored)
- [ ] PEN-INVENTORY extended per D-167 — Homepage row + ujMLJ line 65 LOCK + Variant Evidence Phase 31 + Open Flags Phase 31 + OPEN-25-01/-23-09 RESOLVED + OPEN-23-04 LAST-VISIT
- [ ] 31-01-SUMMARY.md committed with frontmatter + 7-row fidelity labels + calibration reference + PEN-INVENTORY diff
- [ ] get_variables returns 107 (token surface unchanged from Phase 30 close)
- [ ] Library count unchanged at 19 (zero new components)
- [ ] v2.0 per-page reconstruction chain closes at 10/10 IN-SCOPE Joel pages; only Phase 32 milestone-close sweep + handoff remains
</success_criteria>

<output>
After completion, ensure all files committed to git on a feature branch per CLAUDE.md git workflow (do NOT commit directly to main). Files: design/Crito.pen (Pencil-managed, but commit any artifacts indirectly referenced) + .planning/research/PEN-INVENTORY.md + .planning/phases/31-homepage-reconstruction/31-01-SUMMARY.md.
</output>
