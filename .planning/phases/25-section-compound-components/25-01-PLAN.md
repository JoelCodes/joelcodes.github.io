---
phase: 25-section-compound-components
plan: 25-01
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen (NEW reusable `Section / Header` inside `_Components / Sections` (g9oRa5) — fixed-width parent frame matching Crito Home Page Menu bar source `ujMLJ` per D-41; children: logo-slot per D-40, nav-links row with literal Crito labels per D-38, affordances row with search + chevron-down + menu icons per D-39, cta-row with dual Button instances per D-42; iconTrailing slot of each Button populated with `Primitive / Icon / 24` ref + lucide `arrow-right` per D-44)
  - design/Crito.pen (NEW Secondary Button variant — D-43 PREFERRED PATH: new `purpose` variant axis cell added inside existing `Primitive / Button` (M7eUr); D-43 FALLBACK PATH if Task 1 probe shows variant-axis-extension clobbers Hover/Focus cells: ship sibling `Primitive / Button / Secondary` reusable component as direct child of `_Components / Primitives` (avgor); source-evidenced values from Hero `mkw8g` "Discover More": fill:none, strokeWidth:0.5, strokeAlignment:inner, cornerRadius:10, padding:[16,20], gap:10; stroke color per Open Question 3 resolution at Task 1 — default navy `#141f39ff` for light-bg Header context, surfaced as OPEN-25-XX since deviates from source-literal `#ffffffff`)
  - design/Crito.pen (NEW sibling Pencil note inside `_Components / Sections` documenting `Section / Header` slot signature: logo-slot, nav-links, affordances, dual CTAs)
  - .planning/research/PEN-INVENTORY.md (## Variant Evidence (Phase 24) EXTENDED with Phase-25-late-addition rows: Secondary Button cell properties citing source `mkw8g`; Section/Header structural rows citing Crito Menu bar `ujMLJ` per D-56)
  - .planning/research/PEN-INVENTORY.md (### Open Flags — Phase 24 Resolution column updated: OPEN-24-06 RESOLVED via Plan 25-01 Header CTA iconTrailing wire-up per D-44; OPEN-24-11 RESOLVED via Plan 25-01 Task 1 Secondary Button addition per D-42/D-43)
  - .planning/research/PEN-INVENTORY.md (### Open Flags — Phase 25 (OPEN-25-NN) NEW section header scaffolded if not already; first OPEN-25-XX row for Secondary stroke color context-deviation per Open Question 3)
  - .planning/ROADMAP.md (Phase 25 plan line `- [ ] 25-01: Build Section/Header` checkbox updated at plan close)
autonomous: false
requirements_addressed:
  - COMP-05
  - COMP-08
  - COMP-09
must_haves:
  truths:
    - "`Section / Header` exists under `_Components / Sections` (g9oRa5) as a reusable component with fixed-width matching Crito Home Page Menu bar `ujMLJ` per D-41 (COMP-05 + COMP-08)"
    - "Section/Header content uses Crito-source-verbatim labels per D-38 — nav-links text nodes ship the literal strings enumerated from `ujMLJ` Task 0 audit (no Joel-side overrides at the component level)"
    - "All three Crito Menu-bar affordances ship per D-39: search icon, chevron-down icon, menu (hamburger) icon — each instances `Primitive / Icon / 24` (u7NmaS) with lucide-native glyphs already shipped Phase 24"
    - "Section/Header instances `Primitive / Button` TWICE (dual CTA per D-42 — Default green + new Secondary outline) — VAL-25-07"
    - "Both Header CTA Button instances have `iconTrailing` slot ENABLED + populated with `Primitive / Icon / 24` ref containing lucide `arrow-right` (D-44 actual-use resolution of OPEN-24-06) — VAL-25-08"
    - "Secondary Button variant present per D-43 — preferred path (variant-axis cell added to M7eUr) OR fallback (sibling `Primitive / Button / Secondary`) per Task 1 probe outcome; source-evidenced values from Hero `mkw8g`: fill:none, strokeWidth:0.5, strokeAlignment:inner, cornerRadius:10, padding:[16,20] — VAL-25-11"
    - "Logo slot per D-40 ships with `enabled:true` placeholder content (Crito wordmark text per Open Question 5 resolution)"
    - "Sibling Pencil note inside `_Components / Sections` documents Section/Header slot signature in human-readable form (COMP-05 Success Criterion 3)"
    - "Zero raw color hex / zero raw px values inside Section/Header or Secondary Button beyond accepted Phase 23/24/25 Variant Evidence binding set (COMP-09)"
    - "Every Pencil-mutating batch is preceded by `mcp__pencil__get_editor_state({ include_schema: false })` asserting active editor == `design/Crito.pen` (D-54 carry-forward from D-35)"
    - "`snapshot_layout(maxDepth: 0, problemsOnly: true)` at document root returns `\"No layout problems.\"`; per-frame text-clipping false-positives documented per Phase 24 quirk carry-forward (24-05-SUMMARY)"
    - "PEN-INVENTORY § Variant Evidence (Phase 24) EXTENDED with Phase-25-late-addition rows for Secondary Button cell + Section/Header structural rows per D-23/D-56"
    - "OPEN-24-06 + OPEN-24-11 marked RESOLVED with Plan 25-01 citations in PEN-INVENTORY Open Flags — Phase 24 table"
    - "Phase 24 baseline IDs unmutated EXCEPT for D-42/D-43 Button Secondary variant addition (M7eUr touched ONLY if Task 1 preferred path succeeds; YJhRv/gQa2R/AvKtA/V4Dx4i/ATJK9 + slot/label children NOT mutated outside that scope)"
    - "D-57 plan ordering honored: this plan runs as Wave 1 (depends_on: []); Plan 25-02 (Footer) runs Wave 2 depending on 25-01; Plan 25-03 (Card) runs Wave 3 depending on 25-01+25-02 — so the slot-mechanics pattern established here (logo slot per D-40, iconTrailing wire per D-44) is verified before Card slot work begins"
  artifacts:
    - path: "design/Crito.pen"
      provides: "Section / Header reusable component inside _Components / Sections (g9oRa5)"
      contains: "fixed-width parent frame matching Crito Menu bar; logo-slot (enabled:true); nav-links row (literal labels); affordances row (3 Icon refs); cta-row (2 Button refs with iconTrailing arrow-right wired)"
    - path: "design/Crito.pen"
      provides: "Secondary Button variant — D-43 preferred (new cell in M7eUr) OR fallback (sibling component in avgor)"
      contains: "fill:none, stroke literal, strokeWidth:0.5, strokeAlignment:inner; matches Default padding/radius/layout"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Variant Evidence rows for Secondary Button + Section/Header structural bindings; OPEN-24-06/11 resolution citations; OPEN-25-NN section seeded if needed for Secondary stroke context-deviation"
  key_links:
    - from: "Section/Header CTA Button instances"
      to: "Primitive / Button / Default (M7eUr) + Secondary (new from Task 1)"
      via: "Pencil ref + descendants override pattern (PEN-INVENTORY guideline § 2 + § 3)"
      pattern: "RESEARCH Pattern 3 (Instance Button with iconTrailing wire-up)"
    - from: "Section/Header CTA Button iconTrailing slots (V4Dx4i for Default, equivalent for Secondary)"
      to: "Primitive / Icon / 24 (u7NmaS) with lucide arrow-right"
      via: "Slot insertion path `parentBinding/slotId` + descendants `enabled:true` + icon descendant override"
      pattern: "D-44 ACTUAL-USE resolution of OPEN-24-06 empty-slot collapse mechanic"
    - from: "Section/Header affordances row icon children"
      to: "Primitive / Icon / 24 (u7NmaS) with lucide search / chevron-down / menu"
      via: "Pencil ref descendants override on icon glyph property"
      pattern: "Phase 24 D-26 source-driven glyph enumeration carry-forward (all 3 glyphs shipped Phase 24)"
    - from: "Secondary Button variant"
      to: "Crito Hero source `mkw8g` 'Discover More'"
      via: "PEN-INVENTORY § Variant Evidence (Phase 24) extension rows citing frame_id + node_id + property values"
      pattern: "D-24 source-wins discipline carry-forward (Phase 25 D-55 parallel disciplines)"
---

<objective>
Build `Section / Header` (COMP-05) inside the Phase 24 stub `_Components / Sections` (g9oRa5), instancing Phase 24 primitives (`Primitive / Button` M7eUr, new Secondary variant from this plan's Task 1, `Primitive / Icon / 24` u7NmaS) against the 95-token semantic surface from Phase 23. The plan's first build task (Task 1) probes Pencil 2.13 variant-axis-extension mechanics on M7eUr and adds a Secondary purpose variant cell (preferred per D-43) or ships a sibling `Primitive / Button / Secondary` component (fallback per D-43) — either path resolves Phase 24's OPEN-24-11 (Secondary Button source `mkw8g` carry-forward). Header CTAs then wire real `arrow-right` icons into both Button instances' iconTrailing slots (D-44), resolving Phase 24's OPEN-24-06 (empty-slot collapse mechanic) by ACTUAL consumer use rather than synthetic probe.

**Source-wins discipline (D-38, D-39, D-41, D-55):** Crito Home Page Menu bar `ujMLJ` is the IN-SCOPE non-raster source. Task 0 audits it via `batch_get(readDepth: 4)` to enumerate nav-link labels (literal text — Joel-side overrides happen at Phase 31 Homepage instance time, NOT at component level), all three Menu-bar affordances (search + chevron-down + menu — hamburger ships per D-39 visual-fidelity-not-behavior), logo region (D-40 slot), and dual-CTA structure (D-42).

**Probe-first within the plan (Open Question 1):** Pencil 2.13 variant-axis-extension internals are not publicly documented. Task 1 EXECUTES the probe — try the preferred D-43 path (Update M7eUr to add `purpose` axis cell). If batch_design rejects or clobbers Hover/Focus cells, fall back to sibling component per D-43 fallback. Either path is acceptable; document outcome in 25-01-SUMMARY.md.

**Purpose:** Ships the first Phase 25 section component. Resolves two Phase 24 carry-forward OPEN flags (OPEN-24-06, OPEN-24-11) by actual consumer use. Establishes the slot-mechanics pattern (logo slot per D-40, iconTrailing wire-up per D-44) that Plan 25-03 Card slot work will reuse. Validates Phase 25 D-55 parallel disciplines (source-wins for Section components vs raster-probe for Compound/Card).

**Output:** `Section / Header` inside `_Components / Sections`; Secondary Button variant (cell or sibling); two OPEN-24-NN resolution citations in PEN-INVENTORY; possible OPEN-25-XX for Secondary stroke context-deviation per Open Question 3; 25-01-SUMMARY.md documenting Task 1 probe outcome, Task 0 source audit findings, slot-mechanics observations, and user visual checkpoint approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-section-compound-components/25-CONTEXT.md
@.planning/phases/25-section-compound-components/25-RESEARCH.md
@.planning/phases/25-section-compound-components/25-VALIDATION.md
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md
@.planning/phases/24-layout-primitives-primitive-components/24-02-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-24/id-inventory.json
@./CLAUDE.md
</context>

<threat_model>
**T-25-T1 (active-editor-swap — MEDIUM severity, BLOCKING)** — Pencil's VS Code extension silently switches active editor when VS Code focus changes, causing `batch_design` to land in the WRONG `.pen` file. Original incident: OPEN-23-14 during Plan 23-05. Mitigation: D-54 carries forward Phase 24 D-35 — `mcp__pencil__get_editor_state({ include_schema: false })` called BEFORE every `batch_design` / `set_variables` batch in this plan; assert active editor path == `design/Crito.pen`. Halt and surface to user on mismatch. Expected pre-flight call count for this plan: ~4-6 (Task 1 probe, Task 1 build, Task 2 Section/Header build, Task 3 CTA wire-up, optional re-runs).

**T-25-I1 (information-disclosure / probe-artifact leak — LOW)** — Task 1 probe artifact (test variant cell or test ref) captured via `get_screenshot` for visual confirmation. Mitigation: probe artifacts deleted before plan close (matches Phase 24 plan 24-02 / 24-04 probe-cleanup discipline). Final deliverables live in `_Components / Sections` and `_Components / Primitives` only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + guidelines + Crito Menu bar source audit (RESEARCH Pattern 1)</name>
  <files>none (read-only — no `design/Crito.pen` mutations)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-38, D-39, D-40, D-41, D-42, D-44, D-54)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Standard Stack", § "Recommended `_Components / Sections / Header` Structure", § "Code Examples Pattern 1", § "Open Questions" 3 + 5)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-01, VAL-25-07, VAL-25-12, VAL-25-14, VAL-25-15)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim sections 2 + 3 + 12 + 13; § "Open Flags — Phase 24" rows OPEN-24-06 / OPEN-24-11; § "Variant Evidence (Phase 24)" Button rows; § "Icon Glyphs (Phase 24)" — confirm search / chevron-down / menu / arrow-right all present)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (library_parents, primitives.Button + Icon ids, baseline_drift_verification expected_baseline_ids_unmutated list — ujMLJ MUST appear)
    - .planning/phases/24-layout-primitives-primitive-components/24-02-SUMMARY.md (Button source-mining precedent — `fwSmg` Hero CTA + retroactive note about `mkw8g` Secondary discovery)
  </read_first>
  <action>
    1. Pre-flight per D-54: call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert returned active editor path == `/Users/joel/Desktop/Claude-Demos/joel-shinness-website/design/Crito.pen`. Halt and surface to user on mismatch.

    2. Confirm Pencil schema is current: if not already cached in this conversation, call `mcp__pencil__get_editor_state({ include_schema: true })` per MCP server instructions. Record schema version in 25-01-SUMMARY.md.

    3. Read Pencil composition guidelines for current authoritative guidance:
       - `mcp__pencil__get_guidelines({ topic: "design-system" })` — record any deviation from PEN-INVENTORY § "Pencil Guidelines" snapshot.
       - `mcp__pencil__get_guidelines({ topic: "landing-page" })` — record any Header-specific conventions (likely covers nav-row + CTA-row patterns).
       - Record both outputs (or diffs against PEN-INVENTORY snapshot) inline in 25-01-SUMMARY.md `## Guidelines Snapshot` section.

    4. Read the 95-token surface: `mcp__pencil__get_variables({})`. Confirm count == 95 (or document any Phase 24 extensions discovered late — expected 0 per id-inventory.json `token_extension_row_count: 0`). Record in SUMMARY.

    5. **Crito Menu bar source audit (D-38, D-39, D-41 + RESEARCH Pattern 1):**
       - Call `mcp__pencil__batch_get({ queries: [{ id: "ujMLJ", readDepth: 4 }] })`. ujMLJ is the Home Page IN-SCOPE frame (verified in id-inventory.json baseline list).
       - Identify the Menu bar sub-region (top-region child of ujMLJ). Likely names include "Menu bar", "Header", "Navigation", or similar.
       - Enumerate and record (in 25-01-SUMMARY.md `## Crito Menu Bar Source Audit` table) for each Menu-bar child:
         | node_id | type | name | text (if text) | layout | gap | padding | fill | stroke | strokeWidth | cornerRadius | width | height | x | y |
         |---------|------|------|----------------|--------|-----|---------|------|--------|-------------|--------------|-------|--------|---|---|
       - Specifically capture:
         (a) Menu bar PARENT frame's width (D-41 fixed-width target — likely 1440).
         (b) Logo node — text or vector or other. Record literal text if present (typically "Crito" wordmark).
         (c) Nav-link text nodes — exact literal labels (e.g., "Home", "About", "Service", "Blog", "Contact") AND order. THESE ARE THE LITERAL STRINGS the component will ship per D-38 (Joel-side overrides at Phase 31).
         (d) Affordances row icons — confirm presence of search + chevron-down + menu (hamburger) per D-39. Record each node_id + lucide glyph name confirmed against Phase 24 ship-list.
         (e) Dual CTA structure (D-42) — confirm presence of TWO CTA buttons or a single primary CTA. Capture each CTA's node_id, label text, fill, stroke.
       - Cross-check (e) against the retroactive note in 24-02-SUMMARY.md / id-inventory.json `cross_phase_handoffs.open_24_11_secondary_button` — Crito source `mkw8g` "Discover More" (Hero, NOT necessarily Menu bar) is the Secondary outline pattern. Menu bar may have its own dual-CTA arrangement OR Phase 25 ships Secondary derived from Hero source while Header instances both. Record observation.

    6. **Resolve Open Question 5 (logo slot default content) inline:** Per RESEARCH § Open Questions 5 recommendation, ship `enabled:true` placeholder = Crito wordmark text literal (matches D-38 source-wins discipline; Phase 31 overrides at instance time). Record decision in SUMMARY.

    7. **Resolve Open Question 3 inline (Secondary Button stroke color for light-bg Header context):** Per RESEARCH § Open Questions 3 recommendation, ship Secondary variant default stroke as `#141f39ff` (navy-on-light = visible) rather than source-literal `#ffffffff` (white-on-light = invisible). Record this as the planned OPEN-25-XX (severity: notable; consumer phase: 31 Homepage Hero may override at instance time). Defer the actual OPEN-25-XX row write to Task 4 close.

    8. **Resolve Open Question 4 inline (Newsletter Subscribe button color from Footer raster):** N/A for this plan — Newsletter is a Plan 25-02 concern. Forward to Plan 25-02 Task 0.

    9. Record `## Crito Menu Bar Source Audit` table + `## Decisions` (Open Q3 + Q5 resolutions) + `## Guidelines Snapshot` sections in 25-01-SUMMARY.md DRAFT (full SUMMARY at plan close).
  </action>
  <verify>
    - `mcp__pencil__get_editor_state` returned `design/Crito.pen` as active editor.
    - `mcp__pencil__get_variables({})` returned 95 entries (or documented delta).
    - `mcp__pencil__batch_get({ queries: [{ id: "ujMLJ", readDepth: 4 }] })` returned non-error response with Menu bar subtree enumerable.
    - SUMMARY.md DRAFT contains `## Crito Menu Bar Source Audit` table with ≥ 5 enumerated nodes (logo, nav-link text nodes, affordance icons, CTAs).
    - SUMMARY.md records: Menu bar fixed-width value; literal nav-link labels (in order); 3 affordance icons confirmed against Phase 24 lucide ship-list; CTA structure (single vs dual) with source citations.
    - Open Question 3 + 5 resolutions captured.
  </verify>
  <acceptance_criteria>
    - VAL-25-14 satisfied for this task: pre-flight `get_editor_state` invocation logged in SUMMARY.md (no mutation occurred, but discipline applies).
    - Menu bar source audit table populated; supplies the literal labels + affordance ship-list + dual-CTA structure that Tasks 2-3 will reference.
    - Open Question 3 + 5 resolutions documented as decision rows in SUMMARY.md (allows Task 1 + Task 4 to proceed without re-deciding).
    - Zero mutations attempted on `design/Crito.pen` in this task (read-only audit).
  </acceptance_criteria>
  <done>
    25-01-SUMMARY.md DRAFT exists at `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` with sections `## Active-Editor Pre-flight Result (D-54)`, `## Guidelines Snapshot`, `## Crito Menu Bar Source Audit`, `## Decisions (Open Q3 + Q5)`. Tasks 1-4 can reference these without re-running the audit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Probe + add Secondary Button variant (D-42, D-43 — resolves OPEN-24-11)</name>
  <files>design/Crito.pen (Task 1 PROBE: temporary test variant cell or sibling component inside avgor — deleted at task close after outcome confirmed; Task 1 BUILD: final Secondary Button cell inside M7eUr per D-43 PREFERRED, OR sibling `Primitive / Button / Secondary` inside avgor per D-43 FALLBACK)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-42, D-43, D-55, D-56)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Code Examples Pattern 2", § "Open Questions" 1 + 3, § "Common Pitfalls" 6 (Mutating Phase 24 primitives beyond D-42/D-43 scope))
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-11, VAL-25-13, VAL-25-14, VAL-25-15, VAL-25-20)
    - .planning/research/PEN-INVENTORY.md (§ "Variant Evidence (Phase 24)" — Button Default rows for shape reference; § "Open Flags — Phase 24" OPEN-24-11)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (primitives.Button.parent + .variants + .slots — confirm M7eUr / YJhRv / gQa2R / AvKtA / V4Dx4i / ATJK9 ids before any mutation)
    - .planning/phases/24-layout-primitives-primitive-components/24-02-SUMMARY.md (precedent: Button schema probe → flat-property shape; reusable: true verified; OPEN-23-13 literal-hex dual-track)
  </read_first>
  <action>
    1. Pre-flight per D-54: call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert match.

    2. Read existing Button schema state: `mcp__pencil__batch_get({ queries: [{ id: "M7eUr", readDepth: 3 }] })`. Record current variant axis schema (does M7eUr already have a `purpose` axis? Phase 24 shipped Default + Default/Hover + Default/Focus as separate frames — likely no `purpose` axis exists yet).

    3. **PROBE (Open Question 1 resolution — Pencil 2.13 variant-axis-extension mechanics):**
       - Attempt the D-43 PREFERRED path. The exact Pencil 2.13 mechanism for adding a `purpose` variant axis cell to an existing `reusable: true` component is undocumented in public docs. Attempt this approach:

         (a) Try `mcp__pencil__batch_design` Update operation on M7eUr that adds a `purpose` variant axis. If schema 2.13 represents variants via a `variants` property array on the parent, append a new cell. If it represents variants via sibling sibling frames with a `variant` property, that's the schema-snapshot signal.

         (b) If (a) fails with an error or returns successfully but `batch_get({ ids: ["M7eUr"], readDepth: 3 })` shows Hover/Focus cells (YJhRv / gQa2R) have been clobbered (their children's properties changed, ids dropped, or names changed), this is the FALLBACK trigger. Record the exact failure mode in SUMMARY.md.

         (c) If (a) succeeds cleanly (returns a new variant cell id; YJhRv/gQa2R remain untouched per batch_get verification), proceed with PREFERRED path.

       - Record probe outcome in 25-01-SUMMARY.md `## Variant-Axis Probe Result (Open Question 1)` with raw response from each batch_design + verification batch_get.

    4. **BUILD Secondary variant (D-42, D-43):**
       - Source-evidenced values from Hero `mkw8g` "Discover More" (per OPEN-24-11 retroactive note in 24-02-SUMMARY.md):
         * `fill: "none"`
         * `stroke: "#141f39ff"` (navy — context-aware per Open Question 3 resolution from Task 0; deviates from source-literal `#ffffffff` since Header is light-bg)
         * `strokeWidth: 0.5`
         * `strokeAlignment: "inner"`
         * `cornerRadius: 10` (matches Default's `radius-semantic-button` literal)
         * `padding: [16, 20]` (matches Default's `space-semantic-button-py`/`-px` literal pair)
         * `gap: 10` (matches Default literal — OPEN-24-01 continues)
         * `layout: "horizontal"`, `justifyContent: "center"`, `alignItems: "center"`
         * Label text node: `fontFamily: "Inter"`, `fontSize: 16`, `fontWeight: 500`, `fill: "#141f39ff"` (navy text on white bg — different from Default's white-on-green)
         * Two slot children matching Default's pattern: `iconLeading` (enabled:false) + `iconTrailing` (enabled:false). For typed-slot suggestion, `slot: [u7NmaS]` (point at Primitive/Icon/24). Initial state `enabled: false`; Task 3 will enable Header CTA's iconTrailing instance.

       - PREFERRED PATH (D-43): Update M7eUr with new `purpose=Secondary` variant cell containing the above properties. Cell name follows the Default + Hover + Focus precedent (`Primitive / Button / Secondary`).

       - FALLBACK PATH (D-43): If probe Step 3(b) triggered, `mcp__pencil__batch_design` Insert a SIBLING reusable component into avgor:
         ```text
         secondaryButton=I(avgor,{type:"frame",name:"Primitive / Button / Secondary",x:..,y:..,width:160,height:52,reusable:true,layout:"horizontal",justifyContent:"center",alignItems:"center",padding:[16,20],gap:10,fill:"none",stroke:"#141f39ff",strokeWidth:0.5,strokeAlignment:"inner",cornerRadius:10})
         iconLeading=I(secondaryButton,{type:"frame",name:"iconLeading",slot:[],enabled:false})
         label=I(secondaryButton,{type:"text",text:"Button",fontFamily:"Inter",fontSize:16,fontWeight:500,fill:"#141f39ff"})
         iconTrailing=I(secondaryButton,{type:"frame",name:"iconTrailing",slot:[],enabled:false})
         ```
         Place to the right of M7eUr's cells with `find_empty_space_on_canvas` if needed; record the resolved x/y in SUMMARY. Capture returned id as `secondaryButtonId`.

       - Per OPEN-23-13 dual-track: use literal hex/font values in batch_design payload AND add Variant Evidence rows in PEN-INVENTORY (Task 4 commits row writes).

    5. **Verify built variant:**
       - `mcp__pencil__batch_get({ queries: [{ id: <secondaryCellOrComponentId>, readDepth: 2 }] })`. Confirm all properties match the source-evidenced values from Step 4.
       - `mcp__pencil__get_screenshot({ nodeId: <secondaryId> })`. Visually confirm: navy stroke 0.5 weight, transparent fill, "Button" label in navy text, rectangular with 10px radius, slots collapsed.
       - Verify Phase 24 baseline IDs unmutated: `batch_get` on YJhRv (Hover) + gQa2R (Focus) + AvKtA (Default iconLeading) + V4Dx4i (Default iconTrailing) + ATJK9 (Default label). All should return their pre-Phase-25 property values (cross-check against id-inventory.json + Phase 24 Variant Evidence table values). Record verification result in SUMMARY.

    6. **User visual checkpoint (this task is checkpoint:human-verify):** Present `get_screenshot` result + the source citation from `mkw8g`. Ask user to confirm Secondary Button:
       (a) Renders as expected (transparent fill, navy outline, navy label).
       (b) Reads as a "Secondary outline" treatment alongside the green Default.
       (c) Approves Open Question 3 resolution (navy stroke as component default; surface as OPEN-25-XX; Phase 31 may override at Hero instance).

    7. **Probe artifact cleanup:** If Step 3 probe created any temporary test cells/refs beyond the final shipped variant, delete via `mcp__pencil__batch_design` Delete operation. Confirm absence via batch_get. Phase 24 plan 24-02 precedent: probe artifacts CVfYr / zQmcM / etc. deleted before plan close.
  </action>
  <verify>
    - Pre-flight passed.
    - Variant-axis probe outcome documented (preferred vs fallback path chosen).
    - Secondary Button cell or sibling component present at recorded id with source-evidenced properties (`batch_get` confirms).
    - Phase 24 baseline IDs (YJhRv, gQa2R, AvKtA, V4Dx4i, ATJK9 + any other Default-line ids) unmutated.
    - User approved visual checkpoint.
    - Probe artifacts cleaned (if any).
  </verify>
  <acceptance_criteria>
    - VAL-25-11 PASS: Secondary Button variant present with source-evidenced values (fill: none, stroke literal, strokeWidth: 0.5) and source citation to `mkw8g` recordable.
    - VAL-25-20 progress: OPEN-24-11 ready to mark RESOLVED at Task 4 close (with Plan 25-01 citation).
    - VAL-25-14 satisfied for this task: pre-flight + each batch_design preceded by `get_editor_state`.
    - Pitfall 6 prevention verified: No Phase 24 baseline IDs mutated beyond D-42/D-43 scope.
    - Open Question 1 resolution recorded: probe outcome (preferred path success OR fallback triggered) documented in SUMMARY.md with raw responses.
    - Open Question 3 resolution recorded: navy stroke chosen as component default; user-approved; OPEN-25-XX seeded for Task 4 commit.
  </acceptance_criteria>
  <done>
    25-01-SUMMARY.md updated with `## Variant-Axis Probe Result (Open Question 1)` + `## Secondary Button Build` sections. Secondary Button cell/component exists in `design/Crito.pen` at recorded id with source-evidenced values. User visually approved. Probe artifacts (if any) deleted.

    Resume signal: User types "approved" or "secondary cell renders correctly" or describes adjustment needed. Adjustments scope is bounded to the Secondary variant cell itself per Pitfall 6 — do NOT drift into Default/Hover/Focus modifications.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Section / Header parent frame + children (logo slot, nav links, affordances, dual CTA row)</name>
  <files>design/Crito.pen (NEW reusable `Section / Header` inside g9oRa5; child frames: nav-row container, logo-slot frame with placeholder text per D-40 + Open Q5 resolution, nav-links row with N literal Crito-source text nodes per D-38, affordances row with 3 Icon refs per D-39, cta-row container — CTAs themselves wired in Task 3)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-38, D-39, D-40, D-41, D-55, D-56)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Recommended `_Components / Sections / Header` Structure", § "Code Examples Pattern 3" for CTA wiring (Task 3 will use), § "Common Pitfalls" 3 (Skipping iconTrailing wire-up — Task 3 owns), § "Validation Architecture" VAL-25-01/07/08)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-01, VAL-25-07, VAL-25-12)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 2 Slots + § 12 Spacing reference + § 13 Button Hierarchy; § "Variant Evidence (Phase 24)" for semantic-token literal carry-forward)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (library_parents."_Components / Sections" g9oRa5 + primitives.Icon.variants for ref ids)
    - 25-01-SUMMARY.md DRAFT (Task 0's Menu bar source audit table — supplies literal nav-link labels + affordance order + Menu bar fixed-width)
  </read_first>
  <action>
    1. Pre-flight per D-54: `mcp__pencil__get_editor_state({ include_schema: false })`. Assert.

    2. **Build Section / Header parent frame** (D-41 fixed-width matching Crito source):
       - Width from Task 0 audit (likely 1440); height per audit (likely 80-90 for Menu bar).
       - Position inside g9oRa5 using a safe x/y from `find_empty_space_on_canvas` if needed (g9oRa5 already contains the D-37 stub title — Phase 24).
       - `mcp__pencil__batch_design` operations (use flat-property shape per Phase 24 plan 24-02 schema correction):
         ```text
         header=I(g9oRa5,{type:"frame",name:"Section / Header",x:<X>,y:<Y>,width:<W from audit>,height:<H from audit>,reusable:true,layout:"horizontal",justifyContent:"space-between",alignItems:"center",padding:[<py from audit>,<px from audit>],gap:<gap from audit>,fill:"#ffffffff"})
         ```
       - The `fill: "#ffffffff"` binds conceptually to `color-semantic-bg-page` per PEN-INVENTORY (verify in Task 0 audit; record literal in Variant Evidence row at Task 4).

    3. **Build logo-slot child (D-40):**
       - Insert a slot frame with placeholder text per Open Q5 resolution:
         ```text
         logoSlot=I(header,{type:"frame",name:"logo-slot",slot:[],enabled:true,layout:"horizontal",alignItems:"center"})
         logoPlaceholder=I(logoSlot,{type:"text",text:"<Crito wordmark literal from Task 0 audit — likely 'Crito'>",fontFamily:"<from audit>",fontSize:<from audit>,fontWeight:<from audit>,fill:"#141f39ff"})
         ```
       - Slot uses untyped `slot: []` for Task 2 — Plan 25-03 typed-slot probe (Open Question 2) may later inform retroactive typing of the logo slot.

    4. **Build nav-links row** (D-38 strict source-wins for labels):
       - Read literal labels from Task 0 audit table (in order; e.g., "Home", "About", "Service", "Blog", "Contact" — actual list depends on audit).
       - Per audit: determine if any nav link has an adjacent chevron-down icon (suggesting a dropdown). If yes, ship `chevron-down` Icon ref as a sibling text+icon group.
       ```text
       navLinks=I(header,{type:"frame",name:"nav-links",layout:"horizontal",gap:<gap from audit, e.g. 24>,alignItems:"center"})
       link1=I(navLinks,{type:"text",text:"<label 1 from audit>",fontFamily:"Inter",fontSize:16,fontWeight:500,fill:"#141f39ff"})
       link2=I(navLinks,{type:"text",text:"<label 2>",...})
       (...repeat for each nav-link enumerated in Task 0...)
       ```
       - All nav-link literal values: fontFamily/Size/Weight/fill source-derived from audit (if audit shows Plus Jakarta Sans 16/500 for nav, use those; if Inter 16/500, use those — record actual in Variant Evidence Task 4).

    5. **Build affordances row** (D-39 ship all 3 Menu-bar affordances):
       - All 3 lucide glyphs (search, chevron-down, menu) confirmed in Phase 24 ship-list per Icon Glyphs table.
       ```text
       affordances=I(header,{type:"frame",name:"affordances",layout:"horizontal",gap:<gap from audit>,alignItems:"center"})
       searchIcon=I(affordances,{type:"ref",ref:"u7NmaS",descendants:{}})
         — u7NmaS default glyph is chevron-right; override via descendants if Pencil schema allows descendant icon-property override.
         — IF descendant override is not the right mechanism for `type: icon` nodes per Pencil guidelines § 3 ("consumers reference size variant and override glyph"), use ad-hoc Icon insertion instead:
       searchIcon=I(affordances,{type:"icon",library:"lucide",icon:"search",width:24,height:24,fill:"#141f39ff"})
       chevronIcon=I(affordances,{type:"icon",library:"lucide",icon:"chevron-down",width:24,height:24,fill:"#141f39ff"})
       menuIcon=I(affordances,{type:"icon",library:"lucide",icon:"menu",width:24,height:24,fill:"#141f39ff"})
       ```
       - Choose between `ref`-based and ad-hoc `type:icon` based on Pencil schema § 3 best practice. Default per RESEARCH § Pattern 3: ad-hoc `type: icon` with explicit `library/icon` is documented as the consumer pattern; `ref` requires resolvable descendant glyph override.
       - Record choice in SUMMARY.md.

    6. **Build cta-row container** (D-42 dual CTAs — CTA Button instances added in Task 3):
       ```text
       ctaRow=I(header,{type:"frame",name:"cta-row",layout:"horizontal",gap:<gap from audit, e.g. 12>,alignItems:"center"})
       ```
       - Empty for now; Task 3 inserts the 2 Button refs.

    7. **Verify structure:** `mcp__pencil__batch_get({ queries: [{ id: <headerId>, readDepth: 3 }] })`. Confirm:
       - Section/Header marked `reusable: true`.
       - 4 children present: logo-slot, nav-links, affordances, cta-row (cta-row will be empty container).
       - Logo-slot has enabled:true with placeholder text child.
       - Nav-links has N text children (N from audit).
       - Affordances has 3 icon children with library:"lucide" + glyphs search/chevron-down/menu.

    8. `mcp__pencil__get_screenshot({ nodeId: <headerId> })`. Visual sanity-check (Task 3 + Task 4 ship the user-facing checkpoint after CTAs wired).

    9. Record `## Section/Header Build (Task 2)` section in 25-01-SUMMARY.md with structural breakdown + the affordance-insertion-mechanism choice + cited ids.
  </action>
  <verify>
    - Pre-flight passed.
    - `batch_get(<headerId>, readDepth: 3)` returns Section/Header with reusable:true under g9oRa5.
    - 4 child frames present with correct names and properties.
    - 3 affordance icons present (search, chevron-down, menu) with `library: lucide`.
    - Logo slot has enabled:true with text placeholder.
    - Nav-links contains text nodes matching Task 0 literal labels.
    - Phase 24 baseline IDs (incl. ujMLJ + library parent g9oRa5 itself) NOT modified — only g9oRa5's children list grew by one (Section/Header).
  </verify>
  <acceptance_criteria>
    - VAL-25-01 progress: Section/Header placed under `_Components / Sections` g9oRa5 with `reusable: true`. Task 3 + Task 4 complete the structural assertion when CTAs are wired.
    - VAL-25-12 progress: Section/Header uses Pencil auto-layout (horizontal nav-row); no absolute-positioned children. Affordances + nav-links + cta-row all `layout: "horizontal"`.
    - VAL-25-14 satisfied for this task: pre-flight before batch_design call.
    - D-38 compliance: nav-link text nodes match Task 0 literal labels exactly (no Joel-side overrides).
    - D-39 compliance: 3 Menu-bar affordances all shipped.
    - D-40 compliance: logo-slot is a `slot` frame with `enabled:true` placeholder content.
    - D-41 compliance: parent frame width matches Task 0 audit value.
  </acceptance_criteria>
  <done>
    25-01-SUMMARY.md `## Section/Header Build (Task 2)` section populated with structural breakdown + screenshot inline. Section/Header parent + 4 child frames + 3 affordance icons + logo placeholder + nav-link text nodes all present in `design/Crito.pen`. cta-row container is empty pending Task 3.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Wire dual CTAs into cta-row with iconTrailing arrow-right (D-42, D-44 — resolves OPEN-24-06)</name>
  <files>design/Crito.pen (cta-row child of Section/Header populated with 2 Button refs; each Button's iconTrailing slot ENABLED + populated with Primitive/Icon/24 ref OR ad-hoc icon node containing lucide arrow-right)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-42, D-44, D-55)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Code Examples Pattern 3 (Instance Button with iconTrailing wire-up)", § "Common Pitfalls" 3 (Skipping iconTrailing wire-up))
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-07, VAL-25-08, VAL-25-19)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 2 Slots — `parentBinding/slotId` insertion path + descendant override pattern; § "Variant Evidence (Phase 24)" Button slot rows for V4Dx4i)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (primitives.Button.slots.iconTrailing.Default = "V4Dx4i"; primitives.Icon.variants["24"] = "u7NmaS"; primitives.Button.slots.label.Default = "ATJK9")
    - 25-01-SUMMARY.md Task 0 + Task 2 sections (header structure + cta-row id)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Primary CTA (Default green Button with arrow-right):**
       - Per RESEARCH Pattern 3 — use Pencil `ref` + `descendants` override. Per PEN-INVENTORY guideline § 2: slot-insertion uses `parentBinding/slotId` syntax for child insertion.

       Approach A (preferred — descendants object on the ref):
       ```text
       primaryCTA=I(ctaRow,{type:"ref",ref:"M7eUr",descendants:{"ATJK9":{text:"<primary CTA label from Task 0 audit, e.g. 'Get Consulting'>"},"V4Dx4i":{enabled:true}}})
       primaryCTAIcon=I(primaryCTA/V4Dx4i,{type:"icon",library:"lucide",icon:"arrow-right",width:24,height:24,fill:"#ffffffff"})
       ```
       - V4Dx4i `enabled:true` flips Default's iconTrailing slot from collapsed to active.
       - Icon node inserted INTO the slot (parent path `primaryCTA/V4Dx4i`) with explicit `library:lucide, icon:arrow-right`. Fill white (matches Default's label fill — consistency with green-bg).

    3. **Secondary CTA (new Secondary Button with arrow-right):**
       - secondaryButtonId from Task 1 (either new variant cell or sibling component). slot ids for Secondary depend on Task 1 path:
         (a) D-43 PREFERRED path (variant cell inside M7eUr): Secondary cell's iconTrailing has its own new id captured during Task 1. Reference that.
         (b) D-43 FALLBACK path (sibling component): Secondary's iconTrailing id captured during Task 1 build.
       - Use the captured ids:
       ```text
       secondaryCTA=I(ctaRow,{type:"ref",ref:"<secondaryButtonId>",descendants:{"<secondaryLabelId>":{text:"<secondary CTA label from Task 0 audit, e.g. 'Learn More' or 'Discover More'>"},"<secondaryIconTrailingId>":{enabled:true}}})
       secondaryCTAIcon=I(secondaryCTA/<secondaryIconTrailingId>,{type:"icon",library:"lucide",icon:"arrow-right",width:24,height:24,fill:"#141f39ff"})
       ```
       - Icon fill navy (`#141f39ff`) matches Secondary's label color (Secondary is on white bg).

    4. **Verify CTA structure + OPEN-24-06 actual-use resolution:**
       - `mcp__pencil__batch_get({ queries: [{ id: <primaryCTAid>, readDepth: 3 }] })`. Confirm:
         * `type: "ref"`, `ref: "M7eUr"`.
         * Descendant V4Dx4i shows `enabled: true`.
         * V4Dx4i's child contains `type: "icon"`, `library: "lucide"`, `icon: "arrow-right"`, `fill: "#ffffffff"`.
         * Label descendant ATJK9 shows the new text override.
       - Same for secondary CTA against secondaryButtonId.
       - `mcp__pencil__get_screenshot({ nodeId: <ctaRowId> })` — capture for user checkpoint.

    5. **OPEN-24-06 resolution observations (D-44 actual-use validation):**
       - Confirm by visual that both CTAs render with arrow-right icons visible to the right of their labels.
       - Confirm by structural read that empty/disabled slots (iconLeading on both CTAs) remain collapsed (Phase 24 D-30 empty-slot behavior). If a non-icon-bearing CTA was wanted, the slot would stay `enabled: false` and not render.
       - Record observations in 25-01-SUMMARY.md `## OPEN-24-06 Actual-Use Resolution (D-44)`:
         * Does V4Dx4i `enabled: true` correctly render the inserted Icon? (Expected: YES)
         * Does iconLeading remain visually collapsed in both CTAs? (Expected: YES if AvKtA stayed `enabled: false`)
         * Does the auto-layout gap absorb correctly between label and trailing icon? (Expected: YES per Default's gap:10 literal)
         * Any deviation from Phase 24 expectations? Record.

    6. **User visual checkpoint:** Present `get_screenshot` of cta-row (or full Section/Header preferred). Ask user to confirm:
       (a) Primary CTA (green Default) renders with white "Get Consulting" (or actual label from audit) label + white arrow-right icon trailing.
       (b) Secondary CTA (navy outline) renders with navy "Learn More" (or actual label) label + navy arrow-right icon trailing.
       (c) Both CTAs sit side-by-side in the cta-row with appropriate spacing.
       (d) Approve OPEN-24-06 resolution observations.
  </action>
  <verify>
    - Pre-flight passed.
    - cta-row contains 2 Button refs (primary + secondary).
    - Each CTA's iconTrailing slot is `enabled: true` and contains a lucide arrow-right icon node.
    - Each CTA's label has been overridden via descendants to its respective Task-0-audit-derived label.
    - Visual screenshot confirms both icons render.
    - OPEN-24-06 observations recorded.
    - User approved.
  </verify>
  <acceptance_criteria>
    - VAL-25-07 PASS: `batch_get(<ctaRowId>, readDepth: 2)` returns 2 Button `ref` instances.
    - VAL-25-08 PASS: Walk each header-CTA → V4Dx4i (or Secondary's iconTrailing) descendant has `enabled: true` AND contains `type: icon, library: lucide, icon: arrow-right`.
    - VAL-25-19 progress: OPEN-24-06 ready to mark RESOLVED at Task 4 close.
    - VAL-25-14 satisfied for this task: pre-flight before batch_design.
    - Pitfall 3 prevention verified: both CTAs have iconTrailing wired, not label-only.
  </acceptance_criteria>
  <done>
    25-01-SUMMARY.md `## CTA Wire-up (Task 3)` + `## OPEN-24-06 Actual-Use Resolution (D-44)` sections populated. Both CTAs render visibly with arrow-right icons; user visually approved.

    Resume signal: User types "approved" or "CTAs render correctly" or describes a layout fix needed (e.g., gap between CTAs too tight). Layout fixes are bounded to the cta-row container properties — Button variant cells themselves are not modified at this stage.
  </done>
</task>

<task type="auto">
  <name>Task 4: Sibling Pencil note + PEN-INVENTORY updates + plan close (snapshot_layout + screenshot + OPEN flag resolutions)</name>
  <files>
    - design/Crito.pen (NEW sibling Pencil note inside g9oRa5 documenting Section/Header slot signature in human-readable form)
    - .planning/research/PEN-INVENTORY.md (Variant Evidence rows for Secondary Button + Section/Header structural bindings; OPEN-24-06 + OPEN-24-11 resolution column updates; new ### Open Flags — Phase 25 (OPEN-25-NN) section with OPEN-25-XX seed row for Secondary stroke context-deviation)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (final write — all sections + reference screenshot set + validation matrix)
    - .planning/ROADMAP.md (Phase 25 plan checkbox for 25-01 updated)
  </files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-56, D-57, D-54)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Implementation Order Recommendation" — Plan 25-01 close discipline)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-01, VAL-25-07, VAL-25-08, VAL-25-11, VAL-25-12, VAL-25-14, VAL-25-15, VAL-25-16, VAL-25-19, VAL-25-20, VAL-25-23 — all VAL ids relevant to Plan 25-01)
    - .planning/research/PEN-INVENTORY.md (§ "Variant Evidence (Phase 24)" schema for new rows; § "Open Flags — Phase 24" rows OPEN-24-06 + OPEN-24-11 for resolution column updates; § "End-of-Phase-24 Verification" for precedent format)
    - .planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md (precedent: glyph-shipping + screenshot review structure)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (precedent: snapshot_layout text-clipping quirk documentation)
  </read_first>
  <action>
    1. Pre-flight per D-54 (before sibling note insertion).

    2. **Build sibling Pencil note** (COMP-05 Success Criterion 3):
       ```text
       headerNote=I(g9oRa5,{type:"note",text:"Section / Header — slot signature:\n• logo-slot (slot:[]; enabled:true; default: Crito wordmark text placeholder per D-40 — Phase 31 Homepage instance overrides with Joel-brand wordmark when designed)\n• nav-links (fixed text nodes; literal Crito source labels per D-38 — instance overrides handle per-page brand text)\n• affordances (Primitive/Icon/24 instances: search + chevron-down + menu per D-39 strict source-wins; PAGE-09 desktop-only does NOT remove hamburger)\n• cta-row (2 Button ref instances: Primary Default green + Secondary outline navy; both wire Primitive/Icon/24 arrow-right via iconTrailing slot per D-44 — resolves OPEN-24-06 by actual use)\n\nAll children use Pencil auto-layout per LAYOUT-01. Width fixed per D-41 matching Crito source ujMLJ. Phase 24 baseline IDs unmutated; Secondary Button cell added per D-43 (resolves OPEN-24-11) — see PEN-INVENTORY § Variant Evidence (Phase 24).",fontFamily:"Inter",fontSize:12,fill:"#52525bff",width:600})
       ```
       - If `type: "note"` is not in the schema, use `type: "text"` per Phase 24 24-04 SUMMARY precedent (`SGQZC` Icon usage note was a text node).

    3. **PEN-INVENTORY updates (D-56 audit-trail discipline):**

       (a) **Extend `## Variant Evidence (Phase 24)` table** with these new rows (per D-56 rationale: existing Phase 24 table format reused; rows clearly labeled "Phase-25-late-addition" or "Section/Header structural"):

       | primitive_or_component | variant_cell_or_slot | property | literal_value | bound_to_token | source_evidence (frame_id + node_id) | rationale |
       |---|---|---|---|---|---|---|
       | `Primitive / Button / Secondary` (D-43 path used: <preferred or fallback>) | parent frame | fill | `none` | (no fill — outline pattern) | Crito Hero `mkw8g` "Discover More" (Plan 25-01 Task 1) | Source-wins for Secondary outline purpose per D-42 (resolves OPEN-24-11) |
       | `Primitive / Button / Secondary` | parent frame | stroke | `#141f39ff` | `color-semantic-text-primary` (→ navy-900) | Plan 25-01 Task 1 Open Question 3 resolution — context-aware (navy on light-bg) | Source `mkw8g` literal is `#ffffffff` (Hero dark-bg); Header light-bg requires navy; OPEN-25-XX records the context-deviation |
       | `Primitive / Button / Secondary` | parent frame | strokeWidth | `0.5` | (no token) | source `mkw8g` (Plan 25-01 Task 1) | Crito Hero Secondary outline-button strokeWidth literal |
       | `Primitive / Button / Secondary` | parent frame | strokeAlignment | `"inner"` | (no token) | source `mkw8g` (Plan 25-01 Task 1) | Matches Crito Hero outline pattern |
       | `Primitive / Button / Secondary` | parent frame | cornerRadius / padding / gap | `10` / `[16, 20]` / `10` | matches Default's `radius-semantic-button` + `space-semantic-button-py/-px` + (OPEN-24-01 carry) | source `mkw8g` (Plan 25-01 Task 1) | Identical to Default — only fill+stroke differ |
       | `Primitive / Button / Secondary` | label text | fontFamily / fontSize / fontWeight / fill | `Inter` / `16` / `500` / `#141f39ff` | `type-semantic-button-*` + `color-semantic-text-primary` | source `mkw8g` + Task 1 Open Q3 resolution | Navy label on transparent fill (Header light-bg context) |
       | `Section / Header` | parent frame | layout / justifyContent / alignItems / fill / width | `horizontal` / `space-between` / `center` / `#ffffffff` / `<from audit>` | `color-semantic-bg-page` (→ `color-primitive-white`); width literal per D-41 | Crito Home Page Menu bar `ujMLJ` audit (Plan 25-01 Task 0) | D-38/D-41 source-wins Menu bar layout + fixed width |
       | `Section / Header` | parent frame | padding / gap | `[<py>, <px>]` / `<gap>` | `space-semantic-*` per audit-matched literals | source `ujMLJ` (Plan 25-01 Task 0) | Source-derived spacing |
       | `Section / Header / nav-links` | text children | fontFamily / fontSize / fontWeight / fill | `<from audit>` / `16` / `500` / `#141f39ff` | matched semantic tokens | source `ujMLJ` nav-link text node properties (Task 0) | Strict literal labels per D-38; styling source-derived |
       | `Section / Header / affordances` | 3 icon children | type / library / icon (per glyph) / width / height / fill | `icon` / `lucide` / `search\|chevron-down\|menu` / `24` / `24` / `#141f39ff` | Pattern A Pencil-native; `color-semantic-text-primary` | Phase 24 ship-list + ujMLJ Menu bar audit (Task 0) | D-39 strict source-wins — all 3 affordances ship |
       | `Section / Header / cta-row / primaryCTA` | ref + descendants | ref / V4Dx4i descendant enabled / iconTrailing icon glyph / fill | `M7eUr` / `enabled:true` / `arrow-right` / `#ffffffff` | (ref to Default Button) / (Pencil mechanism) / Pattern A lucide / `color-primitive-white` | Plan 25-01 Task 3 (D-44 actual-use resolution of OPEN-24-06) | First production use of Button iconTrailing slot |
       | `Section / Header / cta-row / secondaryCTA` | ref + descendants | ref / iconTrailing descendant enabled / iconTrailing icon glyph / fill | `<secondaryButtonId>` / `enabled:true` / `arrow-right` / `#141f39ff` | (ref to Secondary) / (Pencil mechanism) / Pattern A lucide / `color-semantic-text-primary` | Plan 25-01 Task 3 (D-44) | First production use of Secondary's iconTrailing slot |
       | `Section / Header / logo-slot` | slot + placeholder | slot / enabled / placeholder text | `[]` / `true` / `"<Crito wordmark literal>"` | (typing deferred to Plan 25-03 typed-slot probe outcome) | D-40 + Task 0 Open Q5 resolution | Logo slot pattern establishes the convention Card slots reuse |

       (Row count: 11 new rows minimum — depends on actual audit findings; could be more if nav-link count > 5 or if each is rowed individually.)

       (b) **Update `### Open Flags — Phase 24 (OPEN-24-NN)` table** Resolution column:
         - OPEN-24-06 row: append "Resolved 25-01: Header CTAs wired Primitive/Icon arrow-right via iconTrailing slot. Empty-slot collapse confirmed for unwired CTAs (e.g., label-only consumers); enabled:true + icon-ref-insertion correctly activates slot rendering per D-44 actual-use validation. See 25-01-SUMMARY.md § OPEN-24-06 Actual-Use Resolution."
         - OPEN-24-11 row: append "Resolved 25-01 Task 1: Secondary Button shipped via <preferred D-43 variant-axis-extension path | fallback D-43 sibling-component path per probe outcome>. Source-evidenced from Hero mkw8g per OPEN-24-11 retroactive note. See 25-01-SUMMARY.md § Variant-Axis Probe Result + § Secondary Button Build."

       (c) **Add `### Open Flags — Phase 25 (OPEN-25-NN)` section** if not already scaffolded (Phase 25 may add it here for the first time; Plan 25-03 will extend):
         - OPEN-25-XX (severity: notable; category: variant; consumer phase: 31 Homepage Hero) — "Secondary Button stroke color deviates from source-literal `#ffffffff` (Hero dark-bg) to `#141f39ff` (navy — Header light-bg context-aware) per Plan 25-01 Open Question 3 resolution. Component default is navy (visible on light bg). Phase 31 Homepage Hero may override stroke to white at instance time to match Crito source on dark-bg hero context."

    4. **Snapshot_layout + screenshot validation per VAL-25-12, VAL-25-15:**
       - `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root. Expected: `"No layout problems."` (overlap check between top-level frames).
       - `mcp__pencil__snapshot_layout({ rootId: <headerId>, problemsOnly: true })`. Document per-frame text-clipping false-positives per Phase 24 quirk (24-05-SUMMARY).
       - `mcp__pencil__get_screenshot({ nodeId: <headerId> })`. Final visual checkpoint.
       - Per Phase 24 24-05 precedent: trust `get_screenshot` over `snapshot_layout` text-clipping flags. Document in SUMMARY.

    5. **Roadmap update:**
       - Edit `.planning/ROADMAP.md` Phase 25 plan list:
         - Change `- [ ] 25-01: Build Section / Header (logo + nav links + CTA button instance) using Primitive / Button and semantic tokens; document slot signature in sibling note`
         - To `- [x] 25-01-PLAN.md — Section / Header built; Secondary Button variant added (D-43); CTA iconTrailing wired arrow-right (D-44); resolves OPEN-24-06 + OPEN-24-11`
       - If the ROADMAP `**Plans:** TBD` line is still in place for Phase 25, update to `**Plans:** 3 plans`.

    6. **Write final 25-01-SUMMARY.md** with all sections populated:
       - `## Active-Editor Pre-flight Result (D-54)` (Task 0)
       - `## Guidelines Snapshot` (Task 0)
       - `## Crito Menu Bar Source Audit` (Task 0)
       - `## Decisions (Open Questions 1 + 3 + 5)` (Tasks 0 + 1)
       - `## Variant-Axis Probe Result (Open Question 1)` (Task 1)
       - `## Secondary Button Build` (Task 1)
       - `## Section/Header Build (Task 2)`
       - `## CTA Wire-up (Task 3)`
       - `## OPEN-24-06 Actual-Use Resolution (D-44)` (Task 3)
       - `## PEN-INVENTORY Updates (D-56)` (Task 4)
       - `## snapshot_layout Audit` (Task 4 — document-level PASS + per-frame quirk notes)
       - `## Reference Screenshot Set (VAL-25-23)` (inline screenshots from Tasks 1 + 3 + 4 with user approval timestamps)
       - `## VAL-25-* Outcomes for Plan 25-01` (mark each relevant VAL id as PASS / PENDING)
       - `## Files Modified` (the files_modified list rendered concretely)
       - `## Cross-cutting must_haves Satisfaction` (per-truth row YES/NO)

    7. **Plan close gate (matches Phase 24 plan-close discipline):** Visual verification was captured in Tasks 1 and 3. This task's final user gate is the Roadmap checkbox update + SUMMARY commit. If executing inside `/gsd:execute-phase`, hand control back to orchestrator with structured-return.
  </action>
  <verify>
    - Pre-flight passed.
    - Sibling note exists under g9oRa5 with slot-signature text.
    - PEN-INVENTORY Variant Evidence (Phase 24) extended by N rows (counted in SUMMARY).
    - OPEN-24-06 + OPEN-24-11 resolution columns updated.
    - OPEN-25-NN section seeded (or extended) with OPEN-25-XX Secondary-stroke context-deviation row.
    - Document-level snapshot_layout returns "No layout problems."; per-frame quirks documented per Phase 24 precedent.
    - get_screenshot of Section/Header captured.
    - ROADMAP.md Phase 25 plan checkbox updated; `**Plans:** 3 plans` line written if not already.
    - 25-01-SUMMARY.md written with all sections.
  </verify>
  <acceptance_criteria>
    - VAL-25-01 PASS: `batch_get(g9oRa5, readDepth: 2)` returns `Section / Header` child with `reusable: true`.
    - VAL-25-07 PASS: header cta-row contains 2 Button refs.
    - VAL-25-08 PASS: each CTA's iconTrailing descendant `enabled: true` + contains lucide arrow-right icon.
    - VAL-25-11 PASS: Secondary Button variant present with source-evidenced values + source citation `mkw8g`.
    - VAL-25-12 PASS: Section/Header uses Pencil auto-layout (all containers `horizontal`).
    - VAL-25-14 PASS for Plan 25-01: 4+ pre-flight calls logged, 0 mismatch incidents.
    - VAL-25-15 PASS at document level: "No layout problems."; per-frame quirks documented per 24-05-SUMMARY.
    - VAL-25-16 PASS: PEN-INVENTORY § Variant Evidence (Phase 24) extended with Phase-25-late-addition + Section/Header rows.
    - VAL-25-19 PASS: OPEN-24-06 resolution column updated with Plan 25-01 citation.
    - VAL-25-20 PASS: OPEN-24-11 resolution column updated with Plan 25-01 citation.
    - VAL-25-23 PARTIAL: Plan 25-01 reference screenshot set captured + user-approved at Tasks 1/3 checkpoints; full Phase 25 user gate at Plan 25-03 close.
    - PEN-INVENTORY § Open Flags — Phase 25 (OPEN-25-NN) section now exists with at least 1 row (OPEN-25-XX Secondary stroke context-deviation).
    - ROADMAP.md Phase 25 plan checkbox for 25-01 marked complete.
  </acceptance_criteria>
  <done>
    25-01-SUMMARY.md exists at `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` with all 13 sections populated. PEN-INVENTORY.md extended with N Variant Evidence rows + 2 OPEN-24-NN resolution updates + at least 1 OPEN-25-XX seed row. ROADMAP.md updated. Plan 25-02 can proceed (sequential per D-57).
  </done>
</task>

</tasks>

<verification>
**Per-plan validation (VAL-25-* assertions from 25-VALIDATION.md applicable to Plan 25-01):**

| VAL ID | Owner Task | How Verified |
|---|---|---|
| VAL-25-01 (Section/Header under g9oRa5 reusable:true) | Task 2 build + Task 4 final | `mcp__pencil__batch_get({ queries: [{ id: "g9oRa5", readDepth: 2 }] })` → confirm child node `Section / Header` with `reusable: true` |
| VAL-25-07 (Header instances Button twice — dual CTA per D-42) | Task 3 + Task 4 | `mcp__pencil__batch_get({ queries: [{ id: <ctaRowId>, readDepth: 2 }] })` → confirm 2 `type: ref` children pointing at M7eUr + secondaryButtonId |
| VAL-25-08 (Header CTA iconTrailing populated with lucide arrow-right per D-44) | Task 3 + Task 4 | Walk both CTAs; verify V4Dx4i (primary) + Secondary's iconTrailing have `enabled: true` and contain `type: icon, library: lucide, icon: arrow-right` |
| VAL-25-11 (Secondary Button variant present per D-43 — preferred or fallback) | Task 1 + Task 4 | Walk M7eUr (if D-43 preferred path) OR avgor (if D-43 fallback path); confirm Secondary cell/component present with `fill: none, stroke: #141f39ff, strokeWidth: 0.5` |
| VAL-25-12 (auto-layout, no absolute positioning) | Task 2 + Task 4 | Walk all Section/Header containers; assert `layout` property non-null on every container frame |
| VAL-25-14 (pre-flight active-editor before every mutation) | All tasks | Grep SUMMARY.md for `get_editor_state` pre-flight logs; count ≥ 4 across the plan; assert 0 mismatch incidents |
| VAL-25-15 (snapshot_layout clean at document level + per-frame quirks documented) | Task 4 | Snapshot at maxDepth:0 + per-frame on Section/Header; document quirks per Phase 24 precedent |
| VAL-25-16 (PEN-INVENTORY Variant Evidence extended per D-56) | Task 4 | Diff PEN-INVENTORY before/after; verify ≥ 11 new rows |
| VAL-25-19 (OPEN-24-06 RESOLVED) | Task 3 + Task 4 | PEN-INVENTORY OPEN-24-06 row Resolution column updated |
| VAL-25-20 (OPEN-24-11 RESOLVED) | Task 1 + Task 4 | PEN-INVENTORY OPEN-24-11 row Resolution column updated |
| VAL-25-23 (user visual checkpoint at each plan close — Plan 25-01 portion) | Tasks 1, 3, 4 | Reference screenshot set captured inline; user-approved at Task 1 and Task 3 checkpoints |

**Cross-cutting Phase 24 carry-forward:**
- D-54 active-editor pre-flight enforced before every batch_design (Tasks 1, 2, 3, 4 each include pre-flight).
- snapshot_layout text-clipping quirk documented per 24-05-SUMMARY precedent.
- OPEN-23-13 dual-track: literal hex/font values in batch_design payloads; Variant Evidence rows record the conceptual token binding.

**Phase 24 baseline-drift verification:**
- Walk Phase 24 baseline IDs in id-inventory.json `baseline_drift_verification.expected_baseline_ids_unmutated`: MIXGf, QdwxP, kicJ8, ujMLJ, VleVl, cl8tt, w1m3x, DzqTm, cYlRH, Y2isa, WDGxc, 1nLS3, Jmdw0, maDc3, IKAu3, RpGbe. None should be mutated by this plan (only g9oRa5 and avgor children grow; their property bags don't change).
- M7eUr touched ONLY if Task 1 D-43 preferred path succeeds (adding new variant cell — Hover/Focus cells YJhRv/gQa2R untouched per Pitfall 6 enforcement).
</verification>

<success_criteria>
**Plan 25-01 complete when:**

1. `Section / Header` exists in `design/Crito.pen` as a reusable component inside `_Components / Sections` (g9oRa5), with:
   - Fixed width matching Crito Home Page Menu bar `ujMLJ` per D-41 (value from Task 0 audit).
   - `logo-slot` child frame with `slot:[], enabled:true` + Crito-wordmark text placeholder per D-40 + Task 0 Open Q5 resolution.
   - `nav-links` row with literal source labels enumerated from Task 0 audit per D-38.
   - `affordances` row with 3 Pencil-native lucide icons (search + chevron-down + menu) per D-39.
   - `cta-row` with 2 Button refs (Primary Default + Secondary new) per D-42; each CTA's iconTrailing slot `enabled:true` + populated with lucide arrow-right per D-44.

2. Secondary Button variant exists in `design/Crito.pen` per D-43:
   - Preferred path: new `purpose: Secondary` variant cell inside M7eUr.
   - OR fallback path: sibling `Primitive / Button / Secondary` reusable component inside avgor.
   - Source-evidenced values: fill:none, stroke `#141f39ff` (Task 0 Open Q3 navy default), strokeWidth:0.5, strokeAlignment:inner, cornerRadius:10, padding:[16,20], gap:10.

3. Sibling Pencil note inside g9oRa5 documents Section/Header slot signature in human-readable form (COMP-05 Success Criterion 3).

4. PEN-INVENTORY.md extended:
   - § Variant Evidence (Phase 24) extended with ≥ 11 new rows per D-56.
   - § Open Flags — Phase 24: OPEN-24-06 + OPEN-24-11 Resolution columns updated with Plan 25-01 citations.
   - § Open Flags — Phase 25 (OPEN-25-NN) section exists with at least OPEN-25-XX (Secondary stroke context-deviation) row.

5. Document-level `snapshot_layout(maxDepth:0, problemsOnly:true)` returns "No layout problems."; per-frame text-clipping quirks documented per Phase 24 24-05-SUMMARY precedent.

6. User visually approved at Task 1 and Task 3 checkpoints (Plan 25-01 reference screenshot set built).

7. 25-01-SUMMARY.md exists with all 13 sections populated; VAL-25-* outcomes table marks all Plan 25-01 owned assertions as PASS or PARTIAL with citation.

8. ROADMAP.md Phase 25 plan list checkbox for 25-01 updated; `**Plans:** 3 plans` line set.

9. Phase 24 baseline IDs (incl. ujMLJ, M7eUr-line Hover/Focus YJhRv/gQa2R, Default slots AvKtA/V4Dx4i, label ATJK9) unmutated except for D-42/D-43 variant-axis addition (if preferred path used).

10. Plan 25-02 prerequisite met: Section component shipping pattern verified; OPEN-24-06 + OPEN-24-11 cleared.
</success_criteria>

<output>
After completion, write `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` per template structure shown in 24-04-SUMMARY.md / 24-05-SUMMARY.md precedent. Sections required: Active-Editor Pre-flight Result, Guidelines Snapshot, Crito Menu Bar Source Audit, Decisions (Open Q1+Q3+Q5), Variant-Axis Probe Result (Open Q1), Secondary Button Build, Section/Header Build, CTA Wire-up, OPEN-24-06 Actual-Use Resolution, PEN-INVENTORY Updates, snapshot_layout Audit, Reference Screenshot Set, VAL-25-* Outcomes for Plan 25-01, Files Modified, Cross-cutting must_haves Satisfaction.

PEN-INVENTORY.md updates committed in same plan close. ROADMAP.md updated. Commit message (if `commit_docs: true`): `exec(25-01): Section/Header — Secondary Button (D-43) + CTA arrow-right wiring (D-44) — resolves OPEN-24-06 + OPEN-24-11`.

**Resume signal for Plan 25-02:** Plan 25-01 SUMMARY committed + user-approved at Task 1 + Task 3 checkpoints. Plan 25-02 can begin (sequential per D-57).
</output>
