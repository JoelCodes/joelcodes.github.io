---
phase: 25-section-compound-components
plan: 25-03
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - design/Crito.pen (NEW reusable `Compound / Card` inside `_Components / Compounds` (t67DU6) — single slots-based component per COMP-07 hard-prohibition of multi-variant matrix; 4 slots per D-51: image-slot, title-slot, body-slot, footer-actions-slot; all 4 slots default `enabled:true` with placeholder content per D-53; Pencil-native typed-slot props per D-52 PREFERRED — slot:[<componentIds>] arrays where Task 2 probe shows typed-slot mechanic works cleanly, OR untyped slot:[] fallback per probe outcome)
  - design/Crito.pen (NEW sibling Pencil note inside t67DU6 documenting Compound/Card slot signature in human-readable form — D-52 belt-and-suspenders coverage)
  - .planning/research/PEN-INVENTORY.md (## Variant Evidence (Phase 24) extended with Compound/Card structural rows per D-56)
  - .planning/research/PEN-INVENTORY.md (NEW ## Compound Source Inference (Phase 25) section per D-49 + D-56 — per-slot raster citation rows: image-slot ← image-import-12 + 14, title-slot ← all four raster sources, body-slot ← per-consumer-shape, footer-actions-slot ← per-consumer-shape)
  - .planning/research/PEN-INVENTORY.md (### Open Flags — Phase 25 (OPEN-25-NN) extended with OPEN-25-XX: Card source-coverage gap declaration per D-50 — severity notable; consumer phase 28 Blog or 29 Projects, whichever first instances Card)
  - .planning/research/exports/v2.0/end-of-phase-25/id-inventory.json (NEW directory; structural snapshot per OPEN-23-01 substitution pattern — captures library_parents, sections (Header + Footer with IDs), compounds (Card + 4 slot IDs), substack_glyph, secondary_button (if D-43 preferred path used), validation_outcomes VAL-25-01..23)
  - .planning/research/exports/v2.0/end-of-phase-25/README.md (schema documentation + Phase 26 read instructions + cross-reference to end-of-phase-23/-24 precedent)
  - .planning/ROADMAP.md (Phase 25 plan checkbox for 25-03 updated; Phase 25 row in Progress table marked Complete)
  - .planning/STATE.md (current position updated: Phase 25 COMPLETE; next action: Phase 26)
autonomous: false
requirements_addressed:
  - COMP-07
  - COMP-08
  - COMP-09
must_haves:
  truths:
    - "`Compound / Card` exists under `_Components / Compounds` (t67DU6) as a single reusable component — NOT a multi-variant matrix (COMP-07 hard-prohibition enforced; VAL-25-03)"
    - "Card has EXACTLY 4 slots per D-51: image-slot, title-slot, body-slot, footer-actions-slot — generic enough to serve Project (image + title + date-in-body + Read-More-CTA) / Blog (image + title + excerpt + meta-row) / Service (optional-empty-image + title + description + arrow-CTA) via slot CONTENT, not variants (VAL-25-04)"
    - "All 4 slots default `enabled: true` with placeholder content per D-53 for library discoverability: image-shaped placeholder rect, 'Card title' text, 'Card body text. Replace at instance time...' filler, Primitive/Button/Default instance with 'Read More' label + arrow-right trailing icon (VAL-25-05)"
    - "Card slot signature documented via Pencil-native typed slot props per D-52 PREFERRED (where Task 2 probe confirms typed-slot mechanic) AND sibling Pencil note per D-52 belt-and-suspenders (both ship regardless of probe outcome) — VAL-25-06"
    - "Card source provenance is RASTER-DERIVED per D-49 — `design/images/image-import-12.jpg` (Service + Project cards), `image-import-14.jpg` (Blog cards), `image-import-8.jpg` (Blog detail context), `image-import-22.jpg` (About / Team cards context); v1.3 `src/components/ui/Card.astro` consulted as SECONDARY structural reference only (NOT visual values per Pitfall 2 prevention)"
    - "Card visual values (fill, border, cornerRadius, padding, typography) derive from Phase 23 token surface — `color-semantic-bg-card` (likely `#ffffff`), `color-semantic-border-default` (`#d4d4d8` 1px), `radius-semantic-card` if shipped OR `radius-semantic-button` (Phase 24 D-32 Case B reuse per Pitfall 7 — Open Question 7 resolution); typography: Plus Jakarta Sans title + Inter body matching Phase 23 type-semantic surface"
    - "NO Card hover/focus states ship in Phase 25 per Claude's Discretion default in CONTEXT (Crito raster doesn't depict; surface as OPEN-25-XX if Phase 28+ consumer surfaces need) — primitive-layer button hover/focus inside footer-actions-slot covers interactivity"
    - "Sibling Pencil note inside t67DU6 documents Card slot signature in human-readable form (COMP-07 Success Criterion 3) — even where Pencil-native typed slots succeed, the sibling note ships per D-52 belt-and-suspenders"
    - "PEN-INVENTORY § Variant Evidence (Phase 24) EXTENDED with Compound/Card structural rows per D-56"
    - "PEN-INVENTORY NEW § Compound Source Inference (Phase 25) section per D-49 + D-56 with per-slot raster citation rows for Card"
    - "PEN-INVENTORY § Open Flags — Phase 25 (OPEN-25-NN) extended with OPEN-25-XX Card source-coverage gap row per D-50 (severity: notable; consumer phase: 28 Blog or 29 Projects — first concrete consumer locks real values via overrides or component-edit; resolves the flag)"
    - "Zero raw color hex / zero raw px values inside Compound/Card beyond accepted Phase 23/24/25 Variant Evidence binding set (COMP-09; VAL-25-13 sweep)"
    - "Every Pencil-mutating batch is preceded by `mcp__pencil__get_editor_state({ include_schema: false })` asserting active editor == `design/Crito.pen` (D-54)"
    - "`snapshot_layout(maxDepth: 0, problemsOnly: true)` at document root returns `\"No layout problems.\"`; per-frame text-clipping false-positives documented per Phase 24 quirk (24-05-SUMMARY)"
    - "Phase 24 baseline IDs unmutated; Plan 25-01 outputs (Section/Header + Secondary Button) intact; Plan 25-02 outputs (Section/Footer + Substack glyph) intact — only t67DU6 children grow"
    - "`.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json` written per OPEN-23-01 substitution pattern; mirrors end-of-phase-23/-24 schema"
    - "Phase 25 close gate user-approved; STATE.md updated; ROADMAP.md Progress table marks Phase 25 row Complete"
  artifacts:
    - path: "design/Crito.pen"
      provides: "Compound / Card reusable component inside _Components / Compounds (t67DU6)"
      contains: "Single component with exactly 4 slots (image-slot, title-slot, body-slot, footer-actions-slot); all enabled:true with placeholder content per D-53; Pencil-native typed slot props per Task 2 probe outcome; sibling note per D-52"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Variant Evidence rows for Card + new ## Compound Source Inference (Phase 25) section + OPEN-25-XX seed row"
    - path: ".planning/research/exports/v2.0/end-of-phase-25/id-inventory.json"
      provides: "End-of-Phase-25 structural snapshot per OPEN-23-01 substitution pattern"
    - path: ".planning/research/exports/v2.0/end-of-phase-25/README.md"
      provides: "Schema documentation + Phase 26 read instructions"
    - path: ".planning/STATE.md"
      provides: "Phase 25 COMPLETE position; resume pointer for Phase 26"
  key_links:
    - from: "Compound / Card / footer-actions-slot"
      to: "Primitive / Button / Default (M7eUr) + Primitive / Button / Secondary (from Plan 25-01)"
      via: "Pencil slot.slot:[<componentIds>] typed-suggestion mechanism per D-52 PREFERRED (test in Task 2 probe); fallback untyped slot:[] per D-52 fallback"
      pattern: "RESEARCH Pattern 4 (Slot-typed Compound/Card)"
    - from: "Compound / Card / footer-actions-slot default content"
      to: "Primitive / Button / Default ref with 'Read More' label override + iconTrailing arrow-right wired"
      via: "Pencil ref + descendants override (RESEARCH Pattern 3 carry-forward from Plan 25-01)"
      pattern: "D-53 library-discoverability — Card preview shows complete-looking populated card"
    - from: "Compound / Card visual values"
      to: "Phase 23 token surface (color-semantic-bg-card, -border-default; radius-semantic-card or -button reuse; type-semantic-*)"
      via: "Plan 25-03 raster-probe inference per D-49 → Phase 23 semantic token map"
      pattern: "D-55 raster-probe inference (parallel to Plan 25-01/02 source-wins discipline for Sections)"
    - from: "PEN-INVENTORY § Compound Source Inference (Phase 25)"
      to: "design/images/image-import-{12,14,8,22}.jpg raster files"
      via: "Per-slot citation rows (Task 1 raster-audit table)"
      pattern: "D-49 + D-56 raster-probe provenance audit-trail discipline"
---

<objective>
Build `Compound / Card` (COMP-07) inside the Phase 24 stub `_Components / Compounds` (t67DU6) as a SINGLE slots-based component — NOT a multi-variant matrix — explicitly per COMP-07 hard prohibition. Per D-51 the component ships exactly 4 slots (image-slot, title-slot, body-slot, footer-actions-slot) generic enough to serve Project / Blog / Service card use cases via slot CONTENT. Per D-53 all 4 slots default `enabled: true` with placeholder content (image-shaped placeholder rect, "Card title" text, "Card body text" filler, Primitive/Button/Default "Read More" CTA with arrow-right) for visual library discoverability — consumers REPLACE placeholders at instance time.

**Raster-probe inference (D-49, D-55):** Card consumer pages (Blog index DzqTm, Blog Details w1m3x, Service Details cYlRH, Service Y2isa) are flat raster per Phase 23 OPEN-23-05 — no editable Crito source for cards exists. Task 1 reads `design/images/image-import-{12,14,8,22}.jpg` raster files via direct file I/O (PNG/JPG files outside `.pen` — direct I/O is ALLOWED per planning context) to infer Card visual structure. v1.3 `src/components/ui/Card.astro` is consulted as SECONDARY structural reference ONLY (Pitfall 2 prevention: do NOT use v1.3 neobrutalist visual values).

**Probe-first within the plan (Open Question 2):** Pencil 2.13 typed-slot mechanics — does `slot:[<componentIds>]` enforce non-matching insertion rejection OR is it suggestion-only? Task 2 EXECUTES the probe on a single Card slot before propagating to all 4. The sibling Pencil note per D-52 ships regardless of probe outcome — belt-and-suspenders coverage.

**OPEN-25 flag declaration per D-50:** Per D-49 stronger-than-Badge-inference provenance, Card source-coverage gap is declared as OPEN-25-XX (severity: notable; consumer phase: 28 Blog or 29 Projects). First concrete consumer locks real values via consumer-site overrides or component-edits and resolves the flag. Follows Phase 24's OPEN-24-12 (Badge) consumer-resolution pattern.

**Phase 25 close gate:** Plan 25-03 is the final plan in Phase 25 (D-57 sequential order: Header → Footer → Card). Plan 25-03 close includes (in addition to Card-specific deliverables): Phase 25 sweep (Plan 24-05 walker pattern applied to new Section + Compound + Substack-glyph + Secondary-Button nodes); `id-inventory.json` write to `.planning/research/exports/v2.0/end-of-phase-25/`; final user gate; STATE.md update (Phase 25 → completed); ROADMAP Progress table row Complete.

**Purpose:** Ships the final Phase 25 component. Validates Phase 25 D-55 parallel disciplines (raster-probe-inference for Compound/Card vs source-wins for Sections — both deliver shipping components with documented provenance). Validates Pencil slot mechanics for downstream Phase 28+ Card consumers. Closes Phase 25 cleanly per Phase 24 24-05 precedent.

**Output:** `Compound / Card` inside `_Components / Compounds`; new § Compound Source Inference (Phase 25) section in PEN-INVENTORY; OPEN-25-XX seed row; `end-of-phase-25/id-inventory.json` archival; 25-03-SUMMARY.md documenting Task 1 raster-audit, Task 2 slot-typing probe outcome, Card build, Phase 25 sweep result, user visual checkpoint approval; STATE.md + ROADMAP.md updates.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-section-compound-components/25-CONTEXT.md
@.planning/phases/25-section-compound-components/25-RESEARCH.md
@.planning/phases/25-section-compound-components/25-VALIDATION.md
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md
@.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-24/id-inventory.json
@design/images/image-import-12.jpg
@design/images/image-import-14.jpg
@design/images/image-import-8.jpg
@design/images/image-import-22.jpg
@src/components/ui/Card.astro
@./CLAUDE.md
</context>

<threat_model>
**T-25-T1 (active-editor-swap — MEDIUM severity, BLOCKING)** — Same Pencil VS Code extension active-editor-swap threat as Plans 25-01 + 25-02. Mitigation: D-54 — `mcp__pencil__get_editor_state({ include_schema: false })` BEFORE every `batch_design` batch. Expected pre-flight count: ~3-5 (Task 2 typed-slot probe, Task 3 Card build, Task 4 close-sweep verifications).

**T-25-I1 (probe-artifact leak — LOW)** — Task 2 typed-slot probe inserts a test slot frame for outcome confirmation. Mitigation: probe artifact deleted before Task 3 build commences (Phase 24 24-04 precedent).

**T-25-V1 (Card-variant-creep — HIGH severity, PROCESS-BLOCKING)** — Pitfall 1 (highest-risk Phase 25 pitfall): Task 1 raster-audit surfaces three visually-distinct card shapes (Service / Project / Blog) and tempts shipping three variants. COMP-07 hard-prohibits this. Mitigation: Task 1 explicit acceptance criterion that the single 4-slot signature serves all three card uses; Task 4 audit confirms `_Components / Compounds` contains EXACTLY ONE `Compound / Card` child (no parallel `Compound / Card / Project`, `Compound / Card / Blog`, `Compound / Card / Service` variants).

**T-25-V2 (v1.3-visual-bleed — MEDIUM severity)** — Pitfall 2: v1.3 `src/components/ui/Card.astro` neobrutalist yellow-shadow pattern bleeds into Card visual values when v1.3 is consulted as secondary reference. Mitigation: D-49 ordering: raster-probe PRIMARY (Task 1 reads images BEFORE consulting Card.astro); v1.3 Card.astro consulted ONLY for structural reference (slot count, content order); visual values (fill, border, shadow, cornerRadius) derive from raster + Phase 23 tokens; explicit acceptance: NO neobrutalist `6px 6px 0` shadow pattern; NO yellow accent colors on Card frame.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + Plan 25-01/02 verification + raster-audit infrastructure scaffolding (PEN-INVENTORY new sections)</name>
  <files>
    - .planning/research/PEN-INVENTORY.md (NEW section header `## Compound Source Inference (Phase 25)` scaffolded with empty table per D-18 plain-markdown; § Open Flags — Phase 25 (OPEN-25-NN) section scaffolded if not already created in Plan 25-01/02)
  </files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-49, D-50, D-51, D-52, D-53, D-54, D-55, D-56, D-57)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Recommended `_Components / Compounds / Card` Structure", § "Code Examples Pattern 4 (Slot-typed Compound/Card)", § "Open Questions" 2 + 7)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (Wave 0 Requirements: § "Compound Source Inference (Phase 25)" + § "Open Flags — Phase 25 (OPEN-25-NN)" must be scaffolded BEFORE Plan 25-03 build runs)
    - .planning/research/PEN-INVENTORY.md (current state of § Open Flags — Phase 25 — Plan 25-01 + 25-02 may have already created it)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (Plan 25-01 outputs)
    - .planning/phases/25-section-compound-components/25-02-SUMMARY.md (Plan 25-02 outputs)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (library_parents."_Components / Compounds" t67DU6; primitives.Button.parent M7eUr + secondaryButtonId from Plan 25-01)
  </read_first>
  <action>
    1. Pre-flight per D-54: `mcp__pencil__get_editor_state({ include_schema: false })`. Assert active editor == `design/Crito.pen`.

    2. Confirm Pencil schema is current: call `mcp__pencil__get_editor_state({ include_schema: true })` if not cached.

    3. **Verify Plan 25-01 + 25-02 outputs intact:**
       - `batch_get({ queries: [{ id: "g9oRa5", readDepth: 2 }] })` — confirm Section/Header AND Section/Footer both present with reusable:true.
       - Plan 25-01 D-43 path verification: confirm Secondary Button intact at recorded id (per 25-01-SUMMARY.md — either inside M7eUr or sibling in avgor).
       - Plan 25-02 Substack atomic-glyph verification: confirm `Primitive / Icon / glyphs / substack` reusable component intact at recorded id.
       - Record verification outcomes in 25-03-SUMMARY.md DRAFT.

    4. **Scaffold PEN-INVENTORY new sections (Wave 0 Requirements per 25-VALIDATION.md):**
       - Check if `## Compound Source Inference (Phase 25)` exists in PEN-INVENTORY.md. If not, add the section header with empty table scaffold:
         ```markdown
         ## Compound Source Inference (Phase 25)

         Per D-49: Card source provenance is raster-derived from `design/images/image-import-{12,14,8,22}.jpg` (consumer page raster references; Crito Project/Blog/Service frames are flat raster per OPEN-23-05). v1.3 `src/components/ui/Card.astro` consulted as SECONDARY structural reference only — NOT visual values (Pitfall 2 prevention).

         | component | slot_or_property | source_image | inferred_value | confidence | rationale |
         |---|---|---|---|---|---|

         Populated by Plan 25-03 Task 1 raster-audit + Task 3 build.
         ```
       - Check if `### Open Flags — Phase 25 (OPEN-25-NN)` section exists (Plan 25-01 / 25-02 may have created it). If not, add the section header:
         ```markdown
         ### Open Flags — Phase 25 (OPEN-25-NN)

         Per D-50 + Phase 23 D-09 carry-forward: OPEN flags do NOT block phase close. First concrete consumer resolves each flag.

         | id | category | severity | description | consumer_phase | resolution |
         |---|---|---|---|---|---|
         ```
       - If Plan 25-01/02 already added rows, preserve them. Plan 25-03 will add OPEN-25-XX (Card source-coverage) row at Task 4.

    5. Record `## Plan 25-01/02 Outputs Verification` + `## PEN-INVENTORY Scaffolding` sections in 25-03-SUMMARY.md DRAFT.
  </action>
  <verify>
    - Pre-flight passed.
    - Plan 25-01 + 25-02 outputs verified intact (Section/Header, Section/Footer, Secondary Button, Substack glyph all present).
    - PEN-INVENTORY § Compound Source Inference (Phase 25) section header exists with empty table scaffold.
    - § Open Flags — Phase 25 (OPEN-25-NN) section exists with table header.
    - SUMMARY DRAFT records verification + scaffolding.
  </verify>
  <acceptance_criteria>
    - VAL-25-14 satisfied for this task: pre-flight call logged in SUMMARY.
    - VAL-25-17 progress: § Compound Source Inference (Phase 25) section header now exists per D-49 + D-56 (Task 1 populates rows; Task 4 verifies).
    - VAL-25-18 progress: § Open Flags — Phase 25 (OPEN-25-NN) section exists (Task 4 adds OPEN-25-XX seed row).
    - Wave 0 Requirements per 25-VALIDATION.md satisfied: both new audit-trail sub-sections scaffolded BEFORE build tasks run.
    - Plan 25-01 + 25-02 outputs intact (no regression).
  </acceptance_criteria>
  <done>
    25-03-SUMMARY.md DRAFT exists with sections `## Active-Editor Pre-flight Result (D-54)`, `## Plan 25-01/02 Outputs Verification`, `## PEN-INVENTORY Scaffolding`. PEN-INVENTORY.md has the two new section scaffolds. Tasks 1-4 can populate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Raster-probe Card audit (D-49 — read image-import-{12,14,8,22}.jpg) + secondary v1.3 Card.astro structural reference</name>
  <files>none (read-only — direct file I/O on raster JPGs + v1.3 Card.astro source; PEN-INVENTORY rows committed at Task 4)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-49, D-50, D-51, D-53, D-55)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Recommended `_Components / Compounds / Card` Structure", § "Common Pitfalls" 1 (Card-variants creep) + 2 (Building Compound/Card from inference without raster grounding))
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-04, VAL-25-13, VAL-25-17)
    - .planning/research/PEN-INVENTORY.md (§ "Audit Findings" — list of flat-raster frames per OPEN-23-05; § "Tokens Written — Primitives" + § "Tokens Written — Semantic Aliases" for color/spacing/radius tokens to validate Card values against)
    - design/images/image-import-12.jpg (Service page — Service cards 4-up + Project cards 2x2 — visible per direct-image-read context: bottom-3rd shows "Let's Looks Our Global Projects" with 2x2 image+title+date cards; top-3rd shows "We Provide The Best Service For Consulting" with 4x2 white cards containing only title + body text + circular black "Read More" affordance)
    - design/images/image-import-14.jpg (Blog index — Blog cards 3-up: image-on-top + title + meta-row)
    - design/images/image-import-8.jpg (Blog detail context — Header + post structure; cards may appear in "related posts" strip at bottom)
    - design/images/image-import-22.jpg (About frame — Team member cards row, possibly with image + name + role pattern that maps to Card title + body)
    - src/components/ui/Card.astro (v1.3 design-system Card — SECONDARY structural reference only per D-49; informs slot count + content order; visual values explicitly NOT to be copied per Pitfall 2)
  </read_first>
  <action>
    1. **Read all 4 raster files** via `Read` tool (direct file I/O on JPG files is ALLOWED per planning context — these are PNG/JPG outside Pencil-encrypted `.pen`):
       - `Read({ file_path: "/Users/joel/Desktop/Claude-Demos/joel-shinness-website/design/images/image-import-12.jpg" })`
       - `Read({ file_path: "/Users/joel/Desktop/Claude-Demos/joel-shinness-website/design/images/image-import-14.jpg" })`
       - `Read({ file_path: "/Users/joel/Desktop/Claude-Demos/joel-shinness-website/design/images/image-import-8.jpg" })`
       - `Read({ file_path: "/Users/joel/Desktop/Claude-Demos/joel-shinness-website/design/images/image-import-22.jpg" })`

    2. **Enumerate card sub-regions per raster** in 25-03-SUMMARY.md DRAFT `## Raster Card Audit (D-49)` table:
       | image | card_use_case | observed_structure | notes |
       |---|---|---|---|
       | image-import-12 (Service page top-3rd) | Service card (4x2 grid) | Title + body text + circular "Read More" affordance on solid-color tile (1 of 8 is yellow `#fdba09`-ish; rest are white); rounded corners ~10px; no image at top | Per visual: title "Business Advice" / body "Deliver faster, more personalized support with shared assets and a Support Bot"; black-circle affordance at bottom-right — 1 of 8 cards has yellow background (selected/active state visualization, not a Card variant — instance-level override) |
       | image-import-12 (Service page bottom-3rd) | Project card (2x2 grid) | Image (filling top) + title text + date text + "Read More →" CTA on white tile; rounded corners ~10px; thin neutral border | Per visual: image "Business" cover + title "Why your client needs a responsive website" + date "April 30, 2020" + "Read More →" right-side affordance |
       | image-import-14 (Blog index) | Blog card (3-up) | Image (filling top) + title + meta-row (date + category badge maybe) | Confirm via Task 1 visual; likely image + title + excerpt + meta-row pattern (same as Project but body slot is excerpt not date) |
       | image-import-8 (Blog detail) | Related post cards (if present in strip) | Smaller variant of Blog card | Visual-confirm |
       | image-import-22 (About / Team) | Team-member cards | Image (portrait) + name + role + (optional) social-icons | Title slot = name, body slot = role |

    3. **Confirm single 4-slot signature serves all 3+ card use cases** (D-51 + Pitfall 1 prevention):
       - Project card = image-slot (project cover) + title-slot ("Why your client...") + body-slot (date "April 30, 2020") + footer-actions-slot ("Read More →" CTA).
       - Blog card = image-slot (post cover) + title-slot (post title) + body-slot (excerpt or meta-row) + footer-actions-slot (Read More or social-actions).
       - Service card = image-slot (DISABLED via consumer override OR icon — visible in image-import-12 top-3rd: NO image in Service cards) + title-slot ("Business Advice") + body-slot (description) + footer-actions-slot (circular Read More affordance — instance override using small icon-only Button variant, OR override Button to `iconOnly` style).
       - Team-member card (About) = image-slot (portrait) + title-slot (name) + body-slot (role) + footer-actions-slot (social icons row OR empty per instance need).
       - **All 4 use cases map to the same 4-slot signature via slot CONTENT.** No variants needed. Record explicit acceptance attestation in SUMMARY.

    4. **Consult v1.3 `src/components/ui/Card.astro` as SECONDARY structural reference** (D-49 — for structure, NOT visual values):
       - `Read({ file_path: "/Users/joel/Desktop/Claude-Demos/joel-shinness-website/src/components/ui/Card.astro" })`.
       - Extract structural pattern: how many slots? content order? are there hover/focus props?
       - Compare against the 4-slot signature derived from raster. Record alignment + any deviations.
       - **Explicitly do NOT copy visual values:** v1.3 Card.astro uses neobrutalist yellow-shadow pattern (`6px 6px 0` offset, yellow accent fills). Card.astro shadow + fill values are FORBIDDEN inputs to the Pencil Card visual properties. Visual values derive from raster + Phase 23 tokens. Record this Pitfall 2 attestation.

    5. **Resolve Open Question 7 inline (Card cornerRadius source-evidence):**
       - Phase 23 may have shipped a `radius-semantic-card` token OR not. Check via `mcp__pencil__get_variables({})` Phase 23 token map.
       - If `radius-semantic-card` exists: use it (preferred — semantic alignment).
       - If not: reuse `radius-semantic-button` = 10 per Phase 24 D-32 Case B precedent (Badge radius reuse) → Pitfall 7 enforcement (no new tokens added for inferred component).
       - Record decision in SUMMARY.

    6. **Derive visual values from raster + Phase 23 tokens (D-55 raster-probe-inference):**
       - Card fill: white `#ffffffff` → `color-semantic-bg-card` (verify alias exists; if not, use `color-primitive-white` direct per OPEN-23-13 dual-track).
       - Card border: 1px neutral `#d4d4d8ff` → `color-semantic-border-default` (Phase 24 Input precedent).
       - Card cornerRadius: 10 (per Open Q7 resolution).
       - Card padding (inside content-stack): 24 → `space-semantic-card-inner` if exists, else `space-primitive-24`.
       - Image-slot aspect: ~16:10 (raster visual approximation; e.g. 380w × 220h for a 380px-wide card).
       - Image-slot placeholder fill: `#fafafaff` (light gray placeholder) → `color-semantic-bg-muted` if exists.
       - Title typography: Plus Jakarta Sans 20/700 (matches Phase 23 heading scale; raster shows clear-larger-bold title) → `type-semantic-heading-N-*` where appropriate (or primitive direct per Phase 23 surface).
       - Body typography: Inter 14/400 (matches Phase 23 body-sm) → `type-semantic-body-sm-*` + `color-semantic-text-secondary`.
       - Record all derived values in `## Card Visual Values Derivation` SUMMARY section with raster + token citations.

    7. **Open Question 7 + Card cornerRadius + Pitfall 2 attestations** recorded.
  </action>
  <verify>
    - 4 raster files read; observed-structure rows populated for each card use case.
    - Single 4-slot signature confirmed to serve Project + Blog + Service + Team-member card use cases.
    - v1.3 Card.astro consulted; structural alignment recorded; visual values explicitly NOT copied.
    - Card visual values derived from raster + Phase 23 tokens; all values traceable.
    - Open Question 7 cornerRadius resolution recorded.
    - Pitfall 1 (variants creep) + Pitfall 2 (v1.3 visual bleed) attestations recorded.
  </verify>
  <acceptance_criteria>
    - VAL-25-04 pre-validation: 4-slot signature confirmed adequate for Project/Blog/Service/Team-member uses.
    - VAL-25-13 pre-validation: every derived visual value either matches a Phase 23 token OR is recorded as a Variant Evidence binding row (no raw values without traceability).
    - VAL-25-17 progress: § Compound Source Inference (Phase 25) section populated with per-slot raster citations (Task 4 commits row writes).
    - Pitfall 1 attestation: single component, 4 slots, NO multi-variant matrix planned.
    - Pitfall 2 attestation: v1.3 visual values explicitly NOT used; raster + Phase 23 tokens are the source.
    - Open Q7 resolution: `radius-semantic-card` (if shipped) OR `radius-semantic-button` reuse per Phase 24 D-32 Case B precedent.
    - Zero `design/Crito.pen` mutations attempted in this task.
  </acceptance_criteria>
  <done>
    25-03-SUMMARY.md DRAFT `## Raster Card Audit (D-49)`, `## v1.3 Card.astro Structural Reference (Secondary, D-49)`, `## Card Visual Values Derivation`, `## Decisions (Open Q7)` sections populated. Task 2 + 3 can build without re-running raster audit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pencil typed-slot mechanics probe (D-52, Open Question 2)</name>
  <files>design/Crito.pen (probe artifact: temporary test slot frame inside t67DU6 — deleted at task close)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-52, D-53)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Pencil Slot Mechanics", § "Code Examples Pattern 4 (Slot-typed Compound/Card)", § "Open Questions" 2, § "Common Pitfalls" 4 (Treating slot signature as Pencil-note-only))
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-06 + Manual-Only "Pencil typed-slot probe result (D-52 Q1)")
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 2 Slots: slot accepts `false | string[]` array of recommended component IDs; insertion path `parentBinding/slotId`; `enabled: false` for empty slots)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (primitives.Button.parent = M7eUr; primitives.Button.slots indicate Phase 24 Buttons use untyped slots with enabled:false per OPEN-24-06)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (Plan 25-01 OPEN-24-06 actual-use resolution outcome — informs whether typed-slot patterns work in this Pencil version)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Probe: typed slot suggestion mechanic** (Open Q2 — does `slot:[<componentIds>]` reject non-matching insertions OR is it suggestion-only?):
       - Insert a temporary test slot frame inside t67DU6:
         ```text
         mcp__pencil__batch_design({
           operations: [
             'probeSlotFrame=I(t67DU6,{type:"frame",name:"Probe-TypedSlot",x:<X>,y:<Y>,width:200,height:80,reusable:false})',
             'probeTypedSlot=I(probeSlotFrame,{type:"frame",name:"footer-actions-slot",slot:["M7eUr"],enabled:true,layout:"horizontal",alignItems:"center"})'
           ]
         })
         ```
         (Use the Plan 25-01 secondaryButtonId in the slot array too if useful — `slot: ["M7eUr", "<secondaryButtonId>"]`.)

    3. **Verify typed-slot rendering + insertion behavior:**
       - `mcp__pencil__batch_get({ queries: [{ id: <probeTypedSlotId>, readDepth: 2 }] })`. Confirm:
         (a) `slot` property persisted as `["M7eUr"]` (or `["M7eUr", "<secondaryButtonId>"]`).
         (b) `enabled: true` persisted.
       - `mcp__pencil__get_screenshot({ nodeId: <probeSlotFrameId> })`. Visual: does Pencil render the typed-slot affordance (e.g., a special UI marker, suggestion-tooltip, etc.)? Capture observation.

    4. **Probe: Untyped slot comparison** (control for D-52 fallback):
       - Add a sibling untyped slot in the same probeSlotFrame:
         ```text
         mcp__pencil__batch_design({
           operations: [
             'probeUntypedSlot=I(probeSlotFrame,{type:"frame",name:"untyped-slot",slot:[],enabled:true,layout:"horizontal",alignItems:"center"})'
           ]
         })
         ```
       - `batch_get` + `get_screenshot` again. Compare: does untyped slot get the same affordance? Different affordance? No affordance?

    5. **Probe: Empty-slot collapse behavior** (D-53 + Phase 24 D-30 carry-forward):
       - Add a third sibling with `enabled: false`:
         ```text
         mcp__pencil__batch_design({
           operations: [
             'probeDisabledSlot=I(probeSlotFrame,{type:"frame",name:"disabled-slot",slot:[],enabled:false,layout:"horizontal"})'
           ]
         })
         ```
       - Capture screenshot. Does the disabled slot fully collapse (zero-area in horizontal layout)? Or does it occupy default-content space?

    6. **Decision: D-52 PREFERRED path or FALLBACK?**
       - If typed slots (Step 3) showed a discoverable Pencil-UI affordance (suggestion-list, tooltip, special marker): PREFERRED — Task 3 ships Card slots with `slot: [<componentIds>]` arrays where appropriate.
       - If typed slots showed no special affordance vs untyped: typed-slot mechanism is suggestion-only with no visible UI difference — STILL PREFERRED at the data-shape level for downstream consumers/AI agents (it's metadata that informs Pencil's API even if no UI affordance shows). Task 3 still ships typed where logical.
       - If typed slots rejected/errored: FALLBACK — Task 3 ships untyped `slot: []` everywhere. Sibling note carries the contract.
       - Record decision in 25-03-SUMMARY.md `## Slot Mechanics Probe Results (Open Q2)` section.

    7. **User visual checkpoint:** Present screenshots (typed vs untyped vs disabled). Ask user to confirm:
       (a) Typed-slot behavior observed (Pencil UI affordance present? if so, describe).
       (b) Empty-slot collapse confirmed for `enabled:false`.
       (c) Approve D-52 path decision for Task 3 propagation.

    8. **Probe artifact cleanup:**
       ```text
       mcp__pencil__batch_design({
         operations: [
           'Delete(<probeSlotFrameId>)'
         ]
       })
       ```
       - Verify absence via batch_get.
  </action>
  <verify>
    - Pre-flight passed.
    - 3 probe slots inserted (typed, untyped, disabled); behavior observed for each.
    - User approved D-52 path decision.
    - Probe artifacts deleted.
    - SUMMARY records Open Q2 outcome.
  </verify>
  <acceptance_criteria>
    - VAL-25-14 satisfied for this task: pre-flight + each batch_design preceded by `get_editor_state`.
    - Open Question 2 resolution recorded: typed-slot behavior observed; D-52 PREFERRED or FALLBACK path chosen with user approval.
    - Pitfall 4 prevention: typed-slot mechanism investigated (not skipped); sibling note ships regardless per D-52 belt-and-suspenders.
    - Empty-slot collapse behavior confirmed (carry-forward from Phase 24 D-30 + Plan 25-01 OPEN-24-06 resolution).
    - Probe artifacts absent from `design/Crito.pen` at task close.
  </acceptance_criteria>
  <done>
    25-03-SUMMARY.md `## Slot Mechanics Probe Results (Open Q2)` section populated with typed/untyped/disabled outcomes + screenshots + user approval. probeSlotFrame artifact deleted. D-52 path chosen for Task 3 propagation.

    Resume signal: User types "approved" or describes alternative path (e.g., "ship untyped everywhere, the typed-slot affordance isn't visible enough to be worth").
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build Compound / Card with 4 slots + placeholder content (D-51, D-53; Pencil-native typed slots per Task 2 + sibling note per D-52)</name>
  <files>design/Crito.pen (NEW reusable `Compound / Card` inside t67DU6 with 4 slot child frames: image-slot, title-slot, body-slot, footer-actions-slot; each enabled:true with placeholder content per D-53; slot:[<componentIds>] typed-suggestion arrays per Task 2 outcome; sibling Pencil note under t67DU6 per D-52 belt-and-suspenders)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-51, D-52, D-53, D-55, D-56)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Recommended `_Components / Compounds / Card` Structure" with the verbatim 4-slot layout, § "Code Examples Pattern 4 (Slot-typed Compound/Card)" with the full batch_design payload, § "Common Pitfalls" 1 + 2 + 4 enforcement)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-03, VAL-25-04, VAL-25-05, VAL-25-06, VAL-25-12)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 2 Slots + § 12 Spacing; § "Tokens Written — Semantic Aliases" — color-semantic-bg-card / -border-default / -bg-muted / -text-primary / -text-secondary; type-semantic-* for title + body)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (library_parents."_Components / Compounds" t67DU6; primitives.Button.parent M7eUr + slots.iconTrailing V4Dx4i + slots.label ATJK9; primitives.Icon.variants["24"] u7NmaS)
    - 25-03-SUMMARY.md DRAFT (Task 1 audit table + visual-values derivation + Task 2 probe outcome)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Build Card parent frame** (per RESEARCH § "Recommended Card Structure" + Task 1 derived values):
       - Position inside t67DU6 using `find_empty_space_on_canvas` (t67DU6 already contains the Phase 24 D-37 stub title).
       ```text
       card=I(t67DU6,{type:"frame",name:"Compound / Card",x:<X>,y:<Y>,width:380,height:480,reusable:true,layout:"vertical",gap:0,padding:0,fill:"#ffffffff",stroke:"#d4d4d8ff",strokeWidth:1,cornerRadius:<10 per Open Q7>})
       ```
       - Width 380 + height 480 are RESEARCH-quoted approximations; Task 1 raster-derived if a different size emerged.

    3. **Build image-slot (top of card, image placeholder rect per D-53):**
       ```text
       imageSlot=I(card,{type:"frame",name:"image-slot",slot:[],enabled:true,layout:"horizontal",width:380,height:220,fill:"#fafafaff"})
       imagePlaceholder=I(imageSlot,{type:"frame",name:"image-placeholder",fill:"#fafafaff",width:380,height:220})
       ```
       - `slot: []` untyped (per D-52 default: image slot accepts Image or Frame; specific component-IDs to suggest would only be known if a specific image-component exists — Phase 23/24 shipped no Image primitive, so untyped is correct).
       - `enabled: true` per D-53 — placeholder rect renders so Card preview looks complete.

    4. **Build content-stack inside Card** (holds title + body + footer-actions per RESEARCH structure):
       ```text
       contentStack=I(card,{type:"frame",name:"content-stack",layout:"vertical",gap:16,padding:24,alignItems:"flex-start"})
       ```
       - gap: 16 conceptually = `space-semantic-stack-sm` (Phase 24 Input precedent).
       - padding: 24 conceptually = `space-semantic-card-inner` if shipped Phase 23, else `space-primitive-24`.

    5. **Build title-slot inside content-stack:**
       ```text
       titleSlot=I(contentStack,{type:"frame",name:"title-slot",slot:[],enabled:true,layout:"horizontal",alignItems:"flex-start"})
       titleDefault=I(titleSlot,{type:"text",text:"Card title",fontFamily:"Plus Jakarta Sans",fontSize:20,fontWeight:700,fill:"#141f39ff"})
       ```
       - Title typography: Plus Jakarta Sans 20/700 per Task 1 raster-derivation; conceptually binds to `type-semantic-heading-N-*` or primitive direct per Phase 23 surface.
       - `slot: []` untyped — title is a text node, Pencil suggestion-mechanism less useful here.

    6. **Build body-slot inside content-stack:**
       ```text
       bodySlot=I(contentStack,{type:"frame",name:"body-slot",slot:[],enabled:true,layout:"vertical",alignItems:"flex-start"})
       bodyDefault=I(bodySlot,{type:"text",text:"Card body text. Replace at instance time with project metric, blog excerpt, or service description.",fontFamily:"Inter",fontSize:14,fontWeight:400,fill:"#52525bff"})
       ```
       - Body typography: Inter 14/400 + `color-semantic-text-secondary` (`#52525bff` neutral-700 per Phase 24 Input placeholder precedent).
       - `slot: []` untyped — body may accept Text or rich content (Phase 28 Blog excerpt; Phase 29 Project metric); typed suggestions less informative.

    7. **Build footer-actions-slot inside content-stack — TYPED per D-52 PREFERRED (Task 2 outcome):**
       - Resolve secondaryButtonId from Plan 25-01 (per Plan 25-01 D-43 path):
         (a) D-43 PREFERRED path (variant cell inside M7eUr): Secondary cell has its own id — use that id.
         (b) D-43 FALLBACK path (sibling component): use the sibling Primitive/Button/Secondary id.
       ```text
       footerSlot=I(contentStack,{type:"frame",name:"footer-actions-slot",slot:["M7eUr","<secondaryButtonId>"],enabled:true,layout:"horizontal",alignItems:"center",justifyContent:"flex-start"})
       ```
       - If Task 2 probe outcome resolved to FALLBACK (untyped everywhere): use `slot: []` here too.
       - Default content per D-53: Button/Default instance with "Read More" label + arrow-right iconTrailing (RESEARCH Pattern 3 carry-forward from Plan 25-01):
       ```text
       footerDefault=I(footerSlot,{type:"ref",ref:"M7eUr",descendants:{"ATJK9":{text:"Read More"},"V4Dx4i":{enabled:true}}})
       footerDefaultIcon=I(footerDefault/V4Dx4i,{type:"icon",library:"lucide",icon:"arrow-right",width:24,height:24,fill:"#ffffffff"})
       ```

    8. **Build sibling Pencil note (D-52 belt-and-suspenders, COMP-07 Success Criterion 3):**
       ```text
       cardSlotNote=I(t67DU6,{type:"note",text:"Compound / Card — 4 slots:\n• image (accepts Image or Frame; default: placeholder rectangle filled with color-semantic-bg-muted-equivalent at ~16:10 aspect)\n• title (accepts Text; default: 'Card title' in Plus Jakarta Sans 20/700)\n• body (accepts Text or rich content; default: 'Card body text. Replace at instance time...' in Inter 14/400 + color-semantic-text-secondary)\n• footer-actions (accepts Frame or Primitive/Button instance; slot:[M7eUr,<secondaryButtonId>] suggested per D-52 preferred; default: Primitive/Button/Default ref with 'Read More' label + arrow-right iconTrailing wired per Plan 25-01 D-44 pattern)\n\nAll 4 slots default enabled:true with placeholder content per D-53 — Card preview shows a complete-looking populated card for library discoverability. Consumers REPLACE placeholders via instance overrides at use time.\n\nUse cases served by this 4-slot signature via slot CONTENT (NOT separate variants per COMP-07 hard-prohibition):\n• Project card: image (cover) + title (project name) + body (date or metric) + footer-actions (Read More CTA)\n• Blog card: image (post cover) + title (post title) + body (excerpt or meta) + footer-actions (Read More or meta-row)\n• Service card: image (disabled OR icon override) + title (service name) + body (description) + footer-actions (small Read More or Secondary CTA)\n• Team-member card: image (portrait) + title (name) + body (role) + footer-actions (social icons or empty)\n\nSee PEN-INVENTORY § Compound Source Inference (Phase 25) for per-slot raster citations. OPEN-25-XX flags Card source-coverage gap (raster-derived inference per D-49) — first concrete consumer in Phase 28 (Blog) or 29 (Projects) locks the real values via overrides or component-edits and resolves the flag.\n\nCard hover/focus states NOT shipped this phase per Claude's Discretion default in CONTEXT — primitive-layer Button hover/focus inside footer-actions-slot covers interactivity. Surface as OPEN-25-XX if Phase 28+ consumer surfaces need for Card-level states.",fontFamily:"Inter",fontSize:12,fill:"#52525bff",width:600})
       ```
       - If `type: "note"` not supported, use `type: "text"` per Phase 24 24-04 precedent.

    9. **Verify Card structure (VAL-25-03, VAL-25-04, VAL-25-05, VAL-25-06):**
       - `mcp__pencil__batch_get({ queries: [{ id: "t67DU6", readDepth: 2 }] })`. Confirm:
         (a) SINGLE `Compound / Card` child node — NO parallel `Compound / Card / Project`, `/ Blog`, `/ Service` variants (Pitfall 1 explicit assertion).
       - `mcp__pencil__batch_get({ queries: [{ id: <cardId>, readDepth: 3 }] })`. Confirm:
         (a) Card marked `reusable: true`.
         (b) 4 slot-named children: image-slot, title-slot, body-slot, footer-actions-slot. (Note: content-stack is a layout container, not a slot — verify it's NOT counted as a "slot" per Pencil schema; slots are the 4 named frames inside Card directly + inside content-stack respectively.)
         (c) Each slot has `enabled: true` and non-empty child content.
         (d) footer-actions-slot has `slot: ["M7eUr", "<secondaryButtonId>"]` per Task 2 outcome (or `slot: []` per fallback).
         (e) Default Button instance inside footer-actions has descendants override on ATJK9 (label) + V4Dx4i (iconTrailing enabled:true with arrow-right icon).

    10. **VAL-25-13 pre-validation: zero raw value leaks:**
        - Walk Card subtree for any raw color/px not in accepted Phase 23/24 token-bindings set. The Card visual values derived in Task 1 should all have semantic-token bindings recorded in Variant Evidence rows at Task 4. If any literal lacks a binding, record as a Variant Evidence row.

    11. `mcp__pencil__get_screenshot({ nodeId: <cardId> })`. Capture preview for user checkpoint.

    12. **User visual checkpoint:** Present screenshot of Card preview. Ask user to confirm:
        (a) Card shows complete-looking populated structure — image placeholder rect, "Card title" text, body filler text, "Read More" CTA with arrow-right.
        (b) Card visual values match Phase 25 raster-probe expectations (white bg, subtle neutral border, ~10px corners, NOT neobrutalist yellow-shadow per Pitfall 2).
        (c) Single component (no variants) — explicit acceptance per Pitfall 1.
        (d) Approve D-53 placeholder content choice — would consumers replace the "Card title" + "Card body text" placeholders cleanly at instance time, or does the placeholder content interfere with readability?
  </action>
  <verify>
    - Pre-flight passed.
    - Compound/Card reusable component present inside t67DU6.
    - Exactly 4 slot children (image-slot, title-slot, body-slot, footer-actions-slot); content-stack is layout-only, not counted as slot.
    - All 4 slots `enabled: true` with placeholder content per D-53.
    - footer-actions-slot uses typed `slot: ["M7eUr", "<secondaryButtonId>"]` per Task 2 PREFERRED outcome (or untyped per fallback).
    - Sibling Pencil note exists under t67DU6 with full slot signature.
    - Default Button "Read More" CTA renders with arrow-right icon trailing.
    - No Pitfall 1 multi-variant matrix.
    - No Pitfall 2 v1.3 neobrutalist visuals.
    - User visually approved.
  </verify>
  <acceptance_criteria>
    - VAL-25-03 PASS: `batch_get(t67DU6, readDepth: 2)` returns single `Compound / Card` child, NO parallel variants.
    - VAL-25-04 PASS: Card has exactly 4 slots (image-slot, title-slot, body-slot, footer-actions-slot).
    - VAL-25-05 PASS: All 4 slots `enabled: true` + non-empty child content.
    - VAL-25-06 PASS: footer-actions-slot has typed `slot` array (per Task 2 PREFERRED) OR untyped `slot: []` (per Task 2 FALLBACK); sibling Pencil note exists with slot signature.
    - VAL-25-12 PASS: Card and all containers use Pencil auto-layout (vertical Card outer, horizontal image-slot, vertical content-stack, slots all auto-layout).
    - VAL-25-14 satisfied for this task: pre-flight + each batch_design preceded by `get_editor_state`.
    - Pitfall 1 prevention verified: SINGLE component, NO multi-variant matrix.
    - Pitfall 2 prevention verified: NO neobrutalist `6px 6px 0` shadow; NO yellow accent fills on Card frame; visual values derive from raster + Phase 23 tokens.
    - Pitfall 3 prevention verified: footer-actions Default Button has iconTrailing wired with arrow-right (D-44 carry-forward consistency).
    - Pitfall 4 prevention verified: typed slots used per Task 2 PREFERRED outcome where applicable; sibling note ships REGARDLESS.
  </acceptance_criteria>
  <done>
    25-03-SUMMARY.md `## Compound/Card Build (Task 3)` section populated with structural breakdown + screenshot inline + user approval timestamp. Card renders with complete populated preview per user approval.

    Resume signal: User types "approved" or "Card renders correctly" or describes a fix needed. Fixes bounded to Card's own properties (Task 1 + 2 + Plan 25-01 + Plan 25-02 outputs intact).
  </done>
</task>

<task type="auto">
  <name>Task 4: Phase 25 close — sweep + PEN-INVENTORY updates + id-inventory.json archival + STATE/ROADMAP updates + 25-03-SUMMARY</name>
  <files>
    - .planning/research/PEN-INVENTORY.md (Variant Evidence Phase 24 rows for Card; § Compound Source Inference (Phase 25) populated rows per slot; § Open Flags — Phase 25 OPEN-25-XX Card source-coverage row + any other plan-discovered flags; End-of-Phase-25 Verification section appended per Phase 24 24-05 precedent)
    - .planning/research/exports/v2.0/end-of-phase-25/id-inventory.json (NEW file; structural snapshot per OPEN-23-01 substitution pattern; mirrors end-of-phase-23 + end-of-phase-24 schema)
    - .planning/research/exports/v2.0/end-of-phase-25/README.md (NEW file; schema documentation + Phase 26 read instructions; cross-references to end-of-phase-23/-24 precedents)
    - .planning/phases/25-section-compound-components/25-03-SUMMARY.md (final write)
    - .planning/ROADMAP.md (Phase 25 plan checkbox for 25-03 updated; Phase 25 row in Progress table marked Complete with date)
    - .planning/STATE.md (current_position updated: Phase 25 COMPLETE; next action: `/gsd:verify-work 25` or `/gsd:plan-phase 26`)
  </files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-49, D-50, D-52, D-53, D-54, D-55, D-56, D-57 — phase-close-relevant decisions)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Implementation Order Recommendation" — Phase 25 close discipline)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (ALL 23 VAL-25-* assertions — Plan 25-03 + Phase 25 close owns the unmarked ones)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (precedent: sweep result + snapshot_layout audit + reference screenshot set + id-inventory.json structure)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (full schema to mirror for end-of-phase-25)
    - .planning/research/exports/v2.0/end-of-phase-24/README.md (precedent — same structure for end-of-phase-25/README.md)
    - .planning/research/PEN-INVENTORY.md (current state — verify Plan 25-01 + 25-02 updates landed; § End-of-Phase-24 Verification precedent for End-of-Phase-25 Verification structure)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md + 25-02-SUMMARY.md (carry-forward outcomes for Phase 25 close roll-up)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **PEN-INVENTORY updates (D-56):**

       (a) **Extend `## Variant Evidence (Phase 24)` table** with these new rows for Compound/Card:

       | component | location | property | literal_value | bound_to_token | source_evidence | rationale |
       |---|---|---|---|---|---|---|
       | `Compound / Card` | parent frame | fill | `#ffffffff` | `color-semantic-bg-card` (verify exists) OR `color-primitive-white` direct | Task 1 raster-probe (image-import-12 Project + Service cards both show white bg) | D-55 raster-probe inference |
       | `Compound / Card` | parent frame | stroke / strokeWidth | `#d4d4d8ff` / `1` | `color-semantic-border-default` (Phase 24 Input precedent) | Task 1 raster-probe (subtle neutral border visible on white cards) | Reuses Phase 24 token; no new tokens added (Pitfall 7) |
       | `Compound / Card` | parent frame | cornerRadius | `10` (Open Q7 resolution) | `radius-semantic-card` if exists, else `radius-semantic-button` reuse per Phase 24 D-32 Case B | Task 1 raster + Open Q7 resolution | Pitfall 7 enforcement |
       | `Compound / Card / content-stack` | layout | layout / gap / padding | `vertical` / `16` / `24` | `space-semantic-stack-sm` (16) / `space-semantic-card-inner` if exists else `space-primitive-24` (24) | Task 1 raster + Phase 24 Input stack precedent | Auto-layout per LAYOUT-01; content density per raster |
       | `Compound / Card / image-slot` | slot | slot / enabled / width / height / fill | `[]` / `true` / `380` / `220` / `#fafafaff` | (untyped per D-52); `color-semantic-bg-muted` if exists else `color-primitive-neutral-100` direct | D-53 + Task 1 raster (16:10 aspect, light placeholder) | Empty slot would collapse via enabled:false per D-30; D-53 ships placeholder for library discoverability |
       | `Compound / Card / title-slot / titleDefault` | text | fontFamily / fontSize / fontWeight / fill | `Plus Jakarta Sans` / `20` / `700` / `#141f39ff` | `type-primitive-family-display` + matched semantic + `color-semantic-text-primary` | Task 1 raster (clear title typography) | Heading-tier title per raster + Phase 23 typography surface |
       | `Compound / Card / body-slot / bodyDefault` | text | fontFamily / fontSize / fontWeight / fill | `Inter` / `14` / `400` / `#52525bff` | `type-semantic-body-sm-*` + `color-semantic-text-secondary` | Task 1 raster (smaller secondary-color body) | Body-sm scale per Phase 24 Input precedent |
       | `Compound / Card / footer-actions-slot` | slot | slot / enabled | `["M7eUr", "<secondaryButtonId>"]` or `[]` per Task 2 outcome / `true` | (typed-suggestion mechanism per D-52 PREFERRED, or untyped per fallback) | Plan 25-03 Task 2 probe outcome + Task 3 build | D-52 belt-and-suspenders + D-53 placeholder discoverability |
       | `Compound / Card / footer-actions-slot / footerDefault` | ref + descendants | ref / label override / iconTrailing enabled + glyph | `M7eUr` / "Read More" / `true` + lucide arrow-right | (ref) / (Pencil descendants) / Pattern A | Plan 25-03 Task 3 (D-44 pattern reused — RESEARCH Pattern 3) | Library-discoverability via fully-populated default action |

       (b) **Populate new `## Compound Source Inference (Phase 25)` section** (created at Task 0):

       | component | slot_or_property | source_image | inferred_value | confidence | rationale |
       |---|---|---|---|---|---|
       | `Compound / Card` | parent frame fill | `image-import-12.jpg` (Service + Project cards) | `#ffffffff` white | HIGH | All cards in raster show clean white bg |
       | `Compound / Card` | parent frame border | `image-import-12.jpg` (Project cards visible thin neutral border) | `#d4d4d8ff` 1px | MEDIUM | Subtle line visible against white bg; matches Phase 24 Input neutral-200 |
       | `Compound / Card` | parent frame cornerRadius | `image-import-12.jpg` + `image-import-14.jpg` | `10` | MEDIUM-HIGH | Rounded corners consistent across all card use cases; Open Q7 reuse decision |
       | `Compound / Card` | image-slot aspect | `image-import-12.jpg` Project card image + `image-import-14.jpg` Blog card image | ~`16:10` (380×220 chosen) | MEDIUM | Approximation from raster; consumer override at instance time |
       | `Compound / Card` | title-slot typography | `image-import-12.jpg` Project card title + `image-import-14.jpg` Blog title | Plus Jakarta Sans 20/700 navy | MEDIUM-HIGH | Matches Phase 23 heading scale + Crito typography per audit |
       | `Compound / Card` | body-slot typography | `image-import-12.jpg` Project card date + `image-import-14.jpg` Blog meta | Inter 14/400 + secondary text color | MEDIUM | Body-sm scale per Phase 24 Input precedent |
       | `Compound / Card` | footer-actions-slot default | `image-import-12.jpg` Project card "Read More →" CTA + Service card circular affordance | Primitive/Button/Default with "Read More" label + arrow-right iconTrailing | MEDIUM-HIGH | RESEARCH Pattern 3 + Plan 25-01 D-44 carry-forward |
       | `Compound / Card` | structural slot count | `image-import-12` + `image-import-14` + `image-import-8` + `image-import-22` cross-comparison | 4 slots: image + title + body + footer-actions (D-51) | HIGH | All 4 card use cases (Project, Blog, Service, Team) map cleanly; v1.3 Card.astro consulted as secondary structural reference |

       (c) **Update `### Open Flags — Phase 25 (OPEN-25-NN)` table** with seed row per D-50:

       | id | category | severity | description | consumer_phase | resolution |
       |---|---|---|---|---|---|
       | OPEN-25-XX | source-coverage | notable | Compound/Card visual structure is RASTER-DERIVED (D-49) — design/images/image-import-{12,14,8,22}.jpg provided source inference for slot structure + visual values + typography. Crito Project/Blog/Service frames are flat raster per Phase 23 OPEN-23-05 — no editable Crito source exists for Card. v1.3 src/components/ui/Card.astro consulted as secondary structural reference only (Pitfall 2 prevention). | 28 (Blog) or 29 (Projects) — whichever phase first instances Card | First concrete Card consumer in Phase 28 or 29 locks the real values via consumer-site overrides or component-edits and resolves the flag. Follows Phase 24's OPEN-24-12 (Badge) consumer-resolution pattern. |

       (Renumber OPEN-25-XX to OPEN-25-NN based on whatever Plan 25-01/02 + this plan have already populated. Maintain sequential numbering.)

       (d) **Append `## End-of-Phase-25 Verification` section** to PEN-INVENTORY (mirror Phase 23 / Phase 24 precedent):

       ```markdown
       ## End-of-Phase-25 Verification (plans 25-01 / 25-02 / 25-03)

       Mirrors Phase 23 + Phase 24 End-of-Phase Verification structure. Captured at Plan 25-03 close.

       ### Sweep Result (VAL-25-13 — zero raw values per COMP-09 + OPEN-23-13 dual-track)

       <result populated by Task 4 walker>

       ### snapshot_layout Audit Roll-up (VAL-25-15)

       <result populated by Task 4>

       ### Reference Screenshot Review (VAL-25-23)

       Captured inline via `get_screenshot` at each plan's build-visual checkpoint:
       - Plan 25-01: Secondary Button (Task 1) + Section/Header dual CTAs (Task 3)
       - Plan 25-02: Instagram probe (Task 1) + Substack atomic-glyph (Task 2) + Section/Footer (Task 3)
       - Plan 25-03: typed-slot probe (Task 2) + Compound/Card (Task 3)
       All approved by user at respective plan-task checkpoints.

       ### Variant Evidence Row Count

       Total new rows added across Phase 25: <N> in `## Variant Evidence (Phase 24)` (extended) + <M> rows in new `## Compound Source Inference (Phase 25)` section. Phase 25 token-extension row count: <expected 0; if any, audit-trail row in § Token Extensions (Phase 25) sub-section per D-33 carry-forward>.

       ### OPEN Flag Count

       Phase 24 OPEN flags resolved by Phase 25:
       - OPEN-24-06 → RESOLVED 25-01 (Header CTA iconTrailing wire-up per D-44)
       - OPEN-24-11 → RESOLVED 25-01 (Secondary Button per D-43)
       - OPEN-24-13 → RESOLVED 25-02 (Instagram Pattern A + Substack Pattern B per D-45/D-46)

       Phase 25 OPEN flags added: <N> total in `### Open Flags — Phase 25 (OPEN-25-NN)`:
       - OPEN-25-XX (Card source-coverage per D-50)
       - (any other plan-discovered flags from Plans 25-01 / 25-02 / 25-03)

       ### Active-Editor Pre-flight Compliance (D-54)

       Total pre-flight calls across Phase 25: ~<N> (3-5 per plan × 3 plans). Zero mismatch incidents (matches Phase 24 ~14 calls / 0 incidents precedent).

       ### Files Modified Summary

       <list per plan>

       ### Cross-cutting must_haves.truths Satisfaction

       <YES/NO per truth from Plans 25-01 + 25-02 + 25-03 frontmatter>
       ```

    3. **Phase 25 sweep (VAL-25-13 — zero raw values per COMP-09 + OPEN-23-13 dual-track, applying Plan 24-05 walker pattern):**
       - Walker scope: recursive `batch_get(g9oRa5, readDepth: 4)` over `_Components / Sections` + recursive `batch_get(t67DU6, readDepth: 4)` over `_Components / Compounds` + `batch_get(<substackGlyphId>, readDepth: 2)` + `batch_get(<secondaryButtonId or M7eUr Secondary cell>, readDepth: 2)`.
       - Collect every distinct literal value (colors, padding tuples, gaps, cornerRadii, fontFamilies, fontSizes, fontWeights, lineHeights, strokeWidths, dimensions, opacities).
       - Build accepted-literal reference set:
         * PEN-INVENTORY § Tokens Written — Primitives (Phase 23 — 39 values)
         * PEN-INVENTORY § Tokens Written — Semantic Aliases (Phase 23 — 56 values via primitives chain)
         * PEN-INVENTORY § Token Extensions (Phase 24 — 0 rows expected) + § Token Extensions (Phase 25 — expected 0; if any, populated)
         * PEN-INVENTORY § Variant Evidence (Phase 24 — 30 Phase-24 rows + Phase-25 extensions)
         * PEN-INVENTORY § Compound Source Inference (Phase 25 — Card source-inferred values)
       - Every (node_id, property, literal_value) triple from the walked tree should appear in the accepted-literal set OR be a pre-approved special case (`0` for empty-slot dims, text node implicit defaults, simpleicons SVG path data not a styling value).
       - Document outcome (PASS / leaks-found) in `## Sweep Result` PEN-INVENTORY End-of-Phase-25 section.

    4. **snapshot_layout audit (VAL-25-15):**
       - `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root. Expected: "No layout problems."
       - Per-frame on Section/Header + Section/Footer + Compound/Card. Document per-frame text-clipping false-positives per Phase 24 quirk (24-05-SUMMARY recommendation: prefer get_screenshot + human visual verification).

    5. **Write `.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json`** (mirror end-of-phase-24 schema):
       ```json
       {
         "phase": "25-section-compound-components",
         "captured": "<date>",
         "purpose": "End-of-Phase-25 structural snapshot per OPEN-23-01 archival substitution pattern. PNG archival via export_nodes remains unavailable; this JSON serves as the canonical structural record for Phase 26+ consumers.",
         "source": "design/Crito.pen via mcp__pencil__batch_get readDepth=4 over componentsSectionsFrame g9oRa5 + componentsCompoundsFrame t67DU6 + new primitives delta",
         "library_parents": {
           "_Components / Primitives": "avgor",
           "_Components / Compounds": "t67DU6",
           "_Components / Sections": "g9oRa5",
           "_Tokens & Foundations": "RpGbe"
         },
         "phase_25_deliverables": {
           "Section/Header": {
             "parent": "<headerId>",
             "children": { "logo-slot": "<id>", "nav-links": "<id>", "affordances": "<id>", "cta-row": "<id>" },
             "cta_instances": { "primary": "<id>", "secondary": "<id>" },
             "notes": "Plan 25-01 deliverable; D-44 actual-use resolution of OPEN-24-06; D-43 path used: <preferred or fallback>"
           },
           "Section/Footer": {
             "parent": "<footerId>",
             "children": { "main-row": "<id>", "brand-column": "<id>", "social-row": "<id>", "link-columns": ["<id>", ...], "copyright-row": "<id>", "newsletter-row": "<id or null>" },
             "social_instances": { "instagram": "<id>", "substack": "<id>" },
             "notes": "Plan 25-02 deliverable; D-45/D-46 resolution of OPEN-24-13"
           },
           "Compound/Card": {
             "parent": "<cardId>",
             "slots": { "image-slot": "<id>", "title-slot": "<id>", "body-slot": "<id>", "footer-actions-slot": "<id>" },
             "default_content": { "image-placeholder": "<id>", "title-default": "<id>", "body-default": "<id>", "footer-default-button": "<id>" },
             "slot_typing": "<preferred (typed) or fallback (untyped) per Task 2>",
             "notes": "Plan 25-03 deliverable; raster-derived per D-49; OPEN-25-XX Card source-coverage flag seeded"
           },
           "Primitive/Button/Secondary": {
             "id": "<secondaryButtonId>",
             "path": "<inside M7eUr variant axis (D-43 preferred) OR sibling in avgor (D-43 fallback)>",
             "notes": "Plan 25-01 deliverable; resolves OPEN-24-11"
           },
           "Primitive/Icon/glyphs/substack": {
             "parent": "<substackGlyphId>",
             "path_child": "<id>",
             "provenance": "simpleicons.org/icons/substack (verified 2026-06-01)",
             "notes": "Plan 25-02 deliverable; Pattern B atomic-glyph"
           }
         },
         "token_surface_count": 95,
         "token_extension_row_count_phase_25": 0,
         "variant_evidence_row_count_phase_25_additions": <N>,
         "compound_source_inference_row_count": <M>,
         "open_flag_count": {
           "OPEN-24-NN_resolved_by_phase_25": ["OPEN-24-06", "OPEN-24-11", "OPEN-24-13"],
           "OPEN-25-NN_total": <count>,
           "OPEN-25-NN_seed_row": "OPEN-25-XX Card source-coverage per D-50"
         },
         "baseline_drift_verification": {
           "expected_phase_24_baseline_ids_unmutated": ["MIXGf", "QdwxP", "kicJ8", "ujMLJ", "VleVl", "cl8tt", "w1m3x", "DzqTm", "cYlRH", "Y2isa", "WDGxc", "1nLS3", "Jmdw0", "maDc3", "IKAu3", "RpGbe", "avgor", "t67DU6", "g9oRa5"],
           "expected_phase_24_primitive_ids_unmutated_except_secondary_addition": ["YJhRv", "gQa2R", "AvKtA", "V4Dx4i", "ATJK9", "nwJk7", "j0FxQZ", "EQaMf", "yRvGb", "u7NmaS", "dpO5Y"],
           "verdict": "<PASS or document deviations>"
         },
         "validation_outcomes": {
           "VAL-25-01..23": "<populate each with PASS / N/A / PARTIAL with citation>"
         },
         "snapshot_layout_quirk_documented": "Phase 24 24-05-SUMMARY quirk carried forward — get_screenshot preferred over snapshot_layout text-clipping flags",
         "cross_phase_handoffs": {
           "phase_26_faq_404_reconstruction": "Use Section/Header + Section/Footer instances for FAQ + 404 page reconstructions per ROADMAP. Card NOT instanced in Phase 26 (FAQ uses accordion + CTA; 404 uses message + nav). Calibration workflow established by Phase 26 will reference Section/Header + Footer for top + bottom of every reconstructed page.",
           "phase_27_contact_reconstruction": "Section/Footer's newsletter-row Input instance (if shipped) demonstrates Input/Default composition pattern for Contact form.",
           "phase_28_blog_reconstruction_or_29_projects": "First Card consumer — lock OPEN-25-XX values via consumer-side overrides or component-edits; resolve the flag.",
           "phase_31_homepage": "Section/Header logo-slot consumer override (Joel-brand wordmark when designed); Section/Footer link column text overrides for Joel-brand link content; Hero Secondary CTA may override stroke from navy to white per Plan 25-01 OPEN-25-XX Secondary stroke context-deviation."
         }
       }
       ```

    6. **Write `.planning/research/exports/v2.0/end-of-phase-25/README.md`** (mirror end-of-phase-24/README.md structure):
       - Brief description of what id-inventory.json contains.
       - Schema documentation (top-level keys).
       - Phase 26 read instructions: which keys to consult before per-page reconstruction.
       - Cross-references to end-of-phase-23 + end-of-phase-24 precedents.

    7. **Roadmap + STATE updates:**
       - `.planning/ROADMAP.md` Phase 25 plan list:
         - Change `- [ ] 25-03: Build Compound / Card ...` → `- [x] 25-03-PLAN.md — Compound / Card built with 4 slots (D-51) + D-53 placeholders + D-52 belt-and-suspenders documentation; OPEN-25-XX Card source-coverage flag seeded; Phase 25 close sweep complete`
         - Update Phase 25 row in Progress table: `0/TBD` → `3/3 plans complete` / `Status: Complete` / `Completed: <date>`.
       - `.planning/STATE.md`:
         - `current_position`: Phase: 25 — COMPLETE; Plan: 3 of 3; Status: Phase 25 complete; Last activity: <date> -- Phase 25 marked complete
         - `progress.completed_phases`: 3 (was 2)
         - `progress.completed_plans`: 13 (was 10)
         - `progress.percent`: 30 (was 20)
         - `Session Continuity`: Stopped at: Phase 25 complete; Resume file: ROADMAP.md Phase 26 section; Next action: `/gsd:verify-work 25` (orthogonal goal-backward verification) OR `/gsd:plan-phase 26` (FAQ + 404 reconstruction)
         - `Accumulated Context > Key Decisions (v2.0)`: append Phase 25 entries: D-38 through D-57 summary line.

    8. **Final user gate (matches Phase 24 24-05 close discipline):** This task captures `get_screenshot` of:
       - All Phase 25 deliverables: Section/Header (Plan 25-01), Section/Footer (Plan 25-02), Compound/Card (Plan 25-03), Secondary Button (Plan 25-01), Substack glyph (Plan 25-02).
       - Reference screenshot set archived inline in 25-03-SUMMARY.md `## Reference Screenshot Set (VAL-25-23)`.
       - Ask user to approve Phase 25 close gate.

    9. **Write final 25-03-SUMMARY.md** with all sections:
       - `## Active-Editor Pre-flight Result (D-54)`
       - `## Plan 25-01/02 Outputs Verification`
       - `## PEN-INVENTORY Scaffolding (Task 0)`
       - `## Raster Card Audit (D-49)` (Task 1)
       - `## v1.3 Card.astro Structural Reference (Secondary, D-49)` (Task 1)
       - `## Card Visual Values Derivation` (Task 1)
       - `## Decisions (Open Q7)` (Task 1)
       - `## Slot Mechanics Probe Results (Open Q2)` (Task 2)
       - `## Compound/Card Build (Task 3)`
       - `## PEN-INVENTORY Updates (D-56)` (Task 4)
       - `## Phase 25 Sweep Result (VAL-25-13)` (Task 4 — Plan 24-05 walker pattern)
       - `## snapshot_layout Audit` (Task 4)
       - `## Archival Artifacts` (Task 4 — id-inventory.json + README.md written paths)
       - `## End-of-Phase-25 Verification` (Task 4 — PEN-INVENTORY cross-reference)
       - `## Phase 25 Close Summary` (Task 4 — all 23 VAL-25-* statuses)
       - `## Reference Screenshot Set (VAL-25-23)` with screenshots from all 3 plans
       - `## Files Modified`
       - `## Cross-cutting must_haves Satisfaction`
       - `## Recommended Next Step` (Phase 26 or verify-work-25)

    10. **Plan close gate:** Final user-approval handshake. If executing inside `/gsd:execute-phase`, return control to orchestrator with structured return that Phase 25 is COMPLETE.
  </action>
  <verify>
    - Pre-flight passed.
    - PEN-INVENTORY § Variant Evidence (Phase 24) extended with Card rows; § Compound Source Inference (Phase 25) populated; § Open Flags — Phase 25 OPEN-25-XX seed row written; § End-of-Phase-25 Verification appended.
    - Phase 25 sweep completed: all literals match accepted-literal set OR documented as Variant Evidence rows.
    - Document-level snapshot_layout returns "No layout problems."
    - end-of-phase-25/id-inventory.json + README.md exist.
    - ROADMAP.md Phase 25 plan checkboxes for 25-03 updated; Progress table row marked Complete.
    - STATE.md updated: Phase 25 COMPLETE.
    - 25-03-SUMMARY.md written with all 18 sections.
    - User approved Phase 25 close gate.
  </verify>
  <acceptance_criteria>
    - VAL-25-13 PASS: Phase 25 sweep walker confirms zero raw value leaks beyond accepted Phase 23/24/25 binding set.
    - VAL-25-14 PASS: pre-flight count for Plan 25-03: ≥ 3; total Phase 25 ≥ 10; 0 mismatch incidents.
    - VAL-25-15 PASS: document-level snapshot_layout clean; per-frame quirks documented.
    - VAL-25-16 PASS: PEN-INVENTORY § Variant Evidence extended.
    - VAL-25-17 PASS: PEN-INVENTORY § Compound Source Inference (Phase 25) populated per D-49.
    - VAL-25-18 PASS: OPEN-25-XX seed row written.
    - VAL-25-22 PASS: end-of-phase-25/id-inventory.json + README.md written with required schema sections (library_parents, sections, compounds, secondary_button, substack_glyph, validation_outcomes, snapshot_layout_quirk_documented, cross_phase_handoffs).
    - VAL-25-23 PASS: reference screenshot set archived inline; user approved Phase 25 close gate.
    - All Plan 25-01 + 25-02 + 25-03 owned VAL-25-* assertions marked PASS in 25-03-SUMMARY.md Phase 25 Close Summary section.
    - Phase 24 baseline IDs verified unmutated per id-inventory.json baseline_drift_verification block.
    - ROADMAP + STATE accurately reflect Phase 25 completion.
  </acceptance_criteria>
  <done>
    Phase 25 closed. 25-03-SUMMARY.md exists at `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` with all 18 sections populated. PEN-INVENTORY.md fully updated. end-of-phase-25/ archival written. STATE.md + ROADMAP.md updated. User approved close gate.

    Resume signal: User types "phase 25 complete" or "approved" or proceeds to `/gsd:verify-work 25` OR `/gsd:plan-phase 26`.
  </done>
</task>

</tasks>

<verification>
**Per-plan validation (VAL-25-* assertions from 25-VALIDATION.md owned by Plan 25-03 + Phase 25 close):**

| VAL ID | Owner Task | How Verified |
|---|---|---|
| VAL-25-03 (Compound/Card under t67DU6, single component NOT multi-variant) | Task 3 + Task 4 | `mcp__pencil__batch_get({ queries: [{ id: "t67DU6", readDepth: 2 }] })` → confirm single `Compound / Card` child; assert no parallel `Compound / Card / Project`, `/ Blog`, `/ Service` variants |
| VAL-25-04 (Card has exactly 4 slots) | Task 3 + Task 4 | `batch_get(<cardId>, readDepth: 3)` → enumerate slot frames; assert 4 with names image-slot, title-slot, body-slot, footer-actions-slot |
| VAL-25-05 (all 4 slots enabled:true with placeholder content) | Task 3 + Task 4 | Same batch_get → assert each slot `enabled: true` + non-empty child |
| VAL-25-06 (D-52 belt-and-suspenders) | Task 2 + Task 3 + Task 4 | Verify typed `slot: [...]` on 1+ slots (PREFERRED path per Task 2) OR untyped `slot: []` on all (FALLBACK); verify sibling Pencil note exists under t67DU6 with slot signature |
| VAL-25-12 (auto-layout, no absolute positioning) | Task 3 + Task 4 | Walk Card subtree; assert `layout` property non-null on Card, content-stack, image-slot, title-slot, body-slot, footer-actions-slot |
| VAL-25-13 (zero raw value leaks per Phase 25 sweep) | Task 4 | Plan 24-05 walker pattern applied to Section + Compound + Substack + Secondary nodes; literal-set intersection check against accepted Phase 23/24/25 set |
| VAL-25-14 (pre-flight before every mutation) | All tasks | Grep SUMMARY.md for pre-flight logs; count ≥ 3 in Plan 25-03; ≥ 10 cumulative across Phase 25; 0 mismatch |
| VAL-25-15 (snapshot_layout clean + per-frame quirks documented) | Task 4 | Snapshot maxDepth:0 + per-frame on Card; document quirks per Phase 24 precedent |
| VAL-25-16 (PEN-INVENTORY Variant Evidence extended) | Task 4 | Diff PEN-INVENTORY; verify ≥ 9 new Card rows |
| VAL-25-17 (§ Compound Source Inference (Phase 25) populated) | Tasks 0 + 1 + 4 | Verify section header exists; verify ≥ 7 per-slot/property raster-citation rows |
| VAL-25-18 (OPEN-25-XX Card source-coverage flag added) | Task 4 | Verify OPEN-25-XX row in `### Open Flags — Phase 25 (OPEN-25-NN)` table |
| VAL-25-22 (end-of-phase-25/id-inventory.json written) | Task 4 | `test -f .planning/research/exports/v2.0/end-of-phase-25/id-inventory.json` + JSON schema check + README.md exists |
| VAL-25-23 (user visual checkpoint at Phase 25 close gate) | Tasks 2, 3, 4 | Reference screenshot set archived inline; user-approved Phase 25 close gate |

**Cross-cutting Phase 24 + Plan 25-01/02 carry-forward:**
- D-54 active-editor pre-flight enforced.
- snapshot_layout text-clipping quirk documented.
- OPEN-23-13 dual-track: literal hex/font in batch_design; Variant Evidence rows for binding.
- Plan 25-01 (Section/Header + Secondary Button) verified intact in Task 0.
- Plan 25-02 (Section/Footer + Substack glyph) verified intact in Task 0.

**Phase 24 + Plan 25-01/02 baseline-drift verification:**
- Phase 24 baseline IDs unmutated.
- Plan 25-01 deliverables intact.
- Plan 25-02 deliverables intact.
- Only t67DU6 children grow (Compound/Card + sibling note added).
</verification>

<success_criteria>
**Plan 25-03 complete when:**

1. `Compound / Card` exists in `design/Crito.pen` as a SINGLE reusable component inside `_Components / Compounds` (t67DU6) with:
   - Exactly 4 slots: image-slot, title-slot, body-slot, footer-actions-slot per D-51.
   - All 4 slots `enabled: true` with placeholder content per D-53 (image placeholder rect, "Card title", "Card body text...", Primitive/Button/Default "Read More" + arrow-right).
   - footer-actions-slot typed with `slot: ["M7eUr", "<secondaryButtonId>"]` per D-52 PREFERRED (or untyped per Task 2 FALLBACK outcome).
   - Visual values: white fill, neutral border, ~10px cornerRadius, Plus Jakarta Sans 20/700 title, Inter 14/400 body — all derived from raster-probe + Phase 23 token surface per D-49 + D-55.
   - NO multi-variant matrix (COMP-07 hard-prohibition); NO Card-level hover/focus states (defer per Claude's Discretion).
   - NO v1.3 neobrutalist visuals (Pitfall 2 prevention).

2. Sibling Pencil note inside t67DU6 documents Card slot signature in human-readable form per D-52 belt-and-suspenders + COMP-07 Success Criterion 3.

3. PEN-INVENTORY.md extended:
   - § Variant Evidence (Phase 24) extended with ≥ 9 new Card rows.
   - NEW § Compound Source Inference (Phase 25) section populated with ≥ 7 per-slot raster-citation rows per D-49 + D-56.
   - § Open Flags — Phase 25 (OPEN-25-NN) extended with OPEN-25-XX Card source-coverage seed row per D-50.
   - NEW § End-of-Phase-25 Verification section appended per Phase 24 24-05 precedent (sweep result, snapshot_layout audit roll-up, reference screenshot review, variant evidence count, OPEN flag count, active-editor pre-flight compliance, files modified, cross-cutting must_haves satisfaction).

4. `.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json` exists with required schema sections (mirror end-of-phase-24/id-inventory.json structure).

5. `.planning/research/exports/v2.0/end-of-phase-25/README.md` exists.

6. Document-level `snapshot_layout(maxDepth:0, problemsOnly:true)` returns "No layout problems."

7. Phase 25 sweep (Plan 24-05 walker pattern): zero raw-value leaks; every literal in new Phase 25 nodes appears in accepted Phase 23/24/25 token-binding set.

8. User visually approved Phase 25 close gate.

9. 25-03-SUMMARY.md exists with all 18 sections populated.

10. ROADMAP.md Phase 25 plan checkbox for 25-03 updated; Phase 25 row in Progress table marked Complete.

11. STATE.md updated: Phase 25 COMPLETE; resume pointer for Phase 26.

12. Plan 25-01 + 25-02 deliverables intact (Section/Header + Secondary Button + Section/Footer + Substack glyph all verified by Task 0 verification).

13. Phase 24 baseline IDs unmutated (id-inventory.json baseline_drift_verification PASS).

14. All 23 VAL-25-* assertions marked PASS / N/A / PARTIAL with citation in 25-03-SUMMARY.md Phase 25 Close Summary section.
</success_criteria>

<output>
After completion, write `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` per template structure. Write end-of-phase-25/id-inventory.json + README.md. PEN-INVENTORY.md updates committed. ROADMAP.md + STATE.md updated.

Commit message (if `commit_docs: true`): `exec(25-03): Compound/Card (4 slots, D-51) + D-53 placeholders + D-52 typed-slot belt-and-suspenders + Phase 25 close sweep + archival`.

Phase 25 close commit (separate or combined per orchestrator): `chore(state): mark Phase 25 complete in STATE.md`.

**Resume signal for next phase:** Plan 25-03 SUMMARY committed + user-approved at Phase 25 close gate. Two valid next steps:
1. `/gsd:verify-work 25` — orthogonal goal-backward verification (recommended by Phase 24 precedent before phase transition).
2. `/gsd:plan-phase 26` — proceed to Phase 26 (FAQ + 404 Reconstruction; first per-page reconstruction phase; introduces calibration workflow as definition-of-done per ROADMAP cross-cutting policy).
</output>
