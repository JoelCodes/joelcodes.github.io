---
phase: 25-section-compound-components
plan: 25-02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - design/Crito.pen (NEW reusable `Section / Footer` inside `_Components / Sections` (g9oRa5) — composes Phase 24 primitives + new Substack glyph; children per RESEARCH § "Recommended `_Components / Sections / Footer` Structure": main-row with brand-column + 4 link columns; copyright-row with copyright text; social-row instancing Instagram (Pattern A lucide-native per D-45 probe) + Substack (Pattern B atomic-glyph per D-46); newsletter Subscribe input instance of Primitive/Input/Default (nwJk7) if source audit confirms presence per RESEARCH Open Question 4)
  - design/Crito.pen (NEW Substack atomic-glyph component `_Components / Primitives / Icon / glyphs / substack` inside avgor or as sibling per Pencil schema; SVG path from simpleicons.org canonical per D-46 — verified 2026-06-01: `M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z` viewBox 0 0 24 24)
  - design/Crito.pen (NEW sibling Pencil note inside g9oRa5 documenting `Section / Footer` slot signature)
  - .planning/research/PEN-INVENTORY.md (## Variant Evidence (Phase 24) extended with Section/Footer structural rows + Substack glyph row + Newsletter input instance binding if applicable per D-56)
  - .planning/research/PEN-INVENTORY.md (## Icon Glyphs (Phase 24) extended with `## Glyphs Shipped (Phase 25)` sub-section — Instagram Pattern A lucide row + Substack Pattern B atomic-glyph row with simpleicons.org provenance citation)
  - .planning/research/PEN-INVENTORY.md (### Open Flags — Phase 24 Resolution column: OPEN-24-13 RESOLVED via Plan 25-02 Instagram + Substack ship per D-45/D-46)
  - .planning/research/PEN-INVENTORY.md (### Open Flags — Phase 25 (OPEN-25-NN) — may add rows for: Newsletter Subscribe button color per Open Question 4 outcome; Chivo token strategy per Open Question 6 outcome; or other plan-specific flags)
  - .planning/ROADMAP.md (Phase 25 plan line `- [ ] 25-02: Build Section/Footer` checkbox updated at plan close)
autonomous: false
requirements_addressed:
  - COMP-06
  - COMP-08
  - COMP-09
must_haves:
  truths:
    - "`Section / Footer` exists under `_Components / Sections` (g9oRa5) as a reusable component (COMP-06 + COMP-08)"
    - "Section/Footer content uses Crito-source-verbatim labels per D-48 — link column headings + link list text nodes ship the literal strings enumerated from Home Page Footer Task 0 audit (no Joel-side overrides at the component level)"
    - "Footer uses Chivo typography per D-47 — references the Phase 23 Chivo primitive token (`type-primitive-family-chivo`) OR a Chivo-derived semantic alias per Open Question 6 resolution at Task 0; the Phase 23 conditional Chivo primitive earns its keep"
    - "Section/Footer social-row instances `Primitive / Icon` for Instagram (Pattern A lucide-native per D-45 probe outcome) AND Substack (Pattern B atomic-glyph component shipped this plan per D-46) — VAL-25-09"
    - "Pattern B Substack atomic-glyph SVG path matches the simpleicons.org canonical (verified 2026-06-01) — provenance attestation per D-46 / VAL-25-10"
    - "Joel-brand glyphs (Instagram + Substack) shipped at the component level — Crito's LinkedIn + Twitter NOT shipped (Pitfall 7 prevention: Section/Footer is Joel's component, uses Joel's brand glyphs as defaults; Phase 31 Homepage Footer instance does NOT need to re-override the social row at instance time)"
    - "Sibling Pencil note inside `_Components / Sections` documents Section/Footer slot signature in human-readable form (COMP-06 Success Criterion 3)"
    - "Zero raw color hex / zero raw px values inside Section/Footer or new Substack glyph beyond accepted Phase 23/24/25 Variant Evidence binding set (COMP-09)"
    - "Every Pencil-mutating batch is preceded by `mcp__pencil__get_editor_state({ include_schema: false })` asserting active editor == `design/Crito.pen` (D-54)"
    - "`snapshot_layout(maxDepth: 0, problemsOnly: true)` at document root returns `\"No layout problems.\"`; per-frame text-clipping false-positives documented per Phase 24 quirk (24-05-SUMMARY)"
    - "PEN-INVENTORY § Variant Evidence (Phase 24) EXTENDED with Section/Footer structural rows + Substack glyph row + Newsletter Subscribe binding row (if applicable) per D-23/D-56"
    - "PEN-INVENTORY § Icon Glyphs (Phase 24) extended with new `## Glyphs Shipped (Phase 25)` sub-section: Instagram (Pattern A) + Substack (Pattern B simpleicons-canonical) rows"
    - "OPEN-24-13 marked RESOLVED with Plan 25-02 citation in PEN-INVENTORY Open Flags — Phase 24 table"
    - "Phase 24 baseline IDs unmutated (no mutations to existing primitives — only adding Section/Footer to g9oRa5 + Substack glyph component)"
    - "Plan 25-01 outputs intact: Section/Header unchanged; Secondary Button variant unchanged"
  artifacts:
    - path: "design/Crito.pen"
      provides: "Section / Footer reusable component inside _Components / Sections (g9oRa5)"
      contains: "main-row with brand-column + 4 link columns; social-row instancing Instagram (Pattern A) + Substack (Pattern B); copyright-row; optional Newsletter Subscribe input per source audit"
    - path: "design/Crito.pen"
      provides: "Primitive / Icon / glyphs / substack atomic-glyph component (Pattern B fallback) using simpleicons.org canonical SVG path"
      contains: "vector path matching `M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z` viewBox 24x24"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Variant Evidence rows for Section/Footer + Substack glyph; OPEN-24-13 resolution citation; new Glyphs Shipped (Phase 25) sub-section"
  key_links:
    - from: "Section/Footer social-row Instagram instance"
      to: "Pencil lucide library (Pattern A native)"
      via: "type: icon, library: lucide, icon: instagram (with deprecation-warning awareness per RESEARCH lucide#2792 finding)"
      pattern: "D-45 Pattern A confirmation (or fallback to Pattern B if lucide Instagram rejects)"
    - from: "Section/Footer social-row Substack instance"
      to: "Primitive / Icon / glyphs / substack atomic-glyph component (new this plan)"
      via: "Pencil ref + descendants override on path fill (per Phase 24 D-25 superseded → Pattern B kept as brand-glyph fallback per D-46)"
      pattern: "RESEARCH Pattern 5 (Pattern B atomic-glyph build with simpleicons canonical citation)"
    - from: "Section/Footer copyright + brand-column tagline text nodes"
      to: "Phase 23 Chivo primitive token (type-primitive-family-chivo) OR Chivo-derived semantic alias"
      via: "Open Question 6 resolution at Task 0 — default: primitive direct reference (no new semantic alias) per Pitfall 7 prevention"
      pattern: "D-47 Chivo conditional primitive earns its keep + D-14 components-reference-semantic-only with primitive-direct exception"
    - from: "Section/Footer Newsletter Subscribe input (if shipped per Open Q4)"
      to: "Primitive / Input / Default (nwJk7) + Primitive / Button / Default (M7eUr)"
      via: "Pencil ref + descendants overrides"
      pattern: "Composition discipline — instance primitives, do not inline-author input markup"
---

<objective>
Build `Section / Footer` (COMP-06) inside `_Components / Sections` (g9oRa5), instancing Phase 24 primitives + new Substack atomic-glyph component shipped this plan. The plan's first probe task (Task 1) verifies Pencil's lucide library availability for Instagram (D-45 Pattern A — RESEARCH confirmed Instagram still ships but emits deprecation warning per lucide#2792) and ships Substack as a Pattern B atomic-glyph component (D-46 — RESEARCH confirmed Substack absent from lucide per lucide#2792 closed "not planned"). Both glyphs Joel-brand-default at the component level since Section/Footer IS Joel's component — Crito's LinkedIn+Twitter glyphs are NOT shipped per Pitfall 7 carry-forward.

**Source-wins discipline (D-48, D-55):** Crito Home Page Footer subtree (descendant of `ujMLJ`) is the IN-SCOPE non-raster source for Footer layout structure + link column labels. Task 0 audits it via `batch_get(readDepth: 4)` to enumerate link columns, column headings, link lists, copyright text, and any Newsletter Subscribe input present.

**Chivo typography per D-47:** Phase 23's conditional Chivo primitive (`type-primitive-family-chivo` per OPEN-23-09 resolution: "Footer-conditional, Phase 24+ may remove") earns its keep — Footer IS the condition. Per RESEARCH Open Question 6 default recommendation, reference the primitive directly (no new semantic alias) per Pitfall 7 prevention.

**Probe-first within the plan (D-45 + D-46):** Pencil's lucide library Instagram availability is documented as "deprecated but still ships" per RESEARCH (lucide#2792 — verified 2026-06-01). Task 1 probes this with a real Insert to confirm the lucide Instagram glyph still works in the current Pencil 2.13 build before committing to Pattern A for Instagram. Substack is confirmed absent and ships via Pattern B unconditionally per D-46.

**Purpose:** Ships the second Phase 25 section component. Resolves Phase 24 OPEN-24-13 (Joel-brand glyph gap) by ACTUAL shipping of Instagram + Substack atomic-glyph components. Validates the Pattern A + Pattern B brand-glyph strategy carry-forward from Phase 24 D-25 supersession. Confirms Chivo primitive load-bearing status.

**Output:** `Section / Footer` inside `_Components / Sections`; new Substack atomic-glyph inside `_Components / Primitives` glyphs sub-folder (or sibling per Pencil schema); OPEN-24-13 resolution citation in PEN-INVENTORY; potentially OPEN-25-NN rows for Newsletter Subscribe color deviation (Open Q4) and/or Chivo token strategy (Open Q6); new `## Glyphs Shipped (Phase 25)` sub-section in PEN-INVENTORY; 25-02-SUMMARY.md documenting Task 1 lucide probe outcome, Task 0 source audit findings, simpleicons.org Substack provenance citation, snapshot_layout audit, and user visual checkpoint approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-section-compound-components/25-CONTEXT.md
@.planning/phases/25-section-compound-components/25-RESEARCH.md
@.planning/phases/25-section-compound-components/25-VALIDATION.md
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md
@.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-24/id-inventory.json
@./CLAUDE.md
</context>

<threat_model>
**T-25-T1 (active-editor-swap — MEDIUM severity, BLOCKING)** — Same Pencil VS Code extension active-editor-swap threat as Plan 25-01. Mitigation: D-54 — `mcp__pencil__get_editor_state({ include_schema: false })` BEFORE every `batch_design` batch in this plan. Expected pre-flight count: ~4-5 (Task 1 lucide probe, Task 2 Substack glyph build, Task 3 Footer parent build, Task 4 Footer children + close).

**T-25-I1 (probe-artifact leak — LOW)** — Task 1 lucide-Instagram probe inserts a test icon node for confirmation. Mitigation: probe artifact deleted before plan close per Phase 24 24-04 probe-cleanup precedent.

**T-25-S1 (SVG-path-provenance integrity — LOW)** — Task 2 ships a hand-typed SVG path from simpleicons.org. Risk: typo in path produces visually-wrong rendered glyph. Mitigation: path quoted verbatim from RESEARCH § Code Examples Pattern 5 (also quoted in RESEARCH § Pitfall 5 with viewBox); Task 2 verification step compares the inserted path against the RESEARCH literal character-for-character; visual `get_screenshot` confirms rendered glyph reads as Substack.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + Crito Footer source audit (RESEARCH Pattern 1 — Footer subtree)</name>
  <files>none (read-only)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-45, D-46, D-47, D-48, D-55, D-56)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Recommended `_Components / Sections / Footer` Structure", § "Code Examples Pattern 1", § "Open Questions" 4 + 6, § "Sources" — Crito Footer source via Home Page `ujMLJ` subtree)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-02, VAL-25-09, VAL-25-12, VAL-25-14, VAL-25-15)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 2 + § 3 + § 12; § "Open Flags — Phase 24" OPEN-24-13 row; § "Icon Glyphs (Phase 24)" shipped list — confirm Instagram absent + Substack absent; § "Tokens Written — Primitives" Chivo entry)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (library_parents."_Components / Sections" g9oRa5; primitives.Input.parent = nwJk7; primitives.Button.parent = M7eUr)
    - .planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md (glyph-shipping precedent: 10 lucide-native glyphs shipped Phase 24 + 4 brand glyphs explicitly deferred to Phase 25 Footer per D-28 → OPEN-24-13)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (Plan 25-01 outcomes: Section/Header pattern, Secondary Button presence, D-43 path used)
  </read_first>
  <action>
    1. Pre-flight per D-54: `mcp__pencil__get_editor_state({ include_schema: false })`. Assert match.

    2. Confirm Pencil schema is current: call `mcp__pencil__get_editor_state({ include_schema: true })` if not cached.

    3. Read Pencil composition guidelines: `mcp__pencil__get_guidelines({ topic: "design-system" })`. Record any deviations from PEN-INVENTORY snapshot.

    4. Verify Plan 25-01 outputs intact:
       - `mcp__pencil__batch_get({ queries: [{ id: "g9oRa5", readDepth: 2 }] })` — confirm Section/Header child present with reusable:true.
       - If Plan 25-01 used D-43 PREFERRED path: `batch_get({ queries: [{ id: "M7eUr", readDepth: 3 }] })` to confirm Secondary cell intact.
       - If Plan 25-01 used D-43 FALLBACK path: `batch_get({ queries: [{ id: <secondaryButtonId from 25-01 SUMMARY>, readDepth: 2 }] })` to confirm sibling component intact.
       - Record outcomes in 25-02-SUMMARY.md DRAFT.

    5. Verify the 95-token surface: `mcp__pencil__get_variables({})`. Confirm Chivo primitive present (`type-primitive-family-chivo` per Phase 23 OPEN-23-09 resolution). Record count.

    6. **Crito Footer source audit (D-48 + RESEARCH Pattern 1):**
       - Call `mcp__pencil__batch_get({ queries: [{ id: "ujMLJ", readDepth: 4 }] })`. (Same call as Plan 25-01 Task 0 — Footer is also a subtree of Home Page.)
       - Identify the Footer sub-region (bottom-region child of ujMLJ — likely named "Footer", "footer-section", or similar).
       - Enumerate and record (in 25-02-SUMMARY.md DRAFT `## Crito Footer Source Audit` table) for each Footer child:
         | node_id | type | name | text (if text) | layout | gap | padding | fill | stroke | strokeWidth | fontFamily | fontSize | width | height |
         |---|---|---|---|---|---|---|---|---|---|---|---|---|---|
       - Specifically capture:
         (a) Footer parent frame `fill` (likely `#141f39ff` navy-900 per PEN-INVENTORY Audit Findings → bound to `color-semantic-bg-footer` or `color-semantic-bg-dark`).
         (b) Brand-column structure — Crito logo/wordmark + tagline text + social-row (LinkedIn + Twitter per Phase 24 24-04 audit `eky09` + `2P1I5`). Joel's Section/Footer ships Instagram + Substack INSTEAD per Pitfall 7.
         (c) Link columns — count, headings (text + literal), link list per column (count + literal strings per D-48 strict source-wins). RESEARCH § Recommended Footer Structure anticipates 4 columns ("Company" / "Help" / "Resources" / "Link" — final Crito column literally called "Link" per source-wins).
         (d) Copyright-row text — literal copyright string, font (likely Chivo per D-47), size, fill, opacity.
         (e) Newsletter Subscribe input (Open Question 4) — if visible in audit (image-import-14 raster suggests "Get update by signup newsletter" presence), capture: input control properties (placeholder text, fill, border), Subscribe button properties (label text, fill — yellow or green per Open Q4 resolution).
         (f) Chivo typography usage — confirm which text nodes specifically use Chivo (likely brand-tagline + copyright-row).

       - Record full audit in 25-02-SUMMARY.md DRAFT.

    7. **Resolve Open Question 4 inline (Newsletter Subscribe button color from raster vs source):**
       - If audit step 6(e) returns a yellow Subscribe button color: this is a new Button purpose not in Phase 24 ship-list. Decision:
         (i) Default per RESEARCH Open Question 4 recommendation — instance Default green Button with fill override at the instance level (not shipping a new variant). Surface as OPEN-25-XX (severity: minor; consumer phase: 31 Homepage Footer instance) noting the color override.
         (ii) Alternatively, if Crito's audit color is identical to the Phase 23 amber-500 (`#fdba09`) token, this would justify a third Button purpose variant — but per D-24 source-wins + Pitfall 6 baseline-drift prevention, do NOT extend Phase 24 Button without explicit user approval. Default is path (i).
       - Record decision in SUMMARY.

    8. **Resolve Open Question 6 inline (Chivo token strategy):**
       - Per RESEARCH Open Question 6 default recommendation: reference the Chivo primitive token directly (`type-primitive-family-chivo`); do NOT add `type-semantic-footer-body-*` semantic alias family. Footer is the SINGLE consumer of Chivo per audit; aliasing at this scale adds no readability gain (Pitfall 7 prevention).
       - Record decision in SUMMARY.

    9. Record `## Crito Footer Source Audit` table + `## Decisions (Open Q4 + Q6)` sections in 25-02-SUMMARY.md DRAFT.
  </action>
  <verify>
    - Pre-flight passed.
    - Plan 25-01 outputs intact (Section/Header + Secondary Button present).
    - `mcp__pencil__get_variables({})` confirms Chivo primitive present.
    - Footer source audit table populated with ≥ 5 enumerated nodes covering brand-column, link columns, copyright-row, and (if present) Newsletter Subscribe.
    - Open Questions 4 + 6 resolutions recorded.
    - SUMMARY DRAFT exists with the audit table + decisions.
  </verify>
  <acceptance_criteria>
    - VAL-25-14 satisfied for this task: pre-flight call logged in SUMMARY.
    - Footer source audit supplies the literal link-column labels + copyright text + Chivo usage points + Newsletter button color that Tasks 3-4 will reference.
    - Open Questions 4 + 6 resolutions documented as decision rows in SUMMARY (Tasks 3-4 can proceed without re-deciding).
    - Zero mutations attempted on `design/Crito.pen` in this task.
    - Plan 25-01 baseline verified intact (Section/Header + Secondary Button unchanged).
  </acceptance_criteria>
  <done>
    25-02-SUMMARY.md DRAFT exists at `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` with sections `## Active-Editor Pre-flight Result (D-54)`, `## Plan 25-01 Outputs Verification`, `## Guidelines Snapshot`, `## Crito Footer Source Audit`, `## Decisions (Open Q4 + Q6)`. Tasks 1-4 can reference these without re-running the audit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pencil lucide Instagram probe (D-45) — confirm Pattern A still works for brand glyph</name>
  <files>design/Crito.pen (probe artifact: temporary `Primitive / Icon / Probe-Instagram` test icon node inside avgor — deleted at task close)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-45, D-46)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Don't Hand-Roll" — Instagram via lucide Pattern A (with deprecation-warning caveat per lucide#2792); § "Code Examples Pattern 5", § "Sources" — lucide brand-icon policy section quoting lucide#2792 closed "not planned")
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-09, VAL-25-14, VAL-25-21)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 3 Icons — library: lucide)
    - .planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md (Phase 24 Q4 probe precedent — Pattern A confirmation for lucide chevron-right with deletion of probe artifact)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Probe Pattern A for Instagram via lucide (D-45):**
       ```text
       mcp__pencil__batch_design({
         operations: [
           'probeInstagram=I(avgor,{type:"icon",library:"lucide",icon:"instagram",width:24,height:24,fill:"#ffffffff",name:"Primitive / Icon / Probe-Instagram"})'
         ]
       })
       ```
       - Capture returned id as `probeInstagramId`.

    3. **Verify probe outcome:**
       - `mcp__pencil__batch_get({ queries: [{ id: <probeInstagramId>, readDepth: 2 }] })`. Inspect response:
         (a) Did the insertion succeed (no error)?
         (b) Does the returned node have `type: "icon"`, `library: "lucide"`, `icon: "instagram"`?
         (c) Did Pencil emit a deprecation warning (in `batch_design` response payload or via inspector output)?
       - `mcp__pencil__get_screenshot({ nodeId: <probeInstagramId> })`. Visually confirm: an Instagram camera-with-rounded-square glyph renders correctly (not blank, not placeholder, not error icon).

    4. **Decision: Pattern A confirmed for Instagram, or fallback?**
       - If Step 3 succeeded with visible glyph: PATTERN A SHIPPED for Instagram. Record any deprecation-warning text observed.
       - If Step 3 failed (insertion error, blank glyph, error icon, or any rejection): Pattern B fallback for Instagram. Task 2 would then add an Instagram atomic-glyph component alongside Substack. (Default expectation per RESEARCH § Sources lucide#2792: deprecated-but-present — Pattern A should still work but emit a warning.)
       - Record decision in 25-02-SUMMARY.md `## Instagram Lucide Probe (D-45)` section.

    5. **User visual checkpoint:** Present `get_screenshot` of probe Instagram icon. Ask user to confirm:
       (a) Instagram glyph renders correctly (recognizable as Instagram camera icon).
       (b) Any deprecation warning behavior observed is acceptable (deprecation-warnings are non-blocking; lucide still ships the glyph).
       (c) Approve Pattern A for Instagram (or describe fallback need if probe failed).

    6. **Probe artifact cleanup:**
       ```text
       mcp__pencil__batch_design({
         operations: [
           'Delete(<probeInstagramId>)'
         ]
       })
       ```
       - Verify absence: `batch_get({ queries: [{ id: <probeInstagramId>, readDepth: 1 }] })` should return empty/not-found.
  </action>
  <verify>
    - Pre-flight passed.
    - Probe insertion result captured (success or failure).
    - Visual confirmation captured.
    - User approved Pattern A decision (or fallback path).
    - Probe artifact deleted (Phase 24 24-04 precedent).
  </verify>
  <acceptance_criteria>
    - VAL-25-14 satisfied for this task: pre-flight + batch_design preceded by `get_editor_state`.
    - Instagram glyph mechanism decision recorded with user approval: Pattern A (default expectation) OR Pattern B (fallback if probe failed).
    - Deprecation-warning behavior documented per RESEARCH lucide#2792 awareness.
    - Probe artifact absent from `design/Crito.pen` at task close.
    - Pre-validation for VAL-25-09 (Instagram social-row instance) — mechanism known before Task 3 builds the social-row.
  </acceptance_criteria>
  <done>
    25-02-SUMMARY.md `## Instagram Lucide Probe (D-45)` section populated with probe outcome + user approval. probeInstagramId artifact deleted. Pattern A confirmed (or fallback noted) for use in Task 3 Footer build.

    Resume signal: User types "Pattern A approved" or "fallback to Pattern B for Instagram needed" or describes alternative.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Substack atomic-glyph component (Pattern B per D-46)</name>
  <files>design/Crito.pen (NEW reusable atomic-glyph component named `Primitive / Icon / glyphs / substack` inside avgor or as appropriate sibling per Pencil schema; child vector node with simpleicons.org canonical SVG path)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-46)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Code Examples Pattern 5" — verbatim SVG path; § "Common Pitfalls" 5 (Inventing brand-glyph SVG paths); § "Sources" — simpleicons canonical path verified 2026-06-01)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-09, VAL-25-10, VAL-25-12, VAL-25-14, VAL-25-21)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 3 — Icons + atomic-glyph subfolder pattern; § "Icon Glyphs (Phase 24)" ship-list)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (primitives.Icon — confirms Pattern A is the default Phase 24 mechanism; Pattern B subfolder pattern is documented but unused as of Phase 24)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Determine the parent for the Substack atomic-glyph component:**
       - Per D-46 + RESEARCH § Recommended Footer / Pattern 5: parent should be `_Components / Primitives / Icon / glyphs / substack`. The `glyphs/` subfolder may need to be created (as a frame inside avgor) if not present from Phase 24 Pattern B paths.
       - Check current avgor children via `batch_get({ queries: [{ id: "avgor", readDepth: 2 }] })`. Phase 24 shipped Pattern A only — no `glyphs` subfolder exists. Decision:
         (a) Ship Substack as a sibling reusable component directly inside avgor named `Primitive / Icon / glyphs / substack` (the `/` is part of the name string — visually shows as nested in Pencil's library picker per the slash-separated naming convention used throughout Phase 24).
         (b) OR create an actual nested `glyphs` frame inside avgor first, then ship Substack inside it.
       - Default per Phase 24 naming convention (slash-separated names → visual nesting in picker): path (a) — single insertion, no nested frame.

    3. **Build Substack atomic-glyph component (RESEARCH Pattern 5 verbatim — simpleicons canonical):**
       - Use `find_empty_space_on_canvas` to position inside avgor without overlap with existing primitives.
       ```text
       mcp__pencil__batch_design({
         operations: [
           'substackGlyph=I(avgor,{type:"frame",name:"Primitive / Icon / glyphs / substack",x:<X>,y:<Y>,width:24,height:24,reusable:true})',
           'substackPath=I(substackGlyph,{type:"vector",path:"M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z",fill:"#141f39ff",width:24,height:24})'
         ]
       })
       ```
       - The SVG path is QUOTED VERBATIM from RESEARCH § Code Examples Pattern 5 + § Pitfall 5 (also quoted in § Sources with viewBox 0 0 24 24). Verified 2026-06-01.
       - Default fill `#141f39ff` (navy primary text — generic for now; Section/Footer's social-row consumer will override fill to `#ffffffff` for navy-bg-with-white-glyphs treatment).

    4. **Verify Substack glyph:**
       - `mcp__pencil__batch_get({ queries: [{ id: <substackGlyphId>, readDepth: 2 }] })`. Confirm:
         (a) Frame marked `reusable: true`, name matches.
         (b) Vector child has `path` property matching the simpleicons canonical character-for-character: `M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z`.
       - `mcp__pencil__get_screenshot({ nodeId: <substackGlyphId> })`. Visually confirm: a Substack mark (stylized "S"-pile / sub-letter logo per simpleicons reference) renders correctly.

    5. **Provenance attestation (VAL-25-10 + D-46):**
       - Record in 25-02-SUMMARY.md `## Substack Atomic-Glyph Build (Pattern B)`:
         * Source: https://simpleicons.org/icons/substack (verified 2026-06-01 per RESEARCH § Sources)
         * SVG path: `M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z` (24x24 viewBox)
         * Substack ID: `<substackGlyphId>` captured for Task 3 social-row instance.
         * Path verbatim check: matches RESEARCH literal character-for-character.
  </action>
  <verify>
    - Pre-flight passed.
    - Substack reusable atomic-glyph component exists at `<substackGlyphId>` inside avgor.
    - Vector path matches simpleicons canonical character-for-character.
    - Visual screenshot confirms Substack mark renders.
    - Provenance attestation row drafted for PEN-INVENTORY Task 4 update.
  </verify>
  <acceptance_criteria>
    - VAL-25-10 PASS: Pattern B Substack atomic-glyph SVG path matches simpleicons canonical (provenance attestation in SUMMARY).
    - VAL-25-12 progress: Substack glyph uses Pencil's vector node inside a frame (no absolute positioning issues — single vector child).
    - VAL-25-14 satisfied for this task: pre-flight before batch_design.
    - Pitfall 5 (Inventing brand-glyph SVG paths) prevented: path quoted verbatim from RESEARCH; simpleicons.org cited.
  </acceptance_criteria>
  <done>
    Substack atomic-glyph component shipped at recorded id. 25-02-SUMMARY.md `## Substack Atomic-Glyph Build (Pattern B)` section populated with provenance citation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build Section / Footer parent frame + all children (brand-column, link columns, copyright-row, social-row with Instagram + Substack, optional Newsletter)</name>
  <files>design/Crito.pen (NEW reusable `Section / Footer` inside g9oRa5; child frames per RESEARCH § Recommended Footer Structure: main-row with brand-column + 4 link columns; social-row instancing Instagram via Pattern A confirmed in Task 1 + Substack via ref to Task 2's atomic-glyph; copyright-row with Chivo text; optional newsletter Subscribe row if Task 0 audit confirmed presence)</files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-45, D-46, D-47, D-48, D-55, D-56)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Recommended `_Components / Sections / Footer` Structure", § "Code Examples Pattern 5" — social-row consumer-override pattern for Pattern B Substack instance, § "Common Pitfalls" 7 (Picking up the wrong Footer Joel-brand swap pattern at the component layer))
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-02, VAL-25-09, VAL-25-12)
    - .planning/research/PEN-INVENTORY.md (§ "Pencil Guidelines" verbatim § 2 Slots + § 12 Spacing; § "Tokens Written — Primitives" — Chivo entry + color-primitive-navy-900 + color-semantic-bg-dark / -bg-footer; § "Variant Evidence (Phase 24)" Input + Button rows for instance reuse)
    - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (primitives.Icon.variants["24"] = u7NmaS for size variant; primitives.Input.parent = nwJk7 for Newsletter input; primitives.Button.parent = M7eUr for Subscribe button)
    - 25-02-SUMMARY.md DRAFT (Task 0 audit + Task 1 probe outcome + Task 2 Substack glyph id)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Build Section / Footer parent frame:**
       - Width from Task 0 audit (likely 1440 to match Crito source width). Height per audit (likely ~400-500 for full Footer with brand + 4 columns + copyright).
       - Position inside g9oRa5 — use `find_empty_space_on_canvas` to avoid overlap with Section/Header from Plan 25-01.
       ```text
       footer=I(g9oRa5,{type:"frame",name:"Section / Footer",x:<X>,y:<Y>,width:<W from audit>,height:<H from audit>,reusable:true,layout:"vertical",alignItems:"stretch",padding:[<from audit, e.g. 60>],gap:<from audit, e.g. 40>,fill:"#141f39ff"})
       ```
       - `fill: "#141f39ff"` conceptually binds to `color-semantic-bg-dark` or `color-semantic-bg-footer` per Task 0 audit token match. Record literal in Variant Evidence rows at Task 4.

    3. **Build main-row container:**
       ```text
       footerMain=I(footer,{type:"frame",name:"main-row",layout:"horizontal",justifyContent:"space-between",alignItems:"flex-start",gap:<from audit, e.g. 60>})
       ```

    4. **Build brand-column inside main-row:**
       ```text
       brandCol=I(footerMain,{type:"frame",name:"brand-column",layout:"vertical",gap:<from audit, e.g. 20>,alignItems:"flex-start"})
       brandLogo=I(brandCol,{type:"text",text:"<Crito wordmark literal from Task 0 audit OR placeholder if logo is image>",fontFamily:"<from audit>",fontSize:<from audit>,fontWeight:<from audit>,fill:"#ffffffff"})
       brandTagline=I(brandCol,{type:"text",text:"<tagline literal from Task 0 audit>",fontFamily:"Chivo",fontSize:<from audit, likely 14 or 16>,fontWeight:400,fill:"#ffffffff",opacity:<from audit, likely 0.8>})
       ```
       - Chivo per D-47 + Open Q6 resolution (direct primitive reference). `opacity` if Task 0 audit confirms a transparency treatment.

    5. **Build social-row inside brand-column (or as a separate sibling per audit) — D-45 + D-46 Instagram + Substack:**
       ```text
       socialRow=I(brandCol,{type:"frame",name:"social-row",layout:"horizontal",gap:<from audit, e.g. 12>,alignItems:"center"})
       ```
       - Instagram (Pattern A per Task 1 probe outcome):
         ```text
         footerInsta=I(socialRow,{type:"icon",library:"lucide",icon:"instagram",width:24,height:24,fill:"#ffffffff"})
         ```
         (or fallback per Task 1 outcome if Pattern A failed.)
       - Substack (Pattern B per Task 2 build):
         ```text
         footerSubstack=I(socialRow,{type:"ref",ref:"<substackGlyphId from Task 2>",descendants:{"<substackPathNodeId>":{fill:"#ffffffff"}}})
         ```
         Note: descendants override flips the Substack atomic-glyph's default navy fill to white for the dark-bg Footer context. If the Pencil descendant override for path fill is not supported, fall back to ad-hoc inline vector insertion using the same SVG path string with fill override; record in SUMMARY.
       - **Pitfall 7 carry-forward: do NOT ship LinkedIn or Twitter atomic glyphs** — Section/Footer is Joel's component; LinkedIn+Twitter remain unshipped (Phase 24 OPEN-24-13 ack: Crito had LinkedIn+Twitter but Joel uses Instagram+Substack).

    6. **Build 4 link columns inside main-row:**
       - Iterate over link columns enumerated in Task 0 audit (likely "Company" / "Help" / "Resources" / "Link" — exact names + counts depend on audit; ship literal Crito labels per D-48).
       ```text
       col1=I(footerMain,{type:"frame",name:"link-column-1",layout:"vertical",gap:<from audit, e.g. 16>,alignItems:"flex-start"})
       col1Heading=I(col1,{type:"text",text:"<column 1 heading literal from audit>",fontFamily:"Chivo",fontSize:<from audit, e.g. 18>,fontWeight:700,fill:"#ffffffff"})
       col1Link1=I(col1,{type:"text",text:"<link 1 literal>",fontFamily:"Chivo",fontSize:<from audit, e.g. 14>,fontWeight:400,fill:"#ffffffff",opacity:<from audit>})
       col1Link2=I(col1,{type:"text",text:"<link 2>",...})
       (repeat for each link per column; repeat for each column)
       ```

    7. **Build optional Newsletter Subscribe input row (if Task 0 audit confirmed presence per Open Q4):**
       - If Footer source contained a newsletter input + Subscribe button:
         ```text
         newsletterRow=I(footer,{type:"frame",name:"newsletter-row",layout:"horizontal",gap:<from audit>,alignItems:"center"})
         newsletterInput=I(newsletterRow,{type:"ref",ref:"nwJk7",descendants:{"<placeholderId nwJk7's placeholder dbEyb>":{text:"<placeholder text from audit, e.g. 'Your email...'>"}}})
         subscribeButton=I(newsletterRow,{type:"ref",ref:"M7eUr",descendants:{"ATJK9":{text:"<button label from audit, e.g. 'Subscribe'>"}}})
         ```
         - If Open Q4 decision was to override Subscribe to yellow (`#fdba09ff`) per source: add a fill descendant override OR record as OPEN-25-XX (consumer phase 31 Homepage Footer instance) per Task 0 Open Q4 resolution.
       - If Task 0 audit did NOT find a newsletter input: skip this step. Record in SUMMARY.

    8. **Build copyright-row at bottom of Footer (D-47 Chivo):**
       ```text
       copyrightRow=I(footer,{type:"frame",name:"copyright-row",layout:"horizontal",justifyContent:"space-between",alignItems:"center"})
       copyrightText=I(copyrightRow,{type:"text",text:"<copyright literal from audit, e.g. '© 2026 Crito. All rights reserved.'>",fontFamily:"Chivo",fontSize:14,fontWeight:400,fill:"#ffffffff",opacity:0.8})
       ```
       - Optional social-icons mirror per audit — if present in source, ship; otherwise omit.

    9. **Verify structure:**
       - `mcp__pencil__batch_get({ queries: [{ id: <footerId>, readDepth: 4 }] })`. Confirm:
         (a) `reusable: true` on Section/Footer parent.
         (b) main-row + copyright-row children present.
         (c) main-row contains brand-column + 4 link columns.
         (d) brand-column contains brand-logo + brand-tagline + social-row.
         (e) social-row contains exactly 2 social icons: Instagram (type:icon library:lucide) + Substack (type:ref to Task 2 atomic-glyph).
         (f) Newsletter row present or absent per Task 0 Open Q4 audit outcome.
       - `mcp__pencil__get_screenshot({ nodeId: <footerId> })`. Capture for user checkpoint.

    10. **User visual checkpoint:** Present screenshot. Ask user to confirm:
        (a) Footer renders with navy `#141f39ff` background per Crito source.
        (b) Brand-column shows wordmark + tagline + 2 social icons (Instagram + Substack — NOT LinkedIn/Twitter).
        (c) 4 link columns render with Chivo typography + Crito-literal labels.
        (d) Copyright-row at bottom shows the literal copyright string in Chivo.
        (e) (If shipped) Newsletter Subscribe row renders with email input + Subscribe button.
        (f) Approve overall visual match against Crito Footer reference.
  </action>
  <verify>
    - Pre-flight passed.
    - Section/Footer reusable component present inside g9oRa5.
    - main-row + brand-column + N link columns + social-row + copyright-row + (optional newsletter-row) all present per audit.
    - social-row contains Instagram (Pattern A) + Substack (Pattern B ref to Task 2 glyph) — NO LinkedIn or Twitter.
    - Chivo typography used in brand-tagline + copyright-row + link columns per D-47.
    - User approved visual checkpoint.
  </verify>
  <acceptance_criteria>
    - VAL-25-02 progress: Section/Footer placed under g9oRa5 with reusable:true (Task 4 confirms via batch_get readDepth:2).
    - VAL-25-09 PASS: Walk footer social-row → confirm 2 icon nodes; Instagram is `type:icon library:lucide icon:instagram` (or Pattern B fallback per Task 1); Substack is `type:ref` to Task 2 atomic-glyph at `_Components / Primitives / Icon / glyphs / substack`.
    - VAL-25-12 progress: Section/Footer uses Pencil auto-layout (footer outer:vertical, main-row:horizontal, columns:vertical).
    - VAL-25-14 satisfied for this task: pre-flight before batch_design.
    - D-48 compliance: link column headings + link list text nodes match Task 0 literal labels exactly.
    - D-47 compliance: Chivo primitive references confirmed in brand-tagline + copyright + link columns.
    - Pitfall 7 prevention verified: NO LinkedIn or Twitter atomic glyphs shipped.
  </acceptance_criteria>
  <done>
    25-02-SUMMARY.md `## Section/Footer Build (Task 3)` section populated with structural breakdown + screenshot inline + user approval timestamp. Section/Footer renders correctly per user approval.

    Resume signal: User types "approved" or "Footer renders correctly" or describes a fix needed. Fixes bounded to Footer's own properties (Task 1 + 2 outputs intact).
  </done>
</task>

<task type="auto">
  <name>Task 4: Sibling Pencil note + PEN-INVENTORY updates + plan close (snapshot_layout + screenshot + OPEN-24-13 resolution)</name>
  <files>
    - design/Crito.pen (NEW sibling Pencil note inside g9oRa5 documenting Section/Footer slot signature)
    - .planning/research/PEN-INVENTORY.md (Variant Evidence rows for Footer + Substack glyph + Newsletter binding if applicable; new ## Glyphs Shipped (Phase 25) sub-section under § Icon Glyphs (Phase 24); OPEN-24-13 resolution column update; OPEN-25-NN section extended with rows from Open Q4 + Q6 outcomes if needed)
    - .planning/phases/25-section-compound-components/25-02-SUMMARY.md (final write)
    - .planning/ROADMAP.md (Phase 25 plan checkbox for 25-02 updated)
  </files>
  <read_first>
    - .planning/phases/25-section-compound-components/25-CONTEXT.md (D-56, D-57, D-54)
    - .planning/phases/25-section-compound-components/25-RESEARCH.md (§ "Implementation Order Recommendation" — Plan 25-02 close discipline)
    - .planning/phases/25-section-compound-components/25-VALIDATION.md (VAL-25-02, VAL-25-09, VAL-25-10, VAL-25-12, VAL-25-14, VAL-25-15, VAL-25-16, VAL-25-21, VAL-25-23 — Plan 25-02 owned)
    - .planning/research/PEN-INVENTORY.md (Plan 25-01 updates landed; § "Variant Evidence (Phase 24)" + § "Open Flags — Phase 24" OPEN-24-13 row format; § "Icon Glyphs (Phase 24)" ship-list to extend)
    - .planning/phases/25-section-compound-components/25-01-SUMMARY.md (precedent for SUMMARY section structure + PEN-INVENTORY update pattern)
    - .planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md (precedent for glyph-shipping documentation)
    - .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (precedent for snapshot_layout text-clipping quirk documentation)
  </read_first>
  <action>
    1. Pre-flight per D-54.

    2. **Build sibling Pencil note (COMP-06 Success Criterion 3):**
       ```text
       footerNote=I(g9oRa5,{type:"note",text:"Section / Footer — slot signature:\n• main-row (horizontal): brand-column + 4 link columns (instances + child overrides — no slot pattern; structure is fixed at component level per D-48 strict source-wins)\n• brand-column (vertical): brand-logo placeholder text + brand-tagline (Chivo per D-47) + social-row\n• social-row (horizontal, 2 instances): Instagram (Pattern A Pencil-native lucide per D-45 — deprecation-warning aware per lucide#2792) + Substack (Pattern B ref to Primitive / Icon / glyphs / substack atomic-glyph component, SVG path from simpleicons.org canonical verified 2026-06-01 per D-46)\n• link-column-1..4: heading text + link list text nodes (literal Crito-source strings per D-48 — Phase 31 Homepage Footer instance may override link text via consumer-side text override)\n• copyright-row (horizontal, bottom): copyright text (Chivo per D-47)\n• [optional] newsletter-row: Primitive/Input/Default instance for email + Primitive/Button/Default instance for Subscribe (per Open Q4 source-audit outcome)\n\nPitfall 7: LinkedIn + Twitter (Crito source) NOT shipped. Section/Footer is Joel's component — Joel-brand glyphs Instagram + Substack are the component-level defaults. Crito-brand swap at the component LEVEL is intentional, not a Phase 31 override task.\n\nAll children use Pencil auto-layout per LAYOUT-01. Background fill #141f39ff binds to color-semantic-bg-dark (Crito Footer source navy). Phase 24 baseline IDs unmutated. See PEN-INVENTORY § Variant Evidence (Phase 24) + § Glyphs Shipped (Phase 25).",fontFamily:"Inter",fontSize:12,fill:"#52525bff",width:600})
       ```
       - If `type: "note"` not supported, use `type: "text"` per Phase 24 24-04 precedent (SGQZC).

    3. **PEN-INVENTORY updates (D-56):**

       (a) **Extend `## Variant Evidence (Phase 24)` table** with these new rows:

       | component | location | property | literal_value | bound_to_token | source_evidence | rationale |
       |---|---|---|---|---|---|---|
       | `Section / Footer` | parent frame | fill | `#141f39ff` | `color-semantic-bg-dark` (or `-bg-footer`) → `color-primitive-navy-900` | Crito Home Page Footer subtree audit (Plan 25-02 Task 0) | D-48 source-wins navy background |
       | `Section / Footer` | parent frame | layout / padding / gap | `vertical` / `[<py from audit>]` / `<gap from audit>` | `space-semantic-section-*` matches per audit | source audit (Plan 25-02 Task 0) | Footer outer container auto-layout |
       | `Section / Footer / brand-column / brand-tagline` | text | fontFamily / fontSize / fill / opacity | `Chivo` / `<from audit>` / `#ffffffff` / `<from audit>` | `type-primitive-family-chivo` (D-47 conditional primitive earns its keep) / matched type-semantic-body-sm-* / `color-primitive-white` / (opacity literal) | source audit (Plan 25-02 Task 0) | D-47 Chivo confirmed load-bearing; Open Q6 resolution: primitive direct reference (no semantic alias) per Pitfall 7 prevention |
       | `Section / Footer / social-row / footerInsta` | icon node | type / library / icon / fill | `icon` / `lucide` / `instagram` / `#ffffffff` | Pattern A Pencil-native; `color-primitive-white` | Plan 25-02 Task 1 lucide probe outcome | D-45 Pattern A confirmed (with deprecation-warning awareness per lucide#2792) — OR fallback per Task 1 outcome |
       | `Section / Footer / social-row / footerSubstack` | ref + descendants | ref / pathFill descendant | `<substackGlyphId>` / `#ffffffff` | (ref to atomic-glyph) / `color-primitive-white` | Plan 25-02 Task 2 build (simpleicons.org canonical) | D-46 Pattern B atomic-glyph; descendant fill override for dark-bg-with-white-glyphs treatment |
       | `Primitive / Icon / glyphs / substack` | vector child | path | `M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z` | (no token — SVG path is data, not styling) | simpleicons.org canonical (verified 2026-06-01) | D-46 + VAL-25-10 provenance attestation |
       | `Primitive / Icon / glyphs / substack` | vector child | width / height / fill | `24` / `24` / `#141f39ff` (component default; overridden per consumer) | matches Phase 24 Icon/24 default size + `color-semantic-text-primary` | Plan 25-02 Task 2 | Matches Phase 24 Icon size 24 convention |
       | `Section / Footer / link-column-N / heading` | text | fontFamily / fontSize / fontWeight / fill | `Chivo` / `<from audit>` / `<from audit>` / `#ffffffff` | `type-primitive-family-chivo` + matched semantic + `color-primitive-white` | source audit (Plan 25-02 Task 0) per D-48 + D-47 | Crito link-column headings literal labels + Chivo typography |
       | `Section / Footer / link-column-N / link` | text (N text nodes per column) | fontFamily / fontSize / fill / opacity | `Chivo` / `<from audit>` / `#ffffffff` / `<from audit>` | as above + (opacity if present) | source audit per D-48 | Literal link list text per source-wins |
       | `Section / Footer / copyright-row / copyrightText` | text | fontFamily / fontSize / fill / opacity | `Chivo` / `14` / `#ffffffff` / `0.8` | as above | source audit per D-47 | Copyright micro-text per D-47 Chivo earns its keep |
       | `Section / Footer / newsletter-row / newsletterInput` (if shipped) | ref + descendants | ref / placeholder text override | `nwJk7` / `"Your email..."` (literal from audit) | (ref to Input/Default) | Plan 25-02 Task 3 (Open Q4 ship decision) | Newsletter instance — composition discipline |
       | `Section / Footer / newsletter-row / subscribeButton` (if shipped) | ref + descendants | ref / label text override / fill (if yellow per Q4) | `M7eUr` / `"Subscribe"` / (Default green OR yellow override) | (ref to Button/Default) | Plan 25-02 Task 3 (Open Q4 resolution) | Subscribe button — fill override at instance per Open Q4 if yellow; else Default green |

       (Row count: 10-12 minimum depending on link column count and newsletter presence.)

       (b) **Add new `## Glyphs Shipped (Phase 25)` sub-section** under § Icon Glyphs (Phase 24):

       ```markdown
       ### Glyphs Shipped (Phase 25)

       Pattern A = Pencil-native `library: "lucide"` (Phase 24 D-25 supersession); Pattern B = atomic-glyph component with hand-authored SVG path (D-46 / Phase 24 D-25 original — kept as fallback for lucide-missing brand glyphs).

       | glyph_name | pattern | provenance | shipped_in | consumer |
       |---|---|---|---|---|
       | `instagram` | A (lucide-native, deprecation-warning emitted per lucide#2792) | Pencil-native lucide library | Plan 25-02 Task 3 | Section / Footer social-row |
       | `substack` | B (atomic-glyph component `Primitive / Icon / glyphs / substack` at id `<substackGlyphId>`) | simpleicons.org/icons/substack canonical SVG path (verified 2026-06-01) | Plan 25-02 Task 2 | Section / Footer social-row |
       ```

       (c) **Update OPEN-24-13 Resolution column:**
       - OPEN-24-13 row: append "Resolved 25-02: Instagram shipped via Pattern A (lucide-native, deprecation-warning aware per lucide#2792); Substack shipped via Pattern B atomic-glyph component `Primitive / Icon / glyphs / substack` using simpleicons.org canonical SVG path. See PEN-INVENTORY § Glyphs Shipped (Phase 25). LinkedIn + Twitter NOT shipped per Pitfall 7: Section/Footer is Joel's component, Joel-brand defaults are appropriate at the component level. See 25-02-SUMMARY.md § Section/Footer Build."

       (d) **Add OPEN-25-NN rows per Open Question outcomes:**
       - If Open Q4 yellow Subscribe button surfaced: OPEN-25-XX (severity: minor; consumer phase: 31 Homepage Footer instance) — "Newsletter Subscribe button color may differ from Default green (Crito source audit raster shows yellow #fdba09 per image-import-14). Per Plan 25-02 Open Q4 resolution, Phase 31 Homepage Footer instance may override Button fill via descendant override OR ship a third Button purpose variant if a similar yellow-CTA pattern recurs."
       - If Open Q6 surfaced any token-strategy concern: OPEN-25-XX as appropriate (default no row — primitive-direct reference is the chosen path).

    4. **Snapshot_layout + screenshot validation (VAL-25-12, VAL-25-15):**
       - `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root. Expected: "No layout problems."
       - `mcp__pencil__snapshot_layout({ rootId: <footerId>, problemsOnly: true })`. Document per-frame text-clipping false-positives per Phase 24 quirk (24-05-SUMMARY).
       - `mcp__pencil__get_screenshot({ nodeId: <footerId> })`. Final visual checkpoint (already approved at Task 3).

    5. **Roadmap update:**
       - Edit `.planning/ROADMAP.md` Phase 25 plan list:
         - Change `- [ ] 25-02: Build Section / Footer ...` → `- [x] 25-02-PLAN.md — Section / Footer built; Instagram (Pattern A) + Substack (Pattern B) glyphs shipped; resolves OPEN-24-13`

    6. **Write final 25-02-SUMMARY.md** with all sections:
       - `## Active-Editor Pre-flight Result (D-54)`
       - `## Plan 25-01 Outputs Verification`
       - `## Guidelines Snapshot`
       - `## Crito Footer Source Audit`
       - `## Decisions (Open Q4 + Q6)`
       - `## Instagram Lucide Probe (D-45)` (Task 1)
       - `## Substack Atomic-Glyph Build (Pattern B)` (Task 2)
       - `## Section/Footer Build (Task 3)`
       - `## PEN-INVENTORY Updates (D-56)` (Task 4)
       - `## snapshot_layout Audit`
       - `## Reference Screenshot Set (VAL-25-23)` with Task 1 + Task 3 screenshots + approval timestamps
       - `## VAL-25-* Outcomes for Plan 25-02`
       - `## Files Modified`
       - `## Cross-cutting must_haves Satisfaction`

    7. **Plan close gate:** Visual verification was captured in Tasks 1 + 3. Final close = ROADMAP + SUMMARY commit. If executing inside `/gsd:execute-phase`, return control to orchestrator.
  </action>
  <verify>
    - Pre-flight passed.
    - Sibling note exists under g9oRa5 with Footer slot-signature text.
    - PEN-INVENTORY Variant Evidence (Phase 24) extended by N rows.
    - § Glyphs Shipped (Phase 25) sub-section created with 2 rows (Instagram + Substack).
    - OPEN-24-13 resolution column updated.
    - OPEN-25-NN rows added per Open Q4 + Q6 outcomes.
    - Document-level snapshot_layout returns "No layout problems."; per-frame quirks documented.
    - get_screenshot of Section/Footer captured.
    - ROADMAP.md Phase 25 plan checkbox for 25-02 updated.
    - 25-02-SUMMARY.md written with all 14 sections.
  </verify>
  <acceptance_criteria>
    - VAL-25-02 PASS: `batch_get(g9oRa5, readDepth: 2)` returns Section/Footer child with reusable:true.
    - VAL-25-09 PASS: Footer social-row contains 2 icon nodes; Instagram = `type:icon library:lucide icon:instagram`; Substack = `type:ref` to `<substackGlyphId>`.
    - VAL-25-10 PASS: Substack atomic-glyph SVG path matches simpleicons canonical character-for-character.
    - VAL-25-12 PASS: Section/Footer uses Pencil auto-layout throughout.
    - VAL-25-14 PASS for Plan 25-02: 4+ pre-flight calls logged, 0 mismatch incidents.
    - VAL-25-15 PASS at document level + per-frame quirks documented.
    - VAL-25-16 PASS: PEN-INVENTORY § Variant Evidence (Phase 24) extended; § Glyphs Shipped (Phase 25) sub-section added.
    - VAL-25-21 PASS: OPEN-24-13 resolution column updated with Plan 25-02 citation.
    - VAL-25-23 PARTIAL: Plan 25-02 reference screenshot set captured + user-approved at Tasks 1 + 3 checkpoints.
    - Plan 25-01 outputs intact (Section/Header + Secondary Button unchanged per Task 0 verification).
  </acceptance_criteria>
  <done>
    25-02-SUMMARY.md exists at `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` with all 14 sections populated. PEN-INVENTORY.md extended. ROADMAP.md updated. Plan 25-03 can proceed (sequential per D-57).
  </done>
</task>

</tasks>

<verification>
**Per-plan validation (VAL-25-* assertions from 25-VALIDATION.md applicable to Plan 25-02):**

| VAL ID | Owner Task | How Verified |
|---|---|---|
| VAL-25-02 (Section/Footer under g9oRa5 reusable:true) | Task 3 + Task 4 | `mcp__pencil__batch_get({ queries: [{ id: "g9oRa5", readDepth: 2 }] })` → confirm child node `Section / Footer` with `reusable: true` |
| VAL-25-09 (Footer instances Icon for Instagram + Substack) | Task 3 + Task 4 | Walk footer social-row → confirm 2 icon nodes; Instagram is `type: icon, library: lucide, icon: instagram` (Pattern A) or `type: ref` to Pattern B fallback per Task 1 outcome; Substack is `type: ref` to `<substackGlyphId>` at `_Components / Primitives / Icon / glyphs / substack` |
| VAL-25-10 (Substack atomic-glyph SVG path matches simpleicons canonical) | Task 2 + Task 4 | `batch_get(<substackGlyphId>, readDepth: 2)` → extract `path` attribute; confirm character-for-character match against the RESEARCH-quoted path |
| VAL-25-12 (auto-layout, no absolute positioning) | Task 3 + Task 4 | Walk all Section/Footer containers; assert `layout` property non-null on every container (footer:vertical, main-row:horizontal, brand-column:vertical, social-row:horizontal, link-column-N:vertical, copyright-row:horizontal) |
| VAL-25-14 (pre-flight active-editor before every mutation) | All tasks | Grep SUMMARY.md for `get_editor_state` pre-flight logs; count ≥ 4; assert 0 mismatch incidents |
| VAL-25-15 (snapshot_layout clean at document level + per-frame quirks documented) | Task 4 | Snapshot at maxDepth:0 + per-frame on Section/Footer; document quirks per Phase 24 precedent |
| VAL-25-16 (PEN-INVENTORY Variant Evidence extended per D-56) | Task 4 | Diff PEN-INVENTORY before/after; verify ≥ 10 new rows (Footer + Substack + Newsletter if shipped); plus new § Glyphs Shipped (Phase 25) sub-section |
| VAL-25-21 (OPEN-24-13 RESOLVED) | Task 4 | PEN-INVENTORY OPEN-24-13 row Resolution column updated + § Glyphs Shipped (Phase 25) sub-section present with Instagram + Substack rows |
| VAL-25-23 (user visual checkpoint at each plan close — Plan 25-02 portion) | Tasks 1, 3, 4 | Reference screenshot set captured; user-approved at Tasks 1 + 3 checkpoints |

**Cross-cutting Phase 24 + Plan 25-01 carry-forward:**
- D-54 active-editor pre-flight before every batch_design.
- snapshot_layout text-clipping quirk documented per 24-05-SUMMARY precedent.
- OPEN-23-13 dual-track: literal hex/font values in batch_design payloads; Variant Evidence rows record token bindings.
- Plan 25-01 baseline (Section/Header + Secondary Button) verified intact in Task 0.

**Phase 24 + Plan 25-01 baseline-drift verification:**
- Phase 24 baseline IDs (per id-inventory.json baseline_drift_verification): unmutated.
- Section/Header (Plan 25-01 deliverable): unmutated.
- Secondary Button (Plan 25-01 D-43 path): unmutated.
- Only avgor children grow (Substack atomic-glyph added) and g9oRa5 children grow (Section/Footer added).
</verification>

<success_criteria>
**Plan 25-02 complete when:**

1. `Section / Footer` exists in `design/Crito.pen` as a reusable component inside `_Components / Sections` (g9oRa5), with:
   - Navy `#141f39ff` background fill matching Crito source per D-48.
   - main-row containing brand-column + 4 link columns (or actual count per Task 0 audit).
   - brand-column with wordmark placeholder + Chivo tagline + social-row.
   - social-row instancing Instagram (Pattern A Pencil-native lucide per D-45 Task 1 probe outcome) + Substack (Pattern B ref to atomic-glyph from Task 2 per D-46).
   - 4 link columns with literal Crito-source labels per D-48.
   - copyright-row at bottom with Chivo text per D-47.
   - Optional newsletter-row per Task 0 Open Q4 audit outcome.

2. `Primitive / Icon / glyphs / substack` atomic-glyph component exists in `design/Crito.pen` inside avgor (or appropriate Pencil-schema location) with:
   - simpleicons.org canonical SVG path verbatim (verified 2026-06-01).
   - viewBox 24x24, reusable:true.

3. Sibling Pencil note inside g9oRa5 documents Section/Footer slot signature in human-readable form (COMP-06 Success Criterion 3).

4. PEN-INVENTORY.md extended:
   - § Variant Evidence (Phase 24) extended with ≥ 10 new Footer + Substack + Newsletter rows per D-56.
   - § Open Flags — Phase 24: OPEN-24-13 Resolution column updated with Plan 25-02 citation.
   - NEW § Glyphs Shipped (Phase 25) sub-section with Instagram (Pattern A) + Substack (Pattern B simpleicons-canonical) rows.
   - § Open Flags — Phase 25 (OPEN-25-NN) extended with rows from Open Q4 + Q6 outcomes if applicable.

5. Document-level `snapshot_layout(maxDepth:0, problemsOnly:true)` returns "No layout problems."; per-frame text-clipping quirks documented per 24-05-SUMMARY precedent.

6. User visually approved at Task 1 + Task 3 checkpoints.

7. 25-02-SUMMARY.md exists with all 14 sections populated; VAL-25-* outcomes table marks all Plan 25-02 owned assertions as PASS or PARTIAL with citations.

8. ROADMAP.md Phase 25 plan checkbox for 25-02 updated.

9. Plan 25-01 baseline intact (Section/Header + Secondary Button unchanged per Task 0 verification).

10. Phase 24 baseline IDs unmutated.

11. Plan 25-03 prerequisite met: section component shipping pattern verified twice (Header + Footer); slot mechanics observed via Logo slot (Plan 25-01) and noted carry-forward to Card slot work.

12. Pitfall 7 prevention verified: LinkedIn + Twitter (Crito source) NOT shipped — Section/Footer ships Joel-brand defaults at the component level.
</success_criteria>

<output>
After completion, write `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` per template structure. PEN-INVENTORY.md updates committed in same plan close. ROADMAP.md updated. Commit message (if `commit_docs: true`): `exec(25-02): Section/Footer — Instagram (Pattern A lucide) + Substack (Pattern B atomic-glyph from simpleicons canonical) — resolves OPEN-24-13`.

**Resume signal for Plan 25-03:** Plan 25-02 SUMMARY committed + user-approved at Task 1 + Task 3 checkpoints. Plan 25-03 can begin (sequential per D-57). Card slot work will reuse the slot-mechanics pattern observed by the Plan 25-01 Logo slot.
</output>
