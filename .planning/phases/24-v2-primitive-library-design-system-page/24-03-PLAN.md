---
phase: 24-v2-primitive-library-design-system-page
plan: 03
plan_id: "24-03"
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-04"]
files_modified:
  - tests/accessibility/v2-primitives.spec.ts
  - src/components/v2/ui/Button.astro
  - src/components/v2/ui/Card.astro
  - src/components/v2/ui/Input.astro
  - src/components/v2/ui/Badge.astro
autonomous: true
requirements:
  - COMP-07

must_haves:
  truths:
    - "tests/accessibility/v2-primitives.spec.ts exists and uses @axe-core/playwright with WCAG 2.2 AA tags (wcag2a, wcag2aa, wcag21a, wcag21aa, wcag22aa)"
    - "The spec navigates to /design-system and asserts zero axe-core violations across the full page (covering Button, Card, Input, Badge demos)"
    - "The spec includes a keyboard-navigation test: Tab from the page top reaches every interactive Button on /design-system in DOM order, and the focused element shows the accent outline (outline-color is the --color-accent OKLCH or its computed rgb equivalent)"
    - "The spec includes a keyboard-activation test: pressing Enter on a focused <button> Button triggers a click event; pressing Enter on a focused <a> Button (href variant) navigates"
    - "The spec includes an Input keyboard-reachability test: Tab reaches each Input on /design-system in DOM order, Input fields accept typed input, and the disabled Input is skipped in Tab order"
    - "The spec includes a Card interactive-keyboard test: a Card with interactive=true is reachable via Tab and shows the accent focus outline; non-interactive Cards are NOT in the Tab sequence"
    - "Any axe-core violations or keyboard-test failures discovered during this plan are fixed in the offending v2 primitive component file (Button/Card/Input/Badge) — fixes must preserve Plan 24-01/24-02 must_haves (no is:global, no dark: utilities, no v1 tokens)"
    - "npm exec playwright test tests/accessibility/v2-primitives.spec.ts completes with exit code 0"
    - "Implements CONTEXT decisions: D-18 (focus ring visible on every interactive primitive, validated by computed-style outline assertions), D-20 (Playwright + axe-core tests target /design-system itself rather than isolated routes — real BaseLayoutV2 integration; verifies keyboard tab order, zero axe violations across all variants, :focus-visible ring contrast)"
  artifacts:
    - path: "tests/accessibility/v2-primitives.spec.ts"
      provides: "Playwright + axe-core test suite validating all four v2 primitives on /design-system for WCAG 2.2 AA contrast and keyboard navigation"
      min_lines: 80
  key_links:
    - from: "tests/accessibility/v2-primitives.spec.ts"
      to: "@axe-core/playwright"
      via: "import AxeBuilder from '@axe-core/playwright'"
      pattern: "from '@axe-core/playwright'"
    - from: "tests/accessibility/v2-primitives.spec.ts"
      to: "/design-system route (Plan 24-04 output)"
      via: "page.goto('/design-system')"
      pattern: "/design-system"
    - from: "tests/accessibility/v2-primitives.spec.ts"
      to: "WCAG 2.2 AA tags"
      via: "AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa']).analyze()"
      pattern: "wcag22aa"
---

<objective>
Validate the four v2 primitives (Button, Card, Input, Badge) for WCAG 2.2 AA color contrast and keyboard navigation by running Playwright + axe-core against the rebuilt `/design-system` page from Plan 24-04. Fix any violations surfaced by the tests in the offending component file — the bar is zero axe violations, complete keyboard reachability for every interactive component, and visible accent focus outlines (D-18).

Purpose: COMP-07 is the gate that lets Phase 24 hand off to Phase 25's first real page migration. If interactive primitives fail accessibility, every subsequent page (about, blog, projects, contact) inherits the defect. This plan locks the a11y contract for the entire v2 component library at the earliest possible moment — before any feature page is built on top of these primitives.

Output: One new test file at `tests/accessibility/v2-primitives.spec.ts` plus any minimal fixes applied to v2 primitive component files needed to make the suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-CONTEXT.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-PATTERNS.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-UI-SPEC.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-RESEARCH.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-01-PLAN.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-02-PLAN.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-04-PLAN.md

# Closest existing analog — same Playwright + axe pattern, same WCAG tag list, same describe-block style
@tests/accessibility/v2-layout.spec.ts

# Auxiliary axe pattern reference (Phase 23 a11y harness — broader page coverage)
@tests/accessibility/axe-tests.spec.ts

# Targets under test (built by Plans 24-01, 24-02, 24-04)
@src/components/v2/ui/Button.astro
@src/components/v2/ui/Card.astro
@src/components/v2/ui/Input.astro
@src/components/v2/ui/Badge.astro
@src/pages/design-system.astro

# v2 tokens — focus outline color is var(--color-accent); contrast is measured against --color-text on --color-surface
@src/styles/v2/global.css

# CLAUDE.md (project rules — Lighthouse 90+, accessibility-first)
@CLAUDE.md
</context>

<threat_model>
ASVS L1 surface for tests/accessibility/v2-primitives.spec.ts: minimal — the file is a Playwright spec, not shipped to production, not loaded by the browser at runtime. Threats considered and accepted as low/none:

- V5 Validation/Sanitization: Tests issue page.fill() into Input demos with hardcoded sample strings ("Jane Smith", "test@example.com"). No user-supplied data flows in; no SSRF surface.
- V11 Business Logic: Tests assert on UI behavior; they do not invoke business logic.
- V13/V9 Web Services: Tests run against the local dev/preview server at http://localhost:4321/. No external network calls.
- V14 Configuration: No env vars consumed. Test runner is Playwright with the project's existing playwright.config.ts.
- V8 Data Protection: Tests do not handle real PII; the disabled-input demo uses placeholder strings.

Risk to consider: if fixes made under this plan inadvertently break Plan 24-01 or 24-02 must_haves (e.g., adding a `dark:` utility to fix contrast, adding an is:global block to override focus styles), the fix has regressed the v2 component contract. Mitigation: every fix must preserve the existing component's truths — re-run `grep -cE "is:global|dark:|--color-yellow|--color-turquoise|--color-magenta|--font-heading|--font-body|--border-neo" src/components/v2/ui/*.astro` and verify still 0 after fixes.

Block on: high severity findings. None identified. Note: axe-core findings will be classified as WCAG 2.2 AA violations regardless of severity rating in axe's `impact` field — the bar is zero violations of any severity per ROADMAP Success Criterion #2.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/accessibility/v2-primitives.spec.ts with axe-core + keyboard navigation suites covering all four primitives on /design-system</name>
  <files>tests/accessibility/v2-primitives.spec.ts</files>
  <read_first>
    - tests/accessibility/v2-layout.spec.ts (closest analog — same wcagTags constant, same AxeBuilder({ page }).withTags(wcagTags).analyze() pattern, same test.describe + test() block structure, same expect(results.violations).toEqual([]) pattern. Copy this skeleton verbatim and adapt the routes and assertions.)
    - tests/accessibility/axe-tests.spec.ts (broader axe pattern reference — may show how to scope axe to a region using .include(selector) or .exclude(selector); useful if /design-system has known third-party content to exclude)
    - src/pages/design-system.astro (Plan 24-04 output — read the rendered DOM order to plan Tab-sequence assertions; identify the IDs/selectors of demoed Buttons, Inputs, and the interactive Card so tests can target them precisely)
    - src/components/v2/ui/Button.astro (Plan 24-01 — confirm the disabled Button renders with pointer-events-none + opacity-50 + aria-disabled when href is set, and disabled attribute on a <button>; tests assert disabled buttons are skipped in Tab order)
    - src/components/v2/ui/Input.astro (Plan 24-02 — confirm Input renders <label for={id}>, <input id={id} aria-describedby={errorId}>, <p id={errorId} role="alert">; tests assert each Input is reachable by Tab from its label or the previous control)
    - src/components/v2/ui/Card.astro (Plan 24-02 — confirm Card with interactive=true renders tabindex={0} and shows :focus-visible accent outline)
    - src/styles/v2/global.css (line for --color-accent — the OKLCH value `oklch(0.79 0.184 148.5)`; computed style may report rgb equivalent, so tests may assert outline-color is non-empty + outline-width is "2px" + outline-offset is "2px" rather than matching the exact OKLCH string)
  </read_first>
  <action>
    Create a new file `tests/accessibility/v2-primitives.spec.ts`. Follow the same import/describe/test pattern as `tests/accessibility/v2-layout.spec.ts`.

    File header imports:
    - `import { test, expect } from '@playwright/test'`
    - `import AxeBuilder from '@axe-core/playwright'`

    Define `const wcagTags = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa']` at module scope.

    Wrap all tests in `test.describe('v2 Primitives Accessibility (Phase 24)', () => { ... })`.

    Required test cases:

    1. `test('/design-system has zero axe-core violations (WCAG 2.2 AA)')` — `page.goto('/design-system')`; `const results = await new AxeBuilder({ page }).withTags(wcagTags).analyze()`; `expect(results.violations).toEqual([])`. This is the all-up gate covering color contrast, ARIA, semantic structure, and form-label associations for every primitive demoed on the page.

    2. `test('every <button> Button on /design-system is reachable by Tab and shows accent focus outline')` — `page.goto('/design-system')`; count interactive `<button>` elements emitted by Button primitive instances (use a stable selector — e.g. `page.locator('section#button button:not([disabled])')`); iterate by Tab keypress, after each Tab assert `expect(await page.evaluate(() => document.activeElement?.tagName))` cycles through expected elements in DOM order; for each focused button, assert `getComputedStyle(document.activeElement).outlineWidth === '2px'` AND `outlineOffset === '2px'` AND `outlineColor !== ''` AND `outlineColor !== 'rgba(0, 0, 0, 0)'`. (The exact accent OKLCH-to-rgb depends on browser rendering; assert non-transparent + the 2px sizing per D-18 rather than matching the exact rgb string.)

    3. `test('Enter on a focused <button> Button fires a click event')` — `page.goto('/design-system')`; attach a `page.evaluate(() => { window.__lastClickedId = null; document.querySelectorAll('section#button button').forEach((b, i) => { b.id = b.id || 'ds-btn-' + i; b.addEventListener('click', () => { window.__lastClickedId = b.id; }); }); })`; focus the first non-disabled button via `page.locator('section#button button:not([disabled])').first().focus()`; `page.keyboard.press('Enter')`; expect `await page.evaluate(() => window.__lastClickedId)` to be a non-null id starting with 'ds-btn-'.

    4. `test('Enter on a focused <a> Button (polymorphic href variant) navigates away from /design-system')` — `page.goto('/design-system')`; locate the demoed Button with href (e.g. `page.locator('section#button a[href]').first()`); `.focus()`; `page.keyboard.press('Enter')`; `await page.waitForURL(url => !url.pathname.startsWith('/design-system'), { timeout: 3000 })` — assert URL has changed. (Alternative: assert `await page.locator('section#button a[href]').first().getAttribute('href')` returns a non-empty string and that pressing Enter triggers navigation; if the demo href targets an existing internal route, waitForURL is appropriate.)

    5. `test('every Input on /design-system is reachable by Tab and accepts typed input')` — `page.goto('/design-system')`; locate all `section#input input:not([disabled]), section#input textarea:not([disabled]), section#input select:not([disabled])`; iterate by Tab, after each focus assert `document.activeElement` matches the expected Input element; for at least one text Input, `page.keyboard.type('test value')` then `expect(await locator.inputValue())` equals 'test value'.

    6. `test('disabled Input on /design-system is skipped in Tab order')` — `page.goto('/design-system')`; locate the disabled Input demoed on the page (Plan 24-04 includes one with `disabled={true}`); assert `await locator.isDisabled()` is true; tab through the input section starting from before the disabled Input and verify focus never lands on the disabled element (`document.activeElement === disabledElement` is never true during the traversal).

    7. `test('Card with interactive=true is reachable by Tab and shows accent focus outline')` — `page.goto('/design-system')`; locate the interactive Card demo (`page.locator('section#card [tabindex="0"]')` — Plan 24-02's Card with `interactive=true` sets `tabindex={0}`); `.focus()`; assert `getComputedStyle(...).outlineWidth === '2px'` AND `outlineColor !== 'rgba(0, 0, 0, 0)'`. Then assert non-interactive Cards in `section#card` have NO `tabindex` attribute or `tabindex="-1"`: `expect(await page.locator('section#card > div:not([tabindex="0"])').count())` is ≥ 1.

    Helper rules — the spec must NOT skip violations or use `.disableRules()` to mask real findings. If axe surfaces a violation, fix the offending component (Task 2) rather than silencing the rule.
  </action>
  <verify>
    Run `node -e "console.log(require('@axe-core/playwright/package.json').version)"` — confirms axe-core/playwright is installed and resolves (already verified in 24-RESEARCH.md as 4.11.1).
    Run `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=line` — must complete with exit code 0. If exit code is non-zero, do NOT silence the failures; advance to Task 2 to fix the underlying components.
    Run `grep -c "wcag22aa" tests/accessibility/v2-primitives.spec.ts` — must return ≥ 1.
    Run `grep -c "from '@axe-core/playwright'" tests/accessibility/v2-primitives.spec.ts` — must return 1.
    Run `grep -c "/design-system" tests/accessibility/v2-primitives.spec.ts` — must return ≥ 1.
    Run `grep -cE "test\\(" tests/accessibility/v2-primitives.spec.ts` — must return ≥ 7.
  </verify>
  <acceptance_criteria>
    - File tests/accessibility/v2-primitives.spec.ts exists with ≥ 80 lines.
    - `grep -c "from '@axe-core/playwright'" tests/accessibility/v2-primitives.spec.ts` returns 1.
    - `grep -c "from '@playwright/test'" tests/accessibility/v2-primitives.spec.ts` returns 1.
    - `grep -c "wcag22aa" tests/accessibility/v2-primitives.spec.ts` returns ≥ 1.
    - `grep -c "/design-system" tests/accessibility/v2-primitives.spec.ts` returns ≥ 1.
    - `grep -cE "^test\\(|test\\.describe" tests/accessibility/v2-primitives.spec.ts` returns ≥ 7 (one describe + 7 test cases).
    - `grep -c "expect(results.violations).toEqual(\\[\\])" tests/accessibility/v2-primitives.spec.ts` returns ≥ 1.
    - `grep -c "outline" tests/accessibility/v2-primitives.spec.ts` returns ≥ 1 (focus-outline assertion present).
    - `grep -c "Tab\\|keyboard" tests/accessibility/v2-primitives.spec.ts` returns ≥ 1 (keyboard traversal present).
    - `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=line` exits 0 (after any Task 2 fixes).
  </acceptance_criteria>
  <done>
    tests/accessibility/v2-primitives.spec.ts contains a describe block with at least 7 test cases covering axe-core violations on /design-system, keyboard reachability + focus outlines for Button/Input/interactive Card, Enter activation on <button> and <a> Button variants, and exclusion of disabled controls from Tab order. The suite uses WCAG 2.2 AA tag set and references the @axe-core/playwright AxeBuilder API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix any axe-core or keyboard-test failures by amending the offending v2 primitive component file (Button, Card, Input, or Badge)</name>
  <files>src/components/v2/ui/Button.astro, src/components/v2/ui/Card.astro, src/components/v2/ui/Input.astro, src/components/v2/ui/Badge.astro</files>
  <read_first>
    - The failing axe-core output (run `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=list` and read each violation — axe reports `id` (rule name), `help`, `helpUrl`, `nodes[].html`, and `nodes[].failureSummary`)
    - The component file flagged by the violation (open the matching file under src/components/v2/ui/)
    - .planning/phases/24-v2-primitive-library-design-system-page/24-01-PLAN.md `<must_haves>` block (Button must_haves — every fix to Button must preserve these)
    - .planning/phases/24-v2-primitive-library-design-system-page/24-02-PLAN.md `<must_haves>` block (Card, Input, Badge must_haves — every fix must preserve these)
    - src/styles/v2/global.css (verify any new color/spacing utility you reach for is a v2 token; do NOT introduce v1 tokens to fix contrast)
  </read_first>
  <action>
    If Task 1's verify step shows `npm exec playwright test tests/accessibility/v2-primitives.spec.ts` exited non-zero, iterate the following loop until the suite passes:

    1. Read the failing test output. Identify whether the failure is an axe-core rule violation OR a keyboard-test assertion failure.

    2. For each axe violation:
       - Note the rule id (e.g. `color-contrast`, `label`, `button-name`, `aria-required-attr`, `region`, `landmark-one-main`, `link-name`).
       - Identify which component instance is flagged via `nodes[].html` / `nodes[].target` (Playwright shows the offending CSS selector). Map back to one of: Button.astro, Card.astro (or its sub-components), Input.astro, Badge.astro.
       - Apply the minimal fix in the offending file ONLY. Each rule's typical remediation:
         - `color-contrast`: the foreground/background pair lives in 24-UI-SPEC §"Color > Contrast Ratios"; if the spec lists a passing pair, switch the utility class to the matching v2 token (e.g. swap `text-text-muted` for `text-text` on the offending element). Do NOT add a hex literal, do NOT add a dark: utility, do NOT introduce a v1 token.
         - `label` or `form-field-multiple-labels`: Input.astro must render `<label for={id}>` exactly once per field; verify the destructure or the template path.
         - `button-name` or `link-name`: ensure Button.astro renders `<slot />` content; if Plan 24-04 demos an icon-only Button, add `aria-label` prop support to Button.astro (treat as a forward-compat extension — document in summary).
         - `aria-required-attr` or `aria-required-children`: Input.astro's required path must set `required={true}` and add a visible required-state indicator (asterisk in label, already required by Plan 24-02 must_haves).
         - `region` / `landmark-one-main`: BaseLayout in src/layouts/v2/ must wrap content in a `<main>` landmark. Do NOT modify BaseLayout in this plan — instead, if this rule fires, file the issue under Plan 24-04's revisit list and skip in `withRules` only if absolutely necessary (last resort).

    3. For each keyboard-test assertion failure:
       - If the failure is "outlineWidth is not 2px" or "outlineColor is transparent": Button/Card/Input scoped <style> blocks are not applying. Verify the `:focus-visible` rule is present in the offending component and the selector matches the root element rendered.
       - If the failure is "Tab does not reach the element": the element is missing `tabindex` (e.g. interactive Card missing `tabindex={0}`) OR is hidden/disabled by an ancestor.
       - If the failure is "Enter does not fire click": ensure the component renders a real `<button>` (not a div with role="button" — Button.astro should never do that).

    4. After every change to a component file, immediately re-run `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=line` and re-evaluate. Stop when the suite is green.

    5. After the suite passes, run a regression grep against every modified component to confirm Plan 24-01/24-02 anti-rules still hold:
       - `grep -cE "is:global" src/components/v2/ui/*.astro` returns 0.
       - `grep -cE "dark:" src/components/v2/ui/*.astro` returns 0.
       - `grep -cE "--color-yellow|--color-turquoise|--color-magenta|--font-heading|--font-body|--border-neo" src/components/v2/ui/*.astro` returns 0.

    If the suite passes on first run with no component changes, Task 2 is a no-op — proceed directly to verify.
  </action>
  <verify>
    Run `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=line` — must exit 0 with all tests passing.
    Run `grep -cE "is:global" src/components/v2/ui/Button.astro src/components/v2/ui/Card.astro src/components/v2/ui/CardHeader.astro src/components/v2/ui/CardBody.astro src/components/v2/ui/CardFooter.astro src/components/v2/ui/Input.astro src/components/v2/ui/Badge.astro` — must return 0.
    Run `grep -cE "dark:" src/components/v2/ui/Button.astro src/components/v2/ui/Card.astro src/components/v2/ui/Input.astro src/components/v2/ui/Badge.astro` — must return 0.
    Run `grep -cE "--color-yellow|--color-turquoise|--color-magenta|--font-heading|--font-body|--border-neo" src/components/v2/ui/Button.astro src/components/v2/ui/Card.astro src/components/v2/ui/Input.astro src/components/v2/ui/Badge.astro` — must return 0.
    Run `npm run astro check` — must exit 0.
    Run `npm run build` — must exit 0.
  </verify>
  <acceptance_criteria>
    - `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=line` exits 0.
    - `grep -cE "is:global|dark:" src/components/v2/ui/Button.astro src/components/v2/ui/Card.astro src/components/v2/ui/Input.astro src/components/v2/ui/Badge.astro` returns 0 (Plan 24-01 + 24-02 anti-rules still hold).
    - `grep -cE "--color-yellow|--color-turquoise|--color-magenta|--font-heading|--font-body|--border-neo" src/components/v2/ui/Button.astro src/components/v2/ui/Card.astro src/components/v2/ui/Input.astro src/components/v2/ui/Badge.astro` returns 0 (v1 tokens not reintroduced).
    - `npm run astro check` exits 0.
    - `npm run build` exits 0.
    - Every must_haves.truths assertion in 24-01-PLAN.md and 24-02-PLAN.md still holds (re-verify the targeted grep checks from those plans).
  </acceptance_criteria>
  <done>
    All v2 primitives pass axe-core WCAG 2.2 AA scan on /design-system, every interactive Button and Input is reachable by Tab in DOM order, focus states show the accent outline rule (2px solid + 2px offset), Enter activates <button> and <a> Button variants, disabled Inputs are skipped in Tab order, interactive Cards are tab-reachable while non-interactive Cards are not — and no fix has reintroduced a v1 token, dark: utility, or is:global block into any v2 primitive file.
  </done>
</task>

</tasks>

<verification>
- `npm exec playwright test tests/accessibility/v2-primitives.spec.ts --reporter=line` exits 0.
- `npm run astro check` exits 0.
- `npm run build` exits 0.
- The full accessibility suite, run from `npm exec playwright test tests/accessibility/ --reporter=line`, still exits 0 (no regression in v2-layout.spec.ts, axe-tests.spec.ts, dark-mode.spec.ts).
- Every grep regression check from 24-01-PLAN.md and 24-02-PLAN.md still passes against the (possibly modified) component files.
</verification>

<success_criteria>
- COMP-07 satisfied: All interactive v2 components (Button + Input) are validated for WCAG 2.2 AA color contrast and keyboard navigation; static a11y checks on Card and Badge also pass; tests live in `tests/accessibility/v2-primitives.spec.ts` and run in CI via the existing Playwright configuration.
- ROADMAP Success Criterion #2 satisfied: every interactive v2 component can be reached and activated by keyboard alone; all pass axe-core with zero violations.
- Phase 24 is now ready for /gsd:execute-phase verification — every Phase 24 plan has shipped, every requirement is covered, every Success Criterion has a corresponding plan.
</success_criteria>

<output>
After completion, create `.planning/phases/24-v2-primitive-library-design-system-page/24-03-SUMMARY.md` documenting:
- Final test file location and line count
- List of test cases (case name + brief description)
- List of axe-core rule violations encountered (or "no violations" if suite passed first run)
- List of component fixes applied (file:line + minimal diff description; empty if no fixes needed)
- Confirmation grep checks for Plan 24-01/24-02 anti-rules (with output)
- Verification commands run with exit codes
- Frontmatter `tech_stack`, `subsystem: tests/accessibility`, `affects: []`
</output>
