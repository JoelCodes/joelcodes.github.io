---
phase: 24-v2-primitive-library-design-system-page
plan: 04
plan_id: "24-04"
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/pages/design-system.astro
  - src/pages/design-system.json.ts
autonomous: true
requirements:
  - LEAF-04

must_haves:
  truths:
    - "src/pages/design-system.astro imports v2 BaseLayout from '../layouts/v2/BaseLayout.astro' (NOT '../layouts/BaseLayout.astro')"
    - "design-system.astro imports Button, Card, CardHeader, CardBody, CardFooter, Input, Badge from '../components/v2/ui/' (NOT from '../components/ui/')"
    - "design-system.astro contains zero imports from '../components/design-system/*' (v1 DesignSystemNav, TokenSwatch, ComponentShowcase, CodeBlock removed)"
    - "design-system.astro contains zero imports from '../components/ui/' (no v1 Button, Card, Input, Badge, CheckboxGroup)"
    - "design-system.astro renders at least 9 Button instances covering 3 variants x 3 sizes (primary/ghost/link x sm/md/lg)"
    - "design-system.astro renders Input in default, error, and disabled states"
    - "design-system.astro renders Card with at least default, elevated, and interactive variants plus a Card composition example using CardHeader+CardBody+CardFooter"
    - "design-system.astro renders Badge in accent, muted, and outline variants plus one Badge with iconLeft"
    - "design-system.astro contains zero is:global blocks and zero dark: utilities"
    - "design-system.astro contains zero references to --color-yellow, --color-turquoise, --color-magenta, --font-heading, --font-body, --border-neo*, or v1 .btn-*/.card-*/.badge-* class names"
    - "design-system.json.ts GET returns a JSON body with top-level keys colors, spacing, radii, typography, fonts (flat semantic shape — NOT nested per-palette)"
    - "design-system.json.ts colors object includes primary, primary-hover, surface, surface-muted, text, text-muted, border, accent — exactly the 8 v2 color tokens"
    - "design-system.json.ts contains zero references to yellow, turquoise, magenta, --color-yellow, --color-turquoise, --color-magenta"
    - "design-system.json.ts spacing object has keys xs, sm, md, lg, xl, 2xl with values 0.5rem, 1rem, 1.5rem, 2rem, 3rem, 5rem"
    - "design-system.json.ts radii object has keys sm, md, lg, full with values 0.375rem, 0.625rem, 1rem, 9999px"
    - "GET /design-system.json returns Content-Type application/json with HTTP 200"
    - "npm run build completes with zero errors and emits dist/design-system/index.html plus dist/design-system.json"
    - "Implements CONTEXT decisions: D-13 (Input demo states on page — default + error + helper + required + disabled + as='textarea' + as='select'), D-15 (page sections — Tokens with swatches/type ladder, Primitives with every variant×size×state for all 4, optional Usage code snippets), D-16 (/design-system.json flat semantic shape — colors/spacing/radii/typography/fonts top-level keys, no nested per-palette, no dark variants), D-17 (in-place replacement of v1 design-system.astro and design-system.json.ts on v2 BaseLayout — documented exception to FOUND-06 v1.3-coexistence), D-19 (page itself uses Tailwind v4 utility-first; zero is:global, zero dark: utilities), D-21 (only 4 primitives demoed — CheckboxGroup intentionally excluded per Phase 28 deferral)"
  artifacts:
    - path: "src/pages/design-system.astro"
      provides: "Live design-system documentation page on v2 BaseLayout demoing all four v2 primitives (Button, Card, Input, Badge)"
      min_lines: 80
    - path: "src/pages/design-system.json.ts"
      provides: "Machine-readable v2 token endpoint — flat semantic shape with colors/spacing/radii/typography/fonts"
      min_lines: 35
  key_links:
    - from: "src/pages/design-system.astro"
      to: "src/layouts/v2/BaseLayout.astro"
      via: "import BaseLayout from '../layouts/v2/BaseLayout.astro'"
      pattern: "from '../layouts/v2/BaseLayout.astro'"
    - from: "src/pages/design-system.astro"
      to: "src/components/v2/ui/{Button,Card,CardHeader,CardBody,CardFooter,Input,Badge}.astro"
      via: "named per-file imports"
      pattern: "from '../components/v2/ui/(Button|Card|CardHeader|CardBody|CardFooter|Input|Badge)\\.astro'"
    - from: "src/pages/design-system.json.ts"
      to: "src/styles/v2/global.css @theme tokens"
      via: "values copied verbatim from @theme block (lines 11-68 of global.css)"
      pattern: "0\\.625rem|0\\.79 0\\.184|Plus Jakarta Sans"
---

<objective>
Rebuild the `/design-system` page on the v2 BaseLayout shell, demoing every v2 primitive (Button, Card+CardHeader/Body/Footer, Input, Badge) shipped by Plans 24-01 and 24-02, and rewrite the `/design-system.json` endpoint to return v2 token values in a flat semantic shape (colors, spacing, radii, typography, fonts) — proving the dual-layout strategy works end-to-end before any real page migration (Phases 25-29) begins.

Purpose: The design-system page is the single canonical reference Joel and AI coding agents use to discover/copy v2 primitives. Plan 24-03 then points axe-core + keyboard navigation tests at this rebuilt page to validate Success Criterion #2. Replacing the v1 page (rather than versioning it) enforces a single source of truth — v1 primitives still exist on disk under `src/components/ui/`, they are just no longer documented.

Output: Two rewritten files — `src/pages/design-system.astro` (live demos) and `src/pages/design-system.json.ts` (machine-readable v2 token endpoint).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-CONTEXT.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-PATTERNS.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-UI-SPEC.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-RESEARCH.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-01-PLAN.md
@.planning/phases/24-v2-primitive-library-design-system-page/24-02-PLAN.md

# v2 host shell — design-system.astro wraps content in this BaseLayout
@src/layouts/v2/BaseLayout.astro

# v2 primitives consumed on the page (built by 24-01 and 24-02)
@src/components/v2/ui/Button.astro
@src/components/v2/ui/Card.astro
@src/components/v2/ui/CardHeader.astro
@src/components/v2/ui/CardBody.astro
@src/components/v2/ui/CardFooter.astro
@src/components/v2/ui/Input.astro
@src/components/v2/ui/Badge.astro

# Token source — JSON endpoint values copied verbatim from this file
@src/styles/v2/global.css

# v1 page being replaced — reference structure only (do NOT carry over imports/classes/dark mode)
@src/pages/design-system.astro

# v1 endpoint being replaced — reference for GET signature/Response shape only
@src/pages/design-system.json.ts

# v2 utility-first reference patterns
@src/components/v2/layout/Header.astro

# CLAUDE.md (project rules)
@CLAUDE.md
</context>

<threat_model>
ASVS L1 surface for design-system.astro and design-system.json.ts: minimal — both are read-only documentation routes with no user input, no auth, no persistence, no secrets. Threats considered and accepted as low/none:

- V5 Validation/Sanitization: design-system.astro renders static demo content. Astro escapes attribute values by default. The Input demos render with hardcoded values; no user submission round-trips.
- V11 Business Logic: No business rules. Page is presentational.
- V13/V9 Web Services / Communications: design-system.json.ts is a GET-only endpoint returning a static JSON literal. No upstream fetch, no path parameters, no query string parsing. The endpoint exposes design tokens which are already visible in compiled CSS — no information disclosure beyond what is already public.
- V14 Configuration: No env vars or runtime config consumed.
- V8 Data Protection: Tokens are non-sensitive (color/spacing values). The page is intentionally public (Joel + AI agents discover primitives via the URL).

Block on: high severity findings. None identified. Optional: add `noindex` meta via the BaseLayout `<head>` slot to match the v1 page's intent of "internal reference, not search-indexed" — non-blocking, but consistent with the v1 page's `<meta name="robots" content="noindex, follow">`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite src/pages/design-system.json.ts to emit v2 tokens in flat semantic shape</name>
  <files>src/pages/design-system.json.ts</files>
  <read_first>
    - src/pages/design-system.json.ts (current v1 shape — keep the `export async function GET()` signature and the `new Response(JSON.stringify(...), { headers })` structure; replace the entire tokens object body)
    - src/styles/v2/global.css (lines 11-68 — source of truth for every value emitted; copy OKLCH/rem/unit values verbatim)
    - .planning/phases/24-v2-primitive-library-design-system-page/24-RESEARCH.md (§"State of the Art" — confirms flat semantic shape preferred over nested per-palette)
    - .planning/phases/24-v2-primitive-library-design-system-page/24-PATTERNS.md (§"src/pages/design-system.json.ts" — pattern row clarifying complete file replacement)
  </read_first>
  <action>
    Completely rewrite src/pages/design-system.json.ts. Keep the `export async function GET()` signature, drop all v1 nested-palette structure. The new shape is flat semantic with five top-level keys: colors, spacing, radii, typography, fonts.

    Replace the tokens object with:
    - `colors`: object mapping each of the 8 v2 color tokens to its CSS variable name and OKLCH value. Keys: primary, primary-hover, surface, surface-muted, text, text-muted, border, accent. Each value is `{ cssVar: '--color-<key>', oklch: '<value from global.css>' }`.
    - `spacing`: object mapping t-shirt scale keys to rem values. Keys: xs (0.5rem), sm (1rem), md (1.5rem), lg (2rem), xl (3rem), 2xl (5rem). Each value is `{ cssVar: '--space-<key>', rem: '<value>' }`.
    - `radii`: object mapping radius keys to rem/px values. Keys: sm (0.375rem), md (0.625rem), lg (1rem), full (9999px). Each value is `{ cssVar: '--radius-<key>', value: '<value>' }`.
    - `typography`: object mapping the 8 text size tokens to rem values. Keys: display (3rem), h1 (2.25rem), h2 (1.875rem), h3 (1.5rem), h4 (1.25rem), body (1rem), small (0.875rem), caption (0.75rem). Each value is `{ cssVar: '--text-<key>', rem: '<value>' }`.
    - `fonts`: object with two keys (display, text) plus weights and line-heights. Shape: `{ display: { cssVar: '--font-display', stack: 'Plus Jakarta Sans Variable, ui-sans-serif, system-ui, sans-serif' }, text: { cssVar: '--font-text', stack: 'Inter Variable, ui-sans-serif, system-ui, sans-serif' }, weights: { display: 700, text: 400, textBold: 500 }, leading: { display: 1.4, text: 1.6 } }`.

    Return `new Response(JSON.stringify(tokens, null, 2), { headers: { 'Content-Type': 'application/json' } })`.

    Zero references to: yellow, turquoise, magenta, --color-yellow, --color-turquoise, --color-magenta, dark, lightDark, --font-heading, --font-body, --color-yellow-text, --color-turquoise-text, --color-yellow-dark, --color-turquoise-dark, --color-magenta-dark. The v1 endpoint's nested "primary > yellow.dark" structure is gone; v2 dark-mode-strategy is a separate concern not exposed via this endpoint.
  </action>
  <verify>
    Run `npm run astro check` — must pass with zero TypeScript errors.
    Run `npm run build` — must complete with zero errors; dist/design-system.json must exist.
    Run `cat dist/design-system.json | head -40` — must show flat top-level keys (colors, spacing, radii, typography, fonts), NOT nested primary/text/neutral palette structure.
    Run `grep -cE "yellow|turquoise|magenta|--font-heading|--font-body" src/pages/design-system.json.ts` — must return 0.
    Run `grep -cE "primary-hover|surface-muted|accent" src/pages/design-system.json.ts` — must be ≥ 3 (v2 tokens present).
    Run `grep -cE "Plus Jakarta Sans" src/pages/design-system.json.ts` — must be ≥ 1.
    Run `grep -cE "0\\.625rem" src/pages/design-system.json.ts` — must be ≥ 1 (--radius-md = 0.625rem).
  </verify>
  <acceptance_criteria>
    - File src/pages/design-system.json.ts exists with ≥ 35 lines.
    - `grep -c "export async function GET" src/pages/design-system.json.ts` returns 1.
    - `grep -cE "yellow|turquoise|magenta" src/pages/design-system.json.ts` returns 0.
    - `grep -cE "--font-heading|--font-body" src/pages/design-system.json.ts` returns 0.
    - `grep -c "primary-hover" src/pages/design-system.json.ts` returns ≥ 1.
    - `grep -c "surface-muted" src/pages/design-system.json.ts` returns ≥ 1.
    - `grep -c "accent" src/pages/design-system.json.ts` returns ≥ 1.
    - `grep -c "Plus Jakarta Sans" src/pages/design-system.json.ts` returns ≥ 1.
    - `grep -c "Inter Variable" src/pages/design-system.json.ts` returns ≥ 1.
    - `grep -c "Content-Type" src/pages/design-system.json.ts` returns 1.
    - `npm run astro check` exit code 0.
    - `npm run build` exit code 0.
    - `test -f dist/design-system.json` returns 0 (file exists).
  </acceptance_criteria>
  <done>
    src/pages/design-system.json.ts emits v2 tokens in a flat semantic shape with five top-level keys (colors, spacing, radii, typography, fonts), references zero v1 token names, returns Content-Type application/json, and builds without errors. The endpoint is now machine-readable for AI agents discovering v2 design tokens.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite src/pages/design-system.astro on v2 BaseLayout with live demos of all four primitives</name>
  <files>src/pages/design-system.astro</files>
  <read_first>
    - src/pages/design-system.astro (current v1 page — reference structure ONLY: section IDs, the noindex meta slot, the container/grid scaffolding pattern. DO NOT carry over: BaseLayout from ../layouts/BaseLayout.astro, DesignSystemNav/TokenSwatch/ComponentShowcase/CodeBlock imports, v1 Button/Card/Input/Badge/CheckboxGroup imports, dark: utilities, text-text-muted-light/text-text-muted-dark Tailwind classes, font-heading utility, font-display utility from v1, .btn-*/.card-*/.badge-* class references)
    - src/layouts/v2/BaseLayout.astro (host shell — confirm the title and description props are accepted; confirm the head slot accepts a noindex meta tag)
    - src/components/v2/ui/Button.astro (Plan 24-01 output — Props interface; demo all 9 variant×size combinations)
    - src/components/v2/ui/Card.astro (Plan 24-02 output — elevated, interactive props)
    - src/components/v2/ui/CardHeader.astro
    - src/components/v2/ui/CardBody.astro
    - src/components/v2/ui/CardFooter.astro
    - src/components/v2/ui/Input.astro (Plan 24-02 output — Props: label, id, name, type, value, placeholder, helper, error, disabled, required, as)
    - src/components/v2/ui/Badge.astro (Plan 24-02 output — variant: 'accent' | 'muted' | 'outline', iconLeft)
    - src/components/v2/layout/Header.astro (utility-first reference — class:list with Tailwind tokens, font-display/font-text/text-display/text-h1/text-body utilities, --color-text/--color-accent/--color-border consumption)
    - .planning/phases/24-v2-primitive-library-design-system-page/24-UI-SPEC.md (§"Component Inventory" — every variant/size/state required on the page; §"Tokens" — color swatches section content)
    - .planning/phases/24-v2-primitive-library-design-system-page/24-CONTEXT.md (D-21 / Claude's Discretion — page-structure freedom: section-per-primitive layout encouraged, sidebar nav optional)
  </read_first>
  <action>
    Completely rewrite src/pages/design-system.astro. Replace the v1 frontmatter imports with v2 imports:
    - `import BaseLayout from '../layouts/v2/BaseLayout.astro'`
    - `import Button from '../components/v2/ui/Button.astro'`
    - `import Card from '../components/v2/ui/Card.astro'`
    - `import CardHeader from '../components/v2/ui/CardHeader.astro'`
    - `import CardBody from '../components/v2/ui/CardBody.astro'`
    - `import CardFooter from '../components/v2/ui/CardFooter.astro'`
    - `import Input from '../components/v2/ui/Input.astro'`
    - `import Badge from '../components/v2/ui/Badge.astro'`
    - Optional: `import { ArrowRight, Sparkles, Mail } from '@lucide/astro'` for icon demos.

    Drop ALL imports from `../layouts/BaseLayout.astro`, `../components/design-system/*`, and `../components/ui/*`.

    Wrap content in `<BaseLayout title="Design System (v2) | Joel Shinness" description="Internal v2 design system reference. Live demos of Button, Card, Input, and Badge primitives built on the Crito-anchored token foundation.">`. Inject `<meta slot="head" name="robots" content="noindex, follow" />` so the page is not search-indexed.

    Page structure — top-level container with class `container mx-auto px-lg py-2xl max-w-5xl` (or equivalent v2 token utilities). Inside, render these sections in order, each with a stable id anchor:

    1. `<section id="introduction">` — `<h1 class="font-display text-display font-bold text-text mb-md">Design System (v2)</h1>` with a `<p class="font-text text-body text-text-muted">` lede explaining the page demos v2 primitives on the Crito-anchored token foundation. Optionally link to `/design-system.json` for the machine-readable endpoint.

    2. `<section id="tokens">` — `<h2 class="font-display text-h2 font-bold text-text mb-md">Tokens</h2>`. Render a simple grid of token chips using Tailwind utility classes (no v1 TokenSwatch component). For colors, render 8 small `<div>` swatches with inline style `background-color: var(--color-<name>)` and a caption with the token name. For spacing, render 6 bars with inline style `width: var(--space-<name>)`. For radii, render 4 boxes with inline style `border-radius: var(--radius-<name>)`. Acceptable minimum: just render the 8 color swatches plus the typography scale (8 sample lines using `text-display`/`text-h1`/.../`text-caption` utilities).

    3. `<section id="button">` — `<h2 class="font-display text-h2 font-bold text-text mb-md">Button</h2>`. Render at minimum 9 Button instances covering all 3 variants × 3 sizes. Group as: a row of 3 Buttons (variant=primary, sizes sm/md/lg), a row of 3 Buttons (variant=ghost, sizes sm/md/lg), a row of 3 Buttons (variant=link, sizes sm/md/lg). Plus at least one Button with `iconLeft={Sparkles}`, one with `iconRight={ArrowRight}`, one with `href="/faq"` (polymorphic anchor demo — `/faq` is a live route; do NOT use `/about` since that route does not exist in `src/pages/`), one with `disabled={true}`. Wrap each row in a `<div class="flex flex-wrap items-center gap-md mb-lg">`.

    4. `<section id="card">` — `<h2 class="font-display text-h2 font-bold text-text mb-md">Card</h2>`. Render at minimum: a default Card, a Card with `elevated={true}`, a Card with `interactive={true}`, and a Card composition example using Card + CardHeader + CardBody + CardFooter with sample child content. Arrange in a `<div class="grid grid-cols-1 md:grid-cols-2 gap-md mb-lg">` grid.

    5. `<section id="input">` — `<h2 class="font-display text-h2 font-bold text-text mb-md">Input</h2>`. Render at minimum: a default text Input (`label="Name"`, `id="ds-name"`, `name="name"`, `placeholder="Jane Smith"`), an Input in error state (`label="Email"`, `id="ds-email"`, `error="Please enter a valid email"`, value="not-an-email"), an Input with helper text (`label="Phone"`, `id="ds-phone"`, `helper="Optional"`), a required Input (`label="Subject"`, `id="ds-subject"`, `required={true}`), a disabled Input (`label="Submitted"`, `id="ds-submitted"`, `disabled={true}`, `value="Locked"`), and at least one Input with `as="textarea"` (multi-line). Wrap in a `<div class="grid grid-cols-1 md:grid-cols-2 gap-md mb-lg">`.

    6. `<section id="badge">` — `<h2 class="font-display text-h2 font-bold text-text mb-md">Badge</h2>`. Render at minimum 4 Badge instances: variant=accent with text "New", variant=muted with text "Beta", variant=outline with text "Draft", and one Badge with `iconLeft={Mail}` and text "Inbox". Wrap in `<div class="flex flex-wrap items-center gap-sm mb-lg">`.

    Anti-rules — the file must contain ZERO occurrences of:
    - `is:global`
    - `dark:` (any Tailwind dark-mode utility)
    - `--color-yellow`, `--color-turquoise`, `--color-magenta` (v1 palette tokens)
    - `--font-heading`, `--font-body` (v1 font tokens)
    - `--border-neo` (v1 neobrutalist border)
    - `text-text-muted-light`, `text-text-muted-dark` (v1 split palette)
    - `font-heading` Tailwind utility (the v1 utility, replaced by `font-display`)
    - imports from `'../components/ui/'` or `'../components/design-system/'`
    - imports from `'../layouts/BaseLayout.astro'` (v1 layout)
    - any `.btn-*`, `.card-*`, `.badge-*` class names (v1 neo classes)

    Layout/spacing classes should use v2 tokens (`px-lg`, `gap-md`, `mb-lg`, etc.) wherever possible; Tailwind built-in utilities (e.g., `grid-cols-2`, `flex-wrap`, `items-center`) are acceptable.
  </action>
  <verify>
    Run `npm run astro check` — must pass with zero TypeScript errors.
    Run `npm run build` — must complete with zero errors; dist/design-system/index.html must exist.
    Run `grep -c "from '../layouts/v2/BaseLayout.astro'" src/pages/design-system.astro` — must return 1.
    Run `grep -c "from '../layouts/BaseLayout.astro'" src/pages/design-system.astro` — must return 0.
    Run `grep -cE "from '\\.\\./components/v2/ui/" src/pages/design-system.astro` — must return ≥ 7 (Button, Card, CardHeader, CardBody, CardFooter, Input, Badge).
    Run `grep -cE "from '\\.\\./components/ui/'|from '\\.\\./components/design-system/'" src/pages/design-system.astro` — must return 0.
    Run `grep -cE "is:global|dark:|--color-yellow|--color-turquoise|--color-magenta|--font-heading|--font-body|--border-neo|text-text-muted-light|text-text-muted-dark|font-heading\\b" src/pages/design-system.astro` — must return 0.
    Run `grep -c "<Button" src/pages/design-system.astro` — must return ≥ 9.
    Run `grep -cE "<Card\\b|<CardHeader|<CardBody|<CardFooter" src/pages/design-system.astro` — must return ≥ 4.
    Run `grep -c "<Input" src/pages/design-system.astro` — must return ≥ 5.
    Run `grep -c "<Badge" src/pages/design-system.astro` — must return ≥ 4.
    Run the dev server (`npm run dev &`); curl -sI http://localhost:4321/design-system — must return HTTP 200; curl -s http://localhost:4321/design-system.json | jq -r 'keys[]' — must list colors, fonts, radii, spacing, typography. Kill the dev server.
  </verify>
  <acceptance_criteria>
    - File src/pages/design-system.astro exists with ≥ 80 lines.
    - `grep -c "from '../layouts/v2/BaseLayout.astro'" src/pages/design-system.astro` returns 1.
    - `grep -c "from '../layouts/BaseLayout.astro'" src/pages/design-system.astro` returns 0.
    - `grep -cE "from '\\.\\./components/v2/ui/(Button|Card|CardHeader|CardBody|CardFooter|Input|Badge)" src/pages/design-system.astro` returns ≥ 7.
    - `grep -cE "from '\\.\\./components/(ui|design-system)/" src/pages/design-system.astro` returns 0.
    - `grep -c "is:global" src/pages/design-system.astro` returns 0.
    - `grep -c "dark:" src/pages/design-system.astro` returns 0.
    - `grep -cE "--color-yellow|--color-turquoise|--color-magenta|--font-heading|--font-body|--border-neo" src/pages/design-system.astro` returns 0.
    - `grep -c "<Button" src/pages/design-system.astro` returns ≥ 9.
    - `grep -cE "<Card\\b" src/pages/design-system.astro` returns ≥ 3.
    - `grep -c "<CardHeader" src/pages/design-system.astro` returns ≥ 1.
    - `grep -c "<CardBody" src/pages/design-system.astro` returns ≥ 1.
    - `grep -c "<CardFooter" src/pages/design-system.astro` returns ≥ 1.
    - `grep -c "<Input" src/pages/design-system.astro` returns ≥ 5.
    - `grep -c "<Badge" src/pages/design-system.astro` returns ≥ 4.
    - `grep -c 'meta slot="head"' src/pages/design-system.astro` returns ≥ 1 (noindex meta tag injected).
    - `npm run astro check` exit code 0.
    - `npm run build` exit code 0.
    - `test -f dist/design-system/index.html` returns 0.
  </acceptance_criteria>
  <done>
    src/pages/design-system.astro is rebuilt on the v2 BaseLayout, imports all four v2 primitives from src/components/v2/ui/, demos every variant/size/state required by Phase 24, contains zero v1 token/utility references, and builds without errors. The page is now the canonical reference for v2 primitives — Plan 24-03 will validate its accessibility.
  </done>
</task>

</tasks>

<verification>
- `npm run astro check` exits 0.
- `npm run build` exits 0; dist/design-system/index.html AND dist/design-system.json both exist.
- `cat dist/design-system.json | jq -e 'has("colors") and has("spacing") and has("radii") and has("typography") and has("fonts")'` returns true.
- `cat dist/design-system.json | jq -e '.colors | has("primary") and has("primary-hover") and has("surface") and has("surface-muted") and has("text") and has("text-muted") and has("border") and has("accent")'` returns true.
- `cat dist/design-system.json | jq -r 'tostring' | grep -cE "yellow|turquoise|magenta"` returns 0.
- `grep -rE "is:global|dark:|--font-heading|--font-body|--color-yellow|--color-turquoise|--color-magenta|--border-neo" src/pages/design-system.astro src/pages/design-system.json.ts` returns no matches.
- The /design-system route renders at runtime against the v2 BaseLayout (HeaderV2 + FooterV2 visible) and shows live demos of all four primitives.
</verification>

<success_criteria>
- LEAF-04 satisfied: /design-system page rebuilt on BaseLayoutV2 with live demos of all four v2 primitives; /design-system.json endpoint returns v2 token values in flat semantic shape.
- ROADMAP Success Criterion #1 satisfied: /design-system renders on BaseLayoutV2 with live demos of all four v2 primitives.
- ROADMAP Success Criterion #3 satisfied: /design-system.json returns v2 token values, not v1.
- ROADMAP Success Criterion #4 partially advanced for this page: zero is:global in design-system.astro (Phase 24 component files already enforce this per Plans 24-01/24-02).
- Plan 24-03 can now point Playwright + axe-core at /design-system to validate Success Criterion #2 (keyboard + a11y).
</success_criteria>

<output>
After completion, create `.planning/phases/24-v2-primitive-library-design-system-page/24-04-SUMMARY.md` documenting:
- Final design-system.astro line count and section layout (introduction, tokens, button, card, input, badge)
- Final design-system.json.ts shape (5 top-level keys, token counts)
- Decision on the token visualization approach in the "Tokens" section (color swatches + typography ladder, or extended to include spacing/radii)
- Decision on the page layout (sidebar nav kept, replaced with anchor links in body, or dropped entirely — D-21 Claude's Discretion)
- Verification commands run with exit codes
- Frontmatter `tech_stack`, `subsystem: pages/design-system`, `affects: [24-03]`
</output>
