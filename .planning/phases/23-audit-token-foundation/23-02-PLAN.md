---
phase: 23-audit-token-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - TOKEN-07
gap_closure: false

must_haves:
  truths:
    - "PEN-INVENTORY.md has an explicit Dark-mode omission rationale section citing CONTEXT D-01 + D-02"
    - "Future readers (downstream phases, future-Joel, next code milestone) understand the absence of dark-mode token slots is intentional, not an oversight"
  artifacts:
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Dark-mode omission rationale subsection"
      contains: "## Dark-Mode Omission Rationale (TOKEN-07 + D-01)"
  key_links:
    - from: "## Dark-Mode Omission Rationale section"
      to: "future code milestone + Phase 32 handoff doc"
      via: "explicit text + D-01 / D-02 / TOKEN-07 cross-reference"
      pattern: "D-01|TOKEN-07"
---

<objective>
Append the dark-mode omission rationale to PEN-INVENTORY.md so the deferred-by-design absence of `@light,@dark` token slots is recorded as intentional. RESEARCH determined plan 23-02 collapses from a user-interaction plan (the original ROADMAP shape, before CONTEXT D-01 resolved T7) into a documentation-only step.

Purpose: Resolve TOKEN-07 explicitly so downstream phases, the milestone-close handoff (Phase 32), and the next code milestone all understand dark-mode token slots were deferred by decision (D-01) — not accidentally omitted. Prevent a future contributor from "fixing" the absence by adding `@light,@dark` syntax retroactively.

Output: Updated PEN-INVENTORY.md with a `## Dark-Mode Omission Rationale (TOKEN-07 + D-01)` section.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-audit-token-foundation/23-CONTEXT.md
@.planning/phases/23-audit-token-foundation/23-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/23-audit-token-foundation/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Dark-Mode Omission Rationale section to PEN-INVENTORY.md</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-01 omit dark slots entirely, D-02 document the omission explicitly)
    - .planning/REQUIREMENTS.md (TOKEN-07 — Dark-mode token slots deferred)
    - .planning/research/PEN-INVENTORY.md (current state to append to, not overwrite)
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## State of the Art — old/current dark-slot positions)
  </read_first>
  <action>
    Append (do NOT overwrite) a new section to .planning/research/PEN-INVENTORY.md at the end of the file. Use the exact heading and body shape below:

    `## Dark-Mode Omission Rationale (TOKEN-07 + D-01)`
    blank line
    `**Decision (CONTEXT D-01):** Omit dark slots entirely from the v2.0 token surface. No `@light,@dark` Pencil variable syntax. Tokens are single-theme for v2.0.`
    blank line
    `**Why this is not an oversight:** STACK.md originally recommended encoding `@light,@dark` at definition time with dark stubbed to mirror light (retrofitting later is more expensive). CONTEXT D-01 reverses that recommendation for v2.0 because:`
    `- Pre-naming `--color-surface-dark` slots is a form of premature commitment (PITFALLS O3).`
    `- Dark mode is explicitly deferred per TOKEN-07 and the v2.0 PROJECT.md scope.`
    `- A future milestone will design dark values from scratch with fresh decisions — no pre-named slots.`
    blank line
    `**Forbidden token-name patterns in this milestone:**`
    `- Anything containing `@dark`, `@light`, `-dark`, `-light` as a suffix or theme marker.`
    `- Anything carrying a per-theme variant in the value field.`
    blank line
    `**For downstream readers (Phase 24+ planners, Phase 32 handoff writer, next code milestone):**`
    `- The absence of dark token slots in `get_variables({})` is intentional and load-bearing.`
    `- Do not add `@light,@dark` syntax retroactively in any v2.0 phase.`
    `- Dark mode lives in a later milestone; pre-emptive slots in this one are out of scope.`
    blank line
    `**Cross-reference:** CONTEXT.md D-01 (decision), D-02 (this documentation requirement), TOKEN-07 (requirement), RESEARCH.md ## State of the Art (the reversed STACK recommendation).`

    Append behavior is critical — read the file first, locate the end of file (after the existing `## Coverage Checkpoint` section), and append the new section. Do not rewrite or reorder existing sections.
  </action>
  <verify>
    grep -c '^## Dark-Mode Omission Rationale' .planning/research/PEN-INVENTORY.md   # exactly 1
    grep -E 'D-01|TOKEN-07' .planning/research/PEN-INVENTORY.md   # cross-references present
    grep -c '^## ' .planning/research/PEN-INVENTORY.md   # previous section count + 1
  </verify>
  <acceptance_criteria>
    - `## Dark-Mode Omission Rationale (TOKEN-07 + D-01)` heading appears exactly once in PEN-INVENTORY.md.
    - Section contains the four subheadings/clauses: Decision, Why this is not an oversight, Forbidden token-name patterns, For downstream readers, Cross-reference.
    - Section explicitly names D-01, D-02, TOKEN-07 as cross-references (greppable).
    - All sections written by plan 23-01 (Schema Snapshot, Pencil Guidelines, Frames, Frame Classification Rules Applied, Audit Findings, Open Flags, Coverage Checkpoint) are still present and unchanged.
    - File remains plain markdown — no JSON blocks introduced (D-18 enforcement).
  </acceptance_criteria>
  <done>PEN-INVENTORY.md has the dark-mode omission rationale section appended at the bottom; original audit content is preserved.</done>
</task>

</tasks>

<verification>
  - PEN-INVENTORY.md contains the new `## Dark-Mode Omission Rationale (TOKEN-07 + D-01)` section.
  - All sections written by plan 23-01 are present and intact.
  - The section is greppable for D-01 and TOKEN-07 cross-references.
  - No new files created; only PEN-INVENTORY.md is modified.
</verification>

<success_criteria>
  - TOKEN-07 (dark-mode deferral) is explicitly documented in PEN-INVENTORY.md per D-02.
  - Downstream phase planners reading PEN-INVENTORY.md will encounter the rationale before any `set_variables` call, preventing accidental dark-slot creation.
  - VAL-23-02 condition "Zero tokens carry @light,@dark or any per-theme suffix" is reinforced by the documented forbidden-patterns list.
</success_criteria>

<output>
After completion, create `.planning/phases/23-audit-token-foundation/23-02-SUMMARY.md` with:
- Section added (heading + line count)
- Files modified
- Confirmation that D-01 / D-02 / TOKEN-07 cross-references are present
</output>
