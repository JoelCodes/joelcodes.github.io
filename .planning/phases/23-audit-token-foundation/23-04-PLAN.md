---
phase: 23-audit-token-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 23-03
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04
  - TOKEN-05
  - TOKEN-06
gap_closure: false

must_haves:
  truths:
    - "Calling get_variables({}) returns semantic aliases covering bg/text/border/accent color roles, section-y/container-x/stack/inline spacing roles, display/h1-h6/body/body-sm/caption/button typography roles, prose roles (paragraph/link/list/inline-code), and card/button/input/pill radius roles"
    - "Every semantic alias resolves to a primitive token value — either via Pencil's native variable-to-variable aliasing OR via the description-field fallback that names the source primitive (per RESEARCH Open Question 2 + ARCHITECTURE fallback)"
    - "Components in Phases 24-32 can reference semantic aliases without ever knowing primitive names (TOKEN-01 + D-14)"
    - "Prose typography tokens cover paragraph spacing, link styling, list bullets — derived from Crito Blog + FAQ frames per D-15; inline-code likely an OPEN flag per D-16"
  artifacts:
    - path: "design/Crito.pen (variable surface only)"
      provides: "Semantic alias tier — role-based names referencing primitives"
      contains: "color-semantic-*, space-semantic-*, type-semantic-*, radius-semantic-*"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "## Tokens Written — Semantic Aliases log with primitive-reference per alias"
      contains: "## Tokens Written — Semantic Aliases"
  key_links:
    - from: "semantic aliases"
      to: "primitive tokens (written in 23-03)"
      via: "Pencil aliasing OR description-field primitive name"
      pattern: "color-primitive-|space-primitive-|type-primitive-|radius-primitive-"
    - from: "## Tokens Written — Semantic Aliases log"
      to: "Phase 24+ component plans"
      via: "semantic names listed here are the only names components reference (D-14)"
      pattern: "color-semantic-|space-semantic-|type-semantic-|radius-semantic-"
---

<objective>
Write the semantic alias tier (role-based token names) into design/Crito.pen. Each alias references a primitive from plan 23-03. Use probe-first pattern to resolve whether Pencil supports variable-to-variable aliasing (RESEARCH Open Question 2). If aliasing is unsupported, fall back per ARCHITECTURE: single tier with primitive provenance recorded in the variable description field. Include prose typography aliases (TOKEN-06) derived from Crito Blog + FAQ frames per D-15.

Purpose: Establish the upper tier of the two-tier token system (TOKEN-01). Components in Phases 24-32 reference semantic aliases only (D-14) — primitives are not exposed to component-author plans. This is the contract Phase 24+ planners consume.

Output: Semantic aliases present in design/Crito.pen (verifiable via get_variables); `## Tokens Written — Semantic Aliases` log appended to PEN-INVENTORY.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-audit-token-foundation/23-CONTEXT.md
@.planning/phases/23-audit-token-foundation/23-RESEARCH.md
@.planning/phases/23-audit-token-foundation/23-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/23-audit-token-foundation/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe variable-to-variable aliasing — write ONE semantic alias to resolve aliasing support</name>
  <files>design/Crito.pen</files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## Open Questions 2 — variable-to-variable aliasing unverified; ## Code Examples — 23-04 token-write probe; ARCHITECTURE fallback to description field)
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-12 flat-dash, D-14 components reference semantic only)
    - .planning/research/PEN-INVENTORY.md (## Tokens Written — Primitives — primitive name set from plan 23-03)
    - .planning/phases/23-audit-token-foundation/23-03-SUMMARY.md (resolved set_variables argument shape)
  </read_first>
  <action>
    Probe Pencil's variable-to-variable aliasing capability before writing the full semantic-alias batch. RESEARCH Open Question 2 flagged this as unverified — the two-tier scheme assumes aliasing works; if not, fall back to single tier with description-field primitive provenance.

    Step 1 — Pick an anchor probe. Use a high-confidence semantic role: `color-semantic-bg-accent` aliasing the brand-accent primitive (likely `color-primitive-orange-500` if orange is Crito's accent). Both names must come from PEN-INVENTORY.md `## Tokens Written — Primitives` — never invented.

    Step 2 — Try aliasing path A (reference syntax). Call Pencil MCP set_variables with the argument shape resolved in plan 23-03 Task 1. Value for the new semantic name should reference the primitive — candidate shapes: `"{color-primitive-orange-500}"`, `"var(color-primitive-orange-500)"`, or `"color-primitive-orange-500"` per Pencil's schema. Use whatever shape the editor schema from 23-01 Task 1 Step 2 names for variable references. If the schema doesn't define a reference shape, try shape A1: `{ variables: { "color-semantic-bg-accent": { ref: "color-primitive-orange-500" } } }`.

    Step 3 — Call Pencil MCP get_variables with empty filter {}. Verify:
    - The semantic name appears in the response.
    - The value resolves to the primitive's literal value (or carries a `ref` / `alias` field naming the primitive).

    Step 4 — Outcomes:
    - SUCCESS — aliasing works: record the resolved aliasing shape in working memory for Task 2. Mark "Aliasing Path: NATIVE" in plan execution log.
    - FAILURE — aliasing rejected or resolves to literal-without-link: fall back per ARCHITECTURE. Write the semantic alias as a primitive-tier-style entry with the primitive's literal value AND set the `description` field (or whatever schema field carries metadata; capture from 23-01 schema) to `"alias-of: color-primitive-orange-500"`. Mark "Aliasing Path: DESCRIPTION-FALLBACK" in plan execution log. Add an OPEN-23-NN flag (category=token, severity=notable, blocker-for-phase=none) noting that aliasing is description-only and downstream readers must consult description for primitive provenance.

    Step 5 — Document the chosen path in PEN-INVENTORY.md by appending a short paragraph to the existing `## Tokens Written — Primitives` section's preamble (or, if it's clearer, create a small `## Aliasing Strategy` section before writing the full alias batch in Task 2). Make the choice explicit so Task 2 + plan 23-05 + downstream phases know which strategy is in effect.
  </action>
  <verify>
    # get_variables({}) returns color-semantic-bg-accent after probe
    # Resolved aliasing path (NATIVE or DESCRIPTION-FALLBACK) captured in working memory + PEN-INVENTORY.md
    grep -E 'Aliasing Path|Aliasing Strategy|alias-of' .planning/research/PEN-INVENTORY.md
  </verify>
  <acceptance_criteria>
    - Exactly one set_variables call was made in this task, writing one semantic alias.
    - get_variables({}) returns the probe semantic alias resolving to the expected primitive value (NATIVE) OR a description field naming the primitive (DESCRIPTION-FALLBACK).
    - The probe alias name matches regex `^color-semantic-[a-z0-9-]+$` (flat-dash).
    - The primitive being referenced exists in get_variables({}) (i.e., it was written by plan 23-03).
    - PEN-INVENTORY.md records the chosen aliasing path (NATIVE or DESCRIPTION-FALLBACK) so Task 2 + downstream phases can rely on it.
    - If DESCRIPTION-FALLBACK, an OPEN-23-NN flag with category=token, severity=notable, blocker-for-phase=none is recorded in `## Open Flags`.
  </acceptance_criteria>
  <done>Aliasing strategy resolved (NATIVE or DESCRIPTION-FALLBACK) and documented; probe semantic alias present in design/Crito.pen; full alias batch in Task 2 can proceed.</done>
</task>

<task type="auto">
  <name>Task 2: Full semantic alias batch — color / spacing / typography (incl. prose) / radii roles</name>
  <files>
    design/Crito.pen
    .planning/research/PEN-INVENTORY.md
  </files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-12 flat-dash, D-14 semantic-only consumption, D-15 prose from Blog/FAQ, D-16 inline-code likely OPEN with blocker-for-phase 28)
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## Architecture Patterns Pattern 2 Tier 2 semantic alias name list)
    - .planning/REQUIREMENTS.md (TOKEN-02..TOKEN-06 — semantic role coverage scope)
    - .planning/research/PEN-INVENTORY.md (## Tokens Written — Primitives for primitive name set; ## Frames for Blog/FAQ frame identification)
  </read_first>
  <action>
    Write the full semantic alias batch using the aliasing strategy resolved in Task 1. Every alias references a primitive from plan 23-03.

    Step 1 — Build the semantic alias list per RESEARCH Pattern 2 Tier 2, scoped to what primitives actually exist (do not invent semantic names that have no primitive backing):

    Color semantic aliases (TOKEN-02 roles):
    - color-semantic-bg-page → page background neutral primitive
    - color-semantic-bg-surface → elevated-surface neutral primitive (if audit returned a distinct shade)
    - color-semantic-bg-accent → brand-accent primitive (the probe alias from Task 1)
    - color-semantic-text-primary → darkest text neutral primitive
    - color-semantic-text-secondary → midtone text neutral primitive (if present in audit)
    - color-semantic-text-accent → brand-accent text-tier primitive
    - color-semantic-border-default → neutral border primitive

    Spacing semantic aliases (TOKEN-04 roles):
    - space-semantic-section-y → space-primitive-96 (or whichever primitive matches Crito's section vertical rhythm from audit)
    - space-semantic-container-x → space-primitive-24 (or audit-derived container horizontal padding)
    - space-semantic-stack-sm / -md / -lg → space-primitive-8 / -16 / -32 (audit-derived stack-spacing primitives)
    - space-semantic-inline-sm / -md / -lg → space-primitive-4 / -8 / -16 (audit-derived inline-spacing primitives)

    Typography semantic aliases (TOKEN-03 + TOKEN-06 roles):
    Display + heading + body group (TOKEN-03):
    - type-semantic-display
    - type-semantic-heading-1, type-semantic-heading-2, type-semantic-heading-3, type-semantic-heading-4, type-semantic-heading-5, type-semantic-heading-6
    - type-semantic-body, type-semantic-body-sm, type-semantic-caption, type-semantic-button
    Each typography semantic is a composite — references type-primitive-family-*, type-primitive-size-*, type-primitive-weight-*, type-primitive-lh-* per Pencil's typography variable composition (verify shape from 23-01 schema; if composite typography isn't natively supported, write the four sub-variables as separate semantic aliases per role, e.g., type-semantic-heading-1-family / -size / -weight / -lh).

    Prose group (TOKEN-06) — D-15 says derive from Crito Blog + FAQ frames only:
    - type-semantic-prose-paragraph (paragraph body style + line-height + paragraph spacing)
    - type-semantic-prose-link (color + underline behavior)
    - type-semantic-prose-list (list bullet style + indent)
    - type-semantic-prose-inline-code — D-16: if Crito Blog/FAQ frames do not depict inline code, do NOT invent it. Add OPEN-23-NN to `## Open Flags` with category=token, severity=notable, blocker-for-phase=28 (Blog reconstruction). Skip the alias write for this one entry.

    Radius semantic aliases (TOKEN-05 roles):
    - radius-semantic-card → radius-primitive-md (or audit-derived primitive for card-shaped frames)
    - radius-semantic-button → radius-primitive-sm or md (audit-derived)
    - radius-semantic-input → audit-derived radius primitive
    - radius-semantic-pill → radius-primitive-pill or large-radius primitive

    Step 2 — Drift check: every semantic alias must reference a primitive that already exists per `## Tokens Written — Primitives`. If a planned alias points to a missing primitive, either (a) add the missing primitive via a delta set_variables call (re-running plan 23-03's discipline — source evidence required) OR (b) make the semantic alias an OPEN flag with appropriate blocker-for-phase. NEVER invent a primitive in this plan.

    Step 3 — Call Pencil MCP set_variables with the full batch (one call if size permits; otherwise chunk by category — color batch, then spacing, then typography, then radius). Use the aliasing strategy from Task 1 (NATIVE reference syntax or DESCRIPTION-FALLBACK with `alias-of: ...` in description).

    Step 4 — Call Pencil MCP get_variables with empty filter {}. Verify:
    - Every planned semantic alias is present.
    - Every alias either references a primitive natively or carries `alias-of: <primitive-name>` in description.
    - Every name matches regex `^(color|space|type|radius)-semantic-[a-z0-9-]+$`.
    - Zero names contain `/`, `.`, `@light`, `@dark`, `-dark`, `-light`.

    Step 5 — Append `## Tokens Written — Semantic Aliases` section to PEN-INVENTORY.md. Markdown table with columns: `semantic_name | primitive_referenced | aliasing_path | source | source-detail`. `aliasing_path` is `NATIVE` or `DESCRIPTION-FALLBACK`. `source` is `search_all_unique_properties` / `Crito .fig` / `OPEN flag`. For prose tokens specifically, `source-detail` cites the Crito Blog or FAQ frame the styling was observed in (per D-15) — never Joel's v1.3 CSS.

    Step 6 — If any OPEN flags were raised (inline-code per D-16, missing-primitive aliases, etc.), ensure each has a complete entry in PEN-INVENTORY.md `## Open Flags` per D-10 schema.

    Forbidden in this task:
    - JPG eyedropping (PITFALLS F3) — every source is search_all_unique_properties / .fig / OPEN flag.
    - Any token name with `@light`, `@dark`, `-dark`, `-light` (D-01).
    - Inventing semantic aliases pointing to non-existent primitives.
    - Deriving prose styling from Joel's v1.3 CSS (D-15) — Crito Blog/FAQ frames only.
    - Modifying any existing Crito page frame's children (VAL-23-05 / Pitfall 5).
  </action>
  <verify>
    # get_variables({}) returns full semantic tier — verified in-task at Step 4

    grep -c '^## Tokens Written — Semantic Aliases' .planning/research/PEN-INVENTORY.md   # exactly 1
    grep -E '^\| color-semantic-|^\| space-semantic-|^\| type-semantic-|^\| radius-semantic-' .planning/research/PEN-INVENTORY.md | wc -l   # row count covers all four categories
    ! grep -E 'image-import-.*\.jpg' .planning/research/PEN-INVENTORY.md   # zero JPG eyedropping (cumulative file check)
    ! grep -E '@(light|dark)|-dark$|-light$' .planning/research/PEN-INVENTORY.md   # no per-theme suffixes
  </verify>
  <acceptance_criteria>
    - get_variables({}) returns semantic aliases covering all four categories: ≥1 color-semantic, ≥1 space-semantic, ≥1 type-semantic, ≥1 radius-semantic.
    - Every semantic alias name matches regex `^(color|space|type|radius)-semantic-[a-z0-9-]+$`.
    - Component-facing semantic role names from TOKEN-02..TOKEN-06 are all present OR have an OPEN flag explaining absence:
      - Color: bg-page, bg-surface, bg-accent, text-primary, text-accent, border-default (text-secondary optional based on audit)
      - Spacing: section-y, container-x, stack-{sm,md,lg}, inline-{sm,md,lg}
      - Typography: display, heading-1..heading-6, body, body-sm, caption, button
      - Prose (TOKEN-06): prose-paragraph, prose-link, prose-list (prose-inline-code is OPEN per D-16)
      - Radius: card, button, input, pill (subset acceptable if audit returned <4 unique radii)
    - Every semantic alias resolves to a primitive — either via NATIVE aliasing (verified by get_variables resolution) or via DESCRIPTION-FALLBACK (verified by `alias-of: ...` in description field).
    - PEN-INVENTORY.md `## Tokens Written — Semantic Aliases` table has one row per written alias with all 5 columns populated.
    - For prose semantic aliases: source-detail cites a Crito Blog or FAQ frame (D-15 enforcement).
    - For inline-code: if absent in Crito Blog/FAQ, an OPEN-23-NN flag with blocker-for-phase=28 is recorded (D-16).
    - Zero tokens have `@light`, `@dark`, `-dark`, `-light` (D-01).
    - No existing Crito page frame's children changed (VAL-23-05 / Pitfall 5 spot-check).
  </acceptance_criteria>
  <done>Semantic alias tier written into design/Crito.pen; full alias log appended to PEN-INVENTORY.md; aliasing strategy explicit; Phase 24+ component plans can reference semantic names directly.</done>
</task>

</tasks>

<verification>
  - get_variables({}) returns both primitive (from 23-03) and semantic (from 23-04) tiers.
  - Every semantic alias either natively references a primitive or carries `alias-of: <primitive>` in description.
  - PEN-INVENTORY.md has `## Tokens Written — Semantic Aliases` section with primitive-reference per alias.
  - Prose semantic aliases trace to Crito Blog/FAQ frames per D-15; inline-code is OPEN if not depicted per D-16.
  - design/Crito.pen page frames remain visually + structurally unchanged.
</verification>

<success_criteria>
  - VAL-23-02 fully satisfied: two-tier surface present, flat-dash names, no theme suffixes, semantic aliases resolve to primitives.
  - VAL-23-04 fully satisfied: every token (primitive + semantic) has source-evidence in PEN-INVENTORY.md.
  - TOKEN-01 (two-tier system with components referencing semantic only), TOKEN-02 (color roles), TOKEN-03 (type roles), TOKEN-04 (spacing roles), TOKEN-05 (radius roles), TOKEN-06 (prose roles) — fully satisfied.
  - Plan 23-05 has the semantic-alias name set it needs to render swatches/specimens/stripes in the reference frame.
</success_criteria>

<output>
After completion, create `.planning/phases/23-audit-token-foundation/23-04-SUMMARY.md` with:
- Aliasing strategy chosen (NATIVE or DESCRIPTION-FALLBACK)
- Semantic alias counts per category (color / type / space / radius / prose)
- Prose token completeness (which of paragraph / link / list / inline-code shipped vs OPEN)
- OPEN flags raised in this plan (count + IDs + blocker-for-phase distribution)
- Files modified
</output>
