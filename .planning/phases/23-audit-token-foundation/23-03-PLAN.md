---
phase: 23-audit-token-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-02
  - TOKEN-03
  - TOKEN-04
  - TOKEN-05
gap_closure: false

must_haves:
  truths:
    - "Calling get_variables({}) returns primitive tokens covering color, typography (sizes/weights/families/line-heights), spacing, and radii — every name flat-dash convention (D-12)"
    - "Every primitive token value traces to source evidence — search_all_unique_properties / Crito .fig / OPEN flag (PITFALLS F3: zero JPG eyedropping)"
    - "Primitive count is ≤ ~2x the unique-property count from the 23-01 audit (drift sanity check per RESEARCH Pitfall 1)"
    - "Zero tokens carry @light, @dark, or any per-theme suffix (D-01)"
  artifacts:
    - path: "design/Crito.pen (variable surface only)"
      provides: "Primitive token tier — raw values written via set_variables"
      contains: "color-primitive-*, space-primitive-*, type-primitive-*, radius-primitive-*"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "## Tokens Written — Primitives log with source-evidence per token"
      contains: "## Tokens Written — Primitives"
  key_links:
    - from: "## Tokens Written — Primitives log"
      to: "VAL-23-04 source-evidence traceability"
      via: "source: field on every token entry"
      pattern: "source: (search_all_unique_properties|Crito \\.fig|OPEN flag)"
    - from: "primitive token names"
      to: "semantic aliases in plan 23-04"
      via: "23-04 set_variables references primitive names exactly as written here"
      pattern: "color-primitive-|space-primitive-|type-primitive-|radius-primitive-"
---

<objective>
Write the primitive token tier (raw values for color, typography, spacing, radii) into design/Crito.pen via Pencil MCP set_variables. Use the probe-first pattern from RESEARCH ## Code Examples: write ONE token first, verify via get_variables({}), then commit the full batch. Log every written token with its source-evidence in PEN-INVENTORY.md.

Purpose: Establish the lower tier of the two-tier token system (TOKEN-01). Without primitives, semantic aliases in 23-04 have nothing to reference. Source-evidence discipline (VAL-23-04) is what prevents the v1.4 failure mode of inventing values to fill a sparse audit.

Output: Primitive tokens present in design/Crito.pen (verifiable via get_variables); `## Tokens Written — Primitives` log appended to PEN-INVENTORY.md with source field on every entry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-audit-token-foundation/23-CONTEXT.md
@.planning/phases/23-audit-token-foundation/23-RESEARCH.md
@.planning/phases/23-audit-token-foundation/23-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/23-audit-token-foundation/23-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe-first set_variables — write ONE primitive token to resolve argument shape</name>
  <files>design/Crito.pen</files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## Common Pitfalls 6 — set_variables argument-shape mismatch; ## Code Examples — Token-write sequence)
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-12 flat-dash naming, D-13 REQUIREMENTS.md override)
    - .planning/research/PEN-INVENTORY.md (## Audit Findings — Unique Property Values for the probe value source)
  </read_first>
  <action>
    Probe Pencil's set_variables argument shape with one token before writing the full primitive set. This catches schema mismatches early (RESEARCH Pitfall 6: STACK.md's shape `'color.bg=color:#FFFFFF@light,#0F172A@dark'` predates D-12 flat-dash naming and may not match the live schema).

    Precondition check: PEN-INVENTORY.md `## Coverage Checkpoint` verdict must be PASS (or PAUSE with user-direction "proceed with current coverage" / "proceed after fig consult"). If neither, halt — plan 23-01 Task 3 hasn't unblocked this plan.

    Step 1: Pick one anchor color from PEN-INVENTORY.md `## Audit Findings — Unique Property Values` — the highest-occurrence color is ideal. Call it ANCHOR_COLOR_VALUE. Derive a flat-dash primitive name following D-12: `color-primitive-<role>-<step>` where role is inferred from the color's hue family (e.g., orange, navy, neutral) and step is the closest tailwind-style step (50/100/200/.../900). Example: `color-primitive-orange-500` with value `#F26B22`.

    Step 2: Call Pencil MCP set_variables. Argument shape candidate from RESEARCH ## Code Examples: `{ variables: { "<name>": "<value>" }, replace: false }`. If the schema captured in 23-01 Task 1 Step 2 named different keys (e.g., `tokens` instead of `variables`, or required a `type` discriminator), use those instead — schema from 23-01 is authoritative.

    Step 3: Call Pencil MCP get_variables with empty filter {}. Verify the returned set contains the new token name exactly as written, and the value resolves correctly.

    Step 4 — Outcomes:
    - SUCCESS (token name + value appear correctly): record the resolved argument shape in working memory for Task 2 + plan 23-04 to use verbatim. Proceed to Task 2.
    - FAILURE — token absent or malformed: do NOT proceed with full batch. Log the failure mode to PEN-INVENTORY.md `## Open Flags` as `OPEN-23-{NN}` with category=token, severity=critical, blocker-for-phase=23 (this halts the phase). Try ONE adjusted argument shape (e.g., flip key name, add type discriminator) if a candidate is obvious from the schema. If second probe also fails, halt the plan and surface to user.

    No JPG eyedropping (PITFALLS F3). The probe value must come from `search_all_unique_properties` output (primary, per CONTEXT D-03) or from the Crito .fig at design/images/Consulting...Crito (Community).fig (fallback, per D-04) — never from any `image-import-*.jpg` raster.
  </action>
  <verify>
    # After successful probe, get_variables({}) returns the probe token
    # Tool-call sequence is captured in plan execution log; ANCHOR_COLOR_VALUE source documented
  </verify>
  <acceptance_criteria>
    - PEN-INVENTORY.md `## Coverage Checkpoint` verdict is PASS or PAUSE-with-user-direction before any set_variables call is made.
    - Exactly one set_variables call was made in this task with exactly one token in the variables map.
    - After the call, get_variables({}) returns the probe token with the expected flat-dash name and the value from search_all_unique_properties (or .fig fallback).
    - The probe token name matches regex `^color-primitive-[a-z]+-[0-9]+$`.
    - If the probe failed twice, plan execution halted and an OPEN-23-NN flag (category=token, severity=critical, blocker-for-phase=23) was added to PEN-INVENTORY.md.
    - The probe value cited a source from { search_all_unique_properties, Crito .fig }; never an `image-import-*.jpg`.
  </acceptance_criteria>
  <done>Argument shape for set_variables resolved; resolved shape captured in working memory for Task 2; probe token present in design/Crito.pen variables surface.</done>
</task>

<task type="auto">
  <name>Task 2: Full primitive batch — write color / typography / spacing / radii primitives via set_variables</name>
  <files>
    design/Crito.pen
    .planning/research/PEN-INVENTORY.md
  </files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-12 flat-dash, D-03 primary source, D-04 fallback source, "Claude's Discretion" — ramp depth driven by audit returns)
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## Architecture Patterns Pattern 2 — Two-tier token system, ## Common Pitfalls 1 — inventing values)
    - .planning/REQUIREMENTS.md (TOKEN-02..TOKEN-05 — color, typography, spacing, radii scope)
    - .planning/research/PEN-INVENTORY.md (## Audit Findings — every primitive value must trace here or to .fig)
  </read_first>
  <action>
    Write the full primitive token set using the argument shape resolved in Task 1. Build the token list from PEN-INVENTORY.md audit findings — do not invent intermediate ramp steps.

    Step 1 — Build the primitive list. For each category, gather names and values:

    Color primitives — for each unique color value in `## Audit Findings — Unique Property Values`, name it `color-primitive-<role>-<step>` per D-12. Role = hue family (orange/navy/neutral/etc.). Step = tailwind-style numeric (50/100/.../900) chosen by relative lightness within that hue family. If the audit returned only 2 oranges, the orange ramp has 2 entries — no inventing intermediates (RESEARCH Pitfall 1 + Claude's Discretion). For values not in the audit but present in the Crito .fig Variables panel (D-04 fallback), include them with `source: Crito .fig` in the log.

    Spacing primitives — for each unique padding/gap value, name it `space-primitive-<px-value>` per RESEARCH Pattern 2 (e.g., `space-primitive-24` for 24px).

    Typography primitives — split across four primitive families per RESEARCH Pattern 2:
    - `type-primitive-size-<px>` (e.g., type-primitive-size-24)
    - `type-primitive-weight-<number>` (e.g., type-primitive-weight-600)
    - `type-primitive-family-<role>` (e.g., type-primitive-family-display, type-primitive-family-body)
    - `type-primitive-lh-<role>` (e.g., type-primitive-lh-tight, type-primitive-lh-normal)
    Cover at minimum the size + weight + family combinations needed by TOKEN-03 (display through caption + button).

    Radius primitives — for each unique radius value, name it `radius-primitive-<role-or-px>` (e.g., radius-primitive-md, radius-primitive-8). Likely 2-4 entries per TOKEN-05.

    Step 2 — Drift sanity check (RESEARCH Pitfall 1): count primitives planned vs unique-property count from audit. If primitives > ~2x audit unique values, stop and review — the executor is inventing values. Cull invented entries; flag missing source coverage as OPEN flags instead.

    Step 3 — Call Pencil MCP set_variables ONCE with all primitives in one batch (argument shape from Task 1). RESEARCH Pattern 2 single bulk call is preferred; ARCHITECTURE notes risk of partial-state-on-failure is acceptable here because get_variables verify in Step 4 catches it.

    Step 4 — Call Pencil MCP get_variables with empty filter {}. Verify:
    - Every primitive from Step 1 is present in the response.
    - Every name matches regex `^(color|space|type|radius)-primitive-[a-z0-9-]+$`.
    - Zero names contain `/`, `.`, `@light`, `@dark`, `-dark`, `-light`.
    - Total primitive count is ≤ ~2x unique-property count from 23-01 audit.

    Step 5 — Append `## Tokens Written — Primitives` section to PEN-INVENTORY.md. Use a markdown table with columns: `name | value | source | source-detail`. One row per primitive. `source` is one of `search_all_unique_properties` / `Crito .fig` / `OPEN flag`. `source-detail` cites the specific frame or fig variable (e.g., "appeared in Homepage hero CTA fill, 4 nodes" or "Crito .fig Variables panel > Primitives > Orange/500"). If any primitive is an OPEN flag, also append the OPEN entry to `## Open Flags` per D-10 schema.

    NEVER cite `design/images/image-import-*.jpg` as a source (PITFALLS F3).

    NEVER include `@light,@dark` in any token name or value (D-01).
  </action>
  <verify>
    # Pencil-side
    # get_variables({}) returns all written primitives — verified in-task at Step 4

    # Repo-side
    grep -c '^## Tokens Written — Primitives' .planning/research/PEN-INVENTORY.md   # exactly 1
    grep -E '^\| color-primitive-|^\| space-primitive-|^\| type-primitive-|^\| radius-primitive-' .planning/research/PEN-INVENTORY.md | wc -l   # row count > 0
    ! grep -E 'image-import-.*\.jpg' .planning/research/PEN-INVENTORY.md   # zero JPG eyedropping
    ! grep -E '@(light|dark)' .planning/research/PEN-INVENTORY.md   # no per-theme suffix
  </verify>
  <acceptance_criteria>
    - get_variables({}) returns at least one token per category {color, space, type, radius} with `-primitive-` in the name.
    - Every primitive token name matches regex `^(color|space|type|radius)-primitive-[a-z0-9-]+$` (flat-dash; D-12).
    - Zero tokens contain `@light`, `@dark`, `-dark`, `-light`, `/`, `.` in their name.
    - Primitive count is ≤ ~2x the unique-property count documented in PEN-INVENTORY.md `## Audit Findings` (drift sanity check; RESEARCH Pitfall 1).
    - PEN-INVENTORY.md contains `## Tokens Written — Primitives` section with a row per written primitive.
    - Every row has a `source` field whose value is one of `search_all_unique_properties`, `Crito .fig`, or `OPEN flag`.
    - Zero rows cite `image-import-*.jpg` or any raster source (PITFALLS F3).
    - For TOKEN-03 typography coverage: at minimum, primitives exist for sizes covering display (largest), h1-h6 range, body, body-sm, caption, button. Gaps relative to this list are OPEN flags, not omissions.
    - For TOKEN-04 spacing coverage: primitives cover Crito's grid pattern (the audit determines the actual values; e.g. 0/4/8/12/16/24/32/48/64/96 if observed — never invented).
    - For TOKEN-05 radius coverage: 2-4 radius primitives present (per TOKEN-05 expected scope).
    - No existing Crito page frame's child node-id set changed (verified by spot-check batch_get against pre-write baseline; Pitfall 5 / VAL-23-05).
  </acceptance_criteria>
  <done>Primitive token tier written into design/Crito.pen; full primitive log appended to PEN-INVENTORY.md with source-evidence; ready for plan 23-04 to write semantic aliases on top.</done>
</task>

</tasks>

<verification>
  - get_variables({}) returns primitive tokens covering all four categories.
  - Every primitive name flat-dash; no theme suffix; no path syntax.
  - Primitive count ≤ ~2x unique-property count (no inventing).
  - PEN-INVENTORY.md `## Tokens Written — Primitives` log has source-evidence per token.
  - design/Crito.pen page frames are visually + structurally unchanged (Pitfall 5 / VAL-23-05).
</verification>

<success_criteria>
  - VAL-23-02 partial: primitive tier present with flat-dash names and zero theme suffixes.
  - VAL-23-04 partial: every primitive token has a `source` field in the inventory log.
  - TOKEN-01 (primitive tier of two-tier system), TOKEN-02 (color), TOKEN-03 (typography), TOKEN-04 (spacing), TOKEN-05 (radii) — primitive-tier portion satisfied.
  - Plan 23-04 has the primitive name set it needs for semantic alias references.
</success_criteria>

<output>
After completion, create `.planning/phases/23-audit-token-foundation/23-03-SUMMARY.md` with:
- Probe outcome (success / required-adjustment)
- Final resolved set_variables argument shape (for use by plan 23-04)
- Primitive token counts per category (color / type / space / radius)
- Source breakdown (count by `source` field: search_all_unique_properties / .fig / OPEN flag)
- OPEN flags raised (count + IDs)
- Drift check result (primitive count vs ~2x audit unique-property count)
- Files modified
</output>
