---
phase: 23-audit-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/PEN-INVENTORY.md
  - .planning/research/exports/v2.0/baseline-23/
autonomous: false
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
gap_closure: false

must_haves:
  truths:
    - "Reader of PEN-INVENTORY.md can identify every top-level Crito frame's name, IN-SCOPE/OUT-OF-SCOPE classification, per-section status markers, and reconstruction priority"
    - "Every top-level frame is classified against Joel's page set (Homepage, Projects, Blog, FAQ, Contact, Thank-you, Design system, 404) or token-mining-only or out-of-scope"
    - "Coverage checkpoint section explicitly documents PASS or PAUSE verdict before downstream token plans run (per CONTEXT D-05)"
    - "Baseline screenshots of every Crito top-level frame exist on disk for VAL-23-05 zero-mutation diff at end of phase"
  artifacts:
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Versioned audit inventory with per-frame schema + coverage checkpoint + open flags"
      contains: "## Frames, ## Coverage Checkpoint, ## Open Flags, ## Audit Findings"
    - path: ".planning/research/exports/v2.0/baseline-23/"
      provides: "Baseline screenshots of every Crito top-level frame (pre-phase visual state)"
      contains: "One PNG per top-level frame (~15 files)"
  key_links:
    - from: ".planning/research/PEN-INVENTORY.md"
      to: "downstream phases 24-32"
      via: "frame_id / scope / open_flag_ids fields are greppable plain markdown"
      pattern: "frame_id:|scope:|open_flag_ids:"
    - from: "## Coverage Checkpoint section"
      to: "plans 23-03 / 23-04"
      via: "explicit PASS verdict OR user-approved PAUSE proceed gates token writes"
      pattern: "Verdict: (PASS|PAUSE)"
---

<objective>
Run a read-only Pencil MCP audit of `design/Crito.pen` and produce `.planning/research/PEN-INVENTORY.md` — the versioned inventory every later v2.0 phase plans against. Capture baseline screenshots of every Crito top-level frame for end-of-phase zero-mutation verification. End with an explicit coverage checkpoint that either PASSES or PAUSES the phase before any token-writing plans run.

Purpose: Resolve the v2.0 hard block (T2 — audit-as-precondition). Replace the v1.4 failure mode (best-guesses from flat-image inputs) with observed-source discipline. The inventory becomes the contract every later phase reads.

Output: PEN-INVENTORY.md committed to repo; baseline screenshot set on disk; coverage-checkpoint verdict logged.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-audit-token-foundation/23-CONTEXT.md
@.planning/phases/23-audit-token-foundation/23-RESEARCH.md
@.planning/phases/23-audit-token-foundation/23-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/milestones/v1.4-research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Capture baseline screenshots of every Crito top-level frame</name>
  <files>.planning/research/exports/v2.0/baseline-23/</files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-19 archive path convention)
    - .planning/phases/23-audit-token-foundation/23-VALIDATION.md (VAL-23-05 zero-mutation requirement)
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (audit sequence section)
  </read_first>
  <action>
    Wave 0 prerequisite for VAL-23-05. Before any other audit calls, capture a visual baseline of every top-level Crito frame so end-of-phase verification can diff against it.

    Step 1: mkdir -p .planning/research/exports/v2.0/baseline-23/

    Step 2: Call Pencil MCP get_editor_state with argument include_schema=true. Capture: active file (must be design/Crito.pen) and JSON schema for nodes + variables. If active file is NOT design/Crito.pen, halt and surface to user — do not proceed.

    Step 3: Call Pencil MCP batch_get to enumerate top-level frames (use whatever query shape the schema from Step 2 indicates — likely a top-level / root-children query). Capture every frame's id, name, and dimensions.

    Step 4: For each top-level frame, call Pencil MCP get_screenshot with nodeId set to that frame's id. Write the returned PNG to .planning/research/exports/v2.0/baseline-23/{slugified-frame-name}.png. Slugify by lowercasing, replacing spaces with dashes, stripping punctuation.

    Step 5: Verify file count on disk matches top-level frame count from Step 3. If counts diverge, log the missing/extra frames and halt.

    Use ONLY these Pencil MCP tools in this task: get_editor_state, batch_get, get_screenshot. Do NOT call set_variables, batch_design, or any mutation tool — this task is strictly read-only (Pitfall 5 enforcement).
  </action>
  <verify>
    ls .planning/research/exports/v2.0/baseline-23/*.png | wc -l   # must equal top-level frame count (≈15 per v1.4 ARCHITECTURE)
    test -d .planning/research/exports/v2.0/baseline-23/           # directory exists
  </verify>
  <acceptance_criteria>
    - Directory .planning/research/exports/v2.0/baseline-23/ exists.
    - File count in baseline-23/ equals the integer count of top-level frames returned by batch_get in Step 3.
    - Every PNG file is ≥1KB (not empty).
    - The first call of the Pencil session is get_editor_state({ include_schema: true }) per RESEARCH Pattern 1 + Pitfall 5.
    - No Pencil mutation tool (set_variables, batch_design, update_node, etc.) was invoked in this task.
  </acceptance_criteria>
  <done>Baseline screenshot set archived; top-level frame id+name+dimension list captured in working memory for use by Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Live Pencil MCP audit — guidelines, per-frame catalogue, unique properties, current variables</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-06 page set, D-07 token-mining-only frames, D-08 joel-only-no-crito-ref, D-17 per-frame schema, D-18 plain markdown)
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## Code Examples — Audit sequence, ## Architecture Patterns — Pattern 3 coverage checkpoint, ## Common Pitfalls 3 + 5 + 7)
    - .planning/research/SUMMARY.md (T2 audit-as-hard-block, T4 Joel page set ≠ Crito page set)
    - .planning/milestones/v1.4-research/ARCHITECTURE.md (last successful inventory — 15 frames, zero factored)
  </read_first>
  <action>
    Build the audit deliverable by executing the read-only Pencil MCP audit sequence from RESEARCH ## Code Examples and writing findings to .planning/research/PEN-INVENTORY.md per CONTEXT D-17 schema.

    Step 1 — Guidelines: Call Pencil MCP get_guidelines with topic="design-system". Capture verbatim. Then call get_guidelines with topic="landing-page". Capture verbatim. Both outputs go into PEN-INVENTORY.md `## Pencil Guidelines (verbatim)` section.

    Step 2 — Per-frame child catalogue: Using the frame id list captured in Task 1, call Pencil MCP batch_get with a queries array containing one id-query per top-level frame (single batched call when feasible; RESEARCH notes ≤25 ops typical batch). Capture each frame's children with names. For each child, classify status by inspecting node type: type=image OR type=raster-fill node → [FLAT]; mix of image + editable text/shape → partial; only editable nodes (text, shape, frame children) → factored.

    Step 3 — Enumerate unique raw values: Call Pencil MCP search_all_unique_properties (use whatever argument shape the schema from Task 1 Step 2 indicates — output shape may be { colors: [...], fonts: [...], sizes: [...] } or a flat property/value list per RESEARCH Open Question 3). Capture every distinct fill color, font family, font size, weight, line-height, radius, padding, gap value. This is the coverage-checkpoint input.

    Step 4 — Current variables: Call Pencil MCP get_variables with empty filter {}. Capture any pre-existing variables (likely none per v1.4 research, but Figma import may have carried some across).

    Step 5 — Write PEN-INVENTORY.md. The file MUST contain these top-level sections in this order:

    `# PEN-INVENTORY.md — design/Crito.pen Audit (Phase 23)`
    `## Schema Snapshot` — abbreviated schema names + node-type vocabulary captured from Task 1 Step 2 (one paragraph; full schema not pasted — that's available via re-call).
    `## Pencil Guidelines (verbatim)` — design-system topic + landing-page topic, each in its own subsection.
    `## Frames` — per CONTEXT D-17 schema. Decision per D-17 + RESEARCH Pitfall 3: at ≈15 frames, prefer one combined markdown table with columns frame_name | frame_id | scope | joel_page_map | child_section_count | status_counts | reconstruction_priority | open_flag_ids. One row per top-level frame. Every row populated — zero `TBD` placeholders.
    `## Frame Classification Rules Applied` — short note documenting how each scope value was assigned: IN-SCOPE → frame maps to a Joel page in {Homepage, Projects, Blog, FAQ, Contact, Thank-you, Design-system, 404}; IN-SCOPE token-mining-only → Crito About or Service standalone frames per D-07; joel-only-no-crito-ref → Joel page with no matching Crito frame (Design-system and 404 per D-08); OUT-OF-SCOPE → Crito-only frames (View More, Information, Free Design Sample, Team etc.).
    `## Audit Findings — Unique Property Values` — the search_all_unique_properties output. Subsections for colors / font families / font sizes / weights / line-heights / radii / padding-gap values. Each value listed with the count of nodes using it if the tool surfaces that detail.
    `## Audit Findings — Current Variable Surface` — output of get_variables({}). If empty, state "No pre-existing variables in design/Crito.pen at audit time." If non-empty, list names + values verbatim and flag any naming-convention conflicts with D-12 flat-dash plan.
    `## Open Flags` — per CONTEXT D-10 schema (id, category, severity, description, blocker-for-phase). Empty list is acceptable at this point.

    Use ONLY these Pencil MCP tools in this task: get_guidelines, batch_get, search_all_unique_properties, get_variables. Do NOT call set_variables or batch_design — read-only enforced (Pitfall 5).

    Critical: Frames classified IN-SCOPE token-mining-only get joel_page_map = "none (token mining only)" and reconstruction_priority = "n/a (out of scope)" — per Pitfall 7. Do not assign reconstruction priority to About/Service standalone frames.
  </action>
  <verify>
    test -f .planning/research/PEN-INVENTORY.md
    grep -c '^## ' .planning/research/PEN-INVENTORY.md   # at least 7 (Schema / Guidelines / Frames / Classification Rules / Findings-Values / Findings-Variables / Open Flags)
    grep -E '^\| (Homepage|Projects|Blog|FAQ|Contact|Thank|Design|404)' .planning/research/PEN-INVENTORY.md | wc -l   # IN-SCOPE rows present
    grep -E 'frame_id|scope:|joel_page_map' .planning/research/PEN-INVENTORY.md   # D-17 schema fields appear
    ! grep -E '\bTBD\b|\?\?\?' .planning/research/PEN-INVENTORY.md   # zero placeholders
  </verify>
  <acceptance_criteria>
    - .planning/research/PEN-INVENTORY.md exists.
    - File contains all 7 required top-level sections in the order specified above.
    - Frames table has one row per top-level frame returned by Task 1 batch_get; row count equals baseline-23/ PNG count.
    - Every row has all 8 D-17 fields populated; zero `TBD` / `???` / blank cells.
    - Every `scope` value is exactly one of: `IN-SCOPE` / `IN-SCOPE token-mining-only` / `OUT-OF-SCOPE` / `joel-only-no-crito-ref`.
    - Every IN-SCOPE row has child_section_count > 0 and status_counts {flat:N, partial:N, factored:N} that sums to child_section_count.
    - About / Service standalone Crito frames (if present) are classified `IN-SCOPE token-mining-only` with `joel_page_map: none (token mining only)` and `reconstruction_priority: n/a (out of scope)` — Pitfall 7 enforcement.
    - File is plain markdown — zero JSON blocks (D-18 enforcement).
    - No Pencil mutation tool was invoked.
  </acceptance_criteria>
  <done>PEN-INVENTORY.md has full inventory body, classification rules, audit findings, and empty Open Flags section ready for Task 3 to add the coverage checkpoint.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Coverage checkpoint — write verdict, surface to user if PAUSE (CONTEXT D-05 gate)</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - .planning/phases/23-audit-token-foundation/23-CONTEXT.md (D-05 coverage checkpoint thresholds + pause action)
    - .planning/phases/23-audit-token-foundation/23-RESEARCH.md (## Architecture Patterns — Pattern 3 coverage checkpoint format)
    - .planning/research/PEN-INVENTORY.md (the audit findings written in Task 2)
  </read_first>
  <action>
    Append the `## Coverage Checkpoint (23-01 end)` section to PEN-INVENTORY.md and surface the verdict to the user. This is the disciplinary gate that prevents v1.4's failure mode (inventing values to fill sparse audits) from repeating.

    Step 1: From the `## Audit Findings — Unique Property Values` section in PEN-INVENTORY.md, count: unique color values (N_colors), unique font sizes (N_sizes), unique font families (N_fonts). Inspect padding/gap values for an obvious multiplicative pattern (multiples of 4 or 8 = visible scale; scattered with no pattern = WEAK or NO).

    Step 2: Apply D-05 thresholds: PASS if (N_colors ≥ 5) AND (N_sizes ≥ 3) AND (spacing pattern visible). Otherwise PAUSE.

    Step 3: Append to PEN-INVENTORY.md (matching RESEARCH Pattern 3 format verbatim):

    `## Coverage Checkpoint (23-01 end)`
    `**Unique color values found via search_all_unique_properties:** N_colors`
    `**Unique font sizes found:** N_sizes`
    `**Unique font families found:** N_fonts`
    `**Spacing scale pattern visible:** YES (multiples of 4/8) / NO / WEAK`
    blank line
    `**Verdict:** PASS` (or PAUSE)
    blank line
    If PAUSE: `**Findings to surface to user before 23-03/23-04 begin:**` followed by a bulleted list with specific numbers + frames where coverage is thin + recommended fallback path (open the Crito .fig at `design/images/Consulting & Agency Website Template I Crito (Community).fig` per D-04, NOT the Alliatus .fig).
    If PASS: `**Action:** Proceed to plan 23-02 / 23-03 / 23-04 / 23-05.`

    Step 4 (checkpoint behavior):
    - If verdict is PASS: present the verdict summary to the user as a confirmation banner (1-3 lines: counts + PASS). Resume signal: any non-objection (user types "approved" or "continue" or remains silent through next prompt) → unblock downstream plans.
    - If verdict is PAUSE: HALT this plan. Present the bulleted findings to the user verbatim. Recommend opening the Crito .fig for fallback per D-04, OR adding OPEN flags + proceeding with thin coverage. Wait for explicit user direction. Resume signal: user types "proceed with current coverage" / "proceed after fig consult" / "abandon phase" — and the corresponding action is taken before any downstream plan runs.
  </action>
  <verify>
    grep -A 10 '^## Coverage Checkpoint' .planning/research/PEN-INVENTORY.md   # section present with all required fields
    grep -E '^\*\*Verdict:\*\* (PASS|PAUSE)' .planning/research/PEN-INVENTORY.md   # verdict line present
  </verify>
  <acceptance_criteria>
    - `## Coverage Checkpoint (23-01 end)` section is appended to PEN-INVENTORY.md.
    - Section contains all four count/pattern fields (color count, font size count, font family count, spacing pattern verdict).
    - `**Verdict:**` line is present and equals exactly `PASS` or `PAUSE`.
    - If PAUSE, the "Findings to surface to user" bullet list is present AND the user has been shown the findings AND the user has given an explicit direction (recorded in working memory and in a one-line note appended to the coverage section).
    - If PASS, the "Action: Proceed to plan 23-02..." line is present.
    - Plans 23-03, 23-04, 23-05 do NOT begin until user has confirmed verdict resolution (PASS-approved or PAUSE-direction-given).
  </acceptance_criteria>
  <what-built>Coverage checkpoint section in PEN-INVENTORY.md with PASS/PAUSE verdict computed from audit findings against CONTEXT D-05 thresholds. If PASS: brief confirmation surfaced to user. If PAUSE: full findings surfaced with fallback recommendations.</what-built>
  <how-to-verify>
    1. Read the appended `## Coverage Checkpoint` section verbatim — counts + verdict.
    2. If verdict is PASS: confirm the audit found sufficient unique colors / sizes / spacing pattern before authorizing plans 23-03+ to proceed. Type "approved" or "continue".
    3. If verdict is PAUSE: review the bulleted findings and decide:
       - "proceed with current coverage" → tokens written from thin findings, gaps become OPEN flags
       - "proceed after fig consult" → executor opens Crito .fig at design/images/Consulting...Crito (Community).fig (NOT the Alliatus .fig) to fill gaps before 23-03 begins
       - "abandon phase" → halt v2.0 milestone, surface that the audit revealed a deeper problem
    4. Reply with the chosen direction. The executor records your choice and unblocks (or halts) downstream plans accordingly.
  </how-to-verify>
  <resume-signal>Type "approved" / "continue" (PASS path) OR "proceed with current coverage" / "proceed after fig consult" / "abandon phase" (PAUSE path)</resume-signal>
  <done>Coverage Checkpoint section appended to PEN-INVENTORY.md with explicit verdict; user direction recorded if PAUSE; downstream plans authorized to proceed (or phase halted).</done>
</task>

</tasks>

<verification>
  - .planning/research/PEN-INVENTORY.md exists and contains all required sections: Schema Snapshot, Pencil Guidelines, Frames, Frame Classification Rules Applied, Audit Findings — Unique Property Values, Audit Findings — Current Variable Surface, Open Flags, Coverage Checkpoint.
  - .planning/research/exports/v2.0/baseline-23/ exists with one PNG per Crito top-level frame.
  - Frame count in inventory table equals baseline-23/ PNG count equals Task 1 batch_get top-level frame count.
  - Every Frames-table row has all 8 D-17 fields populated; zero placeholders.
  - About/Service standalone frames (if present) are correctly classified `IN-SCOPE token-mining-only` (Pitfall 7).
  - Coverage Checkpoint section has explicit PASS/PAUSE verdict.
  - If PAUSE, user direction is recorded before any downstream plan starts.
  - No mutation to any existing Crito frame (verified by file modification check on design/Crito.pen showing no node-tree changes — only the read-only tool calls were made).
</verification>

<success_criteria>
  - VAL-23-01 conditions all PASS (PEN-INVENTORY.md schema completeness — see 23-VALIDATION.md).
  - Baseline screenshot set archived for VAL-23-05 end-of-phase zero-mutation diff.
  - Coverage checkpoint verdict logged and user-confirmed; plans 23-02 / 23-03 / 23-04 / 23-05 are unblocked OR phase is paused per user direction.
  - Plan 23-01 SUMMARY.md captures: top-level frame count, classification distribution (IN-SCOPE / token-mining-only / OUT-OF-SCOPE / joel-only-no-crito-ref counts), unique property counts, verdict, open-flag count.
</success_criteria>

<output>
After completion, create `.planning/phases/23-audit-token-foundation/23-01-SUMMARY.md` with:
- Top-level frame count
- Classification distribution (counts per scope value)
- Unique property counts (colors, sizes, families)
- Spacing pattern verdict
- Coverage checkpoint verdict (PASS or PAUSE — and user direction if PAUSE)
- Open flags raised (count + IDs)
- Files created/modified
- Pencil MCP tools invoked
</output>
