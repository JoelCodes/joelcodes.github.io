---
phase: 36-content-components-expandable-cards
plan: 06
type: execute
wave: 3
depends_on: ["36-03", "36-04", "36-05"]
files_modified:
  - src/pages/dev/content-components.astro
  - tests/accessibility/content-components.spec.ts
autonomous: false
requirements: [COMP-03, COMP-04, COMP-05]

threat_model:
  summary: >
    Static Astro site; this plan builds a DEV-only isolation page, an axe/keyboard a11y
    spec, a screenshot fidelity gate, and a cleanup commit. No runtime, no user input,
    no new dependencies. The one security-relevant surface is deployment scope: the
    isolation page must never ship to production.
  surfaces:
    - id: dev-page-leak
      note: >
        src/pages/dev/content-components.astro renders placeholder project data (D-09) and
        raw component variants. It MUST be DEV-gated (import.meta.env.PROD → Astro.redirect('/'))
        AND deleted from the working tree in the final commit. The deletion gate is the primary
        control: `npm run build && grep -r "content-components" dist/` returns zero. If the page
        (or its axe spec) survives to prod, unfinished placeholder content and a dev-only route
        would be publicly reachable.
    - id: stale-test-404
      note: >
        content-components.spec.ts targets /dev/content-components. If the spec is left in the tree
        after the page is deleted, CI axe runs 404 and fail. The spec and page are deleted together
        in the same final commit.
    - id: supply-chain
      note: "Zero new npm dependencies — @playwright/test and @axe-core/playwright already installed (existing a11y specs). package.json unchanged."
  asvs_l1: "PASS — static build-time output, no user input, dev artifacts gated then deleted."

must_haves:
  truths:
    - "npm run test:a11y passes with zero axe violations on /dev/content-components in BOTH light and dark modes (ROADMAP SC-1, SC-2)."
    - "Keyboard-only operation of the FAQItem group is verified: Tab reaches each summary, Space/Enter toggles, opening one item closes the others (exclusive-open), no mouse (ROADMAP SC-2)."
    - "node scripts/check-contrast.mjs passes with the Phase 36 pairs added in 36-03."
    - "The rendered ProjectCard (closed + expanded) and FAQItem match Figma 36:5 / 12:3 within tolerance and Joel approves the fidelity gate (ROADMAP SC-5)."
    - "After the final cleanup commit, npm run build && grep -r 'content-components' dist/ returns zero — the isolation page never ships to prod."
    - "grep for old neobrutalist tokens (bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow) in the three new wl components returns zero."
  artifacts:
    - path: "src/pages/dev/content-components.astro"
      provides: "DEV-gated isolation page rendering all three Phase 36 components in Figma 36:5 order (TEMPORARY — deleted in final commit)"
      contains: "import.meta.env.PROD"
    - path: "tests/accessibility/content-components.spec.ts"
      provides: "Playwright + axe spec, light + dark, WCAG 2.2 AA tags (TEMPORARY — deleted in final commit)"
      contains: "wcag22aa"
  key_links:
    - from: "tests/accessibility/content-components.spec.ts"
      to: "/dev/content-components"
      via: "page.goto"
      pattern: "goto\\('/dev/content-components'\\)"
    - from: "src/pages/dev/content-components.astro"
      to: "src/components/wl/ProjectCard.astro"
      via: "component import + render (closed + expanded, real collection data)"
      pattern: "ProjectCard"
    - from: "src/pages/dev/content-components.astro"
      to: "prod build exclusion"
      via: "import.meta.env.PROD redirect + final-commit deletion"
      pattern: "import.meta.env.PROD"
---

<objective>
Verify the three Phase 36 components against their accessibility, contrast, and Figma-fidelity
contracts, then remove the temporary verification scaffolding so nothing dev-only ships to prod.

This plan builds a DEV-gated isolation page that renders `ProjectCard` (closed + expanded, real
collection data), a `FAQItem` group (exclusive-open demo), and `FrequencyWave` (on both paper and
sea-glass surfaces) in Figma `36:5` order; adds a Playwright + axe spec covering light + dark modes
plus a keyboard-only FAQ check; runs the contrast gate; presents the fidelity gate to Joel; and then
deletes the isolation page + axe spec in a final cleanup commit — verified by a zero-result grep of
the build output.

Purpose: Close the Phase 36 verification gates (ROADMAP SC-1, SC-2, SC-5) and enforce the
"isolation page never ships to prod" contract.
Output: A passing a11y + contrast run, an approved fidelity gate, and a clean working tree with
the dev page/spec removed and the build proven free of `content-components`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md
@.planning/phases/36-content-components-expandable-cards/36-PATTERNS.md
@.planning/phases/36-content-components-expandable-cards/36-RESEARCH.md
@.planning/phases/36-content-components-expandable-cards/36-03-SUMMARY.md
@.planning/phases/36-content-components-expandable-cards/36-04-SUMMARY.md
@.planning/phases/36-content-components-expandable-cards/36-05-SUMMARY.md
@src/components/wl/ProjectCard.astro
@src/components/wl/FAQItem.astro
@src/components/wl/FrequencyWave.astro
@tests/accessibility/helpers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the DEV-gated isolation page (Figma 36:5 order, all three components)</name>
  <files>src/pages/dev/content-components.astro</files>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (src/pages/dev/content-components.astro section, lines ~381–429 — full file structure from primitives.astro git 549332f; section-label pattern; DEV-gate redirect)
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md ("Dev Isolation Page" section, lines ~498–523 — route, DEV-gate, Figma 36:5 layout order, FrequencyWave on paper AND sea-glass, deletion gate)
    - .planning/phases/36-content-components-expandable-cards/36-04-SUMMARY.md and 36-05-SUMMARY.md (final component prop signatures + any FLAGGED gaps to render around)
    - src/components/wl/ProjectCard.astro, FAQItem.astro, FrequencyWave.astro (as built — confirm exact prop names before wiring)
    - src/content.config.ts (confirm getCollection('projects') is registered; use entry.data.* fields)
  </read_first>
  <action>
    Create `src/pages/dev/content-components.astro` following the primitives.astro analog
    (36-PATTERNS.md). Structure:

    1. DEV-gate FIRST in frontmatter (Astro.redirect never runs after markup):
       `if (import.meta.env.PROD) { return Astro.redirect('/'); }`
    2. Import BaseLayout and the three wl components; import getCollection from 'astro:content'
       and load `const projects = await getCollection('projects');`.
    3. Render sections in Figma `36:5` component order (confirm the exact order from
       36-04/05 summaries or the UI-SPEC extraction; make it mechanical for the fidelity gate).
       Use the 11px uppercase section-label pattern from 36-PATTERNS.md before each block.

    Sections to render:
      - ProjectCard — CLOSED state: render a ProjectCard from a real `projects` entry
        (`entry.data.eyebrow/title/outcome/summary/tags/problem/built/result`). Do NOT hardcode
        copy — pull from the collection so the SEO-in-DOM contract is exercised on real data.
      - ProjectCard — EXPANDED state: render a second ProjectCard with the `open` attribute forced
        (add `open` to the underlying <details> — pass through however the component exposes it; if
        it does not expose an open-passthrough prop, wrap in a note and render closed + document the
        gap rather than editing the component in this plan). Purpose: fidelity screenshots need a
        statically-expanded card.
      - FAQItem group: render 2–3 FAQItem components sharing ONE `groupName` (e.g. "faq-dev-demo")
        with placeholder question/answer slot text, so the exclusive-open behavior (D-03 is
        ProjectCard-only; FAQ uses the name attr) can be demonstrated and keyboard-tested.
      - FrequencyWave: render it TWICE — once inside a `background: var(--color-wl-paper)` section
        and once inside a `background: var(--color-wl-sea-glass)` section — to prove the
        `var(--color-wl-accent)` stroke reads on both surfaces in both themes (UI-SPEC on-dark
        variant display).

    Use only `--wl-*` / `--color-wl-*` tokens for the page's own chrome (backgrounds, labels).
    Add a header comment marking the file TEMPORARY and stating it is deleted in the final Phase 36
    commit alongside the axe spec. No `<script>` tag. Zero old neobrutalist token references.
  </action>
  <verify>
    npx astro check 2>/dev/null; npm run build && test -f dist/dev/content-components/index.html && echo "PAGE BUILT (dev build)"; grep -q "import.meta.env.PROD" src/pages/dev/content-components.astro && echo "DEV-GATE PRESENT"
  </verify>
  <done>
    The isolation page builds, renders all three components (ProjectCard closed + expanded from real
    collection data, a FAQItem group, FrequencyWave on paper + sea-glass) in Figma 36:5 order, and is
    DEV-gated via import.meta.env.PROD.
  </done>
  <acceptance_criteria>
    - `grep -q "import.meta.env.PROD" src/pages/dev/content-components.astro` matches (DEV-gate present, before any markup).
    - The page imports and renders ProjectCard, FAQItem, and FrequencyWave (grep each name → ≥1 match).
    - ProjectCard content is sourced from `getCollection('projects')` (grep `getCollection` → match), not hardcoded.
    - Two ProjectCard instances exist (closed + a forced-open/expanded one) OR the open-passthrough gap is documented in the SUMMARY.
    - Two or more FAQItem instances share a single `groupName` value (exclusive-open group).
    - FrequencyWave appears in both a `--color-wl-paper` and a `--color-wl-sea-glass` section.
    - `grep -E "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow" src/pages/dev/content-components.astro` returns zero.
    - `npm run build` exits 0 and `dist/dev/content-components/index.html` exists (dev build emits the page).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the axe + keyboard a11y spec (light + dark) and run the a11y + contrast gates</name>
  <files>tests/accessibility/content-components.spec.ts</files>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (tests/accessibility/content-components.spec.ts section, lines ~433–481 — full spec from primitives.spec.ts git 549332f; note the axe-tests.spec.ts / dark-mode.spec.ts /projects cleanup already handled in the deletion plan)
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md ("axe-core gate" section, lines ~396–407 — tags, contexts, settleAnimations, manual keyboard check for FAQ exclusive-open)
    - tests/accessibility/helpers.ts (settleAnimations signature — import and call before every .analyze())
    - tests/accessibility/dark-mode.spec.ts (dark-context pattern: browser.newContext({ colorScheme: 'dark' }); html gets .dark class from the FOUC script)
  </read_first>
  <action>
    Create `tests/accessibility/content-components.spec.ts` following the primitives.spec.ts analog.
    Header comment must state the file is TEMPORARY and MUST be deleted in the final Phase 36 commit
    (simultaneously with the isolation page) or CI will 404.

    Include these tests, all against `/dev/content-components`:

    1. Light mode — zero axe violations:
       `await page.goto('/dev/content-components'); await settleAnimations(page);`
       `const results = await new AxeBuilder({ page }).withTags(wcagTags).analyze();`
       `expect(results.violations).toEqual([]);`
       where `const wcagTags = ['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa'];`

    2. Dark mode — zero axe violations, via `browser.newContext({ colorScheme: 'dark' })`; assert
       `await expect(page.locator('html')).toHaveClass(/dark/)` (FOUC script applied .dark) BEFORE
       settleAnimations + analyze; close the context after.

    3. Keyboard-only FAQ exclusive-open (COMP-04 SC-2, no mouse): drive with keyboard only.
       - Focus the first FAQItem `<summary>` via `page.keyboard.press('Tab')` (repeat until the
         first summary is `:focus`; assert via `page.locator('summary:focus')` or a focused-element
         eval). Open it with `page.keyboard.press('Enter')` (or 'Space'); assert its parent
         `<details>` now has `[open]`.
       - Tab to the next FAQItem summary and open it with Enter/Space; assert the SECOND details is
         `[open]` AND the FIRST details is no longer `[open]` (exclusive-open enforced by the shared
         `name` attribute). Use NO `page.click`/mouse calls in this test — keyboard only.

    Then run the full gate:
      - `npm run test:a11y` (Playwright starts the dev server per playwright.config; the DEV-gate
        lets the page render). All three tests pass.
      - `node scripts/check-contrast.mjs` passes (Phase 36 pairs were added in 36-03).
  </action>
  <verify>
    grep -q "wcag22aa" tests/accessibility/content-components.spec.ts && npm run test:a11y && node scripts/check-contrast.mjs
  </verify>
  <done>
    The spec exists with light-mode axe, dark-mode axe (colorScheme dark + .dark class assertion),
    and a keyboard-only FAQ exclusive-open test; `npm run test:a11y` passes with zero violations and
    `node scripts/check-contrast.mjs` exits 0.
  </done>
  <acceptance_criteria>
    - The spec tags array is exactly `['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']`.
    - Both an axe light-mode and an axe dark-mode test exist; the dark test uses `browser.newContext({ colorScheme: 'dark' })` and asserts `html` has class `dark`.
    - `settleAnimations(page)` is called before every `.analyze()`.
    - A keyboard-only FAQ test exists that opens item 1, then item 2, and asserts item 1 closed (exclusive-open) using no mouse/click calls.
    - `npm run test:a11y` exits 0 with zero axe violations in both modes.
    - `node scripts/check-contrast.mjs` exits 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fidelity gate — Joel approves rendered vs Figma (36:5 + 12:3, closed + expanded, light + dark)</name>
  <what-built>
    A DEV-gated isolation page (`/dev/content-components`) rendering ProjectCard (closed + expanded
    from real collection data), a FAQItem exclusive-open group, and FrequencyWave on paper +
    sea-glass surfaces, in Figma 36:5 component order. axe passes light + dark; contrast passes.
  </what-built>
  <how-to-verify>
    1. Capture rendered screenshots at 1440px width via Playwright:
       - Light: `page.setViewportSize({ width: 1440, height: 2200 })`, goto `/dev/content-components`,
         `settleAnimations`, `page.screenshot({ path: 'scratchpad/36-fidelity-light.png', fullPage: true })`.
       - Dark: same, but in a `browser.newContext({ colorScheme: 'dark' })` context; save
         `scratchpad/36-fidelity-dark.png`.
       (Write a throwaway Playwright snippet or reuse the spec harness; these screenshots are NOT
       committed — scratchpad only.)
    2. Capture the Figma reference frames for side-by-side comparison. Pencil `export_nodes` is BROKEN
       (see MEMORY: pencil-export-nodes-broken) — do NOT use it. Instead use the figma-desktop MCP
       `get_screenshot` on nodes:
         - `36:5` (Components page — ProjectCard closed + expanded, FAQItem)
         - `12:3` (Showcase — ProjectCard in context, closed + expanded)
       If MCP screenshot is unavailable, ask Joel to export those frames manually from the Figma editor.
    3. Present rendered vs Figma side-by-side for: ProjectCard closed, ProjectCard expanded, FAQItem
       (closed + one open) — in both light and dark.
    4. Walk the UI-SPEC pre-gate checklist (lines ~553–562) as the acceptance backbone:
       - ProjectCard closed + expanded render; keyboard toggle works
       - FAQItem exclusive-open confirmed (opening one closes others)
       - FrequencyWave stroke adapts to dark mode without JS
       - projects.json v2 populated (Figma-shaped placeholder, no invented copy)
       - `npm run test:a11y` passes (light + dark) — already green from Task 2
       - `node scripts/check-contrast.mjs` passes — already green from Task 2
  </how-to-verify>
  <resume-signal>
    Joel types "approved" (proceed to cleanup, Task 4), or describes specific fidelity gaps to fix.
    If gaps are in the built components (36-04/05 output), note them for a gap-closure follow-up
    rather than editing components in this cleanup plan.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Final cleanup — delete the isolation page + axe spec, prove the build is clean</name>
  <files>src/pages/dev/content-components.astro</files>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md ("Dev Isolation Page" → Deletion gate, lines ~521–523; Fidelity Gate pre-gate checklist final two items)
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (DELETE instruction + the grep gate)
  </read_first>
  <action>
    ONLY after Task 3 is approved. Delete both temporary files from the working tree:
      - `src/pages/dev/content-components.astro`
      - `tests/accessibility/content-components.spec.ts`
    (Use `git rm` so the deletion is staged.)

    Then run the two closing gates and confirm zero results:
      1. `npm run build && grep -r "content-components" dist/` → must return zero (nothing named
         content-components ships to prod). The `&&` ensures build succeeds first.
      2. Old-neobrutalist-token gate on the three NEW wl components (they must remain clean after all
         verification): `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow" \
         src/components/wl/ProjectCard.astro src/components/wl/FAQItem.astro \
         src/components/wl/FrequencyWave.astro` → must return zero.

    Also confirm `npm run test:a11y` still passes with the isolation spec gone (it should have no
    remaining tests referencing /dev/content-components; if any other spec references that route,
    that is a bug from an earlier plan — flag it, do not silently patch here).

    Commit the cleanup on the current phase branch with a message noting the isolation page + axe
    spec were removed and both gates are green.
  </action>
  <verify>
    ! test -f src/pages/dev/content-components.astro && ! test -f tests/accessibility/content-components.spec.ts && npm run build && test -z "$(grep -r 'content-components' dist/ 2>/dev/null)" && echo "CLEAN: no content-components in dist" && test -z "$(grep -rE 'bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow' src/components/wl/ProjectCard.astro src/components/wl/FAQItem.astro src/components/wl/FrequencyWave.astro 2>/dev/null)" && echo "CLEAN: no old tokens in wl components"
  </verify>
  <done>
    Both temporary files are deleted; `npm run build && grep -r "content-components" dist/` returns
    zero; the old-token grep on the three wl components returns zero; the cleanup is committed.
  </done>
  <acceptance_criteria>
    - `test -f src/pages/dev/content-components.astro` is FALSE (file deleted).
    - `test -f tests/accessibility/content-components.spec.ts` is FALSE (file deleted).
    - `npm run build` exits 0.
    - `grep -r "content-components" dist/` returns zero lines.
    - `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow" src/components/wl/ProjectCard.astro src/components/wl/FAQItem.astro src/components/wl/FrequencyWave.astro` returns zero lines.
    - `npm run test:a11y` exits 0 (no stale spec 404s the /dev/content-components route).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run test:a11y` passes with zero axe violations (light + dark) and the keyboard-only FAQ exclusive-open test passes (SC-1, SC-2).
- `node scripts/check-contrast.mjs` exits 0 with the Phase 36 pairs.
- Joel approved the fidelity gate: rendered ProjectCard (closed + expanded) and FAQItem match Figma 36:5 / 12:3 at 1440px in light + dark (SC-5).
- Post-cleanup: both temporary files are gone; `npm run build && grep -r "content-components" dist/` returns zero; the old-token grep on the three wl components returns zero.
</verification>

<success_criteria>
Phase 36's accessibility (SC-1, SC-2), contrast, and Figma-fidelity (SC-5) gates are all green and
approved, and the temporary verification scaffolding (isolation page + axe spec) has been removed and
proven absent from the production build — so nothing dev-only ships to prod and the component library
is ready for Phases 37–38 to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/36-content-components-expandable-cards/36-06-SUMMARY.md`
recording: the a11y result (light + dark axe + keyboard FAQ), the contrast result, the fidelity-gate
outcome (approved / gaps noted for follow-up, and how the Figma frames were captured given
export_nodes is broken), the two files deleted, and the two closing grep gates both returning zero.
Note any FLAGGED fidelity gaps deferred to a gap-closure plan.
</output>
