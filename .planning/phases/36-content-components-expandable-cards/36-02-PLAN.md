---
phase: 36-content-components-expandable-cards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/index.astro
  - src/pages/projects/[slug].astro
  - astro.config.mjs
  - tests/accessibility/axe-tests.spec.ts
  - tests/accessibility/dark-mode.spec.ts
autonomous: true
requirements: [CONT-01]

threat_model:
  summary: >
    Deletes two legacy neobrutalist route files and repoints two redirects. Static site,
    no server code, no user input. The only security-relevant change is redirect targets.
  surfaces:
    - id: open-redirect
      note: >
        `/portfolio` and `/portfolio/[slug]` are repointed from `/projects*` to `/` — both
        are static, same-origin, hardcoded destinations. No user-controlled redirect target;
        no open-redirect surface.
    - id: dead-code
      note: >
        Removing v1 route files eliminates references to dropped v1 projects.json fields,
        shrinking attack/maintenance surface. No new code added.
  asvs_l1: "PASS — no auth, no input handling, static redirects with fixed same-origin targets."

must_haves:
  truths:
    - "Visiting /projects or /projects/[slug] no longer resolves to a v1 neobrutalist page."
    - "Visiting /portfolio redirects to / (not to the now-deleted /projects)."
    - "npm run build succeeds with zero import errors after the v1 pages are removed."
    - "The accessibility test suite no longer navigates to the deleted /projects route."
  artifacts:
    - path: "astro.config.mjs"
      provides: "Redirect map with /portfolio* repointed to /"
      contains: "'/portfolio': '/'"
  key_links:
    - from: "astro.config.mjs redirects"
      to: "/"
      via: "static redirect entries"
      pattern: "'/portfolio': '/'"
---

<objective>
Tear down the last consumers of the v1 `projects.json` so the v2 rewrite (36-03) can land on a
green build. The old neobrutalist `/projects` pages import v1 fields (`category`, `technologies`,
`draft`, etc.) that v2 drops; they must be deleted first (D-06, RESEARCH Pitfall 4). Repoint the
`/portfolio*` redirects to `/` (same treatment as `/faq`, until `/showcase` ships in Phase 38),
and remove the stale `/projects` accessibility tests that would 404 after deletion (RESEARCH Pitfall 6).

Purpose: Unblock the v2 data rewrite; keep the build green at every commit.
Output: v1 `/projects` route files deleted, redirects repointed, stale a11y tests removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-content-components-expandable-cards/36-RESEARCH.md
@.planning/phases/36-content-components-expandable-cards/36-PATTERNS.md
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 /projects pages and repoint /portfolio redirects</name>
  <files>src/pages/projects/index.astro, src/pages/projects/[slug].astro, astro.config.mjs</files>
  <read_first>
    - astro.config.mjs (current redirects block, lines 73–78 — the exact keys/values to edit)
    - src/pages/projects/index.astro (confirm it is the v1 neobrutalist listing before deleting)
    - src/pages/projects/[slug].astro (confirm it consumes v1 fields before deleting)
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (astro.config redirects section, lines ~321–345 — exact target state)
  </read_first>
  <action>
    Delete `src/pages/projects/index.astro` and `src/pages/projects/[slug].astro` (D-06 — both are
    v1 neobrutalist, both entries are draft-hidden so the prod listing is already empty). In
    `astro.config.mjs`, change the redirect values: `'/portfolio'` from `'/projects'` to `'/'`, and
    `'/portfolio/[slug]'` from `'/projects/[slug]'` to `'/'`. Leave `'/contact': '/#contact'` and
    `'/faq': '/'` unchanged. Do NOT add a `/projects` → `/showcase` redirect (that lands in Phase 40;
    `/showcase` does not exist yet). The `/projects` routes will 404 after deletion — that is correct
    per D-06. Also remove the empty `src/pages/projects/` directory if it is left empty after deletion.
  </action>
  <verify>
    test ! -f src/pages/projects/index.astro && test ! -f "src/pages/projects/[slug].astro";
    grep -n "'/portfolio'" astro.config.mjs shows `'/portfolio': '/'`.
  </verify>
  <done>
    Both v1 route files are gone; both /portfolio redirect targets are '/'; other redirects unchanged.
  </done>
  <acceptance_criteria>
    - `test -f src/pages/projects/index.astro` returns non-zero (file absent).
    - `test -f "src/pages/projects/[slug].astro"` returns non-zero (file absent).
    - `grep "'/portfolio': '/'" astro.config.mjs` matches; `grep "'/portfolio/\[slug\]': '/'" astro.config.mjs` matches.
    - `grep "'/faq': '/'" astro.config.mjs` still matches (unchanged).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove stale /projects a11y tests and confirm green build</name>
  <files>tests/accessibility/axe-tests.spec.ts, tests/accessibility/dark-mode.spec.ts</files>
  <read_first>
    - tests/accessibility/axe-tests.spec.ts (the /projects test around line 27 — remove that test block only)
    - tests/accessibility/dark-mode.spec.ts (the /projects goto around line 39 — remove that test block only)
    - .planning/phases/36-content-components-expandable-cards/36-RESEARCH.md (Pitfall 6, lines ~415–424)
  </read_first>
  <action>
    In `tests/accessibility/axe-tests.spec.ts`, remove the single test that navigates to `/projects`
    (the `page.goto('/projects')` block near line 27) — delete the whole `test(...)` closure for that
    route, not just the goto line. In `tests/accessibility/dark-mode.spec.ts`, remove the test that does
    `page.goto('/projects')` near line 39 the same way. Leave all other route tests (landing, blog, etc.)
    intact. Then run `npm run build` and confirm it succeeds with zero import errors — this proves no
    remaining file references the deleted v1 pages or v1 projects.json fields. (At this point projects.json
    is still v1 and still valid, since no page consumes it after deletion.)
  </action>
  <verify>
    grep -rn "'/projects'" tests/accessibility/ returns zero matches;
    npm run build exits 0.
  </verify>
  <done>
    No accessibility test references /projects; `npm run build` succeeds.
  </done>
  <acceptance_criteria>
    - `grep -rn "/projects'" tests/accessibility/` returns zero matches.
    - `npm run build` exits with code 0 (no "Cannot find module" / import errors in output).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` is green after v1 pages are deleted and redirects repointed.
- `grep -rn "projects" tests/accessibility/` shows no `/projects` route navigation remains.
- `astro.config.mjs` redirect map has `/portfolio` and `/portfolio/[slug]` pointing at `/`.
</verification>

<success_criteria>
The v1 `projects.json` has zero remaining consumers, the build is green, and 36-03 can safely
overwrite `projects.json` with the v2 schema without breaking any page or test.
</success_criteria>

<output>
After completion, create `.planning/phases/36-content-components-expandable-cards/36-02-SUMMARY.md`
noting the deleted files, the repointed redirects, and confirmation of a green build.
</output>
