---
phase: 36-content-components-expandable-cards
plan: 03
type: execute
wave: 2
depends_on: ["36-01", "36-02"]
files_modified:
  - src/data/projects.json
  - src/content.config.ts
  - scripts/check-contrast.mjs
autonomous: true
requirements: [CONT-01]

threat_model:
  summary: >
    Rewrites a build-time JSON data file, adds an Astro content collection with a Zod schema,
    and extends a contrast-check script. Static site, no runtime, no user input.
  surfaces:
    - id: content-injection
      note: >
        projects.json copy is rendered into HTML by ProjectCard (Phase 38 pages). Astro escapes
        interpolated expressions by default; this copy is static, author-supplied placeholder text
        from Figma (D-09), not user input. No injection vector. Do not use set:html with this data.
    - id: schema-validation
      note: >
        The Zod schema (content.config.ts) provides build-time validation of the data shape —
        a positive control that malformed/unexpected fields fail the build rather than silently rendering.
    - id: supply-chain
      note: "Zero new npm dependencies — file() and z are already provided by astro:content / astro/loaders. package.json unchanged."
  asvs_l1: "PASS — static build-time data, schema-validated, output auto-escaped."

must_haves:
  truths:
    - "getCollection('projects') returns the v2 entries with typed fields at build time."
    - "Every projects.json entry has an id field (required by the Astro file() loader) equal to its slug."
    - "The v2 schema carries section, eyebrow, title, outcome, summary, tags, problem, built, result — v1 fields are dropped."
    - "node scripts/check-contrast.mjs passes with the Phase 36 ProjectCard pairs added."
  artifacts:
    - path: "src/data/projects.json"
      provides: "v2 project data (placeholder copy per D-09) with id+slug on every entry"
      contains: '"id"'
    - path: "src/content.config.ts"
      provides: "projects collection via file() loader + Zod schema"
      contains: "file("
    - path: "scripts/check-contrast.mjs"
      provides: "Phase 36 ProjectCard contrast pairs"
      contains: "ProjectCard"
  key_links:
    - from: "src/content.config.ts"
      to: "src/data/projects.json"
      via: "file() loader"
      pattern: "file\\('src/data/projects.json'\\)"
    - from: "src/content.config.ts collections export"
      to: "projects"
      via: "collections = { blog, projects }"
      pattern: "collections = \\{ blog, projects \\}"
---

<objective>
Promote `projects.json` to the v2 schema and register it as a typed Astro content collection so
Phase 38's showcase page can consume it via `getCollection('projects')`. Content is explicit
placeholder (D-09): the single Figma expanded-card copy captured in `36-EXTRACTION.md` is duplicated
across all entries. Extend the contrast script with the Phase 36 ProjectCard text pairs so component
work in 36-04 has a passing contrast gate to rely on.

Purpose: Deliver CONT-01's data contract and the schema Phase 38 depends on.
Output: v2 `projects.json`, a `projects` collection in `content.config.ts`, extended contrast script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md
@.planning/phases/36-content-components-expandable-cards/36-RESEARCH.md
@.planning/phases/36-content-components-expandable-cards/36-PATTERNS.md
@.planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md
@src/content.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite projects.json to v2 schema with id + placeholder copy (D-09)</name>
  <files>src/data/projects.json</files>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md ("Placeholder card copy (D-09)" section — the 8 verbatim fields to duplicate)
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md (Content Contract — projects.json v2 Schema, lines ~422–465, incl. legacy fields to drop)
    - .planning/phases/36-content-components-expandable-cards/36-RESEARCH.md (Pitfall 1 — id field requirement; Pattern 4 — v2 structure)
    - src/data/projects.json (current v1 — carry slugs bakery-order-system, inventory-sync-automation)
  </read_first>
  <action>
    Overwrite `src/data/projects.json` as a v2 array of exactly two entries keyed by the existing slugs
    `bakery-order-system` and `inventory-sync-automation`. Each entry MUST have these fields in this
    shape: `id` (string, equal to slug — REQUIRED by the file() loader per RESEARCH Pitfall 1; slug does
    NOT substitute), `slug` (string), `section` (one of the two literals `"client-work"` or
    `"craft-experiments"` — assign one to each entry using the section labels recorded in 36-EXTRACTION.md),
    `eyebrow`, `title`, `outcome`, `summary`, `tags` (string array), `problem`, `built`, `result`. Populate
    eyebrow/title/outcome/summary/tags/problem/built/result by copying the ONE Figma expanded-card's verbatim
    copy from 36-EXTRACTION.md into BOTH entries (D-09 placeholder mechanism — same copy duplicated; do not
    invent variation). Drop ALL v1 fields: `category`, `categoryLabel`, `thumbnail`, `screenshots`,
    `testimonial`, `technologies`, `results` (array form), `solution`, `draft`. If any placeholder field was
    marked FLAGGED in 36-EXTRACTION.md, leave it as an empty string and note the gap in the SUMMARY — do not
    invent copy (CONT-01 / v1.4 lesson). Ensure valid JSON (array root, no trailing commas).
  </action>
  <verify>
    node -e "const p=require('./src/data/projects.json'); if(!Array.isArray(p)) throw 'not array'; p.forEach(e=>{if(!e.id||e.id!==e.slug) throw 'id!=slug: '+e.slug; ['section','title','tags','problem','built','result'].forEach(k=>{if(!(k in e)) throw 'missing '+k})}); console.log('ok',p.length)"
  </verify>
  <done>
    projects.json is a valid v2 array; every entry has id===slug; all v2 fields present; zero v1 fields remain.
  </done>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('src/data/projects.json'))"` exits 0 (valid JSON).
    - Every entry has an `id` string equal to its `slug`.
    - Every entry has a `section` value that is exactly `client-work` or `craft-experiments`.
    - `grep -E '"category"|"thumbnail"|"technologies"|"testimonial"|"draft"' src/data/projects.json` returns zero matches (v1 fields dropped).
    - Each entry contains the keys eyebrow, title, outcome, summary, tags, problem, built, result.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register projects collection in content.config.ts</name>
  <files>src/content.config.ts</files>
  <read_first>
    - src/content.config.ts (current blog collection — extend, do not replace the file)
    - .planning/phases/36-content-components-expandable-cards/36-RESEARCH.md (Pattern 4 — file() loader + schema, lines ~164–214; Pitfall 7 — id not in data)
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (content.config.ts section, lines ~266–317 — exact schema to add)
  </read_first>
  <action>
    Add a `projects` collection to `src/content.config.ts` without altering the existing `blog` collection.
    Add the import `import { file } from 'astro/loaders';` alongside the existing `glob` import. Define
    `const projects = defineCollection({ loader: file('src/data/projects.json'), schema: z.object({...}) })`
    with schema fields: `slug: z.string()`, `section: z.enum(['client-work','craft-experiments'])`,
    `eyebrow: z.string().optional()`, `title: z.string()`, `outcome: z.string().optional()`,
    `summary: z.string().optional()`, `tags: z.array(z.string()).default([])`, `problem: z.string().optional()`,
    `built: z.string().optional()`, `result: z.string().optional()`. Do NOT put `id` in the schema — Astro
    exposes the loader key as `entry.id`, not `entry.data.id` (RESEARCH Pitfall 7). Update the export to
    `export const collections = { blog, projects };`.
  </action>
  <verify>
    grep -q "file('src/data/projects.json')" src/content.config.ts && grep -q "collections = { blog, projects }" src/content.config.ts && npm run build
  </verify>
  <done>
    content.config.ts defines a projects collection via file() loader; export includes projects; build passes.
  </done>
  <acceptance_criteria>
    - `grep "import { file } from 'astro/loaders'" src/content.config.ts` matches.
    - `grep "file('src/data/projects.json')" src/content.config.ts` matches.
    - `grep "collections = { blog, projects }" src/content.config.ts` matches.
    - The schema does NOT contain a line declaring `id:` (id is the loader key, not a schema field).
    - `npm run build` exits 0 (schema validates the v2 JSON — no ContentLoaderInvalidDataError).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend check-contrast.mjs with Phase 36 ProjectCard pairs</name>
  <files>scripts/check-contrast.mjs</files>
  <read_first>
    - scripts/check-contrast.mjs (existing PAIRS array + Phase 35 additions block ~lines 220–283; palette constants L_INK/D_INK/L_SUB/D_SUB/CARD_WHITE/CARD_DARK — reuse, do not redefine)
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md (Contrast script extension table, lines ~408–419)
    - .planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md (ProjectCard outcome accent-on-card-bg hex, once extracted — resolves the outcome-pair FIDELITY-GAP)
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (check-contrast section, lines ~349–378 — array-literal entry format)
  </read_first>
  <action>
    Append a clearly-commented "PHASE 36 ADDITIONS" block to the PAIRS array in `scripts/check-contrast.mjs`,
    following the existing array-literal entry format `[fg_hex, bg_hex, label, 4.5, true]` used by the Phase 35
    block. Reuse existing constants (L_INK, D_INK, L_SUB, D_SUB, CARD_WHITE, CARD_DARK). Add these ProjectCard
    text pairs at target 4.5:1: title ink-on-card-bg (light: L_INK on CARD_WHITE; dark: D_INK on CARD_DARK);
    body sub-on-card-bg (light: L_SUB on CARD_WHITE; dark: D_SUB on CARD_DARK); and the outcome accent-on-card-bg
    pair using the accent-on-card hex confirmed in 36-EXTRACTION.md (light + dark). For FAQItem pairs: if
    36-EXTRACTION.md confirmed the FAQItem surface fill and question color, add the FAQItem question and answer
    pairs too; if the FAQ surface fill was FLAGGED, add a comment placeholder noting FAQItem pairs pend Figma
    confirmation rather than guessing a hex. Then run the script and confirm it passes.
  </action>
  <verify>
    grep -q "PHASE 36" scripts/check-contrast.mjs && node scripts/check-contrast.mjs
  </verify>
  <done>
    check-contrast.mjs contains Phase 36 ProjectCard pairs and `node scripts/check-contrast.mjs` exits 0.
  </done>
  <acceptance_criteria>
    - `grep -i "ProjectCard" scripts/check-contrast.mjs` returns >=1 match.
    - `node scripts/check-contrast.mjs` exits 0 (all pairs including new ProjectCard pairs pass their threshold).
    - No newly-invented hex literals for FAQItem appear if the FAQ surface was FLAGGED in 36-EXTRACTION.md (a comment placeholder is present instead).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` succeeds with the v2 collection registered (schema validates the data).
- `node -e "..."` confirms every entry has id===slug and required v2 fields.
- `node scripts/check-contrast.mjs` exits 0 with Phase 36 pairs.
</verification>

<success_criteria>
Phase 38 can call `getCollection('projects')` and receive typed v2 entries; the ProjectCard
contrast pairs pass, giving 36-04 a green contrast gate to build the component against.
</success_criteria>

<output>
After completion, create `.planning/phases/36-content-components-expandable-cards/36-03-SUMMARY.md`
noting the v2 schema, id/slug convention, which entry got which section, any FLAGGED-empty fields,
and the contrast-pair additions.
</output>
