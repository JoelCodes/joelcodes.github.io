---
phase: 36-content-components-expandable-cards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md
autonomous: false
requirements: [COMP-03, COMP-04, COMP-05, CONT-01]

threat_model:
  summary: >
    Extraction-only plan. No source code, no runtime, no user input. Produces a
    markdown artifact of Figma-extracted values. No attack surface introduced.
  surfaces:
    - id: supply-chain
      note: "Zero new npm dependencies (UI-SPEC Registry Safety). This plan installs nothing — figma-desktop MCP only. package.json unchanged."
    - id: content-injection
      note: >
        Placeholder copy extracted here is later rendered into HTML (projects.json v2, 36-03).
        Astro escapes interpolated strings by default; copy is static build-time text authored by Joel/Figma, not user-supplied. No injection surface at extraction time.
  asvs_l1: "N/A — no code produced this plan; downstream plans inherit static-site posture."

must_haves:
  truths:
    - "Every FIDELITY-GAP listed in 36-UI-SPEC.md and 36-PATTERNS.md has a concrete extracted value recorded (or is explicitly flagged as a Figma gap for Joel)."
    - "The exact SVG path data for the ProjectCard/FAQItem toggle indicator is captured."
    - "The exact viewBox, five path d-values, and stroke-width for FrequencyWave are captured."
    - "The one Figma expanded-card's verbatim copy (8 fields) is captured for D-09 placeholder duplication."
  artifacts:
    - path: ".planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md"
      provides: "Single source of truth for all extracted geometry, SVG path data, type-class confirmations, contrast hex values, and placeholder card copy"
      contains: "FrequencyWave"
  key_links:
    - from: "36-EXTRACTION.md"
      to: "Figma node 36:5"
      via: "figma get_design_context / get_screenshot"
      pattern: "36:5"
    - from: "36-EXTRACTION.md"
      to: "Figma node 12:3"
      via: "verbatim placeholder card copy (D-09)"
      pattern: "12:3"
---

<objective>
Extract every FIDELITY-GAP value from the Figma design source of truth into a single
artifact (`36-EXTRACTION.md`) so that all downstream component and data plans build from
confirmed Figma values — never invented ones (v1.4 lesson). This is the Wave 1 gate: no
component CSS, no toggle SVG, no FrequencyWave, and no placeholder card copy may be written
until this artifact exists and resolves the gaps.

Purpose: Enforce the FIDELITY-GAP protocol — flag, never invent. Removes all geometry/SVG/
content ambiguity from Waves 2–4.
Output: `.planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md` recording
every gap value with its Figma node source.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md
@.planning/phases/36-content-components-expandable-cards/36-RESEARCH.md
@.planning/phases/36-content-components-expandable-cards/36-PATTERNS.md
</context>

<figma_access_note>
Figma file `1tg8wIPcvOVC5tPZ8pkGO2` "Joel Shinness Solutions — Brand Exploration" must be
open in Figma desktop for the figma-desktop MCP to resolve nodes. If MCP calls fail with a
node-not-found / file-not-open error, this is a checkpoint — ask Joel to open the file in
Figma desktop, then retry. Do NOT proceed to record invented values. `export_nodes` is broken
(see MEMORY); use `get_design_context` / `get_screenshot` for extraction, not PNG export.
</figma_access_note>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm Figma file is open for MCP extraction</name>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-RESEARCH.md (FIDELITY-GAP Inventory table, lines ~645–672)
    - .planning/phases/36-content-components-expandable-cards/36-PATTERNS.md (FIDELITY-GAP Summary for Planner, lines ~598–620)
  </read_first>
  <what-built>
    Attempted a figma-desktop MCP `get_metadata` (or equivalent) call on file
    `1tg8wIPcvOVC5tPZ8pkGO2` node `36:5` to confirm the file resolves.
  </what-built>
  <how-to-verify>
    1. Run a figma-desktop MCP metadata/context call against file `1tg8wIPcvOVC5tPZ8pkGO2`, node `36:5`.
    2. If it returns node data: this gate is satisfied automatically — proceed to Task 2.
    3. If it errors (file not open / node not found): ask Joel to open
       "Joel Shinness Solutions — Brand Exploration" (`1tg8wIPcvOVC5tPZ8pkGO2`) in Figma desktop,
       then re-run the call.
  </how-to-verify>
  <resume-signal>Node `36:5` metadata resolves via figma-desktop MCP (or Joel confirms file open)</resume-signal>
  <acceptance_criteria>
    - A figma-desktop MCP call against `1tg8wIPcvOVC5tPZ8pkGO2` node `36:5` returns node data (not an error).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extract ProjectCard + FAQItem geometry and toggle SVGs (node 36:5, 12:3)</name>
  <files>.planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md</files>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md (Component Visual Contracts + FIDELITY-GAP tables, lines ~169–260)
    - .planning/phases/36-content-components-expandable-cards/36-RESEARCH.md (FIDELITY-GAP Inventory, lines ~645–672)
    - src/components/wl/ServiceCard.astro (card geometry precedent: 18px radius, 32px/27px padding, --wl-card-bg/--wl-card-shadow — values to CONFIRM against 36:5, not assume)
  </read_first>
  <action>
    Using figma-desktop MCP `get_design_context` and `get_screenshot` on nodes `36:5` (Components
    page: Project Card closed + expanded, FAQ Item) and `12:3` (Showcase: ProjectCard in context),
    extract and RECORD each of the following into a new `36-EXTRACTION.md` under a "ProjectCard"
    and "FAQItem" section. Record the exact value AND note whether it confirms or overrides the
    ServiceCard precedent (18px radius / 32px/27px padding / 1px solid var(--color-wl-line) border):
    ProjectCard — border-radius (px), border (color/width, confirm var(--color-wl-line) or literal),
    closed padding (T/R/B/L px), summary-row gaps between eyebrow/title/outcome/summary/tags/chevron (px),
    expanded section padding-top (px), section divider (color/style), expanded section-label type class
    (confirm .wl-label-eyebrow vs alternative), and the toggle-indicator SVG (full path `d`, viewBox,
    stroke/fill, closed vs open direction — chevron-down closed rotating to chevron-up open per UI-SPEC).
    FAQItem — question type class (confirm .wl-text-body-large 17px vs .wl-heading-h3 21px), question
    color (confirm var(--color-wl-ink)), surface fill (paper / sea-glass / dedicated hex), padding
    (closed + expanded T/R/B/L px), divider color/style between items, and toggle-indicator SVG path
    (note if identical to ProjectCard's — D: may share). For any value Figma does not specify (e.g.
    hover state — no Figma hover spec exists), mark it FLAGGED and record the ServiceCard-derived
    fallback with a note that Phase 35 precedent applies (Joel approved derived hover at Phase 35 gate).
    Do NOT record a value you did not read from Figma without the FLAGGED marker.
  </action>
  <verify>
    grep -c "36:5" .planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md returns >=1;
    grep -iE "border-radius|padding|toggle|viewBox|stroke" 36-EXTRACTION.md shows populated values.
  </verify>
  <done>
    Every ProjectCard and FAQItem FIDELITY-GAP from the RESEARCH.md inventory has a recorded value
    (or an explicit FLAGGED note with a Phase-35-precedent fallback) with its Figma node source.
  </done>
  <acceptance_criteria>
    - `36-EXTRACTION.md` contains a "ProjectCard" section listing concrete px values for border-radius, closed padding, summary-row gaps, expanded padding-top, and a divider spec.
    - `36-EXTRACTION.md` contains the full SVG `d` path string(s) for the toggle indicator, with viewBox and stroke.
    - `36-EXTRACTION.md` contains a "FAQItem" section stating the confirmed question type class (one of `.wl-text-body-large` or `.wl-heading-h3`), question color hex/token, surface-fill hex/token, padding, and divider spec.
    - Every value not present in Figma is marked with the literal token `FLAGGED` and a stated fallback source.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extract FrequencyWave SVG + placeholder card copy + isolation-page order (12:2, 12:3, 36:5)</name>
  <files>.planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md</files>
  <read_first>
    - .planning/phases/36-content-components-expandable-cards/36-UI-SPEC.md (FrequencyWave contract + Content Contract, lines ~265–295 and ~422–465)
    - .planning/phases/36-content-components-expandable-cards/36-RESEARCH.md (projects.json v2 minimum structure, lines ~509–527)
    - src/components/WaveMark.astro (inline-SVG precedent: aria-hidden, var(--color-wl-accent) stroke, stroke-linecap="round")
    - src/data/projects.json (v1 — the two entries bakery-order-system, inventory-sync-automation whose ids/slugs carry into v2)
  </read_first>
  <action>
    Continue writing `36-EXTRACTION.md`, adding three sections.
    (a) "FrequencyWave" — from node `12:2` (landing page waveform / frequency-field background),
    extract: viewBox dimensions, all FIVE line path `d` values, per-line stroke-width, line-to-line
    vertical spacing, and stroke-linecap (confirm "round" per WaveMark precedent). Stroke color is
    fixed at var(--color-wl-accent) (no extraction needed — token flip handles dark mode). Also record
    the intended width/height sizing (likely 100% width, fixed height — confirm from landing context).
    (b) "Placeholder card copy (D-09)" — from node `12:3` expanded card state, transcribe VERBATIM the
    eight fields of the ONE expanded card: eyebrow, title, outcome (italic line), summary, tags (as a
    string array), problem, built, result. This single card's copy is duplicated across all projects.json
    v2 entries by 36-03 (D-09). Also record the two Showcase section labels (the "client-work" and
    "craft-experiments" display text). If any of the 8 fields is absent from Figma, mark it FLAGGED —
    do not invent copy (CONT-01 / v1.4 lesson).
    (c) "Isolation page order" — from node `36:5`, record the top-to-bottom component order shown on the
    Figma Components page so 36-05's dev page mirrors it for mechanical fidelity comparison.
    Carry the v1 slugs `bakery-order-system` and `inventory-sync-automation` forward as the two entry
    ids/slugs (both become id AND slug per RESEARCH.md file()-loader requirement).
  </action>
  <verify>
    grep -c "path" .planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md shows the 5 FrequencyWave paths recorded;
    grep -iE "eyebrow|outcome|problem|built|result" 36-EXTRACTION.md shows the 8 placeholder fields present.
  </verify>
  <done>
    FrequencyWave viewBox + 5 paths + stroke-width recorded; all 8 placeholder-card fields transcribed
    verbatim (or FLAGGED); Showcase section labels and isolation-page component order recorded.
  </done>
  <acceptance_criteria>
    - `36-EXTRACTION.md` "FrequencyWave" section contains a viewBox value, exactly 5 path `d` strings, and a stroke-width value.
    - `36-EXTRACTION.md` "Placeholder card copy (D-09)" section contains all 8 named fields (eyebrow, title, outcome, summary, tags, problem, built, result), each with verbatim Figma text or the literal `FLAGGED` marker.
    - `36-EXTRACTION.md` records the two Showcase section display labels and the Components-page component order.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `36-EXTRACTION.md` exists and every FIDELITY-GAP row from 36-RESEARCH.md's inventory table maps to a recorded value or a `FLAGGED` note.
- No value in the artifact is unsourced: each has a Figma node reference (`36:5`, `12:3`, or `12:2`) or a `FLAGGED` fallback note.
- Zero copy invented: any missing Figma copy is `FLAGGED`, not filled.
</verification>

<success_criteria>
Downstream plans (36-03, 36-04, 36-05) can read `36-EXTRACTION.md` and obtain every geometry,
SVG path, type class, surface hex, and copy string they need without any further Figma access
and without inventing a single value.
</success_criteria>

<output>
After completion, create `.planning/phases/36-content-components-expandable-cards/36-01-SUMMARY.md`
summarizing which gaps were resolved from Figma and which were `FLAGGED` (with fallback source).
</output>
