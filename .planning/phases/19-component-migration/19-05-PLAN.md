---
phase: 19-component-migration
plan: 05
type: execute
wave: 4
depends_on: ["19-04"]
files_modified:
  - src/pages/blog/[slug].astro
autonomous: true

must_haves:
  truths:
    - "Blog detail tag links use consistent border-[3px]"
    - "Border width matches design system standard"
  artifacts:
    - path: "src/pages/blog/[slug].astro"
      provides: "Consistent tag styling"
      contains: "border-[3px]"
  key_links: []
---

<objective>
Fix Blog detail tag border inconsistency (MEDIUM priority).

Purpose: Tag links use border-2 instead of design system's border-[3px]. Quick fix for visual consistency.

Output: Blog tags with correct border width matching design system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/AUDIT.md
@.planning/phases/19-component-migration/19-CONTEXT.md

@src/pages/blog/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix blog tag border width</name>
  <files>src/pages/blog/[slug].astro</files>
  <action>
Find the tag links section (around lines 70-76) and update border width.

Current styling likely uses `border-2`. Change to `border-[3px]` to match design system.

Look for pattern like:
```astro
<a href={`/blog/tags/${tag}`} class="... border-2 ...">
```

Change to:
```astro
<a href={`/blog/tags/${tag}`} class="... border-[3px] ...">
```

This is a simple find-and-replace within the tag link classes.
  </action>
  <verify>
Run `npm run dev` and visit any blog post.
Verify tag links have thicker 3px border matching other neobrutalist elements.
  </verify>
  <done>
Blog detail tag links use border-[3px] consistent with design system.
  </done>
</task>

</tasks>

<verification>
```bash
# Type check
npm run astro check

# Build succeeds
npm run build
```

Visual check:
- [ ] Blog post tags have visible 3px border
- [ ] Matches styling of other bordered elements (badges, cards)
</verification>

<success_criteria>
- Blog post tag links use border-[3px]
- Visual consistency with other neobrutalist elements
</success_criteria>

<output>
After completion, create `.planning/phases/19-component-migration/19-05-SUMMARY.md`
</output>
