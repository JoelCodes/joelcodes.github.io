---
phase: 10-projects-blog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BlogCard.astro
  - src/pages/blog/index.astro
  - src/pages/blog/tags/[tag].astro
  - src/pages/blog/[slug].astro
autonomous: true

must_haves:
  truths:
    - "Blog index page has neobrutalist header styling"
    - "Blog cards have thick borders and offset shadows matching project cards"
    - "Blog tag pages have same neobrutalist treatment as index"
    - "Blog post headings have neobrutalist accents (H1 uppercase, H2/H3 with border accents)"
    - "Blog post body text remains clean and readable (no brutalist treatment)"
    - "Blog cards have proper hover effects and focus states"
  artifacts:
    - path: "src/components/BlogCard.astro"
      provides: "Neobrutalist blog card component"
      min_lines: 60
    - path: "src/pages/blog/index.astro"
      provides: "Blog index with neobrutalist styling"
      min_lines: 80
    - path: "src/pages/blog/tags/[tag].astro"
      provides: "Tag pages with neobrutalist styling"
      min_lines: 50
    - path: "src/pages/blog/[slug].astro"
      provides: "Blog post page with two-tier typography"
      min_lines: 80
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/components/BlogCard.astro"
      via: "component import"
      pattern: "import BlogCard"
    - from: "src/pages/blog/tags/[tag].astro"
      to: "src/components/BlogCard.astro"
      via: "component import"
      pattern: "import BlogCard"
    - from: "src/pages/blog/[slug].astro"
      to: "prose styles"
      via: "CSS class"
      pattern: 'class="prose"'
---

<objective>
Apply neobrutalist styling to the blog section while maintaining readability for long-form content.

Purpose: Create a visually distinctive blog that matches the site's neobrutalist design system while preserving reading comfort. Apply the two-tier typography approach: bold neobrutalist headings with clean, neutral body text.

Output:
- Updated BlogCard with neobrutalist styling (thick borders, shadows)
- Restyled blog index with neobrutalist header and filter buttons
- Updated tag pages with same neobrutalist treatment
- Blog post pages with neobrutalist heading accents and readable body text
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-projects-blog/10-RESEARCH.md

# Existing files to modify
@src/components/BlogCard.astro
@src/pages/blog/index.astro
@src/pages/blog/tags/[tag].astro
@src/pages/blog/[slug].astro
@src/components/ui/Card.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BlogCard with Neobrutalist Styling</name>
  <files>
    - src/components/BlogCard.astro
  </files>
  <action>
    Update BlogCard.astro to match neobrutalist design system:

    1. Card container styling:
       - Add 4px solid border: border-[4px] border-text-light dark:border-text-dark
       - Add 6px offset shadow: Use custom shadow-neo-turquoise (turquoise for blog to differentiate from project cards which use yellow)
       - Dark mode: glow instead of shadow (shadow-[0_0_20px_color-mix(in_oklch,var(--color-turquoise-dark)_75%,transparent)])
       - Keep rounded-lg for corners
       - Background: bg-bg-light dark:bg-bg-dark

    2. Hover state:
       - Translate up: hover:-translate-y-2
       - Shadow change: On hover, reduce or remove offset shadow to create "pressed" feel
       - Smooth transition: transition-all duration-300

    3. Focus state (accessibility):
       - Box-shadow focus ring per Phase 8 pattern
       - Visible focus indicator: focus-visible:shadow-[0_0_0_2px_var(--color-bg-light),0_0_0_6px_var(--color-turquoise)]
       - Dark mode focus: dark:focus-visible:shadow-[0_0_0_2px_var(--color-bg-dark),0_0_0_6px_var(--color-turquoise-dark)]

    4. Image container:
       - Keep 16:9 aspect ratio
       - Keep existing fallback placeholder

    5. Content section:
       - Title: font-heading font-bold (keep existing)
       - Date/reading time: keep existing muted styling
       - Excerpt: keep line-clamp-3
       - Tags: update to neobrutalist pills with thin border
         * Use turquoise accent: border-2 border-turquoise/50 bg-turquoise/10
         * Rounded-full, text-sm

    6. Ensure entire card is wrapped in `<a>` tag (already is - verify accessible)
  </action>
  <verify>
    - BlogCard.astro has 4px border and offset shadow styling
    - Run `npm run dev` and visit http://localhost:4321/blog
    - Cards show thick borders and turquoise offset shadows
    - Hover translates card up with shadow transition
    - Tab through cards - focus ring is visible
    - Dark mode shows glow instead of offset shadow
  </verify>
  <done>
    - BlogCard has neobrutalist styling with thick borders
    - Turquoise accent shadows differentiate from project cards
    - Hover and focus states work correctly
    - Dark mode shows glow effect
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Blog Index and Tag Pages</name>
  <files>
    - src/pages/blog/index.astro
    - src/pages/blog/tags/[tag].astro
  </files>
  <action>
    1. Update src/pages/blog/index.astro:

       Header section:
       - H1: uppercase, font-heading, text-4xl md:text-5xl, font-bold
       - Subtitle: keep existing readable styling

       Filter buttons (neobrutalist treatment):
       - Active button:
         * bg-turquoise text-bg-dark (using turquoise for blog)
         * border-[3px] border-text-light dark:border-text-dark
         * font-bold rounded-full
       - Inactive buttons:
         * bg-transparent
         * border-[3px] border-text-light dark:border-text-dark
         * font-medium rounded-full
         * Hover: bg-turquoise/10

       Update setActiveButton JavaScript:
       - Change from bg-accent-yellow classes to bg-turquoise classes
       - Update class toggling to match new neobrutalist button styles

       Card grid:
       - Keep grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
       - Cards now use updated BlogCard component (no changes needed here)

       Load more button:
       - Apply neobrutalist button styling: thick border, rounded-full
       - Use turquoise accent for consistency

    2. Update src/pages/blog/tags/[tag].astro:

       Apply same neobrutalist styling as blog index:
       - H1: uppercase, font-heading, font-bold
       - Display tag name prominently
       - Back to Blog link: turquoise accent
       - Card grid: same responsive layout
       - No filter buttons needed (already filtered by tag)

       If tag page doesn't exist or is minimal, it may be dynamically generated.
       Check current implementation and apply consistent styling.
  </action>
  <verify>
    - Blog index H1 is uppercase with neobrutalist styling
    - Filter buttons have thick borders and proper active/inactive states
    - Visit http://localhost:4321/blog and verify filter functionality
    - Visit a tag page (e.g., http://localhost:4321/blog/tags/automation) - verify styling matches
    - Load more button (if visible) has neobrutalist styling
    - Dark mode: all elements display correctly
  </verify>
  <done>
    - Blog index has neobrutalist header and filter buttons
    - Tag pages have matching neobrutalist styling
    - Filter button JavaScript updated for new classes
    - Turquoise accent used consistently for blog section
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Blog Post Pages with Two-Tier Typography</name>
  <files>
    - src/pages/blog/[slug].astro
  </files>
  <action>
    Apply two-tier typography approach to blog post pages:

    1. Post header:
       - H1: uppercase, font-heading, text-3xl md:text-4xl, font-bold
       - Date/reading time: keep existing muted styling (readable, not brutalist)
       - Tags: neobrutalist pills with turquoise accent (border-2, rounded-full)
       - Featured image: add subtle border (border-2 border-text-light/20 dark:border-text-dark/20)

    2. Back to Blog link:
       - Use turquoise accent color
       - Keep existing arrow icon

    3. Prose content styling (in the `.prose` wrapper):
       The global.css likely already has prose styles. Add neobrutalist accents:

       - H2 headings: Add left border accent
         * Add border-l-4 border-turquoise pl-4 treatment
         * Keep font-heading, bold
         * Normal case (not uppercase - only H1 is uppercase per design system)

       - H3 headings: Subtle accent
         * Font-heading, semibold
         * Optional: lighter turquoise underline or no accent

       - Body paragraphs: KEEP CLEAN AND READABLE
         * No borders, no shadows, no decorative elements
         * Standard body font (DM Sans)
         * Generous line-height (1.75)
         * This is critical for reading comfort

       - Code blocks: Already styled by astro-expressive-code - leave as is

       - Blockquotes: Keep subtle styling (not brutalist)

    4. If prose styles are in global.css, add scoped overrides in [slug].astro:
       ```css
       .prose :global(h2) {
         border-left: 4px solid var(--color-turquoise);
         padding-left: 1rem;
       }
       ```
       Or add to global.css if prose styles are managed there.

    5. Bottom "Back to Blog" link:
       - Match top link styling (turquoise accent)

    6. Dark mode verification:
       - H2 border accent visible in dark mode
       - Body text readable (good contrast)
       - No glow effects on prose content (too distracting for reading)
  </action>
  <verify>
    - Visit a blog post at http://localhost:4321/blog/[any-slug]
    - H1 is uppercase with neobrutalist treatment
    - H2 headings have turquoise left border accent
    - Body paragraphs are clean and readable (no brutalist treatment)
    - Tags are neobrutalist pills
    - Featured image has subtle border
    - Back to Blog links use turquoise accent
    - Dark mode: all elements readable, H2 accents visible
  </verify>
  <done>
    - Blog posts use two-tier typography (bold headings, clean body)
    - H1 uppercase, H2 with border accent
    - Body text remains readable without brutalist treatment
    - Turquoise accent color used consistently
    - Dark mode works correctly
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Build verification:
   ```bash
   npm run build
   ```
   Should complete without errors.

2. Blog index verification:
   - Visit http://localhost:4321/blog
   - H1 is uppercase with neobrutalist styling
   - Filter buttons have thick borders
   - Cards have thick borders and turquoise shadows
   - Cards hover animation works
   - Filter functionality works

3. Blog card verification:
   - Cards have 4px borders and offset shadows
   - Tab through cards - focus rings visible
   - Dark mode: glow instead of offset shadows

4. Tag page verification:
   - Visit http://localhost:4321/blog/tags/[any-tag]
   - Page has neobrutalist styling matching blog index
   - Cards display correctly

5. Blog post verification:
   - Visit http://localhost:4321/blog/[any-slug]
   - H1 is uppercase
   - H2 has turquoise left border accent
   - Body text is clean and readable
   - Dark mode works

6. Density verification:
   - Blog content follows 2/10 density (headings only have neobrutalist treatment)
   - Body text has no borders, shadows, or decorative elements
</verification>

<success_criteria>
- BlogCard has neobrutalist styling with turquoise accent
- Blog index and tag pages have neobrutalist headers and filter buttons
- Blog posts use two-tier typography (bold headings, clean body)
- All pages work in both light and dark modes
- Readability maintained for long-form content
- Build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/10-projects-blog/10-02-SUMMARY.md`
</output>
