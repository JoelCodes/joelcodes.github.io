# Plan: Code Block Padding Enhancement

---
phase: 17-design-system-reference-page
plan: 07
type: execute
wave: 1
depends_on: []
files_modified: [src/components/design-system/CodeBlock.astro]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Code block has adequate padding so code isn't right at the edge"
  artifacts:
    - path: "src/components/design-system/CodeBlock.astro"
      provides: "Padded code content display"
      contains: ".code-content pre"
  key_links:
    - from: "CodeBlock.astro style block"
      to: "Astro Code component output"
      via: "CSS selector targeting .code-content pre"
      pattern: "\\.code-content pre"
---

<objective>
Verify and enhance code block padding in the design system CodeBlock component.

Purpose: UAT feedback indicated code was "right to the edge" - ensure adequate padding exists and is visually sufficient.
Output: CodeBlock component with clearly padded code content, not cramped against edges.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/design-system/CodeBlock.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance code block padding</name>
  <files>src/components/design-system/CodeBlock.astro</files>
  <action>
1. Check existing `.code-content pre` styles in the `<style>` block (should be around line 123-127)
2. Current padding is `1rem` - this may have been added but could be insufficient
3. If padding exists but is minimal, increase to `1.25rem` or `1.5rem` for more breathing room
4. Ensure the pre element also has proper overflow handling for long code lines
5. Add a subtle background color differentiation if not present (e.g., `bg-bg-dark/5 dark:bg-bg-light/5` equivalent)

Expected final style block:
```css
.code-content pre {
  padding: 1.25rem;
  margin: 0;
  border-radius: 0.375rem;
  overflow-x: auto;
}
```
  </action>
  <verify>
1. Run `npm run dev` and navigate to http://localhost:4321/design-system
2. Click "View Code" on any ComponentShowcase
3. Verify code has visible padding on all sides (not flush against the container edge)
4. Test in both light and dark mode
  </verify>
  <done>Code blocks display with comfortable padding (minimum 1.25rem) and code is clearly not touching the edges of the container.</done>
</task>

</tasks>

<verification>
1. Build passes: `npm run build`
2. Visual check: Code in CodeBlock has adequate breathing room
3. Both modes: Padding visible in light and dark mode
</verification>

<success_criteria>
- CodeBlock code content has at least 1.25rem padding
- Code is visibly separated from container edges
- No visual cramping or edge-touching in either theme
</success_criteria>

<output>
After completion, create `.planning/phases/17-design-system-reference-page/17-07-SUMMARY.md`
</output>
