---
phase: 34-baselayout-chrome
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [CHROME-01, CHROME-02, CHROME-03]
files_modified:
  - src/styles/global.css
autonomous: true
must_haves:
  truths:
    - "The --wl-on-ink token exists in both light and dark and is available to CTA label + footer wordmark (D fidelity)"
    - "Footer-local color tokens exist as CSS custom properties that never flip with .dark (always-dark footer)"
    - "Six chrome-scoped .wl-* type utilities exist at their exact Figma sizes/weights"
    - "Anchor scroll offset matches the new 64px header height"
  artifacts:
    - path: "src/styles/global.css"
      provides: "--wl-on-ink token (light+dark), --wl-footer-* locals, .wl-wordmark/.wl-nav-link/.wl-cta-label/.wl-footer-tagline/.wl-footer-body/.wl-footer-link, scroll-margin-top 64px"
      contains: "--color-wl-on-ink"
  key_links:
    - from: "src/styles/global.css @theme block"
      to: "src/styles/global.css .dark block"
      via: "--color-wl-on-ink defined in both so it flips with theme"
      pattern: "--color-wl-on-ink"
    - from: ":root footer-local tokens"
      to: "SiteFooter.astro (Plan 03)"
      via: "var(--wl-footer-*) references consumed by footer, never flip"
      pattern: "--wl-footer-bg"
---

<objective>
Extend `src/styles/global.css` with the chrome token + utility layer that SiteHeader and SiteFooter
consume: the new `--wl-on-ink` semantic token (light + dark), the always-dark footer-local color
tokens (outside the theme-flip blocks), the six chrome-scoped `.wl-*` type utilities, and the
`scroll-margin-top` update from 60px to 64px for the new header height.

Purpose: All chrome components depend on these tokens/utilities. This is the Wave 1 foundation —
Plan 03 (header/footer) cannot use the correct sizes/colors until these exist. Namespace isolation
holds: only the `--wl-*` block and the `@layer utilities` block are touched; old neobrutalist tokens
are not edited (constraint: old tokens alive until Phase 41).
Output: global.css carrying the on-ink token, footer locals, six utilities, and 64px scroll offset.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-baselayout-chrome/34-UI-SPEC.md
@.planning/phases/34-baselayout-chrome/34-FIGMA-EXTRACTION.md
@.planning/phases/34-baselayout-chrome/34-PATTERNS.md
</context>

<threat_model>
Static-site stylesheet: no runtime input, no secrets, no injection surface. ASVS L1: not applicable
to CSS token definitions. The only correctness risk is a contrast regression — the footer-local
`#8FB4B2` copyright tint on `#0D2A31` is borderline for AA (UI-SPEC Color Contract). Mitigation: run
`scripts/check-contrast.mjs` after defining footer tokens and record the result; if a pair fails AA,
flag it (do NOT silently ship a failing pair — flag per v1.4 "never invent, always flag").
</threat_model>

<tasks>

<task type="execute">
  <name>Task 1: Add --wl-on-ink token and always-dark footer-local color tokens</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (current @theme WL block ~lines 13-37, .dark flip block ~lines 41-50, :root usage)
    - .planning/phases/34-baselayout-chrome/34-FIGMA-EXTRACTION.md (Figma variables table + Footer color inventory)
    - .planning/phases/34-baselayout-chrome/34-UI-SPEC.md (Color Contract + Footer Color Contract)
    - .planning/phases/34-baselayout-chrome/34-PATTERNS.md (global.css section — exact insert points)
  </read_first>
  <action>
    In the `@theme` WL block, after `--color-wl-accent-soft-text`, add `--color-wl-on-ink: #EAF6F3;`
    with a comment noting it participates in the Figma variable system (node 42:27 `--on-ink`), used
    for CTA label on ink bg and footer wordmark. In the `.dark {}` flip block add the dark value for
    `--color-wl-on-ink`: use `#EAF6F3` (it is already a light-on-dark color) UNLESS the Figma dark
    variable mode specifies otherwise — the main session may query `get_variable_defs` dark mode to
    confirm; if not queried, ship `#EAF6F3` for dark and record "dark --wl-on-ink assumed #EAF6F3;
    confirm at fidelity gate" in the summary.
    Add the footer-local tokens in a `:root` block OUTSIDE both `@theme` and `.dark` (so they never
    flip): `--wl-footer-bg: #0D2A31;`, `--wl-footer-tagline-color: #5AA9A5;`,
    `--wl-footer-text-secondary: #7FA4A2;`, `--wl-footer-text-link: #CDE6E5;`,
    `--wl-footer-text-muted: #8FB4B2;`, `--wl-footer-divider: rgba(255,255,255,0.09);`. Comment them
    as "always dark-background, do NOT flip with .dark; local values by Figma designer intent."
    Do NOT edit any old neobrutalist token (`--color-yellow`, `--color-bg-*`, `--color-text-*`).
  </action>
  <verify>grep -n "\-\-color-wl-on-ink\|\-\-wl-footer-bg" src/styles/global.css && npm run build</verify>
  <acceptance_criteria>
    - `grep -c "--color-wl-on-ink" src/styles/global.css` returns 2 (once in @theme, once in .dark)
    - `grep -c "--wl-footer-bg: #0D2A31" src/styles/global.css` returns 1
    - All six `--wl-footer-*` tokens present: `grep -c "\-\-wl-footer-" src/styles/global.css` returns 6 or more
    - The `--wl-footer-*` tokens are NOT inside the `.dark {` block (verify by inspection — they live in a standalone `:root`)
    - No old token names appear in the diff (only `--wl-*` additions)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>--wl-on-ink flips with theme; footer-local tokens exist as non-flipping :root custom properties; build is green.</done>
</task>

<task type="execute">
  <name>Task 2: Add six chrome-scoped .wl-* type utilities and update scroll-margin-top to 64px</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (@layer utilities block, existing .wl-accent-kicker pattern ~lines 700-712, scroll-margin-top rule ~line 147)
    - .planning/phases/34-baselayout-chrome/34-UI-SPEC.md (Typography tables + New CSS Utilities table)
    - .planning/phases/34-baselayout-chrome/34-PATTERNS.md (global.css section — full utility definitions to author)
  </read_first>
  <action>
    In the `@layer utilities` block, following the existing `.wl-accent-kicker` commenting+property
    pattern, add six new utilities with these EXACT values:
    `.wl-wordmark` — Fraunces (var(--font-wl-heading)), 18.4px, 400, line-height normal,
      `font-variation-settings: "SOFT" 0, "WONK" 1`, font-style normal.
    `.wl-nav-link` — Hanken Grotesk (var(--font-wl-body)), 15px, 500, line-height normal, font-style normal.
    `.wl-cta-label` — Hanken Grotesk, 14px, 600, line-height normal, font-style normal.
    `.wl-footer-tagline` — Fraunces, 17px, 400, line-height 1.6, font-style italic,
      `font-variation-settings: "SOFT" 0, "WONK" 1`.
    `.wl-footer-body` — Hanken Grotesk, 14px, 400, line-height 1.6, font-style normal.
    `.wl-footer-link` — Hanken Grotesk, 14px, 400, line-height normal, font-style normal.
    Each utility gets a comment noting the extracted source node and why it is NOT the mismatched
    Phase 33 `.wl-label-button` (16px/600) / `.wl-accent-kicker` (20px) / `.wl-text-body` (16px).
    Separately, change the existing `section[id] { scroll-margin-top: 60px; }` to `64px` to match the
    new header height.
  </action>
  <verify>grep -n "wl-wordmark\|wl-nav-link\|wl-cta-label\|wl-footer-tagline\|wl-footer-body\|wl-footer-link\|scroll-margin-top" src/styles/global.css && npm run build</verify>
  <acceptance_criteria>
    - All six utility selectors present: `grep -c "\.wl-wordmark\|\.wl-nav-link\|\.wl-cta-label\|\.wl-footer-tagline\|\.wl-footer-body\|\.wl-footer-link" src/styles/global.css` returns 6 or more
    - `.wl-wordmark` block contains `font-size: 18.4px` and `font-variation-settings: "SOFT" 0, "WONK" 1`
    - `.wl-nav-link` block contains `font-size: 15px` and `font-weight: 500`
    - `.wl-cta-label` block contains `font-size: 14px` and `font-weight: 600`
    - `.wl-footer-tagline` block contains `font-style: italic` and `line-height: 1.6`
    - `grep -c "scroll-margin-top: 64px" src/styles/global.css` returns 1 and `grep -c "scroll-margin-top: 60px" ...` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Six chrome-scoped utilities exist at exact Figma sizes/weights; scroll offset is 64px; build is green.</done>
</task>

<task type="execute">
  <name>Task 3: Verify footer-tint contrast against the footer background</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (the footer-local tokens added in Task 1)
    - .planning/phases/34-baselayout-chrome/34-UI-SPEC.md (WCAG Contrast on Footer Background table)
    - scripts/check-contrast.mjs (contrast checker — confirm invocation signature)
  </read_first>
  <action>
    Run `scripts/check-contrast.mjs` (or the project's contrast tool) against the footer pairs:
    `#EAF6F3`, `#CDE6E5`, `#5AA9A5`, `#7FA4A2`, `#8FB4B2` each on `#0D2A31`. Record each ratio.
    The borderline pair is `#8FB4B2` (copyright/muted) on `#0D2A31` — it is used only for the
    copyright line and dev-only muted blog link (both small non-essential text). If any AA-relevant
    pair fails, do NOT alter the Figma-locked hex — instead FLAG it in the summary as a fidelity
    tension (per v1.4 "flag, never invent") for Joel to decide at the screenshot gate. If all pass
    (or the failing pairs are decorative/copyright per the UI-SPEC note), record PASS with ratios.
  </action>
  <verify>node scripts/check-contrast.mjs 2>/dev/null || echo "record ratios manually per UI-SPEC table"</verify>
  <acceptance_criteria>
    - Contrast ratios for all five footer text colors on `#0D2A31` are recorded in the summary (numeric values)
    - Any pair below AA is explicitly flagged in the summary with its ratio (not silently accepted)
    - No footer hex value is changed away from its Figma-extracted value as a result of this check
  </acceptance_criteria>
  <done>Footer contrast is measured and recorded; any AA tension is flagged for the gate rather than silently altering Figma-locked colors.</done>
</task>

</tasks>

<verification>
- `npm run build` green.
- `--color-wl-on-ink` present in both `@theme` and `.dark`.
- Six `.wl-*` chrome utilities present at exact sizes/weights.
- `scroll-margin-top` is 64px.
- Footer contrast ratios recorded; failures flagged.
</verification>

<success_criteria>
The chrome token + utility foundation exists in global.css so SiteHeader/SiteFooter (Plan 03) can be
authored entirely against `--wl-*` tokens and `.wl-*` utilities with no old-token leakage.
</success_criteria>

<output>
After completion, create `.planning/phases/34-baselayout-chrome/34-02-SUMMARY.md`.
</output>
