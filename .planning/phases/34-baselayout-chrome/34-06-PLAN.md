---
phase: 34-baselayout-chrome
plan: 06
type: execute
wave: 4
depends_on: ["34-05"]
requirements: [CHROME-02, CHROME-03, CHROME-04]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "Rendered SiteHeader (desktop 1440 + mobile 390) matches Figma nodes 42:29 / 42:47 within the approved deviations"
    - "Rendered SiteFooter (desktop) matches Figma nodes 42:77 / 42:104"
    - "Dark-mode chrome matches Figma dark instances (header 117:104; footer always-dark)"
    - "Joel approves the fidelity comparison before the phase is marked done (v1.4 milestone discipline)"
  artifacts:
    - path: ".planning/phases/34-baselayout-chrome/34-REVIEW.md"
      provides: "Screenshot comparison record + approved deviations + Joel's sign-off"
      contains: "Approved deviations"
  key_links:
    - from: "rendered chrome screenshots (1440 + 390, light + dark)"
      to: "Figma frames 42:29 / 42:47 / 42:77 / 42:104 / 117:104"
      via: "side-by-side comparison approved by Joel"
      pattern: "42:29"
---

<objective>
Run the phase-closing Figma-frame vs. rendered-screenshot fidelity gate for the Wavelength chrome and
get Joel's approval before Phase 34 is marked done. This is the v1.4 milestone discipline (every UI
phase ends with a fidelity comparison) and a locked phase-gate task per 34-CONTEXT.md. It captures the
rendered SiteHeader (desktop 1440 + mobile 390) and SiteFooter, compares them against the Figma frames,
documents the two approved deviations, and records Joel's sign-off.

Purpose: Fidelity gate — the phase cannot close without it. Depends on the live chrome from Plan 05.
Output: A 34-REVIEW.md fidelity record and Joel's explicit approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-baselayout-chrome/34-UI-SPEC.md
@.planning/phases/34-baselayout-chrome/34-FIGMA-EXTRACTION.md
@.planning/phases/34-baselayout-chrome/34-VALIDATION.md
</context>

<threat_model>
Verification-only plan: captures screenshots and records approval. No code changes, no user input,
no runtime surface. ASVS L1: not applicable. The only integrity concern is that the approved
deviations (mark-only mobile wordmark; no theme toggle) are honestly documented so the gate is not
falsely passed — mitigated by listing them explicitly in 34-REVIEW.md.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Capture rendered chrome screenshots and assemble the fidelity record</name>
  <files>.planning/phases/34-baselayout-chrome/34-REVIEW.md</files>
  <read_first>
    - .planning/phases/34-baselayout-chrome/34-UI-SPEC.md (Fidelity Gate table + Approved deviations)
    - .planning/phases/34-baselayout-chrome/34-FIGMA-EXTRACTION.md (Remaining flags for the screenshot gate)
    - src/components/layout/SiteHeader.astro (as rendered)
    - src/components/layout/SiteFooter.astro (as rendered)
  </read_first>
  <action>
    Build/preview the site (`npm run build && npm run preview`, or `npm run dev`). Using Playwright or
    the browser, capture rendered screenshots of: SiteHeader at 1440px (light), SiteHeader at 390px
    (light), SiteHeader at 1440px (dark, via OS/colorScheme dark), SiteFooter at 1440px (light),
    SiteFooter at 1440px (dark). Save them under a phase-local screenshots folder.
    Create `34-REVIEW.md` capturing, per comparison row (from the UI-SPEC Fidelity Gate table): the
    Figma node, the viewport, the rendered screenshot path, and a notes/observations field for spacing
    and typography deltas. Explicitly list the APPROVED DEVIATIONS section:
    (a) mobile wordmark hidden — mark-only at <640px (D-02, user-approved due to 390px overflow;
    Figma shrinks to 15px rather than hiding);
    (b) no theme toggle anywhere (D-05; Figma never had one — this MATCHES Figma).
    Add a fidelity-gate CHECKLIST item to VERIFY at approval time: the dark-header WaveMark
    "circle-badge" treatment (Figma dark instance 117:104 renders the mark inside a light circular
    badge) — compare rendered dark header against 117:104 and note whether the current bare-stroke
    WaveMark needs a circled variant, and confirm the dark `--wl-on-ink` value against the Figma dark
    variable mode. Leave the sign-off line blank for Joel.
  </action>
  <verify>ls .planning/phases/34-baselayout-chrome/34-REVIEW.md && grep -c "Approved deviations\|42:29\|42:77" .planning/phases/34-baselayout-chrome/34-REVIEW.md</verify>
  <acceptance_criteria>
    - `34-REVIEW.md` exists with a comparison row for each of: SiteHeader desktop light, SiteHeader mobile light, SiteHeader desktop dark, SiteFooter desktop light, SiteFooter desktop dark
    - Each row references its Figma node (42:29 / 42:47 / 42:77 / 42:104 / 117:104) and a rendered screenshot path
    - An "Approved deviations" section lists both deviations (mark-only mobile wordmark; no theme toggle) with their D-references
    - A gate checklist item exists for the dark-header WaveMark circle-badge treatment and the dark --wl-on-ink confirmation
    - Rendered screenshots exist on disk at the referenced paths
  </acceptance_criteria>
  <done>The fidelity record is assembled with all five comparison rows, screenshots, approved deviations, and the dark-header/on-ink checklist item — ready for Joel's review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Wavelength SiteHeader and SiteFooter are live site-wide with system-only dark mode. Plan 06
    Task 1 captured rendered screenshots (header desktop 1440 + mobile 390, footer desktop; light + dark)
    and assembled 34-REVIEW.md with the Figma comparison rows and approved deviations.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/34-baselayout-chrome/34-REVIEW.md` and the referenced screenshots.
    2. Open the Figma file `1tg8wIPcvOVC5tPZ8pkGO2` to frames 42:29 (header desktop), 42:47 (header
       mobile), 42:77 (footer desktop), and dark Landing 117:103 (header dark 117:104).
    3. Compare each rendered screenshot against its Figma frame for spacing, typography, and color.
    4. Confirm the two APPROVED DEVIATIONS are acceptable: mark-only mobile wordmark (< 640px), and no
       theme toggle (matches Figma).
    5. Resolve the dark-header checklist item: does the WaveMark need a light circular badge on the dark
       header (per 117:104), or is the bare-stroke mark acceptable? Confirm the dark `--wl-on-ink` value.
    6. Optionally run `npm run dev` and view the live chrome at 1440px and 390px, light and dark OS.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close the fidelity gate, or describe any spacing/typography/color deltas or the
    dark-header badge decision to fix before approval (fixes route back to Plan 04 chrome components /
    Plan 02 tokens).
  </resume-signal>
</task>

</tasks>

<verification>
- `34-REVIEW.md` exists with all five comparison rows, screenshots, and approved deviations.
- Joel has typed "approved" (or requested deltas that were then addressed and re-approved).
</verification>

<success_criteria>
The Figma-vs-rendered fidelity gate for SiteHeader (desktop + mobile) and SiteFooter is documented and
approved by Joel, satisfying the phase-closing milestone discipline so Phase 34 can be marked done.
</success_criteria>

<output>
After completion, create `.planning/phases/34-baselayout-chrome/34-06-SUMMARY.md`.
</output>
