---
phase: 34-baselayout-chrome
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [CHROME-01]
files_modified:
  - tests/accessibility/dark-mode.spec.ts
autonomous: true
must_haves:
  truths:
    - "dark-mode.spec.ts drives dark mode via prefers-color-scheme, not a DOM toggle (D-05/D-06)"
    - "The dark-mode a11y suite passes against the new chrome with zero references to #theme-toggle"
  artifacts:
    - path: "tests/accessibility/dark-mode.spec.ts"
      provides: "colorScheme-context dark + light axe scans; no #theme-toggle references"
      contains: "colorScheme"
  key_links:
    - from: "tests/accessibility/dark-mode.spec.ts"
      to: "html.dark class set by FOUC script"
      via: "browser.newContext({ colorScheme: 'dark' }) + expect(html).toHaveClass(/dark/)"
      pattern: "colorScheme:\\s*'dark'"
---

<objective>
Rewrite the four dark-mode accessibility tests to drive dark mode via Playwright's `colorScheme`
browser context instead of clicking `#theme-toggle`. This is the Wave 0 requirement from
34-VALIDATION.md: the current tests hard-fail the moment the toggle is removed from the chrome
(Pitfall 5), so the rewrite MUST land before any chrome/BaseLayout change removes the toggle.

Purpose: Keep the a11y test suite green throughout the phase so subsequent chrome tasks have a
reliable feedback signal. This is a test-first task per <tdd_mode_active>.
Output: A rewritten dark-mode.spec.ts that emulates OS dark preference and verifies `.dark` is set.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/34-baselayout-chrome/34-VALIDATION.md
@.planning/phases/34-baselayout-chrome/34-RESEARCH.md
@.planning/phases/34-baselayout-chrome/34-PATTERNS.md
</context>

<threat_model>
This is a static-site test file with no runtime attack surface, no user input, no secrets.
ASVS L1 relevant items: none applicable to a Playwright spec. The only correctness concern is that
the rewritten test must still ASSERT the dark class is applied (a false-green test that silently
passes without verifying `.dark` would hide a real FOUC/dark-mode regression). Mitigation: the dark
test retains `expect(html).toHaveClass(/dark/)`.
</threat_model>

<tasks>

<task type="tdd">
  <name>Task 1: Rewrite dark-mode.spec.ts to colorScheme contexts (RED → GREEN)</name>
  <files>tests/accessibility/dark-mode.spec.ts</files>
  <read_first>
    - tests/accessibility/dark-mode.spec.ts (current toggle-based tests — see current #theme-toggle usage)
    - tests/accessibility/axe-tests.spec.ts (clean baseline: wcagTags array, AxeBuilder pattern, per-page structure)
    - .planning/phases/34-baselayout-chrome/34-PATTERNS.md (dark-mode.spec.ts section — full new pattern)
    - .planning/phases/34-baselayout-chrome/34-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Replace every `page.locator('#theme-toggle').click()` flow with the Playwright OS-preference
    pattern. Each dark test must: take the `{ browser }` fixture, create
    `const context = await browser.newContext({ colorScheme: 'dark' })`, `await context.newPage()`,
    `page.goto(...)`, assert `await expect(page.locator('html')).toHaveClass(/dark/)`, run the
    AxeBuilder scan with the same `wcagTags` array used in axe-tests.spec.ts
    (`['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']`), assert `results.violations` equals `[]`,
    then `await context.close()`.
    Remove all `page.waitForTimeout(500)` calls (the FOUC script is synchronous; no transition wait).
    Cover these targets: Homepage `/` in dark, Projects `/projects` in dark, Homepage `/` in light
    (using `colorScheme: 'light'`). REMOVE any `/blog` dark test — `/blog` returns 404 in the prod
    build the suite runs against (D-13). Keep the describe block named "Dark Mode Accessibility".
    Do NOT reference `#theme-toggle`, `localStorage`, or any DOM toggle anywhere in the file.
    This runs BEFORE the toggle is removed, so the RED state is: tests currently reference a toggle
    that still exists; after rewrite they must pass against the CURRENT build too (the old FOUC script
    still sets `.dark` from OS preference when no localStorage.theme is set, so `colorScheme: 'dark'`
    produces the `.dark` class today as well). Verify GREEN against the current build.
  </action>
  <verify>npm run build && npx playwright test tests/accessibility/dark-mode.spec.ts</verify>
  <acceptance_criteria>
    - `grep -c "theme-toggle" tests/accessibility/dark-mode.spec.ts` returns 0
    - `grep -c "waitForTimeout" tests/accessibility/dark-mode.spec.ts` returns 0
    - `grep -c "colorScheme" tests/accessibility/dark-mode.spec.ts` returns 3 or more
    - File contains at least one `await expect(page.locator('html')).toHaveClass(/dark/)` assertion
    - File contains no `/blog` navigation (`grep -c "goto('/blog')" ...` returns 0)
    - `npx playwright test tests/accessibility/dark-mode.spec.ts` exits 0 (all tests pass against current build)
  </acceptance_criteria>
  <done>The dark-mode a11y suite drives dark mode via colorScheme context, asserts the .dark class, passes green, and contains zero toggle references — safe for the toggle to be removed downstream.</done>
</task>

</tasks>

<verification>
- `npm run build` stays green.
- `npx playwright test tests/accessibility/dark-mode.spec.ts` passes with zero violations.
- No `#theme-toggle` / `localStorage` / `waitForTimeout` remain in the spec.
</verification>

<success_criteria>
The dark-mode accessibility spec is toggle-independent and green, unblocking the FOUC-script
simplification and chrome swap in later waves without breaking the test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/34-baselayout-chrome/34-01-SUMMARY.md`.
</output>
