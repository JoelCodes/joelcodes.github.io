---
phase: 29-projects-reconstruction-index-project-detail
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - PAGE-02
gap_closure: false

must_haves:
  truths:
    - "Compound / ProjectCard ships inside t67DU6 as a NEW reusable component (sibling of t40xct + ZSxZU) with a 4-slot signature (image-slot + category-badge-slot + title-slot + body-slot) per D-131 + D-132"
    - "Section / ResultsMetrics ships inside g9oRa5 as a NEW reusable component with heading-slot + 3-fixed metric-tile composition per D-135"
    - "Section / RelatedProjects ships inside g9oRa5 as a NEW reusable component with heading-slot + cards-row (3 ProjectCard refs default) per D-134"
    - "Zero mutations to Phase 23-28 ship-set (all baseline IDs intact)"
    - "Token surface drift matches D-135 outcome (unchanged if heading-1 reuse default holds; +4 composite vars if type-semantic-metric-large ships)"
    - "Layout sweep at document root returns 'No layout problems.' at plan close"
    - "PEN-INVENTORY extended with Variant Evidence rows for the 3 new components + any Badge variant + any new token; library count moves 25 → 28 (or 29 if Badge variant ships)"
  artifacts:
    - path: "design/Crito.pen"
      provides: "3 NEW reusable components inside t67DU6 (ProjectCard) and g9oRa5 (ResultsMetrics + RelatedProjects); possible new Badge variant inside avgor; possible new type-semantic-metric-large token surface"
      contains: "Compound / ProjectCard reusable:true with 4 slots; Section / ResultsMetrics reusable:true with heading + 3 metric-tile children; Section / RelatedProjects reusable:true with heading + 3-ProjectCard cards-row"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Variant Evidence rows + possible Token Extensions row + library count update"
      contains: "Plan 29-00 additions section with ProjectCard / ResultsMetrics / RelatedProjects evidence rows"
  key_links:
    - from: "Section / RelatedProjects cards-row"
      to: "Compound / ProjectCard (new id)"
      via: "slot: ['<new-projectcard-id>'] TYPED suggestion + 3 default ref children"
      pattern: "cards-row slot typed against new ProjectCard id with 3 ref children at default"
    - from: "Compound / ProjectCard category-badge-slot"
      to: "Badge primitive (j0FxQZ default or kJQmJ Outline)"
      via: "slot: ['j0FxQZ'] TYPED suggestion (default lean per CONTEXT) — plan-execution may switch to kJQmJ if image-import-12.jpg shows outline treatment"
      pattern: "category-badge-slot typed against chosen Badge primitive"
    - from: "Section / ResultsMetrics metric-tile metric-value"
      to: "type-semantic-heading-1 (default) OR type-semantic-metric-large (new, if mid-plan gate fires)"
      via: "Plus Jakarta Sans 48px (heading-1 default) bound at text fontFamily/fontSize/fontWeight/lineHeight; OR 60px new composite if shipped"
      pattern: "metric-value text node bound to selected token surface per D-135 evaluation"
---

<objective>
Ship 3 NEW reusable library components inside design/Crito.pen as the foundation for Phase 29 per-page reconstructions:

1. **Compound / ProjectCard** (sibling of t40xct + ZSxZU inside t67DU6) — narrow-scoped per D-131; 4-slot signature per D-132 (image-slot + category-badge-slot + title-slot + body-slot); upfront commit to skip Plan 28-01 BlogCard REVISE-loop risk per D-131 evidence (image-import-12.jpg + v1.3 ProjectCard.astro provide enough source).

2. **Section / ResultsMetrics** (NEW sibling inside g9oRa5) — Joel-content essential per D-129 + D-135; heading-slot + metrics-row with 3 FIXED metric-tile children (NOT a slot — narrow-scoped per D-135).

3. **Section / RelatedProjects** (NEW sibling inside g9oRa5) — mirrors etY5x Section/RelatedPosts shape per D-134; heading-slot + cards-row with 3 ProjectCard refs (vs etY5x's 3 t40xct refs).

Plus possible 4th library entry (Badge variant) per D-132 plan-execution + possible 1 new token (type-semantic-metric-large) per D-135 plan-execution. Library count moves 25 → 28 (or 29 if Badge variant ships).

Purpose: Phase 29 per-page plans (29-01 Projects index + 29-02 Project detail) depend on these components. Foundation-first ordering per D-138 mirrors Phase 28 Plan 28-00 + Phase 27 Plan 27-00 patterns.

Output:
- design/Crito.pen mutated with 3 new reusable components (+ possible Badge variant + possible new token)
- .planning/research/PEN-INVENTORY.md extended with Variant Evidence rows + library count update
- Plan-close SUMMARY at .planning/phases/29-projects-reconstruction-index-project-detail/29-00-SUMMARY.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md

**CRITICAL — INLINE EXECUTION REQUIRED:** Pencil MCP tools (mcp__pencil__*) are NOT inherited by spawned gsd-executor subagents per Plan 28-00 + 27-00 carry-forward. Every task in this plan MUST execute INLINE by the main orchestrator. Do NOT spawn a subagent.

**PRE-FLIGHT ENFORCEMENT per D-140:** Every Pencil-mutating call (set_variables, batch_design, find_empty_space_on_canvas, Update, Delete) MUST be preceded by `mcp__pencil__get_editor_state({include_schema: false})` asserting `active editor == "design/Crito.pen"`. Halt and surface to user on mismatch.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-VALIDATION.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PITFALLS.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@src/components/ProjectCard.astro
@src/data/projects.json
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + baseline ID verification + library scan</name>
  <files>design/Crito.pen (read-only probe)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md (D-127..D-141 locked decisions)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Validation Architecture + § Standard Stack (Pencil MCP tool catalogue)
    - .planning/research/PEN-INVENTORY.md (baseline IDs + library counts after Phase 28)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.4 step 1-2 (pre-flight + baseline check)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md (Phase 28 foundation pattern carry-forward — TagFilter + RelatedPosts + Badge/Outline ship-set)
  </read_first>

  <action>
    1. Call `mcp__pencil__get_editor_state({include_schema: false})`. Assert `active editor == "design/Crito.pen"`. On mismatch: HALT, surface to user with diagnostic.

    2. Call `mcp__pencil__get_guidelines({topic: "design-system"})` once to refresh slot mechanics (Component authoring guidance for the 3 new components per CONTEXT line 230).

    3. Call `mcp__pencil__get_variables({})` to record OPEN baseline token surface count. Expected: 127 user-facing tokens (or 107 logical per plan-spec convention — Phase 28 plan-spec inconsistency known per Plan 28-03 SUMMARY line 21). Record actual count in plan SUMMARY for drift comparison at plan close.

    4. Call `mcp__pencil__batch_get` with the baseline ID array below to verify ZERO regression of Phase 23-28 ship-set. Expected: all return existing structure unchanged.

       ```
       batch_get({
         nodeIds: ['avgor', 't67DU6', 'g9oRa5', 'RpGbe',
                   't40xct', 'ZSxZU',
                   'O1IwyS', 'etY5x', 'Hs5rc', 'N1jo3i', 'G0wNOc', 'Xs0Hs',
                   'M7eUr', 'hIWuC', 'j0FxQZ', 'kJQmJ', 'nwJk7',
                   'EQaMf', 'yRvGb', 'u7NmaS', 'dpO5Y',
                   'vGH3A', 'SoXch', 'FGdti', 'oTSwn', 'eNqxd',
                   'Y2isa', 'cYlRH'],
         readDepth: 1
       })
       ```

    5. Call `mcp__pencil__batch_get(['t67DU6', 'g9oRa5'], readDepth: 2)` to enumerate current children counts. Expected per Plan 28-03 SUMMARY: t67DU6 = 3 children (Compound/Card t40xct + Compound/BlogCard ZSxZU + parent metadata note); g9oRa5 = 6 children (Header G0wNOc + Footer Xs0Hs + CTA Hs5rc + NavBack N1jo3i + TagFilter O1IwyS + RelatedPosts etY5x). Record in SUMMARY.

    6. Verify Y2isa + cYlRH are still `enabled: true` (NOT prematurely hidden — confirms PAGE-11 ACTIVE for Plans 29-01/02 still applies). Record both `fill.image` filenames if returned at readDepth 1 (likely image-import-NN.jpg paths — defer to Plan 29-01/02 Task 0 for confirmation).
  </action>

  <verify>
    - `mcp__pencil__get_editor_state` returns `active editor == "design/Crito.pen"`
    - `mcp__pencil__get_variables({})` returns count == 127 (or known 107 logical) — record exact actual
    - `mcp__pencil__batch_get` returns existing structure for all 28 baseline IDs (no nulls/errors)
    - t67DU6 child count == 3; g9oRa5 child count == 6
    - Y2isa.enabled == true AND cYlRH.enabled == true
  </verify>

  <acceptance_criteria>
    - [ ] `mcp__pencil__get_editor_state({include_schema: false})` returns active editor == `design/Crito.pen`
    - [ ] `mcp__pencil__get_variables({})` returns a recorded count (record actual in SUMMARY for drift comparison)
    - [ ] `mcp__pencil__batch_get` returns existing structure for all 28 baseline IDs (avgor, t67DU6, g9oRa5, RpGbe, t40xct, ZSxZU, O1IwyS, etY5x, Hs5rc, N1jo3i, G0wNOc, Xs0Hs, M7eUr, hIWuC, j0FxQZ, kJQmJ, nwJk7, EQaMf, yRvGb, u7NmaS, dpO5Y, vGH3A, SoXch, FGdti, oTSwn, eNqxd, Y2isa, cYlRH); zero nulls
    - [ ] t67DU6 children count == 3 (t40xct + ZSxZU + Plan 25 deferral note)
    - [ ] g9oRa5 children count == 6 (G0wNOc + Xs0Hs + Hs5rc + N1jo3i + O1IwyS + etY5x)
    - [ ] Y2isa.enabled == true AND cYlRH.enabled == true at probe (PAGE-11 ACTIVE prerequisite preserved)
  </acceptance_criteria>

  <done>Pre-flight verified, baseline ship-set confirmed intact, library counts recorded for drift comparison at plan close.</done>
</task>

<task type="auto">
  <name>Task 1: Build Compound / ProjectCard inside t67DU6 (NEW per D-131 + D-132)</name>
  <files>design/Crito.pen (mutation: NEW reusable Compound inside t67DU6)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-131 + D-132)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Code Examples § Example 1 (Compound/ProjectCard slot signature)
    - .planning/research/PEN-INVENTORY.md lines 695-702 (Plan 28-01 BlogCard precedent — slot signature reference)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md (BlogCard birth + image-import-14.jpg pairing pattern)
    - .planning/research/PITFALLS.md Pitfall 1 (D-131 D-132 upfront-commit Pitfall-1 tension acknowledged — dual evidence: image-import-12.jpg + v1.3 src) + Pitfall 7 (drop CTA slots)
    - src/components/ProjectCard.astro (v1.3 ProjectCard slot signature source per D-132)
    - .planning/research/CALIBRATION-PROTOCOL.md § 8 (single-file strategy + library parent placement)
  </read_first>

  <action>
    PRE-FLIGHT (mandatory per D-140): `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    Then EVALUATE Badge variant choice per D-132 plan-execution evaluation (CONTEXT Open Question 2):
    - Probe `mcp__pencil__batch_get(['j0FxQZ', 'kJQmJ'], readDepth: 1)` to confirm both Badge primitives present.
    - Default lean per CONTEXT Claude's Discretion: `j0FxQZ` (Badge/Default solid) — stronger affordance reads as category indicator than Outline.
    - Plan-execution may swap to `kJQmJ` if intuition reads outline-better at probe — default solid for upfront commit.
    - If image-import-12.jpg evaluation surfaces a clear new-variant need (rare — Pitfall 7 prevention), raise mid-plan AskUserQuestion per Phase 28 Plan 28-00 D-119 + Plan 28-01 D-115 precedent. Default: ship the chosen existing primitive id in the slot signature, NO new variant.

    Then BUILD Compound/ProjectCard via a single `mcp__pencil__batch_design` call (split into multiple if >25 ops cap per Plan 27-02 Pitfall 9). Operations (per Example 1 in 29-RESEARCH.md):

    ```
    batch_design({
      operations: [
        // 1. Parent frame (sibling of t40xct + ZSxZU inside t67DU6)
        Insert(t67DU6, {
          type: "frame",
          reusable: true,
          name: "Compound / ProjectCard",
          layout: "vertical",
          gap: 0,
          padding: 0,
          width: 380,            // matches Plan 28-01 BlogCard 3-col math (1200 - 2*24 / 3 = 384, rounded 380)
          cornerRadius: 10,       // radius-semantic-card
          fill: "#ffffffff",      // color-semantic-bg-page
          stroke: "#d4d4d8ff",    // color-semantic-border-default (reserved for forward-state variants)
          strokeWidth: 0,         // borderless per Crito-vocab register (Plan 28-01 BlogCard precedent)
          strokeAlignment: "inner",
          clip: true              // for image top-corner rounding
        }) → record returned id as <projectCard-id>

        // 2. image-slot (16:9 placeholder per v1.3 ProjectCard.astro line 24 aspect-ratio)
        Insert(<projectCard-id>, {
          type: "frame",
          name: "image-slot",
          slot: [],               // untyped per BlogCard precedent (consumers populate fill at instance time)
          enabled: true,          // default-ON per D-53 placeholder pattern
          layout: "vertical",
          justifyContent: "center",
          alignItems: "center",
          width: "fill_container",
          height: 240,            // matches Plan 28-01 BlogCard prominence (240h)
          fill: "#f2f2f7ff"       // color-semantic-bg-surface-elevated
        }) → record returned id as <image-slot-id>

        // 2a. image-slot placeholder text
        Insert(<image-slot-id>, {
          type: "text",
          content: "Project image",
          fontFamily: "Inter",
          fontSize: 14,
          fontWeight: "500",
          fill: "#52525bff",      // color-semantic-text-secondary
          lineHeight: 1.5
        })

        // 3. content-area frame
        Insert(<projectCard-id>, {
          type: "frame",
          name: "content-area",
          layout: "vertical",
          gap: 12,                // space-semantic-stack-sm-adjacent (matches BlogCard rhythm)
          padding: 24,            // space-primitive-24 per Pencil § 12 "Inside cards"
          width: "fill_container"
        }) → record returned id as <content-area-id>

        // 3a. category-badge-slot — TYPED per D-132 + D-52 PREFERRED
        Insert(<content-area-id>, {
          type: "frame",
          name: "category-badge-slot",
          slot: ["j0FxQZ"],       // DEFAULT TYPED — Badge/Default solid per CONTEXT Claude's Discretion lean
                                  // (plan-execution may swap to ["kJQmJ"] Badge/Outline if image-import-12.jpg shows outline treatment)
          layout: "horizontal",
          alignItems: "center",
          width: "fill_container"
        }) → record returned id as <category-badge-slot-id>

        // 3a-i. Default Badge ref child (instance-time consumers override label)
        Insert(<category-badge-slot-id>, {
          type: "ref",
          ref: "j0FxQZ",          // matches typed slot suggestion (or "kJQmJ" if swapped)
          descendants: {
            // Plan 28-01 BlogCard precedent: descendants override on Badge label child at instance time
            // For default at component definition, leave label as Badge default
          }
        })

        // 3b. title-slot — heading-3 per D-132 + Plan 28-01 BlogCard heading-3 first-consumer precedent
        Insert(<content-area-id>, {
          type: "text",
          name: "title",
          content: "Project title placeholder",
          fontFamily: "Plus Jakarta Sans",   // type-semantic-heading-3-family
          fontSize: 24,                       // type-semantic-heading-3-size (CONTEXT Claude's Discretion default; heading-4 20px also valid per v1.3 text-xl — plan-execution may downsize at Plan 29-01 calibration if visual reads too heavy)
          fontWeight: "700",                  // type-semantic-heading-3-weight
          fill: "#141f39ff",                  // color-semantic-text-primary
          lineHeight: 1.4,                    // type-semantic-heading-3-lh
          textGrowth: "fixed-width",
          width: "fill_container"
        })

        // 3c. body-slot — problem excerpt 160-char truncate convention per v1.3 ProjectCard.astro lines 14-16
        Insert(<content-area-id>, {
          type: "text",
          name: "body",
          content: "Project problem excerpt placeholder describing the challenge briefly. Replace at instance time with first 160 chars of project.problem from projects.json.",
          fontFamily: "Inter",                // type-semantic-prose-paragraph-family
          fontSize: 16,                       // type-semantic-prose-paragraph-size
          fontWeight: "400",                  // type-semantic-prose-paragraph-weight
          fill: "#52525bff",                  // color-semantic-text-secondary
          lineHeight: 1.625,                  // type-semantic-prose-paragraph-lh
          textGrowth: "fixed-width",
          width: "fill_container"
        })
      ]
    })
    ```

    4. Insert sibling Pencil note at CANVAS ROOT (NOT inside t67DU6 — per Plan 28-01 quirk: notes inside parent frames don't follow flex layout the same way and double-count height):

    ```
    batch_design({
      operations: [
        Insert(<document-root>, {
          type: "note",
          name: "Compound / ProjectCard note",
          content: "Compound / ProjectCard slot signature (Plan 29-00 per D-131 + D-132):\n" +
                   "- image-slot (untyped slot, 240h, color-semantic-bg-surface-elevated placeholder; consumers populate fill: {type: 'image', url: '<project.thumbnail>'} at instance time OR replace whole slot at code milestone)\n" +
                   "- content-area (24 padding, 12 gap)\n" +
                   "  - category-badge-slot (TYPED slot ['j0FxQZ' OR 'kJQmJ'] — see plan-execution choice; default Badge ref child with consumer-overridable label)\n" +
                   "  - title (heading-3 PJS 24/700/lh 1.4 navy — heading-3 second-consumer after BlogCard; plan-execution may downsize to heading-4 20px at Plan 29-01 calibration if visual reads heavy)\n" +
                   "  - body (prose-paragraph Inter 16/400/lh 1.625 secondary; consumers override with first 160 chars of project.problem from projects.json)\n\n" +
                   "Narrow-scope rationale per D-131: sibling of t40xct + ZSxZU. t40xct continues serving Phase 27 Contact sidebar + Phase 28 RelatedPosts + future Phase 31 Homepage. NOT generalized for any other consumer — Phase 31 may build its own card if needed.\n\n" +
                   "Provenance: image-import-12.jpg lower 2x2 Project grid (Phase 25 D-49 raster-probe identification) + v1.3 src/components/ProjectCard.astro (lines 19-52 slot derivation, lines 14-16 160-char truncate convention).\n\n" +
                   "Crito-visual register per D-58 + D-132: NO v1.3 yellow neobrutalist chrome (4px border + 6px hard shadow + uppercase Bricolage Grotesque); borderless white card per Plan 28-01 BlogCard precedent.\n\n" +
                   "Hover/focus states deferred to code milestone per Pitfall 7 (clickable-card pattern is interactive state; static design tool ships READING state per D-92/D-59 carry-forward).",
          width: 1200,
          height: 320
        })
      ]
    })
    ```

    5. Verify with `mcp__pencil__batch_get(['<projectCard-id>'], readDepth: 3)` that the structure matches:
       - reusable: true, name: "Compound / ProjectCard"
       - Children: image-slot + content-area
       - content-area children: category-badge-slot + title + body
       - All slots `enabled: true` per D-53
  </action>

  <verify>
    - `mcp__pencil__batch_get(['<projectCard-id>'], readDepth: 3)` returns reusable:true, name "Compound / ProjectCard", with 2 children (image-slot + content-area); content-area has 3 children (category-badge-slot + title + body)
    - `mcp__pencil__batch_get(['t67DU6'], readDepth: 1)` returns 4 children (Plan 25 deferral note + t40xct + ZSxZU + NEW ProjectCard)
    - Sibling Pencil note exists at document root
  </verify>

  <acceptance_criteria>
    - [ ] NEW Compound/ProjectCard reusable component exists inside t67DU6 with reusable:true
    - [ ] Slot signature verified: image-slot (untyped, enabled:true, 240h) + content-area (24 padding, 12 gap) containing category-badge-slot (typed ['j0FxQZ'] OR ['kJQmJ']) + title (heading-3 PJS 24/700/lh 1.4 navy) + body (prose-paragraph Inter 16/400/lh 1.625 secondary)
    - [ ] All slot frames `enabled: true` per D-53 placeholder pattern
    - [ ] Sibling Pencil note at canvas root documents 4-slot signature + narrow-scope rationale + provenance + Crito-visual-register decision + hover/focus deferral
    - [ ] t67DU6 children count moves 3 → 4
  </acceptance_criteria>

  <done>Compound/ProjectCard structurally complete; ready for instance consumption by Plan 29-01 (6 grid instances) + Plan 29-02 (RelatedProjects 2-3 instances).</done>
</task>

<task type="auto">
  <name>Task 2: Build Section / ResultsMetrics inside g9oRa5 (NEW per D-135) + possible mid-plan token gate</name>
  <files>design/Crito.pen (mutation: NEW reusable Section inside g9oRa5; possible new token primitives in RpGbe)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-135) + § Claude's Discretion (metric-value typography + chrome treatment)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Code Examples § Example 2 (ResultsMetrics 3 fixed metric-tile composition) + § Open Questions (3 + 4)
    - .planning/research/PEN-INVENTORY.md § Token Extensions (Phase 26 + Phase 28 — type-semantic-heading-* surface)
    - .planning/research/PITFALLS.md Pitfall 1 (no inventing primitives without source — D-135 default reuses heading-1; ship metric-large ONLY if visual evaluation surfaces real need)
    - src/data/projects.json (bakery-order-system project.results for default metric-tile content)
    - src/pages/projects/[slug].astro lines 98-116 (v1.3 Results section structure — 3-up grid)
    - Phase 26 D-72 + Phase 28 D-106 (mid-plan token-value gate precedent)
  </read_first>

  <action>
    PRE-FLIGHT (mandatory per D-140): `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    Then EVALUATE metric-value token need per D-135 + RESEARCH § Open Question 3:
    - Default: reuse `type-semantic-heading-1` (48px PJS 700/lh 1.4 navy) per D-135 + Phase 23 D-33 open-extension policy minimalism.
    - Mid-plan AskUserQuestion gate fires ONLY IF visual evaluation at first metric-tile render shows 48px reads too small vs v1.3 text-5xl (Tailwind ~48-60px). Use the gate format per Phase 26 D-72 + Phase 28 D-106 precedent:
      - **Option A:** Keep `type-semantic-heading-1` (48px) — closes without new token surface; no PEN-INVENTORY Token Extensions row.
      - **Option B:** Ship NEW `type-semantic-metric-large-{family,size,weight,lh}` composite primitive at 60px (4 vars per OPEN-23-10 partial-resolution path); raises OPEN-29-NN noting source-evidence (v1.3 text-5xl ≈ Tailwind 48-60px); requires `set_variables` call BEFORE building ResultsMetrics.
    - DEFAULT for upfront commit: Option A (heading-1 reuse). Skip the gate unless visual reads escalates need.

    Then BUILD Section/ResultsMetrics via `mcp__pencil__batch_design`:

    ```
    batch_design({
      operations: [
        // 1. Parent frame (sibling of TagFilter + RelatedPosts + future RelatedProjects inside g9oRa5)
        Insert(g9oRa5, {
          type: "frame",
          reusable: true,
          name: "Section / ResultsMetrics",
          layout: "vertical",
          gap: 32,                // space-primitive-32 (matches RelatedPosts heading-to-cards gap)
          padding: [32, 0],        // space-primitive-32 vertical
          width: 1200,             // matches Phase 25-28 ship width
          alignItems: "start"
        }) → record returned id as <resultsMetrics-id>

        // 2. heading-slot (default 'Results' enabled:true per D-53)
        Insert(<resultsMetrics-id>, {
          type: "frame",
          name: "heading-slot",
          slot: [],
          enabled: true,
          layout: "vertical",
          alignItems: "start",
          width: "fill_container"
        }) → record returned id as <heading-slot-id>

        // 2a. heading text 'Results' (heading-2 REUSE per Phase 26 Plan 26-00 + OPEN-26-01 carry-forward)
        Insert(<heading-slot-id>, {
          type: "text",
          content: "Results",
          fontFamily: "Plus Jakarta Sans",   // type-semantic-heading-2-family
          fontSize: 32,                       // type-semantic-heading-2-size
          fontWeight: "700",                  // type-semantic-heading-2-weight
          fill: "#141f39ff",                  // color-semantic-text-primary
          lineHeight: 1.4                     // type-semantic-heading-2-lh
        })

        // 3. metrics-row (3 FIXED metric-tile children — NOT a slot per D-135 narrow-scoping)
        Insert(<resultsMetrics-id>, {
          type: "frame",
          name: "metrics-row",
          layout: "horizontal",
          gap: 24,                  // space-semantic-stack-md (matches BlogCard grid gap)
          alignItems: "start",
          width: "fill_container"
        }) → record returned id as <metrics-row-id>

        // 4. 3 FIXED metric-tile children — default content from bakery-order-system project per CONTEXT Claude's Discretion
        // For each i in 1..3:
        Insert(<metrics-row-id>, {
          type: "frame",
          name: "metric-tile-{i}",
          layout: "vertical",
          gap: 8,                    // space-semantic-inline-sm adjacent
          padding: 24,               // Pencil § 12 'Inside cards'
          width: "fill_container",
          fill: "#ffffffff",         // color-semantic-bg-page (CONTEXT default: plain white + border + 10 radius)
                                     // ALTERNATIVE per Plan 28-01 coral-banner precedent: fill: "#ff928aff" decorative-coral
                                     // — DEFAULT: plain per CONTEXT Claude's Discretion; pivot at Plan 29-02 calibration if rendering needs distinction
          stroke: "#d4d4d8ff",       // color-semantic-border-default
          strokeWidth: 1,
          strokeAlignment: "inner",
          cornerRadius: 10,          // radius-semantic-card
          alignItems: "start"
        }) → record returned id as <metric-tile-i-id>

        // 4a. metric-value (display number — DEFAULT heading-1 reuse per D-135)
        Insert(<metric-tile-i-id>, {
          type: "text",
          name: "metric-value",
          content: i==1 ? "85%" : i==2 ? "20hrs" : "Zero",     // bakery-order-system project.results verbatim
          fontFamily: "Plus Jakarta Sans",                      // type-semantic-heading-1-family (or metric-large-family if Option B shipped)
          fontSize: 48,                                          // type-semantic-heading-1-size (or 60 if Option B)
          fontWeight: "700",
          fill: "#141f39ff",                                    // color-semantic-text-primary
          lineHeight: 1.4
        })

        // 4b. metric-label (caption-tier — type-semantic-body-sm)
        Insert(<metric-tile-i-id>, {
          type: "text",
          name: "metric-label",
          content: i==1 ? "Time reduction in order processing"
                : i==2 ? "Saved per week"
                       : "Missed orders since launch",         // bakery-order-system labels verbatim
          fontFamily: "Inter",                                  // type-semantic-body-sm-family
          fontSize: 14,                                          // type-semantic-body-sm-size
          fontWeight: "400",
          fill: "#52525bff",                                    // color-semantic-text-secondary
          lineHeight: 1.5,
          textGrowth: "fixed-width",
          width: "fill_container"
        })
      ]
    })
    ```

    5. Insert sibling Pencil note at CANVAS ROOT documenting:
       - 3-FIXED-cell signature (NOT slot — narrow-scoping per D-135)
       - Joel-content essential per D-129 carve-out (ships regardless of cYlRH evidence)
       - metric-value token choice (heading-1 reuse default; Option B 60px if shipped)
       - chrome treatment choice (plain default per CONTEXT; coral pivot path per Plan 29-02 calibration if needed)
       - Default content from bakery-order-system project (consumers override 3 metric-tile contents per project.results array at Plan 29-02 instance time)

    6. Verify with `mcp__pencil__batch_get(['<resultsMetrics-id>'], readDepth: 3)`:
       - reusable: true
       - Children: heading-slot + metrics-row
       - metrics-row has 3 metric-tile children
       - Each metric-tile has metric-value + metric-label

    7. If Option B was selected at gate, call `mcp__pencil__get_variables({})` and verify drift +4 (composite-as-N-sub-vars: family + size + weight + lh per Phase 28 carry-forward). Default Option A: drift unchanged.
  </action>

  <verify>
    - `mcp__pencil__batch_get(['<resultsMetrics-id>'], readDepth: 3)` returns reusable:true with heading-slot + metrics-row; metrics-row has exactly 3 metric-tile children; each tile has metric-value + metric-label
    - `mcp__pencil__batch_get(['g9oRa5'], readDepth: 1)` returns 7 children (6 baseline + NEW ResultsMetrics)
    - If Option B token shipped: `mcp__pencil__get_variables({})` count == baseline +4; else == baseline
    - Sibling Pencil note at canvas root documents signature + carve-out + token-choice + chrome decision
  </verify>

  <acceptance_criteria>
    - [ ] NEW Section/ResultsMetrics reusable component exists inside g9oRa5 with reusable:true
    - [ ] Slot signature: heading-slot (enabled:true, 'Results' heading-2 default) + metrics-row (3 FIXED metric-tile children, NOT a slot)
    - [ ] Each metric-tile: 1px border + 10 radius + 24 padding + 8 gap + plain white fill (default chrome) OR decorative-coral if Plan 29-02 pivots
    - [ ] metric-value text bound to type-semantic-heading-1 (48px PJS 700/lh 1.4 navy) per D-135 default — OR to type-semantic-metric-large (60px) if Option B mid-plan gate fires
    - [ ] metric-label text bound to type-semantic-body-sm (14px Inter 400/lh 1.5 secondary)
    - [ ] Default content from bakery-order-system project.results verbatim ('85%' + '20hrs' + 'Zero' values; 3 labels verbatim)
    - [ ] If Option B shipped: `mcp__pencil__get_variables({})` count == baseline +4; OPEN-29-NN row added to PEN-INVENTORY at Task 5
    - [ ] If Option A default holds: token surface unchanged
    - [ ] Sibling Pencil note at canvas root
    - [ ] g9oRa5 children count moves 6 → 7
  </acceptance_criteria>

  <done>Section/ResultsMetrics structurally complete; ready for instance consumption by Plan 29-02 with 3 metric-tile descendants overrides per selected project.results.</done>
</task>

<task type="auto">
  <name>Task 3: Build Section / RelatedProjects inside g9oRa5 (NEW per D-134 — mirror of etY5x)</name>
  <files>design/Crito.pen (mutation: NEW reusable Section inside g9oRa5)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-134)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Code Examples § Example 3 (Section/RelatedProjects mirror of Section/RelatedPosts)
    - .planning/research/PEN-INVENTORY.md lines 680-685 (Phase 28 etY5x Section/RelatedPosts slot signature reference)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md Task 5 (RelatedPosts ship pattern)
  </read_first>

  <action>
    PRE-FLIGHT (mandatory per D-140): `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    Then BUILD Section/RelatedProjects via `mcp__pencil__batch_design` MIRRORING the etY5x Section/RelatedPosts structure EXACTLY with one swap (cards-row TYPED slot suggestion + default refs swap from `['t40xct']` → `['<new-ProjectCard-id-from-Task-1>']`):

    ```
    batch_design({
      operations: [
        // 1. Parent frame (sibling of RelatedPosts etY5x inside g9oRa5)
        Insert(g9oRa5, {
          type: "frame",
          reusable: true,
          name: "Section / RelatedProjects",
          layout: "vertical",
          gap: 32,                // matches etY5x (space-primitive-32)
          padding: [32, 0],
          width: 1200,
          alignItems: "start"
        }) → record returned id as <relatedProjects-id>

        // 2. heading-slot (mirror W1edi pattern from etY5x)
        Insert(<relatedProjects-id>, {
          type: "frame",
          name: "heading-slot",
          slot: [],
          enabled: true,             // default-ON per D-53 + W1edi precedent
          layout: "vertical",
          alignItems: "start"
        }) → record returned id as <heading-slot-id>

        // 2a. heading text 'Related projects' (NOT 'Related posts') — heading-2 PJS 32/700/lh 1.4 navy
        Insert(<heading-slot-id>, {
          type: "text",
          content: "Related projects",
          fontFamily: "Plus Jakarta Sans",
          fontSize: 32,
          fontWeight: "700",
          fill: "#141f39ff",
          lineHeight: 1.4
        })

        // 3. cards-row (mirror j9KOm pattern; swap TYPED slot suggestion + ref children to ProjectCard)
        Insert(<relatedProjects-id>, {
          type: "frame",
          name: "cards-row",
          slot: ["<projectCard-id-from-Task-1>"],   // TYPED per D-52 PREFERRED — NEW ProjectCard id
          layout: "horizontal",
          gap: 24,                                    // matches j9KOm space-semantic-stack-md
          alignItems: "start",
          width: "fill_container"
        }) → record returned id as <cards-row-id>

        // 4. 3 ProjectCard refs (default density per D-118 carry-forward — matches RelatedPosts 3-card density)
        // For each i in 1..3:
        Insert(<cards-row-id>, {
          type: "ref",
          ref: "<projectCard-id-from-Task-1>",
          width: "fill_container"
          // descendants empty — consumers override at instance time (Plan 29-02)
        })
      ]
    })
    ```

    5. Insert sibling Pencil note at CANVAS ROOT documenting:
       - Narrow-scope rationale per D-134 (project domain ONLY, NOT generalized into Section/RelatedContent — different content domain, different vocabulary, honest discoverability for downstream agents)
       - Mirror of RelatedPosts (etY5x) structural shape with ProjectCard ref swap per D-134
       - Plan 29-02 first consumer instance pattern (descendants override on 2-3 ProjectCard refs per v1.3 sample project count; default 3-card density may downsize to 2 if pad-feels-forced per CONTEXT Claude's Discretion)

    6. Verify with `mcp__pencil__batch_get(['<relatedProjects-id>'], readDepth: 3)`:
       - reusable: true, name: "Section / RelatedProjects"
       - Children: heading-slot + cards-row
       - cards-row has slot:['<projectCard-id>'] TYPED + 3 ref children
  </action>

  <verify>
    - `mcp__pencil__batch_get(['<relatedProjects-id>'], readDepth: 3)` returns reusable:true with heading-slot + cards-row; cards-row has slot:['<projectCard-id>'] AND 3 ref children
    - `mcp__pencil__batch_get(['g9oRa5'], readDepth: 1)` returns 8 children (6 baseline + ResultsMetrics from Task 2 + NEW RelatedProjects)
    - Sibling Pencil note at canvas root documents narrow-scope + mirror rationale
  </verify>

  <acceptance_criteria>
    - [ ] NEW Section/RelatedProjects reusable component exists inside g9oRa5 with reusable:true
    - [ ] Slot signature: heading-slot (enabled:true, 'Related projects' heading-2 default) + cards-row (TYPED slot:['<projectCard-id>'] with 3 ProjectCard ref children at fill_container width)
    - [ ] Structure MIRRORS etY5x exactly (same vertical layout, gap 32, padding [32,0], width 1200, alignItems start; same heading-slot+cards-row composition)
    - [ ] Sibling Pencil note at canvas root documents narrow-scope + mirror rationale
    - [ ] g9oRa5 children count moves 7 → 8
  </acceptance_criteria>

  <done>Section/RelatedProjects structurally complete; ready for instance consumption by Plan 29-02 with descendants override on 2-3 ProjectCard refs.</done>
</task>

<task type="auto">
  <name>Task 4: Plan-close sweep + PEN-INVENTORY extensions + library count drift verification</name>
  <files>.planning/research/PEN-INVENTORY.md (mutation: NEW additions section for Plan 29-00); design/Crito.pen (read-only sweep)</files>

  <read_first>
    - .planning/research/PEN-INVENTORY.md (current Phase 28 close state — for placement of new Plan 29-00 additions section)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md (PEN-INVENTORY extension format precedent — Variant Evidence rows + Token Extensions rows + library count update)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § D-141 (PEN-INVENTORY extension pattern)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Validation Architecture (Plan 29-00 plan-close checkpoints)
  </read_first>

  <action>
    PRE-FLIGHT (mandatory per D-140 — even though this task is mostly markdown + a final read-only Pencil sweep): `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    1. **Layout sweep at document root:**
       ```
       mcp__pencil__snapshot_layout({maxDepth: 0, problemsOnly: true})
       ```
       Expected: `"No layout problems."` per Phase 24-28 plan-close discipline. Per-frame text-clipping false-positives documented per Pitfall 6 (Phase 24-28 precedent — benign Pencil text-y-coordinate quirk).

    2. **Token surface drift verification:**
       ```
       mcp__pencil__get_variables({})
       ```
       Expected:
       - Default (Task 2 Option A): count UNCHANGED from Task 0 baseline (127 / or 107 logical).
       - Option B (Task 2 metric-large shipped): count == baseline +4 composite vars.

    3. **Library count drift verification:**
       ```
       mcp__pencil__batch_get(['avgor', 't67DU6', 'g9oRa5'], readDepth: 1)
       ```
       Expected:
       - avgor children: 16 (UNCHANGED unless Task 1 shipped a Badge variant — default: no variant shipped)
       - t67DU6 children: 4 (was 3 + NEW ProjectCard from Task 1)
       - g9oRa5 children: 8 (was 6 + NEW ResultsMetrics from Task 2 + NEW RelatedProjects from Task 3)

    4. **Baseline ID re-verification:**
       Re-run the 28-ID batch_get from Task 0 → assert ZERO regression.

    5. **PEN-INVENTORY.md mutation** — append a NEW section at the bottom of the Variant Evidence area (after Plan 28-03 additions). Use the Edit tool:

       ```
       ### Plan 29-00 additions (Phase 29 Foundation — Compound/ProjectCard + Section/ResultsMetrics + Section/RelatedProjects)

       Plan 29-00 ships 3 NEW narrow-scoped reusable library entries per D-131 + D-134 + D-135. Compound/ProjectCard (4-slot signature) is the third Compound inside t67DU6 (sibling of t40xct + ZSxZU per Plan 28-01 BlogCard precedent — upfront commit per D-131 skips REVISE-loop risk). Section/ResultsMetrics ships as Joel-content essential per D-129 carve-out (3 FIXED metric-tile children, NOT a slot — narrow-scoping per D-135). Section/RelatedProjects mirrors etY5x Section/RelatedPosts structure with cards-row swap to ProjectCard refs per D-134.

       {Token Extensions sub-section ONLY IF Option B shipped:}
       ### Token Extensions (Phase 29 Plan 29-00 — type-semantic-metric-large composite)

       | token | value | rationale | resolution |
       |---|---|---|---|
       | type-semantic-metric-large-family | Plus Jakarta Sans | matches heading-1 family; metric-value display register | Plan 29-00 Task 2 Option B (mid-plan gate per D-72/D-106 precedent) |
       | type-semantic-metric-large-size | 60 | v1.3 text-5xl upper bound (Tailwind 48-60px) | Plan 29-00 Task 2 Option B + Phase 23 D-33 open-extension policy |
       | type-semantic-metric-large-weight | 700 | matches heading-1 weight | Plan 29-00 Task 2 Option B |
       | type-semantic-metric-large-lh | 1.4 | matches heading-1 lh | Plan 29-00 Task 2 Option B |

       OPEN-29-NN (token-extension, minor): type-semantic-metric-large composite shipped via mid-plan gate (Phase 26 D-72 + Phase 28 D-106 precedent). v1.3 text-5xl is ~48-60px ambiguous; ship 60px as upper-bound match. Plan 29-00 Task 2 visual evaluation surfaced need. OPEN-23-10 partial-resolution annotation (heading-5/-6 still open).

       ### Variant Evidence — Plan 29-00 additions

       | node | element | properties | values (literal) | semantic ref / source | source_evidence | rationale |
       |---|---|---|---|---|---|---|
       | Compound / ProjectCard (<projectCard-id>) | parent frame (NEW per D-131 sibling-component pattern) | type/reusable/layout/gap/padding/width/cornerRadius/fill/stroke/strokeWidth/strokeAlignment/clip | frame/true/vertical/0/0/380/10/#ffffffff/#d4d4d8ff/0/inner/true | — / radius-semantic-card / color-semantic-bg-page / color-semantic-border-default (reserved) / Crito-borderless / — / clip:true for image top-corner rounding | Plan 29-00 + D-131 + Plan 28-01 BlogCard precedent + image-import-12.jpg lower 2x2 Project grid + v1.3 ProjectCard.astro | NEW reusable Compound distinct from t40xct + ZSxZU. Narrow-scoped per D-131 — NOT generalized; t40xct continues serving Phase 27 + future Phase 31 |
       | Compound / ProjectCard / image-slot (<image-slot-id>) | slot frame | type/slot/enabled/layout/justifyContent/alignItems/width/height/fill | frame/[]/true/vertical/center/center/fill_container/240/#f2f2f7ff | — / untyped per BlogCard precedent / D-53 / — / — / — / — / matches BlogCard 240h prominence / color-semantic-bg-surface-elevated | Plan 29-00 + v1.3 ProjectCard.astro 16:9 aspect | Image placeholder area. Consumers populate fill: {type: 'image', url: '<project.thumbnail>'} at instance time OR replace whole slot at code milestone |
       | Compound / ProjectCard / content-area (<content-area-id>) | content frame | type/layout/gap/padding/width | frame/vertical/12/24/fill_container | — / — / space-semantic-stack-sm adjacent / space-primitive-24 / — | Plan 29-00 + Plan 28-01 BlogCard content-area precedent | Wraps category-badge + title + body at 24 padding |
       | Compound / ProjectCard / category-badge-slot (<category-badge-slot-id>) | slot frame | type/slot/layout/alignItems/width | frame/['j0FxQZ']/horizontal/center/fill_container | — / TYPED per D-52 PREFERRED + D-132 Badge/Default solid default lean / — / — / — | Plan 29-00 + D-132 + CONTEXT Claude's Discretion + image-import-12.jpg evaluation | NEW typed slot for Badge primitive. Default Badge/Default solid; plan-execution may swap to ['kJQmJ'] Outline if image-import-12.jpg shows outline treatment at Plan 29-01 calibration |
       | Compound / ProjectCard / title (text node) | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight | 'Project title placeholder'/Plus Jakarta Sans/24/700/#141f39ff/1.4 | — / type-semantic-heading-3-family / type-semantic-heading-3-size / type-semantic-heading-3-weight / color-semantic-text-primary / type-semantic-heading-3-lh | Plan 29-00 + D-132 + Plan 28-01 BlogCard heading-3 first-consumer precedent | Heading-3 SECOND-CONSUMER after BlogCard. Plan-execution may downsize to heading-4 (20px) at Plan 29-01 calibration if visual reads heavy |
       | Compound / ProjectCard / body (text node) | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight/textGrowth/width | (placeholder ~160 chars)/Inter/16/400/#52525bff/1.625/fixed-width/fill_container | — / type-semantic-prose-paragraph-family / type-semantic-prose-paragraph-size / type-semantic-prose-paragraph-weight / color-semantic-text-secondary / type-semantic-prose-paragraph-lh / — / — | Plan 29-00 + v1.3 ProjectCard.astro 160-char truncate convention | Project problem excerpt. Consumers override with first 160 chars of project.problem from projects.json |
       | Compound / ProjectCard sibling Pencil note | note | content (slot signature + narrow-scope rationale + Crito-visual-register + heading-3-vs-4 plan-execution flexibility + hover/focus deferral) | (multi-line) | — | Plan 29-00 + D-52 belt-and-suspenders | Documents component-author's intent for downstream consumers |
       | Section / ResultsMetrics (<resultsMetrics-id>) | parent frame (NEW per D-135 Joel-content essential) | type/reusable/layout/gap/padding/width/alignItems | frame/true/vertical/32/[32,0]/1200/start | — / — / — / space-primitive-32 / space-primitive-32 vertical / matches Phase 25-28 ship width / — | Plan 29-00 + D-135 + Section/RelatedPosts heading-to-cards rhythm precedent | NEW reusable Section. Narrow-scoped Joel-content essential per D-129 carve-out — ships regardless of cYlRH evidence |
       | Section / ResultsMetrics / heading-slot (<heading-slot-id>) | slot frame | type/slot/enabled/layout/alignItems/width | frame/[]/true/vertical/start/fill_container | — / untyped / D-53 / — / — / — | Plan 29-00 + D-53 placeholder pattern | Heading-slot with default 'Results' text child. Consumers override or disable per instance |
       | Section / ResultsMetrics / heading-slot / heading text | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight | 'Results'/Plus Jakarta Sans/32/700/#141f39ff/1.4 | — / type-semantic-heading-2-family / type-semantic-heading-2-size / type-semantic-heading-2-weight / color-semantic-text-primary / type-semantic-heading-2-lh | Plan 29-00 + Phase 26 Plan 26-00 heading-2 + OPEN-26-01 carry-forward | heading-2 REUSE per D-135 default |
       | Section / ResultsMetrics / metrics-row (<metrics-row-id>) | row frame (3 FIXED children, NOT slot per D-135) | type/layout/gap/alignItems/width | frame/horizontal/24/start/fill_container | — / — / space-semantic-stack-md / — / — | Plan 29-00 + D-135 narrow-scoping | Holds 3 FIXED metric-tile children. NOT a slot — consumers override descendants per instance time |
       | Section / ResultsMetrics / metric-tile-{1,2,3} (3 fixed) | tile frame × 3 | type/layout/gap/padding/width/fill/stroke/strokeWidth/strokeAlignment/cornerRadius/alignItems | frame/vertical/8/24/fill_container/#ffffffff/#d4d4d8ff/1/inner/10/start | — / — / space-semantic-inline-sm adjacent / space-primitive-24 / — / color-semantic-bg-page / color-semantic-border-default / — / — / radius-semantic-card / — | Plan 29-00 + D-135 chrome default (plain white + border + 10 radius) | Default chrome: plain white. Coral pivot path documented (CONTEXT Claude's Discretion + Plan 28-01 banner precedent) if Plan 29-02 calibration needs distinction |
       | Section / ResultsMetrics / metric-tile-{i} / metric-value (text × 3) | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight | ('85%'/'20hrs'/'Zero')/Plus Jakarta Sans/48/700/#141f39ff/1.4 | — / type-semantic-heading-1-family / type-semantic-heading-1-size / type-semantic-heading-1-weight / color-semantic-text-primary / type-semantic-heading-1-lh | Plan 29-00 + D-135 default heading-1 REUSE + bakery-order-system project.results verbatim | Display number. Option A default (heading-1 48px). Option B path: ship type-semantic-metric-large 60px if mid-plan gate fires |
       | Section / ResultsMetrics / metric-tile-{i} / metric-label (text × 3) | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight/textGrowth/width | (3 labels)/Inter/14/400/#52525bff/1.5/fixed-width/fill_container | — / type-semantic-body-sm-family / type-semantic-body-sm-size / type-semantic-body-sm-weight / color-semantic-text-secondary / type-semantic-body-sm-lh / — / — | Plan 29-00 + D-135 default body-sm + bakery-order-system labels verbatim | Caption-tier label under metric-value |
       | Section / ResultsMetrics sibling Pencil note | note | content (3-FIXED signature rationale + Joel-content essential carve-out + token-choice + chrome-treatment) | (multi-line) | — | Plan 29-00 + D-52 belt-and-suspenders | Documents component intent |
       | Section / RelatedProjects (<relatedProjects-id>) | parent frame (NEW per D-134 mirror of etY5x) | type/reusable/layout/gap/padding/width/alignItems | frame/true/vertical/32/[32,0]/1200/start | — / — / — / matches etY5x / matches etY5x / matches Phase 25-28 ship width / — | Plan 29-00 + D-134 + etY5x mirror | NEW reusable Section. MIRRORS etY5x structure exactly with cards-row ref swap |
       | Section / RelatedProjects / heading-slot | slot frame | type/slot/enabled/layout/alignItems | frame/[]/true/vertical/start | — / untyped / D-53 + W1edi mirror / — / — | Plan 29-00 + W1edi precedent | Default 'Related projects' (NOT 'Related posts') heading-2 child |
       | Section / RelatedProjects / heading-slot / heading text | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight | 'Related projects'/Plus Jakarta Sans/32/700/#141f39ff/1.4 | — / type-semantic-heading-2-* / color-semantic-text-primary | Plan 29-00 + Phase 26 heading-2 + D-134 | heading-2 REUSE (same as RelatedPosts heading) |
       | Section / RelatedProjects / cards-row | row frame | type/slot/layout/gap/alignItems/width | frame/['<projectCard-id>']/horizontal/24/start/fill_container | — / TYPED per D-52 PREFERRED — NEW ProjectCard id swap / matches j9KOm / matches j9KOm / — / — | Plan 29-00 + D-134 + j9KOm precedent | TYPED slot mirrors j9KOm cards-row with ProjectCard ref swap |
       | Section / RelatedProjects / cards-row / ProjectCard-{1,2,3} (3 refs) | ref × 3 | type/ref/width | ref/<projectCard-id>/fill_container | — / — / — | Plan 29-00 + D-118 3-card density carry-forward | 3 default refs. Consumers override descendants per instance time (Plan 29-02 may ship 2 or 3 per project sample) |
       | Section / RelatedProjects sibling Pencil note | note | content (mirror rationale + narrow-scope + Plan 29-02 consumer pattern) | (multi-line) | — | Plan 29-00 + D-52 + D-134 | Documents narrow-scope intent (NOT generalized into RelatedContent) |

       {Possible Badge variant row IF Task 1 surfaced a need (default: no variant shipped):}

       **Plan 29-00 Variant Evidence outcome:** {~18-20 rows} documenting Compound/ProjectCard (7 rows incl. note) + Section/ResultsMetrics (8 rows incl. note + 3 fixed tiles) + Section/RelatedProjects (5 rows incl. note). {+1 Badge variant row if Task 1 evaluation shipped one.} {+1 Token Extensions sub-section if Task 2 Option B shipped.}

       Token drift: {baseline} → {baseline} (Option A default — zero new tokens; heading-1 + heading-2 + heading-3 + prose-paragraph + body-sm + colors + spacing + radius all REUSE per D-91-style minimalism)
       — OR — {baseline} → {baseline+4} (Option B — type-semantic-metric-large composite shipped per mid-plan gate).

       **Library count after Phase 29 Plan 29-00:**
       - `_Components / Primitives` parent (avgor): {16 unchanged — OR 17 if Badge variant shipped at Task 1}
       - `_Components / Compounds` parent (t67DU6): **4 reusable children** (Plan 28-01 close 3 + 1 NEW Compound/ProjectCard)
       - `_Components / Sections` parent (g9oRa5): **8 reusable children** (Plan 28-03 close 6 + 2 NEW: Section/ResultsMetrics + Section/RelatedProjects)
       - Total reusable component surface: **{28 or 29} reusable components in v2.0 ship-set** (was 25 at Phase 28 close).

       **Open Flags updated:**
       - **OPEN-25-07** further partial-resolution annotation: Phase 29 Plan 29-00 ships sibling Compound/ProjectCard per Plan 28-01 BlogCard precedent. Plan 29-01 + Plan 29-02 will produce the 9 instance consumers (6 Projects index + 3 RelatedProjects). Final resolution moves to Phase 29 Plan 29-02 close per D-141.
       - **OPEN-23-10** partial-resolution annotation IF Option B token shipped (heading-1/-2/-3/-4 + metric-large now shipped; heading-5/-6 remain open).
       - **OPEN-23-12** status check noted: ProjectCard category-badge inside Plan 29-01 grid may surface enough rounded-pill consumers (9999 cornerRadius) to justify radius-semantic-pill at Phase 32 sweep. Plan 29-00 does NOT ship the token; Plan 29-01 plan-execution evaluates.
       ```

       Write via the Edit tool — insert after the last Phase 28 additions section (likely after Plan 28-03's additions).

    6. Final sweep: re-run `mcp__pencil__snapshot_layout({maxDepth: 0, problemsOnly: true})` to confirm `"No layout problems."` AFTER all mutations.
  </action>

  <verify>
    - `mcp__pencil__snapshot_layout({maxDepth: 0, problemsOnly: true})` returns `"No layout problems."` at document root
    - `mcp__pencil__get_variables({})` returns baseline count (Option A default) OR baseline+4 (Option B)
    - `mcp__pencil__batch_get(['avgor', 't67DU6', 'g9oRa5'], readDepth: 1)` returns counts (16 OR 17) + 4 + 8
    - `mcp__pencil__batch_get` for the 28 baseline IDs returns ZERO regression
    - `grep -c "Plan 29-00 additions" .planning/research/PEN-INVENTORY.md` returns ≥ 1
    - `grep -c "Compound / ProjectCard" .planning/research/PEN-INVENTORY.md` shows the new rows added
  </verify>

  <acceptance_criteria>
    - [ ] `mcp__pencil__snapshot_layout({maxDepth: 0, problemsOnly: true})` returns `"No layout problems."` at plan close
    - [ ] Token surface drift matches Task 2 outcome (Option A: unchanged; Option B: +4)
    - [ ] Library counts: t67DU6 == 4; g9oRa5 == 8; avgor == 16 (default) OR 17 (if Badge variant shipped)
    - [ ] Total reusable component count moves 25 → 28 (default) OR 29 (Badge variant shipped)
    - [ ] All 28 baseline IDs from Task 0 verified intact (zero regression)
    - [ ] PEN-INVENTORY.md has NEW "### Plan 29-00 additions" section with: explanatory prose + (optional) Token Extensions sub-section + Variant Evidence table (~18-20 rows) + library count update + OPEN-25-07 / OPEN-23-10 / OPEN-23-12 annotations
    - [ ] Sibling Pencil notes for all 3 components exist at canvas root (NOT inside library parent frames per Plan 28-01 quirk)
  </acceptance_criteria>

  <done>Plan 29-00 closes WITHOUT user calibration gate per D-139 (agent-deterministic foundation work). Per D-139: possible mid-plan AskUserQuestion gate may have fired ONLY IF Task 2 Option B token escalation surfaced OR Task 1 Badge variant evidence-gate fired; otherwise plan closes silently. SUMMARY documents which paths were taken.</done>
</task>

</tasks>

<verification>
**Phase-level verification (after Plan 29-00 close):**

1. `mcp__pencil__get_editor_state({include_schema: false})` returns active editor == `design/Crito.pen` throughout
2. `mcp__pencil__batch_get(['<new-projectCard-id>', '<new-resultsMetrics-id>', '<new-relatedProjects-id>'], readDepth: 3)` returns all 3 new components structurally intact with correct slot signatures per D-131 + D-134 + D-135
3. `mcp__pencil__batch_get(['t67DU6', 'g9oRa5'], readDepth: 1)` returns t67DU6 == 4 children + g9oRa5 == 8 children
4. `mcp__pencil__get_variables({})` returns baseline count (Option A — zero new tokens) OR baseline+4 (Option B — type-semantic-metric-large composite shipped)
5. `mcp__pencil__snapshot_layout({maxDepth: 0, problemsOnly: true})` returns `"No layout problems."` at document root
6. PEN-INVENTORY.md grep confirms NEW Plan 29-00 additions section + Variant Evidence rows + library count update + OPEN flag annotations
7. `grep -c "Plan 29-00 additions" .planning/research/PEN-INVENTORY.md` returns ≥ 1
8. All 28 baseline IDs (Phase 23-28 ship-set) verified intact via batch_get — ZERO regression
</verification>

<success_criteria>
**Plan 29-00 success criteria (measurable):**

1. ✅ Compound/ProjectCard (NEW) ships inside t67DU6 with reusable:true + 4-slot signature (image-slot + content-area containing category-badge-slot + title + body) per D-131 + D-132
2. ✅ Section/ResultsMetrics (NEW) ships inside g9oRa5 with reusable:true + heading-slot + metrics-row (3 FIXED metric-tile children) per D-135
3. ✅ Section/RelatedProjects (NEW) ships inside g9oRa5 with reusable:true + heading-slot + cards-row (TYPED slot:['<projectCard-id>'] + 3 ProjectCard ref children) per D-134
4. ✅ Possible 4th library entry (Badge variant) per D-132 plan-execution: ship ONLY if image-import-12.jpg evaluation surfaces clear need (default: NO variant shipped)
5. ✅ Possible 1 new token (type-semantic-metric-large composite) per D-135 plan-execution: ship ONLY if mid-plan gate fires (default: heading-1 reuse, NO new token)
6. ✅ Library count moves 25 → 28 (default) OR 29 (Badge variant shipped)
7. ✅ Token surface drift matches outcome (0 default / +4 Option B)
8. ✅ All 28 Phase 23-28 baseline IDs verified intact (ZERO regression)
9. ✅ Layout sweep clean at plan close
10. ✅ PEN-INVENTORY.md extended per D-141 (Variant Evidence rows + library count + OPEN flag annotations)
11. ✅ Sibling Pencil notes for all 3 new components placed at canvas root (NOT inside library parent frames per Plan 28-01 quirk)
12. ✅ Plan 29-00 closes WITHOUT user calibration gate per D-139 default (possible mid-plan gate only fires if Task 1 Badge or Task 2 token escalation surfaces)

**Downstream consumer readiness:**
- Plan 29-01 can instance Compound/ProjectCard × 6 (Projects index grid) with descendants overrides per project content
- Plan 29-02 can instance Section/ResultsMetrics (1 instance, 3 metric-tile descendants override per selected project.results) + Section/RelatedProjects (1 instance, 2-3 ProjectCard descendants override) + Compound/ProjectCard inside RelatedProjects
</success_criteria>

<output>
After Task 4 completes, write `.planning/phases/29-projects-reconstruction-index-project-detail/29-00-SUMMARY.md` covering:

- Pencil MCP Subagent-Tool-Inheritance Caveat (INLINE execution confirmation)
- Task 0: Pre-flight + baseline verification outcome (token count, library counts, all 28 IDs intact)
- Task 1: Compound/ProjectCard outcome (new id, slot signature, Badge variant choice — default solid OR Outline, heading-3 default)
- Task 2: Section/ResultsMetrics outcome (new id, Option A default OR Option B token shipped — record which + why)
- Task 3: Section/RelatedProjects outcome (new id, mirror of etY5x verified)
- Task 4: Plan-close sweep outcome (layout problemsOnly result, token drift, library count change)
- PEN-INVENTORY.md additions written
- OPEN flags raised + updated (OPEN-25-07 partial-resolution, OPEN-23-10 if Option B, OPEN-23-12 status check)
- Token surface change (baseline → outcome)
- Library count change (25 → 28 OR 29)
- Page-frame count UNCHANGED (foundation plan; no page frames)
- Plan 29-01 + Plan 29-02 readiness checklist
</output>
