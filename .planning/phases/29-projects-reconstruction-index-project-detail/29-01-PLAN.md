---
phase: 29-projects-reconstruction-index-project-detail
plan: 01
type: execute
wave: 1
depends_on:
  - 29-00
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - PAGE-02
gap_closure: false

must_haves:
  truths:
    - "Projects index top-level page frame exists at canvas page-frame row (y ≈ −4111) with Header + page-intro + TagFilter instance + 6-ProjectCard grid + Footer composition"
    - "Y2isa (Crito 05_Service raster) is hidden via enabled:false ONLY after user APPROVE at calibration gate per Pitfall 4 strict observance"
    - "Section/TagFilter (O1IwyS) is instanced with descendants override per D-137: 4 pill labels overridden ('All Projects' / 'Web Apps' / 'Automation' / 'AI Development'); Position 5 disabled at both Active + Inactive shadows"
    - "6 Compound/ProjectCard refs (instances of Plan 29-00 NEW component) populate a 3-col × 2-row grid with descendants overrides per 2 real projects.json entries + 4 representative agency-template instances"
    - "Plan 28-01 Blog index TagFilter instance (M1Wu9) remains UNCHANGED after Plan 29-01 mutations (per-instance descendants validates Pencil 2.13 ref semantics)"
    - "PEN-INVENTORY Y2isa row reclassified IN-PLACE per D-128 + D-141 (drops token-mining-only qualifier; joel_page_map 'none' → 'Projects'; status_counts flat:1 → hidden:1 post-APPROVE)"
    - "Layout sweep at document root returns 'No layout problems.' at plan close"
  artifacts:
    - path: "design/Crito.pen"
      provides: "NEW Projects top-level page frame at 1440 width; PAGE-11 ACTIVE mutation on Y2isa raster"
      contains: "Projects frame composition: Section/Header ref + page-intro section + Section/TagFilter ref (descendants override) + 6-ProjectCard grid + Section/Footer ref; Y2isa.enabled == false post-APPROVE"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Reclassification of Y2isa row + NEW Projects frame row + Variant Evidence rows + OPEN flag annotations"
      contains: "Plan 29-01 additions section; Y2isa joel_page_map: 'Projects'; status_counts: hidden:1; OPEN-23-05 partial-resolution annotation"
  key_links:
    - from: "Projects frame Section/TagFilter ref"
      to: "O1IwyS (Phase 28 Section/TagFilter)"
      via: "ref + descendants override on pill labels (ATJK9 + UbwMv via paired-ref children RxShj/z1aro/al86r/u3X64E/C93nA/j4KiI/m35My/EXgTG/CCXCE/hMIRk)"
      pattern: "Position 1 active 'All Projects'; Positions 2-4 inactive 'Web Apps'/'Automation'/'AI Development'; Position 5 both shadows enabled:false"
    - from: "Projects frame 6-card grid"
      to: "<projectCard-id> from Plan 29-00"
      via: "6 ref instances inside 2 row frames (3-col × 2-row) with per-card descendants override on title + category-badge + body"
      pattern: "2 refs populate real projects.json data (bakery-order-system + inventory-sync-automation); 4 refs populate representative agency-template content"
    - from: "Y2isa raster"
      to: "Projects frame (reconstruction target)"
      via: "PAGE-11 ACTIVE Update(Y2isa, {enabled: false}) post-APPROVE per § 3.3"
      pattern: "Structural archive persists at original canvas position; enabled:false hides from active view"
---

<objective>
Reconstruct the Crito 05_Service flat raster (Y2isa) as a NEW editable `Projects` top-level page frame at 1440 width per PAGE-02 + PAGE-09 + PAGE-11. Crito-source-flat-raster branch per CALIBRATION-PROTOCOL § 3.4 + D-127. FOURTH production use of PAGE-11 ACTIVE in v2.0 (after cl8tt Plan 27-02 + DzqTm Plan 28-01 + w1m3x Plan 28-02). Validates Phase 28 D-115 Section/TagFilter broad-scoping with THIRD consumer (Blog + Tag + Projects).

Composition: Section/Header ref (G0wNOc, no override per D-77) + page-intro section (v1.3 verbatim from src/pages/projects/index.astro lines 19-23) + Section/TagFilter ref (O1IwyS) with descendants override per D-137 (4 pills: 'All Projects' active + 'Web Apps' + 'Automation' + 'AI Development'; Position 5 both shadows disabled since v1.3 has only 4 categories) + 6-Compound/ProjectCard grid (3-col × 2-row; 2 real projects + 4 representative agency-template per Plan 28-01 BlogCard precedent) + Section/Footer ref (Xs0Hs, no override per D-77).

Calibration gate per § 3.4 with DUAL pairing per D-130 (Y2isa overall page-layout + image-import-12.jpg ProjectCard fidelity). On APPROVE → PAGE-11 ACTIVE on Y2isa.

PEN-INVENTORY reclassifies Y2isa row IN-PLACE per D-128 (first non-additive change in v2.0 — drops token-mining-only qualifier; joel_page_map 'none' → 'Projects'; status_counts flat:1 → hidden:1 post-APPROVE).

Purpose: Phase 29 success criterion 1 (Projects index frame fully reconstructed — zero `[FLAT]` markers) + 2 (project cards are Compound/ProjectCard instances). Closes Y2isa OPEN-23-05 partial-resolution leg.

Output:
- design/Crito.pen: NEW Projects frame at canvas page-frame row + Y2isa.enabled:false post-APPROVE
- .planning/research/PEN-INVENTORY.md: Y2isa row reclassified IN-PLACE + NEW Projects row + Variant Evidence + OPEN flag updates
- .planning/phases/29-projects-reconstruction-index-project-detail/29-01-SUMMARY.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md

**CRITICAL — INLINE EXECUTION REQUIRED:** Pencil MCP tools NOT inherited by spawned subagents per Plan 28-00 + 27-00 carry-forward. INLINE main-orchestrator execution ONLY.

**PRE-FLIGHT ENFORCEMENT per D-140:** `mcp__pencil__get_editor_state({include_schema: false})` before EVERY Pencil-mutating call.

**PITFALL 4 STRICT OBSERVANCE:** `Update("Y2isa", {enabled: false})` fires ONLY after user APPROVE at the calibration gate. NEVER before APPROVE. Split across two batch_design calls separated by AskUserQuestion.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-VALIDATION.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PITFALLS.md
@.planning/phases/29-projects-reconstruction-index-project-detail/29-00-SUMMARY.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-03-SUMMARY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
@src/pages/projects/index.astro
@src/data/projects.json
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + Y2isa raster probe + v1.3 content extraction + image-import-12.jpg accessibility</name>
  <files>design/Crito.pen (read-only probe); src/pages/projects/index.astro + src/data/projects.json (read-only)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-127, D-128, D-130, D-137, D-138) + § Specifics
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Common Pitfalls Pitfall 3 (image-import-NN.jpg index identification) + § Validation Architecture (Plan 29-01 checkpoints) + § Code Examples § Example 3 (TagFilter descendants override)
    - .planning/research/PEN-INVENTORY.md line 71 (Y2isa baseline classification: IN-SCOPE token-mining-only, joel_page_map: none, flat:1)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.4 step 1-2 (pre-flight + baseline) + § 10.4 (FindEmptySpace nodeId anchor)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-03-SUMMARY.md (FUctJ = Tag frame; latest Phase 28 page-frame for FindEmptySpace anchor)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-00-SUMMARY.md (Plan 29-00 NEW Compound/ProjectCard id + library counts)
    - src/pages/projects/index.astro lines 19-23 (page-intro verbatim content)
    - src/data/projects.json (2 real projects + categoryLabel field)
    - .planning/research/PEN-INVENTORY.md lines 670-686 + 768 (TagFilter O1IwyS paired-ref children IDs)
  </read_first>

  <action>
    PRE-FLIGHT: `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    1. **Verify Plan 29-00 deliverables intact:**
       ```
       batch_get(['<projectCard-id-from-29-00>', '<resultsMetrics-id-from-29-00>', '<relatedProjects-id-from-29-00>'], readDepth: 2)
       ```
       Expected: all 3 components present with correct structure (per 29-00-SUMMARY).

    2. **Probe Y2isa raster for image-import-NN.jpg filename:**
       ```
       batch_get(['Y2isa'], readDepth: 2)
       ```
       Expected: returns the raster `fill.image` field with a filename string (likely `image-import-NN.jpg` per Plan 27-02 / 28-01 / 28-02 precedent). Record the filename in plan SUMMARY for calibration gate pairing reference at Task 4. Per CONTEXT D-130: Y2isa image-import-NN.jpg is the FIRST pairing target.

    3. **Verify image-import-12.jpg accessibility for D-130 dual pairing:**
       ```bash
       ls design/images/image-import-12.jpg
       ```
       Expected: file present (confirmed at research time per CONTEXT line 204 + Phase 25 D-49). This is the SECOND pairing target at Task 4 calibration gate (Project lower 2x2 grid visual proxy per OPEN-25-07 line 303 carry-forward).

    4. **Verify FUctJ Tag frame anchor (Phase 28 Plan 28-03):**
       ```
       batch_get(['FUctJ'], readDepth: 1)
       ```
       Expected: Tag frame exists at canvas position ~ (x, −4111.55). This is the FindEmptySpace nodeId anchor for Task 1.

    5. **Verify O1IwyS TagFilter + paired-ref children IDs intact:**
       ```
       batch_get(['O1IwyS'], readDepth: 3)
       ```
       Expected: 10 paired-ref children at positions 1-5: (RxShj+z1aro position-1 'All Posts' active+inactive shadows), (al86r+u3X64E position-2 'automation'), (C93nA+j4KiI position-3 'small-business'), (m35My+EXgTG position-4 'productivity'), (CCXCE+hMIRk position-5 'ai'). Verify ATJK9 (M7eUr default button label child) + UbwMv (hIWuC secondary button label child) descendants for label-content override path.

    6. **Verify Plan 28-01 Blog index TagFilter instance M1Wu9 baseline:**
       ```
       batch_get(['M1Wu9'], readDepth: 2)
       ```
       Expected: empty descendants map (per Plan 28-03 SUMMARY line 48 — Blog index TagFilter UNCHANGED verification baseline). Record for Task 5 cross-check (Plan 29-01 mutation must NOT affect M1Wu9).

    7. **Extract v1.3 verbatim content** from src/pages/projects/index.astro lines 19-23:
       - Page heading: `Projects` (line 19; uppercase Tailwind class — visual rendering only, not content — ships as written-case 'Projects' per Plan 28-01 precedent line 707)
       - Page-intro body verbatim (lines 20-23): `Real projects, real results. See how I've helped small businesses automate workflows, build custom tools, and leverage AI to work smarter.`
       - 4 filter categories per lines 30-62: `All Projects` (active) / `Web Apps` / `Automation` / `AI Development`

    8. **Extract real project content** from src/data/projects.json (for 2 of the 6 grid cards):
       - bakery-order-system: title "Bakery Order Automation", categoryLabel "Web Apps", problem (first ~160 chars)
       - inventory-sync-automation: title "Multi-Channel Inventory Sync", categoryLabel "Automation", problem (first ~160 chars)

    9. **Plan 4 representative agency-template projects** (cards 3-6 per CONTEXT Claude's Discretion line 119) — examples (plan-execution writes verbatim final content):
       - Card 3: "Custom CRM for Local Real Estate" (Web Apps)
       - Card 4: "AI-Powered Recipe Generator" (AI Development)
       - Card 5: "Restaurant Inventory Dashboard" (Web Apps)
       - Card 6: "Slack Bot for Customer Support" (Automation)
  </action>

  <verify>
    - `mcp__pencil__get_editor_state` returns `active editor == "design/Crito.pen"`
    - Plan 29-00 NEW components verified intact (ProjectCard + ResultsMetrics + RelatedProjects)
    - Y2isa.fill.image returns a filename (record for SUMMARY)
    - design/images/image-import-12.jpg accessible
    - FUctJ exists at page-frame row y ≈ −4111.55
    - O1IwyS returns 10 paired-ref children with ATJK9/UbwMv descendants accessible
    - M1Wu9 baseline descendants recorded for cross-check
    - v1.3 content extraction recorded (page-intro verbatim + filter labels + 2 real projects)
  </verify>

  <acceptance_criteria>
    - [ ] Active editor == `design/Crito.pen`
    - [ ] Plan 29-00 deliverables (`<projectCard-id>`, `<resultsMetrics-id>`, `<relatedProjects-id>`) all return correct structure
    - [ ] Y2isa probe returns a `fill.image` filename string (record verbatim in SUMMARY for D-130 pairing)
    - [ ] design/images/image-import-12.jpg confirmed present (D-130 SECOND pairing target)
    - [ ] FUctJ Tag frame exists at canvas position y ≈ −4111.55 (FindEmptySpace nodeId anchor for Task 1)
    - [ ] O1IwyS returns 10 paired-ref children + ATJK9/UbwMv accessible
    - [ ] M1Wu9 baseline descendants captured for Task 5 cross-check
    - [ ] v1.3 content extracted: page heading + intro body verbatim + 4 filter labels + 2 real projects (title + categoryLabel + problem 160-char truncated)
  </acceptance_criteria>

  <done>Pre-flight + content extraction + raster probes complete; ready for Projects frame construction.</done>
</task>

<task type="auto">
  <name>Task 1: FindEmptySpace + create Projects top-level page frame at 1440 width</name>
  <files>design/Crito.pen (mutation: NEW top-level page frame at canvas page-frame row)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Architecture Patterns (Recommended Project Structure — page-frame row y ≈ −4111) + § Common Pitfalls Pitfall 5 (fit_content quirk + Pencil notes NOT inside page frames)
    - .planning/research/CALIBRATION-PROTOCOL.md § 10.4 (FindEmptySpace nodeId anchor pattern — Plan 26-02 contribution)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md (EDAf1 Blog frame placed at (22407.27, −4111.55) via FindEmptySpace nodeId:n0QqTd anchor) + 28-03-SUMMARY.md Task 1 (FUctJ Tag frame placement — Plan 29-01's anchor)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-138 + D-139 — Projects frame placement specification)
  </read_first>

  <action>
    PRE-FLIGHT: `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    1. **FindEmptySpace with nodeId:FUctJ anchor** (per CONTEXT line 290 + § 10.4 contribution):
       ```
       find_empty_space_on_canvas({
         nodeId: "FUctJ",      // Phase 28 Tag frame anchor per 28-03 SUMMARY
         direction: "right",
         padding: 80,
         width: 1440,           // D-75 desktop page width
         height: 2200            // initial estimate — will fit_content at Task 3 per Pitfall 5 carry-forward
       })
       ```
       Expected: returns {x, y} where y ≈ −4111.55 (page-frame row) — NOT library row y ≈ −11711. If y returns library-row, HALT and surface — anchor not honored.

    2. **Create Projects top-level page frame** at returned (x, y) via `batch_design`:
       ```
       batch_design({
         operations: [
           Insert(<document-root>, {
             type: "frame",
             name: "Projects",
             x: <returned-x>,
             y: <returned-y>,
             width: 1440,
             height: 2200,                // initial estimate; switch to fit_content at Task 3
             layout: "vertical",
             gap: 0,
             padding: 0,
             alignItems: "center",
             fill: "#ffffffff",           // color-semantic-bg-page
             placeholder: true            // Plan 26-02 pattern — placeholder until content settles
           }) → record returned id as <projects-frame-id>
         ]
       })
       ```

    3. **Verify placement:**
       ```
       batch_get(['<projects-frame-id>'], readDepth: 1)
       ```
       Expected: returns frame at x ≈ (FUctJ.x + FUctJ.width + 80), y ≈ −4111.55, width 1440, height 2200, vertical layout, white fill, placeholder:true.
  </action>

  <verify>
    - `find_empty_space_on_canvas` returns y ≈ −4111.55 (page-frame row), NOT library row
    - `batch_get` confirms Projects frame at correct canvas position + width 1440 + vertical layout + placeholder:true
  </verify>

  <acceptance_criteria>
    - [ ] FindEmptySpace honors nodeId:FUctJ anchor (returned y ≈ −4111.55, NOT library row)
    - [ ] NEW Projects top-level page frame exists at document root with width 1440, height 2200 (initial), vertical layout, gap 0, padding 0, alignItems center, fill #ffffffff, placeholder:true
    - [ ] Frame positioned next-right of FUctJ (Phase 28 Tag frame) with 80px padding
    - [ ] `<projects-frame-id>` recorded in SUMMARY for Task 2 + Task 5 + Plan 29-02 Task 1 anchor reference
  </acceptance_criteria>

  <done>Projects frame placed correctly at page-frame row; ready for composition.</done>
</task>

<task type="auto">
  <name>Task 2: Compose Projects frame — Header + page-intro + TagFilter instance (descendants override) + 6-ProjectCard grid + Footer</name>
  <files>design/Crito.pen (mutation: insert composition children into Projects frame)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-138 specifics for Projects frame) + § Claude's Discretion (page-intro variants + 6-card grid sample content)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Code Examples § Example 3 (TagFilter descendants override + paired-ref active-state flip mechanic) + § Example 4 (page-intro Option A default vs Option B coral banner)
    - .planning/research/PEN-INVENTORY.md lines 670-686 (Phase 28 TagFilter + paired-ref + RelatedPosts shape) + lines 703-714 (Plan 28-01 Blog index composition pattern — pivot reference: page-intro + TagFilter ref + 6-Card grid + Footer)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md (Blog index composition pattern — Phase 29-01 mirrors with TagFilter descendants override + ProjectCard refs swap)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-00-SUMMARY.md (<projectCard-id> for the 6 grid refs)
    - src/pages/projects/index.astro lines 19-23 (page-intro verbatim)
    - src/data/projects.json (2 real projects + categoryLabel field)
  </read_first>

  <action>
    PRE-FLIGHT: `mcp__pencil__get_editor_state({include_schema: false})` → assert `active editor == "design/Crito.pen"`.

    Use SEPARATE `batch_design` calls per sub-section to stay under the ~25-ops/call cap per Plan 27-02 Pitfall 9 lesson.

    **Sub-section A: Section/Header instance (D-77 — no override):**
    ```
    batch_design({
      operations: [
        Insert(<projects-frame-id>, {
          type: "ref",
          ref: "G0wNOc",       // Phase 25 Section/Header
          descendants: {}       // D-77: Crito-source nav labels stay through Phase 30
        })
      ]
    })
    ```

    **Sub-section B: page-intro section (DEFAULT Option A per CONTEXT D-138 + RESEARCH § Example 4):**

    DEFAULT (plain centered intro per Phase 26/27 + Plan 28-03 Tag page-intro precedent — Plan 28-01 coral banner was REVISE-loop outcome flagged in OPEN-28-02 as APPROXIMATE; Phase 32 sweep is the systematic refinement point):

    ```
    batch_design({
      operations: [
        // page-intro container frame
        Insert(<projects-frame-id>, {
          type: "frame",
          name: "page-intro",
          layout: "vertical",
          gap: 12,                      // space-primitive-12 (matches Plan 28-03 Tag page-intro rhythm)
          padding: [80, 0, 48, 0],       // matches Plan 28-03 Tag page-intro padding per FUctJ precedent
          alignItems: "center",
          width: 1200                    // matches Phase 25-28 inner content width
        }) → record returned id as <page-intro-id>

        // heading: 'Projects' (v1.3 verbatim — heading-1)
        Insert(<page-intro-id>, {
          type: "text",
          name: "heading",
          content: "Projects",            // v1.3 line 19 verbatim (Tailwind uppercase class is visual-only — ships written-case per Plan 28-01 precedent line 707)
          fontFamily: "Plus Jakarta Sans", // type-semantic-heading-1-family
          fontSize: 48,                    // type-semantic-heading-1-size
          fontWeight: "700",               // type-semantic-heading-1-weight
          fill: "#141f39ff",               // color-semantic-text-primary
          lineHeight: 1.4,                 // type-semantic-heading-1-lh
          textAlign: "center"
        })

        // body: v1.3 lines 20-23 verbatim (prose-paragraph)
        Insert(<page-intro-id>, {
          type: "text",
          name: "body",
          content: "Real projects, real results. See how I've helped small businesses automate workflows, build custom tools, and leverage AI to work smarter.",
          fontFamily: "Inter",            // type-semantic-prose-paragraph-family
          fontSize: 16,                    // type-semantic-prose-paragraph-size
          fontWeight: "400",               // type-semantic-prose-paragraph-weight
          fill: "#52525bff",               // color-semantic-text-secondary
          lineHeight: 1.625,               // type-semantic-prose-paragraph-lh
          textAlign: "center",
          textGrowth: "fixed-width",
          width: 600                       // matches v1.3 max-w-2xl proxy per Plan 28-01 line 708 precedent
        })
      ]
    })
    ```

    **OPTIONAL Option B (coral banner pivot)** — DO NOT ship by default. Reserved for calibration-gate REVISE if user flags hero alignment vs Y2isa raster shows banner-rhythm. Mirror Plan 28-01 line 705 coral-decorative + wave-path pattern; record path as Plan 28-01 precedent inheritance.

    **Sub-section C: Section/TagFilter ref with descendants override per D-137:**

    Reference paired-ref children IDs per PEN-INVENTORY lines 675-686 + Plan 28-03 SUMMARY D-117 active-state flip mechanic:

    ```
    batch_design({
      operations: [
        Insert(<projects-frame-id>, {
          type: "ref",
          ref: "O1IwyS",       // Phase 28 Section/TagFilter
          descendants: {
            // Position 1 — Active label change "All Posts" → "All Projects" (default-active preserved)
            "RxShj": { descendants: { "ATJK9": { content: "All Projects" } } },
            "z1aro": { descendants: { "UbwMv": { content: "All Projects" } } },   // shadow content match
            // Position 2 — "automation" → "Web Apps" (inactive default preserved per v1.3 line 42-46 'web-apps')
            "al86r": { descendants: { "ATJK9": { content: "Web Apps" } } },
            "u3X64E": { descendants: { "UbwMv": { content: "Web Apps" } } },
            // Position 3 — "small-business" → "Automation" (v1.3 line 50-54)
            "C93nA": { descendants: { "ATJK9": { content: "Automation" } } },
            "j4KiI": { descendants: { "UbwMv": { content: "Automation" } } },
            // Position 4 — "productivity" → "AI Development" (v1.3 line 58-62)
            "m35My": { descendants: { "ATJK9": { content: "AI Development" } } },
            "EXgTG": { descendants: { "UbwMv": { content: "AI Development" } } },
            // Position 5 — DISABLE BOTH (v1.3 has only 4 categories per D-137)
            "CCXCE": { enabled: false },
            "hMIRk": { enabled: false }
          }
        }) → record returned id as <tagfilter-instance-id>
      ]
    })
    ```

    **Sub-section D: 6-Compound/ProjectCard grid (3-col × 2-row per D-138 + Plan 28-01 BlogCard precedent):**

    ```
    batch_design({
      operations: [
        // projects-grid container frame (matches Plan 28-01 blog-grid pattern line 711)
        Insert(<projects-frame-id>, {
          type: "frame",
          name: "projects-grid",
          width: 1200,
          layout: "vertical",
          gap: 24,                       // space-semantic-stack-md between rows
          padding: [40, 0, 80, 0],        // matches Plan 28-01 blog-grid padding
          alignItems: "center"
        }) → record returned id as <projects-grid-id>

        // row-1
        Insert(<projects-grid-id>, {
          type: "frame",
          name: "row-1",
          width: "fill_container",
          layout: "horizontal",
          gap: 24,
          alignItems: "start"
        }) → record returned id as <row-1-id>

        // row-2
        Insert(<projects-grid-id>, {
          type: "frame",
          name: "row-2",
          width: "fill_container",
          layout: "horizontal",
          gap: 24,
          alignItems: "start"
        }) → record returned id as <row-2-id>
      ]
    })
    ```

    Then for each of 6 cards (split across 2-3 batch_design calls to stay under 25-ops cap; 2 ops per card = card-1 Insert + descendants), insert ProjectCard refs into <row-1-id> (cards 1-3) and <row-2-id> (cards 4-6):

    ```
    // Sample card 1 (REAL — bakery-order-system):
    Insert(<row-1-id>, {
      type: "ref",
      ref: "<projectCard-id-from-29-00>",
      width: "fill_container",
      descendants: {
        // image-slot stays default placeholder per D-113-style — code milestone wires fill:{type:'image', url:'/images/portfolio/bakery-thumb.jpg'}
        "<category-badge-slot-Badge-ref-id>": { descendants: { "<Badge-label-text-id>": { content: "Web Apps" } } },
        "<title-text-node-id>": { content: "Bakery Order Automation" },
        "<body-text-node-id>": { content: "Sarah's bakery was receiving orders through 5 different channels: phone, SMS, Facebook, Instagram, and email. She spent 4-5 hours daily just trans..." }   // first 157 chars + '...' per v1.3 ProjectCard.astro 160-char truncate
      }
    })

    // Card 2 (REAL — inventory-sync-automation):
    Insert(<row-1-id>, {
      type: "ref",
      ref: "<projectCard-id-from-29-00>",
      width: "fill_container",
      descendants: {
        "<category-badge-slot-Badge-ref-id>": { descendants: { "<Badge-label-text-id>": { content: "Automation" } } },
        "<title-text-node-id>": { content: "Multi-Channel Inventory Sync" },
        "<body-text-node-id>": { content: "A growing e-commerce retailer was selling on Amazon, Shopify, and eBay simultaneously. Inventory counts were updated manually across platforms, ..." }   // 160-char truncate
      }
    })

    // Card 3 (REPRESENTATIVE per Plan 28-01 BlogCard precedent):
    Insert(<row-1-id>, {
      type: "ref",
      ref: "<projectCard-id-from-29-00>",
      width: "fill_container",
      descendants: {
        "<category-badge-slot-Badge-ref-id>": { descendants: { "<Badge-label-text-id>": { content: "Web Apps" } } },
        "<title-text-node-id>": { content: "Custom CRM for Local Real Estate" },
        "<body-text-node-id>": { content: "Independent realtors needed a CRM that didn't require a $5k/month subscription. Built a streamlined client tracking system with lead pipeline auto..." }
      }
    })

    // Cards 4-6 (REPRESENTATIVE) — plan-execution writes 3 more agency-template entries per CONTEXT Claude's Discretion line 119:
    //   Card 4: "AI-Powered Recipe Generator" (AI Development)
    //   Card 5: "Restaurant Inventory Dashboard" (Web Apps)
    //   Card 6: "Slack Bot for Customer Support" (Automation)
    // Each follows the same descendants override pattern.
    ```

    Note on descendants paths: plan-execution must inspect the actual descendant node ids inside Plan 29-00's <projectCard-id> at insert-time via batch_get readDepth 3 to determine the precise nested keys for category-badge-slot's Badge ref + title text + body text. The pseudocode above uses placeholder names.

    **Sub-section E: Section/Footer ref (D-77 — no override):**
    ```
    batch_design({
      operations: [
        Insert(<projects-frame-id>, {
          type: "ref",
          ref: "Xs0Hs",       // Phase 25 Section/Footer
          descendants: {}       // D-77
        })
      ]
    })
    ```

    **Sub-section F: Sibling Pencil note at canvas root** (NOT inside Projects frame per Plan 28-01 quirk):
    ```
    batch_design({
      operations: [
        Insert(<document-root>, {
          type: "note",
          name: "Projects index composition note",
          content: "Projects index page frame (Plan 29-01 per D-127 crito-source-flat-raster branch).\n\n" +
                   "Composition: Section/Header ref (G0wNOc, no override per D-77) + page-intro (v1.3 verbatim from src/pages/projects/index.astro lines 19-23) + Section/TagFilter ref (O1IwyS) with descendants override per D-137 (Position 5 disabled both shadows since v1.3 has only 4 categories) + 6-Compound/ProjectCard grid (3-col × 2-row; 2 real projects.json entries + 4 representative agency-template per Plan 28-01 BlogCard precedent) + Section/Footer ref (Xs0Hs, no override per D-77).\n\n" +
                   "TagFilter validates D-115 broad-scoping with THIRD consumer (Blog + Tag + Projects). Plan 28-01 Blog index TagFilter instance (M1Wu9) verified UNCHANGED post-mutation — per-instance descendants behavior validated.\n\n" +
                   "PAGE-11 ACTIVE on Y2isa fires AT TASK 5 post-APPROVE per § 3.3 + Pitfall 4. Image references (project thumbnails) ship as placeholder content; code milestone wires real images.\n\n" +
                   "Calibration gate at Task 4 uses DUAL pairing per D-130: Y2isa raster overall page-layout + image-import-12.jpg ProjectCard fidelity visual proxy per OPEN-25-07 line 303.",
          width: 1200,
          height: 280
        })
      ]
    })
    ```

    Finally, verify composition:
    ```
    batch_get(['<projects-frame-id>'], readDepth: 3)
    ```
    Expected: 5 children — Header ref + page-intro frame + TagFilter ref (with descendants overrides) + projects-grid frame (with 2 row frames containing 3 cards each) + Footer ref.
  </action>

  <verify>
    - `batch_get(['<projects-frame-id>'], readDepth: 3)` returns 5 children in order: Header ref + page-intro + TagFilter ref + projects-grid + Footer ref
    - projects-grid has 2 row frames (row-1 + row-2); each row has 3 ProjectCard refs
    - TagFilter instance descendants override applied: ATJK9/UbwMv pill labels swapped to All Projects/Web Apps/Automation/AI Development; CCXCE + hMIRk enabled:false
    - Each ProjectCard ref has descendants override for title + category-badge label + body
    - Sibling Pencil note at canvas root documents composition + D-130 dual-pairing + Y2isa PAGE-11 ACTIVE deferred to Task 5
  </verify>

  <acceptance_criteria>
    - [ ] Projects frame children count == 5 (Header ref + page-intro + TagFilter ref + projects-grid + Footer ref)
    - [ ] page-intro contains heading 'Projects' (heading-1 PJS 48/700/lh 1.4 navy textAlign center) + body verbatim from v1.3 lines 20-23 (prose-paragraph Inter 16/400/lh 1.625 secondary textAlign center width 600)
    - [ ] TagFilter instance (<tagfilter-instance-id>): Position 1 'All Projects' (RxShj+z1aro labels) + Position 2 'Web Apps' (al86r+u3X64E) + Position 3 'Automation' (C93nA+j4KiI) + Position 4 'AI Development' (m35My+EXgTG) + Position 5 BOTH enabled:false (CCXCE+hMIRk)
    - [ ] projects-grid: 1200w vertical 24 gap [40,0,80,0] padding center-aligned; 2 row frames (row-1 + row-2) each horizontal 24 gap fill_container width with 3 ProjectCard refs
    - [ ] 6 ProjectCard refs total: cards 1-2 real (bakery-order-system + inventory-sync-automation) + cards 3-6 representative (4 agency-template), each with descendants override on title + category-badge label + body (~160 char truncate)
    - [ ] Section/Footer ref (Xs0Hs, no override)
    - [ ] Sibling Pencil note at canvas root
  </acceptance_criteria>

  <done>Projects frame composed; ready for plan-close sweep + calibration gate.</done>
</task>

<task type="auto">
  <name>Task 3: Plan-close layout sweep iteration 1 + fit_content settle</name>
  <files>design/Crito.pen (sweep + minor frame-property update)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Common Pitfalls Pitfall 5 (fit_content quirk) + Pitfall 6 (snapshot_layout text-clipping false positives)
    - .planning/research/CALIBRATION-PROTOCOL.md § 4.4 step 8 (fit_content + position clear pre-screenshot pattern — Plan 26-02 contribution)
  </read_first>

  <action>
    PRE-FLIGHT: `mcp__pencil__get_editor_state({include_schema: false})`.

    1. **Page-frame fit_content + placeholder clear (Plan 26-02 pattern per § 4.4 step 8):**
       ```
       batch_design({
         operations: [
           Update("<projects-frame-id>", {
             placeholder: false,
             height: "fit_content"
           })
         ]
       })
       ```

    2. **Document-root layout sweep:**
       ```
       snapshot_layout({maxDepth: 0, problemsOnly: true})
       ```
       Expected: `"No layout problems."` per Phase 24-28 plan-close discipline. Document any text-clipping false positives per Pitfall 6 (benign Pencil quirk on text-y-coordinate in auto-layout frames; carry-forward from Phase 24-28).

    3. **Per-frame composition verification:**
       ```
       batch_get(['<projects-frame-id>'], readDepth: 3)
       ```
       Verify all 5 children present + correct order + child counts intact.

    4. **Plan 28-01 Blog index TagFilter integrity cross-check** (per Plan 28-03 SUMMARY pattern — confirm Plan 29-01's TagFilter descendants override did NOT bleed into M1Wu9):
       ```
       batch_get(['M1Wu9'], readDepth: 2)
       ```
       Expected: M1Wu9 descendants map STILL EMPTY (Plan 28-01's pre-configured default 'All Posts' active). If non-empty: HALT — per-instance descendants behavior broken, surface to user.

    5. **Token surface no-drift verification:**
       ```
       get_variables({})
       ```
       Expected: count UNCHANGED from Plan 29-00 close (Plan 29-01 ships zero new tokens — composes from Phase 23-28 + Plan 29-00 surface).
  </action>

  <verify>
    - Projects frame height switched to fit_content + placeholder:false
    - `snapshot_layout({maxDepth: 0, problemsOnly: true})` returns `"No layout problems."`
    - `batch_get(['<projects-frame-id>'], readDepth: 3)` confirms 5 children in correct order
    - `batch_get(['M1Wu9'], readDepth: 2)` returns EMPTY descendants (Plan 28-01 instance UNCHANGED)
    - `get_variables({})` count UNCHANGED from Plan 29-00 close
  </verify>

  <acceptance_criteria>
    - [ ] Projects frame height: fit_content + placeholder:false
    - [ ] Document-root layout sweep returns "No layout problems."
    - [ ] Projects frame composition intact (5 children, correct order)
    - [ ] M1Wu9 (Plan 28-01 Blog index TagFilter instance) descendants map still EMPTY — per-instance behavior verified
    - [ ] Token surface drift == 0 (no new tokens shipped in Plan 29-01)
  </acceptance_criteria>

  <done>Composition structurally clean; ready for calibration gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Calibration gate — single AskUserQuestion per § 3.4 with DUAL pairing per D-130</name>
  <files>design/Crito.pen (read-only — get_screenshot for visual + Y2isa + image-import-12.jpg pairing)</files>

  <read_first>
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.1 (VALID-02 crito-source side-by-side) + § 3.2 (VALID-01 fidelity definitions) + § 3.4 step 4-6 (per-step script — calibration gate + AskUserQuestion) + § 6.4 (OPEN-26-02 stale-cache get_screenshot Tier-1/Tier-2 workarounds)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-130 dual pairing) + § Claude's Discretion (cross-row stale-cache fallback)
    - .planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md (first crito-source-flat-raster calibration gate in v2.0 — pattern to mirror)
    - .planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md (BlogCard REVISE-loop lesson; Plan 29-01 commits ProjectCard upfront per D-131 to skip this risk)
  </read_first>

  <what-built>
    Projects index top-level page frame at canvas page-frame row (y ≈ −4111.55) with composition:
    - Section/Header ref (G0wNOc, no override per D-77)
    - page-intro section (1200w, 80/0/48/0 padding, alignItems center): 'Projects' heading-1 + v1.3 verbatim body
    - Section/TagFilter ref (O1IwyS) with descendants override per D-137: 'All Projects' active (Position 1) + Web Apps + Automation + AI Development (Positions 2-4) + Position 5 disabled both shadows
    - projects-grid (1200w, vertical, 24 gap, [40,0,80,0] padding): 2 row frames each containing 3 Compound/ProjectCard refs with descendants overrides (cards 1-2 real bakery-order-system + inventory-sync-automation; cards 3-6 representative agency-template)
    - Section/Footer ref (Xs0Hs, no override per D-77)

    NOT YET hidden: Y2isa raster (PAGE-11 ACTIVE strict observance per Pitfall 4 — fires ONLY after APPROVE).
  </what-built>

  <how-to-verify>
    PRE-FLIGHT: `mcp__pencil__get_editor_state({include_schema: false})`.

    1. **Capture inline screenshot of Projects frame:**
       ```
       get_screenshot({nodeId: "<projects-frame-id>"})
       ```
       If returns blank-white → OPEN-26-02 stale-cache. Apply Tier-1 cross-row Update workaround per § 6.4:
       ```
       Update("<projects-frame-id>", {y: -11711})    // library row
       Update("<projects-frame-id>", {y: -4111.55})   // back to page row
       get_screenshot({nodeId: "<projects-frame-id>"})
       ```
       If Tier-1 fails → Tier-2 fallback: instruct user to verify visually in Pencil's editor at canvas position (<x>, <y>); structural verification via batch_get + snapshot_layout remains authoritative per § 6.4.

    2. **Identify pairing references per D-130 DUAL pairing:**
       - **Pairing 1 (Y2isa overall page-layout):** Y2isa raster filename from Task 0 probe — e.g., `design/images/image-import-NN.jpg` (record actual filename from Task 0)
       - **Pairing 2 (image-import-12.jpg ProjectCard visual proxy):** `design/images/image-import-12.jpg` per OPEN-25-07 line 303 carry-forward (Project lower 2x2 grid)

    3. **Fire single AskUserQuestion per § 3.4 step 5 with DUAL pairing structure:**

       **Question:** "Calibration spot-check — Projects index (Plan 29-01 / crito-source-flat-raster per D-127). Does the reconstructed composition match the Crito source AND the ProjectCard fidelity proxy?"

       **Description (markdown):**
       ```
       ## Calibration Spot-Check — Projects Index (Plan 29-01 / crito-source-flat-raster branch per D-127)

       **Inline render shown above:** Projects frame (id <projects-frame-id>) at canvas (<x>, <y>)
       **Pairing 1 (Y2isa overall page-layout per § 3.1 VALID-02):** design/images/<Y2isa-image-import-NN.jpg> (record from Task 0)
       **Pairing 2 (image-import-12.jpg ProjectCard visual proxy per D-130 + OPEN-25-07):** design/images/image-import-12.jpg

       ### Section 1: Section/Header (Phase 25 G0wNOc ref, no override per D-77)
       Fidelity label proposal: EXACT per D-83

       ### Section 2: page-intro (v1.3 verbatim from src/pages/projects/index.astro lines 19-23)
       Fidelity label proposal: EXACT per D-83 (v1.3 verbatim brand-neutral structural label + body)
       Semantic tokens consumed: type-semantic-heading-1-* + type-semantic-prose-paragraph-* + color-semantic-text-primary + color-semantic-text-secondary

       ### Section 3: Section/TagFilter instance (O1IwyS ref + descendants override per D-137)
       Fidelity label proposal: APPROXIMATE per D-83 (broad-scoping third consumer with category-label descendants override; Position 5 disabled at both shadows since v1.3 has only 4 categories)
       Validates Phase 28 D-115 broad-scoping with third consumer; Plan 28-01 Blog index instance (M1Wu9) verified UNCHANGED at Task 3.

       ### Section 4: 6-Compound/ProjectCard grid (Plan 29-00 NEW component, 2 real + 4 representative instances)
       Fidelity label proposal: APPROXIMATE per D-83 (representative content per Plan 28-01 BlogCard precedent; 2 real projects.json entries + 4 agency-template; image-slots ship default placeholder per D-113 — code milestone wires real images)
       Validates D-131 upfront commit (ProjectCard ships per image-import-12.jpg + v1.3 src dual evidence)

       ### Section 5: Section/Footer (Phase 25 Xs0Hs ref, no override per D-77)
       Fidelity label proposal: EXACT per D-83

       ### Calibration target (D-127 dual pairing per D-130):
       1. Does the Projects frame match Y2isa raster overall page-layout (header + page-intro + filter strip + grid + footer rhythm)?
       2. Does the 6-ProjectCard grid match image-import-12.jpg Project lower 2x2 grid visual fidelity (card shape + category badge + title + body proportion)?

       **Description identifiers (per § 3.5 — INLINE ONLY, NOT disk-written):**
       - 29-projects-frame--side-by-side-Y2isa.png
       - 29-projects-card-grid--side-by-side-image-import-12.png
       ```

       **Options:**
       - "APPROVE — composition matches both pairings; fidelity labels per D-83 as proposed"
       - "REVISE — composition needs adjustment (describe gap; loop back to Task 2)"
       - "GAP — composition reveals declared deviation (raise OPEN-29-NN; proceed to Task 6 with documented gap)"

    4. **Record user decision** in plan SUMMARY for Task 5 + Task 6 sequencing.
  </how-to-verify>

  <resume-signal>
    Reply with `APPROVE`, `REVISE: <gap description>`, or `GAP: <gap description>` to continue.

    - APPROVE → Task 5 fires PAGE-11 ACTIVE Update("Y2isa", {enabled: false}) + Task 6 PEN-INVENTORY mutations
    - REVISE → loop back to Task 2 with described adjustments (e.g., page-intro coral banner pivot per Plan 28-01 precedent; ProjectCard title heading-4 downsize; Badge variant swap to kJQmJ Outline; etc.)
    - GAP → proceed to Task 6 + raise OPEN-29-NN with declared deviation; PAGE-11 ACTIVE still fires per § 3.4 step 7 unless gap is severe enough to defer
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: PAGE-11 ACTIVE on Y2isa (post-APPROVE ONLY per Pitfall 4)</name>
  <files>design/Crito.pen (mutation: Y2isa.enabled = false)</files>

  <read_first>
    - .planning/research/PITFALLS.md Pitfall 4 (NEVER hide raster before APPROVE)
    - .planning/research/CALIBRATION-PROTOCOL.md § 3.3 (PAGE-11 ACTIVE pattern) + § 3.4 step 7 (Update raster enabled:false post-APPROVE)
    - .planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md (cl8tt first production use of PAGE-11 ACTIVE) + .planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md + 28-02-SUMMARY.md (DzqTm + w1m3x second + third uses)
    - Task 4 user decision (APPROVE / REVISE / GAP)
  </read_first>

  <action>
    **Conditional execution per Task 4 outcome:**

    - **If REVISE:** SKIP this task; loop back to Task 2 with the described gap. Re-fire Task 4 after iteration.
    - **If APPROVE or GAP-with-proceed:** Continue.

    PRE-FLIGHT: `mcp__pencil__get_editor_state({include_schema: false})`.

    1. **Fire PAGE-11 ACTIVE on Y2isa** per § 3.4 step 7:
       ```
       batch_design({
         operations: [
           Update("Y2isa", { enabled: false })
         ]
       })
       ```
       This is the FOURTH production use of PAGE-11 ACTIVE in v2.0 (after cl8tt + DzqTm + w1m3x). Y2isa structural archive persists at original canvas position per § 3.3.

    2. **Verify:**
       ```
       batch_get(['Y2isa'], readDepth: 1)
       ```
       Expected: Y2isa.enabled == false; x, y unchanged (structural archive at original position).
  </action>

  <verify>
    - Task 4 outcome == APPROVE (or GAP-with-proceed)
    - `batch_get(['Y2isa'], readDepth: 1)` returns enabled:false; x + y unchanged from baseline (structural archive preserved)
  </verify>

  <acceptance_criteria>
    - [ ] Task 4 user decision was APPROVE (or GAP-with-proceed for severe gap exceptions)
    - [ ] Y2isa.enabled == false post-mutation
    - [ ] Y2isa canvas position UNCHANGED from baseline (structural archive persists per § 3.3)
    - [ ] FOURTH production use of PAGE-11 ACTIVE in v2.0 recorded in SUMMARY
  </acceptance_criteria>

  <done>Y2isa hidden via PAGE-11 ACTIVE post-APPROVE; structural archive preserved at original canvas position.</done>
</task>

<task type="auto">
  <name>Task 6: PEN-INVENTORY mutations per D-128 + D-141 (FIRST non-additive reclassification in v2.0)</name>
  <files>.planning/research/PEN-INVENTORY.md (mutation: Y2isa row reclassified IN-PLACE + new Projects row + Variant Evidence + OPEN flag updates)</files>

  <read_first>
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md § Decisions (D-128 + D-141) + § Specifics (D-128 first non-additive change)
    - .planning/research/PEN-INVENTORY.md line 71 (Y2isa baseline row to mutate) + line 67 (cl8tt Phase 27 reclassification precedent for parallel pattern) + lines 703-716 (Plan 28-01 EDAf1 Blog frame additions format precedent)
    - .planning/phases/29-projects-reconstruction-index-project-detail/29-RESEARCH.md § Common Pitfalls Pitfall 2 (reclassification mutation order)
    - Task 4 user decision (APPROVE / REVISE / GAP) + Task 5 outcome
    - Plan 29-01 SUMMARY notes (image-import-NN.jpg index identification, Badge variant choice, title heading size, OPEN-29-NN flags)
  </read_first>

  <action>
    1. **Mutate Y2isa row IN-PLACE** per D-128 (parallel to cl8tt + DzqTm + w1m3x reclassification chain). Edit PEN-INVENTORY.md line 71 from:
       ```
       | 05_Service | Y2isa | IN-SCOPE token-mining-only | none (token mining only — per D-07) | 0 | flat:1, partial:0, factored:0 | n/a (out of scope) | OPEN-23-05 |
       ```
       TO:
       ```
       | 05_Service | Y2isa | reconstructed-PHASE-29 | Projects (per D-06; reconstructed as `Projects` frame <projects-frame-id> per Plan 29-01) | 0 | hidden:1, partial:0, factored:0 | n/a (Phase 29 plan 29-01 — reconstructed; Y2isa raster hidden via enabled:false per PAGE-11 ACTIVE post-APPROVE at calibration gate; structural archive persists at canvas (<x>, <y>) per § 3.3) | OPEN-23-05 (Plan 29-01 partial-resolution — only 04_About remains long-tail) |
       ```
       Key mutations per D-128 + D-141:
       - scope: `IN-SCOPE token-mining-only` → `reconstructed-PHASE-29` (drops token-mining-only qualifier)
       - joel_page_map: `none (token mining only — per D-07)` → `Projects (per D-06; reconstructed as \`Projects\` frame <projects-frame-id> per Plan 29-01)`
       - status_counts: `flat:1, partial:0, factored:0` → `hidden:1, partial:0, factored:0`
       - reconstruction_priority: `n/a (out of scope)` → `n/a (Phase 29 plan 29-01 — reconstructed; ... structural archive persists at canvas (<x>, <y>))`
       - open_flag_ids: `OPEN-23-05` → `OPEN-23-05 (Plan 29-01 partial-resolution — only 04_About remains long-tail)`

    2. **Add NEW Projects frame row** to Frames Inventory (after the existing IN-SCOPE rows, parallel to Plan 27-02 + 28-01/02/03 page-frame additions):
       ```
       | **Projects** | **<projects-frame-id>** | **IN-SCOPE** | **/projects (per Plan 29-01; v1.3 src/pages/projects/index.astro)** | **5** | **flat:0, partial:0, factored:5** | **n/a (Phase 29 plan 29-01 — reconstructed; first crito-source-flat-raster branch use in Phase 29; FOURTH PAGE-11 ACTIVE production use in v2.0; THIRD Section/TagFilter consumer validating D-115 broad-scoping; first Compound/ProjectCard consumer at scale)** | — |
       ```

    3. **Append NEW Plan 29-01 additions section** to PEN-INVENTORY § Variant Evidence (after Plan 29-00 additions section). Use Edit tool to insert:
       ```
       ### Plan 29-01 additions (Projects index page frame reconstruction — crito-source-flat-raster branch; FOURTH PAGE-11 ACTIVE production use)

       Plan 29-01 reconstructs the Crito 05_Service flat raster (Y2isa) as an editable Projects top-level page frame at canvas (<x>, <y>). Composition: Section/Header ref + page-intro (v1.3 verbatim) + Section/TagFilter instance with descendants override per D-137 (THIRD consumer validates D-115 broad-scoping) + 6-Compound/ProjectCard grid (first instance consumers of Plan 29-00 NEW component; 2 real projects.json + 4 representative agency-template per Plan 28-01 BlogCard precedent) + Section/Footer. Calibration gate per § 3.4 with DUAL pairing per D-130 (Y2isa raster + image-import-12.jpg ProjectCard visual proxy). Plan 28-01 Blog index TagFilter instance (M1Wu9) verified UNCHANGED at Task 3 cross-check — per-instance descendants behavior validated. Y2isa raster filename identified: <image-import-NN.jpg from Task 0 probe>.

       | node | element | properties | values (literal) | semantic ref / source | source_evidence | rationale |
       |---|---|---|---|---|---|---|
       | **Projects page frame** (<projects-frame-id>) | parent frame | type/width/height/layout/gap/padding/alignItems/fill | frame/1440/fit_content/vertical/0/0/center/#ffffffff | — / D-75 desktop page width / Plan 26-02 fit_content pattern / Pitfall 5 outer-vertical + inner-1200-centered / — / — / — / color-semantic-bg-page | Plan 29-01 + § 3.4 + Pitfall 3 + FUctJ FindEmptySpace anchor per § 10.4 | FOURTH crito-source-flat-raster page frame reconstructed (after Contact + Blog index + Blog post). Anchored on FUctJ Tag frame at (x, y ≈ -4111.55). fit_content height after content settles per Plan 26-02 contribution |
       | Projects / Section/Header instance (<header-ref-id>, ref G0wNOc) | ref + descendants | type/ref/descendants | ref/G0wNOc/(none — D-77) | — / Phase 25 Section/Header / — | D-77 | No descendants override per Crito-source nav labels stay through Phase 30 |
       | Projects / page-intro (<page-intro-id>) | section frame | type/width/layout/gap/padding/alignItems | frame/1200/vertical/12/[80,0,48,0]/center | — / inner content width / — / space-primitive-12 / matches Plan 28-03 Tag page-intro padding / — | Plan 29-01 + § 3.4 + Plan 28-03 Tag precedent | Centered page-intro per Phase 26/27/28 plain-intro default. Coral banner Option B reserved for REVISE pivot |
       | Projects / page-intro / heading | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight/textAlign | 'Projects'/Plus Jakarta Sans/48/700/#141f39ff/1.4/center | — / type-semantic-heading-1-family / type-semantic-heading-1-size / type-semantic-heading-1-weight / color-semantic-text-primary / type-semantic-heading-1-lh / — | Plan 29-01 + v1.3 src/pages/projects/index.astro line 19 | EXACT per D-83. Tailwind uppercase visual-only — ships written-case per Plan 28-01 line 707 precedent |
       | Projects / page-intro / body | text | content/fontFamily/fontSize/fontWeight/fill/lineHeight/textAlign/textGrowth/width | (v1.3 verbatim 'Real projects, real results...')/Inter/16/400/#52525bff/1.625/center/fixed-width/600 | — / type-semantic-prose-paragraph-family / type-semantic-prose-paragraph-size / type-semantic-prose-paragraph-weight / color-semantic-text-secondary / type-semantic-prose-paragraph-lh / — / — / Pencil § 11 centered body width matches v1.3 max-w-2xl proxy | Plan 29-01 + v1.3 verbatim lines 20-23 | EXACT per D-83 (brand-neutral structural body) |
       | Projects / Section/TagFilter instance (<tagfilter-instance-id>, ref O1IwyS) | ref + descendants | type/ref/descendants | ref/O1IwyS/(10-key descendants override map) | — / Phase 28 Section/TagFilter / D-137 third-consumer + D-117 paired-ref active-state-flip | D-137 + Plan 28-03 D-117 mechanism extension | Validates Phase 28 D-115 broad-scoping with THIRD consumer (Blog + Tag + Projects). Position 1 'All Projects' active (RxShj+z1aro labels) + Positions 2-4 'Web Apps'/'Automation'/'AI Development' inactive (al86r/u3X64E + C93nA/j4KiI + m35My/EXgTG labels) + Position 5 DISABLED both shadows (CCXCE+hMIRk) since v1.3 has only 4 categories |
       | Projects / projects-grid (<projects-grid-id>) | section frame | type/width/layout/gap/padding/alignItems | frame/1200/vertical/24/[40,0,80,0]/center | — / inner content width / — / space-semantic-stack-md / matches Plan 28-01 blog-grid padding / — | Plan 29-01 + Plan 28-01 line 711 blog-grid precedent + D-114 carry-forward | Container for 6 ProjectCard refs (3-col × 2-row). 40px top breathing room separates from TagFilter; 80px bottom matches page-section rhythm |
       | Projects / projects-grid / row-1 + row-2 | row frames × 2 | type/width/layout/gap/alignItems | frame/fill_container/horizontal/24/start | — / — / — / space-semantic-stack-md / — | Plan 29-01 + D-114 3-col grid distribution + Plan 28-01 line 712 precedent | Horizontal row frames; each contains 3 ProjectCard refs at fill_container width so cards distribute evenly across 1200 |
       | Projects / projects-grid / row-{1,2} / ProjectCard-{1..6} (6 refs of <projectCard-id>) | ref × 6 | type/ref/width/descendants | ref/<projectCard-id>/fill_container/{ category-badge-slot Badge label + title content + body content (~160 char) } | — / Plan 29-00 Compound/ProjectCard / — / D-131 first instance consumers + Plan 28-01 BlogCard descendants override pattern | Plan 29-01 + 2 real projects.json (bakery-order-system + inventory-sync-automation) + 4 representative agency-template + v1.3 ProjectCard.astro 160-char truncate convention | First instance consumers of Plan 29-00 NEW Compound/ProjectCard component. Image-slots use default placeholder (code milestone wires real thumbnails via project.thumbnail field) |
       | Projects / Section/Footer instance (<footer-ref-id>, ref Xs0Hs) | ref + descendants | type/ref/descendants | ref/Xs0Hs/(none — D-77) | — / Phase 25 Section/Footer / — | D-77 | No descendants override |
       | Projects sibling Pencil note | note | content (composition + D-130 dual-pairing + PAGE-11 ACTIVE deferral) | (multi-line) | — | Plan 29-01 + D-52 belt-and-suspenders | Documents reconstruction intent for downstream code-milestone consumers |
       | **Y2isa raster mutation (FOURTH PAGE-11 ACTIVE production use)** | frame property mutation | enabled | false (was true) | — / PAGE-11 ACTIVE per § 3.3 + § 3.4 step 7 post-APPROVE only | CALIBRATION-PROTOCOL § 3.3 + Plan 27-02 + Plan 28-01/02 precedent | FOURTH production use of PAGE-11 ACTIVE in v2.0. Y2isa canvas position UNCHANGED at original (<x>, <y>) per § 3.3 structural archive persistence. OPEN-23-05 partial-resolution (Y2isa hidden; cYlRH next at Plan 29-02; 04_About remains long-tail) |

       **Plan 29-01 Variant Evidence outcome:** ~12 rows documenting Projects frame composition + Y2isa PAGE-11 ACTIVE mutation. Token drift: <baseline> → <baseline> (zero new tokens per D-91 reuse — entirely composes from Phase 23-28 + Plan 29-00 surface). Library count UNCHANGED at <28 or 29 per Plan 29-00 close>.

       **Library count after Phase 29 plan 29-01:** UNCHANGED — no new library entries (Plan 29-01 is page-frame composition only).

       **Page-frame count after Phase 29 plan 29-01:** 7 → **8** reconstructed top-level page frames in v2.0: FAQ + 404 + Thank-you + Contact + Blog Index + Blog Post + Tag + **Projects**. Plus Y2isa raster hidden via PAGE-11 ACTIVE (joined cl8tt + DzqTm + w1m3x in hidden-state). PAGE-11 ACTIVE production-use count: 3 → **4**.
       ```

    4. **Update OPEN flag annotations:**
       - **OPEN-23-05** partial-resolution annotation: extend to note Y2isa hidden post-Plan-29-01 APPROVE. Only `04_About` and (pending Plan 29-02) `cYlRH` remain in long-tail. Plan 29-02 will further partial-resolve.
       - **OPEN-25-07** partial-resolution annotation: Plan 29-01 ships FIRST Compound/ProjectCard instance consumers (6 instances in grid). Plan 29-02 will ship 2-3 more (RelatedProjects strip). Final resolution moves to Plan 29-02 close per Phase 29 phase-close pattern.
       - **OPEN-23-12** status check: ProjectCard category-badge instances in Projects grid use Badge primitive with cornerRadius (per Badge.j0FxQZ default or kJQmJ Outline). Plan 29-01 does NOT ship radius-semantic-pill token; Plan 29-02 + Phase 32 sweep evaluate if enough rounded-pill consumer evidence emerges.

    5. **Add any OPEN-29-NN rows** per Task 4 calibration gate outcome:
       - If APPROVE-clean: no new OPEN flags.
       - If APPROVE-with-GAP: raise OPEN-29-NN per declared gap (e.g., page-intro hero-rhythm gap per Plan 28-01 OPEN-28-02 + Plan 28-03 OPEN-28-06 precedent).
       - If GAP-proceed: raise OPEN-29-NN with severity + consumer phase + resolution path per § 7 template.
  </action>

  <verify>
    - PEN-INVENTORY Y2isa row mutated IN-PLACE: scope dropped 'token-mining-only'; joel_page_map: 'Projects'; status_counts: 'hidden:1'; reconstruction_priority: 'Phase 29 plan 29-01 — reconstructed'
    - PEN-INVENTORY has NEW Projects row at Frames Inventory
    - PEN-INVENTORY has NEW "### Plan 29-01 additions" section with explanatory prose + ~12-row Variant Evidence table + library/page-frame count updates
    - OPEN-23-05 + OPEN-25-07 + OPEN-23-12 annotations updated
    - Any OPEN-29-NN rows added per Task 4 calibration gate outcome
    - `grep -c "reconstructed-PHASE-29" .planning/research/PEN-INVENTORY.md` >= 1 (Y2isa row now reclassified)
  </verify>

  <acceptance_criteria>
    - [ ] PEN-INVENTORY Y2isa row mutated IN-PLACE (drops token-mining-only qualifier; joel_page_map 'none' → 'Projects'; status_counts flat:1 → hidden:1; reconstruction_priority updated)
    - [ ] PEN-INVENTORY has NEW Projects row at IN-SCOPE section
    - [ ] PEN-INVENTORY has NEW "### Plan 29-01 additions" section + Variant Evidence rows (~12) + library/page-frame count updates
    - [ ] OPEN-23-05 partial-resolution annotation extended (only 04_About + cYlRH-pending remain long-tail)
    - [ ] OPEN-25-07 partial-resolution annotation extended (ProjectCard first 6 instance consumers in Projects index; Plan 29-02 ships more)
    - [ ] OPEN-23-12 status check annotation (Plan 29-02 + Phase 32 final evaluation)
    - [ ] Any OPEN-29-NN rows added per Task 4 outcome
    - [ ] Page-frame count moves 7 → 8; PAGE-11 ACTIVE count moves 3 → 4
  </acceptance_criteria>

  <done>PEN-INVENTORY syntactically valid + cross-references intact + reclassification mutations recorded; first non-additive change in v2.0 complete.</done>
</task>

</tasks>

<verification>
**Plan-level verification (after Task 6 close):**

1. `mcp__pencil__get_editor_state({include_schema: false})` returns active editor == `design/Crito.pen` throughout
2. `mcp__pencil__batch_get(['<projects-frame-id>'], readDepth: 3)` returns Projects frame with 5 children (Header + page-intro + TagFilter + projects-grid + Footer)
3. `mcp__pencil__batch_get(['<tagfilter-instance-id>'], readDepth: 4)` returns Position 1-4 label overrides applied + Position 5 (CCXCE+hMIRk) enabled:false
4. `mcp__pencil__batch_get(['M1Wu9'], readDepth: 2)` returns EMPTY descendants (Plan 28-01 Blog index TagFilter UNCHANGED)
5. `mcp__pencil__batch_get(['Y2isa'], readDepth: 1)` returns enabled:false (post-APPROVE)
6. `mcp__pencil__snapshot_layout({maxDepth: 0, problemsOnly: true})` returns `"No layout problems."` at document root
7. `grep -c "reconstructed-PHASE-29" .planning/research/PEN-INVENTORY.md` >= 1 (Y2isa reclassified)
8. `grep -c "Plan 29-01 additions" .planning/research/PEN-INVENTORY.md` >= 1
9. Page-frame count == 8 reconstructed; PAGE-11 ACTIVE count == 4
</verification>

<success_criteria>
**Plan 29-01 success criteria (measurable):**

1. ✅ NEW Projects top-level page frame exists at canvas page-frame row (y ≈ −4111.55) with width 1440, vertical layout, 5 children in correct order
2. ✅ Section/Header ref (G0wNOc, no override per D-77) at top
3. ✅ page-intro section: v1.3 verbatim from src/pages/projects/index.astro lines 19-23 (heading 'Projects' + body) at heading-1 + prose-paragraph tokens
4. ✅ Section/TagFilter ref (O1IwyS) with descendants override per D-137: 4 pill labels overridden ('All Projects' / 'Web Apps' / 'Automation' / 'AI Development'); Position 5 disabled at both shadows
5. ✅ projects-grid (1200w, 3-col × 2-row): 6 Compound/ProjectCard refs with descendants overrides (2 real projects + 4 representative agency-template)
6. ✅ Section/Footer ref (Xs0Hs, no override per D-77)
7. ✅ Plan 28-01 Blog index TagFilter instance (M1Wu9) verified UNCHANGED post-mutation (per-instance descendants behavior validated)
8. ✅ Calibration gate per § 3.4 with DUAL pairing per D-130 closed with user decision (APPROVE / REVISE / GAP)
9. ✅ On APPROVE: PAGE-11 ACTIVE fires on Y2isa (Y2isa.enabled == false; structural archive preserved at original canvas position)
10. ✅ PEN-INVENTORY Y2isa row mutated IN-PLACE per D-128 (FIRST non-additive change in v2.0)
11. ✅ PEN-INVENTORY NEW Projects frame row added; Plan 29-01 additions section with ~12 Variant Evidence rows
12. ✅ OPEN-23-05 + OPEN-25-07 + OPEN-23-12 annotations updated; any OPEN-29-NN raised per calibration outcome
13. ✅ Layout sweep clean at plan close
14. ✅ Page-frame count moves 7 → 8; PAGE-11 ACTIVE count moves 3 → 4
15. ✅ Token surface drift == 0 (Plan 29-01 ships zero new tokens — composes from Phase 23-28 + Plan 29-00 surface)

**Downstream consumer readiness:**
- Plan 29-02 can FindEmptySpace next-right of <projects-frame-id> for Project detail frame placement
- Plan 29-02 can instance Compound/ProjectCard for RelatedProjects strip (2-3 refs)
- Phase 30 Design-system reference can showcase Section/TagFilter as 3-consumer reusable component (Blog + Tag + Projects validated)
</success_criteria>

<output>
After Task 6 completes, write `.planning/phases/29-projects-reconstruction-index-project-detail/29-01-SUMMARY.md` covering:

- Pencil MCP Subagent-Tool-Inheritance Caveat (INLINE execution confirmation)
- Task 0: Pre-flight outcome (Y2isa image-import-NN.jpg filename identified, image-import-12.jpg confirmed accessible, FUctJ anchor verified, O1IwyS + M1Wu9 baselines captured, v1.3 content extracted)
- Task 1: Projects frame placement outcome (FindEmptySpace returned coordinates + frame id)
- Task 2: Composition outcome (5 children inserted; TagFilter descendants override pattern applied; 6-ProjectCard grid with descendants overrides; sibling Pencil note)
- Task 3: Layout sweep iteration 1 outcome (fit_content settle, snapshot_layout clean, M1Wu9 cross-check passed, token drift 0)
- Task 4: Calibration gate outcome (APPROVE / REVISE / GAP — record verbatim; any get_screenshot Tier-1/Tier-2 fallback path taken)
- Task 5: Y2isa PAGE-11 ACTIVE outcome (Y2isa.enabled:false; FOURTH production use)
- Task 6: PEN-INVENTORY mutations outcome (Y2isa reclassified IN-PLACE; Projects row added; ~12 Variant Evidence rows; OPEN flag updates)
- Per-section fidelity labels per D-83 (per user APPROVE decision)
- Token surface change (UNCHANGED from Plan 29-00 close)
- Library count change (UNCHANGED)
- Page-frame count change (7 → 8)
- PAGE-11 ACTIVE production-use count (3 → 4)
- OPEN flags raised + updated
- Plan 29-02 readiness checklist (Projects frame id available as FindEmptySpace anchor for Project detail; ProjectCard component validated at scale)
</output>
