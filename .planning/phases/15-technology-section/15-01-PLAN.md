---
phase: 15-technology-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/illustrations/TechAI.svg
  - src/components/illustrations/TechAutomations.svg
  - src/components/illustrations/TechWebApps.svg
  - src/pages/test-isometric.astro
autonomous: true

must_haves:
  truths:
    - "Three distinct technology category illustrations exist"
    - "Illustrations are recognizable at 64x64px"
    - "Illustrations adapt to CSS color theming"
  artifacts:
    - path: "src/components/illustrations/TechAI.svg"
      provides: "AI/neural network illustration"
      contains: "currentColor"
    - path: "src/components/illustrations/TechAutomations.svg"
      provides: "Automation/workflow illustration"
      contains: "currentColor"
    - path: "src/components/illustrations/TechWebApps.svg"
      provides: "Web application illustration"
      contains: "currentColor"
  key_links:
    - from: "TechAI.svg"
      to: "CSS theming"
      via: "currentColor fill attribute"
      pattern: "fill=\"currentColor\""
---

<objective>
Create 3 isometric SVG illustrations for technology service categories (AI, Automations, Web Apps) following the established Phase 14 style guide.

Purpose: Visual differentiation for the 3 technology service offerings, matching the process section illustration style for consistency.

Output: 3 SVG files ready for TechSection.astro integration with currentColor theming and responsive viewBox.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase patterns
@.planning/phases/14-process-section/14-01-SUMMARY.md

# Illustration style reference
@src/components/illustrations/ProcessBuild.svg
@src/components/illustrations/ProcessDiscovery.svg

# Test page for visual verification
@src/pages/test-isometric.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 technology category SVG illustrations</name>
  <files>
    src/components/illustrations/TechAI.svg
    src/components/illustrations/TechAutomations.svg
    src/components/illustrations/TechWebApps.svg
  </files>
  <action>
Create 3 minimal isometric SVG illustrations following the Phase 14 style guide:

**TechAI.svg - AI/Neural Network (concept: connected nodes representing intelligence)**
- Central larger node (brain/processor metaphor)
- 2-3 smaller connecting nodes
- Simple connection lines between nodes
- Use opacity: 0.7 for connections, 0.85 for outer nodes, 1.0 for central node

**TechAutomations.svg - Workflow Automation (concept: gears or flowing arrows)**
- Two interlocking gears or 2-3 flowing arrows
- Represents automated connection between systems
- Use opacity variation for depth

**TechWebApps.svg - Web Applications (concept: browser window or screen with content)**
- Window frame with address bar
- 2-3 content blocks inside
- Similar to ProcessPrototype.svg but distinct
- Use opacity variation for depth

**Style requirements (from Phase 14):**
- `viewBox="0 0 100 100"` for responsive scaling
- `fill="currentColor"` on all shapes (NO hard-coded colors)
- `aria-hidden="true"` as decorative elements
- 3-5 simple geometric shapes maximum
- Opacity-based face differentiation (0.7, 0.85, 1.0)
- Target file size: under 1KB each

**Reference the existing ProcessBuild.svg pattern:**
```svg
<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
  <!-- shapes with currentColor and opacity -->
</svg>
```
  </action>
  <verify>
    - Each file exists in src/components/illustrations/
    - Each file contains `viewBox="0 0 100 100"`
    - Each file contains `fill="currentColor"` (no hard-coded colors)
    - Each file contains `aria-hidden="true"`
    - Each file is under 1KB: `wc -c src/components/illustrations/Tech*.svg`
  </verify>
  <done>3 SVG files exist with currentColor theming, viewBox, and aria-hidden attributes, each under 1KB</done>
</task>

<task type="auto">
  <name>Task 2: Add technology illustrations to test page</name>
  <files>src/pages/test-isometric.astro</files>
  <action>
Add a new section to test-isometric.astro to preview all 3 technology illustrations.

1. Import the 3 new SVG components at the top of the file
2. Add a new section after the existing process illustrations section
3. Display each illustration at 64x64px with different colors (yellow, turquoise, magenta)
4. Apply iso-shadow-sm utility to each wrapper

**Section structure (mirror the process illustrations section):**
```astro
<!-- Technology Illustrations -->
<section class="mb-16">
  <h2 class="font-heading text-2xl font-bold mb-6 text-text-light dark:text-text-dark">
    Technology Illustrations
  </h2>
  <div class="flex flex-wrap gap-8">
    <div class="text-center">
      <div class="text-yellow iso-shadow-sm rounded mb-2 inline-block">
        <TechAI class="w-16 h-16" />
      </div>
      <p class="text-sm text-text-muted-light dark:text-text-muted-dark">AI</p>
    </div>
    <!-- Repeat for TechAutomations (turquoise) and TechWebApps (magenta) -->
  </div>
</section>
```
  </action>
  <verify>
    - `npm run dev` and navigate to /test-isometric
    - All 3 technology illustrations render at 64x64px
    - Colors inherit correctly from parent text-color classes
    - iso-shadow-sm creates visible offset shadow in light mode
    - Dark mode shows glow effect instead of shadow
  </verify>
  <done>All 3 technology illustrations visible on test page with correct sizing, colors, and shadow effects</done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build` completes without errors
2. All 3 SVG files exist: `ls src/components/illustrations/Tech*.svg`
3. Files use currentColor: `grep -l "currentColor" src/components/illustrations/Tech*.svg | wc -l` returns 3
4. Files have viewBox: `grep -l "viewBox" src/components/illustrations/Tech*.svg | wc -l` returns 3
5. Files are under 1KB each: `wc -c src/components/illustrations/Tech*.svg`
6. Visual check: Illustrations recognizable at 64px on test page
</verification>

<success_criteria>
- 3 technology category SVG illustrations created (TechAI.svg, TechAutomations.svg, TechWebApps.svg)
- All use currentColor for CSS theming (no hard-coded colors)
- All include viewBox="0 0 100 100" for responsive scaling
- All marked aria-hidden="true" for accessibility
- Each file under 1KB
- Visual preview available on /test-isometric page
- All illustrations recognizable at 64x64px display size
</success_criteria>

<output>
After completion, create `.planning/phases/15-technology-section/15-01-SUMMARY.md`
</output>
