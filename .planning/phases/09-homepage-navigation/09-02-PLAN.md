---
phase: 09-homepage-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/homepage/TechSection.astro
  - src/components/homepage/ContactSection.astro
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tech section displays technology stack with magenta accent"
    - "Tech section uses asymmetric grid layout"
    - "Contact section has neobrutalist form with Input component"
    - "Contact section uses turquoise accent"
    - "Form has accessible error states and ARIA attributes"
  artifacts:
    - path: "src/components/homepage/TechSection.astro"
      provides: "Technology showcase section with magenta accent"
      contains: "magenta"
    - path: "src/components/homepage/ContactSection.astro"
      provides: "Contact form section with turquoise accent"
      contains: "turquoise"
  key_links:
    - from: "ContactSection.astro"
      to: "Input.astro"
      via: "component import"
      pattern: "import Input from"
    - from: "ContactSection.astro"
      to: "Button.astro"
      via: "component import"
      pattern: "import Button from"
---

<objective>
Create new homepage section components for Tech and Contact that follow the narrative structure with neobrutalist styling.

Purpose: Complete the component set needed for the narrative homepage (Solutions -> Process -> Tech -> About -> Contact).
Output: TechSection.astro (magenta accent, asymmetric layout) and ContactSection.astro (turquoise accent, form using primitives).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-homepage-navigation/09-RESEARCH.md
@.planning/phases/08-primitive-components/08-01-SUMMARY.md
@.planning/phases/08-primitive-components/08-02-SUMMARY.md
@src/components/ui/Button.astro
@src/components/ui/Input.astro
@src/components/ui/Card.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TechSection component with magenta accent</name>
  <files>src/components/homepage/TechSection.astro</files>
  <action>
Create new file `src/components/homepage/TechSection.astro`:

1. **Directory**: Create `src/components/homepage/` if it doesn't exist

2. **Section structure**:
   - `<section id="tech">` with `aria-labelledby="tech-heading"`
   - Padding: `py-24 md:py-32`
   - Background: `bg-bg-light dark:bg-bg-dark`

3. **Heading**:
   - H2 with id="tech-heading"
   - Text: "How I Build" (or similar - technology approach)
   - Styling: `font-heading font-bold text-3xl md:text-4xl uppercase mb-8`
   - NOT ALL CAPS for H2 (only H1 uses ALL CAPS per density guidelines)

4. **Asymmetric grid layout** (1:2 ratio):
   ```html
   <div class="grid grid-cols-1 md:grid-cols-[1fr_2fr] gap-8 md:gap-12 items-start">
     <!-- Left: brief intro text -->
     <!-- Right: tech stack cards -->
   </div>
   ```

5. **Left column**: Brief intro paragraph about technology philosophy
   - Focus on "right tool for the job" messaging
   - Modern, proven technologies
   - No over-engineering

6. **Right column**: Technology cards in grid
   - Use Card component with `variant="magenta"`
   - 2x2 grid on larger screens: `grid grid-cols-1 sm:grid-cols-2 gap-4`
   - Four categories:
     - **Frontend**: Astro, React, Tailwind CSS
     - **Backend**: Node.js, Python, PostgreSQL
     - **AI/ML**: OpenAI, LangChain, vector databases
     - **Infrastructure**: Vercel, AWS, GitHub Actions

7. **Card content**:
   - H3 for category name
   - List of technologies with brief descriptors
   - Keep content concise (density 4/10 per research)

8. **Magenta accent usage**:
   - Card variant is magenta
   - Icons/bullets could use magenta color
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Verify file exists at `src/components/homepage/TechSection.astro`.
Grep TechSection.astro for `id="tech"` - should find 1 match.
Grep TechSection.astro for `magenta` - should find matches (Card variant).
Grep TechSection.astro for `grid-cols-\[1fr_2fr\]` - should find 1 match (asymmetric layout).
  </verify>
  <done>
TechSection component created with magenta accent cards, asymmetric 1:2 grid layout, and technology stack showcase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ContactSection component with turquoise accent</name>
  <files>src/components/homepage/ContactSection.astro</files>
  <action>
Create new file `src/components/homepage/ContactSection.astro`:

1. **Section structure**:
   - `<section id="contact">` with `aria-labelledby="contact-heading"`
   - Padding: `py-24 md:py-32`
   - Background: `bg-bg-light dark:bg-bg-dark`

2. **Heading**:
   - H2 with id="contact-heading"
   - Text: "Let's Talk" or "Start a Conversation"
   - Styling: `font-heading font-bold text-3xl md:text-4xl uppercase mb-4`

3. **Subheading/intro**:
   - Brief text: "Tell me about your project and I'll get back to you within 48 hours."
   - `text-lg md:text-xl text-text-muted-light dark:text-text-muted-dark mb-8`

4. **Layout**: Asymmetric 2:1 grid
   ```html
   <div class="grid grid-cols-1 md:grid-cols-[2fr_1fr] gap-8 md:gap-12 items-start">
     <!-- Left: Contact form (2fr) -->
     <!-- Right: Alternative contact info (1fr) -->
   </div>
   ```

5. **Contact form (left column)**:
   - Use `<form action="https://formspree.io/f/YOUR_FORM_ID" method="POST">`
   - Import and use Input component for fields
   - Fields:
     - Name: `<Input variant="turquoise" label="Name" type="text" name="name" required minlength={2} />`
     - Email: `<Input variant="turquoise" label="Email" type="email" name="email" required />`
     - Message: Custom textarea styled like Input (Input component is for inputs only)
   - Textarea styling to match Input:
     - `border-[3px] border-text-light dark:border-text-dark rounded p-4`
     - Focus ring: same box-shadow pattern as Input
     - `font-body text-text-light dark:text-text-dark bg-bg-light dark:bg-bg-dark`
   - Submit button: `<Button variant="turquoise" type="submit">Send Message</Button>`
   - Privacy note: "Your info stays between us. No spam, ever."

6. **Alternative contact (right column)**:
   - Card component with `variant="turquoise"`
   - Content:
     - H3: "Prefer Email?"
     - Direct email link: `joel@joelshinness.com`
     - Brief reassurance: "I respond to every message within 48 hours."

7. **Turquoise accent usage**:
   - Input variant turquoise
   - Button variant turquoise
   - Card variant turquoise

8. **Accessibility**:
   - Labels associated with inputs (handled by Input component)
   - Required fields marked with `*` and `aria-required`
   - Error states use aria-invalid and aria-describedby (Input component handles this)
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Verify file exists at `src/components/homepage/ContactSection.astro`.
Grep ContactSection.astro for `id="contact"` - should find 1 match.
Grep ContactSection.astro for `turquoise` - should find multiple matches.
Grep ContactSection.astro for `import Input` - should find 1 match.
Grep ContactSection.astro for `import Button` - should find 1 match.
  </verify>
  <done>
ContactSection component created with turquoise accent, asymmetric 2:1 grid layout, accessible form using Input/Button primitives.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Both TechSection.astro and ContactSection.astro exist in src/components/homepage/
3. TechSection uses magenta accent for cards
4. ContactSection uses turquoise accent for form elements
5. Both sections have asymmetric grid layouts
6. ContactSection imports and uses Input and Button primitives
7. Form fields have proper labels and accessibility attributes
</verification>

<success_criteria>
- TechSection.astro exists with magenta accent and 1:2 asymmetric layout
- ContactSection.astro exists with turquoise accent and 2:1 asymmetric layout
- ContactSection uses Input and Button primitives from Phase 8
- Form has proper accessibility attributes (labels, aria-required)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-homepage-navigation/09-02-SUMMARY.md`
</output>
