---
phase: 09-homepage-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Header.astro
  - src/components/layout/Footer.astro
  - src/components/layout/MobileNav.astro
  - src/styles/global.css
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Header has thick 3px bottom border and opaque background"
    - "Header stays within 60px height on mobile"
    - "Footer has thick 3px top border"
    - "FAQ accordion appears in footer with collapsible details/summary"
    - "Dark mode toggle has neobrutalist styling with focus ring"
    - "Mobile nav has neobrutalist borders and styling"
  artifacts:
    - path: "src/components/layout/Header.astro"
      provides: "Neobrutalist sticky header with navigation"
      contains: "border-b-\\[3px\\]"
    - path: "src/components/layout/Footer.astro"
      provides: "Neobrutalist footer with FAQ accordion"
      contains: "<details"
    - path: "src/components/layout/MobileNav.astro"
      provides: "Neobrutalist mobile navigation panel"
      contains: "border-\\[3px\\]"
    - path: "src/styles/global.css"
      provides: "Smooth scroll and scroll-margin-top for sections"
      contains: "scroll-behavior: smooth"
  key_links:
    - from: "Header.astro"
      to: "MobileNav.astro"
      via: "import and render"
      pattern: "import MobileNav"
    - from: "Footer.astro"
      to: "FAQ content"
      via: "details/summary elements"
      pattern: "<summary.*FAQ"
---

<objective>
Update Header, Footer, and MobileNav with neobrutalist styling. Relocate FAQ from homepage to footer as collapsible accordion.

Purpose: Establish consistent neobrutalist navigation chrome across the site, with FAQ content accessible but not disrupting homepage narrative flow.
Output: Updated layout components with thick borders, opaque backgrounds, and FAQ accordion in footer.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-homepage-navigation/09-RESEARCH.md
@src/components/layout/Header.astro
@src/components/layout/Footer.astro
@src/components/layout/MobileNav.astro
@src/components/FAQ.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Header with neobrutalist styling</name>
  <files>
    src/components/layout/Header.astro
    src/styles/global.css
  </files>
  <action>
Update Header.astro with neobrutalist design:

1. **Border**: Change `border-b border-text-light/10` to `border-b-[3px] border-text-light dark:border-text-dark`
2. **Height constraint**: Add `h-[60px]` to ensure 60px max height on mobile
3. **Background**: Keep opaque `bg-bg-light dark:bg-bg-dark` (already correct)
4. **Logo**: Keep existing styling (already uses font-heading and hover:text-accent-yellow)
5. **Desktop nav links**: Add `font-heading font-bold` to nav links (currently just hover colors)
6. **Dark mode toggle button**: Apply neobrutalist styling:
   - `border-[3px] border-text-light dark:border-text-dark rounded`
   - `hover:bg-yellow dark:hover:bg-yellow`
   - Add focus ring: `focus:outline-none focus:shadow-[0_0_0_2px_var(--color-bg-light),0_0_0_6px_var(--color-text-light)] dark:focus:shadow-[0_0_0_2px_var(--color-bg-dark),0_0_0_6px_var(--color-text-dark)]`

In global.css, add smooth scroll behavior:
```css
html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}

section[id] {
  scroll-margin-top: 60px;
}
```
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep Header.astro for `border-b-[3px]` - should find 1 match.
Grep Header.astro for `h-[60px]` - should find 1 match.
Grep global.css for `scroll-behavior: smooth` - should find 1 match.
  </verify>
  <done>
Header has 3px bottom border, 60px height constraint, and neobrutalist styling on dark mode toggle.
Smooth scroll enabled with scroll-margin-top for section anchors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Footer with neobrutalist styling and FAQ accordion</name>
  <files>
    src/components/layout/Footer.astro
    src/components/FAQ.astro
  </files>
  <action>
Update Footer.astro with neobrutalist design and FAQ:

1. **Border**: Change `border-t border-text-light/10` to `border-t-[3px] border-text-light dark:border-text-dark`

2. **Layout structure**: Reorganize into sections:
   - Primary footer content (copyright, built with, social links) in top section
   - FAQ accordion section below with separator

3. **FAQ accordion**: Add after the primary content section:
   - Add `pt-8 border-t-[3px] border-text-light/20 dark:border-text-dark/20` separator
   - H2 heading: "Frequently Asked Questions" with `font-heading text-2xl font-bold mb-6`
   - Use native HTML `<details>` and `<summary>` elements
   - Each FAQ item styling:
     - `border-[3px] border-text-light dark:border-text-dark rounded p-4`
     - `<summary>`: `font-heading font-bold cursor-pointer list-none flex items-center justify-between`
     - Add + icon with `transition-transform group-open:rotate-45`
     - Answer div: `mt-4 pt-4 border-t-[3px] border-text-light/20`

4. **Migrate FAQ content** from FAQ.astro (5 questions):
   - "How long does a typical project take?"
   - "Do you work with clients outside your area?"
   - "What if I'm not sure exactly what I need?"
   - "How do you handle changes during the project?"
   - "What happens after handover?"

5. **Social links**: Apply neobrutalist hover styling - `hover:text-turquoise` instead of `hover:text-accent-teal`

Note: FAQ.astro will be removed from homepage in Plan 03 but keep the file for now in case it's used elsewhere.
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep Footer.astro for `border-t-[3px]` - should find 2+ matches (main border + separator).
Grep Footer.astro for `<details` - should find 5 matches (5 FAQ items).
Grep Footer.astro for `<summary` - should find 5 matches.
  </verify>
  <done>
Footer has 3px top border, FAQ accordion with 5 collapsible questions using native HTML details/summary.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update MobileNav with neobrutalist styling</name>
  <files>src/components/layout/MobileNav.astro</files>
  <action>
Update MobileNav.astro with neobrutalist design:

1. **Hamburger button**: Apply neobrutalist styling:
   - Add `border-[3px] border-text-light dark:border-text-dark rounded`
   - Add focus ring: `focus:outline-none focus:shadow-[0_0_0_2px_var(--color-bg-light),0_0_0_6px_var(--color-text-light)] dark:focus:shadow-[0_0_0_2px_var(--color-bg-dark),0_0_0_6px_var(--color-text-dark)]`

2. **Hamburger lines**: Make thicker - change `h-0.5` to `h-[3px]`

3. **Slide-in panel**: Add border when open:
   - Add `border-l-[3px] border-text-light dark:border-text-dark`

4. **Close button**: Apply same neobrutalist styling as hamburger:
   - Add `border-[3px] border-text-light dark:border-text-dark rounded`
   - Add focus ring

5. **Nav links**: Add `font-bold` to nav links if not present

6. **Add dark mode toggle to mobile nav**: Currently only in desktop header
   - Copy dark mode toggle button from Header.astro
   - Place after the nav links
   - Use same neobrutalist styling with border and focus ring
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep MobileNav.astro for `border-[3px]` - should find 3+ matches (hamburger, close, panel).
Grep MobileNav.astro for `h-[3px]` - should find 3 matches (hamburger lines).
Grep MobileNav.astro for `theme-toggle` - should find 1 match (dark mode toggle in mobile).
  </verify>
  <done>
Mobile nav has neobrutalist borders on hamburger button, close button, and slide-in panel.
Dark mode toggle is accessible in mobile navigation.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Header shows 3px bottom border in both light and dark modes
3. Footer shows 3px top border and FAQ accordion
4. FAQ items expand/collapse using native HTML behavior
5. Mobile hamburger menu has neobrutalist borders
6. Dark mode toggle is accessible in both desktop and mobile views
7. Focus rings are visible when tabbing through interactive elements
8. Smooth scroll works when clicking anchor links (test with any section id)
</verification>

<success_criteria>
- Header has 3px bottom border, 60px height, neobrutalist dark mode toggle
- Footer has 3px top border and FAQ accordion with 5 collapsible items
- Mobile nav has neobrutalist borders and includes dark mode toggle
- Smooth scroll is enabled with scroll-margin-top for section offsets
- All focus states meet WCAG 2.4.13 requirements
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-homepage-navigation/09-01-SUMMARY.md`
</output>
