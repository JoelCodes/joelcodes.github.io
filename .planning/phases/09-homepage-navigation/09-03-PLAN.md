---
phase: 09-homepage-navigation
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - src/pages/index.astro
  - src/components/Hero.astro
  - src/components/Services.astro
  - src/components/Process.astro
  - src/components/About.astro
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Homepage follows narrative order: Hero -> Solutions -> Process -> Tech -> About -> Contact"
    - "Hero section has neobrutalist styling with bold borders and shadows"
    - "Solutions section (formerly Services) uses yellow accent with Card component"
    - "Process section uses turquoise accent with neobrutalist timeline"
    - "About section uses yellow accent with asymmetric layout"
    - "FAQ section is NOT on homepage (moved to footer in 09-01)"
    - "All sections have section IDs for anchor navigation"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage with narrative section order"
      contains: "TechSection"
    - path: "src/components/Hero.astro"
      provides: "Neobrutalist hero section"
      contains: "border-\\[3px\\]|shadow-neo"
    - path: "src/components/Services.astro"
      provides: "Solutions section with yellow accent"
      contains: "yellow"
    - path: "src/components/Process.astro"
      provides: "Process section with turquoise accent"
      contains: "turquoise"
    - path: "src/components/About.astro"
      provides: "About section with asymmetric layout"
      contains: "grid-cols-\\[.*fr.*fr\\]"
  key_links:
    - from: "index.astro"
      to: "TechSection.astro"
      via: "import"
      pattern: "import TechSection from"
    - from: "index.astro"
      to: "ContactSection.astro"
      via: "import"
      pattern: "import ContactSection from"
    - from: "Services.astro"
      to: "Card.astro"
      via: "import"
      pattern: "import Card from"
---

<objective>
Transform homepage into narrative journey by updating existing sections with neobrutalist styling and integrating new Tech/Contact sections in correct order.

Purpose: Complete the homepage redesign with narrative flow (Solutions -> Process -> Tech -> About -> Contact) and consistent neobrutalist styling across all sections.
Output: Fully redesigned homepage with neobrutalist sections following the narrative structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-homepage-navigation/09-RESEARCH.md
@.planning/phases/09-homepage-navigation/09-01-SUMMARY.md
@.planning/phases/09-homepage-navigation/09-02-SUMMARY.md
@src/pages/index.astro
@src/components/Hero.astro
@src/components/Services.astro
@src/components/Process.astro
@src/components/About.astro
@src/components/ui/Button.astro
@src/components/ui/Card.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hero with neobrutalist styling</name>
  <files>src/components/Hero.astro</files>
  <action>
Update Hero.astro with neobrutalist design (density 10/10 - hero is exception):

1. **Section ID**: Add `id="hero"` for anchor navigation

2. **H1 styling** (ALL CAPS exclusivity - only H1):
   - Add `uppercase` class
   - Keep existing: `font-heading text-4xl md:text-5xl lg:text-6xl font-bold`
   - Text should be impactful headline (keep current or refine)

3. **Visual treatment**:
   - Add decorative border box around headline or subhead
   - Example: Wrap headline in div with `border-[3px] border-text-light dark:border-text-dark p-6 md:p-8 inline-block`
   - Or add shadow: `shadow-neo-yellow` (defined in global.css)

4. **CTA button**: Replace with Button component
   - `import Button from '../components/ui/Button.astro';`
   - `<Button variant="yellow" size="lg" href="/contact">Start a Conversation</Button>`
   - Remove the old anchor tag styling

5. **Layout refinement**:
   - Keep centered layout for hero
   - Consider adding asymmetric decorative element (optional)
   - Generous padding maintained: `py-12` already present, consider `py-24 md:py-32`

6. **Yellow accent**: Hero uses yellow as primary accent
   - Button variant yellow
   - Any decorative shadows use yellow
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep Hero.astro for `id="hero"` - should find 1 match.
Grep Hero.astro for `uppercase` - should find 1 match (H1 only).
Grep Hero.astro for `import Button` - should find 1 match.
Grep Hero.astro for `variant="yellow"` - should find 1 match.
  </verify>
  <done>
Hero section has neobrutalist styling with uppercase H1, Button component, and yellow accent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Services to Solutions with neobrutalist cards</name>
  <files>src/components/Services.astro</files>
  <action>
Update Services.astro to become Solutions section with neobrutalist design:

1. **Section ID**: Ensure `id="solutions"` is present with `aria-labelledby="solutions-heading"`

2. **Heading**:
   - H2 id="solutions-heading"
   - Change text from "What I Build" to more solution-focused: "Solutions That Fit"
   - Styling: `font-heading font-bold text-3xl md:text-4xl mb-8` (NOT uppercase - only H1)

3. **Replace service cards with Card component**:
   - `import Card from '../components/ui/Card.astro';`
   - Use `variant="yellow"` for all cards
   - Keep the 3-column grid: `grid grid-cols-1 md:grid-cols-3 gap-6`

4. **Card content structure**:
   ```astro
   <Card variant="yellow">
     <h3 class="font-heading text-xl md:text-2xl font-semibold mb-3">
       {service.title}
     </h3>
     <p class="font-body text-base mb-3">
       {service.description}
     </p>
     <p class="font-body text-sm italic text-text-muted-light dark:text-text-muted-dark">
       {service.example}
     </p>
   </Card>
   ```

5. **Padding**: Increase to `py-24 md:py-32` for generous spacing

6. **Consider adding CTA**: After cards, add a Button to contact
   - `<Button variant="yellow" href="#contact">Discuss Your Project</Button>`

7. **Yellow accent usage**:
   - All three cards use yellow variant
   - Maintains "one accent per section" rule
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep Services.astro for `id="solutions"` - should find 1 match.
Grep Services.astro for `import Card` - should find 1 match.
Grep Services.astro for `variant="yellow"` - should find 3+ matches (cards + optional button).
  </verify>
  <done>
Services section transformed to Solutions with yellow accent Card components and neobrutalist styling.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Process with neobrutalist timeline</name>
  <files>src/components/Process.astro</files>
  <action>
Update Process.astro with neobrutalist design:

1. **Section ID**: Ensure `id="process"` with `aria-labelledby="process-heading"`

2. **Heading**:
   - H2 id="process-heading"
   - Keep "How We Work Together" or similar
   - Styling: `font-heading font-bold text-3xl md:text-4xl text-center mb-12`

3. **Timeline styling update**:
   - Change timeline border from `border-l-4 border-accent-teal` to `border-l-[4px] border-turquoise`
   - Step numbers: Change from `bg-accent-teal` to `bg-turquoise`
   - Keep the step number circles but make borders thicker: `border-[3px] border-text-light dark:border-text-dark`

4. **Step content boxes**:
   - Change from `bg-text-light/5` to bordered boxes
   - Add `border-[3px] border-text-light/20 dark:border-text-dark/20 rounded-lg p-5`
   - Keep the "You:" / "Joel:" structure

5. **Padding**: Increase to `py-24 md:py-32`

6. **Turquoise accent usage**:
   - Timeline line: turquoise
   - Step number backgrounds: turquoise
   - Maintains "one accent per section" rule

7. **Density**: This is content-focused section (5/10 per research) - maintain good spacing between steps
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep Process.astro for `id="process"` - should find 1 match.
Grep Process.astro for `border-turquoise` or `turquoise` - should find matches.
Grep Process.astro for `border-\[3px\]` or `border-\[4px\]` - should find matches.
  </verify>
  <done>
Process section has neobrutalist timeline with turquoise accent and thicker borders.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update About with asymmetric layout</name>
  <files>src/components/About.astro</files>
  <action>
Update About.astro with neobrutalist design:

1. **Section ID**: Ensure `id="about"` with `aria-labelledby="about-heading"`

2. **Heading**:
   - H2 id="about-heading"
   - Keep "About Joel"
   - Styling: `font-heading font-bold text-3xl md:text-4xl mb-8`

3. **Asymmetric layout**:
   - Current grid: `grid-cols-1 md:grid-cols-[1fr_1.5fr]` - this is already asymmetric, good
   - Consider adjusting to `grid-cols-1 md:grid-cols-[1fr_2fr]` for stronger asymmetry

4. **Image treatment**:
   - Add thick border: `border-[4px] border-text-light dark:border-text-dark`
   - Add shadow: `shadow-neo-yellow` (yellow accent for About section)
   - Keep grayscale filter

5. **Stats styling**:
   - Wrap stats in Card component or add borders
   - Change accent color from `text-accent-teal` to `text-yellow`
   - Example: `<span class="font-heading text-4xl font-bold text-yellow">10+</span>`

6. **Content styling**:
   - Paragraphs remain readable (body font)
   - Consider adding subtle borders around the content area
   - `border-l-[4px] border-yellow pl-6` for accent stripe effect (optional)

7. **Padding**: Increase to `py-24 md:py-32`

8. **Yellow accent usage**:
   - Stats numbers in yellow
   - Image shadow in yellow
   - Maintains "one accent per section" rule
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep About.astro for `id="about"` - should find 1 match.
Grep About.astro for `text-yellow` or `border-yellow` or `shadow-neo-yellow` - should find matches.
Grep About.astro for `border-\[4px\]` - should find at least 1 match.
  </verify>
  <done>
About section has asymmetric layout with yellow accent for stats and image styling.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update index.astro with narrative section order</name>
  <files>src/pages/index.astro</files>
  <action>
Update index.astro to use new narrative structure:

1. **Import new components**:
   ```astro
   import TechSection from '../components/homepage/TechSection.astro';
   import ContactSection from '../components/homepage/ContactSection.astro';
   ```

2. **Remove FAQ import**: FAQ is now in footer (Plan 09-01)
   - Delete: `import FAQ from '../components/FAQ.astro';`

3. **Update section order** (narrative flow):
   ```astro
   <BaseLayout title="Joel Shinness | Custom Software for Small Business">
     <Hero />
     <Services />       <!-- Solutions section (id="solutions") -->
     <Process />        <!-- How we work (id="process") -->
     <TechSection />    <!-- Technology (id="tech") -->
     <About />          <!-- About Joel (id="about") -->
     <ContactSection /> <!-- Contact form (id="contact") -->
   </BaseLayout>
   ```

4. **Remove FAQ component from rendering**: Delete `<FAQ />` from the page

5. **Verify all sections have IDs**: For anchor navigation from header
   - hero, solutions, process, tech, about, contact
  </action>
  <verify>
Run `npm run build` - should complete without errors.
Grep index.astro for `import TechSection` - should find 1 match.
Grep index.astro for `import ContactSection` - should find 1 match.
Grep index.astro for `import FAQ` - should find 0 matches (removed).
Grep index.astro for `<FAQ` - should find 0 matches (removed).
  </verify>
  <done>
Homepage follows narrative structure: Hero -> Solutions -> Process -> Tech -> About -> Contact.
FAQ removed from homepage (available in footer).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete homepage redesign with neobrutalist styling and narrative flow.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` to start the development server
2. Visit http://localhost:4321

**Check each section in order:**
- [ ] Hero: ALL CAPS heading, yellow Button component, decorative border/shadow
- [ ] Solutions: Three yellow cards with thick borders
- [ ] Process: Turquoise timeline with thick borders on step boxes
- [ ] Tech: Magenta cards in asymmetric 1:2 layout
- [ ] About: Yellow accents on stats, asymmetric image/text layout
- [ ] Contact: Turquoise form with Input/Button components

**Check navigation:**
- [ ] Header links to #solutions, #process, etc. scroll smoothly
- [ ] Scroll stops at correct position (accounting for sticky header height)

**Check dark mode:**
- [ ] Toggle dark mode using header button
- [ ] All sections transition correctly
- [ ] Shadows become glows in dark mode

**Check mobile:**
- [ ] Resize browser or use DevTools mobile view
- [ ] All grids stack properly on mobile
- [ ] Mobile nav includes dark mode toggle
  </how-to-verify>
  <resume-signal>Type "approved" if homepage looks correct, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Homepage renders all 6 sections in correct narrative order
3. Each section has appropriate accent color (yellow, turquoise, magenta)
4. All sections have IDs for anchor navigation
5. Smooth scrolling works with header offset
6. Dark mode works across all sections
7. Mobile layouts stack correctly
8. Human verification confirms visual quality
</verification>

<success_criteria>
- Homepage follows narrative: Hero -> Solutions -> Process -> Tech -> About -> Contact
- FAQ is NOT on homepage (moved to footer)
- Hero has uppercase H1 and Button component
- Solutions uses yellow Card components
- Process has turquoise timeline with neobrutalist borders
- Tech has magenta cards in asymmetric layout
- About has yellow accents and asymmetric layout
- Contact has turquoise form using Input/Button primitives
- All sections have anchor IDs for navigation
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/09-homepage-navigation/09-03-SUMMARY.md`
</output>
