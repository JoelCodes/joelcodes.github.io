---
phase: 33-token-foundation-fonts
plan: 06
type: execute
wave: 5
depends_on: ["33-01", "33-04"]
autonomous: false
requirements: [FOUND-05]
files_modified:
  - src/components/WaveMark.astro
  - public/favicon.svg
  - public/favicon.ico
  - scripts/generate-og.mjs
  - public/og-image.png
  - src/components/SEO.astro
user_setup: []

threat_model:
  assets:
    - "Brand asset integrity + OG-image tooling supply chain"
  threats:
    - id: T-11
      description: "Hand-authored SVG geometry (guessing the waveform by eye) = brand fidelity drift (v1.4 root cause repeat)."
      mitigation: "All SVG path data comes from the 33-01 extraction artifact (Figma download_assets or Joel's Copy-as-SVG); no geometry is authored by eye (D-16)."
    - id: T-12
      description: "Supply-chain: the OG script installs @vercel/satori + sharp as dev deps (npm package integrity)."
      mitigation: "Both are installed as devDependencies only, used by a one-off local script never shipped to the client bundle or CI; official Vercel/standard packages; committed lockfile. The generated PNG is a static asset, not runtime code."
    - id: T-13
      description: "Wiring an unapproved OG image into SEO could ship an off-brand social card."
      mitigation: "SEO.astro wire-in is gated behind an explicit Joel approval checkpoint (D-15) — the .png must be approved BEFORE the one-line SEO change."
  asvs_controls:
    - "V14.2 (dev-dependency integrity — dev-only satori/sharp, committed lockfile)"
    - "V5.3 (output encoding — OG is a static image asset, no user input; low risk noted for completeness)"
  block_on: high

must_haves:
  truths:
    - "WaveMark.astro renders the Figma-extracted waveform as INLINE SVG (not <img src>) with stroke=currentColor so it flips with --wl-ink under .dark; geometry is Figma-sourced, never hand-authored (FOUND-05, D-13, D-16)."
    - "public/favicon.svg is replaced with the waveform mark carrying a prefers-color-scheme dark <style> block using --wl-ink light/dark hex values; public/favicon.ico is regenerated from it; BaseLayout favicon <link> tags stay unchanged (FOUND-05, D-14)."
    - "public/og-image.png is a 1200x630 composition of Figma-sourced brand elements (mark + wordmark + verbatim tagline + palette bg), APPROVED by Joel before wiring (FOUND-05, D-15)."
    - "SEO.astro line 36 references /og-image.png (was /og-image.svg) ONLY after Joel approves the composition (D-15)."
  artifacts:
    - path: "src/components/WaveMark.astro"
      provides: "Theme-adaptive inline SVG waveform mark component"
      contains: "currentColor"
    - path: "public/favicon.svg"
      provides: "Waveform favicon with prefers-color-scheme dark support"
      contains: "prefers-color-scheme"
    - path: "public/og-image.png"
      provides: "1200x630 branded OG image"
    - path: "src/components/SEO.astro"
      provides: "OG image wire-in to the PNG"
      contains: "og-image.png"
  key_links:
    - from: "public/favicon.svg <style>"
      to: "--wl-ink light/dark hex (from 33-04 / 33-01)"
      via: "hardcoded stroke hex + prefers-color-scheme override"
      pattern: "prefers-color-scheme"
    - from: "src/components/SEO.astro"
      to: "public/og-image.png"
      via: "ogImage constant"
      pattern: "og-image.png"
---

<objective>
Deliver the three brand assets — the inline-SVG WaveMark component, the theme-adaptive favicon,
and the approval-gated 1200x630 OG image — and wire the OG image into SEO. This is FOUND-05.

Purpose: The site chrome (Phase 34+) and social sharing need the waveform mark, favicon, and OG
image in place, all sourced from Figma (no invented geometry).
Output: `WaveMark.astro`, replaced `favicon.svg`/`favicon.ico`, generated `og-image.png`, and
the one-line `SEO.astro` wire-in (post-approval).

This plan is `autonomous: false`: it contains Joel's OG-image approval checkpoint (D-15). If the
33-01 waveform extraction was flagged "MANUAL SVG NEEDED", the orchestrator must obtain the
Copy-as-SVG from Joel before Task 1.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md
@.planning/phases/33-token-foundation-fonts/33-04-SUMMARY.md
@.planning/phases/33-token-foundation-fonts/33-PATTERNS.md
@.planning/phases/33-token-foundation-fonts/33-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WaveMark.astro and replace favicon assets</name>
  <files>src/components/WaveMark.astro, public/favicon.svg, public/favicon.ico</files>
  <read_first>
    - .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md (Waveform Mark SVG — the exact path/line geometry and viewBox to use)
    - .planning/phases/33-token-foundation-fonts/33-04-SUMMARY.md (the final --wl-ink LIGHT and DARK hex values for the favicon stroke colors)
    - src/components/design-system/TokenSwatch.astro (props/inline-SVG analog)
    - src/components/ui/Button.astro (the `class: className = ''` rename pattern; un-exported `interface Props`)
    - public/favicon.svg (current file — already uses the prefers-color-scheme <style> pattern to emulate)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (WaveMark.astro + favicon.svg sections — exact structures)
  </read_first>
  <action>
    Create `src/components/WaveMark.astro` following the TokenSwatch/Button analog: un-exported
    `interface Props { class?: string; size?: number }`, destructure with `class: className = ''` and
    `size = 32`. Render an inline `<svg>` (width/height=size, the viewBox from the extraction artifact,
    `fill="none"`, `class={className}`, `aria-hidden="true"`) containing the EXACT `<path>`/`<line>`
    geometry from 33-FIGMA-EXTRACTION.md with `stroke="currentColor"` (so it inherits the parent's
    color and flips with `--wl-ink` under `.dark` — no `.dark` CSS needed). Do NOT hand-author any path
    data. Then replace `public/favicon.svg` in place: same waveform geometry, a `<style>` block whose
    default rule strokes with the `--wl-ink` LIGHT hex (from 33-04-SUMMARY) and a
    `@media (prefers-color-scheme: dark)` block strokes with the `--wl-ink` DARK hex; include
    `color-scheme: light dark`. Regenerate `public/favicon.ico` from the new SVG (use `sharp` or an ICO
    tool to rasterize at 16/32/48 px — do not leave the old mark). Do NOT change the favicon `<link>`
    tags in BaseLayout (they already reference `/favicon.svg` and `/favicon.ico`).
  </action>
  <verify>`npm run build` succeeds; WaveMark.astro renders inline `<svg>` with `stroke="currentColor"` and no `<img`; `grep "prefers-color-scheme" public/favicon.svg` matches; `public/favicon.ico` file mtime is newer than phase start.</verify>
  <acceptance_criteria>
    - `src/components/WaveMark.astro` exists, uses un-exported `interface Props`, renders inline `<svg>` (no `<img src>`), and its `<path>`/`<line>` elements use `stroke="currentColor"`.
    - The SVG path/line `d`/coordinate data matches the geometry recorded in 33-FIGMA-EXTRACTION.md (not hand-authored).
    - `public/favicon.svg` contains a `<style>` block with `@media (prefers-color-scheme: dark)`, a light stroke = `--wl-ink` light hex, and a dark stroke = `--wl-ink` dark hex.
    - `public/favicon.ico` has been regenerated (mtime newer than phase start) from the new mark.
    - `npm run build` completes; favicon `<link>` tags in BaseLayout are unchanged.
  </acceptance_criteria>
  <done>The waveform mark exists as a theme-adaptive inline-SVG component and as the site favicon (SVG + ICO), all geometry Figma-sourced.</done>
</task>

<task type="auto">
  <name>Task 2: Write generate-og.mjs and produce the 1200x630 PNG</name>
  <files>scripts/generate-og.mjs, public/og-image.png</files>
  <read_first>
    - .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md (OG Tagline — verbatim copy; OG Frame Check; Waveform Mark SVG for the mark element)
    - .planning/phases/33-token-foundation-fonts/33-04-SUMMARY.md (--wl-paper / --wl-sea-glass background hex and --wl-ink text hex)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (scripts/generate-og.mjs section — satori+sharp one-off script structure)
    - .planning/phases/33-token-foundation-fonts/33-RESEARCH.md (OG image tooling — satori→SVG→sharp→PNG path)
  </read_first>
  <action>
    Install dev deps: `npm install --save-dev @vercel/satori@^0.10.0 sharp@^0.33.0`. Write
    `scripts/generate-og.mjs` (ESM): compose a 1200x630 layout from Figma-sourced ingredients ONLY —
    the waveform mark, the "Joel Shinness Solutions" wordmark in Fraunces, the VERBATIM tagline from
    the extraction artifact, on a `--wl-paper` (or `--wl-sea-glass`) background with `--wl-ink` text.
    Use `@vercel/satori` to render the element tree to SVG (load the Fraunces + Hanken woff2 from
    `node_modules/@fontsource-variable/*/files/`), then `sharp(Buffer.from(svg)).png()` to write
    `public/og-image.png`. If 33-01's OG Frame Check found an existing 1200x630 Figma frame, prefer
    exporting that frame directly instead of composing. Run the script: `node scripts/generate-og.mjs`.
    Verify output dimensions are exactly 1200x630. Do NOT wire it into SEO yet — that waits for Joel's
    approval in Task 3.
  </action>
  <verify>`node scripts/generate-og.mjs` runs clean; `node -e "import('sharp').then(s=>s.default('public/og-image.png').metadata().then(m=>{if(m.width!==1200||m.height!==630)process.exit(1)}))"` exits 0 (dimensions 1200x630).</verify>
  <acceptance_criteria>
    - `@vercel/satori` and `sharp` are in `devDependencies`.
    - `scripts/generate-og.mjs` exists, is ESM, and composes ONLY Figma-sourced ingredients (mark, wordmark, verbatim tagline, palette bg, ink text).
    - `public/og-image.png` exists and is exactly 1200x630 px.
    - The tagline text in the composition matches the verbatim OG Tagline in 33-FIGMA-EXTRACTION.md.
    - SEO.astro is NOT yet modified (wire-in is gated to Task 3).
  </acceptance_criteria>
  <done>A 1200x630 branded OG PNG is generated from Figma-sourced elements, ready for Joel's approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: OG approval gate, then wire og-image.png into SEO.astro</name>
  <files>src/components/SEO.astro</files>
  <read_first>
    - src/components/SEO.astro (line ~36: `const ogImage = ${SITE_URL}/og-image.svg` — the single line to change)
    - .planning/phases/33-token-foundation-fonts/33-CONTEXT.md (D-15 — OG approval-gated before wiring)
    - .planning/phases/33-token-foundation-fonts/33-UI-SPEC.md (OG Image contract — approval gate, one-line wire-in)
  </read_first>
  <what-built>
    A 1200x630 `public/og-image.png` composed from Figma-sourced brand elements (waveform mark,
    "Joel Shinness Solutions" wordmark in Fraunces, the verbatim Figma tagline, sea-cool palette
    background, ink text). This replaces the old placeholder `og-image.svg` (SVG is unreliable on
    social platforms).
  </what-built>
  <how-to-verify>
    1. Open `public/og-image.png` and confirm it looks on-brand: correct waveform mark, correct
       wordmark, verbatim tagline, sea-cool palette, legible ink text at 1200x630.
    2. Confirm the tagline copy is the exact Figma tagline (no invented words).
    3. Approve, or describe changes (colors, layout, tagline placement) to revise scripts/generate-og.mjs.
  </how-to-verify>
  <action>
    PAUSE for Joel's approval of `public/og-image.png` (D-15). ONLY after approval, change the single
    `ogImage` constant line in `src/components/SEO.astro` from `${SITE_URL}/og-image.svg` to
    `${SITE_URL}/og-image.png`. Make NO other changes to SEO.astro (all Props, meta tags, JSON-LD,
    Twitter Card tags stay identical). If Joel requests changes, revise `scripts/generate-og.mjs`,
    regenerate, and re-present before wiring.
  </action>
  <resume-signal>Type "approved" to wire the PNG into SEO.astro, or describe the composition changes needed.</resume-signal>
  <verify>After approval: `grep "og-image.png" src/components/SEO.astro` matches and `grep "og-image.svg" src/components/SEO.astro` returns nothing; `npm run build` succeeds.</verify>
  <acceptance_criteria>
    - Joel has approved the OG composition (approval recorded before the SEO edit).
    - `src/components/SEO.astro` `ogImage` constant references `/og-image.png` (not `/og-image.svg`).
    - No other lines in SEO.astro changed.
    - `npm run build` completes with zero errors.
  </acceptance_criteria>
  <done>The approved 1200x630 OG PNG is wired into SEO meta; the SVG placeholder is no longer referenced.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- `src/components/WaveMark.astro` renders inline SVG with `currentColor`, geometry from Figma.
- `public/favicon.svg` has `prefers-color-scheme` dark support; `favicon.ico` regenerated.
- `public/og-image.png` is 1200x630 and Joel-approved.
- `src/components/SEO.astro` references `/og-image.png`; no `/og-image.svg` reference remains.
- BaseLayout favicon `<link>` tags unchanged.
</verification>

<success_criteria>
FOUND-05 satisfied: the waveform mark (WaveMark.astro inline SVG), favicon (SVG + ICO with
prefers-color-scheme), and the approval-gated 1200x630 OG PNG are all delivered from
Figma-sourced elements and wired into BaseLayout/SEO — with no hand-authored geometry and
Joel's OG approval on record.
</success_criteria>

<output>
After completion, create `.planning/phases/33-token-foundation-fonts/33-06-SUMMARY.md`
noting whether the waveform SVG came from MCP or manual Copy-as-SVG, the OG composition
approach (composed vs exported frame), and Joel's OG approval.
</output>
