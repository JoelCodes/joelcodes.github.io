---
phase: 33-token-foundation-fonts
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [FOUND-04]
files_modified:
  - lighthouserc.json
  - lighthouserc-mobile.json
  - .github/workflows/deploy.yml
user_setup: []

threat_model:
  assets:
    - "CI quality-gate configuration integrity (Lighthouse assertions guard perf/a11y/SEO)"
  threats:
    - id: T-03
      description: "CI config integrity: a malformed or overly-lax lighthouserc could silently let performance/accessibility regressions ship (weakened quality gate)."
      mitigation: "Assertion thresholds are copied verbatim from the existing hardened config; only URL list and two audit toggles change. Both JSON files validate as parseable JSON in a build check."
    - id: T-04
      description: "Pinned GitHub Action (treosh/lighthouse-ci-action@v12) is a third-party CI dependency (supply-chain)."
      mitigation: "Reuse the already-vetted, already-pinned @v12 tag the repo already runs; add no new action, introduce no unpinned version."
  asvs_controls:
    - "V14.2 (dependency / build pipeline integrity — reuse pinned action, no new supply-chain surface)"
  block_on: high

must_haves:
  truths:
    - "Lighthouse CI tests landing + one real blog post URL on both mobile and desktop (FOUND-04)."
    - "The lcp-lazy-loaded and prioritize-lcp-image audits are re-enabled as warn (were off) so the Phase-38 LCP bug is surfaced without blocking CI now (FOUND-04)."
    - "A separate lighthouserc-mobile.json exists with the same URL list and assertions but no desktop preset (Lighthouse default mobile emulation)."
  artifacts:
    - path: "lighthouserc-mobile.json"
      provides: "Mobile Lighthouse pass config"
      contains: "numberOfRuns"
    - path: "lighthouserc.json"
      provides: "Expanded desktop Lighthouse config"
      contains: "lcp-lazy-loaded"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "lighthouserc-mobile.json"
      via: "second treosh/lighthouse-ci-action step configPath"
      pattern: "lighthouserc-mobile.json"
---

<objective>
Expand the Lighthouse CI net before any page migration: test the landing page plus one real
blog post URL on both mobile and desktop, and re-enable the `lcp-lazy-loaded` /
`prioritize-lcp-image` audits. This is the FOUND-04 quality-gate expansion.

Purpose: Every downstream page phase must run against a hardened, multi-URL, mobile+desktop
Lighthouse gate. This plan installs that net now, before regressions can appear.
Output: expanded `lighthouserc.json`, new `lighthouserc-mobile.json`, and a second mobile
Lighthouse step in `deploy.yml`.

This plan is fully independent of Figma extraction and can run in Wave 1 alongside 33-01.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-token-foundation-fonts/33-PATTERNS.md
@.planning/phases/33-token-foundation-fonts/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover real blog slug and expand lighthouserc.json</name>
  <files>lighthouserc.json</files>
  <read_first>
    - lighthouserc.json (current config — full structure: collect.url, assert.assertions with the two disabled audits)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (lighthouserc.json section — the exact "Changes required" list)
    - .planning/phases/33-token-foundation-fonts/33-RESEARCH.md (Lighthouse CI Technical Details — audit enable/disable rationale)
  </read_first>
  <action>
    Run `npm run build`, then `ls dist/blog/` to identify a real blog post directory. The current
    blog content has slugs `getting-started-with-automation` and `im-pivoting`; use whichever
    directory actually exists in `dist/blog/` (prefer `getting-started-with-automation`). In
    `lighthouserc.json`: expand `collect.url` from `["http://localhost/"]` to
    `["http://localhost/", "http://localhost/blog/{real-slug}/"]`. Do NOT add the `/showcase/` URL
    yet — that route does not exist until Phase 38 (per RESEARCH.md open question 4); instead add a
    top-level `"_comment"` key noting showcase activates in Phase 38. Change assertion
    `lcp-lazy-loaded` from `"off"` to `"warn"` and `prioritize-lcp-image` from `"off"` to `"warn"`.
    Keep `render-blocking-insight` and `render-blocking-resources` at `"off"` (Google Fonts still
    loading until Phase 41). Leave `settings.preset` = `"desktop"` and ALL threshold assertions
    (`categories:performance` error 0.9, etc.) exactly as they are. The file must remain valid JSON.
  </action>
  <verify>`node -e "JSON.parse(require('fs').readFileSync('lighthouserc.json','utf8'))"` exits 0; `grep '"lcp-lazy-loaded": "warn"' lighthouserc.json` matches; `grep 'blog/' lighthouserc.json` matches a real slug.</verify>
  <acceptance_criteria>
    - `lighthouserc.json` parses as valid JSON.
    - `collect.url` array has exactly 2 entries: `http://localhost/` and `http://localhost/blog/{slug}/` where `{slug}` is a directory present in `dist/blog/`.
    - `"lcp-lazy-loaded": "warn"` and `"prioritize-lcp-image": "warn"` are present (grep confirms both).
    - `"render-blocking-insight": "off"` and `"render-blocking-resources": "off"` are still present.
    - `settings.preset` is still `"desktop"`; the four `categories:*` assertions are byte-for-byte unchanged.
  </acceptance_criteria>
  <done>Desktop config tests 2 real URLs with the two LCP audits re-enabled as warnings, thresholds unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create lighthouserc-mobile.json and add mobile step to deploy.yml</name>
  <files>lighthouserc-mobile.json, .github/workflows/deploy.yml</files>
  <read_first>
    - lighthouserc.json (the just-updated desktop config — source to copy from)
    - .github/workflows/deploy.yml (existing "Run Lighthouse CI" step at lines ~38-45 — the step to duplicate; note 6-space step indent, 8-space with-key indent, @v12 pin)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (lighthouserc-mobile.json + deploy.yml sections — exact structural changes)
  </read_first>
  <action>
    Create `lighthouserc-mobile.json` as a copy of the updated `lighthouserc.json` with ONE
    structural change: remove the `"settings": { "preset": "desktop" }` key from `collect` entirely
    (Lighthouse's default is Moto G4 mobile emulation). Keep the identical `url` array,
    `numberOfRuns`, `staticDistDir`, the full `assert` block (same thresholds, same
    `lcp-lazy-loaded`/`prioritize-lcp-image` = warn), and the `upload` block. In `deploy.yml`, insert
    a new step named "Run Lighthouse CI (mobile)" immediately AFTER the existing "Run Lighthouse CI"
    step and BEFORE the "Upload build artifact" step. The new step uses `treosh/lighthouse-ci-action@v12`
    with `configPath: './lighthouserc-mobile.json'`, `staticDistDir: './dist'`, `uploadArtifacts: true`,
    `temporaryPublicStorage: true`, `runs: 3` — matching the existing step's field order and
    indentation exactly (6-space step indent, 8-space `with` keys).
  </action>
  <verify>`node -e "JSON.parse(require('fs').readFileSync('lighthouserc-mobile.json','utf8'))"` exits 0; `grep -c "treosh/lighthouse-ci-action@v12" .github/workflows/deploy.yml` returns 2; `grep "lighthouserc-mobile.json" .github/workflows/deploy.yml` matches.</verify>
  <acceptance_criteria>
    - `lighthouserc-mobile.json` parses as valid JSON and has NO `settings.preset` key (`grep -c '"preset"' lighthouserc-mobile.json` returns 0).
    - `lighthouserc-mobile.json` `collect.url` equals the `lighthouserc.json` `collect.url` (same 2 URLs).
    - `deploy.yml` contains exactly two `treosh/lighthouse-ci-action@v12` steps.
    - The mobile step references `configPath: './lighthouserc-mobile.json'` and sits between the desktop Lighthouse step and the "Upload build artifact" step.
    - `deploy.yml` remains valid YAML (`npm run build` unaffected; workflow file parses — no tab characters, consistent indentation).
  </acceptance_criteria>
  <done>A mobile Lighthouse pass runs in CI alongside the desktop pass, both against the same 2-URL list.</done>
</task>

</tasks>

<verification>
- Both `lighthouserc.json` and `lighthouserc-mobile.json` parse as valid JSON.
- `deploy.yml` has two `treosh/lighthouse-ci-action@v12` steps (desktop + mobile).
- Both configs re-enable `lcp-lazy-loaded` and `prioritize-lcp-image` as `warn`.
- `npm run build` still succeeds (the build the blog-slug discovery depended on).
</verification>

<success_criteria>
FOUND-04 satisfied: Lighthouse CI tests landing + one real blog post on both mobile and
desktop, `lcp-lazy-loaded` and `prioritize-lcp-image` are re-enabled as warnings, and the
mobile config carries no desktop preset. Thresholds are unchanged from the hardened baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/33-token-foundation-fonts/33-02-SUMMARY.md`
noting the real blog slug used and confirming both CI passes are wired.
</output>
