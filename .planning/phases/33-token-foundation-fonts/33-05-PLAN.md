---
phase: 33-token-foundation-fonts
plan: 05
type: execute
wave: 4
depends_on: ["33-01", "33-04"]
autonomous: true
requirements: [FOUND-02]
files_modified:
  - package.json
  - package-lock.json
  - src/styles/global.css
  - astro.config.mjs
  - src/layouts/BaseLayout.astro
user_setup: []

threat_model:
  assets:
    - "Font-loading supply chain + CLS/CWV integrity"
  threats:
    - id: T-09
      description: "Supply-chain: installing @fontsource-variable/fraunces, @fontsource-variable/hanken-grotesk, and fontaine adds three npm dependencies (npm package integrity)."
      mitigation: "All three are official ecosystem packages (fontsource.org, unjs/fontaine) pinned to the researched versions; install via npm with the committed package-lock; no postinstall scripts beyond the packages' own; versions match RESEARCH Standard Stack table."
    - id: T-10
      description: "A wrong woff2 preload filename or missing Fontaine metrics reintroduces FOUT/CLS (self-inflicted CWV regression, not security but a quality gate)."
      mitigation: "Filenames are verified via `ls node_modules/...` before writing preload imports; Fontaine output is confirmed by grepping the built CSS for ascent-override; CLS validated in the verify step."
  asvs_controls:
    - "V14.2 (dependency integrity — pinned official packages, committed lockfile)"
  block_on: high

must_haves:
  truths:
    - "Fraunces (upright + italic, opsz/wght axes) and Hanken Grotesk (wght) load from self-hosted @fontsource-variable packages via wght.css/wght-italic.css imports — NOT Google Fonts (FOUND-02, D-08)."
    - "Three woff2 preload <link>s (Fraunces normal, Fraunces italic, Hanken normal) are emitted in <head> using Vite ?url imports, filenames verified from node_modules."
    - "Fontaine Vite plugin generates ascent-override/descent-override/size-adjust fallback metrics so homepage CLS = 0 (FOUND-02, RESEARCH Pattern 3)."
    - "All 13 type-ramp styles exist as composite .wl-* utility classes in @layer utilities, names mirroring Figma style names, with breakpoints baked at 390/768/1440/1920 (no clamp) (FOUND-02, D-05/D-06/D-07)."
    - "The existing Google Fonts block stays in BaseLayout (removal is Phase 41); existing pages render unchanged."
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Self-hosted font preload links + fontsource ?url imports"
      contains: "rel=\"preload\""
    - path: "astro.config.mjs"
      provides: "Fontaine Vite plugin for CLS=0 fallback metrics"
      contains: "FontaineTransform"
    - path: "src/styles/global.css"
      provides: "fontsource @imports + 13 .wl-* type-ramp utility classes"
      contains: "@fontsource-variable/fraunces/wght.css"
  key_links:
    - from: "src/layouts/BaseLayout.astro ?url font imports"
      to: "self-hosted woff2 in node_modules/@fontsource-variable"
      via: "Vite ?url resolves to hashed /_astro asset, preloaded"
      pattern: "fraunces-latin-wght-normal.woff2"
    - from: "global.css .wl-* type utilities"
      to: "--font-wl-heading / --font-wl-body tokens (from 33-04)"
      via: "font-family: var(--font-wl-*)"
      pattern: "\\.wl-"
---

<objective>
Self-host Fraunces + Hanken Grotesk via `@fontsource-variable`, wire CLS=0 fallback metrics
with Fontaine, preload the woff2 files, and author the 13-style type ramp as composite `.wl-*`
utility classes. This is FOUND-02 — the fonts and type system every downstream page uses.

Purpose: Downstream components need the variable fonts loaded (self-hosted, no Google latency,
CLS=0) and the full 13-style ramp available as one-class-per-Figma-style utilities.
Output: fontsource packages installed, Fontaine plugin added, preloads in BaseLayout, and
`.wl-*` type utilities in global.css.

Depends on 33-04 because it shares `src/styles/global.css` (must run after the @theme/token
block exists so the type utilities can reference `--font-wl-*`).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md
@.planning/phases/33-token-foundation-fonts/33-PATTERNS.md
@.planning/phases/33-token-foundation-fonts/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fontsource + fontaine, add imports and Fontaine plugin</name>
  <files>package.json, package-lock.json, src/styles/global.css, astro.config.mjs</files>
  <read_first>
    - package.json (confirm "type": "module"; see existing deps to match version-pin style)
    - astro.config.mjs (existing vite.plugins array with tailwindcss() at line ~22; integrations array pattern)
    - src/styles/global.css (the top of file — @import "tailwindcss" at line 1; fontsource @imports go immediately after it)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (astro.config.mjs + global.css sections — exact Fontaine two-invocation pattern and @import placement)
    - .planning/phases/33-token-foundation-fonts/33-RESEARCH.md (Standard Stack versions; Pitfall 3 Fontaine resolvePath caveat)
  </read_first>
  <action>
    Run `npm install @fontsource-variable/fraunces@^5.2.9 @fontsource-variable/hanken-grotesk@^5.2.8 fontaine@^0.5.0`.
    Add three `@import` lines at the TOP of `src/styles/global.css` immediately after `@import "tailwindcss";`:
    `@fontsource-variable/fraunces/wght.css`, `@fontsource-variable/fraunces/wght-italic.css`,
    `@fontsource-variable/hanken-grotesk/wght.css` (wght.css + wght-italic.css ONLY — never `full.css`).
    In `astro.config.mjs`, import `{ FontaineTransform } from 'fontaine'` and add TWO
    `FontaineTransform.vite({...})` entries to `vite.plugins` AFTER `tailwindcss()`: one with
    `fallbacks: ['Georgia', 'Times New Roman', 'serif']` (Fraunces), one with
    `fallbacks: ['Arial', 'Helvetica Neue', 'sans-serif']` (Hanken Grotesk), each with a `resolvePath`
    that maps fontsource ids into node_modules. Per RESEARCH Pitfall 3, VERIFY the resolvePath works:
    run `npm run build`, then `grep -r "ascent-override" dist/` — if no Fontaine `@font-face` fallback
    blocks appear, the resolvePath is wrong (likely needs `./node_modules/@fontsource-variable` rather
    than `./node_modules`); fix and rebuild until `ascent-override` appears in the built CSS. Do NOT
    remove or alter the Google Fonts block (that is Phase 41).
  </action>
  <verify>`ls node_modules/@fontsource-variable/fraunces/wght.css node_modules/@fontsource-variable/fraunces/wght-italic.css node_modules/@fontsource-variable/hanken-grotesk/wght.css` all exist; `npm run build` succeeds; `grep -rl "ascent-override" dist/` returns at least one file (Fontaine metrics present).</verify>
  <acceptance_criteria>
    - `package.json` dependencies include `@fontsource-variable/fraunces`, `@fontsource-variable/hanken-grotesk`, and `fontaine`.
    - `src/styles/global.css` contains the three `@import` lines for `wght.css`, `wght-italic.css` (fraunces), and `wght.css` (hanken-grotesk); it does NOT import any `full.css`.
    - `astro.config.mjs` imports `FontaineTransform` and has two `FontaineTransform.vite(` entries after `tailwindcss()`.
    - `npm run build` completes with zero errors.
    - `grep -rl "ascent-override" dist/` returns >= 1 file (proves Fontaine fallback metrics were generated).
  </acceptance_criteria>
  <done>Fonts are self-hosted and imported, and Fontaine emits CLS-eliminating fallback metrics into the built CSS.</done>
</task>

<task type="auto">
  <name>Task 2: Add woff2 preloads in BaseLayout</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (frontmatter imports; the <head> block with existing Google Fonts preload/link tags at lines ~30-48; the FOUC is:inline script at ~51-58 which must stay untouched; favicon tags at ~19-20 untouched)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (BaseLayout.astro section — exact ?url import lines and the 3 <link rel="preload"> tags, placement before the Google Fonts block)
    - .planning/phases/33-token-foundation-fonts/33-RESEARCH.md (Pitfall 2 — verify exact woff2 filenames from node_modules before writing the imports)
  </read_first>
  <action>
    First run `ls node_modules/@fontsource-variable/fraunces/files/ | grep wght` and
    `ls node_modules/@fontsource-variable/hanken-grotesk/files/ | grep wght` to CONFIRM the exact woff2
    filenames (expected `fraunces-latin-wght-normal.woff2`, `fraunces-latin-wght-italic.woff2`,
    `hanken-grotesk-latin-wght-normal.woff2` — but verify, per Pitfall 2). Add three `?url` imports to
    the BaseLayout frontmatter (after existing imports): the confirmed Fraunces normal, Fraunces
    italic, and Hanken normal woff2 files, each with `?url` suffix. In `<head>`, add three
    `<link rel="preload" as="font" type="font/woff2" href={...} crossorigin="anonymous" />` tags — one
    per imported url — placed BEFORE the existing Google Fonts block. Do NOT modify or remove the
    Google Fonts block, the FOUC `is:inline` script, or the favicon `<link>` tags.
  </action>
  <verify>`npm run build` succeeds; build the site and confirm `dist/index.html` (or the relevant built page) contains three `<link rel="preload" as="font"` tags referencing hashed `/_astro/*.woff2` URLs.</verify>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` frontmatter has three `?url` font imports whose filenames match actual files in `node_modules/@fontsource-variable/*/files/`.
    - `<head>` contains exactly three new `<link rel="preload" as="font" type="font/woff2" ... crossorigin="anonymous" />` tags.
    - `npm run build` completes; `grep -c 'rel="preload" as="font"' dist/index.html` returns >= 3.
    - The preloaded font URLs resolve to `/_astro/` hashed assets (self-hosted), not `fonts.googleapis.com`/`fonts.gstatic.com`.
    - The existing Google Fonts block, FOUC `is:inline` script, and favicon tags are still present and unchanged.
  </acceptance_criteria>
  <done>Three self-hosted variable fonts are preloaded from hashed local assets; Google Fonts coexistence preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Author the 13-style type ramp as composite .wl-* utilities</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (existing @layer utilities block — the type ramp classes append here; the --font-wl-heading/--font-wl-body tokens added by 33-04 must already exist)
    - .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md (Type Ramp table — all 13 class names, font tokens, per-breakpoint sizes, weight, line-height, letter-spacing, italic, opsz)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (global.css type-ramp pattern — the inline @media breakpoint structure at 390/768/1440/1920)
    - .planning/phases/33-token-foundation-fonts/33-CONTEXT.md (D-05 composite one-class-per-style, D-06 kebab wl- names, D-07 baked breakpoints no clamp)
  </read_first>
  <action>
    Append 13 composite utility classes to the existing `@layer utilities` block in global.css, one
    per Figma type style, using the exact `.wl-{name}` class names and values from the 33-01 Type Ramp
    table. Each class sets `font-family: var(--font-wl-heading)` or `var(--font-wl-body)` per the table,
    plus `font-size` (the 1440px value as the base), `font-weight`, `line-height`, `letter-spacing`,
    and `font-optical-sizing: auto` for Fraunces styles (opsz axis); set `font-style: italic` for rows
    flagged italic. Bake the breakpoint steps per D-07: add inline `@media (max-width: 1920px)`,
    `@media (max-width: 768px)`, and `@media (max-width: 390px)` overrides of ONLY `font-size` using the
    corresponding Figma-specced sizes — for any width the table marks "not specced", omit that media
    step (do NOT interpolate, no `clamp()`). Do not touch old utility classes.
  </action>
  <verify>`npm run build` succeeds; `grep -c "^\.wl-\| \.wl-" src/styles/global.css` shows 13 distinct base class selectors; each class references `var(--font-wl-heading)` or `var(--font-wl-body)`.</verify>
  <acceptance_criteria>
    - `src/styles/global.css` defines exactly 13 base `.wl-*` type-ramp class selectors, names matching the extraction Type Ramp table.
    - Every `.wl-*` type class sets `font-family: var(--font-wl-heading)` or `var(--font-wl-body)`.
    - Fraunces-family classes include `font-optical-sizing: auto`.
    - Breakpoint overrides use only hardcoded `@media (max-width: 390px|768px|1920px)` steps; `grep -c "clamp(" src/styles/global.css` returns 0 (no fluid interpolation).
    - `npm run build` completes with zero errors.
  </acceptance_criteria>
  <done>All 13 Figma type styles are available as composite .wl-* utility classes with baked breakpoint sizes.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- fontsource packages + fontaine installed and pinned in package.json.
- Three woff2 preloads emitted in built HTML from `/_astro/` (self-hosted, not Google).
- Fontaine `ascent-override` present in built CSS (CLS=0 mechanism live).
- 13 `.wl-*` type utilities exist with baked breakpoints, no clamp.
- Google Fonts block + FOUC script + favicon tags unchanged.
</verification>

<success_criteria>
FOUND-02 satisfied: Fraunces (upright + italic, opsz/wght) and Hanken Grotesk load
self-hosted with preloads and Fontaine CLS=0 fallback metrics, and the 13-style Figma type
ramp is available as composite `.wl-*` utility classes with baked breakpoints. Existing pages
still render via Google Fonts coexistence.
</success_criteria>

<output>
After completion, create `.planning/phases/33-token-foundation-fonts/33-05-SUMMARY.md`
noting the confirmed woff2 filenames, the working Fontaine resolvePath, and the final list of
13 `.wl-*` class names.
</output>
