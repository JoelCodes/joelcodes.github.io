---
phase: 33-token-foundation-fonts
plan: 04
type: execute
wave: 3
depends_on: ["33-01", "33-03"]
autonomous: true
requirements: [FOUND-01, FOUND-03]
files_modified:
  - src/styles/global.css
user_setup: []

threat_model:
  assets:
    - "Design token integrity + namespace isolation (existing pages must keep rendering)"
  threats:
    - id: T-07
      description: "A --wl-* token name colliding with an existing old token or a Tailwind default (e.g. --color-accent) would shadow/break existing neobrutalist pages before Phase 41 cleanup."
      mitigation: "All new tokens carry the --color-wl-* infix; the new @theme block is placed above the old block with an isolation comment; a collision grep asserts no --wl- name equals an existing name."
    - id: T-08
      description: "Invented hex values (not Figma-traced) entering @theme = fidelity drift."
      mitigation: "Every value is copied from the 33-01 extraction artifact rows; a grep asserts no EXTRACT-FROM-FIGMA placeholder remains."
  asvs_controls: []
  block_on: high

must_haves:
  truths:
    - "A new @theme block (no `inline`) in global.css defines all 8 --color-wl-* light tokens, each value copied verbatim from the 33-01 extraction artifact (FOUND-01, D-02)."
    - "A plain .dark {} block redefines the same custom properties with dark values (semantic flip, one token set, no dark: pairs) — D-01; tokens with no dark evidence keep light value per D-04."
    - "The --color-wl-accent-soft-text companion (value from 33-03 summary) is defined in @theme so AA-failing text use has a compliant token (FOUND-03, D-09)."
    - "The @layer base body rule sets font-family: var(--font-wl-body) AND an explicit line-height sourced from Figma body style (D-08 + UI-SPEC FLAG); existing pages' font-body utility still wins over base."
    - "Old neobrutalist tokens and Tailwind defaults are untouched; --wl-* names collide with none of them (D-02 namespace isolation)."
  artifacts:
    - path: "src/styles/global.css"
      provides: "8 --wl-* palette tokens (light+dark) + companion + font-family tokens + body base default"
      contains: "--color-wl-ink"
  key_links:
    - from: "global.css @theme --wl-* block"
      to: "33-05 type ramp utilities + downstream components"
      via: "Tailwind generates bg-wl-*/text-wl-* utilities from these tokens"
      pattern: "--color-wl-"
    - from: "global.css .dark block"
      to: "existing BaseLayout FOUC is:inline .dark toggle"
      via: "cascade — .dark class flips --wl-* values, no new JS"
      pattern: "\\.dark"
---

<objective>
Write the eight `--wl-*` sea-cool palette tokens (light + dark), the `--wl-accent-soft-text`
companion, the two font-family tokens, and the body base default into `src/styles/global.css`
— all Figma-traced, namespace-isolated from the old neobrutalist tokens. This is FOUND-01 (the
token set) plus the FOUND-03 companion wire-in.

Purpose: Every downstream component and page builds against these tokens. They must be
correct, Figma-sourced, and isolated so existing pages keep rendering unchanged until Phase 41.
Output: an expanded `global.css` with a new `@theme` block, a `.dark {}` flip block, and an
`@layer base` body rule.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md
@.planning/phases/33-token-foundation-fonts/33-03-SUMMARY.md
@.planning/phases/33-token-foundation-fonts/33-PATTERNS.md
@.planning/phases/33-token-foundation-fonts/33-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --wl-* @theme block, .dark flip, and font-family tokens</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (current file — existing @theme block lines 1-68, the @custom-variant dark at line ~71, the existing --color-*-text companion precedent at lines 17-21; confirms the project never uses @theme inline)
    - .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md (Palette Token Mapping + FIDELITY-GAPS — the exact light/dark hex values to copy for all 8 tokens)
    - .planning/phases/33-token-foundation-fonts/33-03-SUMMARY.md (the derived --color-wl-accent-soft-text hex value + any other companion tokens)
    - .planning/phases/33-token-foundation-fonts/33-PATTERNS.md (global.css section — exact insertion placement: fontsource @imports at top, new @theme ABOVE old block with isolation comment, .dark block as plain block not second @theme)
  </read_first>
  <action>
    Insert a NEW `@theme` block (plain `@theme`, NOT `@theme inline` — runtime override of `.dark`
    depends on this) ABOVE the existing neobrutalist `@theme` block, preceded by the isolation comment
    banner ("WL TOKENS (v3.0 Wavelength — namespace isolated via --wl- prefix) / DO NOT edit old token
    block below until Phase 41 cleanup"). Define all 8 palette properties with the LIGHT hex values
    copied verbatim from 33-FIGMA-EXTRACTION.md: `--color-wl-ink`, `--color-wl-sub`, `--color-wl-accent`,
    `--color-wl-accent-soft`, `--color-wl-sea-glass`, `--color-wl-sea-glass-deep`, `--color-wl-paper`,
    `--color-wl-line` (line may be a `color-mix(...)` expression — copy as recorded). Add the companion
    `--color-wl-accent-soft-text` with the hex from 33-03-SUMMARY (plus any other `-text` companions
    that summary lists). Add the two font tokens: `--font-wl-heading: 'Fraunces Variable', Georgia, serif;`
    and `--font-wl-body: 'Hanken Grotesk Variable', ui-sans-serif, system-ui, sans-serif;`. Then add a
    PLAIN `.dark { }` block (not a second `@theme`) redefining the `--color-wl-*` properties with the
    DARK values from the extraction artifact; OMIT any token flagged `keep-light` in FIDELITY-GAPS
    (D-04 — it keeps its light value automatically). Do NOT touch the existing old `@theme` block, the
    existing `@custom-variant dark`, or any old `--color-*`/`--font-*`/`--spacing-neo-*`/`--border-neo`
    token. Ensure no `--wl-*` name equals an existing token name or the Tailwind default `--color-accent`.
  </action>
  <verify>`npm run build` succeeds; `grep -c "EXTRACT FROM FIGMA\|\[FIGMA VALUE\]" src/styles/global.css` returns 0; `grep -c "color-wl-" src/styles/global.css` returns >= 16 (8 light + 8 dark, minus any keep-light omissions plus companion).</verify>
  <acceptance_criteria>
    - `npm run build` completes with zero errors.
    - All 8 property names present in the new @theme block: `grep -c -- "--color-wl-\(ink\|sub\|accent\|accent-soft\|sea-glass\|sea-glass-deep\|paper\|line\)" src/styles/global.css` >= 8.
    - `--color-wl-accent-soft-text` is defined in the @theme block.
    - `--font-wl-heading` and `--font-wl-body` are defined with the exact family stacks above.
    - A `.dark {` block exists that redefines `--color-wl-*` properties (grep `\.dark {` matches and dark block contains `--color-wl-paper`), and it is NOT introduced as `@theme` (no second `@theme` for wl tokens).
    - No placeholder strings remain: `grep -c "EXTRACT FROM FIGMA\|\[FIGMA VALUE\]\|PLACEHOLDER" src/styles/global.css` returns 0.
    - The new `@theme` block is not `@theme inline` (`grep "@theme inline" src/styles/global.css` returns nothing).
    - No collision: no `--wl-` token name duplicates an existing `--color-*`/`--font-*` name already in the old block (spot-check `--color-accent` is NOT redefined without the `wl-` infix).
  </acceptance_criteria>
  <done>All 8 palette tokens (light + dark), the companion, and font tokens exist in an isolated @theme block with a working .dark flip; the build is green and old pages are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add @layer base body default with explicit line-height</name>
  <files>src/styles/global.css</files>
  <read_first>
    - src/styles/global.css (confirm there is no existing @layer base block; the existing @layer utilities pattern shows layer usage)
    - .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md (Type Ramp — the body/regular style's line-height value, the UI-SPEC FLAG requires an explicit line-height on the body base rule sourced from the Figma body style)
    - .planning/phases/33-token-foundation-fonts/33-CONTEXT.md (D-08 — body default + explicit classes; existing pages' font-body utility wins over base layer)
    - .planning/phases/33-token-foundation-fonts/33-UI-SPEC.md (Body Default section + the APPROVED FLAG: add explicit line-height to the @layer base body rule)
  </read_first>
  <action>
    Add a NEW `@layer base { body { ... } }` block (the file currently has no `@layer base`). Set
    `font-family: var(--font-wl-body);` AND an explicit `line-height:` whose value is the Figma body
    style's line-height recorded in the 33-01 Type Ramp table (this satisfies the UI-SPEC APPROVED FLAG
    — do NOT invent a line-height; use the extracted body-style value). Because this is a base-layer
    rule, existing pages that set `font-body` (a utility class) on `<body>` still win via
    utility-over-base specificity (D-08) — verify this does not visually change existing pages. Do NOT
    add any other element rules to `@layer base` (blog/MDX prose mapping is deferred to Phase 38).
  </action>
  <verify>`npm run build` succeeds; `grep -A3 "@layer base" src/styles/global.css` shows `body` with `font-family: var(--font-wl-body)` and an explicit `line-height`.</verify>
  <acceptance_criteria>
    - `src/styles/global.css` contains exactly one `@layer base` block, and it targets `body`.
    - The body rule sets `font-family: var(--font-wl-body);`.
    - The body rule sets an explicit `line-height:` value (grep `line-height` within the @layer base block matches) matching the Figma body style line-height from the extraction artifact.
    - `npm run build` completes with zero errors.
    - No other selectors are added inside `@layer base` (only `body`).
  </acceptance_criteria>
  <done>The body base default applies Hanken Grotesk + the Figma-sourced line-height to new WL pages without affecting existing font-body pages.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- All 8 `--wl-*` tokens defined light + dark (minus keep-light omissions), plus companion + 2 font tokens.
- New `@theme` block is plain `@theme` (not inline); `.dark` flip is a plain block.
- `@layer base body` has font-family + explicit line-height.
- Zero placeholders; zero collisions with old tokens.
- Existing pages render unchanged (namespace isolation intact).
</verification>

<success_criteria>
FOUND-01 satisfied: `global.css` carries all 8 `--wl-*` palette tokens in both light and dark,
every value Figma-traced, plus the FOUND-03 companion and the two font-family tokens — all
namespace-isolated so old neobrutalist pages keep rendering. The body base default is in place
with an explicit Figma-sourced line-height.
</success_criteria>

<output>
After completion, create `.planning/phases/33-token-foundation-fonts/33-04-SUMMARY.md`
listing the final token values written, which tokens kept light values in dark mode (per
FIDELITY-GAPS), and confirming the build stayed green.
</output>
