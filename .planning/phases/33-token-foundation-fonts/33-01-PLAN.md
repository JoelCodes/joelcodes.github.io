---
phase: 33-token-foundation-fonts
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-05]
files_modified:
  - .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md
user_setup: []

threat_model:
  assets:
    - "Figma-sourced token/asset values (design fidelity integrity)"
  threats:
    - id: T-01
      description: "Invented (non-Figma) hex/SVG values silently entering the design foundation, causing fidelity drift that cascades to every downstream phase (v1.4 root-cause)."
      mitigation: "Every value in the extraction artifact carries a Figma source column (variable name or node id). No row without a Figma provenance is valid. Gaps go to FIDELITY-GAPS, never filled by estimate."
    - id: T-02
      description: "Supply-chain: none in this plan (no packages installed, no code executed)."
      mitigation: "N/A — this plan only reads Figma and writes a markdown artifact."
  asvs_controls: []
  block_on: high

must_haves:
  truths:
    - "Every one of the 8 --wl-* light-mode hex values is traceable to a named Figma variable (D-02: Figma palette names 1:1)."
    - "Dark-mode values are sourced Figma-variables-first, dark-frame-117:103-fallback (D-03); any token with no dark evidence is listed in FIDELITY-GAPS keeping its light value (D-04)."
    - "All 13 type-ramp styles are recorded with family/size-per-breakpoint/weight/line-height/letter-spacing/italic/opsz, class names mirroring Figma style names kebab-cased with wl- prefix (D-05, D-06, D-07)."
    - "The waveform mark's SVG path geometry is captured from Figma, never hand-authored (D-13, D-16)."
    - "The OG tagline is captured verbatim from Figma (D-15); the check for an existing 1200x630 OG frame is recorded."
  artifacts:
    - path: ".planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md"
      provides: "Committed token-mapping + type-ramp + waveform-SVG + OG-tagline artifact that all downstream autonomous plans read"
      contains: "## Palette Token Mapping"
  key_links:
    - from: "33-FIGMA-EXTRACTION.md palette table"
      to: "33-03 @theme block in global.css"
      via: "downstream executor reads committed hex values"
      pattern: "wl-ink|wl-paper|wl-accent"
    - from: "33-FIGMA-EXTRACTION.md waveform SVG"
      to: "33-06 WaveMark.astro + favicon.svg"
      via: "downstream executor reads committed path data"
      pattern: "path d="
---

<objective>
Extract every Figma-sourced value this phase needs into ONE committed markdown artifact
(`33-FIGMA-EXTRACTION.md`) so that all downstream plans can run autonomously in executor
subagents that have NO Figma MCP access.

Purpose: Figma MCP tools (`get_variable_defs`, `get_design_context`, `download_assets`) exist
ONLY in the main orchestrator session. This plan front-loads all extraction into a single
`autonomous: false` step; the resulting artifact is the contract every later plan reads from.
No downstream task may invent a value — it must trace to a row in this artifact.

Output: `.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md` containing the
palette mapping (light + dark), the 13-style type ramp, the waveform SVG path data, and the
OG tagline + OG-frame check.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-token-foundation-fonts/33-CONTEXT.md
@.planning/phases/33-token-foundation-fonts/33-UI-SPEC.md
@.planning/phases/33-token-foundation-fonts/33-RESEARCH.md
</context>

<execution_note>
ALL tasks in this plan require Figma MCP access, which is available ONLY to the main
orchestrator session — not to executor subagents. This entire plan is `autonomous: false`.
The orchestrator MUST perform the Figma extraction inline (calling `get_variable_defs`,
`get_design_context`, `get_screenshot`, `download_assets` on file `1tg8wIPcvOVC5tPZ8pkGO2`)
and write the results into the artifact directly. Do NOT delegate these tasks to a subagent
that lacks Figma tools.
</execution_note>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Extract palette (light + dark) into token-mapping table</name>
  <files>.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md</files>
  <read_first>
    - .planning/phases/33-token-foundation-fonts/33-UI-SPEC.md (Token Contract section — the 8 CSS custom property names, Figma palette names, PROJECT.md reference hexes, and the dark-mode sourcing rule)
    - .planning/phases/33-token-foundation-fonts/33-CONTEXT.md (D-02 Figma names 1:1, D-03 dark sourcing, D-04 dark gaps flag-and-ship-light)
    - .planning/PROJECT.md (fixed palette reference values to sanity-check the extracted values against)
  </read_first>
  <action>
    Using the main-session Figma MCP, call `get_variable_defs` on file `1tg8wIPcvOVC5tPZ8pkGO2`.
    Create `33-FIGMA-EXTRACTION.md` with a `## Palette Token Mapping` section: a table with
    columns `Figma variable name | CSS custom property | Light hex | Dark hex | Dark source`.
    Populate exactly these 8 CSS properties: `--color-wl-ink`, `--color-wl-sub`, `--color-wl-accent`,
    `--color-wl-accent-soft`, `--color-wl-sea-glass`, `--color-wl-sea-glass-deep`, `--color-wl-paper`,
    `--color-wl-line`. For light hex: use the extracted Figma variable value (D-02, names 1:1 with
    Figma). For dark hex: check for a Figma dark mode variable set first (D-03); if none exists, call
    `get_design_context` or `get_screenshot` on node `117:103` (dark Landing mockup) and sample the
    fills. Record the dark source per row ("figma-var" or "frame-117:103"). Per D-04, any token with
    no dark evidence in either source keeps its light value in dark mode AND is added to a
    `## FIDELITY-GAPS` section (format: "Token `--wl-{name}`: no dark value found in Figma. Keeping
    light value `#XXXXXX`. Needs Figma review."). Cross-check every extracted light value against the
    PROJECT.md reference table; if a value diverges materially, note it — Figma is source of truth,
    but flag surprises. `--color-wl-line` may legitimately be a `color-mix(...)` expression rather
    than a flat hex — record it as-is.
  </action>
  <verify>The `## Palette Token Mapping` section has exactly 8 rows, every row has a non-empty Figma variable name and a light hex; every dark cell is either a hex, a color-mix expression, or explicitly "keep light (see FIDELITY-GAPS)".</verify>
  <acceptance_criteria>
    - `33-FIGMA-EXTRACTION.md` contains a `## Palette Token Mapping` heading followed by a table.
    - The table lists all 8 property names verbatim: `--color-wl-ink`, `--color-wl-sub`, `--color-wl-accent`, `--color-wl-accent-soft`, `--color-wl-sea-glass`, `--color-wl-sea-glass-deep`, `--color-wl-paper`, `--color-wl-line`.
    - No cell contains a placeholder token (`grep -c "EXTRACT FROM FIGMA\|\[FIGMA VALUE\]\|TBD" 33-FIGMA-EXTRACTION.md` returns 0 within the palette section).
    - Every row's "Dark source" cell is one of: `figma-var`, `frame-117:103`, or `keep-light`.
    - If any row is `keep-light`, a matching entry exists under a `## FIDELITY-GAPS` heading.
  </acceptance_criteria>
  <done>All 8 palette tokens have Figma-traced light values and a resolved dark disposition; gaps (if any) are listed under FIDELITY-GAPS.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Extract 13-style type ramp into lookup table</name>
  <files>.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md</files>
  <read_first>
    - .planning/phases/33-token-foundation-fonts/33-UI-SPEC.md (Typography Contract → 13-Style Type Ramp — the lookup-table column contract and breakpoint-baking rule)
    - .planning/phases/33-token-foundation-fonts/33-CONTEXT.md (D-05 composite classes, D-06 names mirror Figma kebab-cased with wl- prefix, D-07 breakpoints baked at 390/768/1440/1920, D-08 body default)
  </read_first>
  <action>
    Using main-session Figma MCP, call `get_design_context` on Components page node `36:5`. Add a
    `## Type Ramp` section to `33-FIGMA-EXTRACTION.md` with a table of exactly 13 rows and columns:
    `Figma style name | .wl-class (kebab, wl- prefix) | font-family (--font-wl-heading or --font-wl-body) | size@390 | size@768 | size@1440 | size@1920 | weight | line-height | letter-spacing | italic (y/n) | opsz`.
    Derive each `.wl-class` name by kebab-casing the Figma style name and prefixing `wl-` (D-06).
    Map each style's font to `--font-wl-heading` (Fraunces) or `--font-wl-body` (Hanken Grotesk)
    based on the Figma family. Record the size at each of the four Figma viewport widths
    (390/768/1440/1920) — these are the baked breakpoint steps (D-07); if Figma only specs a size at
    one or two widths, record only the specced widths and mark the others "not specced" (do NOT
    interpolate — no clamp, no invented intermediate values). Record weight, line-height,
    letter-spacing, the italic flag, and the opsz value (or "auto" where the Figma style uses optical
    default). Also note which single style is the base body style that D-08 maps onto `body` in
    `@layer base` (the Hanken Grotesk body/regular style).
  </action>
  <verify>The `## Type Ramp` section has exactly 13 rows; every row has a non-empty `.wl-class` name and a font-family token; one row is flagged as the D-08 base body style.</verify>
  <acceptance_criteria>
    - `33-FIGMA-EXTRACTION.md` contains a `## Type Ramp` heading with a 13-row table.
    - Every `.wl-class` value starts with `wl-` and is kebab-cased (no spaces, no capitals).
    - Every font-family cell is exactly `--font-wl-heading` or `--font-wl-body`.
    - Exactly one row is annotated as the base body style for D-08 (`@layer base body`).
    - No size cell contains an interpolated/clamp value; unspecced widths are literally "not specced".
  </acceptance_criteria>
  <done>All 13 type styles are recorded with class name, font token, per-breakpoint sizes, and full type metrics; the body-default style is identified.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Extract waveform SVG, OG tagline, and OG-frame check</name>
  <files>.planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md</files>
  <read_first>
    - .planning/phases/33-token-foundation-fonts/33-UI-SPEC.md (Brand Asset Contract — WaveMark, Favicon, OG Image sections)
    - .planning/phases/33-token-foundation-fonts/33-CONTEXT.md (D-13 inline SVG mark, D-15 OG composition, D-16 SVG extraction MCP-first manual-fallback)
  </read_first>
  <action>
    Using main-session Figma MCP: (a) call `download_assets` (or `get_design_context`) on the
    waveform mark node to obtain clean SVG path geometry. Add a `## Waveform Mark SVG` section to the
    artifact containing the raw `<path .../>` (or `<line .../>`) elements and the source viewBox. Per
    D-16, if the MCP output is not clean vector data, do NOT re-author geometry by eye — instead add a
    note "MANUAL SVG NEEDED" and pause to ask Joel for a "Copy as SVG" from Figma desktop, then paste
    that in. (b) Add a `## OG Tagline` section with the tagline copy captured VERBATIM from the Figma
    mockup (D-15, CONT-02 discipline — never invent). (c) Call `get_design_context` on the file to
    check whether a 1200x630 OG frame already exists; record the result in a `## OG Frame Check`
    section as either the frame node id (if found) or "No 1200x630 OG frame — compose from brand
    elements (D-15)". Do NOT wire any asset into code in this plan — extraction only.
  </action>
  <verify>The artifact has `## Waveform Mark SVG` with at least one `path` or `line` element and a viewBox, `## OG Tagline` with non-empty verbatim copy, and `## OG Frame Check` with a resolution.</verify>
  <acceptance_criteria>
    - `33-FIGMA-EXTRACTION.md` contains `## Waveform Mark SVG`, `## OG Tagline`, and `## OG Frame Check` headings.
    - `grep -c "path d=\|line x" 33-FIGMA-EXTRACTION.md` returns >= 1 (real geometry present) OR the section explicitly states "MANUAL SVG NEEDED" pending Joel.
    - The `## OG Tagline` section contains a non-empty line of copy with a note that it is verbatim from Figma.
    - The `## OG Frame Check` section states either a frame node id or "No 1200x630 OG frame".
  </acceptance_criteria>
  <done>Waveform SVG geometry, verbatim OG tagline, and OG-frame status are all captured in the artifact ready for downstream consumption.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/33-token-foundation-fonts/33-FIGMA-EXTRACTION.md` (artifact exists)
- Artifact contains all five required sections: Palette Token Mapping, Type Ramp, Waveform Mark SVG, OG Tagline, OG Frame Check.
- `grep -c "EXTRACT FROM FIGMA\|\[FIGMA VALUE\]" 33-FIGMA-EXTRACTION.md` returns 0 (no unresolved placeholders; MANUAL-SVG-NEEDED note is acceptable only for the waveform section pending Joel).
- FIDELITY-GAPS section present (may be empty/none) documenting any token with no dark evidence.
</verification>

<success_criteria>
The committed `33-FIGMA-EXTRACTION.md` is a complete, Figma-traced contract: 8 palette tokens
(light + dark disposition), 13 type-ramp styles, waveform SVG geometry, OG tagline, OG-frame
check. Every downstream plan (33-02..33-06) can read literal values from it without any Figma
access. No value is invented; gaps are flagged, not filled.
</success_criteria>

<output>
After completion, create `.planning/phases/33-token-foundation-fonts/33-01-SUMMARY.md`
noting: the extracted values source (variables vs frame 117:103), any FIDELITY-GAPS raised,
and whether the waveform SVG came from MCP or a manual Copy-as-SVG.
</output>
