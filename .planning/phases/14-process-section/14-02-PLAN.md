---
phase: 14-process-section
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/Process.astro
autonomous: true

must_haves:
  truths:
    - "Each process step shows 1-2 sentence description explaining what happens"
    - "Each process step displays its corresponding illustration"
    - "Illustrations are 64x64px on desktop, 48x48px on mobile"
    - "Illustrations positioned consistently across all 5 steps"
    - "Process section maintains vertical timeline visual pattern"
    - "Page still passes existing accessibility tests"
  artifacts:
    - path: "src/components/Process.astro"
      provides: "Updated process section with illustrations and descriptions"
      contains: "ProcessDiscovery"
      min_lines: 100
  key_links:
    - from: "src/components/Process.astro"
      to: "src/components/illustrations/*.svg"
      via: "import statements"
      pattern: "import Process.* from"
    - from: "src/components/Process.astro"
      to: "iso-shadow utility"
      via: "class attribute on illustration wrappers"
      pattern: "iso-shadow"
---

<objective>
Update Process.astro component with detailed descriptions and isometric illustrations for each step.

Purpose: Transform the process section from a simple timeline into an engaging, visual explanation of the workflow that builds trust and sets expectations.

Output: Enhanced Process.astro with 5 illustrated steps, each with user-focused descriptions and responsive illustration sizing.
</objective>

<execution_context>
@/Users/joel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-process-section/14-RESEARCH.md
@.planning/phases/14-process-section/14-01-SUMMARY.md

# Current component and utilities
@src/components/Process.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Process.astro with illustrations and descriptions</name>
  <files>src/components/Process.astro</files>
  <action>
Enhance the Process component to include isometric illustrations and detailed descriptions.

**Import illustrations:**
```astro
---
import ProcessDiscovery from './illustrations/ProcessDiscovery.svg';
import ProcessPrototype from './illustrations/ProcessPrototype.svg';
import ProcessProposal from './illustrations/ProcessProposal.svg';
import ProcessBuild from './illustrations/ProcessBuild.svg';
import ProcessHandover from './illustrations/ProcessHandover.svg';
---
```

**Updated step descriptions (user-focused, 1-2 sentences each per PROC-01):**

1. **Discovery:**
   "We start by understanding your specific workflow challenges and business goals through a collaborative discovery session."

2. **Prototype:**
   "You'll see a working demo of the solution before committing, so you can validate the approach and provide feedback early."

3. **Proposal:**
   "We outline the project scope, timeline, and investment clearly so you can make an informed decision with no surprises."

4. **Build:**
   "Your solution takes shape with regular updates keeping you in the loop throughout the development process."

5. **Handover:**
   "We deploy your solution and provide training so you feel confident using it from day one."

**Layout structure for each step:**
```astro
<article class="relative pl-16 md:pl-20 pb-8 border-l-[4px] border-turquoise">
  <!-- Step number badge - keep existing -->
  <div class="absolute left-0 top-0 -ml-4.5 w-8 h-8 rounded-full bg-turquoise flex items-center justify-center border-[3px] border-text-light dark:border-text-dark">
    <span class="font-heading text-lg font-bold text-bg-light">1</span>
  </div>

  <!-- Illustration - NEW -->
  <div class="absolute left-8 md:left-10 top-0 text-yellow iso-shadow-sm rounded">
    <ProcessDiscovery class="w-12 h-12 md:w-16 md:h-16" />
  </div>

  <!-- Step heading -->
  <h3 class="font-heading text-2xl font-semibold text-text-light dark:text-text-dark mb-3">
    Discovery
  </h3>

  <!-- Step description - NEW (replaces You:/Joel: boxes) -->
  <p class="text-text-muted-light dark:text-text-muted-dark leading-relaxed max-w-xl">
    We start by understanding your specific workflow challenges and business goals through a collaborative discovery session.
  </p>
</article>
```

**Key changes from current design:**
1. **Remove** the You:/Joel: dual-perspective boxes (too verbose)
2. **Add** single paragraph description (cleaner, scannable)
3. **Add** illustration positioned to the left of heading
4. **Increase** left padding to accommodate illustration (pl-16 md:pl-20)
5. **Apply** iso-shadow-sm to illustration wrapper (smaller shadow for 64px elements)
6. **Set** max-w-xl on description to prevent overly long lines

**Responsive illustration sizing:**
- Mobile (default): `w-12 h-12` (48x48px) - Tailwind class on SVG component
- Desktop (md:): `w-16 h-16` (64x64px)

**Color assignments per step (for visual variety):**
- Discovery: text-yellow
- Prototype: text-turquoise
- Proposal: text-magenta
- Build: text-yellow
- Handover: text-turquoise

**Maintain existing structure:**
- Section ID (`id="process"`)
- ARIA labelling (`aria-labelledby="process-heading"`)
- Heading text ("How We Work Together")
- Vertical timeline with turquoise border
- Container and max-width
  </action>
  <verify>
1. `npm run build` succeeds without errors
2. `npm run dev` - homepage shows updated process section
3. All 5 illustrations display in correct positions
4. Illustrations resize responsively (48px mobile, 64px desktop)
5. Descriptions are visible and readable
6. Timeline visual pattern maintained (turquoise vertical line)
7. Dark mode shows glow effect on illustrations
  </verify>
  <done>
Process.astro updated with:
- 5 imported SVG illustrations
- 5 user-focused 1-2 sentence descriptions
- Responsive illustration sizing (48px mobile, 64px desktop)
- iso-shadow-sm applied for subtle depth
- Simplified layout (removed You:/Joel: boxes)
- Color variety across steps
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify LCP impact and mobile recognition</name>
  <files>None (verification only)</files>
  <action>
Measure performance impact and verify mobile usability.

**LCP Baseline Check:**
```bash
# Run Lighthouse on built site
npm run build
npx lighthouse http://localhost:4321 --only-categories=performance --output=json --output-path=./lighthouse-before.json

# After viewing the Process section scrolled into view
```

Or use Chrome DevTools:
1. Open homepage in Chrome
2. Open DevTools > Lighthouse tab
3. Run Performance audit
4. Note LCP value (should be <2.5s, ideally <1.2s)

**Expected impact:**
- 5 SVGs at ~300 bytes each = ~1.5KB total inline
- Should have negligible LCP impact (<50ms increase)
- If LCP increase >200ms, flag for optimization in SUMMARY

**Mobile Recognition Test:**
1. Open Chrome DevTools
2. Toggle device toolbar (Ctrl+Shift+M)
3. Select iPhone SE or 375px width viewport
4. Scroll to Process section
5. Verify each illustration is recognizable at 48x48px
6. If not recognizable, note which illustrations need simplification

**Accessibility Check:**
```bash
npm run test:a11y  # If available
```

Or manually:
1. Use Tab key to navigate through section
2. Verify focus states visible
3. Check illustrations have aria-hidden="true" (decorative)
4. Ensure descriptions are readable by screen readers
  </action>
  <verify>
1. LCP impact measured (document in SUMMARY if >200ms increase)
2. All 5 illustrations recognizable at 375px viewport width
3. Accessibility tests pass (if available) or manual check confirms no regressions
4. No Lighthouse performance score regression
  </verify>
  <done>
Performance and accessibility verified:
- LCP impact documented
- Mobile illustrations confirmed recognizable at 48x48px
- Accessibility maintained (aria-hidden on decorative illustrations)
- No performance regression
  </done>
</task>

</tasks>

<verification>
**Build verification:**
- [ ] `npm run build` completes without errors
- [ ] No TypeScript or import errors

**Visual verification (homepage):**
- [ ] All 5 process steps display with illustrations
- [ ] Illustrations positioned consistently (left of heading)
- [ ] Descriptions visible and readable
- [ ] Timeline pattern maintained (turquoise vertical line)
- [ ] Color variety across steps (yellow, turquoise, magenta)
- [ ] Dark mode shows glow effects on illustrations

**Responsive verification:**
- [ ] Mobile (375px): Illustrations 48x48px, descriptions stack properly
- [ ] Desktop (1024px+): Illustrations 64x64px, layout spacious

**Performance verification:**
- [ ] LCP increase <200ms from baseline (or documented in SUMMARY if exceeded)
- [ ] Total inline SVG size <20KB (5 illustrations)
- [ ] No new Lighthouse warnings

**Accessibility verification:**
- [ ] Section has aria-labelledby="process-heading"
- [ ] Illustrations have aria-hidden="true"
- [ ] Descriptions are readable (contrast, font size)
- [ ] Keyboard navigation works (Tab through section)
</verification>

<success_criteria>
1. Process.astro displays all 5 illustrated steps on homepage
2. Each step has 1-2 sentence user-focused description
3. Illustrations responsive (48px mobile, 64px desktop)
4. iso-shadow utility applied to illustrations
5. Timeline visual pattern preserved
6. LCP increase <200ms from baseline
7. Illustrations recognizable on mobile (375px viewport)
8. Build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-process-section/14-02-SUMMARY.md`
</output>
