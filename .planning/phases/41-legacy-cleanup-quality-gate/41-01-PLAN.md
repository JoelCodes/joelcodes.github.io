---
phase: 41-legacy-cleanup-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/component-demo.astro
  - src/pages/test-isometric.astro
  - src/pages/design-system.astro
  - src/pages/design-system.json.ts
  - src/components/ui/
  - src/components/illustrations/
  - src/components/design-system/
  - src/components/About.astro
  - src/components/FAQ.astro
  - src/components/Hero.astro
  - src/components/Process.astro
  - src/components/ProjectCard.astro
  - src/components/BlogCard.astro
  - src/components/layout/Header.astro
  - src/components/layout/MobileNav.astro
  - src/components/layout/Footer.astro
autonomous: true
requirements: [CLEAN-01]

threat_model:
  - threat: "Deleting a file a shipped page/layout still imports, breaking the build or removing a live surface"
    severity: low
    mitigation: "Orphan-before-delete: re-verify zero live importers with grep at execution time BEFORE each rm; delete pages before the components they import; run npm run build after each deletion group"

must_haves:
  truths:
    - "No neobrutalist demo/design-system routes exist in the built site"
    - "All orphaned v1/v2 root components are gone from src/components"
    - "The build succeeds with zero import-resolution errors after deletion"
  artifacts: []
  key_links:
    - from: "src/pages (live routes)"
      to: "src/components/wl/*"
      via: "live pages import only wl/* — root duplicates removed"
      pattern: "components/wl/"
---

<objective>
Delete every verified-orphaned neobrutalist v1/v2 artifact from the codebase. This is pure subtraction — no live page or layout imports any of these files; each is imported (if at all) only by another file in this same deletion set.

Purpose: Clears the way for CLEAN-02 (global.css purge) — while these files remain they carry old-token references (`var(--color-yellow)`, `shadow-neo-*`, `iso-*`) that keep the CLEAN-02 grep non-zero.
Output: A leaner src/ tree containing only the live `wl/*`-based surfaces; `npm run build` green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-CONTEXT.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned demo/design-system pages</name>
  <read_first>
    - src/pages/component-demo.astro
    - src/pages/test-isometric.astro
    - src/pages/design-system.astro
    - src/pages/design-system.json.ts
    - 41-RESEARCH.md §"Deletion Order (topological)" — pages first
  </read_first>
  <action>
    Delete the four orphaned page/route files FIRST (they import the orphaned components, so removing them first prevents intermediate import-resolution errors): `src/pages/component-demo.astro`, `src/pages/test-isometric.astro`, `src/pages/design-system.astro`, `src/pages/design-system.json.ts`.

    Before deleting, re-verify each has no live importer with `grep -rn "component-demo\|test-isometric\|design-system" src/pages src/layouts src/components/wl src/components/layout/Site*` — expect no shipped-route hit (pages are leaf routes, not imported). Then `rm` each. Do NOT touch `src/components/design-system/` in this task (that is the component folder, handled in Task 2).

    Run `npm run build` and confirm exit 0 with no `[ERROR]` / "failed to resolve import" lines.
  </action>
  <acceptance_criteria>
    - `ls src/pages/component-demo.astro src/pages/test-isometric.astro src/pages/design-system.astro src/pages/design-system.json.ts` returns "No such file" for all four
    - `npm run build` exits 0 with zero import-resolution errors
    - `ls dist/ | grep -E "design-system|component-demo|test-isometric"` returns empty
  </acceptance_criteria>
  <done>The three retired demo routes and the design-system JSON endpoint are gone; build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete orphaned root components + illustration/design-system dirs</name>
  <read_first>
    - src/components/ui/ (Badge, Button, Card, CheckboxGroup, Input)
    - src/components/illustrations/ (8 SVGs)
    - src/components/design-system/ (CodeBlock, ComponentShowcase, DesignSystemNav, TokenSwatch)
    - src/components/{About,FAQ,Hero,Process,ProjectCard,BlogCard}.astro
    - src/components/layout/{Header,MobileNav,Footer}.astro
    - 41-RESEARCH.md §"Deletion Target Verification (D-01)" — orphan evidence table + KEEP list
  </read_first>
  <action>
    Now that the only importers (the pages deleted in Task 1) are gone, delete the orphaned components. For EACH target, re-verify zero live importers first, then `rm`.

    Orphan re-verification (must return no live-page/layout/wl hit before deleting): `grep -rn "components/ui/\|components/illustrations/\|components/design-system/\|components/About\|components/FAQ'\|components/Hero\|components/Process\|components/ProjectCard'\|components/BlogCard'\|layout/Header'\|layout/MobileNav\|layout/Footer'" src/pages src/layouts src/components/wl src/components/layout/Site*`.

    Delete directories: `src/components/ui/`, `src/components/illustrations/`, `src/components/design-system/`. Delete root components: `src/components/About.astro`, `src/components/FAQ.astro`, `src/components/Hero.astro`, `src/components/Process.astro`, `src/components/ProjectCard.astro`, `src/components/BlogCard.astro`. Delete orphaned layout components: `src/components/layout/Header.astro`, `src/components/layout/MobileNav.astro`, `src/components/layout/Footer.astro`.

    DO NOT delete (live — confirm still present after): `src/components/WaveMark.astro`, `src/components/SEO.astro`, `src/components/layout/SiteHeader.astro`, `src/components/layout/SiteFooter.astro`, `src/components/TableOfContents.astro` (stale but token-free, out of D-01 scope), and all `src/components/wl/*`.

    Run `npm run build` again; confirm exit 0.
  </action>
  <acceptance_criteria>
    - `ls src/components/ui src/components/illustrations src/components/design-system` returns "No such file" for all three directories
    - `ls src/components/About.astro src/components/FAQ.astro src/components/Hero.astro src/components/Process.astro src/components/ProjectCard.astro src/components/BlogCard.astro` returns "No such file" for all six
    - `ls src/components/layout/Header.astro src/components/layout/MobileNav.astro src/components/layout/Footer.astro` returns "No such file" for all three
    - `ls src/components/layout/SiteHeader.astro src/components/layout/SiteFooter.astro src/components/WaveMark.astro src/components/SEO.astro` all still exist
    - `npm run build` exits 0 with zero import-resolution errors
  </acceptance_criteria>
  <done>All verified-orphaned neobrutalist components and directories are deleted; live wl/*, SiteHeader/SiteFooter, WaveMark, SEO remain; build green.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0, no `[ERROR]` or "failed to resolve import" lines
- `ls dist/ | grep -E "design-system|component-demo|test-isometric"` returns empty
- Live routes still build: `dist/index.html`, `dist/showcase/index.html`, `dist/blog/` (dev-context), `dist/404.html`
</verification>

<success_criteria>
CLEAN-01 satisfied: every retired neobrutalist surface (demo pages, design-system route + JSON endpoint, ui/, illustrations/, design-system/ components, orphaned root + layout components) is deleted, and `npm run build` succeeds with zero import errors.
</success_criteria>

<output>
After completion, create `.planning/phases/41-legacy-cleanup-quality-gate/41-01-SUMMARY.md`
</output>
