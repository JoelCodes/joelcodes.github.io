---
phase: 41-legacy-cleanup-quality-gate
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [CLEAN-02]

threat_model:
  - threat: "Purging a token or utility that a live wl/ component or page transitively depends on, breaking styling with no build error (Tailwind silently omits unknown utilities)"
    severity: low
    mitigation: "Read global.css in full against the RESEARCH keep/cut map; delete only the mapped ranges; NEVER delete line 222 (@custom-variant dark); update BaseLayout body utilities in the same commit; verify with build + CLEAN-02 grep == 0 + downstream visual gate"

must_haves:
  truths:
    - "Only --wl-* tokens (plus genuinely-shared base) remain in global.css"
    - "The site renders with the v3.0 palette (paper/ink) on <body> in both themes"
    - "The build succeeds and dark: variants still work site-wide"
  artifacts:
    - path: "src/styles/global.css"
      provides: "v3.0 --wl-* token system + base + wl type ramp/chrome only"
      contains: "@custom-variant dark"
  key_links:
    - from: "src/layouts/BaseLayout.astro <body>"
      to: "--wl-paper / --wl-ink tokens"
      via: "bg-wl-paper / text-wl-ink utilities"
      pattern: "bg-wl-paper"
    - from: "global.css"
      to: "Tailwind 4 dark: variant"
      via: "@custom-variant dark line preserved"
      pattern: "@custom-variant dark"
---

<objective>
Strip the old neobrutalist token/utility system out of `src/styles/global.css` so only the `--wl-*` system plus genuinely-shared base survives, and update the `<body>` class in `BaseLayout.astro` to use `--wl-*` utilities (the old `bg-bg-light`/`text-text-light` utilities lose their definitions once the old `@theme` block is deleted).

Purpose: Finalizes the `--wl-*` namespace as the single token system (CLEAN-02) and makes the CLEAN-02 grep return zero.
Output: A purged `global.css`, an updated BaseLayout body, `npm run build` green, and the CLEAN-02 grep returning zero.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-CONTEXT.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge old neobrutalist blocks from global.css</name>
  <read_first>
    - src/styles/global.css (read IN FULL — line numbers shift as you delete; re-map before each cut)
    - 41-RESEARCH.md §"global.css Purge Map (D-02)" — the verified keep/cut section-map table
    - 41-RESEARCH.md §"Common Pitfalls" — Pitfall 1 (line 222) and Pitfall 3
  </read_first>
  <action>
    Delete only the mapped OLD blocks, working from the bottom up so earlier line numbers stay valid:

    - The `.prose` block (old blog styling, ~lines 497–651) — live blog uses `.wl-prose`, not `.prose`.
    - The `.toc` block (~lines 460–496) — TableOfContents has zero live importers; `.toc` is unused.
    - The `.project-card` / `.blog-card` animation classes + `@keyframes fadeInScale` (~lines 427–458) — only the deleted root BlogCard used `.blog-card`; wl/ProjectCard and wl/BlogCard use their own scoped classes.
    - The old neobrutalist `@layer utilities` block (`.text-yellow-text`, `.shadow-neo-*`, `.iso-*`, ~lines 281–425).
    - The old neobrutalist `@theme` block (`--color-yellow*`, `--color-turquoise*`, `--color-magenta*`, `--color-bg-*`, `--color-text-*`, `--border-neo*`, `--spacing-neo*`, `--font-heading`, old `--font-body`, `--font-weight-*`, `--text-*`, `--leading-*`, ~lines 150–219).

    MANDATORY KEEP — do NOT delete: line 222 `@custom-variant dark (&:where(.dark, .dark *));` (deleting it breaks every `dark:` utility site-wide). Also keep: the `@import` statements; the `--wl-*` `@theme` block; the `.dark` WL flip blocks; the `:root` `--wl-footer/cta/breadcrumb/proof/portrait/card` block; `@layer base { body { font-family: var(--font-wl-body) } }`; `html { scroll-behavior }`; `details::details-content` animation + `prefers-reduced-motion`; `section[id] { scroll-margin-top }`; the `.wl-prose` block; and the entire WL TYPE RAMP + WL CHROME `@layer utilities` at the bottom.

    Verify the cut against the keep/cut map by content (block names), not raw line numbers, since numbers shift during deletion.
  </action>
  <acceptance_criteria>
    - `grep -c "@custom-variant dark" src/styles/global.css` returns 1 (line preserved)
    - `grep -n "shadow-neo\|iso-shadow\|iso-glow\|iso-rotate\|--border-neo\|--font-heading\|--color-yellow\|--color-turquoise\|--color-magenta\|\.blog-card\|\.project-card\|\.toc\b\|\.prose\b" src/styles/global.css` returns zero matches
    - `grep -n "\-\-wl-\|wl-prose\|wl-heading\|wl-display-hero\|wl-nav-link" src/styles/global.css` still returns matches (v3.0 system intact)
  </acceptance_criteria>
  <done>global.css contains only the --wl-* system, the mandatory dark variant line, shared base/animation rules, and the WL type ramp/chrome — all old neobrutalist tokens and utilities removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update BaseLayout body to --wl-* utilities + verify CLEAN-02 gate</name>
  <read_first>
    - src/layouts/BaseLayout.astro (the `<body class="...">` line, ~line 80)
    - 41-RESEARCH.md §"CRITICAL: BaseLayout body must be updated simultaneously" — old→new utility mapping
  </read_first>
  <action>
    Update the `<body>` class in `src/layouts/BaseLayout.astro`. Replace the old neobrutalist utilities `font-body bg-bg-light dark:bg-bg-dark text-text-light dark:text-text-dark` with the v3.0 equivalents: drop `font-body` (the `@layer base` rule already sets `--font-wl-body` on `body`), use `bg-wl-paper dark:bg-wl-paper` and `text-wl-ink dark:text-wl-ink`. Keep the remaining utilities `min-h-screen flex flex-col` unchanged. (`--color-wl-paper` and `--color-wl-ink` both flip in `.dark`, so the `dark:` duplicates are for explicitness and are harmless; keeping the non-dark form is sufficient — match RESEARCH mapping.)

    Then run the CLEAN-02 verification gates. Run `npm run build` and confirm exit 0. Run the CLEAN-02 grep and the supplemental BaseLayout grep, both must return zero.
  </action>
  <acceptance_criteria>
    - `grep -n "font-body\|bg-bg-light\|bg-bg-dark\|text-text-light\|text-text-dark" src/` returns zero matches (supplemental gate — BaseLayout updated)
    - `grep -r "var(--color-yellow\|var(--font-heading\|var(--border-neo\|bg-yellow\|bg-turquoise\|shadow-neo\|iso-shadow" src/` returns zero matches (CLEAN-02 SC grep)
    - `grep -n "bg-wl-paper" src/layouts/BaseLayout.astro` returns a match on the `<body>` line
    - `npm run build` exits 0 with zero errors
  </acceptance_criteria>
  <done>BaseLayout body uses --wl-* utilities; both CLEAN-02 greps return zero; build is green. Any residual visual regression is caught downstream by the QUAL-03 visual gate.</done>
</task>

</tasks>

<verification>
- `grep -r "var(--color-yellow\|var(--font-heading\|var(--border-neo\|bg-yellow\|bg-turquoise\|shadow-neo\|iso-shadow" src/` == 0
- `grep -n "font-body\|bg-bg-light\|bg-bg-dark\|text-text-light\|text-text-dark" src/` == 0
- `grep -c "@custom-variant dark" src/styles/global.css` == 1
- `npm run build` exits 0
</verification>

<success_criteria>
CLEAN-02 satisfied: old neobrutalist tokens purged from global.css (only --wl-* remain), BaseLayout body migrated to --wl-* utilities, both verification greps return zero, and the build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/41-legacy-cleanup-quality-gate/41-02-SUMMARY.md`
</output>
