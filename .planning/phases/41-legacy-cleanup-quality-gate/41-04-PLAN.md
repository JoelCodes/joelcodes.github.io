---
phase: 41-legacy-cleanup-quality-gate
plan: 04
type: execute
wave: 3
depends_on: ["41-01", "41-02"]
files_modified:
  - src/pages/services/web.astro
  - src/pages/areas/abbotsford.astro
  - lighthouserc.json
  - lighthouserc-mobile.json
autonomous: true
requirements: [QUAL-01, QUAL-02]

threat_model:
  - threat: "A dev-only page (services/web, areas/abbotsford) rendering in production without a prod guard, exposing an unfinished/noindex surface as a real conversion path"
    severity: low
    mitigation: "Add the same import.meta.env.PROD redirect guard showcase.astro uses to both pages, matching the established pattern; verify prod build no longer emits their HTML"
  - threat: "Lighthouse CI scoring a redirect destination instead of a real page (stale /blog/im-pivoting/ entry that redirects to / in prod)"
    severity: low
    mitigation: "Scope the Lighthouse URL set to the only real prod pages after the guard changes: / and /404"

must_haves:
  truths:
    - "In a production build, /services/web and /areas/abbotsford redirect to / (dev-only, like showcase and blog)"
    - "axe-core reports zero violations across all rendered dev pages in both light and dark themes"
    - "The Lighthouse CI URL set contains exactly the real prod pages (/ and /404)"
  artifacts:
    - path: "lighthouserc.json"
      provides: "Desktop Lighthouse URL set scoped to real prod pages"
      contains: "/404"
  key_links:
    - from: "src/pages/services/web.astro"
      to: "prod redirect to /"
      via: "if (import.meta.env.PROD) return Astro.redirect('/')"
      pattern: "import.meta.env.PROD"
    - from: "src/pages/areas/abbotsford.astro"
      to: "prod redirect to /"
      via: "if (import.meta.env.PROD) return Astro.redirect('/')"
      pattern: "import.meta.env.PROD"
---

<objective>
Add the production redirect guard to the two remaining dev-only pages (`services/web`, `areas/abbotsford`) so they match `showcase` and `blog` (D-06), scope the Lighthouse CI URL set to the only real prod pages that remain (`/` and `/404`), and run the axe-core gate (QUAL-01) across all rendered dev pages in both themes.

Purpose: This is the autonomous portion of the quality gate. The guard-add is the phase's ONE additive edit and it must precede the Lighthouse URL-scope change (QUAL-02). axe (QUAL-01) runs against the dev server where all pages render.
Output: Both dev-only pages guarded, both lighthouserc files scoped to `/` + `/404`, axe green (zero violations) across all pages/themes, and a build verified clean.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-CONTEXT.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-RESEARCH.md
@src/pages/showcase.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROD guard to services/web + areas/abbotsford; scope Lighthouse URLs</name>
  <read_first>
    - src/pages/showcase.astro (the PROD redirect guard pattern at lines 41–42: `if (import.meta.env.PROD) { return Astro.redirect('/'); }`)
    - src/pages/services/web.astro (frontmatter — where to insert the guard)
    - src/pages/areas/abbotsford.astro (frontmatter — where to insert the guard)
    - lighthouserc.json and lighthouserc-mobile.json (current URL lists carry stale /blog/im-pivoting/)
    - 41-CONTEXT.md D-06 and 41-RESEARCH.md §"Lighthouse CI (QUAL-02)"
  </read_first>
  <action>
    In `src/pages/services/web.astro` and `src/pages/areas/abbotsford.astro`, add the same production guard `showcase.astro` uses in its frontmatter: an early `if (import.meta.env.PROD) { return Astro.redirect('/'); }`. Place it in the component frontmatter before any content rendering, matching showcase.astro's position/form exactly. This is the phase's single additive edit.

    Then update BOTH `lighthouserc.json` and `lighthouserc-mobile.json`: replace the stale `http://localhost/blog/im-pivoting/` entry so the `url` array is exactly `http://localhost/` and `http://localhost/404` (the only real prod pages after the guard changes — showcase/blog/services/areas all redirect to / in prod). Update the `_comment` field to reflect the real prod URL set (drop the "showcase activates in Phase 38" note). Keep all other config (staticDistDir, assertions/thresholds) unchanged.

    Verify with a production build that the guarded pages no longer emit standalone HTML.
  </action>
  <acceptance_criteria>
    - `grep -n "import.meta.env.PROD" src/pages/services/web.astro src/pages/areas/abbotsford.astro` returns a match in each file
    - `grep -c "im-pivoting" lighthouserc.json lighthouserc-mobile.json` returns 0 for both
    - `grep -c "/404" lighthouserc.json lighthouserc-mobile.json` returns >= 1 for both, and each `url` array contains exactly `/` and `/404`
    - `npm run build` exits 0; `ls dist/services/web/index.html dist/areas/abbotsford/index.html 2>&1` shows they are absent (or their content is a redirect stub, matching showcase's prod behavior)
  </acceptance_criteria>
  <done>Both dev-only pages redirect to / in prod; both lighthouserc files test exactly / and /404; prod build no longer ships services/areas content.</done>
</task>

<task type="auto">
  <name>Task 2: Run axe-core gate across all dev pages, both themes (QUAL-01)</name>
  <read_first>
    - tests/accessibility/ (all spec files: landing, showcase, blog, 404, services-web, areas-abbotsford)
    - 41-RESEARCH.md §"axe-Core Harness (QUAL-01)" and §"Validation Architecture > QUAL-01"
    - package.json (test:a11y script → `playwright test tests/accessibility`)
  </read_first>
  <action>
    Run the full axe suite against the dev server: `npm run test:a11y` (Playwright starts `npm run dev` per playwright.config.ts; all pages are reachable in dev). This covers landing, showcase, blog index, blog post, 404, services/web, areas/abbotsford — each in light and dark (dark via `colorScheme: 'dark'` browser context driving the FOUC `.dark` class).

    Confirm exit code 0 and zero violations. If ANY spec reports a violations array with items, DO NOT auto-fix — capture the failing page/theme/rule and report it as a blocker for Joel (per D-03: axe failures block-and-report, no silent auto-fix). Record the pass/fail result in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `npm run test:a11y` exits 0 with zero failed tests
    - All 14 page×theme accessibility assertions pass (landing, showcase, blog index, blog post, 404, services/web, areas/abbotsford — light + dark)
    - No axe violations array contains items; any failure is reported (not auto-fixed) in the SUMMARY
  </acceptance_criteria>
  <done>axe-core reports zero violations across every rendered dev page in both themes; QUAL-01 gate green (or, on failure, a block-and-report entry recorded for Joel).</done>
</task>

</tasks>

<verification>
- `grep -n "import.meta.env.PROD" src/pages/services/web.astro src/pages/areas/abbotsford.astro` — match in each
- Both lighthouserc files: `url` arrays = exactly `/` and `/404`, no `im-pivoting`
- `npm run test:a11y` exits 0, zero violations
- `npm run build` exits 0
- QUAL-02 note: Lighthouse itself runs in CI (treosh/lighthouse-ci-action, no local lhci) on push/PR to main — the URL-scope config change is what this plan lands; the ≥90 result is observed on the CI run.
</verification>

<success_criteria>
QUAL-01 satisfied (axe zero violations, all pages, both themes) and QUAL-02 configuration landed (dev-only guards added, Lighthouse URL set scoped to the real prod pages / and /404, ready for the CI ≥90 gate).
</success_criteria>

<output>
After completion, create `.planning/phases/41-legacy-cleanup-quality-gate/41-04-SUMMARY.md`
</output>
