---
phase: 41-legacy-cleanup-quality-gate
plan: 05
type: execute
wave: 4
depends_on: ["41-01", "41-02", "41-03", "41-04"]
files_modified:
  - scripts/41-fidelity-screenshots.mjs
  - .planning/phases/41-legacy-cleanup-quality-gate/fidelity/
autonomous: false
requirements: [QUAL-03]

threat_model:
  - threat: "Marking the milestone shipped before Joel's explicit visual sign-off, hiding a token-purge visual regression"
    severity: low
    mitigation: "This task is a HARD HUMAN GATE (autonomous: false); execution HALTS and waits for Joel's explicit approval; the milestone-shipped marker is NEVER auto-set, even under --chain"

must_haves:
  truths:
    - "Rendered post-cleanup screenshots of Landing (light+dark) and Showcase exist beside the Figma frames"
    - "Joel has explicitly approved the Figma-vs-rendered comparison before the milestone is marked shipped"
  artifacts:
    - path: "scripts/41-fidelity-screenshots.mjs"
      provides: "Post-cleanup rendered screenshot capture (landing + showcase, light + dark, 1440)"
    - path: ".planning/phases/41-legacy-cleanup-quality-gate/fidelity/"
      provides: "Rendered comparison screenshots for QUAL-03"
  key_links:
    - from: "rendered fidelity screenshots"
      to: "Figma frames 12:2 / 117:103 / 12:3"
      via: "side-by-side visual comparison presented to Joel"
      pattern: "fidelity"
---

<objective>
Capture fresh post-cleanup rendered screenshots of Landing (light + dark) and Showcase, present them side-by-side with the stored Figma frames, and HALT for Joel's explicit visual-fidelity sign-off. This is the milestone-close gate (QUAL-03).

Purpose: Confirm the deletions and token purge introduced no visual regression, and secure Joel's formal approval before v3.0 is marked shipped.
Output: A fidelity screenshot set and Joel's explicit approval. The milestone-shipped marker is downstream of that approval and is never auto-set.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-CONTEXT.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-RESEARCH.md
@scripts/fidelity-screenshots.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture post-cleanup rendered fidelity screenshots</name>
  <read_first>
    - scripts/fidelity-screenshots.mjs (Phase 39 pattern: chromium.launch, colorScheme light/dark, document.fonts.ready + settleAnimations, fullPage screenshot)
    - scripts/blog-fidelity-screenshots.mjs (blog-specific variant, same pattern)
    - 41-RESEARCH.md §"Visual Fidelity Gate (QUAL-03)"
  </read_first>
  <action>
    Create `scripts/41-fidelity-screenshots.mjs` following the existing script pattern (chromium.launch from @playwright/test, per-mode browser context, `document.fonts.ready` + settle-animations wait, `page.screenshot({ fullPage: true })`). Target the dev server at `localhost:4321` (showcase is dev-only). Capture at minimum: Landing `/` at 1440 light and dark; Showcase `/showcase` at 1440 (closed state, and expanded if the script can trigger the details-open, matching the Figma expanded frame). Output PNGs to `.planning/phases/41-legacy-cleanup-quality-gate/fidelity/`.

    Start the dev server (`npm run dev`), run the script, then confirm the expected PNG files were written.
  </action>
  <acceptance_criteria>
    - `test -f scripts/41-fidelity-screenshots.mjs` succeeds
    - `.planning/phases/41-legacy-cleanup-quality-gate/fidelity/` contains rendered PNGs for landing 1440 light, landing 1440 dark, and showcase 1440 (at least closed)
    - Each PNG is non-empty (file size > 0)
  </acceptance_criteria>
  <done>Fresh post-cleanup rendered screenshots exist for Landing (light+dark) and Showcase in the phase fidelity directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: QUAL-03 milestone-close visual sign-off (HARD HUMAN GATE)</name>
  <read_first>
    - .planning/phases/41-legacy-cleanup-quality-gate/fidelity/ (the rendered PNGs from Task 1)
    - .planning/phases/37-landing-page/fidelity/37-figma-12-2.png (Landing light Figma 12:2)
    - .planning/phases/37-landing-page/fidelity/37-figma-117-103.png (Landing dark Figma 117:103)
    - .planning/phases/38-showcase-page-blog-restyle/fidelity/figma-1440-closed.png and figma-1440-expanded.png (Showcase 12:3)
  </read_first>
  <what-built>
    The full v3.0 milestone after legacy cleanup: orphaned neobrutalist files deleted (CLEAN-01), global.css purged to --wl-* (CLEAN-02), CLAUDE.md + design/ cleaned (CLEAN-03), axe zero violations + Lighthouse URL scope (QUAL-01/02). Fresh rendered screenshots of Landing (light+dark) and Showcase have been captured post-cleanup.
  </what-built>
  <how-to-verify>
    1. Open the rendered screenshots in `.planning/phases/41-legacy-cleanup-quality-gate/fidelity/` beside the Figma frames:
       - Landing light: rendered vs `.planning/phases/37-landing-page/fidelity/37-figma-12-2.png`
       - Landing dark: rendered vs `.planning/phases/37-landing-page/fidelity/37-figma-117-103.png`
       - Showcase: rendered vs `.planning/phases/38-showcase-page-blog-restyle/fidelity/figma-1440-closed.png` (+ expanded if captured)
    2. Confirm the token purge introduced no visual regression: paper/ink background, Fraunces/Hanken type, sea-cool palette, waveform mark all render correctly in both themes.
    3. Report any fidelity gap, or approve.
    Note: axe/Lighthouse failures (if any surfaced in Plan 04 / CI) are block-and-report — they are NOT auto-fixed. Any such failure must be resolved before approval.
  </how-to-verify>
  <resume-signal>Type "approved" to close the milestone, or describe the fidelity gaps / gate failures to address. The milestone-shipped marker is set ONLY after this explicit approval — never automatically, even under --chain.</resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/41-legacy-cleanup-quality-gate/fidelity/` contains the rendered Landing (light+dark) + Showcase PNGs
- Execution HALTED at the checkpoint and waited for Joel's explicit approval
- Milestone-shipped marker not set until "approved" received
</verification>

<success_criteria>
QUAL-03 satisfied: post-cleanup rendered screenshots of Landing (light + dark) and Showcase were presented beside the Figma frames (12:2 / 117:103 / 12:3), and Joel explicitly approved — the milestone-close gate.
</success_criteria>

<output>
After completion (post-approval), create `.planning/phases/41-legacy-cleanup-quality-gate/41-05-SUMMARY.md`
</output>
