---
phase: 41-legacy-cleanup-quality-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - design/ARCHIVE.md
  - design/image-import-*.png
  - design/image-import-*.jpg
autonomous: true
requirements: [CLEAN-03]

threat_model:
  - threat: "Deleting a design/ asset that a src/ import or content collection actually references"
    severity: low
    mitigation: "Verify-then-delete: confirm grep -rn 'image-import' src/ returns zero before rm; only the untracked root-level image-import-* dupes (originals live in design/images/) are removed; Crito/.fig archives are KEPT"

must_haves:
  truths:
    - "A fresh Claude session reading CLAUDE.md builds correctly on v3.0 with zero v1/v2 wrong signals"
    - "The 80 duplicate design/image-import-* files are gone; no referenced asset is deleted"
    - "The Crito/.fig archives are retained and documented as abandoned-v2"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accurate v3.0 project guidance (fonts, tokens, wl/ components, IA, structure)"
      contains: "Fraunces"
    - path: "design/ARCHIVE.md"
      provides: "Note marking Crito.pen + .fig files as abandoned-v2 archive"
      contains: "Crito"
  key_links:
    - from: "CLAUDE.md component section"
      to: "src/components/wl/*"
      via: "documents the live wl/ component set"
      pattern: "wl/"
---

<objective>
Rewrite `CLAUDE.md` for v3.0 (it is currently 100% stale v1/v2 and gives a fresh session wrong signals), and clean the `design/` folder by deleting the ~80 untracked root-level `image-import-*` duplicates while retaining and documenting the Crito/.fig archives.

Purpose: CLAUDE.md is the primary onboarding signal for any future Claude session; leaving v1/v2 content guarantees wrong builds. The design/ dupes are pure clutter.
Output: An accurate v3.0 CLAUDE.md, a `design/ARCHIVE.md` note, and the 80 duplicate images removed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-CONTEXT.md
@.planning/phases/41-legacy-cleanup-quality-gate/41-RESEARCH.md
@src/lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CLAUDE.md for v3.0</name>
  <read_first>
    - CLAUDE.md (current v1/v2 content to replace)
    - src/components/wl/ (the live component set to document: AuthorCard, BlogCard, Breadcrumb, CTAButton, Callout, Eyebrow, FAQItem, FeaturedPostCard, FrequencyWave, LinkCard, ProjectCard, ServiceCard, Step, Tag)
    - src/components/{WaveMark,SEO}.astro and src/components/layout/{SiteHeader,SiteFooter}.astro (live chrome)
    - src/lib/constants.ts (BOOKING_URL, CONTACT_EMAIL)
    - src/layouts/BaseLayout.astro (structure/dark-mode reference)
    - 41-RESEARCH.md §"CLAUDE.md Rewrite (D-04)" — the stale-claim → v3.0-target table and new-sections list
  </read_first>
  <action>
    Rewrite every stale section of CLAUDE.md to reflect v3.0. Concrete corrections:

    - Fonts: replace "Poppins/Inter" and "Bricolage Grotesque/DM Sans" with Fraunces Variable (headings) + Hanken Grotesk Variable (body) + Roboto Mono Variable (code).
    - Tokens: replace "yellow #ffef6a / teal", "iso-shadow/iso-glow/iso-rotate" with the `--wl-*` token prefix (Ink/Sub/Accent/Sea-glass/Paper/Line, light + dark). Remove all references to `/design-system` and `/design-system.json` (both deleted).
    - Components: replace the old Button/Card/Input/Badge "Design System" block with the live `wl/` set (list the 14 wl/ components + WaveMark, plus SiteHeader/SiteFooter/SEO chrome). State the design source of truth is Figma `1tg8wIPcvOVC5tPZ8pkGO2`.
    - IA: no `/faq`, no `/projects`, no `/portfolio`; blog is dev-only (prod-excluded via `import.meta.env.PROD` guard in blog/index.astro + [slug].astro); note redirects `/projects→/showcase`, `/faq→/`. Note `/services/web` and `/areas/abbotsford` are dev-only (noindex + sitemap-filtered), reachable by URL in dev only.
    - Directory structure: correct to the actual tree (showcase.astro at pages root; no /portfolio; src/data/projects.json with the v2 schema fields section/thumbLabel/story fields; src/lib/constants.ts with BOOKING_URL + CONTACT_EMAIL).
    - Dark mode: system-only via `prefers-color-scheme` setting the `.dark` class (no localStorage theme toggle).
    - Site description: "portfolio and service site with a dev-only blog", not "static portfolio/blog site".

    Do NOT use fenced code blocks containing invented file bodies; keep the directory-structure block accurate to the actual tree. Preserve accurate existing sections (build commands, git workflow, CI/CD, SEO) — correct only what is stale.
  </action>
  <acceptance_criteria>
    - `grep -c "Fraunces" CLAUDE.md` >= 1 and `grep -c "Hanken" CLAUDE.md` >= 1
    - `grep -c "wl/\|--wl-" CLAUDE.md` >= 1
    - `grep -in "Poppins\|Inter\|Bricolage\|DM Sans\|iso-shadow\|iso-glow\|#ffef6a\|design-system.json\|/portfolio\|/faq " CLAUDE.md` returns zero matches
    - CLAUDE.md documents the wl/ component set and names Figma file `1tg8wIPcvOVC5tPZ8pkGO2`
  </acceptance_criteria>
  <done>CLAUDE.md is an accurate v3.0 reference — correct fonts, --wl-* tokens, wl/ components, IA (dev-only blog + services/areas, no /faq or /projects), and directory structure, with zero v1/v2 leftovers.</done>
</task>

<task type="auto">
  <name>Task 2: Delete design/image-import-* duplicates + add archive note</name>
  <read_first>
    - design/ (root-level image-import-*.png/.jpg dupes; design/images/ tracked originals; Crito.pen + .fig files)
    - 41-RESEARCH.md §"design/ Cleanup (D-05)" — verification commands + Crito archive-note content
  </read_first>
  <action>
    Verify-then-delete. First confirm the root dupes are unreferenced: `grep -rn "image-import" src/` and `grep -rn "image-import" src/content/` must both return zero. Then delete the ~80 untracked root-level duplicates: `rm design/image-import-*.png design/image-import-*.jpg` (they are untracked `??` files with tracked originals in `design/images/`; use `rm`, not `git rm`).

    Create `design/ARCHIVE.md` noting the retained artifacts: `Crito.pen` = v2.0 design system, abandoned 2026-07-14, full record on branch `feature/phase-32-fidelity-sweep-handoff`; the `.fig` files (Alliatus / finesse-ui / Crito consulting template) = template references imported during v2.0, not in active use. Do NOT delete Crito.pen or any `.fig` file.
  </action>
  <acceptance_criteria>
    - `grep -rn "image-import" src/ src/content/` returns zero matches (referenced-asset safety check)
    - `ls design/image-import-*.png design/image-import-*.jpg 2>/dev/null | wc -l` returns 0
    - `test -f design/ARCHIVE.md && grep -c "Crito" design/ARCHIVE.md` returns >= 1
    - `test -f design/Crito.pen` still succeeds (archive retained)
  </acceptance_criteria>
  <done>The 80 duplicate root images are gone, no referenced asset was touched, design/ARCHIVE.md documents the retained Crito/.fig archives.</done>
</task>

</tasks>

<verification>
- CLAUDE.md: `grep -in "Poppins\|Inter\|Bricolage\|iso-shadow\|design-system.json\|/portfolio" CLAUDE.md` == 0; `grep -c "Fraunces" CLAUDE.md` >= 1
- design/: `ls design/image-import-* 2>/dev/null | wc -l` == 0; `test -f design/ARCHIVE.md`; `test -f design/Crito.pen`
</verification>

<success_criteria>
CLEAN-03 satisfied: CLAUDE.md accurately documents v3.0 (fonts, --wl-* tokens, wl/ components, IA, structure), the design/image-import-* duplicates are deleted with no referenced asset removed, and the Crito/.fig archives are retained and noted in design/ARCHIVE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/41-legacy-cleanup-quality-gate/41-03-SUMMARY.md`
</output>
