---
phase: 24-layout-primitives-primitive-components
plan: 24-04
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - design/Crito.pen (Pattern A — Pencil-native lucide route — preferred per RESEARCH supersession of D-25: `Primitive / Icon / 16`, `/ 20`, `/ 24`, `/ 32` size variants, each containing a `type: "icon", library: "lucide"` node with a default glyph; consumers override via descendants per Pencil guidelines § 3)
  - design/Crito.pen (Pattern B — D-25 fallback IF Pattern A probe fails: `Primitive / Icon / 16`, `/ 20`, `/ 24`, `/ 32` size-variant wrapper frames; `_Components / Primitives / Icon / glyphs / <name>` atomic-glyph subfolder per enumerated source-driven glyph list)
  - .planning/research/PEN-INVENTORY.md (## Variant Evidence (Phase 24): per Icon size variant + glyph entry rows; new subsection `## Icon Glyphs (Phase 24)` listing the enumerated ship-list with source citations; ### Open Flags — Phase 24: OPEN-24-NN for any Pencil icon-library limitations + D-28 brand-glyph deferral)
autonomous: false
requirements_addressed:
  - COMP-04
  - COMP-08
  - COMP-09
  - LAYOUT-01
must_haves:
  truths:
    - "Opening `_Components / Primitives` shows `Primitive / Icon` with 4 size variants 16/20/24/32 per COMP-04 + D-27"
    - "Icon-mechanism probe outcome (Q4) documented in 24-04-SUMMARY.md `## Icon Mechanism Probe Result`: Pattern A (Pencil-native `library: lucide`) shipped if probe succeeded — supersedes D-25 assumption — OR Pattern B (D-25 atomic-glyph fallback) shipped if probe failed"
    - "Glyph enumeration is source-driven (D-26): every shipped glyph appears in at least one IN-SCOPE Crito frame, recorded in PEN-INVENTORY.md `## Icon Glyphs (Phase 24)` with source citation (frame_id + node_id)"
    - "Brand glyphs (Instagram, Substack) NOT shipped per D-28; OPEN-25-NN flag (referencing Phase 25 Footer plan) added"
    - "Every Pencil-mutating batch is preceded by `get_editor_state` asserting active editor == `design/Crito.pen` (D-35)"
    - "If Pattern A used: literal `fill` values bound to semantic tokens via Variant Evidence rows (OPEN-23-13 dual-track)"
    - "ROADMAP line 169's stale `[x] 24-04 ... (completed 2026-05-31)` marker is corrected at plan close per CONTEXT canonical_refs note"
  artifacts:
    - path: "design/Crito.pen"
      provides: "Primitive / Icon with 4 size variants 16/20/24/32 inside _Components / Primitives"
      contains: "Pattern A: `type: icon` library:lucide nodes per size; OR Pattern B: wrapper frames + glyphs/ subfolder per D-25"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Variant Evidence rows + ## Icon Glyphs (Phase 24) ship-list + OPEN-24-NN/OPEN-25-NN forward-deferred flags"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 plan list line 169 stale marker corrected"
  key_links:
    - from: "Primitive / Icon (Pattern A or B)"
      to: "Primitive / Button iconLeading / iconTrailing slots (built in 24-02)"
      via: "Slot.slot array containing Icon component id (Pencil guidelines § 2 + RESEARCH Pattern 4)"
      pattern: "D-29 Button-owns-slots + D-25 instance-swap (Pattern B) OR descendants-override (Pattern A)"
    - from: "Primitive / Icon glyph set"
      to: "Crito IN-SCOPE frames (home, contact, blog, FAQ, 404, Thank-you)"
      via: "PEN-INVENTORY.md `## Icon Glyphs (Phase 24)` source citations"
      pattern: "D-26 source-driven enumeration"
---

<objective>
Build `Primitive / Icon` per COMP-04 + D-27 — 4 size variants `16` / `20` / `24` / `32`. **First task is an icon-mechanism probe (RESEARCH Q4 + Pitfall 3 + Pattern 6 supersession of D-25):** attempt Pencil-native `type: "icon", library: "lucide"` per Pencil guidelines § 3. If it works (Pattern A), supersede D-25 and ship that — simpler, matches Joel's v1.3 `@lucide/astro` code-side library. If it fails (Pattern B), fall back to D-25 atomic-glyph pattern with glyphs in `_Components / Primitives / Icon / glyphs/`.

**Source-driven glyph enumeration per D-26:** audit IN-SCOPE Crito frames (Home Page, Contact, Blog, FAQ, 404, Thank-you) for every glyph used in editable nodes. The audit result becomes the Phase 24 shipping set (likely 5-10 glyphs).

**Brand glyphs (Instagram, Substack) explicitly deferred per D-28** to Phase 25 Section/Footer — record as `OPEN-25-NN` flag.

**Purpose:** Establishes the icon mechanism Phase 25 Section components and Phase 27-31 page reconstructions will consume. Confirms or supersedes D-25 assumption. Ships only source-cited glyphs (no invention per D-24).

**Output:** `Primitive / Icon` size variants + glyph set inside `_Components / Primitives`, ROADMAP.md line 169 correction, SUMMARY documenting probe outcome.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md
@.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md
@.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md
@.planning/phases/24-layout-primitives-primitive-components/24-01-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
</context>

<threat_model>
**T-24-T1 (active-editor swap — HIGH severity, BLOCKING)** — same as 24-01/24-02/24-03. Mitigation: per D-35, `mcp__pencil__get_editor_state` is called as a per-task pre-flight before every `batch_design`. This plan has 3-5 mutation calls (probe, then 4 size variants per Pattern A, or 4 wrappers + N glyphs per Pattern B).

**T-24-I1 (Information disclosure — LOW)** — Probe artifacts captured via `get_screenshot` for visual confirmation. Mitigation: probe artifacts stay non-public; final shipped Icon variants live in `_Components / Primitives` (the deliverable).
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Icon-mechanism probe (RESEARCH Q4 + Pitfall 3 + Pattern 6) — pause for user on outcome</name>
  <files>design/Crito.pen (temporary: `Primitive / Icon / Probe` inside `_Components / Primitives` — deleted at task close after probe outcome confirmed)</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Pattern 6 (Instance-swap for Icon glyphs — Pencil guidelines § 3 Icons supersedes D-25), § Pitfall 3 (icon library route probe procedure), § Open Questions Q4
    - `.planning/research/PEN-INVENTORY.md` § Pencil Guidelines verbatim § 3 Icons (Libraries: lucide, feather, Material Symbols Outlined/Rounded/Sharp; Usage: `Insert(container, {type: "icon", library: "lucide", icon: "settings", width: 24, height: 24, fill: "$--foreground"})`)
    - `.planning/research/PEN-INVENTORY.md` § Schema Snapshot (node types include `icon` — but Phase 23 didn't exercise it)
    - `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` D-25 (atomic-glyph fallback — Pattern B)
  </read_first>
  <action>
    1. Re-flight per D-35.
    2. Attempt Pattern A via `mcp__pencil__batch_design`:
       - Op A: Insert into `componentsPrimitivesFrame` (from 24-01) an `icon` node named `Primitive / Icon / Probe`: `type: "icon"`, `library: "lucide"`, `icon: "chevron-right"`, `width: 24`, `height: 24`, `fill: "#141f39ff"` (literal of `color-semantic-text-primary`). Capture id as `probeIcon`.
    3. Verify via `mcp__pencil__batch_get({ ids: [probeIcon], readDepth: 2 })`. Record in SUMMARY.md `## Icon Mechanism Probe Result`:
       - Did the node insert successfully (no error)?
       - What is the returned `.type` value (`"icon"` confirms Pattern A)?
       - What property keys does the node show (does `library` survive, does `icon` survive)?
    4. Inline `mcp__pencil__get_screenshot({ nodeId: probeIcon })`. Visually confirm a chevron-right renders (not blank, not placeholder).
    5. **Pattern A vs B decision (Pitfall 3):**
       - **Pattern A works:** node returns `type: "icon"`, library + icon properties survive, screenshot shows chevron-right. SUPERSEDE D-25; ship Pattern A. Document in SUMMARY.
       - **Pattern A partial:** node inserts but icon doesn't render OR property dropped. Document failure mode + try a different lucide name (`mail`, `arrow-right`). If still fails: fall back to Pattern B.
       - **Pattern A errors:** `batch_design` rejects the payload at insert. Fall back to Pattern B (D-25 atomic-glyph).
    6. **Pause for user confirmation:** AskUserQuestion summarizing probe outcome (Pattern A vs B) + screenshot. Confirm path forward.
    7. After user approval: `mcp__pencil__batch_design({ operations: ['D(<probeIcon-id>)'] })` to delete the probe artifact. Verify deleted.
  </action>
  <verify>
    - SUMMARY.md `## Icon Mechanism Probe Result` records: payload attempted, `batch_get` outcome, screenshot outcome, Pattern A or Pattern B decision
    - User approval recorded
    - `probeIcon` deleted (verifiable by `batch_get` returning empty)
    - If Pattern A: Pencil guidelines § 3 confirmed as the canonical reference; D-25 explicitly superseded in SUMMARY
    - If Pattern B: D-25 retained as the canonical reference; SUMMARY documents what Pattern A failure mode was
  </verify>
  <acceptance_criteria>
    - Q4 resolved: icon mechanism strategy locked
    - VAL-24-06 evidence: `get_editor_state` pre-flight before batch_design call recorded
    - SUMMARY.md `## Icon Mechanism Probe Result` complete with user-approval
    - Pattern A or B locked for Task 3 build
  </acceptance_criteria>
  <done>Icon mechanism resolved; ready for glyph enumeration (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: D-26 source-driven glyph enumeration</name>
  <files>.planning/research/PEN-INVENTORY.md (NEW subsection `## Icon Glyphs (Phase 24)` enumerating ship-list with source citations); .planning/research/PEN-INVENTORY.md (### Open Flags — Phase 24: OPEN-25-NN row for D-28 brand glyph deferral to Phase 25)</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` D-26 (source-driven enumeration), D-28 (Instagram/Substack brand glyphs deferred to Phase 25)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples Plan 24-04 Step 2 (enumeration procedure), § Pattern 6 supersession note
    - `.planning/research/PEN-INVENTORY.md` § Frames (IN-SCOPE frames per scope column — Home Page, Contact, Blog, FAQ, 404, Thank-you, About Me)
    - `.planning/research/PEN-INVENTORY.md` § Tokens Written — Primitives (icon size literals: 16, 20, 24, 32 per COMP-04 + D-27)
  </read_first>
  <action>
    1. Re-flight per D-35.
    2. For each IN-SCOPE Crito frame (Home Page, Contact, Blog, FAQ, 404, Thank-you, About Me — read frame ids from PEN-INVENTORY § Frames):
       - `mcp__pencil__batch_get({ ids: ["<frame-id>"], readDepth: 3 })` (or 4 for deeper Home Page Hero structure).
       - Walk the returned tree looking for nodes with `type: "icon"` (Pencil-native) OR nodes with names containing "icon" / "arrow" / "chevron" / "mail" / "phone" / "user" / "menu" / "close" / "check" / etc.
       - For each glyph found, record: (a) the glyph name (lucide name if Pattern A, descriptive name if Pattern B), (b) the source frame id, (c) the source node id, (d) the section it appears in.
       - Limit total `batch_get` calls to ≤ 7 (one per IN-SCOPE frame).
    3. De-duplicate the glyph list. Filter out brand glyphs (Instagram, Substack, Twitter, etc.) per D-28 — these are deferred.
    4. Write a new section in PEN-INVENTORY.md: `## Icon Glyphs (Phase 24)` with a table:
       `| glyph_name | source_frame_id | source_node_id | section | shipped_in_phase_24 |`
       `|------------|-----------------|----------------|---------|---------------------|`
       Each row = one unique glyph from the source audit.
    5. Write `OPEN-25-NN` row in `### Open Flags — Phase 24 (OPEN-24-NN)` for brand glyphs deferred per D-28 (referencing OPEN-25-NN as a forward flag for Phase 25 Footer plan ownership):
       `OPEN-25-<NN> | Instagram + Substack (Joel-brand) glyphs not in Crito source; Phase 25 Section/Footer owns this gap per D-28 | consumer_phase: 25 | severity: notable | resolution: add when Section/Footer is built`
    6. Final glyph ship-list count documented in SUMMARY.md.
    7. NO Pencil mutations in this task (audit + audit-trail only).
  </action>
  <verify>
    - PEN-INVENTORY.md has new `## Icon Glyphs (Phase 24)` section with N rows (N is the de-duplicated source-driven count, likely 5-10 per D-26)
    - PEN-INVENTORY.md has `OPEN-25-NN` row for D-28 brand-glyph deferral
    - Every glyph row cites a real source frame id + node id (no "TBD")
    - Brand glyphs (Instagram, Substack) NOT in the ship-list
  </verify>
  <acceptance_criteria>
    - D-26 source-driven enumeration complete with audit trail
    - D-28 brand-glyph deferral documented via OPEN-25-NN
    - Ship-list locked before Task 3 (Pitfall 6 prevention — no glyph added in Task 3 that's not on this list)
    - VAL-24-02 prep: every glyph that will be shipped has a source-evidence row
  </acceptance_criteria>
  <done>Glyph ship-list locked; D-28 deferral documented; ready for Icon construction (Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Build `Primitive / Icon` 4 size variants per Task 1 locked Pattern</name>
  <files>design/Crito.pen (inside `_Components / Primitives`: Pattern A — 4 size-variant icon nodes `Primitive / Icon / 16`, `/ 20`, `/ 24`, `/ 32`; OR Pattern B — 4 wrapper frames + `_Components / Primitives / Icon / glyphs/<name>` atomic components per Task 2 ship-list)</files>
  <read_first>
    - 24-04-SUMMARY.md partial (Task 1 Pattern decision + Task 2 ship-list)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples Plan 24-04 Step 3 (both Pattern A and Pattern B payload shapes), § Pattern 2 (Icon row: no layout — sized wrapper around glyph or slot)
    - `.planning/research/PEN-INVENTORY.md` § Tokens Written — Primitives (literals: 16, 20, 24, 32 sizes; default fill literal `#141f39ff` for `color-semantic-text-primary`; or `$--foreground` if Pencil-native semantic per § 14 — but Phase 23 D-12 uses flat-dash so use literal)
    - 24-02-SUMMARY.md slot strategy (Pattern A's descendants override is the Icon analog of Button's slot mechanism)
  </read_first>
  <action>
    1. Re-flight per D-35.
    2. **Pattern A path (Pencil-native lucide — preferred per RESEARCH supersession):**
       - For each size in [16, 20, 24, 32]: `mcp__pencil__batch_design({ operations: ['icon<N>=I(<componentsPrimitivesFrame-id>, {type: "icon", library: "lucide", icon: "<default-glyph-from-Task2-list>", width: <N>, height: <N>, fill: "#141f39ff", name: "Primitive / Icon / <N>"})'] })`. Pick `chevron-right` or whatever glyph is on Task 2's ship-list as the default visible content.
       - Consumers override the icon per Pencil guidelines § 3 `descendants` mechanism: `descendants: { "icon-node-id": { icon: "<glyph-name>" } }`. Document this in a sibling note on `Primitive / Icon`: `mcp__pencil__batch_design({ operations: ['I(<componentsPrimitivesFrame-id>, {type: "note", content: "Consumers override icon via descendants: { iconNodeId: { icon: \"<lucide-name>\" } }. Ship-list of supported glyphs in .planning/research/PEN-INVENTORY.md § Icon Glyphs (Phase 24)."})'] })`.
    3. **Pattern B path (D-25 atomic-glyph fallback):**
       - For each size in [16, 20, 24, 32]: Insert wrapper frame `Primitive / Icon / <N>` with `width: <N>, height: <N>, layout: { direction: "horizontal", gap: 0, padding: 0, alignment: "center" }`. Add a slot child accepting any glyph component (untyped if Q2 in 24-02 found slot typing limited; typed otherwise — reuse same strategy).
       - Build `_Components / Primitives / Icon / glyphs/` parent frame.
       - For each glyph in Task 2 ship-list: build an atomic component frame `glyphs/<name>` inside the glyphs/ folder. Each glyph component has a single child rendering the glyph (likely a `path` node or a `polygon` node — use Pencil-native equivalents for lucide paths where possible; otherwise simple geometric placeholders with note documenting "TODO: trace lucide path", and add OPEN-24-NN for the trace work).
    4. Verify via `mcp__pencil__batch_get({ ids: ["<componentsPrimitivesFrame-id>"], readDepth: 3 })`: children include the 4 size variants. If Pattern B, also includes `Primitive / Icon / glyphs/` with N children.
    5. `mcp__pencil__snapshot_layout` on each size variant, `problemsOnly: true`. Assert clean.
    6. Inline `mcp__pencil__get_screenshot` on each size variant. Confirm visual glyph rendering.
    7. Write Variant Evidence rows to PEN-INVENTORY.md for each size variant + glyph (Pattern A: 4 rows for sizes × fill literal; Pattern B: 4 size-wrapper rows + N glyph rows).
  </action>
  <verify>
    - `batch_get` confirms 4 size variants of `Primitive / Icon` present
    - If Pattern A: each has `type: "icon"`, `library: "lucide"`, expected width/height
    - If Pattern B: each wrapper has expected width/height/layout; glyphs/ subfolder present with N children
    - `snapshot_layout` clean on each
    - Inline `get_screenshot` shows glyph rendering at correct size
    - PEN-INVENTORY.md Variant Evidence rows match built artifacts
  </verify>
  <acceptance_criteria>
    - VAL-24-02 satisfied (4 of 4 primitives complete after this task: Button + Input + Badge + Icon)
    - VAL-24-03 satisfied (Icon portion): wrapper has token-driven size (16/20/24/32 literals = `space-primitive-*` tokens per Phase 23 audit)
    - VAL-24-05 partial: snapshot_layout clean on every Icon variant
    - VAL-24-06 evidence: pre-flight before every batch_design call
    - Pattern A or B fully built per Task 1's locked decision
  </acceptance_criteria>
  <done>Icon variants built; audit trail complete; ready for ROADMAP correction + SUMMARY (Task 4).</done>
</task>

<task type="auto">
  <name>Task 4: Correct ROADMAP.md line 169 stale marker + write SUMMARY.md</name>
  <files>.planning/ROADMAP.md (line 169 marker correction); .planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md (new)</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 149-170 (full Phase 24 section)
    - `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` § canonical_refs note about line 169 stale marker
    - `.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md` VAL-24-02, VAL-24-03, VAL-24-05, VAL-24-06
  </read_first>
  <action>
    1. Edit `.planning/ROADMAP.md` line 169. Current text reads:
       `- [x] 24-04: Build `Primitive / Icon` (size variants 16/20/24/32) with documented glyph-swap mechanism (instance swap or component property per Pencil's slot model from Phase 23 audit); semantic-token-only; `snapshot_layout` (completed 2026-05-31)`
       Replace with:
       `- [ ] 24-04: Build `Primitive / Icon` (size variants 16/20/24/32) with documented glyph-swap mechanism — Pattern A (Pencil-native `library: "lucide"`) or Pattern B (D-25 atomic-glyph) per probe outcome; source-driven glyph enumeration per D-26; semantic-token-only; `snapshot_layout`` (the `[x]` becomes `[ ]` because this plan execution will mark it completed via /gsd:execute-phase's normal completion flow OR the user can mark it post-hoc — but the stale "completed 2026-05-31" annotation is removed).
       Reasoning recorded in SUMMARY: per CONTEXT canonical_refs, the 2026-05-31 marker was copied from the reverted v1.4 Phase 24 plan list; Phase 24 v2.0 has NOT been previously completed.
    2. Write `.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md`:
       1. `## Active-Editor Pre-flight Result (D-35)` — every call recorded
       2. `## Icon Mechanism Probe Result` (Q4) — Pattern A succeeded / Pattern B used; user-approval
       3. `## Glyph Ship-List` — count + reference to PEN-INVENTORY.md `## Icon Glyphs (Phase 24)` rows
       4. `## Size Variants Built` — 4 size variants + their frame ids
       5. `## Pattern B Glyph Atoms Built` (CONDITIONAL) — if Pattern B, listing of glyphs/ subfolder contents
       6. `## VAL-24-02 Satisfied (Icon portion)` — evidence per sub-test
       7. `## VAL-24-03 Satisfied (Icon portion)` — sized wrapper with token-driven width/height
       8. `## VAL-24-05 Satisfied (Icon portion)` — snapshot_layout clean per size
       9. `## VAL-24-06 Satisfied` — pre-flight evidence per call
       10. `## OPEN-24-NN / OPEN-25-NN Flags Added` — D-28 brand-glyph deferral + any Pattern B path-trace deferral
       11. `## Variant Evidence Row Count` — total new rows this plan
       12. `## Token Surface Final Count` — `get_variables({})` count (should be Phase 23's 95 + whatever 24-03 added)
       13. `## ROADMAP Line 169 Correction` — confirmation that stale `2026-05-31` marker was removed from ROADMAP.md; rationale cited
       14. `## D-25 Status` — superseded (Pattern A) or retained (Pattern B); clear statement for Phase 25 readers
  </action>
  <verify>
    - `.planning/ROADMAP.md` line 169 no longer contains `(completed 2026-05-31)` substring (verifiable by `grep -c "completed 2026-05-31" .planning/ROADMAP.md` returning 0 — or 1 if other phases use that date, in which case grep the specific 24-04 line)
    - 24-04-SUMMARY.md exists with all 14 sections
    - `get_variables({})` count consistent with prior plans
  </verify>
  <acceptance_criteria>
    - ROADMAP.md stale marker corrected per CONTEXT canonical_refs note
    - SUMMARY.md ready for 24-05 sweep cross-ref
    - VAL-24-06 evidence consolidated
    - D-25 status clear for Phase 25 + downstream readers
  </acceptance_criteria>
  <done>ROADMAP corrected; SUMMARY.md complete; plan complete.</done>
</task>

</tasks>

<verification>
- VAL-24-02 (Icon portion) — primitive present with source-evidence-justified size variants + glyph ship-list
- VAL-24-03 (Icon portion) — sized wrappers with token-driven width/height
- VAL-24-05 (Icon portion) — snapshot_layout clean
- VAL-24-06 — get_editor_state pre-flight before every batch_design call
- Q4 probe outcome documented with user approval
- D-26 source-driven enumeration complete
- D-28 brand glyphs deferred via OPEN-25-NN
- ROADMAP.md line 169 stale marker corrected
- D-25 supersession (Pattern A) or retention (Pattern B) clearly documented
</verification>

<success_criteria>
- `Primitive / Icon` with 4 size variants in `_Components / Primitives`
- Icon mechanism probe outcome documented; Pattern A or B locked
- Glyph ship-list source-cited per D-26
- Brand glyphs deferred per D-28 with OPEN-25-NN flag
- ROADMAP.md line 169 corrected
- 24-04-SUMMARY.md captures all VAL evidence + probe results + D-25 status
</success_criteria>

<output>
After completion, create `.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md` (sections enumerated in Task 4).
</output>
