---
phase: 24-layout-primitives-primitive-components
plan: 24-05
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
  - 24-03
  - 24-04
files_modified:
  - .planning/research/PEN-INVENTORY.md (NEW section `## End-of-Phase-24 Verification` with sweep result, snapshot_layout audit roll-up, token-aware property-walker result, reference-screenshot review summary)
  - .planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (NEW — structural snapshot of every primitive id + variant cell id + glyph atom id + slot id per OPEN-23-01 archival substitution)
  - .planning/research/exports/v2.0/end-of-phase-24/README.md (NEW — describes the id-inventory schema + how to read it + cites parallel pattern from end-of-phase-23/)
autonomous: false
requirements_addressed:
  - COMP-08
  - COMP-09
must_haves:
  truths:
    - "Recursive `batch_get(readDepth: 4)` over `_Components / Primitives` returns a tree containing exactly the primitives + variant cells + glyph atoms + slots documented in 24-01..24-04 SUMMARYs"
    - "Claude-side token-aware property walker (OPEN-23-02 substitute per RESEARCH Pattern 7) over every literal color / number / string value found in `_Components / Primitives` confirms each literal matches: (a) PEN-INVENTORY.md `## Tokens Written — Primitives`, OR (b) `## Token Extensions (Phase 24)`, OR (c) a binding row in `## Variant Evidence (Phase 24)` — zero undocumented literals (COMP-09)"
    - "`snapshot_layout({ rootId, problemsOnly: true })` returns 'No layout problems.' on every primitive parent + every variant cell (VAL-24-05)"
    - "`get_screenshot` reference set captured inline per primitive parent; user has reviewed each and confirmed visual correctness vs Crito source (VAL-24-05 + VAL-24-08 user-review gate)"
    - "`end-of-phase-24/id-inventory.json` exists with every primitive id + variant cell id + glyph atom id + slot id (VAL-24-08; OPEN-23-01 archival substitution per Phase 23 plan 23-05 pattern)"
    - "Every Pencil-mutating batch is preceded by `get_editor_state` asserting active editor == `design/Crito.pen` — N/A for this plan (no mutations), but the read-only sweep still pre-flights `get_editor_state` to ensure reads come from the correct file"
  artifacts:
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "## End-of-Phase-24 Verification section with sweep result + snapshot_layout audit + token-aware walker result + reference-screenshot review summary"
      contains: "End-of-Phase-24 Verification (analogous to Phase 23 plan 23-05's End-of-Phase Verification)"
    - path: ".planning/research/exports/v2.0/end-of-phase-24/id-inventory.json"
      provides: "Structural snapshot for Phase 25 readers + future-export-tool replay"
      contains: "JSON tree: { primitives: { Button: {...}, Input: {...}, Badge: {...}, Icon: {...} } } with all node ids + variant cell ids"
    - path: ".planning/research/exports/v2.0/end-of-phase-24/README.md"
      provides: "Documentation of the id-inventory.json schema + OPEN-23-01 archival substitution rationale"
      contains: "Schema description + cross-reference to Phase 23 plan 23-05 pattern"
  key_links:
    - from: "## End-of-Phase-24 Verification (PEN-INVENTORY.md)"
      to: "VAL-24-01..VAL-24-08 evidence cross-references"
      via: "Roll-up of evidence from 24-01..24-04 SUMMARYs + sweep result"
      pattern: "Final verification gate before Phase 25 begins"
    - from: "end-of-phase-24/id-inventory.json"
      to: "Phase 25 (Section + Compound) plans"
      via: "Pre-read by 25-NN plans to know primitive ids + variant cell ids before composing"
      pattern: "Same handoff Phase 23 plan 23-05 used for Phase 24 (id-inventory.json in end-of-phase-23/)"
---

<objective>
**Phase 24 close — sweep + archival.** Run the token-aware property walker over `_Components / Primitives` (Claude-side recursive `batch_get(readDepth: 4)` per RESEARCH Pattern 7 + OPEN-23-02 workaround) to confirm COMP-09 zero-raw-values compliance with literal-binding tolerance per OPEN-23-13 dual-track. Roll up `snapshot_layout` results across every primitive. Capture inline `get_screenshot` reference set for visual review (VAL-24-08 user-review gate). Write the structural snapshot to `end-of-phase-24/id-inventory.json` per OPEN-23-01 archival substitution pattern (matches Phase 23 plan 23-05 `end-of-phase-23/id-inventory.json` precedent).

This plan is **read-only on `design/Crito.pen`** — no mutations. The output is the Phase 24 close gate: if the sweep finds raw-value leaks (literals without Variant Evidence binding rows), the plan halts and surfaces to user.

**Purpose:** Closes the audit-trail loop set up in 24-01 and populated by 24-02..24-04. Provides Phase 25 with a structural snapshot to compose against. Satisfies the milestone-close-prep validations VAL-24-04 + VAL-24-05 + VAL-24-08.

**Output:** `## End-of-Phase-24 Verification` section in PEN-INVENTORY.md, `end-of-phase-24/id-inventory.json` + `README.md`, user-reviewed reference set, 24-05-SUMMARY.md consolidating all VAL evidence.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md
@.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md
@.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md
@.planning/phases/24-layout-primitives-primitive-components/24-01-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-02-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json
</context>

<threat_model>
**T-24-T1 (active-editor swap — HIGH severity)** — even though this plan is read-only, reads from the wrong active editor would produce misleading sweep results (false-PASS or false-FAIL). Mitigation: per D-35, Task 1 pre-flights `get_editor_state` and asserts `activeFile == design/Crito.pen` before any `batch_get` calls.

**T-24-T2 (audit-trail tampering — LOW)** — this plan's sweep is the audit-trail integrity verifier. If a Variant Evidence row was authored without source-evidence backing (Pitfall 7), the token-aware walker catches it: row count match check (Variant Evidence row count must equal distinct literal-binding count in `_Components / Primitives`). Mitigation: explicit cross-ref in Task 2; halt-on-mismatch.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight + recursive batch_get + token-aware property walker (sweep)</name>
  <files>(read-only on `design/Crito.pen`)</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples Plan 24-05 Steps 1-2 (sweep procedure), § Pattern 7 (token-aware property walker rationale; Pattern 3 dual-track), § Pitfall 4 (OPEN-23-13 literal-value workaround silently violates COMP-09 if audit-trail skipped)
    - `.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md` VAL-24-04 (sweep assertions)
    - `.planning/research/PEN-INVENTORY.md` § Tokens Written — Primitives (full literal value list — the walker's reference table)
    - `.planning/research/PEN-INVENTORY.md` § Token Extensions (Phase 24) (any additions from 24-03 — extend the walker's reference table)
    - `.planning/research/PEN-INVENTORY.md` § Variant Evidence (Phase 24) (binding rows — every row's `literal_value` column becomes an additional accepted-literal entry)
    - All 4 plan SUMMARYs (24-01..24-04) — captured frame ids
  </read_first>
  <action>
    1. Re-flight per D-35: `mcp__pencil__get_editor_state({ include_schema: false })`; assert `.activeFile` ends with `/design/Crito.pen`. Halt on mismatch.
    2. `mcp__pencil__get_variables({})` — record current variable count (Phase 23's 95 + N from 24-03 if applicable). This becomes part of the End-of-Phase verification record.
    3. Recursive `batch_get` over `_Components / Primitives`: `mcp__pencil__batch_get({ ids: ["<componentsPrimitivesFrame-id>"], readDepth: 4 })`. Capture the returned tree.
    4. Walk the returned tree. For every node, collect every literal property value of the following types:
       - colors: `fill`, `stroke`, label `fill`, etc.
       - numbers: `padding` (each value in the array), `gap`, `cornerRadius`, `fontSize`, `fontWeight`, `lineHeight`, `letterSpacing`, `strokeWeight`, `width` (where it's a token-size like 16/20/24/32 for Icon), `height` (same)
       - strings: `fontFamily`
       Build a set of `(node_id, property_name, literal_value)` triples.
    5. Build the accepted-literal reference table:
       - Read PEN-INVENTORY.md `## Tokens Written — Primitives` → collect every literal value column entry into a set `phase23_literals`
       - Read PEN-INVENTORY.md `## Token Extensions (Phase 24)` → collect every value entry into a set `phase24_token_extensions`
       - Read PEN-INVENTORY.md `## Variant Evidence (Phase 24)` → collect every `literal_value` column entry into a set `phase24_binding_literals`
       - Union = `accepted_literals = phase23_literals ∪ phase24_token_extensions ∪ phase24_binding_literals`
    6. For every `(node_id, property_name, literal_value)` triple from step 4, check membership in `accepted_literals`:
       - If `literal_value ∈ accepted_literals`: PASS, document as compliant via OPEN-23-13 dual-track.
       - If `literal_value ∉ accepted_literals`: FAIL — record the leak `(node_id, property_name, literal_value)`.
       - Special case: layout-irrelevant numbers like `0` for padding/gap and `auto` strings are pre-approved (document this rule in SUMMARY).
       - Special case: `x` / `y` coordinates of children that are auto-layout-positioned can be ignored (auto-layout makes them irrelevant); only `x` / `y` of top-level frames matter (those should match Task 1 placement coordinates from 24-01).
    7. If leaks found: HALT, surface each leak to user with: node id, property, literal value, the closest matching token (if any), and a recommended action (add Variant Evidence row OR add Token Extension row OR fix the payload). DO NOT proceed to Task 2 until leaks resolved (re-run sweep after the human-authored audit-trail rows are added).
    8. If no leaks: record sweep result in SUMMARY as `PASS — N distinct literals checked, all matched accepted-literal set`.
  </action>
  <verify>
    - `mcp__pencil__get_editor_state` returned `.activeFile` ending with `/design/Crito.pen` (recorded in SUMMARY)
    - Recursive `batch_get` returned a tree containing all primitives from 24-01..24-04 SUMMARYs (cross-checked via expected ids)
    - Walker output documented: total distinct literals collected, count of (node_id, property, value) triples, count of distinct accepted-literal matches, count of leaks (must be 0 for PASS)
    - If leaks found: each leak surfaced to user with diagnostic info
  </verify>
  <acceptance_criteria>
    - VAL-24-04 satisfied — zero raw values inside primitives OR every literal is documented via dual-track
    - VAL-24-06 evidence: `get_editor_state` pre-flight before sweep recorded
    - Pitfall 4 prevention: token-aware sweep correctly handles OPEN-23-13 dual-track literals
    - If leaks: SUMMARY documents resolution path before Task 2 starts
  </acceptance_criteria>
  <done>Sweep PASS recorded; ready for snapshot_layout audit (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: snapshot_layout audit across every primitive parent + variant cell + reference screenshot set</name>
  <files>(read-only on `design/Crito.pen`); produces inline screenshots</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md` VAL-24-05 (snapshot_layout audit), VAL-24-08 (reference set archival)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples Plan 24-05 Steps 2-3
    - All 4 plan SUMMARYs (24-01..24-04) — captured frame ids for every primitive parent + variant cell + glyph atom + slot
  </read_first>
  <action>
    1. Re-flight per D-35.
    2. Enumerate every primitive id from 24-01..24-04 SUMMARYs: `_Components / Primitives` (24-01), Button + each Button variant cell (24-02), Input + each Input variant cell + Badge variant cell(s) (24-03), Icon size variants + glyph atoms if Pattern B (24-04).
    3. For each id: `mcp__pencil__snapshot_layout({ rootId: <id>, problemsOnly: true })`. Record result per id. Expected: "No layout problems." for every one. Tally PASS / FAIL counts in SUMMARY.
    4. For each primitive parent (`Primitive / Button`, `Primitive / Input`, `Primitive / Badge`, `Primitive / Icon`): `mcp__pencil__get_screenshot({ nodeId: <id> })`. Capture inline. The inline screenshot is the reference set; review visually for correctness (matches Crito source semantics — green CTA Button, bordered Input, pill-or-metric Badge, sized Icon).
    5. For each variant cell (Button/Default/Hover/Focus, Input/Default/Focus/Error, etc.): `get_screenshot`. Inline review.
    6. If any snapshot_layout returns problems: HALT, surface to user with the specific issues (clipping / overlap / zero-size). Re-snapshot after fix.
    7. SUMMARY.md records:
       - snapshot_layout PASS count / total
       - Screenshot review notes (any visual surprises caught vs Crito source)
       - Any OPEN-24-NN flags discovered during visual review (e.g., a forward variant that looks off but works structurally)
  </action>
  <verify>
    - SUMMARY.md tallies snapshot_layout results: PASS count equals total node count audited
    - Inline screenshots captured for every primitive parent (4 screenshots minimum) + every variant cell (≥ 8 additional)
    - Visual review notes recorded
  </verify>
  <acceptance_criteria>
    - VAL-24-05 satisfied — snapshot_layout clean across every primitive + variant + glyph
    - VAL-24-08 partial — inline screenshot reference set captured for user review in Task 4
    - VAL-24-06 evidence: `get_editor_state` pre-flight before screenshot batch recorded
  </acceptance_criteria>
  <done>snapshot_layout PASS; reference set captured; ready for archival (Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Write `end-of-phase-24/id-inventory.json` + README.md per OPEN-23-01 archival substitution</name>
  <files>.planning/research/exports/v2.0/end-of-phase-24/id-inventory.json (NEW); .planning/research/exports/v2.0/end-of-phase-24/README.md (NEW)</files>
  <read_first>
    - `.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json` (Phase 23 precedent — read full file to learn the JSON schema)
    - `.planning/research/exports/v2.0/end-of-phase-23/README.md` (Phase 23 precedent for README format) — if it exists, mirror its structure; if not, create per Phase 23 plan 23-05 SUMMARY description
    - `.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md` VAL-24-08 (archival substitution requirement)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples Plan 24-05 Step 3 + § "Tools the planner CANNOT assume exist" `export_nodes` row (OPEN-23-01 archival substitution rationale)
  </read_first>
  <action>
    1. Create directory: confirm `.planning/research/exports/v2.0/end-of-phase-24/` doesn't yet exist; if not, prepare via Write tool (Write tool creates parent dirs).
    2. Build the `id-inventory.json` content as a JSON object structurally analogous to `end-of-phase-23/id-inventory.json`:
       ```
       {
         "phase": "24-layout-primitives-primitive-components",
         "captured": "<ISO date>",
         "rationale": "OPEN-23-01 archival substitution: export_nodes is broken; structural JSON snapshot replaces PNG archival per Phase 23 plan 23-05 pattern.",
         "library_parents": {
           "_Components / Primitives": "<id-from-24-01-SUMMARY>",
           "_Components / Compounds": "<id-from-24-01-SUMMARY>",
           "_Components / Sections": "<id-from-24-01-SUMMARY>"
         },
         "primitives": {
           "Button": {
             "parent": "<id>",
             "variants": {
               "Default": "<id>",
               "Default/Hover": "<id>",
               "Default/Focus": "<id>"
             },
             "slots": {
               "iconLeading": "<id-or-strategy-note>",
               "iconTrailing": "<id-or-strategy-note>"
             }
           },
           "Input": {
             "parent": "<id>",
             "variants": {
               "Default": "<id>",
               "Focus": "<id>",
               "Error": "<id>"
             }
           },
           "Badge": {
             // Single component OR Pill+Metric per D-34 outcome
             "parent": "<id>",
             "variants": { ... } // if Case II
           },
           "Icon": {
             "parent": "<id>",
             "variants": {
               "16": "<id>",
               "20": "<id>",
               "24": "<id>",
               "32": "<id>"
             },
             "mechanism": "Pattern A (Pencil-native lucide)" OR "Pattern B (D-25 atomic-glyph)",
             "glyphs": { "<name>": "<id>", ... } // Pattern B only
           }
         },
         "token_surface_count": <get_variables count from Task 1>,
         "variant_evidence_row_count": <count from PEN-INVENTORY>,
         "token_extension_row_count": <count from PEN-INVENTORY>,
         "open_flags_phase_24": <count of OPEN-24-NN rows>
       }
       ```
       Use the Write tool to create the JSON file (UTF-8, formatted with 2-space indent).
    3. Build the README.md content describing:
       - What this directory contains (structural snapshot, not PNG archival)
       - Why (OPEN-23-01 — `export_nodes` broken in current Pencil MCP build)
       - Schema of id-inventory.json (one paragraph per top-level key)
       - How Phase 25 readers should use it (pre-read primitive ids before composing Sections + Compounds)
       - Cross-reference to `end-of-phase-23/` precedent
       Use the Write tool to create the README.
    4. Verify both files exist via `ls .planning/research/exports/v2.0/end-of-phase-24/` (recorded in SUMMARY).
  </action>
  <verify>
    - `.planning/research/exports/v2.0/end-of-phase-24/id-inventory.json` exists; valid JSON (parses without error)
    - `.planning/research/exports/v2.0/end-of-phase-24/README.md` exists with all required sections
    - id-inventory.json contains ids for every primitive + variant cell + slot + (if Pattern B) glyph atom from 24-01..24-04 SUMMARYs
  </verify>
  <acceptance_criteria>
    - VAL-24-08 satisfied — archival JSON + README written per OPEN-23-01 substitution pattern
    - id-inventory.json schema mirrors Phase 23 plan 23-05's precedent for consistency
    - Phase 25 readers have a single-file structural snapshot to pre-read
  </acceptance_criteria>
  <done>Archival written; ready for user review gate (Task 4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User review of inline reference screenshot set (VAL-24-05 + VAL-24-08 human gate)</name>
  <what-built>The Phase 24 deliverable: `Primitive / Button` + `Primitive / Input` + `Primitive / Badge` + `Primitive / Icon` inside `_Components / Primitives`, with variant matrices justified by per-cell audit-trail rows. Token-aware sweep confirmed zero raw-value leaks (every literal documented). `snapshot_layout` clean on every primitive. Structural snapshot archived to `end-of-phase-24/id-inventory.json`.</what-built>
  <how-to-verify>
    1. Open `design/Crito.pen` in Pencil. Scroll to top of canvas.
    2. Open `_Components / Primitives`. Confirm you see (in order): `Primitive / Button` (with Default + state variants), `Primitive / Input` (with Default + Focus + Error), `Primitive / Badge` (single or Pill+Metric per probe), `Primitive / Icon` (with 16/20/24/32 sizes; Pattern A icon nodes or Pattern B wrapper frames + glyphs/ subfolder).
    3. Confirm each primitive parent visually matches its Crito source counterpart per 24-02/03/04 SUMMARY screenshot reviews. Watch for: green CTA Button, bordered white-bg Input, pill or metric Badge, lucide-icon-style glyphs.
    4. Confirm `_Components / Compounds` and `_Components / Sections` still show their "Populated in Phase 25" stub notes (Phase 24 should NOT have populated these).
    5. Confirm `_Tokens & Foundations` (id `RpGbe`) is unchanged from Phase 23.
    6. Confirm the 15 baseline Crito page frames (Home Page, View More, etc.) are unmutated.
  </how-to-verify>
  <resume-signal>Type "approved" if all four primitives look correct and the 15 baseline Crito frames + `_Tokens & Foundations` are unchanged. Type "issues: ..." if anything is wrong — describe the primitive + variant + issue.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write `## End-of-Phase-24 Verification` section to PEN-INVENTORY.md + 24-05-SUMMARY.md (Phase 24 close)</name>
  <files>.planning/research/PEN-INVENTORY.md (NEW section `## End-of-Phase-24 Verification`); .planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md (new)</files>
  <read_first>
    - `.planning/research/PEN-INVENTORY.md` § End-of-Phase Verification (plan 23-05) — Phase 23 precedent; mirror structure
    - All 4 prior plan SUMMARYs (24-01..24-04)
    - 24-VALIDATION.md (full VAL-24-01..08 assertion list)
  </read_first>
  <action>
    1. Append to PEN-INVENTORY.md a new section: `## End-of-Phase-24 Verification`. Structure mirrors Phase 23's `## End-of-Phase Verification (plan 23-05)`:
       - `### Sweep Result (VAL-24-04)`: distinct-literal count, accepted-literal set size (Phase 23 tokens + Phase 24 extensions + Variant Evidence binding rows), leak count (must be 0)
       - `### snapshot_layout Audit Roll-up (VAL-24-05)`: total nodes audited, PASS count, problems count (must be 0)
       - `### Reference Screenshot Review (VAL-24-05 + VAL-24-08)`: list of screenshots captured + user-approval timestamp from Task 4
       - `### Variant Evidence Row Count (D-23 enforcement)`: total rows across plans 24-02 + 24-03 + 24-04 (and any from 24-01 stub literals)
       - `### Token Extensions Row Count (D-33 enforcement)`: total rows from 24-03 if any
       - `### OPEN Flag Count (Phase 24)`: distinct OPEN-24-NN + OPEN-25-NN added; total, severity breakdown
       - `### Active-Editor Pre-flight Compliance (D-35)`: count of `get_editor_state` calls across all plans; any mismatches recorded; expected zero per VAL-24-06
       - `### Files Modified Summary`: list of files touched per plan (cross-ref each plan SUMMARY's files_modified section)
       - `### Cross-cutting `must_haves.truths` Satisfaction`: tick each cross-cutting truth from planning-context with the SUMMARY citation that satisfies it
    2. Write `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md`:
       1. `## Active-Editor Pre-flight Result (D-35)` — every Task 1-3 call recorded
       2. `## Sweep Result (VAL-24-04)` — leak count, distinct literal count, walker rationale
       3. `## snapshot_layout Audit (VAL-24-05)` — PASS count per primitive
       4. `## Reference Screenshot Set (VAL-24-08)` — list of screenshots + user-approval
       5. `## Archival Artifacts (VAL-24-08)` — id-inventory.json + README.md paths + structural summary
       6. `## End-of-Phase-24 Verification` cross-reference (cite PEN-INVENTORY.md section)
       7. `## Phase 24 Close Summary` — list every VAL-24-01..08 with PASS evidence
       8. `## Recommended Next Step` — "Run `/gsd:verify-work 24` to confirm Phase 24 close gate; then `/gsd:plan-phase 25` for Section + Compound components"
  </action>
  <verify>
    - `grep -c "^## End-of-Phase-24 Verification$" .planning/research/PEN-INVENTORY.md` returns 1
    - 24-05-SUMMARY.md exists with all 8 sections
    - Cross-cutting must_haves explicitly ticked off
  </verify>
  <acceptance_criteria>
    - All VAL-24-01..08 PASS evidence consolidated in End-of-Phase-24 Verification section + 24-05-SUMMARY.md
    - PEN-INVENTORY.md serves as the canonical milestone-close-readiness record
    - Phase 25 readers can confirm Phase 24 close by reading the End-of-Phase Verification section alone
  </acceptance_criteria>
  <done>Phase 24 close artifact written; ready for `/gsd:verify-work 24`.</done>
</task>

</tasks>

<verification>
- VAL-24-04 — token-aware property walker PASS (zero undocumented literals)
- VAL-24-05 — snapshot_layout PASS on every primitive + variant + glyph
- VAL-24-06 — every batch_get pre-flighted with get_editor_state assertion
- VAL-24-08 — id-inventory.json + README.md + user-reviewed screenshot set
- PEN-INVENTORY.md `## End-of-Phase-24 Verification` section written
- 24-05-SUMMARY.md consolidates all VAL evidence
- User has approved reference screenshot review
</verification>

<success_criteria>
- Token-aware sweep PASS (zero raw-value leaks per COMP-09 + OPEN-23-13 dual-track tolerance)
- snapshot_layout PASS across every primitive + variant cell
- Inline reference screenshots captured + user-reviewed
- end-of-phase-24/id-inventory.json + README.md exist (OPEN-23-01 archival substitution)
- PEN-INVENTORY.md `## End-of-Phase-24 Verification` section serves as canonical close record
- 24-05-SUMMARY.md captures all VAL-24-01..08 PASS evidence
- Phase 24 ready for `/gsd:verify-work 24`
</success_criteria>

<output>
After completion, create `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` (sections enumerated in Task 5).
</output>
