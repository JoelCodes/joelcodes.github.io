---
phase: 24-layout-primitives-primitive-components
plan: 24-01
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen (new top-level frames: _Components / Primitives, _Components / Compounds, _Components / Sections + their title/note children — zero mutation to the 16 baseline top-level frames)
  - .planning/research/PEN-INVENTORY.md (## Frames table: add 3 new rows; ## Open Flags: scaffold OPEN-24-NN section; ## Variant Evidence (Phase 24): scaffold empty table; ## Token Extensions (Phase 24): scaffold empty table)
autonomous: false
requirements_addressed:
  - LAYOUT-01
  - LAYOUT-02
  - COMP-08
must_haves:
  truths:
    - "Opening design/Crito.pen shows exactly 19 top-level frames (16 baseline + 3 new): _Components / Primitives, _Components / Compounds, _Components / Sections, _Tokens & Foundations (id RpGbe), and 15 baseline Crito page frames — all unmutated except the 3 new"
    - "Every Pencil-mutating batch is preceded by get_editor_state asserting active editor == design/Crito.pen (D-35)"
    - "Sibling stubs (_Components / Compounds, _Components / Sections) contain a title text node + a note node declaring 'Populated in Phase 25' — not empty TODOs (D-37 + Pitfall 8 prevention)"
    - "PEN-INVENTORY.md has scaffolded empty tables for ## Variant Evidence (Phase 24), ## Token Extensions (Phase 24), and an OPEN-24-NN subsection in ## Open Flags — ready for plans 24-02..24-04 to populate"
    - "D-36: New top-level frames use slash-separated component-style naming (`_Components / Primitives`, `_Components / Compounds`, `_Components / Sections`) distinct from the flat-dash token convention; this establishes the naming convention plans 24-02..24-05 follow when authoring primitives"
  artifacts:
    - path: "design/Crito.pen"
      provides: "Three new top-level library frames at top of canvas (above or aligned with _Tokens & Foundations)"
      contains: "_Components / Primitives, _Components / Compounds, _Components / Sections"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Audit-trail scaffold for Phase 24 — Frames table extension + empty Variant Evidence/Token Extensions tables + OPEN-24-NN section header"
      contains: "## Variant Evidence (Phase 24), ## Token Extensions (Phase 24), ## Open Flags additions"
  key_links:
    - from: "_Components / Primitives (new top-level frame)"
      to: "_Tokens & Foundations (id RpGbe)"
      via: "vertical-stack neighborhood at top of canvas (above or directly adjacent)"
      pattern: "Single-file strategy (COMP-08): library frames clustered at top with _ prefix"
    - from: "_Components / Compounds + _Components / Sections (stubs)"
      to: "Phase 25 plans (25-01 Header, 25-02 Footer, 25-03 Compound/Card)"
      via: "note node citing '.planning/ROADMAP.md § Phase 25'"
      pattern: "Designed-pause stub per D-37 (not empty TODO)"
---

<objective>
Create three new top-level parent library frames in `design/Crito.pen` — `_Components / Primitives`, `_Components / Compounds`, `_Components / Sections` — clustered at top of canvas as siblings of the existing `_Tokens & Foundations` (id `RpGbe`). The Primitives frame becomes the parent container for plans 24-02..24-04. The Compounds and Sections frames ship as **designed pauses per D-37**: a title text node + a Pencil `note` node citing Phase 25's plan numbers — visibly intentional, not forgotten TODOs.

Scaffold three audit-trail surfaces in `PEN-INVENTORY.md` so plans 24-02..24-04 can populate them: `## Variant Evidence (Phase 24)`, `## Token Extensions (Phase 24)`, and an `OPEN-24-NN` subsection within `## Open Flags`.

**Purpose:** Single-file strategy (COMP-08) is visible at top of canvas. Subsequent plans have a known parent frame to insert primitives into. PEN-INVENTORY.md is ready to absorb the dual-track audit trail per OPEN-23-13 (literal-value-in-payload + semantic-token-binding-row).

**Output:** Three new top-level frame ids (recorded in this plan's SUMMARY.md), an unmutated 16-baseline top-level frame set, and a scaffolded PEN-INVENTORY.md ready for Wave 2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md
@.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md
@.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json
</context>

<threat_model>
**T-24-T1 (active-editor swap — HIGH severity, BLOCKING)** — carry-forward of T-23-T4 / OPEN-23-14. If `batch_design` lands in a `.pen` file other than `design/Crito.pen`, this plan corrupts a foreign file and leaves Phase 24 without its parent frames.

**Mitigation:** Wave-0 Task 0 calls `mcp__pencil__get_editor_state({ include_schema: false })`, asserts `activeFile` matches `design/Crito.pen`, and halts on mismatch via AskUserQuestion. Per D-35, this assertion is re-run before EVERY `batch_design` call within this plan (3 batches → 3 pre-flight checks).

**T-24-I1 (Information disclosure — LOW)** — `get_screenshot` of the new stubs may be captured. Mitigation: archive lives in `.planning/research/exports/v2.0/` (non-public). Block-on: no.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0 (Wave-0 pre-flight + audit-trail scaffold): Assert active editor + scaffold PEN-INVENTORY.md tables</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` § Implementation Decisions D-23 (Variant Evidence schema), D-33 (Token Extensions schema), D-35 (active-editor pre-flight), D-37 (sibling stubs)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Pattern 3 (OPEN-23-13 dual-track), § Pitfall 5 (active-editor swap recovery), § Code Examples "Pre-flight pattern"
    - `.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md` § Wave 0 Requirements
    - `.planning/research/PEN-INVENTORY.md` § Open Flags (read the existing OPEN-23-* row format to match style)
  </read_first>
  <action>
    1. Call `mcp__pencil__get_editor_state({ include_schema: false })`. Read the `.activeFile` field.
    2. Assert `.activeFile` ends with `/design/Crito.pen`. If mismatch: HALT, surface to user via AskUserQuestion explaining "Pencil active editor is `<actual>`, expected `design/Crito.pen`. Open Crito.pen as the active Pencil tab in VS Code and confirm to resume." Wait for user resume.
    3. Call `mcp__pencil__get_variables({})`. Assert the returned variable count is ≥ 95 (Phase 23's surface). If less: HALT, surface to user.
    4. Open `.planning/research/PEN-INVENTORY.md` (regular file Read) and append (use Edit tool) three new sections after the existing `## Open Flags` section:

       a. `## Variant Evidence (Phase 24)` with an empty markdown table whose header row is exactly (per D-23):
          `| primitive | variant_cell | property | literal_value | bound_to_token | source_evidence (frame_id + node_id + section) | rationale |`
          `|-----------|--------------|----------|---------------|----------------|------------------------------------------------|-----------|`
          And a body paragraph: "Populated by plans 24-02, 24-03, 24-04. Each row binds a literal value used in a `batch_design` payload to the semantic token name it conceptually references (OPEN-23-13 dual-track workaround). Plan 24-05 sweep cross-refs every literal in `_Components / Primitives` against this table."

       b. `## Token Extensions (Phase 24)` with an empty markdown table whose header row is exactly (per D-33):
          `| token_name | added_by_plan | source_evidence (frame_id + node_id) | rationale |`
          `|------------|---------------|--------------------------------------|-----------|`
          And a body paragraph: "Populated conditionally by plan 24-03 if D-32 Badge radius probe surfaces a needed token. Each row must answer: 'Does any other primitive need this token, or does it serve only [Plan 24-NN's primitive]?' Plan 24-05 sweep verifies every NEW token (beyond Phase 23's 95) has a row here."

       c. In the existing `## Open Flags` section, append a subsection header `### Open Flags — Phase 24 (OPEN-24-NN)` with a paragraph: "Populated by plans 24-02, 24-03, 24-04 per D-22 (compositional minimum forward variants with consumer phase), D-24 (Crito-source-not-depicting-COMP-01 purposes), D-28 (brand glyphs deferred to Phase 25), D-31 (token gap deferred). Per Phase 23 D-09 carry-forward: OPEN flags do not block Phase 24 close."

    5. Save `PEN-INVENTORY.md`.
  </action>
  <verify>
    - `mcp__pencil__get_editor_state({ include_schema: false })` returned `.activeFile` ending with `/design/Crito.pen` (record the exact path in SUMMARY)
    - `mcp__pencil__get_variables({})` returned `>= 95` variables (record exact count in SUMMARY)
    - `grep -c "^## Variant Evidence (Phase 24)$" .planning/research/PEN-INVENTORY.md` returns `1`
    - `grep -c "^## Token Extensions (Phase 24)$" .planning/research/PEN-INVENTORY.md` returns `1`
    - `grep -c "^### Open Flags — Phase 24 (OPEN-24-NN)$" .planning/research/PEN-INVENTORY.md` returns `1`
  </verify>
  <acceptance_criteria>
    - VAL-24-06 evidence: SUMMARY.md records `get_editor_state` pre-flight result with exact `.activeFile` path
    - VAL-24-07 prep: PEN-INVENTORY.md has scaffolded `## Token Extensions (Phase 24)` empty table
    - `## Variant Evidence (Phase 24)` table exists with the exact header row schema declared by D-23 (verifiable by `grep "primitive | variant_cell | property | literal_value | bound_to_token | source_evidence" .planning/research/PEN-INVENTORY.md` returning a match)
    - Token surface integrity assertion logged: `get_variables` count recorded in SUMMARY.md (Phase 23 shipped 95; plan 24-01 makes zero token changes)
    - No mutation to `design/Crito.pen` in this task (the Pencil mutations happen in Task 1+)
  </acceptance_criteria>
  <done>Active editor confirmed `design/Crito.pen`; 95-token surface intact; three new audit-trail surfaces scaffolded in PEN-INVENTORY.md; ready for Task 1 (frame stubs).</done>
</task>

<task type="auto">
  <name>Task 1: Enumerate baseline top-level frames + compute placement coordinates</name>
  <files>design/Crito.pen (READ-ONLY — no mutations in this task)</files>
  <read_first>
    - `.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json` (full file — 16 baseline top-level frame ids + coordinates)
    - `.planning/research/PEN-INVENTORY.md` § Frames (15 baseline Crito frame catalogue + `_Tokens & Foundations` at id `RpGbe`)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Tools the planner CANNOT assume exist — `find_empty_space_on_canvas` row + § Code Examples "Plan 24-01: Library frame stubs" Step 1
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Architecture Patterns "Top-of-canvas placement strategy"
  </read_first>
  <action>
    1. Re-confirm active editor: `mcp__pencil__get_editor_state({ include_schema: false })` — assert `.activeFile` matches per D-35 (re-run before any tool call that could lead to a mutation).
    2. ATTEMPT (RESEARCH Q5): try calling `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 600 })` ONCE. If the tool exists and returns a coordinate, use it for the first new frame. If it errors (tool not in build) OR returns no usable result, record outcome and proceed to step 3 (fallback).
    3. FALLBACK: call `mcp__pencil__batch_get({ readDepth: 1 })` at the document root. Extract every top-level frame's `id`, `name`, `x`, `y`, `width`, `height`. Assert the returned list contains exactly 16 frames matching `.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json`. If count != 16 or ids drift: HALT, surface to user with diff.
    4. Read `_Tokens & Foundations` (id `RpGbe`) `x`, `y`, `width`, `height` — this is the anchor.
    5. Compute three new y coordinates **above** `_Tokens & Foundations` (vertical-stack siblings per RESEARCH Pattern 4):
       - `y_primitives = RpGbe.y - (primitives_height + buffer)` where `primitives_height = 600`, `buffer = 200`
       - `y_compounds = RpGbe.y - (primitives_height + buffer) - (compounds_height + buffer)` where `compounds_height = 200`, `buffer = 200`
       - `y_sections = RpGbe.y - (primitives_height + buffer) - (compounds_height + buffer) - (sections_height + buffer)` where `sections_height = 200`, `buffer = 200`
       - All x = `RpGbe.x` (horizontally aligned).
    6. Cross-check: each computed (x, y, x+width, y+height) rectangle does NOT overlap any of the 16 baseline frames' bounding boxes. If overlap detected: shift y further negative until clear (and record decision in SUMMARY).
    7. If `find_empty_space_on_canvas` was usable in step 2, the (x, y) for the first frame comes from it; reuse that x for the other two, stacking vertically with `+ height + buffer` offset.
    8. Record all chosen coordinates + the 16 baseline frame ids in SUMMARY.md (will be referenced by Task 2-4).
  </action>
  <verify>
    - SUMMARY.md captures: the result of the `find_empty_space_on_canvas` probe (works / errors / not-in-build) — fulfils RESEARCH Q5
    - `mcp__pencil__batch_get({ readDepth: 1 })` returned exactly 16 top-level frames, ids matching `.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json` 1:1
    - `RpGbe` is present in the returned set with `name: "_Tokens & Foundations"`
    - Three computed (x, y) coordinates documented in SUMMARY.md with rationale (anchor reference to `RpGbe`)
  </verify>
  <acceptance_criteria>
    - VAL-24-01 prep: 16-frame baseline set verified intact before any mutation
    - Zero-mutation contract maintained: no `batch_design` or `set_variables` called in this task
    - SUMMARY.md cites the exact `RpGbe.y` value used as anchor and the formula used to compute new y coordinates
    - Q5 (find_empty_space_on_canvas availability) resolved with documented outcome in SUMMARY.md
  </acceptance_criteria>
  <done>Three placement coordinates ready; baseline frame set verified intact; ready for Task 2 (build the three frames).</done>
</task>

<task type="auto">
  <name>Task 2: Build `_Components / Primitives` parent frame via batch_design</name>
  <files>design/Crito.pen (new top-level frame: `_Components / Primitives` + 2 child nodes — title text + note)</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples "Plan 24-01: Library frame stubs" Step 2 (literal `batch_design` payload shape)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Pattern 1 (component declaration via batch_design — frame node type)
    - `.planning/research/PEN-INVENTORY.md` § Tokens Written — Primitives (literal value for `color-semantic-text-primary = #141f39ff` used for title fill; `space-primitive-40` for padding; `space-semantic-stack-md = 24` or `40` for gap — confirm from current PEN-INVENTORY rows)
    - `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` D-36 (component naming uses slash separation)
  </read_first>
  <action>
    1. Re-flight per D-35: `mcp__pencil__get_editor_state({ include_schema: false })`; assert activeFile.
    2. Call `mcp__pencil__batch_design` with the following 3-op chunked call (≤25 ops total — well under limit):
       - Op A: Insert top-level frame named `_Components / Primitives` at the (x, y) from Task 1, with `width: 1440`, `height: 600`, `layout: { direction: "vertical", gap: 40, padding: [40, 40] }`. Capture returned id as `componentsPrimitivesFrame`.
       - Op B: Insert into `componentsPrimitivesFrame` a `text` child with `content: "_Components / Primitives"`, `fontFamily: "Plus Jakarta Sans"` (literal value of `type-primitive-family-display` — verify in PEN-INVENTORY before payload), `fontSize: 48` (literal of `type-primitive-size-48`), `fontWeight: "700"`, `fill: "#141f39ff"` (literal of `color-semantic-text-primary` / `color-primitive-navy-900`).
       - Op C: Insert into `componentsPrimitivesFrame` a `note` child with `content: "Phase 24 deliverable: Primitive / Button, Primitive / Input, Primitive / Badge, Primitive / Icon. Built via plans 24-02 / 24-03 / 24-04. See .planning/research/PEN-INVENTORY.md § Variant Evidence (Phase 24)."`
    3. Call `mcp__pencil__batch_get({ ids: ["<componentsPrimitivesFrame-id>"], readDepth: 2 })`. Assert the returned node has `name: "_Components / Primitives"` and contains exactly 2 children (text + note). Record `componentsPrimitivesFrame` id in SUMMARY.md (Task 5 will use it).
    4. Call `mcp__pencil__snapshot_layout({ rootId: "<componentsPrimitivesFrame-id>", problemsOnly: true })`. Assert "No layout problems." If problems: surface to user with the returned issues.
    5. Add a row to PEN-INVENTORY.md `## Variant Evidence (Phase 24)` for each literal value used (3 rows: fontFamily binding, fontSize binding, fill binding — per OPEN-23-13 dual-track):
       - `_Components / Primitives` | `(parent frame title)` | `fontFamily` | `Plus Jakarta Sans` | `type-primitive-family-display` | `(this plan — plan 24-01 Task 2)` | `Title typography for top-of-canvas library label`
       - (similar rows for fontSize and fill)
  </action>
  <verify>
    - `mcp__pencil__batch_get` on the new id returns `name: "_Components / Primitives"`, `type: "frame"`, `width: 1440`, `height: 600`, `layout.direction: "vertical"`, 2 children
    - `mcp__pencil__snapshot_layout({ rootId, problemsOnly: true })` returns "No layout problems."
    - PEN-INVENTORY.md `## Variant Evidence (Phase 24)` has 3+ new rows for the title typography literals (verifiable by grep)
    - `componentsPrimitivesFrame-id` is recorded in SUMMARY.md as the parent container for plans 24-02..24-04
  </verify>
  <acceptance_criteria>
    - VAL-24-01 partial (1/3 new frames): `_Components / Primitives` present at top of canvas with title + note children
    - VAL-24-04 prep: literal values used in payload all have audit-trail rows in `## Variant Evidence (Phase 24)`
    - VAL-24-05 partial: `snapshot_layout` clean on the new frame
    - VAL-24-06 evidence: `get_editor_state` pre-flight recorded in SUMMARY.md before this `batch_design` call
  </acceptance_criteria>
  <done>`_Components / Primitives` exists as new top-level frame, snapshot_layout clean, audit-trail rows added; id captured for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 3: Build `_Components / Compounds` + `_Components / Sections` stub frames via batch_design (D-37 sibling-stub pattern)</name>
  <files>design/Crito.pen (two new top-level frames: `_Components / Compounds` + `_Components / Sections`, each with title text + Phase-25-deferral note)</files>
  <read_first>
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Code Examples "Plan 24-01: Library frame stubs" Steps 3-4 (payload shape)
    - `.planning/phases/24-layout-primitives-primitive-components/24-RESEARCH.md` § Pitfall 8 (sibling-stub frames look like TODOs — the note is load-bearing)
    - `.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md` D-37 (sibling library frame stubs)
    - `.planning/ROADMAP.md` lines 174-194 (Phase 25 plan numbers: 25-01 Header, 25-02 Footer, 25-03 Compound/Card)
  </read_first>
  <action>
    1. Re-flight per D-35: `mcp__pencil__get_editor_state({ include_schema: false })`; assert activeFile.
    2. Call `mcp__pencil__batch_design` with a chunked 6-op call (3 ops per stub × 2 stubs):
       **Compounds stub:**
       - Op A: Insert top-level frame `_Components / Compounds` at (Task1.x, Task1.y_compounds), width: 1440, height: 200, layout: `{ direction: "vertical", gap: 24, padding: [40, 40] }`. Capture id as `componentsCompoundsFrame`.
       - Op B: Insert text child: `content: "_Components / Compounds"`, fontFamily: `"Plus Jakarta Sans"`, fontSize: 48, fontWeight: "700", fill: `"#141f39ff"`.
       - Op C: Insert note child: `content: "Populated in Phase 25 (plan 25-03 Compound / Card). See .planning/ROADMAP.md § Phase 25. Intentionally empty in Phase 24 per D-37."`
       **Sections stub:**
       - Op D: Insert top-level frame `_Components / Sections` at (Task1.x, Task1.y_sections), width: 1440, height: 200, layout: `{ direction: "vertical", gap: 24, padding: [40, 40] }`. Capture id as `componentsSectionsFrame`.
       - Op E: Insert text child: `content: "_Components / Sections"`, fontFamily/Size/Weight/fill identical to Op B.
       - Op F: Insert note child: `content: "Populated in Phase 25 (plans 25-01 Section / Header + 25-02 Section / Footer). See .planning/ROADMAP.md § Phase 25. Intentionally empty in Phase 24 per D-37."`
    3. Call `mcp__pencil__batch_get({ ids: [componentsCompoundsFrame, componentsSectionsFrame], readDepth: 2 })`. Assert each frame has 2 children (text + note) and the note content includes both "Populated in Phase 25" AND a plan number reference (per Pitfall 8 prevention).
    4. Call `mcp__pencil__snapshot_layout` on each new frame, `problemsOnly: true`. Assert clean.
    5. Add audit-trail rows in PEN-INVENTORY.md `## Variant Evidence (Phase 24)` for the title typography literals (same 3 properties as Task 2, but for each of the 2 new frames — record as a single grouped row each per D-23 row-granularity discretion).
  </action>
  <verify>
    - `batch_get` on `componentsCompoundsFrame` returns 2 children; the note child content includes BOTH "Populated in Phase 25" AND "25-03"
    - `batch_get` on `componentsSectionsFrame` returns 2 children; the note child content includes BOTH "Populated in Phase 25" AND ("25-01" OR "25-02")
    - `snapshot_layout` returns "No layout problems." on each
    - PEN-INVENTORY.md has additional rows in `## Variant Evidence (Phase 24)` for the stubs' literals
  </verify>
  <acceptance_criteria>
    - VAL-24-01 partial (2/3 new frames complete after this task — combined with Task 2 = 3/3)
    - D-37 enforcement (VAL-24-01 test 5): both stub frames contain BOTH a title text node AND a note node citing Phase 25 plan numbers (Pitfall 8 prevention)
    - VAL-24-05 partial: snapshot_layout clean on both new stubs
    - VAL-24-06 evidence: `get_editor_state` pre-flight recorded before this batch_design call
  </acceptance_criteria>
  <done>Both stub frames present at top of canvas with title + Phase-25-referencing note; snapshot_layout clean; designed-pause discipline visible (not empty TODO).</done>
</task>

<task type="auto">
  <name>Task 4: Update PEN-INVENTORY.md ## Frames table + record final state in SUMMARY.md</name>
  <files>.planning/research/PEN-INVENTORY.md (## Frames table extension); .planning/phases/24-layout-primitives-primitive-components/24-01-SUMMARY.md (new)</files>
  <read_first>
    - `.planning/research/PEN-INVENTORY.md` § Frames (existing 16-row table format — match column shape)
    - `.planning/phases/24-layout-primitives-primitive-components/24-VALIDATION.md` VAL-24-01 (assertion to evidence)
    - `.planning/phases/23-audit-token-foundation/23-05-SUMMARY.md` (read first 50 lines as a template reference for SUMMARY.md format Phase 23 settled on)
  </read_first>
  <action>
    1. Edit `.planning/research/PEN-INVENTORY.md § Frames` table: append 3 new rows (matching the existing column schema — `id | name | type | x | y | width | height | scope | reconstruction_priority | status` or whatever existing columns are present; replicate exactly):
       - row for `componentsPrimitivesFrame-id | _Components / Primitives | frame | <x> | <y> | 1440 | 600 | library | n/a (Phase 24 deliverable) | populated by 24-02/03/04`
       - row for `componentsCompoundsFrame-id | _Components / Compounds | frame | <x> | <y> | 1440 | 200 | library | n/a (Phase 25 deliverable) | stub per D-37`
       - row for `componentsSectionsFrame-id | _Components / Sections | frame | <x> | <y> | 1440 | 200 | library | n/a (Phase 25 deliverable) | stub per D-37`
    2. Final verification: call `mcp__pencil__batch_get({ readDepth: 1 })` at document root. Assert returned count == 19 (16 baseline + 3 new). Assert all 16 baseline ids still present (cross-ref against `id-inventory.json`). Assert all 3 new ids match those recorded in Tasks 2-3.
    3. Write `.planning/phases/24-layout-primitives-primitive-components/24-01-SUMMARY.md` documenting:
       - `## Active-Editor Pre-flight Result` (D-35 evidence): exact `activeFile` path returned by each `get_editor_state` call during this plan
       - `## Find-Empty-Space Probe Result` (Q5 evidence): whether `find_empty_space_on_canvas` worked, errored, or didn't exist; what fallback was used
       - `## New Frame IDs` (for downstream plans): the three captured ids + their (x, y, width, height) coordinates
       - `## VAL-24-01 Satisfied`: cite each VAL-24-01 sub-test from 24-VALIDATION.md and the evidence (batch_get count, snapshot_layout result, PEN-INVENTORY row added)
       - `## VAL-24-06 Satisfied`: list each `get_editor_state` pre-flight call with timestamp/order
       - `## Audit-trail rows added`: count of new rows added to `## Variant Evidence (Phase 24)` (should be ~9: 3 properties × 3 frames)
       - `## Token Surface Integrity`: confirm `get_variables({})` count unchanged from Phase 23's 95
       - `## ROADMAP correction note`: a reminder that line 169's stale `[x] 24-04 ... (completed)` marker is being addressed by 24-04's close (not this plan)
    4. Stage commit (defer to orchestrator's commit step at phase close; do not commit here).
  </action>
  <verify>
    - `wc -l .planning/research/PEN-INVENTORY.md` returns a count higher than the pre-plan baseline (recorded in SUMMARY)
    - `grep -c "^| <componentsPrimitivesFrame-id>" .planning/research/PEN-INVENTORY.md` returns 1 (after substituting actual id)
    - `mcp__pencil__batch_get({ readDepth: 1 })` returns 19 top-level frames; all 16 baseline ids present
    - `.planning/phases/24-layout-primitives-primitive-components/24-01-SUMMARY.md` exists with all required sections
  </verify>
  <acceptance_criteria>
    - VAL-24-01 (Success Criterion 1) fully satisfied — evidence per sub-test recorded in SUMMARY.md `## VAL-24-01 Satisfied`
    - VAL-24-06 (D-35 active-editor pre-flight enforcement) evidence recorded for every batch_design call in this plan
    - VAL-24-08 prep: new frame ids captured in SUMMARY.md for plan 24-05's `id-inventory.json` archival
    - PEN-INVENTORY.md `## Frames` table extended with 3 new rows matching existing column shape
  </acceptance_criteria>
  <done>PEN-INVENTORY.md updated; SUMMARY.md written with all required evidence; ready for Wave 2 (plans 24-02, 24-03, 24-04 in parallel).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User confirms top-of-canvas placement looks correct</name>
  <what-built>Three new top-level library frames in `design/Crito.pen`: `_Components / Primitives` (1440×600), `_Components / Compounds` (1440×200, designed-pause stub), `_Components / Sections` (1440×200, designed-pause stub). Each contains a 48px title text + a Pencil note. PEN-INVENTORY.md has scaffolded audit-trail tables for Phase 24.</what-built>
  <how-to-verify>
    1. Open `design/Crito.pen` in Pencil (VS Code Pencil tab).
    2. Scroll to top of canvas. Confirm you see four `_`-prefixed parent frames clustered together: `_Components / Primitives`, `_Components / Compounds`, `_Components / Sections`, `_Tokens & Foundations`.
    3. Confirm `_Components / Primitives` is empty except for its title + note (Phase 24-02/03/04 will populate).
    4. Confirm `_Components / Compounds` and `_Components / Sections` each show their title AND a visible note saying "Populated in Phase 25". This visual designed-pause signal is the D-37 contract.
    5. Confirm the 15 baseline Crito page frames (Home Page, View More, About Me, Contact, Blog, FAQ, etc.) are unchanged visually — none mutated.
  </how-to-verify>
  <resume-signal>Type "approved" if the four library frames look correct and the 15 baseline Crito frames are unchanged. Type "issues: ..." if anything is wrong (e.g., overlap with existing frames, wrong coordinates, missing note nodes).</resume-signal>
</task>

</tasks>

<verification>
- VAL-24-01 (all 5 sub-tests in 24-VALIDATION.md) PASS — evidence in SUMMARY.md
- VAL-24-06 evidence — `get_editor_state` pre-flight before each `batch_design` call recorded in SUMMARY.md
- 16 baseline top-level frames unmutated — cross-ref `mcp__pencil__batch_get({ readDepth: 1 })` against `.planning/research/exports/v2.0/end-of-phase-23/id-inventory.json`
- PEN-INVENTORY.md `## Variant Evidence (Phase 24)`, `## Token Extensions (Phase 24)`, and `### Open Flags — Phase 24 (OPEN-24-NN)` headers exist (scaffolded for Wave 2 consumers)
- User signed off on top-of-canvas placement
</verification>

<success_criteria>
- Three new top-level frames present in `design/Crito.pen` at top of canvas
- Each frame has the correct name (regex `^_Components / (Primitives|Compounds|Sections)$`)
- Stub frames (Compounds + Sections) each contain title + Phase-25-deferral note (D-37 + Pitfall 8 prevention)
- Token surface unchanged from Phase 23 (95 vars)
- 16 baseline top-level frames unmutated
- PEN-INVENTORY.md has scaffolded audit-trail tables for Phase 24
- SUMMARY.md documents pre-flight results, frame ids, VAL-24-01/06 evidence
- User has visually confirmed top-of-canvas placement
</success_criteria>

<output>
After completion, create `.planning/phases/24-layout-primitives-primitive-components/24-01-SUMMARY.md` documenting:
- Active-editor pre-flight result (D-35 evidence per call)
- `find_empty_space_on_canvas` probe outcome (Q5)
- The three new frame ids + coordinates (consumed by 24-02..24-04)
- VAL-24-01 and VAL-24-06 satisfaction with evidence per sub-test
- Token surface integrity check result
- Number of audit-trail rows added to PEN-INVENTORY.md `## Variant Evidence (Phase 24)`
- ROADMAP correction note (line 169 stale marker — to be addressed by 24-04's close)
</output>
