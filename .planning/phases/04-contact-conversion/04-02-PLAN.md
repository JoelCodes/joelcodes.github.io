---
phase: 04-contact-conversion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Footer.astro
autonomous: true

must_haves:
  truths:
    - "User can see LinkedIn and GitHub icons in footer"
    - "Clicking LinkedIn icon opens LinkedIn profile in new tab"
    - "Clicking GitHub icon opens GitHub profile in new tab"
  artifacts:
    - path: "src/components/layout/Footer.astro"
      provides: "Social links with accessible icons"
      contains: ["linkedin", "github", "target=\"_blank\"", "rel=\"noopener noreferrer\""]
  key_links:
    - from: "src/components/layout/Footer.astro"
      to: "external social profiles"
      via: "anchor tags with target=_blank"
      pattern: "aria-label.*opens in new tab"
---

<objective>
Add LinkedIn and GitHub social links to the site footer with accessible, clean icon design.

Purpose: Provide alternative ways for visitors to connect with Joel and establish credibility.
Output: Footer displays social icons that link to Joel's professional profiles.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-contact-conversion/04-CONTEXT.md
@.planning/phases/04-contact-conversion/04-RESEARCH.md
@src/components/layout/Footer.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add social links with Lucide icons to footer</name>
  <files>src/components/layout/Footer.astro</files>
  <action>
Update src/components/layout/Footer.astro:

1. Import icons from lucide-static at top of frontmatter:
   ```astro
   import { Github, Linkedin } from 'lucide-static';
   ```

2. Replace the placeholder social links section (currently has comment "Social links will be added in Phase 4") with actual links:
   - LinkedIn link: href="https://linkedin.com/in/joelshinness" (placeholder URL)
   - GitHub link: href="https://github.com/joelshinness" (placeholder URL)

3. Each link must have:
   - target="_blank" to open in new tab
   - rel="noopener noreferrer" for security (see RESEARCH.md pitfall)
   - aria-label="LinkedIn (opens in new tab)" or "GitHub (opens in new tab)" for accessibility
   - Icon rendered via Fragment: `<Fragment set:html={Linkedin} />` and `<Fragment set:html={Github} />`

4. Style the social links:
   - Flex container with gap-6 (existing structure)
   - Icon color: text-text-muted-light dark:text-text-muted-dark
   - Hover: text-accent-teal transition-colors
   - Icon size: w-5 h-5 (apply via wrapping span or CSS)

5. Add CSS for icon sizing in a style tag:
   ```css
   .social-link svg {
     width: 1.25rem;
     height: 1.25rem;
   }
   ```

Structure should look like:
```html
<div class="mt-6 flex justify-center gap-6">
  <a href="..." target="_blank" rel="noopener noreferrer"
     aria-label="LinkedIn (opens in new tab)"
     class="social-link text-text-muted-light dark:text-text-muted-dark hover:text-accent-teal transition-colors">
    <Fragment set:html={Linkedin} />
  </a>
  <a href="..." ...>
    <Fragment set:html={Github} />
  </a>
</div>
```
  </action>
  <verify>
    - Footer displays two social icons (LinkedIn, GitHub)
    - Icons are visible and properly sized
    - Hovering changes icon color to teal
    - Clicking opens link in new tab
    - `npm run build` succeeds
  </verify>
  <done>Footer shows LinkedIn and GitHub icons with proper accessibility attributes, hover states, and new-tab behavior</done>
</task>

</tasks>

<verification>
1. Start dev server: `npm run dev`
2. Navigate to any page and scroll to footer
3. Verify two social icons are visible
4. Hover over each icon, verify color changes to teal
5. Click each icon, verify it opens placeholder URL in new tab
6. Inspect HTML, verify aria-label and rel attributes are present
7. Run `npm run build` to ensure production build succeeds
</verification>

<success_criteria>
- LinkedIn icon visible in footer
- GitHub icon visible in footer
- Both icons have accessible aria-labels mentioning "opens in new tab"
- Both links have rel="noopener noreferrer" security attribute
- Icons have hover state with teal color
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-contact-conversion/04-02-SUMMARY.md`
</output>
