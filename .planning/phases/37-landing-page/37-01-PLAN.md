---
phase: 37-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/37-landing-page/37-EXTRACTION.md
autonomous: true
requirements: [PAGE-01, CONT-02]

must_haves:
  truths:
    - "Every landing section has extracted copy, background token, padding, and component roster recorded from Figma 12:2"
    - "FAQ-section presence in frame 12:2 is definitively answered (yes/no)"
    - "Any always-dark (ink-bg) landing section is identified with its literal color values"
    - "FrequencyWave placement, sizing, and section(s) are recorded"
  artifacts:
    - path: ".planning/phases/37-landing-page/37-EXTRACTION.md"
      provides: "Section-by-section extracted values (copy, bg tokens, padding at 4 breakpoints, components, on-dark flags, FrequencyWave placement, FAQ presence)"
      contains: "12:2"
  key_links:
    - from: "37-EXTRACTION.md"
      to: "Figma node 12:2"
      via: "get_design_context + get_screenshot"
      pattern: "12:2"
---

<objective>
Extract every FIDELITY-GAP value for the landing page from Figma frame `12:2` (light, canonical) and verify dark treatment against `117:103`. This is the blocking Wave 1: no `index.astro` section content can be authored without this artifact. Section geometry, copy, backgrounds, component rosters, FrequencyWave placement, always-dark section detection, and FAQ-section presence are ALL currently FIDELITY-GAPs.

Purpose: De-risk the entire phase by resolving all unknowns before implementation. The 37-UI-SPEC.md declares dozens of FIDELITY-GAPs; this plan converts them to concrete values.
Output: `.planning/phases/37-landing-page/37-EXTRACTION.md` — the source of truth for all downstream section implementation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/37-landing-page/37-CONTEXT.md
@.planning/phases/37-landing-page/37-UI-SPEC.md
@.planning/phases/37-landing-page/37-RESEARCH.md
</context>

<threat_model>
Static-site phase — no runtime attack surface introduced by extraction. This plan writes only a planning-doc markdown artifact; no code, no secrets, no user input. The one downstream security-relevant fact this plan must capture: whether any section contains external links (beyond the known Calendly CTA) that would need `rel="noopener"` (reverse-tabnabbing mitigation, D-06). Record any external URLs found in the frame so Wave 3 wires them safely. No ASVS L1 control applies to this doc-only task.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract landing structure and screenshots from Figma 12:2 and 117:103</name>
  <files>.planning/phases/37-landing-page/37-EXTRACTION.md</files>
  <read_first>
    - .planning/phases/37-landing-page/37-UI-SPEC.md (the FIDELITY-GAP list this task resolves — Section Visual Contracts, Spacing Scale exceptions table, Typography FIDELITY-GAPs, Color on-dark section question)
    - .planning/phases/37-landing-page/37-CONTEXT.md (D-10: light 12:2 canonical for copy/structure/layout; 117:103 dark colors ONLY)
    - .planning/phases/36-content-components-expandable-cards/36-EXTRACTION.md (prior extraction artifact format to mirror — node-referenced value tables)
  </read_first>
  <action>
    Confirm Figma file `1tg8wIPcvOVC5tPZ8pkGO2` is open in Figma desktop. Use figma-desktop MCP `get_design_context` on node `12:2` for full structure and `get_screenshot` on `12:2` (light) and `117:103` (dark) for reference. Use `get_metadata` for specific child node geometry where padding/width values are needed. Do NOT use `export_nodes` (broken per project memory).

    For EACH of the nine roadmap sections (Hero, Who, Three-ways, How-it-works, Automations, Proof, About, Agencies, Final CTA) record: section background fill (map to `--wl-paper`, `--wl-sea-glass`, or a literal ink hex if always-dark), vertical padding at 390/768/1440/1920, horizontal content max-width and gutters, and which `wl/` components appear (CTAButton/Eyebrow/Tag/Callout/LinkCard/Step/ServiceCard/FrequencyWave) with their content.

    Definitively answer three open questions from 37-RESEARCH.md: (1) does frame `12:2` contain a FAQ section below the nine listed (if yes, record it as section 10 with FAQItem content)? (2) is any section an always-dark ink-background surface requiring non-flippable literals like the footer (record the literal hex if so)? (3) does the About section include a photo/image and at what placement?

    Record FrequencyWave: which section(s) it decorates, its sizing (full-bleed / fixed height / absolute-positioned) and any offset. Record any external URLs present in the frame beyond the known Calendly CTA (for downstream `rel="noopener"` wiring).

    Where a value cannot be read from the frame, mark it explicitly as `[EXTRACTION GAP: <description>]` in the artifact rather than guessing — never invent values (v1.4 lesson). Save reference screenshots into `.planning/phases/37-landing-page/fidelity/` as `37-figma-12-2.png` and `37-figma-117-103.png` if MCP returns image data that can be persisted; otherwise note that Figma reference capture happens at the Wave 5 gate.
  </action>
  <verify>
    File `.planning/phases/37-landing-page/37-EXTRACTION.md` exists and contains a per-section table for all nine roadmap sections plus explicit yes/no answers for FAQ-section presence, always-dark-section presence, and About-photo presence. Run: `grep -c "12:2" .planning/phases/37-landing-page/37-EXTRACTION.md` returns > 0. Run: `grep -iE "FAQ|always-dark|FrequencyWave" .planning/phases/37-landing-page/37-EXTRACTION.md` returns matches for each keyword.
  </verify>
  <acceptance_criteria>
    - 37-EXTRACTION.md contains one section subsection for each of: Hero, Who, Three-ways, How-it-works, Automations, Proof, About, Agencies, Final CTA (source assertion: nine section headers present)
    - Each section records: background token, vertical padding at all four breakpoints (390/768/1440/1920), content max-width, wl component roster
    - The artifact contains an explicit "FAQ section present in 12:2: YES/NO" line
    - The artifact contains an explicit "Always-dark (ink-bg) section present: YES/NO" line; if YES, the literal hex is recorded
    - The artifact records FrequencyWave section placement and sizing
    - Every unreadable value is marked `[EXTRACTION GAP: ...]`, none silently omitted or invented
    - Every extracted copy string is attributed to `12:2` (never to `117:103`, per D-10)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `.planning/phases/37-landing-page/37-EXTRACTION.md` exists with all nine sections + FAQ/dark/FrequencyWave resolutions
- No invented values — all gaps flagged `[EXTRACTION GAP: ...]`
- Copy attributed to `12:2` only (D-10)
</verification>

<success_criteria>
All FIDELITY-GAPs in 37-UI-SPEC.md have either a concrete extracted value or an explicit `[EXTRACTION GAP]` marker in 37-EXTRACTION.md. Downstream section-assembly plans (37-03, 37-04) can proceed with zero design guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/37-landing-page/37-01-SUMMARY.md` recording: FAQ-section decision, always-dark-section decision, About-photo decision, FrequencyWave placement, count of extraction gaps flagged, and any external URLs found.
</output>
