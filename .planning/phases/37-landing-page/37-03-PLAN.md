---
phase: 37-landing-page
plan: 03
type: execute
wave: 3
depends_on: ["37-01", "37-02"]
files_modified:
  - src/pages/index.astro
  - scripts/check-contrast.mjs
  - .planning/phases/37-landing-page/37-COPY-GAPS.md
autonomous: true
requirements: [PAGE-01, CONT-02]

must_haves:
  truths:
    - "index.astro no longer imports the old neobrutalist components (Hero, Services, Process, About, ContactSection)"
    - "index.astro renders the first six landing sections (Hero, Who, Three-ways#services, How-it-works, Automations, Proof) from the wl component library"
    - "The Three-ways section carries id=services; only one h1 exists on the page (Hero)"
    - "All copy is verbatim from Figma 12:2 per 37-EXTRACTION.md; unreadable copy renders a visible [COPY GAP] marker and is logged"
    - "check-contrast.mjs covers every new text-on-background pair introduced by these sections and exits 0"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Landing sections 1-6 assembled from wl components"
      contains: "id=\"services\""
    - path: ".planning/phases/37-landing-page/37-COPY-GAPS.md"
      provides: "Running log of copy gaps for Joel"
    - path: "scripts/check-contrast.mjs"
      provides: "Phase 37 text-on-bg pairs"
      contains: "PHASE 37"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/lib/constants.ts"
      via: "frontmatter import"
      pattern: "from '../lib/constants'"
    - from: "src/pages/index.astro"
      to: "#services section"
      via: "section id anchor"
      pattern: "id=\"services\""
---

<objective>
Rewrite `src/pages/index.astro`, replacing all five old neobrutalist component imports with the first six Figma landing sections composed from the existing `wl/` library: Hero, Who, Three-ways (`id="services"`), How-it-works, Automations, Proof. All values come from `37-EXTRACTION.md` (Wave 1). All copy is verbatim from Figma `12:2`; any unreadable copy becomes a visible `[COPY GAP]` marker logged to `37-COPY-GAPS.md`. Extend `check-contrast.mjs` for any new text-on-background pairs these sections introduce.

Purpose: Build the upper half of the primary conversion surface (PAGE-01) with authentic copy (CONT-02). Splitting sections 1-6 here and 7-9+FAQ in 37-04 keeps each plan within the quality budget.
Output: `src/pages/index.astro` (rewritten frontmatter + sections 1-6), `37-COPY-GAPS.md` (if gaps found), extended `check-contrast.mjs`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-landing-page/37-EXTRACTION.md
@.planning/phases/37-landing-page/37-CONTEXT.md
@.planning/phases/37-landing-page/37-PATTERNS.md
@.planning/phases/37-landing-page/37-UI-SPEC.md
@.planning/phases/37-landing-page/37-02-SUMMARY.md
</context>

<threat_model>
ASVS L1: reverse tabnabbing (V-Communication) — any Book-a-call CTA in these sections uses `CTAButton href={BOOKING_URL} ... target="_blank" rel="noopener"` (D-06); the `rel="noopener"` is mandatory on every new-tab link. Email CTAs use `mailto:${CONTACT_EMAIL}` (no external tab, no tabnabbing surface). No forms, no user input, no async data, no secrets — the page is fully static. Copy-gap markers are inline literal text, not interpolated user data, so no injection surface. FrequencyWave and any inline SVG are decorative and `aria-hidden` (no script). High-severity gate: confirm no `target="_blank"` without `rel="noopener"` in the rendered sections.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.astro frontmatter and assemble Hero + Who + Three-ways sections</name>
  <files>src/pages/index.astro, .planning/phases/37-landing-page/37-COPY-GAPS.md</files>
  <read_first>
    - src/pages/index.astro (FULL current file lines 1-16 — the five old imports to remove and the BaseLayout shell to preserve)
    - .planning/phases/37-landing-page/37-EXTRACTION.md (Hero, Who, Three-ways rows — bg tokens, padding at 4 breakpoints, copy, component roster, always-dark flags)
    - .planning/phases/37-landing-page/37-PATTERNS.md (index.astro section: import pattern, section-with-id pattern, heading hierarchy contract, CTA patterns, copy-gap marker pattern, section background pattern)
    - src/components/wl/CTAButton.astro (props: variant, icon, target, rel — now available from 37-02)
    - src/components/wl/ServiceCard.astro (props: variant default/highlight, kicker, benefit, title, headingLevel, icon slot)
    - src/components/wl/Eyebrow.astro (on-light/on-dark variant)
  </read_first>
  <action>
    Replace the frontmatter entirely: keep `import BaseLayout from '../layouts/BaseLayout.astro';`; remove the five old imports (Hero, Services, Process, About, ContactSection — they become Phase 41 dead code, do NOT delete the files); add `import { BOOKING_URL, CONTACT_EMAIL } from '../lib/constants';` and imports for the wl components these three sections use per 37-EXTRACTION.md (at minimum CTAButton, Eyebrow, ServiceCard; add Tag/Callout/FrequencyWave only if extraction shows them here). Update the BaseLayout `title` to the extracted page title (verbatim from 12:2 if present, else keep a sensible existing-style title and log a copy gap).

    Build section 1 Hero: a single `<h1 class="wl-display-hero">` (the ONLY h1 on the page), lead subtitle, and the hero CTAs from extraction — Book-a-call uses `variant="solid" icon="calendar" target="_blank" rel="noopener"` with `href={BOOKING_URL}`; any secondary/email CTA uses `href={\`mailto:${CONTACT_EMAIL}\`} variant="ghost" icon="mail"`. Apply Hero background token and per-breakpoint padding from extraction. Hero has no anchor id.

    Build section 2 Who: `<h2 class="wl-heading-h2">` plus body copy; bg + padding from extraction. No id.

    Build section 3 Three-ways: `<section id="services">` (scroll-margin-top:64px applies automatically — do NOT add it). `<h2 class="wl-heading-h2">` heading; ServiceCard components with `headingLevel={3}` and the default/highlight variants per extraction. bg + padding + grid gap from extraction.

    For every copy value that is unreadable/missing in 37-EXTRACTION.md (marked `[EXTRACTION GAP]`), render the visible copy-gap marker (`background: yellow; outline: 3px solid red; padding: 4px; color:#000;`) with a `[COPY GAP: <section> — <description>]` string, and append a row to `.planning/phases/37-landing-page/37-COPY-GAPS.md` (create it with the header table from 37-UI-SPEC.md if it does not exist). NEVER invent copy.

    Use only `--wl-*` tokens (semantic flip — one token per property, no `dark:` pairs on wl colors). If extraction flags an always-dark section here, use the non-flippable footer-precedent literals + CTAButton `ghost-on-dark` + Eyebrow on-dark.
  </action>
  <verify>
    Run `grep -c "id=\"services\"" src/pages/index.astro` returns 1. Run `grep -c "<h1" src/pages/index.astro` returns 1. Run `grep -cE "import (Hero|Services|Process|About|ContactSection) from" src/pages/index.astro` returns 0. Run `grep -c "from '../lib/constants'" src/pages/index.astro` returns 1. Run `npm run build` — exits 0.
  </verify>
  <acceptance_criteria>
    - Zero old neobrutalist component imports remain in index.astro (source assertion: grep returns 0)
    - Exactly one `<h1>` on the page, in the Hero (source assertion)
    - `<section id="services">` present for Three-ways; `scroll-margin-top` NOT re-declared in index.astro (source assertion)
    - All Book-a-call CTAs render `href={BOOKING_URL} target="_blank" rel="noopener"`; email CTAs render `mailto:${CONTACT_EMAIL}` (source assertion)
    - Section headings are `<h2>`; ServiceCard uses `headingLevel={3}` (source assertion)
    - Every `[EXTRACTION GAP]` from 37-EXTRACTION.md becomes a visible `[COPY GAP]` marker AND a row in 37-COPY-GAPS.md; no invented copy (behavior assertion)
    - No `dark:` utility pairs on wl color tokens (source assertion)
    - `npm run build` exits 0 (CLI)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Assemble How-it-works + Automations + Proof sections</name>
  <files>src/pages/index.astro, .planning/phases/37-landing-page/37-COPY-GAPS.md</files>
  <read_first>
    - src/pages/index.astro (current state after Task 1 — sections 1-3 present)
    - .planning/phases/37-landing-page/37-EXTRACTION.md (How-it-works, Automations, Proof rows — bg tokens, padding, copy, Step usage, stat/testimonial content, FrequencyWave placement if in these sections)
    - src/components/wl/Step.astro (number + slot prose; headingLevel default 3)
    - src/components/wl/FrequencyWave.astro (class passthrough for sizing/positioning; stroke via CSS, theme-adaptive)
    - .planning/phases/37-landing-page/37-PATTERNS.md (section patterns, copy-gap markers)
  </read_first>
  <action>
    Append three sections to index.astro (no anchor ids — none is a nav target): section 4 How-it-works using `Step` components per extraction (each Step number + slot prose; heading level default 3); section 5 Automations (content block — use the components extraction names, e.g. Callout/Tag/LinkCard, only if the frame shows them); section 6 Proof (stats + testimonial block — use the extracted type classes for stat numbers and testimonial body).

    Place `FrequencyWave` in whichever section(s) 37-EXTRACTION.md assigned it, using the extracted sizing `class` (full-bleed / fixed-height / absolute). The component handles its own `aria-hidden` and theme-adaptive stroke — do not re-author it, pass only the `class` prop.

    Apply each section's background token and per-breakpoint padding from extraction. Each section heading is `<h2 class="wl-heading-h2">`. Continue the copy-gap protocol: unreadable copy → visible marker + 37-COPY-GAPS.md row, never invented.
  </action>
  <verify>
    Run `npm run build` — exits 0. Run `grep -c "<h2" src/pages/index.astro` returns >= 5 (Who, Three-ways, How-it-works, Automations, Proof — Hero uses h1). Run `grep -c "Step" src/pages/index.astro` returns >= 1 if extraction confirms Step usage in How-it-works. Confirm no second `<h1>` was added: `grep -c "<h1" src/pages/index.astro` still returns 1.
  </verify>
  <acceptance_criteria>
    - Sections How-it-works, Automations, Proof appended with `<h2>` headings and no anchor ids (source assertion)
    - Still exactly one `<h1>` on the page (source assertion: grep returns 1)
    - FrequencyWave placed per extraction with the extracted sizing class and no re-authoring (source assertion)
    - Stat/testimonial type classes match the extracted sizes from 37-EXTRACTION.md (source assertion)
    - Any unreadable copy is a visible `[COPY GAP]` marker + logged; none invented (behavior assertion)
    - `npm run build` exits 0 (CLI)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend check-contrast.mjs for new landing text-on-background pairs</name>
  <files>scripts/check-contrast.mjs</files>
  <read_first>
    - scripts/check-contrast.mjs (palette constants at lines 81-151; existing PAIRS matrix lines 171-348; Phase 36 additions block lines 284-348 — the exact format to mirror: `[fg_hex, bg_hex, 'label', threshold, textUseBool]`)
    - .planning/phases/37-landing-page/37-EXTRACTION.md (section background tokens — identify any text-on-bg pair not already in the matrix, e.g. a new always-dark section, or a stat/testimonial color on a specific surface)
    - .planning/phases/37-landing-page/37-UI-SPEC.md (Contrast Script Extension table — the candidate pairs and which are already present)
  </read_first>
  <action>
    Add a `// ── PHASE 37 ADDITIONS ──` block to `scripts/check-contrast.mjs` (mirroring the Phase 36 block format). Add array entries `[fg, bg, 'label', 4.5, true]` ONLY for text-on-background pairs the six landing sections introduce that are NOT already present in the matrix (per the UI-SPEC Contrast Script Extension table, most paper/sea-glass pairs already exist). If extraction confirmed an always-dark landing section, add its on-dark text pairs using the literal hex (non-flippable). If sections 1-6 introduce no new pairs, add a comment stating so explicitly (do not add duplicate rows). Reuse the existing palette constants (L_INK, D_INK, L_SUB, L_ACCENT, L_SEA_GLASS, L_PAPER, etc.) — do not redefine hex values inline.
  </action>
  <verify>
    Run `node scripts/check-contrast.mjs` — exits 0. Run `grep -c "PHASE 37" scripts/check-contrast.mjs` returns >= 1.
  </verify>
  <acceptance_criteria>
    - A `PHASE 37 ADDITIONS` block exists in check-contrast.mjs (source assertion)
    - Every new text-on-background pair from sections 1-6 (per 37-EXTRACTION.md) is either already in the matrix or added in this block; if none new, a comment says so (source assertion)
    - Any always-dark section text pair uses literal hex, not semantic tokens (source assertion)
    - `node scripts/check-contrast.mjs` exits 0 (CLI)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `node scripts/check-contrast.mjs` exits 0
- Exactly one `<h1>`; `id="services"` present; zero old-component imports
- All copy gaps visible + logged; zero invented copy
- Every `target="_blank"` carries `rel="noopener"` (threat gate)
</verification>

<success_criteria>
`index.astro` renders the first six landing sections from the wl library with verbatim Figma copy (gaps flagged), correct heading hierarchy, the `#services` anchor, safe Calendly CTAs, and passing contrast. The lower sections (37-04) can now append below.
</success_criteria>

<output>
After completion, create `.planning/phases/37-landing-page/37-03-SUMMARY.md` recording: sections built, wl components used, count of copy gaps logged, whether an always-dark section exists, and any new contrast pairs added.
</output>
