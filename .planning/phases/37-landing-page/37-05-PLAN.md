---
phase: 37-landing-page
plan: 05
type: execute
wave: 5
depends_on: ["37-04"]
files_modified:
  - .planning/phases/37-landing-page/fidelity/
  - .planning/phases/37-landing-page/37-COPY-GAPS.md
autonomous: false
requirements: [PAGE-01, CONT-02, IA-04]

must_haves:
  truths:
    - "Direct navigation to /#services and /#about lands on the correct section with 64px header offset"
    - "Scroll-spy toggles aria-current=location accurately: active in #services/#about, no active state elsewhere or on other pages"
    - "All 37-COPY-GAPS.md items are resolved or signed off by Joel"
    - "Eight fidelity screenshots (4 breakpoints x 2 themes) are captured and compared to Figma 12:2/117:103"
    - "Joel approves the fidelity comparison; approved deviations are logged"
  artifacts:
    - path: ".planning/phases/37-landing-page/fidelity/"
      provides: "8 rendered screenshots (37-{light,dark}-{390,768,1440,1920}.png) — permanent gate artifacts"
    - path: ".planning/phases/37-landing-page/37-COPY-GAPS.md"
      provides: "Resolved/signed-off copy gap log"
  key_links:
    - from: "rendered / at 4 breakpoints x 2 themes"
      to: "Figma 12:2 (light) + 117:103 (dark)"
      via: "screenshot comparison"
      pattern: "fidelity"
---

<objective>
Run the three manual gates that close Phase 37: the anchor-nav functional check (IA-04), the copy-gap audit (CONT-02, blocking), and the fidelity gate (PAGE-01, blocking — Joel approval). Capture eight screenshots (390/768/1440/1920 × light/dark), compare against Figma `12:2` and `117:103`, log pre-approved deviations, and obtain Joel's sign-off.

Purpose: These verifications require human judgment (visual comparison, copy resolution, scroll-behavior observation) and cannot be automated. This is the discipline that was absent in v1.4 — a required phase gate, not optional review.
Output: 8 screenshots in `.planning/phases/37-landing-page/fidelity/`; resolved `37-COPY-GAPS.md`; Joel approval recorded in the summary.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-landing-page/37-UI-SPEC.md
@.planning/phases/37-landing-page/37-VALIDATION.md
@.planning/phases/37-landing-page/37-COPY-GAPS.md
@.planning/phases/37-landing-page/37-04-SUMMARY.md
</context>

<threat_model>
No new attack surface — this plan captures screenshots and records approvals; it writes no application code. The only security-relevant confirmation to make during the manual review: visually verify that all Book-a-call CTAs open Calendly in a new tab (D-06 behavior) and that no unexpected external links appear without new-tab safety. This is a spot-check of the `rel="noopener"` mitigation already enforced in code (37-02/03/04), not a new control. No forms, no secrets, no user input.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Capture 8 fidelity screenshots and Figma reference frames</name>
  <files>.planning/phases/37-landing-page/fidelity/</files>
  <read_first>
    - .planning/phases/37-landing-page/37-UI-SPEC.md (Fidelity Gate section — 8-screenshot naming, storage path, capture method, pre-approved deviations)
    - .planning/phases/36-content-components-expandable-cards/36-06-SUMMARY.md (prior fidelity gate mechanics — Playwright fullPage capture + figma-desktop MCP get_screenshot; export_nodes broken)
    - tests/accessibility/helpers.ts (settleAnimations — call before each screenshot to avoid mid-animation captures)
  </read_first>
  <action>
    Ensure a production preview is running (`npm run build && npm run preview`, or dev server at localhost:4321). Write a throwaway Playwright script (NOT committed) that, for each width in {390,768,1440,1920}, sets the viewport, `page.goto('/')`, calls `settleAnimations(page)`, and captures `page.screenshot({ path: '.planning/phases/37-landing-page/fidelity/37-light-<w>.png', fullPage: true })`. Repeat with `browser.newContext({ colorScheme: 'dark' })` for the four dark captures (`37-dark-<w>.png`). Result: 8 PNGs.

    Capture Figma reference frames via figma-desktop MCP `get_screenshot` on node `12:2` (light) and `117:103` (dark). Do NOT use `export_nodes` (broken per project memory). Save/note them alongside the rendered set for side-by-side comparison.

    Confirm the eight rendered files exist and are non-empty.
  </action>
  <verify>
    Run `ls .planning/phases/37-landing-page/fidelity/37-light-390.png .planning/phases/37-landing-page/fidelity/37-light-768.png .planning/phases/37-landing-page/fidelity/37-light-1440.png .planning/phases/37-landing-page/fidelity/37-light-1920.png .planning/phases/37-landing-page/fidelity/37-dark-390.png .planning/phases/37-landing-page/fidelity/37-dark-768.png .planning/phases/37-landing-page/fidelity/37-dark-1440.png .planning/phases/37-landing-page/fidelity/37-dark-1920.png` — all 8 exist. Run `find .planning/phases/37-landing-page/fidelity -name '*.png' -size +0c | wc -l` returns >= 8.
  </verify>
  <acceptance_criteria>
    - Eight PNGs exist: `37-{light,dark}-{390,768,1440,1920}.png`, all non-empty (CLI assertion)
    - Each was captured `fullPage: true` after `settleAnimations` (behavior assertion)
    - Figma reference frames obtained via figma-desktop MCP `get_screenshot` on `12:2` and `117:103` (behavior assertion)
    - The throwaway capture script is NOT committed (behavior assertion)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Anchor-nav functional gate + copy-gap audit</name>
  <what-built>
    The full landing page at `/` with nine (or ten) sections, the `#services`/`#about` anchors, the IntersectionObserver scroll-spy, and all Calendly/email CTAs. `37-COPY-GAPS.md` lists every unreadable-copy gap flagged during assembly.
  </what-built>
  <how-to-verify>
    With the preview running at localhost:4321:
    1. Navigate directly to `/#services` — confirm the page scrolls to the Three-ways section with the 64px header offset (heading not hidden behind the sticky header).
    2. Navigate directly to `/#about` — confirm it scrolls to the About section with the offset.
    3. Scroll slowly through the page — confirm the Services nav link turns accent (`aria-current="location"`) only while the Three-ways section is on screen, and the About nav link only while the About section is on screen.
    4. Confirm NO nav link is active while in Hero, How-it-works, Automations, Proof, Agencies, or Final CTA (D-03 honesty rule).
    5. Visit `/showcase` (if built) or any non-landing page — confirm the Services/About nav links do NOT show a false active state (scroll-spy is inert off the landing page).
    6. Confirm every Book-a-call CTA opens Calendly in a NEW TAB.
    7. Review `37-COPY-GAPS.md` and the visible `[COPY GAP]` markers on the page: Joel resolves each item — supplying verbatim copy to insert, or signing off to ship the gap as-is.
  </how-to-verify>
  <resume-signal>
    Type "approved" if anchor nav behaves correctly AND all copy gaps are resolved/signed-off; otherwise list the nav issues or copy resolutions to apply (these route to a gap-closure follow-up before the fidelity gate).
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fidelity gate — Joel approval (blocking)</name>
  <what-built>
    Eight rendered screenshots of `/` (4 breakpoints × light/dark) and the Figma reference frames `12:2` (light) and `117:103` (dark).
  </what-built>
  <how-to-verify>
    Compare each rendered screenshot against its Figma counterpart at the same breakpoint and theme:
    - Light: `37-light-{390,768,1440,1920}.png` vs Figma `12:2` at each width.
    - Dark: `37-dark-{390,768,1440,1920}.png` vs Figma `117:103` at each width.
    Check section order, copy, backgrounds (paper/sea-glass/any always-dark), padding, ServiceCard/Step/FrequencyWave placement, and the FAQ section (if built).

    Pre-approved deviations to log before review (do not treat as failures):
    1. Scroll-spy active nav state (`text-wl-accent` via `aria-current`) — Figma static frames show no active state; this is a derived phase decision (D-02).
    2. Any remaining `[COPY GAP]` markers (should be resolved in Task 2; if any remain, they carry Joel's sign-off).
    3. Any FrequencyWave sizing/position delta if the extracted dimensions didn't map to an available sizing utility.
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark the fidelity gate passed and the phase complete; otherwise list the specific per-breakpoint deltas to fix (these route to a gap-closure follow-up plan).
  </resume-signal>
</task>

</tasks>

<verification>
- 8 screenshots captured and stored permanently in `fidelity/`
- Anchor nav verified: correct offset, accurate scroll-spy, no false active states
- All copy gaps resolved or signed off
- Joel approves the fidelity comparison; deviations logged
</verification>

<success_criteria>
The landing page matches Figma `12:2`/`117:103` at all four breakpoints in both themes to Joel's satisfaction; anchor nav and scroll-spy behave per D-01–D-04; no unresolved copy gaps. Phase 37 is complete — PAGE-01, CONT-02, IA-03, IA-04 all satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/37-landing-page/37-05-SUMMARY.md` recording: the anchor-nav gate result, the copy-gap resolutions, the fidelity-gate approval (with logged deviations), and the final list of the 8 screenshot artifacts.
</output>
