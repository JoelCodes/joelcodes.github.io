---
phase: 37-landing-page
plan: 04
type: execute
wave: 4
depends_on: ["37-03"]
files_modified:
  - src/pages/index.astro
  - tests/accessibility/landing.spec.ts
autonomous: true
requirements: [PAGE-01, CONT-02, IA-04]

must_haves:
  truths:
    - "index.astro renders the About (id=about), Agencies, and Final CTA sections plus the FAQ section IF 37-EXTRACTION.md confirmed one exists in 12:2"
    - "The About section carries id=about; scroll-spy can now toggle aria-current on the About nav link"
    - "A durable axe spec covers the landing page at / in both light and dark mode with zero violations"
    - "Direct navigation to /#services and /#about lands on the correct section with 64px header offset"
    - "All copy is verbatim from Figma 12:2; unreadable copy renders a visible [COPY GAP] marker and is logged"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Landing sections 7-9 (+ FAQ if present), completing all nine roadmap sections"
      contains: "id=\"about\""
    - path: "tests/accessibility/landing.spec.ts"
      provides: "Durable light + dark axe coverage for /"
      contains: "colorScheme"
  key_links:
    - from: "src/pages/index.astro"
      to: "#about section"
      via: "section id anchor observed by SiteHeader scroll-spy"
      pattern: "id=\"about\""
    - from: "tests/accessibility/landing.spec.ts"
      to: "/"
      via: "page.goto + AxeBuilder"
      pattern: "page.goto\\('/'\\)"
---

<objective>
Complete `index.astro` with the remaining landing sections — About (`id="about"`), Agencies, Final CTA, and a FAQ section IF Wave 1 extraction confirmed one exists in frame `12:2` — then add the durable light+dark axe spec that covers the production landing page. After this plan, all nine (or ten) Figma sections are assembled and the anchor nav is fully wired end to end (`#services` from 37-03, `#about` here).

Purpose: Finish PAGE-01 assembly and IA-04 (the `#about` anchor completes scroll-spy coverage), and lay durable QUAL-01 groundwork with a landing axe spec that persists past this phase (unlike the temporary isolation specs of Phases 35-36).
Output: `src/pages/index.astro` (sections 7-9 + optional FAQ), `tests/accessibility/landing.spec.ts` (new, durable).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-landing-page/37-EXTRACTION.md
@.planning/phases/37-landing-page/37-CONTEXT.md
@.planning/phases/37-landing-page/37-PATTERNS.md
@.planning/phases/37-landing-page/37-UI-SPEC.md
@.planning/phases/37-landing-page/37-03-SUMMARY.md
</context>

<threat_model>
ASVS L1: reverse tabnabbing (V-Communication) — the Final CTA section's Book-a-call button uses `href={BOOKING_URL} target="_blank" rel="noopener"` (D-06); every new-tab link carries `rel="noopener"`. FAQ (if present) uses native `<details name="faq-landing">` — zero JS, no injection surface. No forms, no user input, no async data, no secrets. The axe spec runs against a static build and asserts accessibility, not a security boundary. High-severity gate: confirm no `target="_blank"` without `rel="noopener"` in the Final CTA or any lower section.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Assemble About (id=about) + Agencies + Final CTA sections + FAQ if present</name>
  <files>src/pages/index.astro, .planning/phases/37-landing-page/37-COPY-GAPS.md</files>
  <read_first>
    - src/pages/index.astro (current state after 37-03 — sections 1-6 present; append below)
    - .planning/phases/37-landing-page/37-EXTRACTION.md (About, Agencies, Final CTA rows; the definitive FAQ-section YES/NO answer; About-photo answer; always-dark flag for Final CTA)
    - .planning/phases/37-landing-page/37-PATTERNS.md (section-with-id pattern for #about, CTA patterns, always-dark section pattern, copy-gap markers)
    - src/components/wl/FAQItem.astro (props: question, groupName, slot for answer — only used if FAQ section confirmed)
    - src/components/wl/CTAButton.astro (variants incl. ghost-on-dark for always-dark Final CTA)
    - src/components/wl/Eyebrow.astro (on-dark variant for always-dark sections)
  </read_first>
  <action>
    Append to index.astro: section 7 About as `<section id="about">` (scroll-margin-top:64px applies automatically) with `<h2 class="wl-heading-h2">`, Joel bio copy, and the photo/image if 37-EXTRACTION.md confirmed one (place per extracted layout); section 8 Agencies (secondary-audience pitch, `<h2>`, content per extraction); section 9 Final CTA (`<h2>`, closing copy, Book-a-call `CTAButton href={BOOKING_URL} variant="solid" icon="calendar" target="_blank" rel="noopener"` and any email CTA `mailto:${CONTACT_EMAIL}`).

    IF AND ONLY IF 37-EXTRACTION.md answered "FAQ section present in 12:2: YES", append a FAQ section (section 10) built from `FAQItem` components with `groupName="faq-landing"` (exclusive-open via native `<details name>`), `question` prop and slotted answer per extracted copy. If the extraction answered NO, do NOT build a FAQ section and do NOT import FAQItem — record in the summary that FAQ was absent.

    If 37-EXTRACTION.md flagged Final CTA (or any of these sections) as an always-dark ink-background surface, use the non-flippable footer-precedent literals, CTAButton `variant="ghost-on-dark"`, Eyebrow on-dark variant, and literal focus-ring `focus-visible:outline-[#4FB3B8]`.

    Continue the copy-gap protocol: unreadable copy → visible marker (`background:yellow; outline:3px solid red;`) + a row in 37-COPY-GAPS.md; never invent copy.
  </action>
  <verify>
    Run `grep -c "id=\"about\"" src/pages/index.astro` returns 1. Run `grep -c "<h1" src/pages/index.astro` returns 1 (still only Hero). Run `npm run build` — exits 0. If FAQ confirmed: `grep -c "faq-landing" src/pages/index.astro` returns >= 1; if not confirmed: returns 0.
  </verify>
  <acceptance_criteria>
    - `<section id="about">` present for the About section (source assertion)
    - Still exactly one `<h1>` on the page (source assertion)
    - Final CTA Book-a-call uses `href={BOOKING_URL} target="_blank" rel="noopener"` (source assertion)
    - FAQ section built with `FAQItem` + `groupName="faq-landing"` ONLY if 37-EXTRACTION.md answered YES; otherwise absent and noted (behavior assertion)
    - Always-dark section (if flagged) uses non-flippable literals + ghost-on-dark + on-dark Eyebrow (source assertion)
    - All nine (or ten) roadmap sections now present in index.astro (behavior assertion)
    - Any unreadable copy is a visible `[COPY GAP]` marker + logged; none invented (behavior assertion)
    - `npm run build` exits 0 (CLI)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create durable landing axe spec (light + dark)</name>
  <files>tests/accessibility/landing.spec.ts</files>
  <read_first>
    - tests/accessibility/dark-mode.spec.ts (exact structural analog for the browser.newContext dark-mode axe pattern)
    - tests/accessibility/helpers.ts (settleAnimations helper — import and call before every .analyze())
    - tests/accessibility/axe-tests.spec.ts (confirm the existing homepage light-mode test — landing.spec.ts complements it via explicit light AND dark contexts; both coexist, no dedup needed)
  </read_first>
  <action>
    Create `tests/accessibility/landing.spec.ts` — a DURABLE spec (a header comment must state it is NOT deleted after the gate, unlike Phase 35/36 isolation specs). Import `{ test, expect }` from `@playwright/test`, `AxeBuilder` from `@axe-core/playwright`, and `settleAnimations` from `./helpers`. Use wcag tag set `['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']`.

    Two tests in a `describe('Landing Page Accessibility')` block: (1) light mode — `page.goto('/')`, `settleAnimations(page)`, assert `AxeBuilder(...).withTags(wcagTags).analyze()` returns zero violations; (2) dark mode — `browser.newContext({ colorScheme: 'dark' })`, `page.goto('/')`, assert `html` has class `dark` (FOUC script applied it), `settleAnimations(page)`, assert zero violations, then `context.close()`. Mirror the exact structure of dark-mode.spec.ts.

    If the axe run surfaces a violation caused ONLY by a visible `[COPY GAP]` marker's derived yellow/red styling (contrast), note it in the summary as an expected-until-copy-resolved condition rather than editing the marker away — the markers are intentional and resolved at the fidelity/copy-gap gate (Gate 5). Any OTHER violation must be fixed in index.astro before this plan closes.
  </action>
  <verify>
    Run `npm run test:a11y` — the two new landing tests pass (light + dark zero violations), and the pre-existing suite (7 tests from Phase 36) still passes for a total of 9. Run `grep -c "colorScheme: 'dark'" tests/accessibility/landing.spec.ts` returns 1.
  </verify>
  <acceptance_criteria>
    - `tests/accessibility/landing.spec.ts` exists with a header comment stating it is durable (source assertion)
    - Contains a light-mode test (`page.goto('/')`) and a dark-mode test (`browser.newContext({ colorScheme: 'dark' })` + `.dark` class assertion) (source assertion)
    - Uses `settleAnimations` before every `.analyze()` and the full WCAG 2.2 AA tag set (source assertion)
    - `npm run test:a11y` passes with 9 total tests (7 prior + 2 new), zero violations on `/` in both themes — excepting any documented copy-gap-marker contrast note (CLI/behavior assertion)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npm run test:a11y` — landing light + dark zero violations (9 tests total)
- `id="about"` present; exactly one `<h1>`; all nine/ten sections assembled
- FAQ built iff extraction confirmed it
- Every `target="_blank"` carries `rel="noopener"` (threat gate)
</verification>

<success_criteria>
All nine (or ten) Figma landing sections are assembled in `index.astro` with verbatim copy (gaps flagged), the `#about` anchor completes scroll-spy coverage, and a durable axe spec proves zero violations in both themes. The page is ready for the manual anchor-nav and fidelity gates (37-05).
</success_criteria>

<output>
After completion, create `.planning/phases/37-landing-page/37-04-SUMMARY.md` recording: sections built, FAQ present/absent decision, About-photo handling, always-dark-section handling, axe test count (should be 9), and total copy gaps logged across 37-03 + 37-04.
</output>
