---
phase: 27-thank-you-contact-reconstruction
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - PAGE-05

must_haves:
  truths:
    - "After this plan, `mcp__pencil__get_variables({})` returns 100 tokens — token surface UNCHANGED from end of Phase 26 (Phase 27 adds ZERO tokens per D-91 reuse of `color-semantic-text-error`)."
    - "Phase 24 baseline ID `nwJk7` (Primitive/Input/Default) parent properties (reusable, width, layout, gap, padding, fill, stroke, cornerRadius) are unchanged from Phase 24 end-state; only descendant `oCeJP` is mutated (label-slot extension per D-91)."
    - "`oCeJP` descendant of `nwJk7` is converted from leaf text node into a horizontal-layout frame containing 3 children: (1) original `label-text` text node, (2) new `required-mark` text node with `enabled:false` default + content `*` + fill `#eb5757ff` (color-semantic-text-error), (3) new `optional-text` text node with `enabled:false` default + content `(optional)` + fill `#52525bff` (color-semantic-text-secondary). Fallback path documented if type-conversion fails per RESEARCH Open Question 2."
    - "`_Components / Primitives` parent frame `avgor` contains 7 NEW reusable children: `Primitive / Input / Textarea` (1 variant) + `Primitive / Select / Default` + `Primitive / Select / Focus` + `Primitive / Select / Error` + `Primitive / Checkbox / Default` + `Primitive / Checkbox / Focus` + `Primitive / Checkbox / Error`."
    - "`_Components / Compounds` parent frame `t67DU6` contains 1 NEW reusable child: `Compound / CheckboxGroup` with 3-slot signature (legend-slot / options-slot / helper-slot); options-slot contains 3 ref instances of `Primitive / Checkbox / Default` as placeholder content per Phase 25 D-53 enabled-with-placeholder pattern."
    - "`Primitive / Input / Textarea` slot signature mirrors Input/Default: label / control / helper / errorSlot; control vertical-layout with default height 120 (4-row default) to distinguish multi-line from single-line."
    - "`Primitive / Select / Default` control is horizontal-layout with placeholder text + lucide-native `chevron-down` Icon (Pattern A per Phase 24 D-44) at right via `justifyContent: space_between`; Focus/Error variants override control stroke per D-22 forward consistency (cyan-500 for Focus, red-400 for Error)."
    - "`Primitive / Checkbox / Default` has 2-slot horizontal signature: 16×16 `box-slot` (white fill + grey 1px stroke + radius 4) + `label-slot` (Inter 16/400 primary text); Focus/Error variants override box-slot stroke."
    - "`Compound / CheckboxGroup` has sibling Pencil note inside `t67DU6` documenting slot signature + consumer use (Plan 27-02 Contact Solutions field instance overrides options-slot to 5 children); vertical-stack default per Claude's Discretion."
    - "PEN-INVENTORY has a new `## Calendly Wiring Map (Phase 27)` section seeded with both Calendly URLs (Thank-you: https://calendly.com/joelshinness; Contact sidebar: https://calendly.com/me--juoi/discovery-call) per D-93."
    - "PEN-INVENTORY Variant Evidence (Phase 27) sub-section contains rows for: label-slot extension on nwJk7's oCeJP (required-mark + optional-text); Textarea variant; Select Default/Focus/Error trinity; Checkbox Default/Focus/Error trinity; CheckboxGroup compound + 3 slots; sibling note IDs — ~15+ rows total."
    - "Phase 23/24/25/26 baseline IDs (35-list from `expected_baseline_ids_unmutated` in end-of-phase-26/id-inventory.json) confirmed intact via `batch_get` at plan open AND plan close (excluding intentional `oCeJP` mutation)."
    - "Document-level `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `\"No layout problems.\"` at plan close (text-clipping false-positives per Phase 24 quirk documented in 27-00-SUMMARY.md, NOT mitigated)."
    - "D-89: Hybrid library strategy — Textarea as Input variant; Select/Checkbox as sibling primitives; CheckboxGroup as compound"
    - "D-90: Default/Focus/Error trinity for each new primitive"
    - "D-91: Required-field indicator via label-slot mechanic — `enabled:true/false` text node toggle"
    - "D-92: Selects ship closed-state only; chevron-down via lucide native Pattern A + Primitive/Icon/16 instance"
    - "D-93: Section/CTA + sibling Pencil note + PEN-INVENTORY `## Calendly Wiring Map (Phase 27)`"
    - "D-101: Foundation plan first — primitives/compound must exist before consumer plans 27-01/02"
    - "D-102: Plan 27-00 closes WITHOUT a user-calibration gate (matches Phase 26 D-86 foundation-plan precedent)"
    - "D-103: Pre-flight `mcp__pencil__get_editor_state` before every mutating call"
    - "D-104: PEN-INVENTORY extension pattern — Variant Evidence rows for every new node + `## Calendly Wiring Map (Phase 27)` new section"
  artifacts:
    - path: "design/Crito.pen"
      provides: "Primitive/Input label-slot extension (oCeJP -> frame with required-mark + optional-text children); Primitive/Input/Textarea variant; Primitive/Select Default/Focus/Error trinity; Primitive/Checkbox Default/Focus/Error trinity; Compound/CheckboxGroup with sibling note."
      contains: "Primitive / Input / Textarea, Primitive / Select / Default, Primitive / Select / Focus, Primitive / Select / Error, Primitive / Checkbox / Default, Primitive / Checkbox / Focus, Primitive / Checkbox / Error, Compound / CheckboxGroup"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Variant Evidence (Phase 27) sub-section with literal-to-semantic bindings for all new nodes; new `## Calendly Wiring Map (Phase 27)` section with both Calendly URLs; OPEN-24-04/-07/-08/-09 update notes (partial resolution pointer)."
  key_links:
    - from: "Primitive / Input / Default (nwJk7)"
      to: "oCeJP label-slot extension (label-text + required-mark + optional-text)"
      via: "in-place type-conversion (text -> frame) via batch_design Update + Insert children; fallback Delete+recreate documented"
      pattern: "oCeJP.*layout.*horizontal.*required-mark.*enabled.*false"
    - from: "Primitive / Select / Default control"
      to: "lucide chevron-down Icon (Pattern A native + Primitive/Icon/16 instance)"
      via: "type:icon library:lucide icon:chevron-down width:16 height:16 inside select control (horizontal layout justifyContent: space_between)"
      pattern: "library.*lucide.*chevron-down"
    - from: "Compound / CheckboxGroup options-slot"
      to: "Primitive / Checkbox / Default refs (placeholder 3-ref default)"
      via: "options-slot frame contains 3 type:ref children pointing to Checkbox/Default id; D-52 typed-slot PREFERRED + sibling note belt-and-suspenders"
      pattern: "options-slot.*ref.*Primitive.*Checkbox"
    - from: "PEN-INVENTORY § Calendly Wiring Map (Phase 27)"
      to: "Both Calendly URLs (Thank-you joelshinness + Contact sidebar me--juoi/discovery-call)"
      via: "new section seeded by Plan 27-00; consumed by Plans 27-01 + 27-02 sibling notes"
      pattern: "Calendly Wiring Map.*calendly.com/joelshinness.*calendly.com/me--juoi"
---

<objective>
Phase 27 foundation: ship five new library entries inside `design/Crito.pen` so Plans 27-01 (Thank-you) and 27-02 (Contact 8-field form) can compose them without inline markup or new tokens.

The five entries per D-89 Hybrid library strategy:
1. **Update** existing `Primitive / Input / Default` (nwJk7) label-slot `oCeJP` from leaf text node into a 3-child horizontal-layout frame [label-text + required-mark (enabled:false) + optional-text (enabled:false)] per D-91. This is the FIRST in-place mutation of a Phase 24 baseline descendant in v2.0 — pitfall-aware (Pitfall 2: do NOT touch nwJk7 parent properties; Open Question 2: probe type-conversion feasibility, fall back to Delete+recreate if needed).
2. **Insert** `Primitive / Input / Textarea` variant inside `avgor` — reuses Input slot signature with vertical-layout control + default height 120 for multi-line content.
3. **Insert** `Primitive / Select / Default + Focus + Error` trinity inside `avgor` per D-89 + D-90 — closed-state-only per D-92 (chevron-down via lucide native Pattern A; popover defer to code milestone).
4. **Insert** `Primitive / Checkbox / Default + Focus + Error` trinity inside `avgor` per D-89 + D-90 — 2-slot box-slot + label-slot signature.
5. **Insert** `Compound / CheckboxGroup` inside `t67DU6` per D-89 — composes Checkbox refs; legend-slot + options-slot (3 placeholder refs) + helper-slot per Phase 25 D-53 enabled-with-placeholder + D-52 sibling note belt-and-suspenders.

Also seed PEN-INVENTORY `## Calendly Wiring Map (Phase 27)` per D-93 with both Calendly URLs for Plans 27-01 + 27-02 to consume.

**Purpose:** Plan 27-02's 8-field form (Name + Email + Company + Challenges + Solutions + Budget + Timeline + Message) cannot compose without these primitives. Plan 27-01 reuses existing Phase 26 Section/CTA but inherits the label-slot mechanic transitively if any future override needs the mark. Foundation-first per D-101 — Plan 27-02 has the deepest primitive dependencies; building primitives last would block composition.

**Output:** A `.pen` file with 7 new reusable child nodes inside `avgor` + 1 new compound child inside `t67DU6` + sibling Pencil notes + 1 in-place UPDATE to `oCeJP` (descendant of nwJk7). Token surface unchanged at 100. PEN-INVENTORY extended with Variant Evidence (Phase 27) rows + new `## Calendly Wiring Map (Phase 27)` section + OPEN-24-04/-07/-08/-09 partial-resolution update notes. NO user-calibration gate at plan close per D-102 (agent-deterministic foundation work).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md
@.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md
@.planning/phases/27-thank-you-contact-reconstruction/27-VALIDATION.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-26/id-inventory.json
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight active-editor + token surface + baseline ID verification</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-103 (pre-flight enforcement)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Standard Stack "Baseline IDs that MUST remain unmutated" (the 35-ID list) + § Example 1 (pre-flight pattern)
    - `.planning/research/exports/v2.0/end-of-phase-26/id-inventory.json` — `expected_baseline_ids_unmutated` (35 IDs); end-of-phase-26 token count = 100
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` § "Active-Editor Pre-flight Result" pattern
    - `.planning/research/PEN-INVENTORY.md` § "Token Extensions (Phase 26)" — confirms 100-token surface at end of Phase 26
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103:
    Call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert `activeEditor` string ends with `design/Crito.pen`. Halt + surface to user on mismatch per Pitfall 8.

    Step 2 — Token surface baseline:
    Call `mcp__pencil__get_variables({})`. Assert returned token count is exactly 100 (Phase 26 close baseline). Halt if drift. Record count in 27-00-SUMMARY scratch.

    Step 3 — Get schema if needed:
    Call `mcp__pencil__get_editor_state({ include_schema: true })` once to load Pencil 2.13 schema into context for subsequent mutating calls.

    Step 4 — Get guidelines for new primitive authoring:
    Call `mcp__pencil__get_guidelines({ topic: "design-system" })` to confirm current slot mechanics + variant authoring conventions before inserting new primitives.

    Step 5 — Baseline ID intactness:
    Call `mcp__pencil__batch_get({ nodeIds: [...], readDepth: 1 })` with the 35-ID `expected_baseline_ids_unmutated` list from `.planning/research/exports/v2.0/end-of-phase-26/id-inventory.json`:
    - Crito source frames (15): `MIXGf`, `QdwxP`, `kicJ8`, `ujMLJ`, `VleVl`, `cl8tt`, `w1m3x`, `DzqTm`, `cYlRH`, `Y2isa`, `WDGxc`, `1nLS3`, `Jmdw0`, `maDc3`, `IKAu3`
    - Library parents + Foundations (4): `RpGbe`, `avgor`, `t67DU6`, `g9oRa5`
    - Phase 24 primitives (12): `M7eUr`, `YJhRv`, `gQa2R`, `AvKtA`, `V4Dx4i`, `ATJK9`, `nwJk7`, `j0FxQZ`, `EQaMf`, `yRvGb`, `u7NmaS`, `dpO5Y`
    - Phase 25 additions (5): `hIWuC`, `AzmgQ`, `G0wNOc`, `Xs0Hs`, `t40xct`
    - Phase 26 additions (2): `Hs5rc`, `N1jo3i` (Section/CTA + Section/NavBack)
    - Phase 26 page frames (2): `b7Hgy` (FAQ), `csXky` (404)

    Step 6 — Read `nwJk7` descendants to capture the exact `oCeJP` structure BEFORE mutation:
    Call `mcp__pencil__batch_get({ nodeIds: ['nwJk7'], readDepth: 3 })`. Record `oCeJP`'s current properties (type:text, content, fontFamily, fontSize, fontWeight, fill, lineHeight) in 27-00-SUMMARY scratch — these become the `label-text` child in Task 2.

    No mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token surface: 100 (Phase 26 baseline). 35 baseline IDs intact. oCeJP current structure recorded."
  </verify>
  <acceptance_criteria>
    - `mcp__pencil__get_editor_state({ include_schema: false })` returned `activeEditor` ending in `design/Crito.pen`.
    - `mcp__pencil__get_variables({})` returned exactly 100 tokens.
    - `mcp__pencil__get_guidelines({ topic: "design-system" })` returned successfully and schema is loaded.
    - `mcp__pencil__batch_get` on all 35 baseline IDs returned existing nodes with names matching expected (no node-missing errors).
    - `oCeJP` current structure (type/content/typography/fill) is recorded in 27-00-SUMMARY scratch (feeds Task 2 reconstruction).
    - No mutations issued during Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight assertions all pass; baseline token count + structural IDs confirmed intact; oCeJP baseline structure captured; subsequent tasks may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Build Primitive/Input label-slot extension (UPDATE oCeJP via batch_design with type-conversion probe + fallback)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-91 (required-mark mechanic) + D-103 (pre-flight)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 1 (label-slot extension via in-place UPDATE) + § Open Questions #2 (type-conversion feasibility probe + fallback) + § Pitfall 2 (do NOT mutate nwJk7 parent properties) + § Pitfall 7 (no `$<token>` references in batch_design) + § Example 3 (concrete batch_design shape)
    - `.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md` § "Input Variants Built" (oCeJP = label slot in nwJk7; current Inter 14/400 #141f39ff)
    - Task 0 scratch: captured oCeJP baseline structure
  </read_first>
  <action>
    Step 1 — Pre-flight: Re-call `mcp__pencil__get_editor_state({ include_schema: false })`. Halt on mismatch per D-103.

    Step 2 — PROBE type-conversion feasibility (per RESEARCH Open Question 2):
    Issue `mcp__pencil__batch_design` with the in-place type-conversion UPDATE on `oCeJP`:

    ```js
    Update("oCeJP", {
      type: "frame",
      layout: "horizontal",
      gap: 4,
      alignItems: "baseline",
      padding: 0
    })

    labelText = I("oCeJP", {
      type: "text",
      name: "label-text",
      content: "<oCeJP-original-content from Task 0 scratch — likely 'Label'>",
      fontFamily: "Inter",
      fontSize: 14,
      fontWeight: "400",
      fill: "#141f39ff",
      lineHeight: 1.4
    })

    requiredMark = I("oCeJP", {
      type: "text",
      name: "required-mark",
      content: "*",
      enabled: false,
      fontFamily: "Inter",
      fontSize: 14,
      fontWeight: "400",
      fill: "#eb5757ff"
    })

    optionalText = I("oCeJP", {
      type: "text",
      name: "optional-text",
      content: "(optional)",
      enabled: false,
      fontFamily: "Inter",
      fontSize: 14,
      fontWeight: "400",
      fill: "#52525bff"
    })
    ```

    Pitfall 7 reminder: ALL fills are LITERAL hex (`#141f39ff`, `#eb5757ff`, `#52525bff`) — NO `$<token>` references in batch_design. PEN-INVENTORY Variant Evidence rows (Task 6) audit-trail the literal-to-semantic bindings.

    Step 3 — FALLBACK if type-conversion fails (RESEARCH Open Question 2 fallback path):
    If the Update step throws (Pencil 2.13 rejects text -> frame in-place), execute the Delete+recreate path:
    - `Delete("oCeJP")` — original leaf text node removed.
    - Capture the new generated ID from the next Insert call.
    - `I(nwJk7, { type: "frame", name: "label-slot", layout: "horizontal", gap: 4, alignItems: "baseline", padding: 0, position: <same-as-original-oCeJP> })` — record returned new ID as `<newLabelSlotId>`.
    - Insert the 3 children (label-text + required-mark + optional-text) into `<newLabelSlotId>` with the same property shape as above.
    - Document the ID change in 27-00-SUMMARY scratch + raise OPEN-27-NN noting baseline-ID drift on label-slot (consumer-phase = Plan 27-02 instance authors must use `<newLabelSlotId>` instead of `oCeJP`).

    Step 4 — Defensive verification (Pitfall 2 — verify nwJk7 parent UNCHANGED):
    Call `mcp__pencil__batch_get({ nodeIds: ['nwJk7'], readDepth: 1 })`. Assert these properties match Phase 24 baseline exactly: `reusable: true`, `width`, `layout: vertical`, `gap`, `padding`, `fill`, `stroke`, `cornerRadius`. If any drift, halt + surface to user. Record the verification result in 27-00-SUMMARY scratch.

    Step 5 — Capture the 3 new child IDs (label-text, required-mark, optional-text) and parent ID (oCeJP if preserved; new ID if fallback) for Task 2 Textarea inheritance + Plan 27-02 instance authoring.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ['oCeJP-or-new-label-slot-id'], readDepth: 2 })` returns a horizontal-layout frame with 3 text children: `label-text` (enabled default, primary color), `required-mark` (enabled:false, content `*`, red error color), `optional-text` (enabled:false, content `(optional)`, secondary color). `batch_get(['nwJk7'])` shows parent properties unchanged from Phase 24 baseline.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `batch_design` call (UPDATE or Delete+Insert fallback) completed without unrecoverable error.
    - `oCeJP` (or `<newLabelSlotId>` if fallback path) is now a horizontal-layout frame with `gap: 4`, `alignItems: "baseline"`, `padding: 0`.
    - Frame contains exactly 3 text children: `label-text` (enabled:true default; content matches original oCeJP content from Task 0 scratch; fill `#141f39ff`), `required-mark` (enabled:false; content `*`; fill `#eb5757ff`), `optional-text` (enabled:false; content `(optional)`; fill `#52525bff`).
    - `mcp__pencil__batch_get({ nodeIds: ['nwJk7'], readDepth: 1 })` confirms `nwJk7` parent properties (reusable, width, layout, gap, padding, fill, stroke, cornerRadius) match Phase 24 baseline — NO mutation to nwJk7 parent per Pitfall 2.
    - If fallback path executed, the ID change is recorded in 27-00-SUMMARY scratch + OPEN-27-NN row drafted for Task 6 PEN-INVENTORY extension.
    - All 3 new child IDs (and `<newLabelSlotId>` if fallback) recorded in 27-00-SUMMARY scratch.
  </acceptance_criteria>
  <done>
    Primitive/Input label-slot extension shipped. Required-mark + optional-text mechanic available for Plan 27-02 form instance authoring per D-97.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Primitive/Input/Textarea variant inside avgor (single-variant per D-89 + D-90)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-89 (Hybrid strategy — Textarea as Input variant) + D-90 (single Textarea variant; Focus/Error inherit visually)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 2 (Textarea variant batch_design shape with control height 120) + § Pitfall 7 (literal hex only)
    - `.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md` § Input Variants (label/control/helper/errorSlot structure of nwJk7)
    - Task 1 scratch (label-slot extension IDs — Textarea label inherits same structure)
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` § Pencil Typed-Slot Mechanics (D-52 typed slot is suggestion-only; sibling-note belt-and-suspenders)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`.

    Step 2 — `batch_design` Insert new reusable component `Primitive / Input / Textarea` as child of `avgor`. Follow RESEARCH § Pattern 2 exactly:

    Outer frame:
    - `type: "frame"`, `name: "Primitive / Input / Textarea"`, `reusable: true`, `width: 320`, `layout: "vertical"`, `gap: 16`, `padding: 0`, `fill: "transparent"`.

    Child 1 — label-slot frame (inherits Task 1 extension shape):
    - `type: "frame"`, `name: "label-slot"`, `layout: "horizontal"`, `gap: 4`, `alignItems: "baseline"`, `padding: 0`.
    - Contains 3 text children matching Task 1 oCeJP pattern: label-text (enabled:true, "Label", Inter 14/400 #141f39ff), required-mark (enabled:false, "*", #eb5757ff), optional-text (enabled:false, "(optional)", #52525bff).

    Child 2 — control frame (DIFFERS from Input/Default — vertical-layout, taller):
    - `type: "frame"`, `name: "control"`, `layout: "vertical"`, `gap: 0`, `padding: [12, 16]` (12 top/bottom — taller than Input/Default's 8; 16 sides), `width: "fill_container"`, `height: 120` (default-tall multi-line; instances may override per consumer rows), `fill: "#ffffffff"`, `stroke: "#d4d4d8ff"`, `strokeWidth: 1`, `cornerRadius: 10`.
    - Contains one text node: name "placeholder", content "Multi-line placeholder text", Inter 16/400 #52525bff, lineHeight 1.625.

    Child 3 — helper-slot:
    - `type: "text"`, `name: "helper-slot"`, content "Optional helper text", `enabled: true` (default), Inter 14/400 #52525bff, lineHeight 1.4.

    Child 4 — errorSlot:
    - `type: "text"`, `name: "errorSlot"`, content "Error message placeholder", `enabled: false` (default off; Plan 27-02 instances enable for Message field if validation fails per OPEN-24-09), Inter 14/400 `#eb5757ff`, lineHeight 1.4.

    Record `<textareaVariantId>` + 4 slot child IDs (label-slot id + 3 label-slot children IDs + control id + placeholder id + helper id + errorSlot id) in 27-00-SUMMARY scratch.

    Pitfall 7 reminder: All values LITERAL (no `$<token>`). Variant Evidence rows in Task 6 bind to semantic tokens.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ['<textareaVariantId>'], readDepth: 2 })` returns a reusable frame named "Primitive / Input / Textarea" with parent `avgor`, 4 named child slots (label-slot + control + helper-slot + errorSlot), control height 120, label-slot 3-child horizontal layout matching Task 1 structure.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<textareaVariantId>'])` returns a node named "Primitive / Input / Textarea" with `reusable: true`, parent `avgor`, `width: 320`, vertical auto-layout, gap 16.
    - 4 slot children present: label-slot (horizontal frame with 3 text children matching Task 1 extension pattern), control (vertical-layout, height 120, padding [12, 16], fill #ffffffff, stroke #d4d4d8ff 1px, cornerRadius 10), helper-slot (text Inter 14/400 #52525bff, enabled:true), errorSlot (text Inter 14/400 #eb5757ff, enabled:false).
    - control contains one text node "placeholder" with content "Multi-line placeholder text" (Inter 16/400 #52525bff).
    - `batch_get(['avgor'], readDepth: 1)` confirms avgor now has 1 additional reusable child (Phase 24 baseline + Textarea variant).
    - `batch_get(['nwJk7'], readDepth: 1)` confirms nwJk7 parent properties STILL unchanged from Phase 24 baseline.
    - All new IDs recorded in 27-00-SUMMARY scratch.
  </acceptance_criteria>
  <done>
    Primitive/Input/Textarea variant is first-class library entry — Plan 27-02 Challenges + Message fields can instance it.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Primitive/Select Default/Focus/Error trinity inside avgor (chevron-down via lucide native Pattern A)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-89 (Select as sibling primitive) + D-90 (Default/Focus/Error trinity) + D-92 (closed-state only; chevron Pattern A native)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 3 (Select trinity batch_design shape — control horizontal layout justifyContent space_between with placeholder + chevron Icon/16) + § Pitfall 7 (literal hex only)
    - `.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md` (Primitive/Icon variants 16/20/24/32 — `EQaMf` is Icon/16; Pattern A lucide-native glyph swap)
    - `.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md` § Input Variants (Focus/Error stroke override pattern: Focus = #15bee3ff cyan 2px; Error = #eb5757ff red 2px)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 3 reusable variants in one chunked call (Pitfall 9 — keep batches ≤ 25 ops; this is ~18 ops):

    **Select/Default** (outer + 4 slots + chevron icon + placeholder text):
    Outer: `type: "frame"`, `name: "Primitive / Select / Default"`, `reusable: true`, `width: 320`, `layout: "vertical"`, `gap: 16`, `padding: 0`.

    label-slot: horizontal frame mirroring Task 1 label-slot pattern (3 children: label-text "Label" + required-mark enabled:false + optional-text enabled:false).

    control: `type: "frame"`, `name: "control"`, `layout: "horizontal"`, `justifyContent: "space_between"` (pushes chevron right), `alignItems: "center"`, `gap: 8`, `padding: [8, 16]` (matches Input/Default control padding per Phase 24 D-22), `width: "fill_container"`, `fill: "#ffffffff"`, `stroke: "#d4d4d8ff"`, `strokeWidth: 1`, `cornerRadius: 10`. Contains:
    - Placeholder text: name "placeholder", content "Select option", Inter 16/400 #52525bff, lineHeight 1.625.
    - Chevron icon (Pattern A native per D-92): `type: "icon"`, `name: "chevron-icon"`, `library: "lucide"`, `icon: "chevron-down"`, `width: 16`, `height: 16`, `fill: "#52525bff"` (color-semantic-text-secondary).

    helper-slot: text, content "Optional helper text", enabled:true, Inter 14/400 #52525bff.
    errorSlot: text, content "Error message placeholder", enabled:false, Inter 14/400 #eb5757ff.

    **Select/Focus** (Default + control stroke override):
    Same shape as Default; outer `reusable: true` (sibling-variant per Phase 25 D-43 — variants are siblings).
    Override on control: `stroke: "#15bee3ff"` (cyan-500 — matches Phase 24 D-22 Input/Focus consistency), `strokeWidth: 2`.
    name: "Primitive / Select / Focus".

    **Select/Error** (Default + control stroke override + errorSlot enabled):
    Same shape; override on control: `stroke: "#eb5757ff"` (red-400), `strokeWidth: 2`.
    Override on errorSlot: `enabled: true`, content "This field is required" (placeholder error message).
    name: "Primitive / Select / Error".

    All 3 variants share the same label-slot/helper-slot/errorSlot/chevron pattern — only control stroke + errorSlot enabled differs across the trinity (mirrors Phase 24 D-22 Input pattern exactly).

    Record all 3 outer IDs + per-variant slot IDs (`<selectDefaultId>`, `<selectFocusId>`, `<selectErrorId>`, plus chevron-icon IDs for each) in 27-00-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ['<selectDefaultId>','<selectFocusId>','<selectErrorId>'], readDepth: 2 })` returns 3 reusable frames named "Primitive / Select / Default", "Primitive / Select / Focus", "Primitive / Select / Error", each with parent `avgor`, label-slot + control + helper-slot + errorSlot structure. Control of each contains a chevron icon with `library: "lucide"` + `icon: "chevron-down"`. Default control stroke is #d4d4d8ff 1px; Focus is #15bee3ff 2px; Error is #eb5757ff 2px.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design` Insert completed without error.
    - 3 new reusable frames named "Primitive / Select / Default", "Primitive / Select / Focus", "Primitive / Select / Error" exist as children of `avgor`.
    - Each variant has the 4-slot signature (label-slot horizontal extension + control with chevron + helper-slot + errorSlot).
    - Control of each contains placeholder text "Select option" (Inter 16/400 #52525bff) + chevron-down icon (`library: "lucide"`, `icon: "chevron-down"`, 16×16, fill #52525bff).
    - Default stroke: `#d4d4d8ff` 1px; Focus stroke: `#15bee3ff` 2px; Error stroke: `#eb5757ff` 2px (matches Phase 24 D-22 Input trinity pattern exactly).
    - Error variant's errorSlot has `enabled: true` with content "This field is required".
    - NO Disabled variant shipped (D-90 carry-forward — out of scope).
    - NO Open-dropdown popover variant shipped (D-92 explicit — code-side runtime concern).
    - All Select IDs recorded in scratch.
  </acceptance_criteria>
  <done>
    Primitive/Select Default/Focus/Error trinity shipped. Plan 27-02 Budget + Timeline fields can instance Select/Default.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build Primitive/Checkbox Default/Focus/Error trinity inside avgor (box-slot + label-slot 2-slot signature)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-89 (Checkbox as sibling primitive) + D-90 (Default/Focus/Error trinity; NO Checked state — deferred to future state-coverage phase)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 4 (Checkbox 2-slot signature box-slot + label-slot batch_design shape; 16×16 box; Focus/Error box-slot stroke overrides)
    - `.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md` Focus/Error stroke override pattern carry-forward
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 3 reusable variants (Default + Focus + Error) inside `avgor`:

    **Checkbox/Default:**
    - Outer: `type: "frame"`, `name: "Primitive / Checkbox / Default"`, `reusable: true`, `width: "fit_content"`, `layout: "horizontal"`, `gap: 12`, `alignItems: "center"`, `padding: 0`.
    - Child 1 — box-slot: `type: "frame"`, `name: "box-slot"`, `width: 16`, `height: 16`, `fill: "#ffffffff"`, `stroke: "#d4d4d8ff"` (color-semantic-border-default), `strokeWidth: 1`, `cornerRadius: 4`. Contains empty children array (checked state would insert a checkmark Icon — out of scope per D-90).
    - Child 2 — label-slot: `type: "text"`, `name: "label-slot"`, content "Option label", Inter 16/400 #141f39ff, lineHeight 1.625.

    **Checkbox/Focus:**
    Same shape as Default; outer `reusable: true`.
    Override on box-slot: `stroke: "#15bee3ff"` (cyan-500), `strokeWidth: 2`.
    name: "Primitive / Checkbox / Focus".

    **Checkbox/Error:**
    Same shape; override on box-slot: `stroke: "#eb5757ff"` (red-400), `strokeWidth: 2`.
    name: "Primitive / Checkbox / Error".

    Record all 3 outer IDs + box-slot + label-slot IDs per variant in 27-00-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ['<checkboxDefaultId>','<checkboxFocusId>','<checkboxErrorId>'], readDepth: 2 })` returns 3 reusable frames "Primitive / Checkbox / Default", "Primitive / Checkbox / Focus", "Primitive / Checkbox / Error" as children of `avgor`, each with horizontal layout (gap 12, alignItems center) + 2-slot signature (16×16 box-slot + label-slot text). Stroke overrides: Default #d4d4d8ff 1px; Focus #15bee3ff 2px; Error #eb5757ff 2px.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design` Insert completed without error.
    - 3 new reusable frames named "Primitive / Checkbox / Default", "Primitive / Checkbox / Focus", "Primitive / Checkbox / Error" exist as children of `avgor`.
    - Each variant has horizontal 2-slot signature: box-slot (16×16 frame, white fill, grey 1px stroke radius 4) + label-slot (text "Option label", Inter 16/400 #141f39ff).
    - Default stroke: `#d4d4d8ff` 1px; Focus stroke: `#15bee3ff` 2px; Error stroke: `#eb5757ff` 2px.
    - NO Checked state variant shipped (D-90 — out of scope).
    - NO Disabled variant shipped (D-90 — out of scope).
    - All Checkbox IDs recorded in scratch.
  </acceptance_criteria>
  <done>
    Primitive/Checkbox Default/Focus/Error trinity shipped — CheckboxGroup compound (Task 5) can compose Checkbox/Default refs.
  </done>
</task>

<task type="auto">
  <name>Task 5: Build Compound/CheckboxGroup inside t67DU6 (legend/options/helper 3-slot + sibling Pencil note)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-89 (CheckboxGroup as compound)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 5 (CheckboxGroup batch_design shape with 3 placeholder ref children) + § Pitfall 9 (chunk ops to ≤ 25)
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` § "Pencil Typed-Slot Mechanics" (D-52 typed-slot suggestion-only + sibling-note belt-and-suspenders pattern; uses footer-actions-slot pattern `slot: ['M7eUr','hIWuC']`)
    - Task 4 scratch: `<checkboxDefaultId>` for placeholder ref children
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert `Compound / CheckboxGroup` as child of `t67DU6`:

    Outer: `type: "frame"`, `name: "Compound / CheckboxGroup"`, `reusable: true`, `width: 320`, `layout: "vertical"`, `gap: 12`, `padding: 0`.

    Child 1 — legend-slot:
    - `type: "text"`, `name: "legend-slot"`, content "Choose options", Inter 14/400 #141f39ff, lineHeight 1.4.

    Child 2 — options-slot frame (D-52 PREFERRED typed-slot + 3 placeholder Checkbox refs per Phase 25 D-53 enabled-with-placeholder):
    - `type: "frame"`, `name: "options-slot"`, `slot: ["<checkboxDefaultId>"]` (typed suggestion per D-52 PREFERRED — accepts Checkbox/Default refs), `enabled: true`, `layout: "vertical"`, `gap: 12` (matches v1.3 `space-y-3` Tailwind class observation per CONTEXT Claude's Discretion).
    - Contains 3 ref children (placeholder content):
      - Ref 1: `{ type: "ref", ref: "<checkboxDefaultId>", descendants: { "<checkbox-label-slot-id>": { content: "Option 1" } } }`
      - Ref 2: same shape, label "Option 2"
      - Ref 3: same shape, label "Option 3"
    (Plan 27-02 instance overrides options-slot contents to 5 Checkbox refs with v1.3 Solutions labels: AI / Automations / Web Apps / Consultation / Not Sure per D-97.)

    Child 3 — helper-slot:
    - `type: "text"`, `name: "helper-slot"`, content "Optional helper text", `enabled: false` (default off), Inter 14/400 #52525bff.

    Step 3 — Sibling Pencil note inside `t67DU6` (D-52 belt-and-suspenders per Phase 25 precedent):
    `I(t67DU6, { type: "note", content: "Compound / CheckboxGroup — slot signature: legend-slot (text, body-sm typography, REQUIRED — group-level prompt like HTML <legend>); options-slot (frame, typed suggestion: Primitive/Checkbox/Default refs; vertical-stack gap 12 default per Claude's Discretion / v1.3 space-y-3; Plan 27-02 Contact Solutions field instance overrides to 5 Checkbox refs with v1.3 labels AI/Automations/Web Apps/Consultation/Not Sure per D-97; horizontal layout possible if cl8tt raster probe reveals different shape); helper-slot (text, body-sm secondary, OPTIONAL — enabled:false default per Phase 25 D-53). No per-option legend (legend-slot is group-level). D-89 Hybrid library strategy — CheckboxGroup composes Checkbox refs (distinct from Compound/Card which composes images + text via slots).", fontFamily: "Inter", fontSize: 12, width: 1200 })`

    Record `<checkboxGroupId>` + legend-slot + options-slot + helper-slot IDs + sibling note ID + the 3 placeholder ref-child IDs in 27-00-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ['<checkboxGroupId>'], readDepth: 3 })` returns a reusable frame "Compound / CheckboxGroup" as child of `t67DU6` with vertical layout + 3 slots (legend-slot text, options-slot frame, helper-slot text). options-slot has `slot: ['<checkboxDefaultId>']` typed suggestion AND contains 3 ref children pointing to Checkbox/Default with descendants override for label content. Sibling Pencil note exists as child of t67DU6 (NOT inside CheckboxGroup). helper-slot has `enabled: false` default.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<checkboxGroupId>'])` returns a reusable frame named "Compound / CheckboxGroup" with parent `t67DU6`, vertical auto-layout, gap 12.
    - 3 slots present: legend-slot (text, "Choose options", Inter 14/400 #141f39ff), options-slot (frame, vertical layout gap 12, `slot: ['<checkboxDefaultId>']` typed suggestion, contains 3 ref children with Checkbox/Default + descendants override labels "Option 1/2/3"), helper-slot (text, "Optional helper text", enabled:false, Inter 14/400 #52525bff).
    - Sibling Pencil note exists as child of `t67DU6` (NOT inside CheckboxGroup itself), `fontSize: 12`, `width: 1200`, content describes slot signature + Plan 27-02 consumer override + D-89 rationale.
    - `batch_get(['t67DU6'], readDepth: 1)` confirms t67DU6 now has 2 additional children (CheckboxGroup compound + sibling note) on top of Phase 25 baseline (Compound/Card t40xct + its sibling note iypPb).
    - Pitfall O5/O6 prevention: NOT a generalized "form-section" or "fieldset" component — CheckboxGroup specifically composes Checkbox refs.
    - All IDs recorded in scratch.
  </acceptance_criteria>
  <done>
    Compound/CheckboxGroup shipped — Plan 27-02 Solutions field can instance it with 5-option override per D-97.
  </done>
</task>

<task type="auto">
  <name>Task 6: Plan close — PEN-INVENTORY extensions (Variant Evidence + Calendly Wiring Map + OPEN updates) + token verification + snapshot_layout sweep + 27-00-SUMMARY.md</name>
  <files>
    - .planning/research/PEN-INVENTORY.md
    - .planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md
    - design/Crito.pen (read-only validation)
  </files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-104 (PEN-INVENTORY extension pattern) + D-102 (no user-calibration gate at plan close — agent-deterministic)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Validation Architecture (Phase 27 validates list) + § Pitfall 5 (snapshot_layout text-clipping false-positive) + § Pitfall 10 (Variant Evidence row count tally)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` § PEN-INVENTORY Updates — pattern reference
    - `.planning/research/PEN-INVENTORY.md` — locate existing Variant Evidence sections to extend with "(Phase 27)" sub-section; locate Open Flags sections; identify OPEN-24-04/-07/-08/-09 rows for update notes
    - All scratch IDs collected across Tasks 1-5
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`.

    Step 2 — Token surface verification (Phase 27 ships ZERO new tokens):
    `mcp__pencil__get_variables({})`. Assert count remains 100. Halt if drift. Record in 27-00-SUMMARY.

    Step 3 — Final baseline ID intactness re-check:
    `mcp__pencil__batch_get` on the 35-ID `expected_baseline_ids_unmutated` list (Task 0 list). Assert all unchanged EXCEPT for `oCeJP` (the intentional descendant mutation per Task 1) — and even then, `nwJk7` parent unchanged.

    Step 4 — `batch_get` on all 7 new primitive variants + 1 compound (Textarea + 3 Select + 3 Checkbox + CheckboxGroup) — confirm all reusable + correctly named + parent `avgor` (primitives) or `t67DU6` (compound).

    Step 5 — `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document level. Expected: `"No layout problems."` per Phase 24/25/26 precedent. Then per-frame snapshots on the 8 new library entries to document text-clipping false-positives (NOT mitigated — Pitfall 5 known benign).

    Step 6 — PEN-INVENTORY extensions per D-104:

    Edit `.planning/research/PEN-INVENTORY.md`:

    A. NEW sub-section in Variant Evidence: `### Variant Evidence (Phase 27)`. Add rows for:
    - **Primitive/Input label-slot extension** (oCeJP → frame with 3 children OR `<newLabelSlotId>` if fallback): rows for label-text + required-mark + optional-text bindings (Inter 14/400, fills #141f39ff / #eb5757ff / #52525bff → color-semantic-text-primary / -error / -secondary; gap 4, padding 0, alignItems baseline). Annotation: "Phase 27 D-91 required-mark mechanic; nwJk7 parent properties unchanged per Pitfall 2; fallback path documented if type-conversion failed."
    - **Primitive/Input/Textarea**: outer (width 320, gap 16, vertical) + label-slot (inherits Task 1 structure) + control (vertical, padding [12, 16], height 120, fill #ffffffff, stroke #d4d4d8ff 1px, cornerRadius 10) + placeholder text (Inter 16/400 #52525bff) + helper-slot (Inter 14/400 #52525bff) + errorSlot (Inter 14/400 #eb5757ff, enabled:false). Annotation: "D-89 Textarea-as-Input-variant per Hybrid library strategy."
    - **Primitive/Select Default/Focus/Error** (~3 rows × ~6 properties each — outer / label-slot / control / chevron-icon / helper / errorSlot): chevron `library: lucide, icon: chevron-down` Pattern A per D-92 + Phase 24 D-44. Stroke override pattern matches Phase 24 D-22 Input trinity exactly. Annotation: "D-89 Select-as-sibling-primitive; D-92 closed-state only."
    - **Primitive/Checkbox Default/Focus/Error** (~3 rows × 2 slots each): box-slot 16×16, fill #ffffffff, cornerRadius 4; stroke trinity (Default #d4d4d8ff 1px / Focus #15bee3ff 2px / Error #eb5757ff 2px). Annotation: "D-89 Checkbox-as-sibling-primitive; D-90 no Checked state (out of scope)."
    - **Compound/CheckboxGroup**: outer (width 320, gap 12, vertical) + legend-slot text (Inter 14/400 #141f39ff) + options-slot frame (vertical gap 12, typed slot `<checkboxDefaultId>`, 3 placeholder refs) + helper-slot text (Inter 14/400 #52525bff, enabled:false) + sibling Pencil note. Annotation: "D-89 CheckboxGroup-as-compound; D-52 PREFERRED typed-slot + belt-and-suspenders note."

    B. NEW top-level section `## Calendly Wiring Map (Phase 27)` per D-93:
    ```
    ## Calendly Wiring Map (Phase 27)

    Single mechanism (sibling Pencil note + PEN-INVENTORY cross-reference) documenting Calendly URLs for both Phase 27 consumers.

    | Consumer | Component instance | Calendly URL | v1.3 source line |
    |---|---|---|---|
    | Thank-you page Section/CTA-Calendly | Section/CTA (Hs5rc) instance — actions-slot Button "Skip the wait - book a call" | https://calendly.com/joelshinness | src/pages/thank-you.astro line 35 |
    | Contact page sidebar Card | Compound/Card (t40xct) instance — footer-actions-slot Button "Book a Call" | https://calendly.com/me--juoi/discovery-call | src/components/homepage/ContactSection.astro line 191 |

    Code milestone reads this table + the sibling Pencil notes alongside each instance to wire the runtime hrefs/iframes. v2.0 .pen file does NOT depict embedded Calendly iframe (deferred per Phase 27 Deferred Ideas).
    ```

    C. OPEN flag updates (partial-resolution pointers):
    - **OPEN-24-04** (Button focus ring on real form-CTA consumer): append "Phase 27 Plan 27-02 Send Message + Book a Call Buttons are real consumers — focus ring tested at Plan 27-02 calibration gate."
    - **OPEN-24-07** (Input control padding semantic alias decision): append "Phase 27 Plan 27-00 Textarea ships padding [12, 16] continuing the literal-with-Variant-Evidence-row dual-track; semantic alias decision remains open for Phase 28+."
    - **OPEN-24-08** (Input/Focus real consumer): append "Phase 27 Plan 27-02 Contact form 8 fields × tab focus is first real consumer."
    - **OPEN-24-09** (Input/Error real consumer): append "Phase 27 Plan 27-02 Name/Email/Message validation failures are first real consumers."

    D. NEW section `## Open Flags — Phase 27 (OPEN-27-NN)` (if not already present): initialize the section. If Task 1 fallback path was used (oCeJP Delete+recreate), add row for OPEN-27-01 noting the ID drift.

    Step 7 — Write `.planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md` containing:
    - Pre-flight outcomes (Task 0): editor + token count 100 + 35 baseline IDs intact + oCeJP baseline structure captured.
    - Task 1: type-conversion probe outcome (in-place success OR fallback used); oCeJP / `<newLabelSlotId>` + 3 child IDs (label-text, required-mark, optional-text); nwJk7 parent unchanged verification; OPEN-27-01 if fallback used.
    - Task 2: `<textareaVariantId>` + slot child IDs.
    - Task 3: `<selectDefaultId>`, `<selectFocusId>`, `<selectErrorId>` + control + chevron-icon + slot IDs per variant.
    - Task 4: `<checkboxDefaultId>`, `<checkboxFocusId>`, `<checkboxErrorId>` + box-slot + label-slot IDs per variant.
    - Task 5: `<checkboxGroupId>` + legend + options + helper IDs + 3 placeholder ref-child IDs + sibling note ID.
    - Task 6 verification: token count still 100; baseline IDs intact (35); 7+1 new library entries verified; snapshot_layout document-level "No layout problems."; per-frame text-clipping quirks documented per Phase 24/25/26 precedent.
    - PEN-INVENTORY diff: Variant Evidence (Phase 27) sub-section (~15 rows); Calendly Wiring Map (Phase 27) new section; OPEN-24-04/-07/-08/-09 update notes; OPEN-27-NN if any.
    - Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md.
    - VAL outcomes: PAGE-05 foundation satisfied (form primitives in place); zero token drift; baseline integrity preserved; pre-flight active-editor compliance.
    - NO user-calibration gate per D-102 (foundation plan agent-deterministic per Phase 26 D-86 carry-forward).
  </action>
  <verify>
    `grep -A 5 "Variant Evidence (Phase 27)" .planning/research/PEN-INVENTORY.md` returns the new sub-section. `grep -A 5 "Calendly Wiring Map (Phase 27)" .planning/research/PEN-INVENTORY.md` returns the new section with both URLs. `grep -E "OPEN-24-04|OPEN-24-07|OPEN-24-08|OPEN-24-09" .planning/research/PEN-INVENTORY.md` shows appended Phase 27 update notes. `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns "No layout problems." `mcp__pencil__get_variables({})` returns 100. 27-00-SUMMARY.md exists.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `get_variables({})` returns 100 tokens (zero drift since Phase 26 close).
    - `batch_get` on 35 baseline IDs confirms intact (oCeJP descendant mutation acknowledged + nwJk7 parent unchanged).
    - `batch_get` on all 8 new library entries confirms presence + correct parent + reusable flag.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns "No layout problems." at document level.
    - PEN-INVENTORY.md has new "Variant Evidence (Phase 27)" sub-section with ≥15 rows covering all new nodes' literal-to-semantic bindings.
    - PEN-INVENTORY.md has new "Calendly Wiring Map (Phase 27)" section with both Calendly URLs explicitly listed.
    - PEN-INVENTORY.md OPEN-24-04, OPEN-24-07, OPEN-24-08, OPEN-24-09 rows have appended Phase 27 update notes (not new rows).
    - If fallback path used in Task 1, OPEN-27-01 row exists documenting ID drift.
    - 27-00-SUMMARY.md exists with all listed sections.
    - NO user-calibration AskUserQuestion called at plan close (D-102 + D-86 carry-forward).
  </acceptance_criteria>
  <done>
    Phase 27 foundation complete. 7 new primitives + 1 compound shipped; label-slot mechanic in place; Calendly Wiring Map seeded; PEN-INVENTORY audit-trail extended. Plan 27-01 (Thank-you joel-only) may now proceed.
  </done>
</task>

</tasks>

<verification>
- Active editor remained `design/Crito.pen` throughout (D-103 pre-flight on every mutating task).
- Token count remained at 100 (zero drift across Tasks 1-5 — Phase 27 adds zero tokens per D-91 reuse).
- 35 baseline IDs from `expected_baseline_ids_unmutated` are intact, with the explicit exception of `oCeJP` (descendant of nwJk7) intentionally mutated per D-91; `nwJk7` parent properties confirmed unchanged per Pitfall 2.
- 5 new library deliverables shipped: Primitive/Input label-slot extension (UPDATE) + Primitive/Input/Textarea variant + Primitive/Select trinity (3 variants) + Primitive/Checkbox trinity (3 variants) + Compound/CheckboxGroup with sibling note.
- PEN-INVENTORY extensions per D-104: Variant Evidence (Phase 27) sub-section ≥15 rows + Calendly Wiring Map (Phase 27) new section + OPEN-24-04/-07/-08/-09 partial-resolution update notes.
- Document-level `snapshot_layout` returned "No layout problems."; per-frame text-clipping false-positives documented per Phase 24/25/26 precedent (NOT mitigated).
- 27-00-SUMMARY.md exists and committed to git.
- NO user-calibration AskUserQuestion at plan close (D-102 + Phase 26 D-86 foundation-plan-no-gate).
</verification>

<success_criteria>
- `mcp__pencil__get_variables({})` returns exactly 100 tokens (zero drift).
- `mcp__pencil__batch_get(['avgor'], readDepth: 1)` shows 7 NEW reusable children since Phase 25 end: Textarea + Select × 3 + Checkbox × 3.
- `mcp__pencil__batch_get(['t67DU6'], readDepth: 1)` shows 1 NEW reusable child (CheckboxGroup) + 1 NEW sibling note.
- `mcp__pencil__batch_get(['nwJk7'], readDepth: 2)` shows label-slot `oCeJP` (or `<newLabelSlotId>` if fallback) is now a horizontal-layout frame with 3 text children (label-text + required-mark enabled:false + optional-text enabled:false); nwJk7 parent properties unchanged from Phase 24 baseline.
- `mcp__pencil__batch_get` on chevron-down nodes inside Select control returns `library: "lucide"` + `icon: "chevron-down"` (Pattern A native per D-92).
- `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns "No layout problems." at document level.
- PEN-INVENTORY.md Variant Evidence (Phase 27) sub-section exists with ≥15 audit-trail rows.
- PEN-INVENTORY.md `## Calendly Wiring Map (Phase 27)` section exists with both Calendly URLs.
- 27-00-SUMMARY.md exists with all listed sections per task 6 action.
- PAGE-05 foundation requirement satisfied (form primitives in place — Plan 27-02 form composition unblocked).
</success_criteria>

<output>
After completion, create `.planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md` containing:
- Pre-flight outcomes (Task 0): active editor + token count 100 + 35 baseline IDs intact + oCeJP baseline structure captured
- Task 1: type-conversion probe outcome (in-place success OR fallback used); 3 child IDs (label-text, required-mark, optional-text); preserved oCeJP ID OR new `<newLabelSlotId>`; nwJk7 parent unchanged verification; OPEN-27-01 row if fallback used
- Task 2: `<textareaVariantId>` + label-slot + control + placeholder + helper + errorSlot IDs
- Task 3: `<selectDefaultId>`, `<selectFocusId>`, `<selectErrorId>` + per-variant control + chevron-icon + helper + errorSlot IDs
- Task 4: `<checkboxDefaultId>`, `<checkboxFocusId>`, `<checkboxErrorId>` + per-variant box-slot + label-slot IDs
- Task 5: `<checkboxGroupId>` + legend-slot + options-slot + helper-slot + 3 placeholder ref-child IDs + sibling note ID
- Task 6: final token count = 100 (verified); baseline ID re-check outcome; 8 new library entries verified; snapshot_layout outcomes (document level + per-frame with quirk notes per Phase 24/25/26 carry-forward); PEN-INVENTORY edits diff (Variant Evidence Phase 27 rows + Calendly Wiring Map section + OPEN update notes + any OPEN-27-NN raised); commits zero new tokens
- Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md
- VAL outcomes for PAGE-05 foundation (form primitives shipped) — Plan 27-01/27-02 unblocked
</output>
