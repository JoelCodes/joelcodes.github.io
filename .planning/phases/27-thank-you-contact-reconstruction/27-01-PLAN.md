---
phase: 27-thank-you-contact-reconstruction
plan: 01
type: execute
wave: 2
depends_on:
  - 27-00
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-06
  - PAGE-09
  - PAGE-11
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "A new top-level page frame named `Thank-you` exists in `design/Crito.pen` as a direct child of the document root (NOT inside any library parent — Pitfall 3 compliance from Phase 26 Plan 26-01/02 carry-forward)."
    - "The `Thank-you` frame is positioned at the page-frame row (y ≈ -4111) via `mcp__pencil__find_empty_space_on_canvas` with `nodeId: csXky` (Phase 26 404 frame) anchor per CALIBRATION-PROTOCOL § 10.4 + D-101."
    - "The `Thank-you` frame has width 1440, vertical auto-layout with alignItems center, fill `#ffffffff` (color-semantic-bg-page literal per OPEN-23-13)."
    - "The `Thank-you` frame contains 5 stacked content blocks in top-down vertical auto-layout: (1) Section/Header ref (G0wNOc, NO descendants override per D-77 carry-forward), (2) success-message section (joel-only fresh design: Icon/32 with lucide circle-check Pattern A per D-100 + heading 'Thanks for reaching out!' + body 'I'll email you within 48 hours with next steps. Looking forward to learning more about your project!' verbatim from `src/pages/thank-you.astro` lines 22 + 27), (3) Section/CTA ref (Hs5rc, Phase 26) with descendants override for Calendly headline/Button-label + sibling Pencil note documenting Calendly URL per D-93, (4) plain-text secondary 'Return to homepage' link section per D-99 with Claude's Discretion default = plain text node, (5) Section/Footer ref (Xs0Hs, NO descendants override per D-77)."
    - "Success-message Icon is `Primitive / Icon / 32` (dpO5Y) ref instance with descendants override `library: 'lucide'`, `icon: 'circle-check'` (Pattern A native per D-100 + Phase 24 D-44 carry-forward)."
    - "Section/CTA-Calendly instance descendants override: headline-slot text = 'Skip the wait — book a call' (or v1.3 verbatim mapping); body-slot `enabled: false` per D-99 (success-message section carries body context); actions-slot Button label = 'Skip the wait - book a call' (v1.3 verbatim from `src/pages/thank-you.astro` line 38)."
    - "Calendly wiring sibling Pencil text node exists at the Section/CTA-Calendly canvas position with content containing `https://calendly.com/joelshinness` per D-93 + D-94 (PEN-INVENTORY § Calendly Wiring Map seeded in Plan 27-00 cross-references this note)."
    - "Secondary 'Return to homepage' link is a plain text node (NOT a Button/Secondary ref unless Claude's Discretion upgrade applied) with `type-semantic-body-sm` typography + `color-semantic-text-secondary` fill (#52525bff) per D-99 + v1.3 lines 43–48 muted-link styling reference."
    - "Per-section fidelity labels per D-83 recorded in 27-01-SUMMARY.md: success-message = EXACT (token-bound + v1.3 verbatim per D-99); Section/CTA-Calendly = EXACT (token-bound + v1.3 Button label verbatim); secondary-link = EXACT (token-bound + v1.3 verbatim); Header = EXACT (Phase 25 shipped, D-77 no override); Footer = EXACT (Phase 25 shipped, D-77 no override)."
    - "PAGE-11 documented as INERT for Thank-you (joel-only-no-crito-ref scope per CALIBRATION-PROTOCOL § 4.3 + § 10.5 carry-forward from Phase 26 Plan 26-02 Task 6) in 27-01-SUMMARY.md + PEN-INVENTORY Frames Inventory row."
    - "User APPROVED the composition via single AskUserQuestion calibration gate at plan close per D-102 + CALIBRATION-PROTOCOL § 4.5 (joel-only format — token-usage check vs `_Tokens & Foundations` RpGbe)."
    - "PEN-INVENTORY Frames Inventory has new row for `Thank-you` (scope: joel-only-no-crito-ref, joel_page_map: /thank-you, status: reconstructed-PHASE-27); Variant Evidence (Phase 27) sub-section extended with rows for Thank-you page-frame structural literal-to-semantic bindings."
    - "Phase 24/25/26 baseline IDs (35-list) + Plan 27-00 new library IDs (7 primitives + 1 compound) intact after this plan (Plan 27-01 instances refs; does NOT mutate library entries)."
    - "Document-level `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `\"No layout problems.\"` at plan close (text-clipping false-positives per Phase 24/25/26 quirk documented in 27-01-SUMMARY.md, NOT mitigated per Pitfall 5)."
    - "D-58: Fresh design in Crito vocab; NOT v1.3 visual mirror (NO turquoise Card + CheckCircle2 size 64 + uppercase 4xl heading + neobrutalist)"
    - "D-82 + D-98 + D-99: v1.3 content shipped verbatim (heading/body/CTA-button-label/secondary-link from src/pages/thank-you.astro)"
    - "D-100: lucide circle-check Pattern A native via Primitive/Icon/32 instance — size-32 proxy for v1.3 CheckCircle2 size=64; larger sizes (48/64) NOT added in Phase 27 per O5/O6 prevention"
    - "D-103: Pre-flight active-editor assertion before every mutating call"
    - "D-104: PEN-INVENTORY extension pattern — Frames Inventory row + Variant Evidence rows + PAGE-11 INERT note"
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `Thank-you` with 5 vertical content blocks: Section/Header ref + success-message section (Icon/32 + heading + body) + Section/CTA-Calendly ref + plain secondary link + Section/Footer ref. Calendly wiring sibling Pencil note at the canvas position."
      contains: "Thank-you page frame, success-message section, Section/CTA ref (Hs5rc) with Calendly descendants override, Calendly wiring sibling note, secondary text link, Section/Header ref G0wNOc, Section/Footer ref Xs0Hs"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for Thank-you (joel-only-no-crito-ref, status reconstructed-PHASE-27, PAGE-11 INERT); Variant Evidence (Phase 27) rows extended with Thank-you page-frame structural bindings; any new OPEN-27-NN flags raised at calibration."
  key_links:
    - from: "Thank-you page frame"
      to: "Section / Header (G0wNOc)"
      via: "ref child at top of auto-layout vertical stack; NO descendants override per D-77 carry-forward"
      pattern: "Thank-you.*ref.*G0wNOc"
    - from: "Thank-you success-message section"
      to: "Primitive / Icon / 32 (dpO5Y) with lucide circle-check Pattern A"
      via: "ref instance + descendants override library:lucide, icon:circle-check per D-100 + Phase 24 D-44"
      pattern: "library.*lucide.*circle-check"
    - from: "Thank-you page frame"
      to: "Section / CTA (Hs5rc, Phase 26)"
      via: "ref child (4th of stack) with descendants override for Calendly headline + body:enabled:false + actions-slot Button label 'Skip the wait - book a call' (v1.3 verbatim)"
      pattern: "Thank-you.*ref.*Hs5rc"
    - from: "Calendly wiring sibling Pencil note"
      to: "https://calendly.com/joelshinness (v1.3 thank-you.astro line 35 URL)"
      via: "sibling Pencil text node at Section/CTA-Calendly canvas position per D-93 belt-and-suspenders convention"
      pattern: "calendly.com/joelshinness"
    - from: "Thank-you page frame"
      to: "Section / Footer (Xs0Hs)"
      via: "ref child at bottom of auto-layout vertical stack; NO descendants override per D-77 carry-forward"
      pattern: "Thank-you.*ref.*Xs0Hs"
---

<objective>
Create the `Thank-you` page frame in `design/Crito.pen` as a fresh-design composition per D-58 + D-99 (joel-only-no-crito-ref scope — no Crito source frame for Thank-you exists per Phase 23 audit; calibration uses CALIBRATION-PROTOCOL § 4 joel-only branch).

Composition per D-99 (top → bottom):
1. `Section / Header` instance (G0wNOc, Phase 25) — Crito-source nav labels stay literal per D-77 carry-forward (Joel's v1.3 4-link override is Phase 31 work).
2. **Success-message section** (joel-only fresh design): vertical-stack centered horizontally inside 1200 max-width content frame:
   - `Primitive / Icon / 32` (dpO5Y) ref instance with descendants override `library: "lucide"` + `icon: "circle-check"` per D-100 (Pattern A native — size-32 proxy for v1.3 CheckCircle2 size=64)
   - Heading: "Thanks for reaching out!" (v1.3 verbatim, `src/pages/thank-you.astro` line 22) — `type-semantic-heading-1` typography per D-99 default (Claude's Discretion may downsize to heading-2 if visual reads oversized at calibration)
   - Body: "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!" (v1.3 verbatim, line 27) — `type-semantic-prose-paragraph-*` typography per D-71 carry-forward
3. `Section / CTA` ref (Hs5rc, Phase 26) — Calendly placeholder per D-93 + D-94 + D-99:
   - headline-slot: "Skip the wait — book a call" (or maps v1.3 button label as headline — plan-execution default ships v1.3 button label verbatim as both headline and Button label)
   - body-slot: `enabled: false` (success-message above carries body context per D-99)
   - actions-slot: Button label "Skip the wait - book a call" (v1.3 verbatim, line 38)
   - Sibling Pencil text node alongside Section/CTA at canvas position: documents Calendly URL `https://calendly.com/joelshinness` per D-93 belt-and-suspenders + cross-references PEN-INVENTORY § Calendly Wiring Map (Phase 27) seeded by Plan 27-00.
4. Optional secondary "Return to homepage" link section (plain text-link per D-99 Claude's Discretion default — `type-semantic-body-sm` + `color-semantic-text-secondary` matches v1.3 lines 43-48 small muted link). Sibling Pencil note: "Code milestone wires to `/` homepage."
5. `Section / Footer` instance (Xs0Hs, Phase 25) — Crito-source labels stay literal per D-77.

PAGE-11 INERT for Thank-you per CALIBRATION-PROTOCOL § 4.3 (joel-only branch — no Crito source raster exists to remove). Plan-close single AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 4.5 (joel-only format — token-usage check vs `_Tokens & Foundations` RpGbe).

**Purpose:** First Phase 27 per-page reconstruction. Tests existing components (Phase 25 Section/Header/Footer + Phase 26 Section/CTA — first cross-phase consumer per Phase 26 D-78 reusability claim) in production before Plan 27-02 stresses the new Phase 27 primitives in the harder crito-source-flat-raster context. Establishes the joel-only branch's first per-page-phase use outside Phase 26.

**Output:** A working `Thank-you` top-level page frame; an extended PEN-INVENTORY with Frames Inventory row + Variant Evidence (Phase 27) rows + PAGE-11 INERT note; per-section fidelity labels recorded; plan-close user APPROVED state. ONE user gate at plan close per CALIBRATION-PROTOCOL § 4.5.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md
@.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md
@.planning/phases/27-thank-you-contact-reconstruction/27-VALIDATION.md
@.planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-26/id-inventory.json
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@src/pages/thank-you.astro
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plan 27-00 outputs intact + read v1.3 Thank-you source for verbatim content</name>
  <files>design/Crito.pen (read-only); src/pages/thank-you.astro (read-only)</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md` — all Plan 27-00 IDs (7 new library entries + label-slot extension structure) + Calendly Wiring Map confirmation
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-103 (pre-flight) + D-99 (Thank-you composition)
    - `.planning/research/exports/v2.0/end-of-phase-26/id-inventory.json` — page-frame row coordinates (csXky at y ≈ -4111.55)
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4 joel-only branch + § 10.4 FindEmptySpace nodeId anchor pattern
    - `src/pages/thank-you.astro` lines 1-53 (full source — content extraction only per D-58 carry-forward, NOT a visual style reference)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`. Halt + surface on mismatch (Pitfall 8).

    Step 2 — Token surface drift check: `mcp__pencil__get_variables({})`. Assert count is exactly 100 (Phase 27 ships zero new tokens per D-91). Halt on drift.

    Step 3 — Plan 27-00 deliverable intactness:
    `mcp__pencil__batch_get({ readDepth: 1, nodeIds: [...] })` with all Plan 27-00 IDs from 27-00-SUMMARY.md:
    - `<textareaVariantId>`, `<selectDefaultId>`, `<selectFocusId>`, `<selectErrorId>`, `<checkboxDefaultId>`, `<checkboxFocusId>`, `<checkboxErrorId>` (7 primitive variants in avgor)
    - `<checkboxGroupId>` (1 compound in t67DU6)
    - `oCeJP` (or `<newLabelSlotId>` if Plan 27-00 Task 1 fallback used) — verify still horizontal-layout frame with 3 text children (label-text + required-mark enabled:false + optional-text enabled:false)
    - `nwJk7` (Primitive/Input/Default) — verify parent properties UNCHANGED from Phase 24 baseline per Pitfall 2 carry-forward

    Step 4 — Phase 25/26 baseline component intactness (Plan 27-01 will instance these):
    `batch_get` on `G0wNOc` (Section/Header), `Xs0Hs` (Section/Footer), `Hs5rc` (Section/CTA), `dpO5Y` (Primitive/Icon/32), `M7eUr` (Primitive/Button/Default), `csXky` (404 page frame — needed as FindEmptySpace anchor in Task 1).

    Step 5 — Read v1.3 Thank-you source to capture verbatim content (NO drift between source and rendering):
    `Read` `src/pages/thank-you.astro` lines 1-53. Extract:
    - Line 22 heading text (verbatim): "Thanks for reaching out!"
    - Line 27 body text (verbatim): "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!"
    - Line 35 Calendly URL: "https://calendly.com/joelshinness"
    - Line 38 CTA Button label (verbatim): "Skip the wait - book a call"
    - Line 47 secondary link label: "Return to homepage"

    Record extracted strings in 27-01-SUMMARY scratch. Note: D-99 default ships line-38 verbatim as both Section/CTA headline AND actions-slot Button label.

    Step 6 — Look up Section/CTA descendant IDs from `26-00-SUMMARY.md` for descendants override map (headline-slot text id + body-slot text id + actions-slot Button-label text id). Record in scratch.

    No mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token surface: 100. Plan 27-00 deliverables intact (7 primitives + 1 compound + oCeJP extension). Phase 25/26 baseline intact (G0wNOc, Xs0Hs, Hs5rc, dpO5Y, M7eUr, csXky). v1.3 verbatim strings captured."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `get_variables({})` returned exactly 100 tokens (zero drift since Phase 26 / Plan 27-00 close).
    - `batch_get` on 7 Plan 27-00 primitive IDs + 1 compound + oCeJP-or-newLabelSlot + nwJk7 confirms structural intactness; nwJk7 parent UNCHANGED from Phase 24 baseline.
    - `batch_get` on G0wNOc, Xs0Hs, Hs5rc, dpO5Y, M7eUr, csXky returned existing nodes.
    - v1.3 verbatim strings (heading, body, Calendly URL, CTA Button label, secondary link label) captured in 27-01-SUMMARY scratch from `src/pages/thank-you.astro`.
    - Section/CTA descendant IDs (headline-slot text + body-slot text + actions-slot Button-label text) looked up from 26-00-SUMMARY for descendants override map.
    - No mutations in Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight assertions all pass; Plan 27-00 outputs intact; v1.3 verbatim content captured; descendants override IDs ready; Plan 27-01 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `Thank-you` top-level page frame via find_empty_space_on_canvas with nodeId:csXky anchor + batch_design Insert</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 10.4 (FindEmptySpace nodeId anchor pattern from Plan 26-02 contribution)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-101 (Plan 27-01 anchors on `csXky` for right-of-404 placement)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Example 5 (FindEmptySpace nodeId anchor + expected coords `x: ~19367.27, y: ~-4111.55`)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md` Task 1 FindEmptySpace usage pattern (Plan 26-02 contribution)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`.

    Step 2 — FindEmptySpace with `csXky` anchor per CALIBRATION-PROTOCOL § 10.4:
    Call `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 1200, direction: "right", padding: 80, nodeId: "csXky" })`. Expected `{x, y}` ≈ `{ x: 19367.27, y: -4111.55 }` (same page-frame row as Phase 26 FAQ + 404). Height estimate 1200 = Header (~80) + success-message (~400) + Section/CTA (~250) + secondary-link (~40) + Footer (~500) + spacing.

    Step 3 — `batch_design` Insert top-level frame:
    - Parent: document root (NOT g9oRa5/avgor/t67DU6/RpGbe — Pitfall 3 carry-forward from Plan 26-01/02).
    - `type: "frame"`, `name: "Thank-you"`, `placeholder: true` (clear in Task 7 with `fit_content` switch per CALIBRATION-PROTOCOL § 10.8 carry-forward), `position: { x, y }` from FindEmptySpace.
    - Dimensions: `width: 1440`, `height: 1200` (estimate; Update to `fit_content` in Task 7).
    - Layout: `layout: "vertical"`, `gap: 0`, `padding: 0`, `alignItems: "center"` (1200 inner content frames center inside 1440 outer per Phase 26 Pitfall 5 carry-forward).
    - Fill: `"#ffffffff"` (color-semantic-bg-page literal per Pitfall 7).

    Record `<thankYouPageId>`, returned `{x, y}` coords, and any directional anomaly in 27-01-SUMMARY scratch (for CALIBRATION-PROTOCOL feedback per Phase 26 D-67 codify-what-worked discipline).
  </action>
  <verify>
    `mcp__pencil__batch_get(['<thankYouPageId>'])` returns a top-level frame named "Thank-you" at document root level, width 1440, vertical auto-layout, alignItems center, fill #ffffffff, position y ≈ -4111 (same page-frame row as csXky 404 frame per CALIBRATION-PROTOCOL § 10.4).
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `find_empty_space_on_canvas({ ..., nodeId: 'csXky' })` returned `{x, y}` with y ≈ -4111 (same row as csXky per CALIBRATION-PROTOCOL § 10.4).
    - `batch_design` Insert completed without error.
    - `batch_get(['<thankYouPageId>'])` returns a frame at document root level (NOT inside g9oRa5/avgor/t67DU6/RpGbe/b7Hgy/csXky) named "Thank-you" with width 1440, vertical auto-layout, alignItems center, fill #ffffffff.
    - No mutations to baseline IDs (`batch_get` on csXky/b7Hgy confirms unchanged).
    - `<thankYouPageId>` + coords recorded in 27-01-SUMMARY scratch.
  </acceptance_criteria>
  <done>
    Empty Thank-you page frame in place at canvas page-frame row; ready for content insertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header ref + success-message section (Icon/32 circle-check + heading + body, v1.3 verbatim)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-77 (Header instanced as-is — Crito-source labels stay; Joel 4-link override = Phase 31) + D-99 (success-message composition) + D-100 (lucide circle-check Pattern A via Primitive/Icon/32)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern (Thank-you Plan 27-01 task list) + § Don't Hand-Roll (Icon Pattern A vs custom SVG)
    - `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` § Section/Header build (G0wNOc structural reference)
    - `.planning/phases/24-layout-primitives-primitive-components/24-04-SUMMARY.md` § Primitive/Icon variants (dpO5Y = Icon/32) + Pattern A lucide-native glyph swap
    - Task 0 scratch: v1.3 verbatim heading + body strings + Section/CTA descendant IDs
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 2 children of `<thankYouPageId>` (first 2 in vertical stack):

    **Insert 1 — Section/Header ref:**
    - `type: "ref"`, `ref: "G0wNOc"` (Phase 25 Section/Header).
    - NO descendants override per D-77 carry-forward (Crito-source nav labels stay; Joel v1.3 4-link override = Phase 31).
    - First child of `<thankYouPageId>`.

    **Insert 2 — Success-message section frame:**
    - `type: "frame"`, `name: "success-message"`, `width: 1200`, `layout: "vertical"`, `gap: 24` (space-semantic-stack-md), `padding: [96, 0]` (generous section-y rhythm — matches Phase 26 404 message-section spacing per Plan 26-02 D-60 BIG section feel), `alignItems: "center"`.
    - Child 1 — circle-check Icon ref (Pattern A native per D-100):
      `{ type: "ref", ref: "dpO5Y", descendants: { "<icon-glyph-id-from-24-04-SUMMARY>": { library: "lucide", icon: "circle-check", fill: "#2dd4bfff" } } }`
      Look up the icon-glyph descendant id of dpO5Y from `24-04-SUMMARY.md` § Primitive/Icon Build (the inner icon node accepting Pattern A library/icon overrides).
      Fill defaults to a turquoise/teal accent or color-semantic-text-success if such a token exists — if not, ship `#141f39ff` (color-semantic-text-primary) for visual contrast. Plan-execution probes if a success-color literal already maps somewhere in PEN-INVENTORY; if not, ship primary navy as the safe default + raise OPEN-27-NN if calibration reveals need for distinct success-color token.
    - Child 2 — Heading text node (v1.3 verbatim per D-99):
      `{ type: "text", name: "heading", content: "Thanks for reaching out!", fontFamily: "Plus Jakarta Sans", fontSize: 48, fontWeight: "700", fill: "#141f39ff", lineHeight: 1.4, textAlign: "center" }` — uses `type-semantic-heading-1` typography per D-99 default. Claude's Discretion: if visual at calibration reads oversized, downsize to heading-2 (Phase 26 D-72 ships heading-2 — Thank-you is a natural consumer).
    - Child 3 — Body text node (v1.3 verbatim per D-99):
      `{ type: "text", name: "body", content: "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!", fontFamily: "Inter", fontSize: 16, fontWeight: "400", fill: "#52525bff", lineHeight: 1.625, textAlign: "center" }` — uses `type-semantic-prose-paragraph-*` per D-71 carry-forward.

    Pitfall 7 reminder: ALL literal hex/px values; Pitfall 5 reminder: text-clipping false-positives expected on multi-line body text — document NOT mitigate.

    Record `<successMessageId>`, `<successIconRefId>` (with descendants override), `<headingTextId>`, `<bodyTextId>` in 27-01-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get(['<thankYouPageId>'], readDepth: 2)` shows 2 direct children: a ref to G0wNOc + a frame named "success-message". The success-message frame contains 3 children: Primitive/Icon/32 ref (descendants override with `library: "lucide"`, `icon: "circle-check"`) + heading text "Thanks for reaching out!" (Plus Jakarta Sans 48/700) + body text "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!" (Inter 16/400).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<thankYouPageId>'])` returns 2 children in order: Section/Header ref + success-message frame.
    - Section/Header ref has NO descendants override (D-77 compliance).
    - success-message frame has width 1200, vertical auto-layout, alignItems center, padding [96, 0], gap 24.
    - success-message contains: ref to dpO5Y with descendants override `library: "lucide", icon: "circle-check"` (Pattern A per D-100) + heading text "Thanks for reaching out!" (Plus Jakarta Sans 48/700/lh 1.4, fill #141f39ff) + body text "I'll email you within 48 hours..." (Inter 16/400/lh 1.625, fill #52525bff).
    - Heading text content matches v1.3 `src/pages/thank-you.astro` line 22 EXACTLY (verbatim — apostrophes preserved).
    - Body text content matches v1.3 line 27 EXACTLY.
    - All IDs recorded in scratch.
  </acceptance_criteria>
  <done>
    Top of Thank-you complete: Header + success-message section with Icon/32 + heading + body (v1.3 verbatim).
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert Section/CTA-Calendly ref (Hs5rc with descendants override) + sibling Calendly wiring Pencil note</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-93 (Section/CTA + sibling note + Calendly URL) + D-94 (Calendly on both pages) + D-99 (Thank-you Section/CTA-Calendly composition; body-slot enabled:false; actions-slot Button label v1.3 verbatim)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern (Section/CTA descendants override mechanics) + § Pitfall 7 (literal hex only)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` § Section/CTA Build — Hs5rc + 3-slot signature (headline-slot text id + body-slot text id + actions-slot frame id + actions-slot Button-label descendant id ATJK9 inside M7eUr Button)
    - Task 0 scratch: v1.3 verbatim button label + Calendly URL + Section/CTA descendant IDs
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 2 children of `<thankYouPageId>` (3rd + 4th of vertical stack):

    **Insert 1 — Section/CTA-Calendly ref:**
    - `type: "ref"`, `ref: "Hs5rc"` (Phase 26 Section/CTA — first cross-phase consumer per D-78 reusability claim).
    - `descendants` override map using Section/CTA descendant text-node IDs from 26-00-SUMMARY:
      - `<sectionCtaHeadlineSlotTextId>`: `{ content: "Skip the wait — book a call" }` — D-99 default ships v1.3 button label as headline.
      - `<sectionCtaBodySlotId>` (the body-slot frame, not its text child): `{ enabled: false }` — D-99 default suppresses body-slot since success-message above carries body context.
      - `ATJK9` (M7eUr Button label text node id per 24-05-SUMMARY): `{ content: "Skip the wait - book a call" }` — v1.3 verbatim from `src/pages/thank-you.astro` line 38 (note: original v1.3 uses ASCII hyphen `-`, NOT em-dash `—`; preserve EXACT character).
      - `V4Dx4i` (M7eUr arrow-trailing icon): `{ enabled: true }` if Section/CTA actions-slot Button needs the v1.3 arrow visual; OR `{ enabled: false }` if v1.3 Thank-you button has no trailing icon. Plan-execution default per Claude's Discretion: ship `enabled: true` for visual consistency with Section/CTA placeholder (Plan 26-00 ships V4Dx4i enabled:true).

    **Insert 2 — Sibling Calendly wiring Pencil note (D-93 belt-and-suspenders):**
    - `type: "note"`, parent: `<thankYouPageId>` (NOT inside Section/CTA-Calendly ref instance — sibling at the page-frame canvas position, NOT a child of the ref).
    - Actually for a "sibling" at the same canvas-y position: insert as a child of `<thankYouPageId>` in the vertical stack right after the Section/CTA ref. (Pencil notes inside auto-layout frames stack alongside other content.) Alternative is to insert at document root with absolute position alongside the Thank-you frame — plan-execution picks the cleaner option (default: inside `<thankYouPageId>` vertical stack as 4th child).
    - Content: "Calendly placeholder (Phase 27 D-93/D-94) — code milestone wires Section/CTA-Calendly actions-slot Button[0] `href` to https://calendly.com/joelshinness (v1.3 thank-you.astro line 35). May replace body-slot with embedded Calendly iframe at wire-time. Cross-reference: PEN-INVENTORY § Calendly Wiring Map (Phase 27) row 1 (Thank-you consumer)."
    - `fontFamily: "Inter"`, `fontSize: 12`, `width: 1200`.

    Pitfall 7 reminder: ALL fills LITERAL (button arrow icon fill stays whatever M7eUr ships by default).

    Record `<sectionCtaCalendlyRefId>` + descendants override map used + `<calendlyWiringNoteId>` in 27-01-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get(['<thankYouPageId>'], readDepth: 2)` shows 4 direct children in order: G0wNOc ref, success-message frame, Hs5rc ref (with descendants override), Calendly wiring note. The Hs5rc ref's descendants override produces: headline text "Skip the wait — book a call", body-slot enabled:false, actions-slot Button label "Skip the wait - book a call" (v1.3 verbatim, ASCII hyphen). The sibling note content contains the string "calendly.com/joelshinness".
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<thankYouPageId>'])` now returns 4 children in expected order.
    - Section/CTA-Calendly ref descendants override produces: headline-slot text matches D-99 mapping; body-slot is `enabled: false`; actions-slot Button label = "Skip the wait - book a call" EXACTLY (v1.3 line 38 verbatim, ASCII hyphen NOT em-dash).
    - Calendly wiring sibling Pencil note exists as direct child of `<thankYouPageId>` (or sibling alongside) with content containing the string "calendly.com/joelshinness" + cross-reference to PEN-INVENTORY § Calendly Wiring Map (Phase 27).
    - Hs5rc parent unchanged (this is an instance-only ref; no library mutation per Plan 26-00 baseline-preservation discipline).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Section/CTA-Calendly instance + Calendly wiring note in place. First cross-phase consumer of Phase 26 Section/CTA validates D-78 reusability claim.
  </done>
</task>

<task type="auto">
  <name>Task 4: Insert secondary "Return to homepage" plain text link + Section/Footer ref</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-99 (optional secondary "Return to homepage" link section; plain text-link default; Claude's Discretion may upgrade to Primitive/Button/Secondary hIWuC) + D-77 (Footer Crito-source labels stay)
    - `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` § Section/Footer build (Xs0Hs structural reference)
    - `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` (Primitive/Button/Secondary hIWuC available as Claude's Discretion upgrade option)
    - Task 0 scratch: v1.3 secondary link label "Return to homepage" (line 47)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 2 children of `<thankYouPageId>` (5th + 6th of vertical stack; if Task 3 sibling note was inserted as a child it counts as 4th and these become 5th/6th):

    **Insert 1 — Secondary "Return to homepage" link section frame:**
    - `type: "frame"`, `name: "secondary-link"`, `width: 1200`, `layout: "vertical"`, `gap: 4`, `padding: [16, 0, 32, 0]` (extra bottom space to breathe before Footer), `alignItems: "center"`.
    - Child — plain text node (D-99 Claude's Discretion DEFAULT — text-link not Button):
      `{ type: "text", name: "return-link", content: "Return to homepage", fontFamily: "Inter", fontSize: 14, fontWeight: "400", fill: "#52525bff", lineHeight: 1.4 }` — uses `type-semantic-body-sm` + `color-semantic-text-secondary` per D-99 default styling (matches v1.3 lines 43-48 small muted link).
    - Sibling note as second child of `secondary-link` frame:
      `{ type: "note", content: "Code milestone wires this text node as <a href='/'>Return to homepage</a>. Plain text link per D-99 default (not Primitive/Button/Secondary — that's the Claude's Discretion upgrade reserved for stronger affordance contexts).", fontFamily: "Inter", fontSize: 12 }`

    **Insert 2 — Section/Footer ref:**
    - `type: "ref"`, `ref: "Xs0Hs"` (Phase 25 Section/Footer).
    - NO descendants override per D-77 carry-forward.
    - Final child of `<thankYouPageId>` vertical stack.

    Record `<secondaryLinkSectionId>`, `<returnLinkTextId>`, `<returnLinkNoteId>`, `<sectionFooterRefId>` in 27-01-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get(['<thankYouPageId>'], readDepth: 2)` returns 6 direct children in order: G0wNOc ref, success-message, Hs5rc ref, Calendly wiring note, secondary-link frame, Xs0Hs ref. The secondary-link frame contains a text node "Return to homepage" (Inter 14/400 #52525bff) + a sibling Pencil note documenting wire-target.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<thankYouPageId>'])` returns 6 direct children (or 5 if Task 3 sibling note was placed alongside outside the vertical stack — plan-execution outcome documented).
    - secondary-link frame has width 1200, vertical layout, alignItems center, padding [16, 0, 32, 0].
    - secondary-link contains a text node with content "Return to homepage" EXACTLY (v1.3 line 47 verbatim), Inter 14/400/lh 1.4, fill #52525bff.
    - secondary-link sibling note documents code-milestone wire-target.
    - Section/Footer ref has NO descendants override (D-77 compliance).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Thank-you page frame is structurally complete with all 5 content blocks per D-99 composition.
  </done>
</task>

<task type="auto">
  <name>Task 5: snapshot_layout sweep + page-frame fit_content + per-section screenshots for calibration gate</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4.4 steps 7-9 (snapshot_layout sweep + fit_content per § 10.8 + per-section screenshots; OPEN-26-02 Tier-1/Tier-2 stale-cache workaround)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pitfall 5 (text-clipping false-positive — NOT mitigated) + § Pitfall 6 (OPEN-26-02 Tier-1/Tier-2 stale-cache for new subtree)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` Task 5 + 6 pattern (FAQ Tier-1 cross-row Update worked) + `.../26-02-SUMMARY.md` Task 4 + 5 pattern (404 Tier-2 user-side editor fallback)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — Document-level snapshot:
    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })`. Expected: `"No layout problems."` per Phase 24/25/26 carry-forward.

    Step 3 — Per-frame snapshot:
    `mcp__pencil__snapshot_layout({ rootId: "<thankYouPageId>", problemsOnly: true })`. Document any text-clipping false-positives per Pitfall 5 (multi-line body text + heading inside auto-layout frame may trigger). NOT mitigated — known benign.

    Step 4 — Page-frame fit_content + position clear (CALIBRATION-PROTOCOL § 10.8 carry-forward from Plan 26-02 Task 4):
    `mcp__pencil__batch_design` Update: `Update("<thankYouPageId>", { placeholder: false, height: "fit_content" })`. Document before/after dimensions in 27-01-SUMMARY scratch.

    Step 5 — Per-section screenshots for Task 6 calibration gate:
    Call `mcp__pencil__get_screenshot({ nodeId: "<successMessageId>" })` — success-message section inline render.
    Call `mcp__pencil__get_screenshot({ nodeId: "<sectionCtaCalendlyRefId>" })` — Section/CTA-Calendly instance inline render.
    Optional: `mcp__pencil__get_screenshot({ nodeId: "<secondaryLinkSectionId>" })` — secondary link inline render.
    Also: `mcp__pencil__get_screenshot({ nodeId: "<thankYouPageId>" })` — full page inline render.

    **OPEN-26-02 stale-cache workaround per CALIBRATION-PROTOCOL § 6.4:**
    If any get_screenshot returns blank-white:
    - **Tier-1:** `Update("<thankYouPageId>", { x: <new-x-different-row>, y: <different-row-y> })` then move back to original coords. Re-call get_screenshot.
    - **Tier-2:** If Tier-1 fails (matches Plan 26-02 outcome on 404 subtree), fall back to user-side verification — note in 27-01-SUMMARY scratch + AskUserQuestion Task 6 description includes "verify visually in Pencil editor" alternative.

    Step 6 — Record snapshot outcomes + fit_content adjustment + any stale-cache workaround tier used in 27-01-SUMMARY scratch.
  </action>
  <verify>
    Document-level `snapshot_layout` returns "No layout problems." Page-frame Updated to `fit_content`. Per-section inline screenshots captured (success-message + Section/CTA-Calendly minimum) OR Tier-2 user-side editor fallback noted.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
    - Per-frame snapshot captured; text-clipping false-positives documented per Phase 24/25/26 precedent (NOT failures — known benign).
    - Page frame Updated: `placeholder: false`, `height: "fit_content"` (per § 10.8 + Plan 26-02 contribution).
    - Per-section screenshots (success-message + Section/CTA-Calendly minimum) produced inline renders OR Tier-1 cross-row Update workaround applied OR Tier-2 user-side editor fallback documented.
    - Outcomes recorded in 27-01-SUMMARY scratch.
  </acceptance_criteria>
  <done>
    Layout-clean; page frame sized to content; per-section screenshots ready for calibration gate.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 6: Calibration spot-check user gate (joel-only branch — per-section token-usage verification per CALIBRATION-PROTOCOL § 4.5)</name>
  <files>None (presentation-only — uses Task 5's inline screenshots)</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4.5 (joel-only AskUserQuestion format — token-usage check vs `_Tokens & Foundations` RpGbe) + § 4.6 description identifier convention + § 5 per-section fidelity labels D-83 + § 6 inline-only screenshot substitution + § 7 gap declaration template
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-102 (Plan 27-01 single calibration gate at plan close — joel-only format) + D-99 fidelity proposals
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` Task 6 outcomes (FAQ joel-only branch calibration pattern reference)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md` Task 5 outcomes (404 joel-only branch calibration pattern reference + Tier-2 fallback handling)
    - Task 5 scratch: inline screenshots already captured (or Tier-2 fallback note)
  </read_first>
  <action>
    Render the AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 4.5 joel-only format. Inline-render the success-message + Section/CTA-Calendly screenshots from Task 5 above the description (if Tier-2 fallback in use, description directs user to verify visually in Pencil editor).

    **Question:** "Calibration spot-check — Thank-you page (joel-only-no-crito-ref branch per CONTEXT D-99 + CALIBRATION-PROTOCOL § 4). Does this composition use the declared tokens correctly?"

    **Description (markdown — both/all screenshots rendered inline above the description via Pencil MCP image-content protocol per § 6.1):**

    ```
    ## Calibration Spot-Check — Thank-you Page (Joel-Only Branch per CALIBRATION-PROTOCOL § 4)

    **Inline renders shown above:** success-message section + Section/CTA-Calendly instance + (optional) secondary-link
    [If Tier-2 fallback applied: "Inline screenshots returned blank-white per OPEN-26-02; verify visually in Pencil editor by selecting `<thankYouPageId>` and viewing the rendered page frame at canvas position y ≈ -4111."]

    ### Section 1: success-message (joel-only fresh design per D-99 + D-100)
    Fidelity label proposal: **EXACT** per D-83 (token-bound + content verbatim per D-99)

    **Semantic tokens consumed (D-62 token-usage check vs `_Tokens & Foundations` RpGbe):**
    - Icon: `Primitive / Icon / 32` (dpO5Y) ref + descendants override library:lucide, icon:circle-check (Pattern A per D-100 + Phase 24 D-44)
    - Heading: `type-semantic-heading-1-family` (Plus Jakarta Sans) + `-size` (48px) + `-weight` (700) + `-lh` (1.4); fill `color-semantic-text-primary` #141f39ff
    - Body: `type-semantic-prose-paragraph-family` (Inter) + `-size` (16px) + `-weight` (400) + `-lh` (1.625); fill `color-semantic-text-secondary` #52525bff
    - Section padding-y: [96, 0] bound to space-semantic-section-y x2 (generous Thank-you rhythm matching Phase 26 404)
    - Stack gap: 24 bound to `space-semantic-stack-md`

    **Content (v1.3 verbatim per D-99 — src/pages/thank-you.astro lines 22 + 27):**
    - Heading: "Thanks for reaching out!"
    - Body: "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!"

    ### Section 2: Section/CTA-Calendly instance (Hs5rc, first cross-phase consumer per Phase 26 D-78)
    Fidelity label proposal: **EXACT** per D-83 (token-bound + v1.3 button label verbatim per D-99)

    **Semantic tokens consumed (inherits Plan 26-00 Section/CTA build):**
    - Outer fill `color-semantic-bg-surface` #f2f2f7ff (Plan 26-00 ship)
    - headline-slot: heading-2 typography (Plan 26-00 Plus Jakarta Sans / heading-2 size / 700 / lh 1.4)
    - body-slot: enabled:false per D-99 (success-message above carries body context)
    - actions-slot Button label "Skip the wait - book a call" (v1.3 verbatim, line 38 — ASCII hyphen preserved)
    - Calendly wiring sibling Pencil note documents URL `https://calendly.com/joelshinness` (PEN-INVENTORY § Calendly Wiring Map row 1)

    ### Section 3: secondary "Return to homepage" plain text link (D-99 Claude's Discretion default)
    Fidelity label proposal: **EXACT** per D-83 (token-bound + v1.3 verbatim)

    **Semantic tokens consumed:**
    - Text typography: `type-semantic-body-sm-family` (Inter) + `-size` (14px) + `-weight` (400) + `-lh` (1.4); fill `color-semantic-text-secondary` #52525bff
    - Frame padding: [16, 0, 32, 0]

    **Content (v1.3 verbatim per D-99 — line 47):** "Return to homepage"

    ### Section 4 + 5: Section/Header + Section/Footer instances
    Fidelity label proposal: **EXACT** (Phase 25 shipped; D-77 Crito-source labels stay literal — Joel 4-link override deferred to Phase 31)

    ### PAGE-11 status (CALIBRATION-PROTOCOL § 4.3 + § 10.5):
    **INERT** for Thank-you — joel-only-no-crito-ref scope means no Crito source raster exists to remove (Phase 23 audit catalogued no Thank-you frame). PAGE-11 ACTIVE only applies to crito-source branch (Plan 27-02 Contact applies it via cl8tt hide).

    **Description identifiers (D-64 — inline only per OPEN-23-01, NOT disk-written):**
    - `27-thank-you-success-message--token-usage.png`
    - `27-thank-you-cta-calendly--token-usage.png`
    - `27-thank-you-secondary-link--token-usage.png` (optional)

    **Calibration target (CALIBRATION-PROTOCOL § 4.1 joel-only branch):**
    `_Tokens & Foundations` reference frame `RpGbe` shows the canonical visual register for these tokens. Are the section compositions using them correctly?
    ```

    **Options per § 4.5:**
    - "APPROVE — all sections use tokens correctly; fidelity labels per D-83 as proposed (success-message=EXACT; Section/CTA-Calendly=EXACT; secondary-link=EXACT; Header=EXACT; Footer=EXACT); PAGE-11 INERT noted"
    - "REVISE — token mismatch (free-text describe)"
    - "GAP — composition reveals a missing token or primitive (raise OPEN-27-NN; specify)"

    Wait for user response. Record outcome in 27-01-SUMMARY scratch.

    If APPROVE: proceed to Task 7.
    If REVISE: address via `batch_design` Update ops + re-gate (loop until APPROVE).
    If GAP: log OPEN-27-NN per CALIBRATION-PROTOCOL § 7 template + proceed to Task 7 with gap documented.

    Possible OPEN-27-NN candidates (anticipated per CONTEXT.md):
    - Success-icon fill color: if calibration reveals the default (#141f39ff primary navy or whatever was chosen) reads wrong, raise OPEN-27-NN for `color-semantic-text-success` token (deferred consumer phase).
    - Heading-1 size 48 reads oversized for Thank-you success-message context: raise OPEN-27-NN — fallback is Claude's Discretion downsize to heading-2 (Phase 26 D-72 token) at REVISE iteration.
    - Provisional `type-semantic-prose-paragraph-*` flag re-verification: if Thank-you body reads wrong at form-context scale (different from FAQ open-prose), flag for Phase 28 Blog re-verification per Open Question 7.
  </action>
  <verify>
    User has responded APPROVE / REVISE / GAP; response recorded in 27-01-SUMMARY scratch; any REVISE iterations resolved; any GAP logged as OPEN-27-NN.
  </verify>
  <acceptance_criteria>
    - Inline `get_screenshot` calls produced renders (or Tier-2 user-side editor fallback applied per OPEN-26-02 + CALIBRATION-PROTOCOL § 6.4).
    - AskUserQuestion presented with screenshots/screenshot-fallback + structured token-usage description per § 4.5 format + PAGE-11 INERT note + per-section fidelity proposals per D-83.
    - User chose APPROVE / APPROVE-after-REVISE / APPROVE-with-GAP-logged.
    - Per-section fidelity labels per D-83 recorded in 27-01-SUMMARY scratch (success-message=EXACT; CTA-Calendly=EXACT; secondary-link=EXACT; Header=EXACT; Footer=EXACT — or as user-adjusted).
    - Any OPEN-27-NN raised has 1-line row ready for Task 7 PEN-INVENTORY extension.
  </acceptance_criteria>
  <done>
    User has spot-checked the Thank-you composition (≥1 section per VALID-02 — likely all 5 via batched gate per Phase 26 § 10.3 precedent) — VALID-02 satisfied for joel-only branch.
  </done>
</task>

<task type="auto">
  <name>Task 7: PEN-INVENTORY extensions (Frames Inventory + Variant Evidence Phase 27 + PAGE-11 INERT note + OPEN-27-NN) + 27-01-SUMMARY.md close</name>
  <files>
    - .planning/research/PEN-INVENTORY.md
    - .planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md
    - design/Crito.pen (read-only validation)
  </files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-104 (PEN-INVENTORY extension pattern — Frames Inventory rows for Thank-you + Variant Evidence rows + Open Flags Phase 27 + PAGE-11 INERT note for joel-only)
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4.3 (PAGE-11 INERT for joel-only) + § 7 (gap declaration template)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` Task 7 PEN-INVENTORY edit pattern + 26-02-SUMMARY.md Task 6 PAGE-11 INERT note pattern
    - 27-01-SUMMARY scratch (all IDs + Task 6 fidelity labels + any OPEN-27-NN)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`.

    Step 2 — Final verification reads:
    `mcp__pencil__get_variables({})` — assert 100 tokens (zero drift through Plan 27-01).
    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` — assert clean at document level.
    `mcp__pencil__batch_get(['<thankYouPageId>'], readDepth: 2)` — confirm 5-or-6 children, all expected refs + sections present.

    Step 3 — Edit `.planning/research/PEN-INVENTORY.md`:

    A. Frames Inventory table — add new row for `Thank-you`:
    - name: "Thank-you"
    - frame_id: `<thankYouPageId>`
    - scope: `joel-only-no-crito-ref`
    - joel_page_map: `/thank-you`
    - child_section_count: 5 (Header ref + success-message + Section/CTA-Calendly ref + secondary-link section + Footer ref) — note: Calendly wiring sibling note counted as adjunct documentation, not a content section per Phase 26 precedent
    - status: `reconstructed-PHASE-27`
    - notes: "Fresh design in Crito vocab per D-58 + joel-only-no-crito-ref scope per CALIBRATION-PROTOCOL § 2 branch matrix; success-message Icon/32 lucide circle-check Pattern A per D-100; Section/CTA-Calendly first cross-phase consumer of Hs5rc validating Phase 26 D-78 reusability claim; secondary 'Return to homepage' plain text-link per D-99 Claude's Discretion default; Calendly wiring sibling note cross-references § Calendly Wiring Map (Phase 27) row 1; PAGE-11 INERT (no Crito source raster to remove per § 4.3 + § 10.5)."

    B. Extend `### Variant Evidence (Phase 27)` sub-section (created in Plan 27-00) with Thank-you rows:
    - Thank-you page frame: fill #ffffffff ↔ color-semantic-bg-page; width 1440; vertical auto-layout + alignItems center + gap 0 + padding 0.
    - success-message frame: width 1200; padding [96, 0] ↔ space-semantic-section-y x2; gap 24 ↔ space-semantic-stack-md; alignItems center.
    - circle-check Icon ref (dpO5Y + descendants override): library:lucide, icon:circle-check (Pattern A per D-100 + Phase 24 D-44); fill literal chosen at calibration.
    - Heading text: Plus Jakarta Sans 48/700/lh 1.4 ↔ type-semantic-heading-1-* tokens; fill #141f39ff ↔ color-semantic-text-primary; content v1.3 verbatim.
    - Body text: Inter 16/400/lh 1.625 ↔ type-semantic-prose-paragraph-* tokens; fill #52525bff ↔ color-semantic-text-secondary; content v1.3 verbatim.
    - Section/CTA-Calendly ref descendants override: headline content + body-slot enabled:false + actions-slot Button label (ATJK9) v1.3 verbatim.
    - Calendly wiring sibling note: content + cross-reference to PEN-INVENTORY § Calendly Wiring Map.
    - secondary-link frame: width 1200; padding [16, 0, 32, 0]; alignItems center; text content "Return to homepage" v1.3 verbatim; Inter 14/400/lh 1.4 ↔ type-semantic-body-sm-*; fill #52525bff ↔ color-semantic-text-secondary; sibling note.
    - Section/Footer ref: no descendants override (D-77 compliance).

    C. PAGE-11 INERT note (carry-forward from Plan 26-02 + CALIBRATION-PROTOCOL § 4.3):
    Add explicit note to Frames Inventory Thank-you row OR extend the existing Phase 26 § "PAGE-11 Status Notes" sub-section:
    "Phase 27 Plan 27-01 (Thank-you) is joel-only-no-crito-ref — PAGE-11 INERT per CALIBRATION-PROTOCOL § 4.3 (carry-forward from Phase 26 Plan 26-02). No Crito source raster exists to remove. PAGE-11 ACTIVE applies only to crito-source branch (Plan 27-02 Contact applies it via cl8tt hide)."

    D. If Task 6 raised OPEN-27-NN (success-icon color, heading-1 oversized, prose-paragraph re-verification, etc.), add row(s) under `## Open Flags — Phase 27 (OPEN-27-NN)` section (initialized in Plan 27-00 Task 6 if not already present).

    E. If Task 6 confirmed prose-paragraph rendering reads correctly on Thank-you, consider whether to drop the "provisional" flag (Phase 26 D-71 only dropped it after FAQ first-consumer; Thank-you is a second consumer). Plan-execution default: leave the provisional flag pending Phase 28 Blog re-verification per Open Question 7.

    Step 4 — Write `.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md` containing:
    - Pre-flight outcomes (Task 0): editor + token count 100 + Plan 27-00 deliverables intact + Phase 25/26 baselines intact + v1.3 verbatim strings extracted
    - Task 1: `<thankYouPageId>` + FindEmptySpace returned coords (nodeId:csXky anchor outcome) + directional anomaly notes if any
    - Task 2: `<successMessageId>`, `<successIconRefId>` + descendants override map, `<headingTextId>`, `<bodyTextId>`
    - Task 3: `<sectionCtaCalendlyRefId>` + descendants override map + `<calendlyWiringNoteId>`
    - Task 4: `<secondaryLinkSectionId>`, `<returnLinkTextId>`, `<returnLinkNoteId>`, `<sectionFooterRefId>`
    - Task 5: snapshot_layout outcomes (document level + Thank-you per-frame, text-clipping false-positives documented); fit_content adjustment before/after; per-section screenshots outcome (OK / Tier-1 / Tier-2)
    - Task 6 user response: APPROVE / REVISE / GAP + per-section fidelity labels per D-83 (success-message=EXACT / CTA-Calendly=EXACT / secondary-link=EXACT / Header=EXACT / Footer=EXACT — or as user-adjusted); any OPEN-27-NN raised + 1-line description
    - Task 7 PEN-INVENTORY diff: Frames Inventory Thank-you row + Variant Evidence (Phase 27) Thank-you rows + PAGE-11 INERT note + any OPEN-27-NN rows added
    - Token surface unchanged: 100
    - Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md
    - VAL outcomes: PAGE-06 satisfied (Thank-you frame reconstructed zero [FLAT]) + PAGE-09 satisfied (desktop-only 1440 width) + PAGE-11 INERT (joel-only — no raster to remove) + VALID-01 satisfied (per-section labels) + VALID-02 satisfied (joel-only-branch calibration via § 4.1 token-usage check) + VALID-03 satisfied (any gaps as OPEN-27-NN)
  </action>
  <verify>
    `grep "Thank-you" .planning/research/PEN-INVENTORY.md` returns Frames Inventory row. `grep -A 5 "Variant Evidence (Phase 27)" .planning/research/PEN-INVENTORY.md` shows Thank-you rows added. `grep -E "PAGE-11.*INERT" .planning/research/PEN-INVENTORY.md` shows the carve-out note. 27-01-SUMMARY.md exists.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `get_variables({})` returns 100 (zero drift through Plan 27-01).
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns "No layout problems." at document level.
    - PEN-INVENTORY Frames Inventory contains a new row for `Thank-you` with scope `joel-only-no-crito-ref`, joel_page_map `/thank-you`, status `reconstructed-PHASE-27`, frame_id = `<thankYouPageId>`, PAGE-11 INERT note in row notes.
    - PEN-INVENTORY Variant Evidence (Phase 27) sub-section has Thank-you page-frame structural rows (~10 rows).
    - PAGE-11 INERT note carry-forward from Plan 26-02 extended to include Plan 27-01 (Thank-you).
    - Any OPEN-27-NN raised in Task 6 has a row under `## Open Flags — Phase 27 (OPEN-27-NN)` section.
    - 27-01-SUMMARY.md exists at `.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md` with all listed sections.
    - VAL outcomes documented for VALID-01, VALID-02, VALID-03, PAGE-06, PAGE-09, PAGE-11 (INERT).
  </acceptance_criteria>
  <done>
    Plan 27-01 complete. Thank-you page frame shipped (5 content blocks, joel-only fresh design per D-99 + D-100); calibration gated (CALIBRATION-PROTOCOL § 4.5); PAGE-11 INERT carve-out extended; audit trail extended. Plan 27-02 (Contact crito-source flat-raster) may proceed with `<thankYouPageId>` as FindEmptySpace anchor.
  </done>
</task>

</tasks>

<verification>
- Active editor remained `design/Crito.pen` throughout (D-103 pre-flight on every mutating task).
- Token surface UNCHANGED at 100 (Phase 27 ships zero new tokens per D-91 reuse).
- Phase 24/25/26 baseline IDs + Plan 27-00 new library IDs all intact (Plan 27-01 instances refs; does NOT mutate library entries).
- Thank-you frame is a direct child of the document root, NOT inside library parents (Pitfall 3 carry-forward).
- FindEmptySpace anchored on `csXky` per CALIBRATION-PROTOCOL § 10.4 — Thank-you placed at same page-frame row as Phase 26 FAQ + 404 (y ≈ -4111).
- 5 content blocks present: Section/Header ref + success-message section + Section/CTA-Calendly ref + secondary-link section + Section/Footer ref.
- v1.3 verbatim content shipped: heading line 22 + body line 27 + CTA Button label line 38 + secondary link line 47 — apostrophes preserved.
- Calendly URL `https://calendly.com/joelshinness` documented in sibling Pencil note + cross-referenced via PEN-INVENTORY § Calendly Wiring Map (Phase 27) row 1.
- First cross-phase consumer of Phase 26 Section/CTA (Hs5rc) validated D-78 reusability claim.
- Plan-close calibration AskUserQuestion presented per CALIBRATION-PROTOCOL § 4.5 joel-only format (inline screenshots + token-usage description + PAGE-11 INERT note + per-section fidelity proposals).
- PEN-INVENTORY extensions per D-104: Frames Inventory row for Thank-you + Variant Evidence (Phase 27) Thank-you rows + PAGE-11 INERT note extended + any OPEN-27-NN.
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
- 27-01-SUMMARY.md committed to git.
</verification>

<success_criteria>
- A top-level frame named `Thank-you` exists at the page-frame row (y ≈ -4111) with 5 vertical content blocks in order: Section/Header ref, success-message section (Icon/32 circle-check + heading + body), Section/CTA-Calendly ref (with descendants override + sibling Calendly note), secondary "Return to homepage" plain text-link, Section/Footer ref.
- success-message Icon is Primitive/Icon/32 (dpO5Y) ref with descendants override `library: "lucide", icon: "circle-check"` (Pattern A per D-100).
- Heading text matches v1.3 line 22 EXACTLY: "Thanks for reaching out!"
- Body text matches v1.3 line 27 EXACTLY: "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!"
- Section/CTA-Calendly actions-slot Button label matches v1.3 line 38 EXACTLY: "Skip the wait - book a call" (ASCII hyphen).
- Calendly wiring sibling Pencil note content contains "calendly.com/joelshinness".
- secondary-link text matches v1.3 line 47 EXACTLY: "Return to homepage".
- Plan-close calibration AskUserQuestion was presented per CALIBRATION-PROTOCOL § 4.5 joel-only format; user APPROVED (possibly after REVISE iteration or with OPEN-27-NN gap logged).
- PEN-INVENTORY.md Frames Inventory row for Thank-you exists; Variant Evidence (Phase 27) Thank-you rows exist; PAGE-11 INERT note exists.
- `mcp__pencil__get_variables({})` returns 100 (zero token drift through Plan 27-01).
- `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
- VALID-01 satisfied (per-section labels per D-83); VALID-02 satisfied (joel-only-branch calibration via § 4.1 token-usage check); VALID-03 satisfied (any gaps as OPEN-27-NN); PAGE-06 satisfied (Thank-you frame reconstructed zero [FLAT]); PAGE-09 satisfied (desktop-only 1440 width); PAGE-11 satisfied as INERT (joel-only — no raster).
</success_criteria>

<output>
After completion, create `.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md` containing:
- Pre-flight outcomes (Task 0): token count + Plan 27-00 deliverables intactness + Phase 25/26 baseline intactness + v1.3 verbatim strings + Section/CTA descendant IDs
- Task 1: `<thankYouPageId>` + FindEmptySpace coords (nodeId:csXky anchor outcome) + directional notes
- Task 2 IDs: `<successMessageId>`, `<successIconRefId>` + descendants override, `<headingTextId>`, `<bodyTextId>`
- Task 3 IDs: `<sectionCtaCalendlyRefId>` + descendants override map (headline + body-slot enabled:false + actions-slot Button label), `<calendlyWiringNoteId>` + content with Calendly URL
- Task 4 IDs: `<secondaryLinkSectionId>`, `<returnLinkTextId>`, `<returnLinkNoteId>`, `<sectionFooterRefId>`
- Task 5 snapshot_layout outcomes (document + per-frame), fit_content before/after, per-section screenshots outcome (OK / Tier-1 / Tier-2 fallback)
- Task 6 user response: APPROVE/REVISE/GAP + per-section fidelity labels per D-83 + any OPEN-27-NN raised
- Task 7 PEN-INVENTORY diff (Frames Inventory Thank-you row + Variant Evidence Phase 27 Thank-you rows + PAGE-11 INERT note + OPEN-27-NN if any)
- Token surface unchanged: 100
- Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md
- VAL outcomes for VALID-01, VALID-02, VALID-03, PAGE-06, PAGE-09, PAGE-11 (INERT)
</output>
