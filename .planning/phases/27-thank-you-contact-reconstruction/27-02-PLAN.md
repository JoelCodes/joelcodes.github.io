---
phase: 27-thank-you-contact-reconstruction
plan: 02
type: execute
wave: 3
depends_on:
  - 27-00
  - 27-01
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-05
  - PAGE-09
  - PAGE-11
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "The cl8tt raster's `fill.image` reference is probed at Task 0 (HIGHEST-PRIORITY DELIVERABLE per RESEARCH § Open Question 1 + Pitfall 3) and the matching `design/images/image-import-NN.jpg` index is recorded in 27-02-SUMMARY.md immediately upon discovery. This index becomes the side-by-side calibration artifact identifier per CALIBRATION-PROTOCOL § 3.5."
    - "A new top-level page frame named `Contact` exists in `design/Crito.pen` as a direct child of the document root (NOT inside library parents — Pitfall 3 carry-forward), positioned at the page-frame row (y ≈ -4111) via `find_empty_space_on_canvas` with `nodeId: <thankYouPageId>` anchor per CALIBRATION-PROTOCOL § 10.4 + D-101."
    - "The `Contact` frame has width 1440, vertical auto-layout with alignItems center, fill `#ffffffff` (color-semantic-bg-page literal per OPEN-23-13)."
    - "The `Contact` frame contains 4 stacked content blocks: (1) Section/Header ref (G0wNOc, NO descendants override per D-77 carry-forward), (2) page-intro section with v1.3 verbatim 'Let's Talk' heading + 48-hours body per D-98, (3) 2-col asymmetric grid frame per D-95 (horizontal-layout, form column ≈ 760 left + sidebar column ≈ 376 right, gap 32, alignItems flex_start) containing the 8-field form composition + Compound/Card sidebar instance, (4) Section/Footer ref (Xs0Hs, NO descendants override per D-77)."
    - "The 8-field form is composed entirely of `Primitive / Input` + `Primitive / Input / Textarea` + `Primitive / Select` + `Compound / CheckboxGroup` instances (NO inlined input markup) per ROADMAP success criterion 2 + D-97 verbatim v1.3 8-field shape."
    - "8 fields shipped per D-97 (v1.3 verbatim from `src/components/homepage/ContactSection.astro`): (1) Name = Input/Default + required-mark enabled:true; (2) Email = Input/Default + required-mark enabled:true; (3) Company = Input/Default + optional-text enabled:true; (4) Challenges = Input/Textarea + optional-text enabled:true; (5) Solutions = Compound/CheckboxGroup with 5 children (AI / Automations / Web Apps / Consultation / Not Sure); (6) Budget = Select/Default; (7) Timeline = Select/Default; (8) Message = Input/Textarea + required-mark enabled:true AND optional-text enabled:true (documents v1.3 inconsistency per D-97 footnote — raises OPEN-27-NN)."
    - "Sidebar is `Compound / Card` (t40xct) instance per D-96 with descendants override: image-slot enabled:false; title-slot text = 'Ready to chat?' (v1.3 line 184 verbatim); body-slot text = 'Schedule a discovery call with me directly!' (v1.3 line 187 verbatim); footer-actions-slot Button label = 'Book a Call' (v1.3 line 196 verbatim)."
    - "Calendly wiring sibling Pencil note at the sidebar Card canvas position with content containing `https://calendly.com/me--juoi/discovery-call` per D-93 + D-94 (PEN-INVENTORY § Calendly Wiring Map seeded in Plan 27-00 row 2 cross-references this note)."
    - "Submit Button (Primitive/Button/Default M7eUr ref) with label 'Send Message' (v1.3 line 171 verbatim) appears at the bottom of the form column; privacy line plain text 'Your info stays between us. No spam, ever.' (v1.3 line 175 verbatim) below the Submit Button."
    - "Per-section fidelity labels per D-83 recorded in 27-02-SUMMARY.md: page-intro = EXACT (token-bound + content verbatim per D-98); form-section = EXACT (token-bound + 8 fields v1.3 verbatim per D-97); sidebar Card = EXACT (token-bound + content verbatim per D-98); Header = EXACT (Phase 25 shipped, D-77); Footer = EXACT (Phase 25 shipped, D-77)."
    - "User APPROVED the composition via single AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 3.4 step 5 (crito-source-flat-raster format) — pairs reconstructed Contact frame screenshot against the probed `image-import-NN.jpg` raster."
    - "PAGE-11 ACTIVE — `cl8tt` raster hidden via `enabled: false` AFTER user APPROVE only per CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7 (NEVER before — Pitfall 4 enforcement). This is the FIRST production use of the PAGE-11 ACTIVE branch in v2.0."
    - "PEN-INVENTORY status_counts for cl8tt moves from `flat:1` to `hidden:1` (or per D-104 chosen label); Frames Inventory cl8tt row status updated from `IN-SCOPE` to `reconstructed-PHASE-27`; OPEN-23-05 marked RESOLVED by Plan 27-02 PAGE-11 ACTIVE."
    - "PEN-INVENTORY Frames Inventory has new row for `Contact` (scope: IN-SCOPE, joel_page_map: /contact, status: reconstructed-PHASE-27); Variant Evidence (Phase 27) sub-section extended with Contact page-frame structural rows + 8-field form composition rows + sidebar Card override rows."
    - "Token surface unchanged at 100 across Plan 27-02 (Phase 27 ships zero new tokens per D-91 reuse)."
    - "Phase 23/24/25/26 baseline IDs + Plan 27-00 new library IDs + Plan 27-01 Thank-you frame intact (Plan 27-02 instances refs + mutates only cl8tt enabled:false at post-APPROVE step)."
    - "Document-level `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `\"No layout problems.\"` at plan close (text-clipping false-positives per Phase 24/25/26 quirk documented in 27-02-SUMMARY.md, NOT mitigated per Pitfall 5; multi-line content surface — textarea + CheckboxGroup vertical-stack — WILL trigger more false-positives than Phase 26)."
    - "D-58: Fresh in Crito vocab with cl8tt raster as visual calibration target (NOT v1.3 turquoise neobrutalist mirror)"
    - "D-89 + D-91: Plan 27-00 primitives used; required-mark + optional-text enabled per field per D-97"
    - "D-93 + D-94: Sidebar Card + Calendly wiring sibling note + PEN-INVENTORY § Calendly Wiring Map row 2"
    - "D-95: 2-col asymmetric grid (form 2fr ≈ 760 + sidebar 1fr ≈ 376; horizontal-layout, alignItems flex_start)"
    - "D-96: Sidebar = Compound/Card NOT Section/CTA; image-slot enabled:false + title/body/footer-actions filled"
    - "D-97: Joel's 8 v1.3 fields verbatim; cl8tt = VISUAL calibration target only"
    - "D-98: Contact microcopy v1.3 verbatim across all surfaces (page-intro + 8 labels + placeholders + select options + sidebar + Submit + privacy)"
    - "D-103: Pre-flight active-editor before every mutating call"
    - "D-104: PEN-INVENTORY extensions; cl8tt status_counts update post-APPROVE"
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `Contact` with 4 vertical content blocks: Section/Header ref + page-intro + 2-col asymmetric grid (form column with 8 fields + Submit + privacy / sidebar column with Compound/Card + Calendly note) + Section/Footer ref. cl8tt raster hidden via enabled:false after user APPROVE (PAGE-11 ACTIVE)."
      contains: "Contact page frame, page-intro section, form-and-sidebar-grid, form-column with 8 field instances + Submit Button + privacy text + sidebar-column with Compound/Card instance + Calendly wiring sibling note, Section/Header ref G0wNOc, Section/Footer ref Xs0Hs, cl8tt enabled:false"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for Contact + cl8tt row status_counts update (flat:1 → hidden:1) + cl8tt scope update (reconstructed-PHASE-27) + Variant Evidence (Phase 27) Contact rows + OPEN-23-05 RESOLVED note + any OPEN-27-NN raised (v1.3 Message field validation inconsistency anticipated)."
  key_links:
    - from: "Contact page frame"
      to: "Section / Header (G0wNOc)"
      via: "ref child at top; NO descendants override per D-77"
      pattern: "Contact.*ref.*G0wNOc"
    - from: "Contact 2-col asymmetric grid (form-column)"
      to: "8 field instances (Primitive/Input + Input/Textarea + Select + CheckboxGroup) + Submit Button + privacy text"
      via: "vertical-layout frame children, no inlined input markup; instances reference Plan 27-00 + Phase 24 library entries"
      pattern: "form-column.*ref.*nwJk7.*<textareaVariantId>.*<selectDefaultId>.*<checkboxGroupId>"
    - from: "Contact 2-col asymmetric grid (sidebar-column)"
      to: "Compound / Card (t40xct, Phase 25)"
      via: "ref instance with descendants override: image-slot enabled:false + title 'Ready to chat?' + body 'Schedule a discovery call...' + footer-actions Button 'Book a Call'"
      pattern: "sidebar-column.*ref.*t40xct"
    - from: "Sidebar Calendly wiring Pencil note"
      to: "https://calendly.com/me--juoi/discovery-call (v1.3 ContactSection.astro line 191 URL)"
      via: "sibling Pencil text node at sidebar Card canvas position per D-93 belt-and-suspenders"
      pattern: "calendly.com/me--juoi/discovery-call"
    - from: "Plan 27-02 Task 10 cl8tt mutation"
      to: "cl8tt `enabled: false` (PAGE-11 ACTIVE post-APPROVE only per § 3.4 step 7)"
      via: "batch_design Update AFTER user APPROVE at Task 9 calibration gate; NOT before"
      pattern: "cl8tt.*enabled.*false"
---

<objective>
Create the `Contact` page frame in `design/Crito.pen` as a crito-source-present-flat-raster reconstruction per D-95 + D-96 + D-97 + D-98 + CALIBRATION-PROTOCOL § 3 (crito-source branch, flat-raster sub-case). The Crito source `cl8tt` (09_Contact) is a single image-import-*.jpg flat raster per OPEN-23-05; calibration pairs the reconstructed Contact frame against that raster per § 3.1 + § 3.5.

**Critical first-task probe (HIGHEST PRIORITY per RESEARCH § Open Question 1 + Pitfall 3):** Plan 27-02 Task 0 MUST call `batch_get({ nodeIds: ['cl8tt'], readDepth: 2 })` to read the `fill.image` reference and identify the matching `image-import-NN.jpg` index BEFORE any reconstruction work. Document the index in 27-02-SUMMARY.md immediately upon discovery.

Composition per D-95 (top → bottom):
1. `Section / Header` instance (G0wNOc, Phase 25) — Crito-source nav labels stay literal per D-77 carry-forward.
2. **Page-intro section** — "Let's Talk" heading + "Tell me about your project and I'll get back to you within 48 hours." body (v1.3 verbatim per D-98). Page-intro alignment (left vs centered) per Claude's Discretion + cl8tt raster probe (D-95 Claude's Discretion note).
3. **2-col asymmetric grid** (D-95 — horizontal-layout, gap 32, alignItems flex_start):
   - **Form column** (left, width ≈ 760, vertical-layout, gap 24): 8 field instances per D-97 + Submit Button + privacy line, all v1.3 verbatim per D-98.
   - **Sidebar column** (right, width ≈ 376, vertical-layout): `Compound / Card` (t40xct) instance per D-96 with descendants override (image-slot enabled:false; title "Ready to chat?"; body "Schedule a discovery call with me directly!"; footer-actions Button "Book a Call") + sibling Calendly wiring Pencil note documenting `https://calendly.com/me--juoi/discovery-call` per D-93.
4. `Section / Footer` instance (Xs0Hs, Phase 25) — Crito-source labels stay literal per D-77.

8-field form per D-97 + D-98 verbatim (read v1.3 source for exact strings at Task 0):
1. **Name** — Primitive/Input/Default + required-mark `enabled:true` + label "Name" + placeholder "Your name"
2. **Email** — Primitive/Input/Default + required-mark `enabled:true` + label "Email" + placeholder "you@example.com"
3. **Company** — Primitive/Input/Default + optional-text `enabled:true` + label "Company" + placeholder "Company name (if applicable)"
4. **Challenges** — Primitive/Input/Textarea + optional-text `enabled:true` + label "What challenges are you facing?" + placeholder "What problems are slowing you down, causing headaches, or just keeping you from doing the work you love?"
5. **Solutions** — Compound/CheckboxGroup with legend "What kinds of solutions do you think you'll need?" + 5 Checkbox children: AI / Automations / Web Apps / Consultation / Not Sure
6. **Budget** — Primitive/Select/Default + label "Do you have a budget in mind?" + placeholder "Select budget range (optional)"
7. **Timeline** — Primitive/Select/Default + label "Do you have a timeline in mind?" + placeholder "Select timeline (optional)"
8. **Message** — Primitive/Input/Textarea + required-mark `enabled:true` AND optional-text `enabled:true` (documents v1.3 inconsistency per D-97 footnote — raises OPEN-27-NN) + label "Message" + placeholder "Anything else you'd like to share about your project, goals, or how I can help?"

Plus: Submit Button (M7eUr instance) "Send Message" + privacy line "Your info stays between us. No spam, ever."

**PAGE-11 ACTIVE** per CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7: Task 10 fires `batch_design Update("cl8tt", { enabled: false })` ONLY AFTER user APPROVE at Task 9 calibration gate. This is the FIRST production use of the PAGE-11 ACTIVE branch in v2.0 (Pitfall 4 — never hide before APPROVE).

**Purpose:** Phase 27's second per-page reconstruction + first crito-source-flat-raster branch use in v2.0. Stress-tests Plan 27-00's 5 new library entries via Joel's actual 8-field form (validates D-89 Hybrid library strategy). Exercises PAGE-11 ACTIVE for the first time. Plan 27-02 is the deepest-dependency plan — requires Plan 27-00 primitives + Phase 25 Compound/Card t40xct + Phase 26 Section/CTA (via Calendly note convention, not actual instance — Section/CTA is not used here per D-96 distinction).

**Output:** A working `Contact` top-level page frame; cl8tt raster hidden post-APPROVE; extended PEN-INVENTORY with Frames Inventory row + cl8tt status update + Variant Evidence (Phase 27) rows + Calendly Wiring Map row 2 + OPEN-23-05 RESOLVED + any OPEN-27-NN; per-section fidelity labels recorded; plan-close user APPROVED state. ONE user gate at plan close per CALIBRATION-PROTOCOL § 3.4 step 5 (crito-source format).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md
@.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md
@.planning/phases/27-thank-you-contact-reconstruction/27-VALIDATION.md
@.planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-26/id-inventory.json
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@src/components/homepage/ContactSection.astro
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plan 27-00 + 27-01 outputs intact + PROBE cl8tt raster image-import-NN.jpg index (HIGHEST PRIORITY) + read v1.3 ContactSection verbatim content</name>
  <files>design/Crito.pen (read-only); src/components/homepage/ContactSection.astro (read-only)</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-00-SUMMARY.md` — Plan 27-00 7 primitive + 1 compound IDs + label-slot extension structure
    - `.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md` — Plan 27-01 Thank-you frame ID (for FindEmptySpace nodeId anchor at Task 1)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Open Question 1 (cl8tt index probe) + § Pitfall 3 (must probe, do NOT guess) + § Example 2 (cl8tt probe code)
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.5 description identifier convention (`27-contact-{section}--side-by-side.png` paired with `design/images/image-import-NN.jpg`)
    - `src/components/homepage/ContactSection.astro` lines 1-204 (full source — content extraction only per D-58)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`. Halt + surface on mismatch.

    Step 2 — Token surface drift check: `mcp__pencil__get_variables({})`. Assert count is exactly 100. Halt on drift.

    Step 3 — **HIGHEST PRIORITY: PROBE cl8tt raster image-import-NN.jpg index (RESEARCH § Pitfall 3 + Open Question 1):**
    Call `mcp__pencil__batch_get({ nodeIds: ['cl8tt'], readDepth: 2 })`. Read the `fill.image` reference (or descendant rectangle's `fill.image` if cl8tt is a frame containing a single rect). Expected structure per RESEARCH Example 2:
    - `cl8tt.fill = { type: "image", image: "image-import-NN.jpg" }` OR
    - `cl8tt.children[0].fill = { type: "image", image: "image-import-NN.jpg" }`

    Record the discovered `image-import-NN.jpg` index in 27-02-SUMMARY scratch IMMEDIATELY. This index is:
    - The visual calibration target for Task 9 side-by-side gate.
    - The description identifier suffix: `27-contact-{section}--side-by-side.png (paired with design/images/image-import-NN.jpg)`.
    - Recorded in PEN-INVENTORY § Calendly Wiring Map adjacent OR § Frames Inventory cl8tt row notes per D-104.

    If `fill.image` is not directly readable from cl8tt OR its first child (Pencil schema may use different property name like `image` or `imageRef`), recursively walk `cl8tt` via readDepth: 3 — fall back to identifying the only image-fill node inside the cl8tt subtree.

    Step 4 — Plan 27-00 + 27-01 deliverable intactness:
    `mcp__pencil__batch_get({ readDepth: 1, nodeIds: [...] })` with all Plan 27-00 IDs (7 primitives + 1 compound + label-slot extension) AND Plan 27-01 `<thankYouPageId>`. Confirm structural intactness.

    Step 5 — Phase 24/25 baseline component intactness (Plan 27-02 instances these):
    `batch_get` on:
    - `G0wNOc` (Section/Header), `Xs0Hs` (Section/Footer), `t40xct` (Compound/Card)
    - `nwJk7` (Primitive/Input/Default — parent properties unchanged from Phase 24)
    - Plan 27-00 Textarea variant id, 3 Select variants, 3 Checkbox variants, CheckboxGroup compound (from 27-00-SUMMARY)
    - `M7eUr` (Primitive/Button/Default for Submit Button + Card footer-actions Button)
    - Section/CTA `Hs5rc` NOT used in Plan 27-02 per D-96 (sidebar is Card, not CTA)

    Step 6 — Read v1.3 ContactSection source for verbatim content:
    `Read` `src/components/homepage/ContactSection.astro` lines 1-204. Extract per D-97 + D-98:
    - Line 18 (or near): page-intro heading "Let's Talk"
    - Page-intro body: "Tell me about your project and I'll get back to you within 48 hours."
    - Lines ~30-162: 8 field labels + placeholders + Select options (5 Budget + 4 Timeline) + CheckboxGroup options (5 Solutions)
    - Line 171: Submit Button label "Send Message"
    - Line 175: privacy line "Your info stays between us. No spam, ever."
    - Line 184: sidebar Card title "Ready to chat?"
    - Line 187: sidebar Card body "Schedule a discovery call with me directly!"
    - Line 191: sidebar Calendly URL `https://calendly.com/me--juoi/discovery-call`
    - Line 196: sidebar Card Button label "Book a Call"
    - Validation truth for Message field (used to confirm D-97 footnote — required:true in validationConfig despite "(optional)" in label)

    Record all extracted verbatim strings in 27-02-SUMMARY scratch.

    Step 7 — Look up Compound/Card descendant IDs from `25-03-SUMMARY.md` for sidebar Card descendants override map (image-slot id FGdti + title-slot text id kI3bc + body-slot text id ASA0X + footer-actions-slot frame id eNqxd + Read More CTA ref id k20bt + Button label text id ATJK9). Record in scratch.

    No mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token: 100. cl8tt raster index PROBED: image-import-NN.jpg (recorded in scratch). Plan 27-00 + 27-01 deliverables intact. Phase 24/25 baselines intact. v1.3 ContactSection verbatim strings extracted (page-intro + 8 fields + Submit + privacy + sidebar + Calendly URL)."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `get_variables({})` returned exactly 100 tokens.
    - **`batch_get(['cl8tt'], readDepth: 2)` returned cl8tt's structure and its `fill.image` reference was identified — the matching `image-import-NN.jpg` index is recorded in 27-02-SUMMARY scratch (Plan 27-02's highest-priority probe deliverable per RESEARCH § Pitfall 3).**
    - `batch_get` on Plan 27-00 IDs (7 primitives + 1 compound + label-slot extension) + Plan 27-01 `<thankYouPageId>` confirms intactness.
    - `batch_get` on G0wNOc, Xs0Hs, t40xct, nwJk7, M7eUr confirms baseline intactness.
    - v1.3 ContactSection verbatim strings extracted from `src/components/homepage/ContactSection.astro` (page-intro heading + body + 8 field labels + 8 placeholders + 5 Budget Select options + 4 Timeline Select options + 5 Solutions Checkbox labels + Submit Button label + privacy line + sidebar title + sidebar body + Calendly URL + sidebar Button label) — recorded in scratch.
    - Compound/Card descendant IDs (image/title/body/footer-actions slot IDs + Button label ATJK9) looked up from 25-03-SUMMARY.
    - No mutations in Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight assertions pass. cl8tt raster index probed and recorded (highest-priority deliverable). v1.3 verbatim content captured. Section/Card descendants override IDs ready. Plan 27-02 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `Contact` top-level page frame via find_empty_space_on_canvas with nodeId:<thankYouPageId> anchor + batch_design Insert</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 10.4 (FindEmptySpace nodeId anchor pattern)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-101 (Plan 27-02 anchors on Thank-you frame from 27-01)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Example 5 (FindEmptySpace nodeId anchor — expected coords `x: ~20887.27, y: ~-4111.55` for Contact)
    - Task 0 scratch: `<thankYouPageId>` from 27-01-SUMMARY
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — FindEmptySpace with `<thankYouPageId>` anchor:
    `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 2000, direction: "right", padding: 80, nodeId: "<thankYouPageId>" })`. Expected `{x, y}` ≈ `{ x: 20887.27, y: -4111.55 }` (same page-frame row as Phase 26 FAQ + 404 + Plan 27-01 Thank-you). Height 2000 estimate = Header (~80) + page-intro (~150) + form-and-sidebar grid (~1200, depends on form column field count + sidebar Card visual height) + Footer (~500) + spacing.

    Step 3 — `batch_design` Insert top-level frame:
    - Parent: document root (Pitfall 3 carry-forward).
    - `type: "frame"`, `name: "Contact"`, `placeholder: true` (clear in Task 8 with `fit_content` switch per § 10.8), `position: { x, y }` from FindEmptySpace.
    - Dimensions: `width: 1440`, `height: 2000`.
    - Layout: `layout: "vertical"`, `gap: 0`, `padding: 0`, `alignItems: "center"`.
    - Fill: `"#ffffffff"` (color-semantic-bg-page literal per Pitfall 7).

    Record `<contactPageId>`, returned `{x, y}` coords in 27-02-SUMMARY scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get(['<contactPageId>'])` returns a top-level frame named "Contact" at document root level, width 1440, vertical auto-layout, alignItems center, fill #ffffffff, position y ≈ -4111 (same page-frame row).
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `find_empty_space_on_canvas({ ..., nodeId: '<thankYouPageId>' })` returned `{x, y}` with y ≈ -4111 (same row).
    - `batch_design` Insert completed without error.
    - `batch_get(['<contactPageId>'])` returns a frame at document root level (NOT inside library parents or other page frames) named "Contact" with width 1440, vertical auto-layout, alignItems center, fill #ffffffff.
    - No mutations to baseline IDs or `<thankYouPageId>`.
    - `<contactPageId>` + coords recorded.
  </acceptance_criteria>
  <done>
    Empty Contact page frame in place at canvas page-frame row.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header ref + page-intro section (v1.3 verbatim "Let's Talk" heading + 48-hours body)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-77 (Header instanced as-is) + D-95 page-intro composition + D-98 microcopy verbatim
    - Task 0 scratch: v1.3 page-intro heading + body verbatim strings
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` Task 2 page-intro pattern (FAQ analogous)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 2 children of `<contactPageId>` (first 2):

    **Insert 1 — Section/Header ref:**
    - `type: "ref"`, `ref: "G0wNOc"`.
    - NO descendants override per D-77 carry-forward.

    **Insert 2 — page-intro section frame:**
    - `type: "frame"`, `name: "page-intro"`, `width: 1200`, `layout: "vertical"`, `gap: 12`, `padding: [64, 0]` (section-y rhythm), `alignItems: <"flex_start" or "center" per Claude's Discretion + cl8tt probe>`. Default: `"flex_start"` (left-aligned, matches agency-template Crito visual register; cl8tt raster probe in Task 0 may confirm). Plan-execution may pivot to "center" if cl8tt shows centered page-intro.
    - Child 1 — Heading text (v1.3 verbatim per D-98):
      `{ type: "text", name: "heading", content: "Let's Talk", fontFamily: "Plus Jakarta Sans", fontSize: 48, fontWeight: "700", fill: "#141f39ff", lineHeight: 1.4 }` — uses `type-semantic-heading-1` typography.
    - Child 2 — Body text (v1.3 verbatim per D-98):
      `{ type: "text", name: "body", content: "Tell me about your project and I'll get back to you within 48 hours.", fontFamily: "Inter", fontSize: 16, fontWeight: "400", fill: "#52525bff", lineHeight: 1.625 }` — uses `type-semantic-prose-paragraph-*` typography per D-71 carry-forward.

    Pitfall 7 reminder: all literal hex; Pitfall 5 reminder: text-clipping false-positives expected on body.

    Record `<pageIntroId>`, `<pageIntroHeadingTextId>`, `<pageIntroBodyTextId>` in scratch.
  </action>
  <verify>
    `batch_get(['<contactPageId>'], readDepth: 2)` shows 2 direct children: G0wNOc ref + page-intro frame. page-intro contains heading "Let's Talk" (Plus Jakarta Sans 48/700) + body "Tell me about your project and I'll get back to you within 48 hours." (Inter 16/400).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<contactPageId>'])` returns 2 children: Section/Header ref + page-intro frame.
    - Section/Header ref has NO descendants override (D-77 compliance).
    - page-intro frame has width 1200, vertical layout, gap 12, padding [64, 0].
    - Heading content matches v1.3 EXACTLY: "Let's Talk" (apostrophe preserved).
    - Body content matches v1.3 EXACTLY: "Tell me about your project and I'll get back to you within 48 hours." (apostrophe preserved).
    - Heading typography: Plus Jakarta Sans 48/700/lh 1.4 fill #141f39ff.
    - Body typography: Inter 16/400/lh 1.625 fill #52525bff.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Top of Contact: Header + page-intro section (v1.3 verbatim "Let's Talk").
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert 2-col asymmetric grid frame (form-column 760 left + sidebar-column 376 right; horizontal-layout flex_start)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-95 (2-col grid; form 2fr + sidebar 1fr; alignItems flex_start matching v1.3 items-start)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 6 (2-col grid mechanics — Pencil auto-layout is flex-based, NO fr units; fixed widths express the ratio)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 1 child of `<contactPageId>` (3rd of vertical stack) + its 2 column children:

    **Insert — 2-col asymmetric grid frame:**
    - `type: "frame"`, `name: "form-and-sidebar-grid"`, `width: 1200`, `layout: "horizontal"`, `gap: 32`, `alignItems: "flex_start"` (top-aligned per v1.3 `items-start` per D-95), `padding: [64, 0]` (section-y rhythm; or 0 if outer page-intro already handled padding — Claude's Discretion based on cl8tt probe).
    - Child 1 — form-column frame:
      - `type: "frame"`, `name: "form-column"`, `width: 760` (~2fr of 1168 effective after 32 gap; alternative 800 if cl8tt probe shows simpler ratio per RESEARCH Pattern 6), `layout: "vertical"`, `gap: 24` (between fields), `padding: 0`.
    - Child 2 — sidebar-column frame:
      - `type: "frame"`, `name: "sidebar-column"`, `width: 376` (~1fr; or 368 if 800/32/368 ratio chosen), `layout: "vertical"`, `gap: 16`, `padding: 0`.

    Total: 760 + 32 + 376 = 1168 (fits inside 1200 with minor padding tolerance per RESEARCH Pattern 6).

    Record `<gridFrameId>`, `<formColumnId>`, `<sidebarColumnId>` in scratch.
  </action>
  <verify>
    `batch_get(['<gridFrameId>'], readDepth: 1)` returns a horizontal-layout frame with 2 children: form-column (width 760) + sidebar-column (width 376), gap 32, alignItems flex_start.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<contactPageId>'])` now has 3 children: G0wNOc ref + page-intro + form-and-sidebar-grid.
    - form-and-sidebar-grid is a horizontal-layout frame, width 1200, gap 32, alignItems flex_start.
    - form-and-sidebar-grid has 2 children: form-column (width 760, vertical, gap 24) + sidebar-column (width 376, vertical, gap 16).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    2-col asymmetric grid in place; form column + sidebar column ready for content insertion.
  </done>
</task>

<task type="auto">
  <name>Task 4: Insert 8-field form composition into form-column (chunked per Pitfall 9 — fields 1-4 first chunk + fields 5-8 + Submit + privacy second chunk)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-97 (8 fields verbatim) + D-98 (microcopy verbatim)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pattern 7 (8-field shape table) + § Pitfall 9 (chunk batch_design ≤ 25 ops)
    - Task 0 scratch: 8 field labels + placeholders + Select options + Checkbox options + Submit label + privacy line + v1.3 Message validation truth
    - Plan 27-00 IDs from 27-00-SUMMARY: `<textareaVariantId>`, `<selectDefaultId>`, `<checkboxGroupId>`, `<checkboxDefaultId>` (for CheckboxGroup options-slot override)
    - Plan 27-00 label-slot extension structure: required-mark child ID + optional-text child ID inside oCeJP (or `<newLabelSlotId>` if fallback)
    - `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` (nwJk7 label slot oCeJP, control slot bhkR3, placeholder dbEyb, etc.) + M7eUr Button label id ATJK9 + V4Dx4i arrow icon
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    **Chunk A (Pitfall 9 — fields 1-4 + initial structure, ≈ 20 ops):**

    Insert as children of `<formColumnId>` in order:

    **Field 1 — Name (Primitive/Input/Default + required-mark enabled:true):**
    - `{ type: "ref", ref: "nwJk7", descendants: { "<oCeJP-or-newLabelSlotId>": { /* no override needed — label slot is the frame; need to override individual children */ }, "<labelTextId>": { content: "Name" }, "<requiredMarkId>": { enabled: true }, "<controlPlaceholderId-dbEyb>": { content: "Your name" } } }`
    - Note: the descendants override map references specific descendant IDs of oCeJP (label-text + required-mark + optional-text) AND the control's placeholder descendant from Phase 24 (`dbEyb` per 24-03-SUMMARY). Plan-execution looks up exact IDs from 27-00-SUMMARY (label-slot children) + 24-03-SUMMARY (control placeholder dbEyb).

    **Field 2 — Email (Input/Default + required-mark enabled:true):**
    - Same shape as Field 1: descendants override `<labelTextId>: { content: "Email" }`, `<requiredMarkId>: { enabled: true }`, `<placeholderId>: { content: "you@example.com" }`.

    **Field 3 — Company (Input/Default + optional-text enabled:true):**
    - Same shape: `<labelTextId>: { content: "Company" }`, `<optionalTextId>: { enabled: true }`, `<placeholderId>: { content: "Company name (if applicable)" }`.

    **Field 4 — Challenges (Input/Textarea variant + optional-text enabled:true):**
    - `{ type: "ref", ref: "<textareaVariantId>", descendants: { "<labelTextId-in-textarea>": { content: "What challenges are you facing?" }, "<optionalTextId-in-textarea>": { enabled: true }, "<placeholderId-in-textarea>": { content: "What problems are slowing you down, causing headaches, or just keeping you from doing the work you love?" } } }`
    - Note: Textarea has its own label-slot/control/helper/errorSlot descendant IDs from Plan 27-00 (recorded in 27-00-SUMMARY); plan-execution uses those IDs not nwJk7's.

    Record field instance IDs `<field1Id>` ... `<field4Id>` in scratch.

    **Chunk B (Pitfall 9 — fields 5-8 + Submit + privacy, ≈ 20 ops):**

    Pre-flight per D-103 again before second batch_design call.

    **Field 5 — Solutions (Compound/CheckboxGroup with 5 children override):**
    - `{ type: "ref", ref: "<checkboxGroupId>", descendants: { "<legendSlotId>": { content: "What kinds of solutions do you think you'll need?" }, "<optionsSlotId>": { contains: [ /* 5 Checkbox refs */ ] } } }`
    - Override `options-slot` contains to 5 Checkbox/Default refs:
      - `{ type: "ref", ref: "<checkboxDefaultId>", descendants: { "<checkbox-label-slot-id>": { content: "AI" } } }`
      - Same shape × 4 more with labels "Automations", "Web Apps", "Consultation", "Not Sure" (v1.3 verbatim).
    - Layout default: vertical-stack per Plan 27-00 default + Claude's Discretion (matches v1.3 `space-y-3`). Plan-execution probes cl8tt raster shape at Task 9 calibration; default vertical.

    **Field 6 — Budget (Primitive/Select/Default):**
    - `{ type: "ref", ref: "<selectDefaultId>", descendants: { "<labelTextId-in-select>": { content: "Do you have a budget in mind?" }, "<placeholderId-in-select>": { content: "Select budget range (optional)" } } }`
    - Note: 5 Budget options (Under $2K, $2K-5K, $5K-10K, $10K-25K, $25K+) per v1.3 are NOT depicted in `.pen` per D-92 closed-state only.

    **Field 7 — Timeline (Primitive/Select/Default):**
    - Same shape as Field 6: label "Do you have a timeline in mind?", placeholder "Select timeline (optional)".
    - 4 Timeline options (This week, This month, This quarter, Flexible) per v1.3 NOT depicted per D-92.

    **Field 8 — Message (Input/Textarea + required-mark enabled:true AND optional-text enabled:true per D-97 v1.3 inconsistency note):**
    - `{ type: "ref", ref: "<textareaVariantId>", descendants: { "<labelTextId-in-textarea>": { content: "Message" }, "<requiredMarkId-in-textarea>": { enabled: true }, "<optionalTextId-in-textarea>": { enabled: true }, "<placeholderId-in-textarea>": { content: "Anything else you'd like to share about your project, goals, or how I can help?" } } }`
    - Both required-mark AND optional-text enabled — documents v1.3 inconsistency for code-milestone resolution. Raises OPEN-27-NN row at Task 11.

    **Submit Button (M7eUr ref):**
    - `{ type: "ref", ref: "M7eUr", descendants: { "ATJK9": { content: "Send Message" }, "V4Dx4i": { enabled: true } } }` (V4Dx4i = arrow-trailing icon per Phase 24 D-29; Claude's Discretion enable per visual consistency with FAQ Section/CTA Button precedent).

    **Privacy line plain text:**
    - `{ type: "text", name: "privacy-line", content: "Your info stays between us. No spam, ever.", fontFamily: "Inter", fontSize: 14, fontWeight: "400", fill: "#52525bff", lineHeight: 1.4 }` — uses `type-semantic-body-sm` + `color-semantic-text-secondary`.

    Record field instance IDs `<field5Id>` ... `<field8Id>` + `<submitButtonRefId>` + `<privacyLineTextId>` in scratch.

    Pitfall 7 reminder: all hex/px literal. Pitfall 9: chunked to ≤ 25 ops each. Pitfall 5: text-clipping false-positives anticipated on long Challenges + Message placeholder text + multi-line CheckboxGroup.
  </action>
  <verify>
    `batch_get(['<formColumnId>'], readDepth: 2)` returns 10 children in order: 4 field refs (Input/Input/Input/Textarea) + 1 CheckboxGroup ref + 2 Select refs + 1 Textarea ref + 1 Submit Button ref + 1 privacy text. Each field has descendants override producing v1.3 verbatim labels + placeholders + required-mark/optional-text enabled per D-97.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED before each chunk.
    - 2 `batch_design` chunks completed without error.
    - `batch_get(['<formColumnId>'])` returns 10 direct children in expected order: 8 field instances + Submit Button + privacy text.
    - Field 1 (Name) ref: descendants override label "Name" + required-mark enabled:true + placeholder "Your name".
    - Field 2 (Email) ref: descendants override label "Email" + required-mark enabled:true + placeholder "you@example.com".
    - Field 3 (Company) ref: descendants override label "Company" + optional-text enabled:true + placeholder "Company name (if applicable)".
    - Field 4 (Challenges) ref: type:ref to `<textareaVariantId>`; descendants label + optional-text enabled:true + placeholder (long v1.3 verbatim string).
    - Field 5 (Solutions) ref: type:ref to `<checkboxGroupId>`; descendants legend + 5 Checkbox refs with labels AI/Automations/Web Apps/Consultation/Not Sure (v1.3 verbatim).
    - Field 6 (Budget) ref: type:ref to `<selectDefaultId>`; descendants label + placeholder; NO open-dropdown variant (D-92).
    - Field 7 (Timeline) ref: type:ref to `<selectDefaultId>`; descendants label + placeholder.
    - Field 8 (Message) ref: type:ref to `<textareaVariantId>`; descendants label "Message" + BOTH required-mark enabled:true AND optional-text enabled:true (D-97 inconsistency documented).
    - Submit Button ref: type:ref to `M7eUr`; descendants ATJK9 label "Send Message" v1.3 verbatim.
    - Privacy line text: content "Your info stays between us. No spam, ever." v1.3 verbatim, Inter 14/400/lh 1.4, #52525bff.
    - All v1.3 strings preserved EXACTLY (apostrophes, hyphens, character-perfect).
    - ROADMAP success criterion 2 satisfied: "8-field lead-qualification form is composed entirely of `Primitive / Input` instances (no inlined input markup)" — confirmed because every field is a ref to a Phase 24/27 library entry.
    - All IDs recorded in scratch.
  </acceptance_criteria>
  <done>
    8-field form composition complete in form-column. Plan 27-00 primitives stress-tested via Joel's real form. OPEN-27-NN candidates (Message validation inconsistency) identified.
  </done>
</task>

<task type="auto">
  <name>Task 5: Insert Compound/Card sidebar instance + sibling Calendly wiring Pencil note into sidebar-column</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-96 (sidebar = Compound/Card NOT Section/CTA; image-slot enabled:false; title/body/footer-actions filled per v1.3 verbatim) + D-93 (sibling Calendly note convention) + D-94 (both Calendly URLs)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Example 4 (Compound/Card sidebar instance batch_design shape)
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` Compound/Card structure (t40xct + slots: FGdti image / vGH3A title frame / kI3bc title text / oTSwn body frame / ASA0X body text / eNqxd footer-actions frame / k20bt Read More ref)
    - Task 0 scratch: v1.3 sidebar verbatim strings + Calendly URL (calendly.com/me--juoi/discovery-call)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 2 children of `<sidebarColumnId>`:

    **Insert 1 — Compound/Card sidebar instance per D-96:**
    - `{ type: "ref", ref: "t40xct", descendants: { "FGdti": { enabled: false }, "kI3bc": { content: "Ready to chat?" }, "ASA0X": { content: "Schedule a discovery call with me directly!" }, "ATJK9": { content: "Book a Call" } } }`
    - `FGdti` = image-slot frame; setting `enabled: false` suppresses the placeholder image per D-96 (no image in v1.3 sidebar Card).
    - `kI3bc` = title text node (descendant of vGH3A title-slot); content = v1.3 line 184 verbatim.
    - `ASA0X` = body text node (descendant of oTSwn body-slot); content = v1.3 line 187 verbatim.
    - `ATJK9` = M7eUr Button label text node (descendant of k20bt Read More ref inside eNqxd footer-actions-slot); content = v1.3 line 196 verbatim ("Book a Call").
    - Note: instance receives Compound/Card's structural width 320 (library preview default per 25-03-SUMMARY). Sidebar column is 376 width; Card centers within 376 OR overrides width to fill_container — plan-execution decides (default: keep 320 library width; the 56px difference becomes margin/centering inside sidebar column).

    **Insert 2 — Sibling Calendly wiring Pencil note per D-93:**
    - `{ type: "note", parent: "<sidebarColumnId>" (as sibling child of sidebar Card in vertical stack), content: "Calendly placeholder (Phase 27 D-93/D-94) — code milestone wires Compound/Card sidebar footer-actions Button[0] `href` to https://calendly.com/me--juoi/discovery-call (v1.3 ContactSection.astro line 191). Cross-reference: PEN-INVENTORY § Calendly Wiring Map (Phase 27) row 2 (Contact sidebar consumer). Sidebar Card uses Compound/Card (Phase 25 t40xct) NOT Section/CTA — different visual shape (sidebar card vs full-width section) per D-96; same wiring mechanism per D-93.", fontFamily: "Inter", fontSize: 12, width: 376 }`

    Record `<sidebarCardRefId>` + descendants override map + `<sidebarCalendlyNoteId>` in scratch.
  </action>
  <verify>
    `batch_get(['<sidebarColumnId>'], readDepth: 2)` returns 2 children: Compound/Card ref (t40xct) + Calendly wiring sibling note. The Card descendants override produces: image-slot disabled, title "Ready to chat?", body "Schedule a discovery call with me directly!", footer-actions Button "Book a Call". Sibling note content contains "calendly.com/me--juoi/discovery-call".
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<sidebarColumnId>'])` returns 2 children: Card ref + Calendly sibling note.
    - Card ref descendants override: FGdti `enabled: false` (image suppressed); kI3bc content "Ready to chat?" (v1.3 line 184 verbatim); ASA0X content "Schedule a discovery call with me directly!" (line 187 verbatim); ATJK9 content "Book a Call" (line 196 verbatim).
    - Sidebar Calendly Pencil note content contains the string "calendly.com/me--juoi/discovery-call" + cross-references PEN-INVENTORY § Calendly Wiring Map (Phase 27) row 2.
    - t40xct parent unchanged (instance-only ref).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Sidebar Card + Calendly wiring note in place. Second cross-phase consumer of Phase 25 Compound/Card validates D-49 reusability claim.
  </done>
</task>

<task type="auto">
  <name>Task 6: Insert Section/Footer ref as last child of Contact page frame</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-77 (Footer Crito-source labels stay)
    - `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` Section/Footer Xs0Hs structural reference
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — `batch_design` Insert 1 child of `<contactPageId>` (4th of vertical stack, after form-and-sidebar-grid):
    - `type: "ref"`, `ref: "Xs0Hs"`.
    - NO descendants override per D-77 carry-forward.

    Record `<contactFooterRefId>` in scratch.
  </action>
  <verify>
    `batch_get(['<contactPageId>'], readDepth: 1)` returns 4 direct children in order: G0wNOc ref + page-intro frame + form-and-sidebar-grid frame + Xs0Hs ref.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<contactPageId>'])` returns 4 children: Section/Header ref + page-intro + form-and-sidebar-grid + Section/Footer ref.
    - Section/Footer ref has NO descendants override (D-77 compliance).
    - `<contactFooterRefId>` recorded.
  </acceptance_criteria>
  <done>
    Contact page frame structurally complete: Header + page-intro + 2-col grid (form + sidebar) + Footer.
  </done>
</task>

<task type="auto">
  <name>Task 7: snapshot_layout sweep + page-frame fit_content + per-section screenshots for calibration gate (anticipate text-clipping false-positives on textarea + CheckboxGroup)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4.4 steps 7-9 + § 10.8 fit_content pattern + § 6.4 OPEN-26-02 stale-cache Tier-1/Tier-2 workaround
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pitfall 5 (multi-line text — Phase 27 textarea + CheckboxGroup WILL trigger more false-positives than Phase 26)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md` Task 4 + 5 fit_content + Tier-2 fallback pattern
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — Document-level snapshot:
    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })`. Expected: `"No layout problems."`

    Step 3 — Per-frame snapshot:
    `mcp__pencil__snapshot_layout({ rootId: "<contactPageId>", problemsOnly: true })`. Document text-clipping false-positives per Pitfall 5 — Phase 27 multi-line content (Challenges + Message textareas, CheckboxGroup vertical-stack, long placeholders) WILL trigger more false-positives than Phase 26.

    Step 4 — Page-frame fit_content + position clear per § 10.8:
    `Update("<contactPageId>", { placeholder: false, height: "fit_content" })`. Document before/after dimensions.

    Step 5 — Per-section screenshots for Task 9 calibration gate (crito-source-flat-raster format):
    `mcp__pencil__get_screenshot({ nodeId: "<pageIntroId>" })` — page-intro inline render.
    `mcp__pencil__get_screenshot({ nodeId: "<formColumnId>" })` — form section inline render.
    `mcp__pencil__get_screenshot({ nodeId: "<sidebarCardRefId>" })` OR `<sidebarColumnId>` — sidebar Card inline render.
    `mcp__pencil__get_screenshot({ nodeId: "<contactPageId>" })` — full page inline render.

    **OPEN-26-02 stale-cache workaround per § 6.4** (carry-forward):
    If blank-white returns:
    - **Tier-1:** cross-row Update on Contact frame + move back.
    - **Tier-2:** user-side editor fallback — instruct user at Task 9 AskUserQuestion to verify visually in Pencil editor.

    Step 6 — Record outcomes in scratch.
  </action>
  <verify>
    Document-level `snapshot_layout` returns "No layout problems." Page frame Updated to `fit_content`. Per-section + full-page inline screenshots captured (or Tier-1/Tier-2 fallback applied).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns "No layout problems." at document level.
    - Per-frame snapshot captured; text-clipping false-positives documented per Pitfall 5 (NOT mitigated — known benign; anticipate more than Phase 26 due to multi-line content).
    - Page frame Updated: `placeholder: false`, `height: "fit_content"`.
    - Per-section + full-page screenshots captured OR Tier-1/Tier-2 fallback applied + documented.
    - Outcomes recorded in scratch.
  </acceptance_criteria>
  <done>
    Layout-clean; page sized to content; per-section screenshots ready for crito-source-flat-raster calibration gate.
  </done>
</task>

<task type="auto">
  <name>Task 8: Identify side-by-side calibration pairing identifier for Task 9 gate</name>
  <files>None (no Pencil mutation — preparation step)</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.1 (crito-source side-by-side pairing) + § 3.5 (description identifier convention `27-contact-{section}--side-by-side.png` paired with `design/images/image-import-NN.jpg`)
    - Task 0 scratch: probed image-import-NN.jpg index for cl8tt
    - Task 7 scratch: captured inline screenshots
  </read_first>
  <action>
    Step 1 — Compile the description identifiers for Task 9 AskUserQuestion gate per § 3.5 convention:

    Description identifiers (D-64 — inline only per OPEN-23-01, NOT disk-written):
    - `27-contact-page-intro--side-by-side.png (paired with design/images/<image-import-NN-from-Task-0>.jpg)`
    - `27-contact-form--side-by-side.png (paired with design/images/<image-import-NN-from-Task-0>.jpg)`
    - `27-contact-sidebar--side-by-side.png (paired with design/images/<image-import-NN-from-Task-0>.jpg)`
    - `27-contact-full-page--side-by-side.png (paired with design/images/<image-import-NN-from-Task-0>.jpg)`

    Step 2 — Compile per-section fidelity proposals per D-83:
    - page-intro: EXACT (token-bound + content verbatim per D-98)
    - form-section: EXACT (token-bound + 8 fields v1.3 verbatim per D-97; Joel's content shape is authority per D-97; cl8tt raster is VISUAL calibration target only — anticipate any mismatch between v1.3 8-field shape and cl8tt's generic-Crito-template 4-or-6-field rendering)
    - sidebar Card: EXACT (token-bound + content verbatim per D-98)
    - Header + Footer: EXACT (Phase 25 shipped, D-77)

    Step 3 — Compile candidate OPEN-27-NN flags (anticipated per CONTEXT + RESEARCH):
    - **OPEN-27-NN (Message validation inconsistency)** — D-97 footnote: v1.3 Message has `valueMissing` in validationConfig (treats as required) but label "(optional)"; Phase 27 ships BOTH required-mark + optional-text enabled to document for code-milestone resolution.
    - **OPEN-27-NN (cl8tt raster image-import-NN.jpg identification)** — Plan 27-02 Task 0 probed; if probe revealed unexpected raster filename pattern, document.
    - **OPEN-27-NN (focus-ring color contrast on Send Message Button)** — per OPEN-24-04 partial-resolution; if user notices contrast issue at calibration gate.
    - **OPEN-27-NN (CheckboxGroup layout — vertical vs horizontal)** — if cl8tt raster shows horizontal layout for Solutions options (unlikely per v1.3 space-y-3 default + Crito generic-template Contact form rarely depicting 5-option checkbox groups).

    Step 4 — Document all pairings + fidelity proposals + candidate OPEN flags in scratch for Task 9 AskUserQuestion description.

    No mutation; pure preparation.
  </action>
  <verify>
    Scratch contains: 4 description identifiers with image-import-NN.jpg embedded; 5 per-section fidelity proposals per D-83; 4 candidate OPEN-27-NN flag descriptions ready.
  </verify>
  <acceptance_criteria>
    - Description identifiers compiled with the actual probed image-import-NN.jpg index from Task 0.
    - Per-section fidelity proposals (page-intro / form-section / sidebar Card / Header / Footer) recorded.
    - Candidate OPEN-27-NN flag descriptions documented (especially Message validation inconsistency — highly anticipated per D-97 footnote).
    - No mutation issued.
  </acceptance_criteria>
  <done>
    Calibration gate preparation complete. Task 9 AskUserQuestion description ready.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 9: Calibration spot-check user gate (crito-source-flat-raster branch — side-by-side with cl8tt image-import-NN.jpg per CALIBRATION-PROTOCOL § 3.4 step 5)</name>
  <files>None (presentation-only — uses Task 7 inline screenshots + cl8tt raster pairing)</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.4 step 5 (crito-source AskUserQuestion format) + § 3.4 step 6 (APPROVE/REVISE/GAP options) + § 3.2 (crito-source fidelity definitions) + § 5 per-section labels
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-102 (Plan 27-02 single calibration gate at plan close — crito-source format)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Example 7 (crito-source side-by-side AskUserQuestion shape — concrete description with proposals)
    - Task 0 scratch: cl8tt image-import-NN.jpg index
    - Task 7 scratch: inline screenshots (or Tier-2 fallback note)
    - Task 8 scratch: description identifiers + fidelity proposals + candidate OPEN-27-NN
  </read_first>
  <action>
    Render the AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 3.4 step 5 crito-source-flat-raster format. Inline-render the Task 7 captured screenshots above the description; also reference `design/images/image-import-NN.jpg` (the cl8tt source) explicitly so user can side-by-side compare.

    **Question:** "Calibration spot-check — Contact page (crito-source-flat-raster branch per CONTEXT D-95 + CALIBRATION-PROTOCOL § 3). Does the reconstructed Contact frame match the cl8tt raster's visual rhythm + Joel's v1.3 content shape per D-97 hybrid authority?"

    **Description (markdown — inline screenshots rendered above per § 6.1):**

    ```
    ## Calibration Spot-Check — Contact Page (Crito-Source Flat-Raster Branch per CALIBRATION-PROTOCOL § 3)

    **Inline renders shown above:**
    - Reconstructed page-intro section
    - Reconstructed form-section (8 fields + Submit + privacy)
    - Reconstructed sidebar Card
    - Full Contact frame (reconstructed)

    [If Tier-2 fallback applied: "Screenshots returned blank-white per OPEN-26-02; verify visually in Pencil editor by selecting `<contactPageId>` at canvas position y ≈ -4111."]

    **cl8tt raster source (CALIBRATION-PROTOCOL § 3.1 visual calibration target):**
    `design/images/image-import-NN.jpg` (probed at Task 0 — index recorded in 27-02-SUMMARY)

    ### Section 1: page-intro
    Fidelity label proposal: **EXACT** per § 3.2 (token-bound + content verbatim per D-98)

    **Content (v1.3 verbatim per D-98):**
    - Heading: "Let's Talk"
    - Body: "Tell me about your project and I'll get back to you within 48 hours."

    **Tokens:** type-semantic-heading-1-* (Plus Jakarta Sans 48/700) + type-semantic-prose-paragraph-* (Inter 16/400) + color-semantic-text-primary / -secondary fills.

    ### Section 2: form-section
    Fidelity label proposal: **EXACT** per § 3.2 (token-bound + 8 fields v1.3 verbatim per D-97; cl8tt raster is VISUAL calibration target only — Joel's 8-field SHAPE is the authority per D-97 hybrid)

    **8 fields per D-97 (v1.3 verbatim):**
    1. Name (required, single-line) — Primitive/Input/Default + required-mark — placeholder "Your name"
    2. Email (required, single-line) — Primitive/Input/Default + required-mark — placeholder "you@example.com"
    3. Company (optional, single-line) — Primitive/Input/Default + optional-text — placeholder "Company name (if applicable)"
    4. Challenges (optional, textarea) — Primitive/Input/Textarea + optional-text
    5. Solutions (optional, CheckboxGroup × 5) — AI / Automations / Web Apps / Consultation / Not Sure
    6. Budget (optional, Select closed-state per D-92) — placeholder "Select budget range (optional)" — 5 popover options NOT depicted (code-side)
    7. Timeline (optional, Select closed-state) — placeholder "Select timeline (optional)" — 4 popover options NOT depicted
    8. Message (required per v1.3 validation truth, textarea — D-97 inconsistency note) — required-mark AND optional-text BOTH enabled

    Plus: Submit Button "Send Message" + privacy line "Your info stays between us. No spam, ever."

    **2-col asymmetric grid:** form-column 760 left + sidebar-column 376 right + gap 32, alignItems flex_start (per D-95 + v1.3 items-start).

    **⚠️ Calibration nuance per D-97 (Pitfall 1 prevention):** cl8tt raster is the VISUAL calibration target for spacing rhythm + page-intro position + sidebar shape + button-then-privacy ordering + page-frame proportions. Joel's 8-field SHAPE per D-97 is the AUTHORITY for what gets shipped. If cl8tt raster depicts a Crito-template-generic 4-or-6-field form, that does NOT invalidate Joel's 8-field shape — the form composition still ships 8 fields per ROADMAP success criterion 2.

    ### Section 3: sidebar Compound/Card instance (Phase 25 t40xct — second cross-phase consumer)
    Fidelity label proposal: **EXACT** per § 3.2 (token-bound + content verbatim per D-98)

    **Content (v1.3 verbatim per D-98):**
    - Title: "Ready to chat?"
    - Body: "Schedule a discovery call with me directly!"
    - Button: "Book a Call"
    - Image-slot: enabled:false (no image in v1.3 sidebar per D-96)

    **Calendly wiring (sibling Pencil note):** `https://calendly.com/me--juoi/discovery-call` (PEN-INVENTORY § Calendly Wiring Map row 2)

    ### Section 4 + 5: Section/Header + Section/Footer instances
    Fidelity label proposal: **EXACT** (Phase 25 shipped; D-77 Crito-source labels stay)

    ### PAGE-11 status (CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7):
    **ACTIVE — cl8tt raster will be hidden via `enabled: false` ONLY AFTER user APPROVE at this gate.** First production use of PAGE-11 ACTIVE branch in v2.0. NOT hidden before APPROVE per Pitfall 4.

    **Description identifiers (D-64 — inline only per OPEN-23-01):**
    - `27-contact-page-intro--side-by-side.png (paired with design/images/image-import-NN.jpg)`
    - `27-contact-form--side-by-side.png (paired with design/images/image-import-NN.jpg)`
    - `27-contact-sidebar--side-by-side.png (paired with design/images/image-import-NN.jpg)`

    **Calibration target (§ 3.1 crito-source flat-raster):** Does the reconstructed Contact frame match the cl8tt raster's visual rhythm + Joel's v1.3 content shape per D-97 hybrid authority?
    ```

    **Options per § 3.4 step 6:**
    - "APPROVE — composition matches cl8tt's visual rhythm + Joel's content; fidelity labels per D-83 as proposed; PAGE-11 ACTIVE step proceeds (Task 10 hides cl8tt via enabled:false)"
    - "REVISE — visual or content mismatch (free-text describe; iterate via batch_design Update; re-gate)"
    - "GAP — composition reveals token-surface or primitive gap (raise OPEN-27-NN; specify)"

    Wait for user response. Record outcome in 27-02-SUMMARY scratch.

    **CRITICAL:** Task 10 (cl8tt hide via enabled:false) fires ONLY on APPROVE. If REVISE, iterate via batch_design Update + re-gate without touching cl8tt. If GAP, log OPEN-27-NN + proceed to Task 10 only if user explicitly confirms APPROVE-with-GAP — otherwise do NOT hide cl8tt and surface the gap to user for resolution decision.

    Anticipated OPEN-27-NN at this gate (per CONTEXT + RESEARCH):
    - **OPEN-27-NN (v1.3 Message validation inconsistency)**: Field 8 ships BOTH required-mark AND optional-text enabled — documents v1.3 inconsistency (label "(optional)" but validationConfig.valueMissing). Consumer phase: code milestone.
    - **OPEN-27-NN (success-color or focus-ring contrast)** if user notices.
    - **OPEN-27-NN (cl8tt raster ≠ Joel's 8-field shape)** if visual gap from cl8tt template form to Joel's lead-qualification form is notable enough to warrant documentation.
  </action>
  <verify>
    User has responded APPROVE / REVISE / GAP; response recorded in 27-02-SUMMARY scratch; any REVISE iterations resolved; any GAP logged as OPEN-27-NN. **CRITICAL: cl8tt is NOT yet mutated at this task — Task 10 happens only if APPROVE.**
  </verify>
  <acceptance_criteria>
    - Inline `get_screenshot` calls produced renders (or Tier-2 user-side editor fallback applied).
    - AskUserQuestion presented with screenshots/fallback + structured side-by-side description per § 3.4 step 5 format + per-section fidelity proposals per D-83 + cl8tt raster pairing identifier with the probed image-import-NN.jpg + PAGE-11 ACTIVE note (pending APPROVE).
    - User chose APPROVE / REVISE-then-APPROVE / APPROVE-with-GAP-logged.
    - Per-section fidelity labels per D-83 recorded in 27-02-SUMMARY scratch (page-intro=EXACT / form-section=EXACT / sidebar=EXACT / Header=EXACT / Footer=EXACT — or as user-adjusted).
    - At least 1 OPEN-27-NN raised for v1.3 Message validation inconsistency per D-97 footnote (anticipated).
    - **cl8tt has NOT been mutated at this point** — Task 10 is the gated step.
  </acceptance_criteria>
  <done>
    User has spot-checked the Contact composition against cl8tt raster — VALID-02 satisfied for crito-source-flat-raster branch. APPROVE gates Task 10 PAGE-11 ACTIVE step.
  </done>
</task>

<task type="auto">
  <name>Task 10: PAGE-11 ACTIVE — hide cl8tt raster via batch_design Update enabled:false (POST-APPROVE ONLY per CALIBRATION-PROTOCOL § 3.4 step 7)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.3 (PAGE-11 ACTIVE rule: hide via enabled:false after APPROVE) + § 3.4 step 7 (post-APPROVE timing)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-104 (cl8tt status_counts update flat:1 → hidden:1)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-RESEARCH.md` § Pitfall 4 (NEVER hide before APPROVE — explicit task ordering) + § Example 6 (concrete batch_design Update + verify pattern)
    - Task 9 scratch: user APPROVE confirmation (if REVISE or GAP-with-no-APPROVE, SKIP this task)
  </read_first>
  <action>
    **PRE-CONDITION (CRITICAL — Pitfall 4):** Task 9 user response MUST be APPROVE (or APPROVE-with-GAP-logged where user explicitly confirmed proceeding). If user response was REVISE or GAP-without-APPROVE, SKIP this task — return to Task 9 iteration loop without hiding cl8tt. Document the skip in 27-02-SUMMARY scratch.

    Step 1 — Pre-flight per D-103: `mcp__pencil__get_editor_state({ include_schema: false })`.

    Step 2 — `batch_design` Update to hide cl8tt:
    ```js
    Update("cl8tt", { enabled: false })
    ```
    This is THE FIRST production use of PAGE-11 ACTIVE in v2.0.

    Step 3 — Verify:
    `mcp__pencil__batch_get({ nodeIds: ['cl8tt'], readDepth: 0 })`. Assert:
    - `cl8tt.enabled === false`
    - `cl8tt.x` + `cl8tt.y` UNCHANGED from pre-mutation values (structural archive persists at same canvas position per § 3.3).

    Step 4 — Record in 27-02-SUMMARY scratch: pre-mutation cl8tt position + post-mutation enabled:false confirmation + canvas position unchanged + cl8tt status_counts will move from flat:1 to hidden:1 in Task 11 PEN-INVENTORY update.
  </action>
  <verify>
    `batch_get(['cl8tt'])` returns `enabled: false` with `x` + `y` unchanged. cl8tt is structurally archived in place but visually hidden in editor.
  </verify>
  <acceptance_criteria>
    - **CRITICAL: Task 9 outcome was APPROVE (or explicit APPROVE-with-GAP) — verified from scratch before this task ran.**
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design Update("cl8tt", { enabled: false })` completed without error.
    - `batch_get(['cl8tt'])` returns `enabled: false`.
    - cl8tt's canvas position (x, y) UNCHANGED from pre-mutation — structural archive persists per § 3.3.
    - PEN-INVENTORY status_counts pending update to flat:1 → hidden:1 (Task 11 executes).
    - This is the FIRST production use of PAGE-11 ACTIVE in v2.0; documented as such in scratch.
  </acceptance_criteria>
  <done>
    cl8tt raster hidden via enabled:false post-APPROVE. PAGE-11 ACTIVE first production application complete. OPEN-23-05 resolution ready for Task 11 PEN-INVENTORY annotation.
  </done>
</task>

<task type="auto">
  <name>Task 11: PEN-INVENTORY extensions (Frames Inventory + cl8tt status update + Variant Evidence Phase 27 + Calendly Map row 2 + OPEN-27-NN + OPEN-23-05 RESOLVED) + 27-02-SUMMARY.md close</name>
  <files>
    - .planning/research/PEN-INVENTORY.md
    - .planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
    - design/Crito.pen (read-only validation)
  </files>
  <read_first>
    - `.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md` D-104 (PEN-INVENTORY extension pattern)
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 7 gap declaration template
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md` Task 6 PEN-INVENTORY edit pattern + PAGE-11 INERT note pattern (Plan 27-02 inverse — ACTIVE applied)
    - 27-02-SUMMARY scratch (all IDs across Tasks 0-10 + Task 9 fidelity labels + Task 10 cl8tt mutation confirmation + OPEN-27-NN candidates)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-103.

    Step 2 — Final verification:
    `mcp__pencil__get_variables({})` — assert 100 tokens (zero drift through Plan 27-02).
    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` — assert clean at document level.
    `mcp__pencil__batch_get(['<contactPageId>'], readDepth: 2)` — confirm 4 children + 8-field form + sidebar Card.
    `mcp__pencil__batch_get(['cl8tt'], readDepth: 0)` — confirm enabled:false.

    Step 3 — Edit `.planning/research/PEN-INVENTORY.md`:

    A. Frames Inventory table — add NEW row for `Contact`:
    - name: "Contact"
    - frame_id: `<contactPageId>`
    - scope: `IN-SCOPE` (per Phase 23 audit + ROADMAP PAGE-05)
    - joel_page_map: `/contact` (v1.3 has no /contact page route — ContactSection.astro is the homepage section; mismatch documented per CONTEXT § Code Insights)
    - child_section_count: 4 (Header ref + page-intro + form-and-sidebar-grid + Footer ref)
    - status: `reconstructed-PHASE-27`
    - notes: "Crito-source flat-raster reconstruction per § 3 branch; cl8tt source raster hidden via enabled:false per PAGE-11 ACTIVE step (first production use in v2.0); 8-field form composed entirely of Plan 27-00 + Phase 24 library instances per D-97 + ROADMAP success criterion 2; sidebar Compound/Card per D-96 (NOT Section/CTA); Calendly wiring sibling note cross-references § Calendly Wiring Map (Phase 27) row 2 (calendly.com/me--juoi/discovery-call); v1.3 Message field validation/label inconsistency raised as OPEN-27-NN per D-97 footnote."

    B. EXISTING cl8tt row update in Frames Inventory:
    - status_counts: `flat:1` → `hidden:1` (or per D-104 chosen label).
    - status: `IN-SCOPE` → `reconstructed-PHASE-27`.
    - open_flag_ids: append "OPEN-23-05 RESOLVED Plan 27-02 (PAGE-11 ACTIVE — raster hidden via enabled:false post-APPROVE; structural archive persists at canvas position per § 3.3)".

    C. Extend `### Variant Evidence (Phase 27)` sub-section (created in Plan 27-00) with Contact rows:
    - Contact page frame: fill #ffffffff ↔ color-semantic-bg-page; width 1440; vertical auto-layout + alignItems center + gap 0 + padding 0.
    - page-intro frame: width 1200; padding [64, 0] ↔ space-semantic-section-y; gap 12 ↔ space-semantic-stack-xs; alignItems flex_start (or center per Task 2 outcome).
    - Page-intro heading: Plus Jakarta Sans 48/700/lh 1.4 ↔ type-semantic-heading-1-* tokens; fill #141f39ff ↔ color-semantic-text-primary; content "Let's Talk" v1.3 verbatim.
    - Page-intro body: Inter 16/400/lh 1.625 ↔ type-semantic-prose-paragraph-* tokens; fill #52525bff ↔ color-semantic-text-secondary; content v1.3 verbatim.
    - form-and-sidebar-grid: width 1200; horizontal layout; gap 32 ↔ space-semantic-stack-lg; alignItems flex_start.
    - form-column: width 760; vertical layout; gap 24 ↔ space-semantic-stack-md.
    - sidebar-column: width 376; vertical layout; gap 16 ↔ space-semantic-stack-sm.
    - 8 field instance rows (Field 1-8) with descendants override map summary per field per D-97 + D-98 verbatim.
    - Submit Button instance (M7eUr ref with ATJK9 "Send Message" v1.3 verbatim).
    - Privacy line text: Inter 14/400 #52525bff ↔ type-semantic-body-sm-* + color-semantic-text-secondary; content v1.3 verbatim.
    - Sidebar Compound/Card instance (t40xct ref) with descendants override map summary per D-96 + D-98 verbatim.
    - Sidebar Calendly wiring sibling note: content + cross-reference to § Calendly Wiring Map row 2.

    D. § Calendly Wiring Map (Phase 27) — already seeded in Plan 27-00 with row 2 (Contact sidebar). Verify row 2 references the correct sibling-note ID created in Task 5. If row 2 was a placeholder ID, update with `<sidebarCalendlyNoteId>`.

    E. Open Flags — Phase 27 (OPEN-27-NN) — add rows for ALL gaps raised at Task 9:
    - **OPEN-27-NN (likely first row): v1.3 Message field validation inconsistency** — D-97 footnote: label reads "(optional)" but `validationConfig.valueMissing` treats as required. Phase 27 ships Field 8 with BOTH required-mark AND optional-text enabled to document for code-milestone resolution. Category: source-attribution. Severity: notable. Consumer phase: code milestone (next milestone after v2.0).
    - Any additional OPEN-27-NN raised at Task 9 calibration gate.

    F. OPEN-23-05 RESOLVED annotation: append to OPEN-23-05 row "Phase 27 Plan 27-02 RESOLVED — cl8tt reconstructed as editable Contact frame; flat raster hidden via PAGE-11 ACTIVE (CALIBRATION-PROTOCOL § 3.3) post-APPROVE at calibration gate."

    G. Update OPEN-24-04 / -07 / -08 / -09 rows (Plan 27-00 already appended Phase 27 update notes; Plan 27-02 may add additional confirmation per real consumer outcomes):
    - OPEN-24-04 (Button focus ring): if calibration confirmed no contrast issue on Send Message + Book a Call Buttons, append "Confirmed at Plan 27-02 calibration gate — focus ring meets visual contrast on green CTA in form context."
    - OPEN-24-08 (Input/Focus real consumer): append "Plan 27-02 8 form fields × tab focus — Input/Focus variant visually verified at calibration."
    - OPEN-24-09 (Input/Error real consumer): if Task 9 didn't exercise error state (likely — error state shows only on validation fail), append "Plan 27-02 ships Error variant primitive in library; real-error-state rendering deferred to code-milestone runtime validation."

    H. Provisional type-semantic-prose-paragraph-* flag: per Open Question 7, if Task 9 calibration confirmed prose-paragraph reads correctly on Contact page-intro body + sidebar Card body, consider dropping provisional flag (or leave pending Phase 28 Blog re-verification per Phase 26 D-71 pattern).

    Step 4 — Write `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` containing:
    - Pre-flight outcomes (Task 0): token 100 + Plan 27-00/01 deliverables intact + Phase 24/25 baselines intact + **cl8tt PROBED image-import-NN.jpg index recorded** + v1.3 verbatim strings + Card descendant IDs
    - Task 1: `<contactPageId>` + FindEmptySpace coords (nodeId:<thankYouPageId> anchor outcome)
    - Task 2 IDs: `<pageIntroId>`, `<pageIntroHeadingTextId>`, `<pageIntroBodyTextId>`
    - Task 3 IDs: `<gridFrameId>`, `<formColumnId>`, `<sidebarColumnId>`
    - Task 4 IDs: `<field1Id>` … `<field8Id>` + per-field descendants override summary + `<submitButtonRefId>` + `<privacyLineTextId>`
    - Task 5 IDs: `<sidebarCardRefId>` + descendants override map, `<sidebarCalendlyNoteId>` + content (Calendly URL)
    - Task 6 IDs: `<contactFooterRefId>`
    - Task 7 snapshot_layout outcomes (document + per-frame, text-clipping false-positives documented per Pitfall 5 — anticipate more than Phase 26 due to textareas + CheckboxGroup); fit_content before/after; per-section + full-page screenshots OK / Tier-1 / Tier-2 outcome
    - Task 8: description identifiers compiled (image-import-NN.jpg pairing) + fidelity proposals + candidate OPEN-27-NN
    - Task 9 user response: APPROVE/REVISE/GAP + per-section fidelity labels per D-83 + all OPEN-27-NN raised
    - Task 10 cl8tt mutation: pre-mutation position recorded; post-mutation enabled:false confirmed; canvas position unchanged; FIRST production use of PAGE-11 ACTIVE in v2.0
    - Task 11 PEN-INVENTORY diff: Frames Inventory Contact row + cl8tt row status update + Variant Evidence (Phase 27) Contact rows + § Calendly Wiring Map row 2 ID confirmation + OPEN-27-NN rows + OPEN-23-05 RESOLVED + OPEN-24-04/-08/-09 update notes
    - Token surface unchanged: 100
    - Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
    - VAL outcomes: PAGE-05 satisfied (Contact frame reconstructed with 8-field form composed entirely from library instances per ROADMAP success criterion 2) + PAGE-09 satisfied (desktop-only 1440 width) + PAGE-11 ACTIVE satisfied (cl8tt hidden post-APPROVE — first production use in v2.0) + VALID-01 satisfied (per-section labels) + VALID-02 satisfied (crito-source-flat-raster calibration via § 3.1 side-by-side) + VALID-03 satisfied (gaps as OPEN-27-NN)
    - Phase 27 close note: Plan 27-02 is final plan; if `commit_docs: true` and `parallelization: false` per .planning/config.json, expect commit by orchestrator post-plan-close.
  </action>
  <verify>
    `grep "Contact" .planning/research/PEN-INVENTORY.md` returns Frames Inventory row. `grep -A 3 "cl8tt" .planning/research/PEN-INVENTORY.md` shows status_counts updated to hidden:1 + status reconstructed-PHASE-27 + OPEN-23-05 RESOLVED. `grep -E "OPEN-27" .planning/research/PEN-INVENTORY.md` shows Message validation inconsistency row. 27-02-SUMMARY.md exists.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `get_variables({})` returns 100 (zero drift through Plan 27-02).
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns "No layout problems." at document level.
    - `batch_get(['cl8tt'])` confirms `enabled: false` (Task 10 mutation persisted).
    - PEN-INVENTORY Frames Inventory contains NEW row for `Contact` with scope IN-SCOPE, status reconstructed-PHASE-27, frame_id `<contactPageId>`.
    - PEN-INVENTORY EXISTING cl8tt row updated: status_counts flat:1 → hidden:1, status reconstructed-PHASE-27, OPEN-23-05 marked RESOLVED.
    - PEN-INVENTORY Variant Evidence (Phase 27) sub-section has Contact rows (~25 rows: page frame + page-intro + grid + form column + 8 field rows + Submit + privacy + sidebar Card + Calendly note + Footer).
    - PEN-INVENTORY § Calendly Wiring Map (Phase 27) row 2 has the correct sibling-note ID.
    - PEN-INVENTORY § Open Flags — Phase 27 (OPEN-27-NN) has at least 1 row for v1.3 Message field validation inconsistency (anticipated per D-97 footnote) + any additional OPEN-27-NN raised at Task 9.
    - OPEN-23-05 row has RESOLVED annotation pointing to Plan 27-02 PAGE-11 ACTIVE.
    - 27-02-SUMMARY.md exists with all listed sections.
    - VAL outcomes documented for VALID-01, VALID-02, VALID-03, PAGE-05, PAGE-09, PAGE-11 (ACTIVE applied).
  </acceptance_criteria>
  <done>
    Plan 27-02 complete. Contact page frame shipped (4 content blocks; 8-field form composed entirely from library instances; sidebar Card with Calendly note). cl8tt raster hidden post-APPROVE — first PAGE-11 ACTIVE in v2.0. OPEN-23-05 resolved. Phase 27 complete.
  </done>
</task>

</tasks>

<verification>
- Active editor remained `design/Crito.pen` throughout (D-103 pre-flight on every mutating task).
- Token surface UNCHANGED at 100 (Phase 27 ships zero new tokens per D-91 reuse).
- cl8tt raster image-import-NN.jpg index probed at Task 0 (HIGHEST-PRIORITY deliverable) and recorded in 27-02-SUMMARY.
- Contact frame placed at page-frame row (y ≈ -4111) via FindEmptySpace anchored on `<thankYouPageId>` per CALIBRATION-PROTOCOL § 10.4.
- 4 content blocks present: Section/Header ref + page-intro + 2-col asymmetric grid (form-column 760 + sidebar-column 376) + Section/Footer ref.
- 8-field form composed entirely of library instances per ROADMAP success criterion 2 — 8 refs (Input/Default × 3 + Textarea × 2 + Select × 2 + CheckboxGroup × 1) + Submit Button + privacy text.
- All 8 field labels + placeholders + 5 Solutions Checkbox labels + Submit Button label + privacy line + page-intro heading/body + sidebar Card title/body/Button label are v1.3 verbatim per D-98 (apostrophes preserved, character-perfect).
- required-mark `enabled:true` on Name + Email + Message label slots per D-97; optional-text `enabled:true` on Company + Challenges + Message per D-97; v1.3 Message field inconsistency (both enabled) raises OPEN-27-NN.
- Sidebar = Compound/Card (t40xct) ref per D-96 with descendants override (image disabled, title/body/Button content v1.3 verbatim); sibling Calendly note documents calendly.com/me--juoi/discovery-call URL.
- Plan-close calibration AskUserQuestion presented per CALIBRATION-PROTOCOL § 3.4 step 5 crito-source-flat-raster format (inline screenshots + side-by-side cl8tt raster pairing + per-section fidelity proposals).
- **PAGE-11 ACTIVE applied ONLY POST-APPROVE per § 3.4 step 7** — Task 10 hides cl8tt via enabled:false; canvas position unchanged. FIRST production use of PAGE-11 ACTIVE in v2.0.
- PEN-INVENTORY extensions per D-104: Frames Inventory Contact row + cl8tt status update + Variant Evidence (Phase 27) Contact rows + § Calendly Wiring Map row 2 + OPEN-27-NN (Message validation inconsistency anticipated + any additional) + OPEN-23-05 RESOLVED annotation + OPEN-24-04/-08/-09 update notes.
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level; per-frame text-clipping false-positives documented per Pitfall 5 (more numerous than Phase 26 due to textareas + CheckboxGroup multi-line content; NOT mitigated).
- 27-02-SUMMARY.md committed to git.
</verification>

<success_criteria>
- A top-level frame named `Contact` exists at the page-frame row (y ≈ -4111) with 4 vertical content blocks: Section/Header ref, page-intro frame, form-and-sidebar-grid frame (containing form-column with 8 fields + Submit + privacy + sidebar-column with Compound/Card instance + Calendly sibling note), Section/Footer ref.
- `batch_get(['<formColumnId>'])` shows 10 children: 8 field instance refs (Primitive/Input/Default × 3 + Input/Textarea × 2 + Select × 2 + CheckboxGroup × 1) + Submit Button (M7eUr) ref + privacy text node. NO inlined input markup anywhere.
- ROADMAP success criterion 2 satisfied: "8-field lead-qualification form is composed entirely of `Primitive / Input` instances (no inlined input markup)" — confirmed.
- All 8 fields show v1.3 verbatim labels + placeholders per D-97 + D-98; required-mark + optional-text enabled per field per D-97.
- Sidebar Compound/Card instance has v1.3 verbatim title "Ready to chat?" + body "Schedule a discovery call with me directly!" + Button label "Book a Call".
- Calendly URL `https://calendly.com/me--juoi/discovery-call` documented in sidebar Calendly Pencil note + cross-referenced via PEN-INVENTORY § Calendly Wiring Map (Phase 27) row 2.
- Submit Button label = "Send Message" v1.3 verbatim; privacy line = "Your info stays between us. No spam, ever." v1.3 verbatim.
- cl8tt raster `enabled: false` after Task 10 (post-APPROVE); canvas position unchanged. PEN-INVENTORY cl8tt status_counts updated flat:1 → hidden:1; OPEN-23-05 marked RESOLVED.
- Plan-close calibration AskUserQuestion presented per CALIBRATION-PROTOCOL § 3.4 step 5 crito-source-flat-raster format; user APPROVED (possibly REVISE iteration; OPEN-27-NN raised for Message validation inconsistency at minimum).
- PEN-INVENTORY.md Frames Inventory row for Contact exists; cl8tt row updated; Variant Evidence (Phase 27) Contact rows exist; § Calendly Wiring Map row 2 referenced; OPEN-27-NN rows exist; OPEN-23-05 RESOLVED annotated.
- `mcp__pencil__get_variables({})` returns 100 (zero token drift through Plan 27-02).
- `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
- VALID-01 satisfied (per-section labels per D-83); VALID-02 satisfied (crito-source-flat-raster calibration via § 3.1 side-by-side with cl8tt raster image-import-NN.jpg); VALID-03 satisfied (any gaps as OPEN-27-NN — Message validation inconsistency anticipated); PAGE-05 satisfied (Contact frame reconstructed with 8-field form per ROADMAP success criterion 2); PAGE-09 satisfied (desktop-only 1440 width); PAGE-11 ACTIVE satisfied (cl8tt hidden post-APPROVE — first production use in v2.0).
- Phase 27 complete (3/3 plans shipped).
</success_criteria>

<output>
After completion, create `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` containing:
- Pre-flight outcomes (Task 0): editor + token count 100 + Plan 27-00/01 deliverables intactness + Phase 24/25 baseline intactness + **cl8tt raster image-import-NN.jpg index PROBED + recorded** + v1.3 ContactSection verbatim strings extracted + Compound/Card descendant IDs looked up
- Task 1: `<contactPageId>` + FindEmptySpace coords (nodeId:<thankYouPageId> anchor outcome)
- Task 2 IDs: `<pageIntroId>`, `<pageIntroHeadingTextId>`, `<pageIntroBodyTextId>`
- Task 3 IDs: `<gridFrameId>`, `<formColumnId>`, `<sidebarColumnId>`
- Task 4 IDs: 8 field instance IDs (`<field1Id>`...`<field8Id>`) + per-field descendants override summary + `<submitButtonRefId>` + `<privacyLineTextId>`; Field 8 Message double-enabled (required-mark + optional-text) per D-97 inconsistency
- Task 5 IDs: `<sidebarCardRefId>` + descendants override map (image disabled + 3 content strings + Button label), `<sidebarCalendlyNoteId>` + content (Calendly URL me--juoi/discovery-call)
- Task 6: `<contactFooterRefId>`
- Task 7 snapshot_layout outcomes (document + per-frame, text-clipping false-positives documented per Pitfall 5 — anticipate more than Phase 26 due to textareas + CheckboxGroup); fit_content adjustment before/after; per-section + full-page screenshots OK / Tier-1 / Tier-2 fallback outcome
- Task 8: description identifiers + fidelity proposals + candidate OPEN-27-NN list
- Task 9 user response: APPROVE/REVISE/GAP + per-section fidelity labels per D-83 (page-intro=EXACT / form-section=EXACT / sidebar=EXACT / Header=EXACT / Footer=EXACT) + ALL OPEN-27-NN raised (at minimum: Message validation inconsistency)
- Task 10 cl8tt mutation: pre-mutation position recorded + post-mutation enabled:false confirmed + canvas position unchanged + first production use of PAGE-11 ACTIVE in v2.0 noted
- Task 11 PEN-INVENTORY diff: Frames Inventory Contact row + cl8tt row status update (flat:1 → hidden:1 + reconstructed-PHASE-27) + Variant Evidence (Phase 27) Contact rows (~25 rows) + § Calendly Wiring Map row 2 ID confirmation + OPEN-27-NN rows + OPEN-23-05 RESOLVED annotation + OPEN-24-04/-08/-09 update notes
- Token surface unchanged: 100
- Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
- VAL outcomes for VALID-01, VALID-02, VALID-03, PAGE-05, PAGE-09, PAGE-11 (ACTIVE — first production use in v2.0)
- Phase 27 close: 3/3 plans shipped (27-00 foundation + 27-01 Thank-you joel-only + 27-02 Contact crito-source-flat-raster)
</output>
