---
phase: 06-performance-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SEO.astro
  - src/layouts/BaseLayout.astro
  - public/og-image.jpg
autonomous: true

must_haves:
  truths:
    - "All pages have title in format 'Page Title | Joel Shinness'"
    - "All pages have description meta tag"
    - "All pages have OpenGraph meta tags (og:title, og:description, og:image)"
    - "All pages have Twitter Card meta tags"
    - "Homepage has JSON-LD Person structured data"
  artifacts:
    - path: "src/components/SEO.astro"
      provides: "Reusable SEO component with all meta tags"
      contains: "og:image"
    - path: "public/og-image.jpg"
      provides: "Default OpenGraph image (1200x630)"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/SEO.astro"
      via: "import and usage in head"
      pattern: "import SEO from"
---

<objective>
Create SEO component with meta tags, OpenGraph, Twitter Cards, and JSON-LD structured data

Purpose: Ensure all pages are properly indexed by search engines and display correctly when shared on social media
Output: SEO.astro component, updated BaseLayout, placeholder OpenGraph image
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-performance-seo/06-CONTEXT.md
@.planning/phases/06-performance-seo/06-RESEARCH.md

@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SEO component with meta tags and JSON-LD</name>
  <files>src/components/SEO.astro, public/og-image.jpg</files>
  <action>
Create src/components/SEO.astro with these features:

Props interface:
- title: string (required)
- description: string (required)
- canonical?: string (optional, defaults to current page URL)
- type?: string (optional, defaults to "website")

The component should output:
1. Title tag in format: `${title} | Joel Shinness`
2. Meta description
3. Canonical URL link
4. OpenGraph tags: og:title, og:description, og:type, og:url, og:image (pointing to /og-image.jpg)
5. og:site_name: "Joel Shinness"
6. Twitter Card tags: twitter:card (summary_large_image), twitter:title, twitter:description, twitter:image
7. JSON-LD script for Person schema with:
   - @type: Person
   - name: Joel Shinness
   - url: https://joelshinness.com
   - jobTitle: Software Developer
   - sameAs array (empty for now, user can fill in social links)

Use Astro.url for canonical URL if not provided.
Use site URL from astro.config.mjs (define as const at top: https://joelshinness.com).

For og-image.jpg: Create a simple 1200x630 placeholder image in public/.
Since we can't create actual images, create a placeholder SVG that Astro will serve, OR document that user needs to add their own og-image.jpg.

IMPORTANT: Do NOT install astro-seo package. Build this component manually using native Astro - it's simple enough and avoids dependency.
  </action>
  <verify>
File exists: src/components/SEO.astro
File contains: og:image, twitter:card, application/ld+json
  </verify>
  <done>SEO component exists with all required meta tags and JSON-LD structured data</done>
</task>

<task type="auto">
  <name>Task 2: Integrate SEO component into BaseLayout</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
Update src/layouts/BaseLayout.astro:

1. Import SEO component at top
2. Update Props interface to include description (already has it)
3. Replace existing meta tags in head with SEO component:
   - Remove: existing title tag
   - Remove: existing meta description
   - Add: SEO component with title and description props

Keep these existing elements:
- charset meta
- viewport meta
- favicon links
- Google Fonts preconnect and link
- Dark mode script
- generator meta

The head section should have SEO component early (for meta tags) but after charset and viewport.

Pass title and description from page props to SEO component.
  </action>
  <verify>
Run: npm run build
Build succeeds without errors
Inspect dist/index.html for og:image and JSON-LD
  </verify>
  <done>BaseLayout uses SEO component, all pages get proper meta tags</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder OpenGraph image instructions</name>
  <files>public/og-image.jpg</files>
  <action>
Since we cannot generate actual images, create a placeholder approach:

Option A (preferred): Create public/og-image.svg as a simple branded placeholder:
- 1200x630 dimensions in viewBox
- Dark background (#1a1a1a)
- "Joel Shinness" text centered
- "Custom Software for Small Business" subtext
- Yellow accent color (#facc15) for visual interest

SVG will work for og:image in many contexts. Rename reference in SEO.astro to /og-image.svg.

Document in a code comment that user should replace with actual JPG for best compatibility.

Alternative: If SVG won't work well, leave a TODO comment in SEO.astro noting that user needs to add public/og-image.jpg (1200x630).
  </action>
  <verify>
File exists: public/og-image.svg (or documented TODO)
SEO.astro references correct image path
  </verify>
  <done>OpenGraph image placeholder exists or is clearly documented for user to add</done>
</task>

</tasks>

<verification>
After all tasks:
1. npm run build succeeds
2. Check dist/index.html contains:
   - og:title, og:description, og:image meta tags
   - twitter:card meta tag
   - JSON-LD script with @type: Person
3. Check multiple page builds (portfolio, contact, blog) have meta tags
</verification>

<success_criteria>
- SEO.astro component exists with full meta tag support
- BaseLayout imports and uses SEO component
- All pages inherit SEO meta tags
- JSON-LD Person schema present on pages
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-seo/06-01-SUMMARY.md`
</output>
