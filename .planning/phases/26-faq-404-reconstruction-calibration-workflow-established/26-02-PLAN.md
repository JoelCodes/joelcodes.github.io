---
phase: 26-faq-404-reconstruction-calibration-workflow-established
plan: 02
type: execute
wave: 1
depends_on:
  - 26-00
  - 26-01
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-08
  - PAGE-09
  - PAGE-11
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "A new top-level page frame named `404` exists in `design/Crito.pen` as a direct child of the document root."
    - "The `404` frame is positioned via `mcp__pencil__find_empty_space_on_canvas` (D-74), has width 1440 (D-75), and an estimated height ~900-1100px (smaller than FAQ — Header + message + nav-back + Footer)."
    - "The `404` frame contains 4 stacked content blocks in vertical auto-layout: (1) Section/Header ref (G0wNOc), (2) message section frame with STUB headline + STUB body text per D-81, (3) Section/NavBack ref (from Plan 26-00) with EXACT link labels Home/Blog/Projects/Contact per Claude's Discretion, (4) Section/Footer ref (Xs0Hs)."
    - "Message section text nodes (headline + body) ship with STUB placeholder strings per D-81 ('Headline placeholder' + 'Body placeholder' OR brand-neutral STUB equivalent); fidelity label STUB for text."
    - "Section/NavBack instance ships with the 4 link labels Home/Blog/Projects/Contact (EXACT structural labels per CONTEXT.md Claude's Discretion — these are STRUCTURAL, not microcopy)."
    - "Per-section fidelity labels recorded in 26-02-SUMMARY.md per D-83: message-section text = STUB; message-section layout = EXACT; NavBack instance = EXACT; Header instance = EXACT; Footer instance = EXACT."
    - "User has spot-checked the 404 page frame via plan-close calibration AskUserQuestion gate (D-65 inline rendering)."
    - "PAGE-11 documented as INERT for Phase 26 (joel-only-no-crito-ref branch; no raster exists to remove per RESEARCH § Focus 5) in 26-02-SUMMARY.md — fed forward to Plan 26-03 CALIBRATION-PROTOCOL.md branch matrix."
    - "PEN-INVENTORY.md Frames Inventory has a new row for `404` (frame_id assigned, scope: `joel-only-no-crito-ref`, joel_page_map: `/404`, status: `reconstructed-PHASE-26`); any plan-surfaced flags added under Open Flags — Phase 26."
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `404` with 4 vertical content blocks; message-section text nodes + Section/NavBack instance + Section/Header + Section/Footer refs."
      contains: "404 page frame, message section, Section/NavBack instance, Section/Header ref G0wNOc, Section/Footer ref Xs0Hs"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for 404; Variant Evidence (Phase 26) rows for page frame + message section + NavBack instance structural literal-to-semantic bindings; any new OPEN-26-NN flags raised at calibration; explicit PAGE-11 INERT note for joel-only branch."
  key_links:
    - from: "404 page frame"
      to: "Section / Header (G0wNOc)"
      via: "ref child at top of auto-layout vertical stack"
      pattern: "404.*ref.*G0wNOc"
    - from: "404 page frame"
      to: "Section / NavBack (from Plan 26-00)"
      via: "ref child with EXACT structural label overrides Home/Blog/Projects/Contact via descendants"
      pattern: "404.*ref.*<sectionNavBackId>"
    - from: "404 page frame"
      to: "Section / Footer (Xs0Hs)"
      via: "ref child at bottom"
      pattern: "404.*ref.*Xs0Hs"
    - from: "Message section"
      to: "STUB placeholder text nodes (headline + body)"
      via: "2 text children using heading-2 (headline) + prose-paragraph (body) typography; content STUB per D-81"
      pattern: "Headline placeholder"
---

<objective>
Create the 404 page frame in `design/Crito.pen` as a fresh-design composition (D-58 — joel-only-no-crito-ref). Layout is EXACT (real Section/Header + message-section frame + Section/NavBack instance + Section/Footer); microcopy is STUB per D-81 (no brand-voice 404 content exists in v1.3; deferred to a future content phase per CONTEXT.md Deferred Ideas).

**Purpose:** Second per-page reconstruction in v2.0; simpler than FAQ (no Q+A list, no new Section/CTA exercise). Applies what FAQ taught (D-85). Tests Section/NavBack's narrow-purpose design (D-79). Validates the joel-only-branch calibration workflow on a simpler page. PAGE-11 INERT carve-out documented for Plan 26-03 CALIBRATION-PROTOCOL.md.

**Output:** A working `404` top-level page frame; an extended PEN-INVENTORY Frames Inventory row + Variant Evidence rows; per-section fidelity labels recorded; a plan-close user-approved state. ONE user gate at plan close (calibration spot-check per D-86). PAGE-11 noted as INERT (no Crito 404 raster exists to remove) — fed forward to Plan 26-03.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plan 26-00 and 26-01 outputs intact</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    - 26-00-SUMMARY.md — Section/NavBack id + heading-slot id + links-slot id + 4 link text-node ids
    - 26-01-SUMMARY.md — `<faqPageId>` and FAQ structural state (drift check)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-87 (pre-flight)
  </read_first>
  <action>
    Call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert active editor ends with `design/Crito.pen`. Halt on mismatch per D-87.

    Call `mcp__pencil__get_variables({})` and assert exact count from 26-00-SUMMARY.md (100 Option A; 99 Option B). Halt if drift.

    Call `mcp__pencil__batch_get` on these IDs from prior plans:
    - `<sectionNavBackId>`, `<sectionNavBackHeadingSlotId>`, `<sectionNavBackLinksSlotId>`, the 4 `<navBackLink*Id>` text node ids (from 26-00-SUMMARY)
    - `<faqPageId>` (from 26-01-SUMMARY)
    - `G0wNOc` (Section/Header), `Xs0Hs` (Section/Footer), `g9oRa5` (Sections parent)

    Confirm Section/NavBack has 2 slots, links-slot has 4 text children with contents "Home", "Blog", "Projects", "Contact". Confirm FAQ page frame still has its 5 children (no regression from Plan 26-01). Confirm g9oRa5 still has 4 reusable section children.

    Record in 26-02-SUMMARY.md scratch.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token count {N} matches. Section/NavBack 2 slots + 4 links intact. FAQ page intact. g9oRa5 4 children intact."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `get_variables({})` count matches Plan 26-00 close state.
    - `batch_get` on Section/NavBack + slots + 4 link text nodes confirms structural intactness with expected contents.
    - `batch_get` on `<faqPageId>` confirms 5 FAQ children unchanged from Plan 26-01 close.
    - `batch_get` on `g9oRa5` confirms 4 reusable section children (Header + Footer + CTA + NavBack).
    - No mutations issued in Task 0.
  </acceptance_criteria>
  <done>
    Prior plan outputs verified intact; Plan 26-02 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `404` top-level page frame via find_empty_space_on_canvas + batch_design</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-74 (placement), D-75 (1440 width + Crito-rhythm height), D-76 (plain name "404")
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Pattern 1 + § Open Questions #3 (fit_content)
    - 26-01-SUMMARY.md — placement outcomes from FAQ creation (directional anomaly notes, if any)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 1000 })` to find non-overlapping coords. (Height 1000 estimate: Header ~80 + message ~250 + NavBack ~150 + Footer ~500 = ~980.)

    Then `mcp__pencil__batch_design` Insert a top-level frame:
    - Parent: document root (NOT inside library parents — Pitfall 3).
    - `type: "frame"`, `name: "404"` (plain name per D-76).
    - Position: `{x, y}` from `find_empty_space_on_canvas`.
    - Dimensions: `width: 1440`, `height: 1000`.
    - Layout: `layout: "vertical"`, `gap: 0`, `padding: 0`, `alignItems: "center"` (Pitfall 5 — Sections width 1200 centered within 1440 outer).
    - Fill: `"#ffffffff"` (color-semantic-bg-page literal per OPEN-23-13).

    Record `<page404Id>`, returned x/y, and height estimate in 26-02-SUMMARY.md scratch.
  </action>
  <verify>
    `batch_get(["<page404Id>"])` returns a frame named "404" at document root, width 1440, vertical auto-layout, alignItems center, fill #ffffffff.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `find_empty_space_on_canvas` returned `{x, y}`.
    - `batch_design` Insert completed without error.
    - `batch_get(["<page404Id>"])` returns a frame at document root level (NOT inside g9oRa5/avgor/t67DU6/RpGbe/`<faqPageId>`) named "404" with width 1440 + vertical auto-layout + alignItems center.
    - No mutations to baseline IDs.
    - `<page404Id>` recorded in 26-02-SUMMARY.md scratch.
  </acceptance_criteria>
  <done>
    Empty 404 page frame in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header ref + message section with STUB headline + body</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-60 (404 visual shape — BIG headline + body + nav-back) + D-77 (Header no override) + D-81 (microcopy STUB) + D-83 (per-section labels)
    - 26-00-SUMMARY.md — heading-2 size value chosen at Plan 26-00 Task 1
    - `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` Section/Header reference
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__batch_design` with 2 Insert operations as children of `<page404Id>`:

    Insert 1 — Section/Header ref:
    - `type: "ref"`, `ref: "G0wNOc"`.
    - NO descendants override per D-77.
    - First child of `<page404Id>`.

    Insert 2 — Message section frame:
    - `type: "frame"`, `name: "message-section"`, `width: 1200`, `layout: "vertical"`, `gap: 24`, `padding: [96, 0]` (extra section-y rhythm to make 404 visually breathe), `alignItems: "center"`.
    - Child A — Headline text node: `{ type: "text", content: "Headline placeholder", fontFamily: "Plus Jakarta Sans", fontSize: 48, fontWeight: "700", fill: "#141f39ff", lineHeight: 1.4 }` — heading-1 typography (BIG headline per D-60). STUB content per D-81.
    - Child B — Body text node: `{ type: "text", content: "Body placeholder — this page does not exist. Try the navigation below.", fontFamily: "Inter", fontSize: 18, fontWeight: "400", fill: "#52525bff", lineHeight: 1.625, textAlign: "center" }` — body-large typography (size 18 from primitive surface; if 18 not in semantic prose-paragraph, fall back to size 16). STUB content per D-81. The "this page does not exist" string is STILL labeled STUB — agent should not pre-empt brand voice; user may revise at calibration gate.

    Record `<messageSectionId>`, `<headlineTextId>`, `<bodyTextId>` in 26-02-SUMMARY.md scratch.

    Note: per D-83, the message section's LAYOUT is EXACT (real frame, real typography token bindings, real auto-layout); the TEXT CONTENT is STUB. The fidelity label is granular per text-vs-layout.
  </action>
  <verify>
    `batch_get(["<page404Id>"])` shows 2 children: G0wNOc ref + message-section frame. message-section contains headline text (Plus Jakarta Sans 48/700) + body text (Inter 18/400 or 16/400) with STUB strings.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(["<page404Id>"])` returns 2 children in order: Section/Header ref + message-section frame.
    - message-section has width 1200, vertical auto-layout, alignItems center, padding [96, 0] (or similar generous y-rhythm).
    - Headline text uses heading-1 typography; body text uses body or prose-paragraph typography; both STUB per D-81.
    - No descendants override on Section/Header ref (D-77 compliance).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Top of 404: Header + message section with STUB text.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert Section/NavBack ref + Section/Footer ref</name>
  <files>design/Crito.pen</files>
  <read_first>
    - 26-00-SUMMARY.md — `<sectionNavBackId>` + heading-slot + links-slot + 4 link text-node ids
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-79 (NavBack narrow) + Claude's Discretion (link labels Home/Blog/Projects/Contact match Joel's v1.3 + Home as structural label, NOT microcopy)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Example 3 (descendants override pattern)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__batch_design` with 2 Insert operations as children of `<page404Id>` (3rd and 4th of vertical stack):

    Insert 1 — Section/NavBack ref:
    - `type: "ref"`, `ref: "<sectionNavBackId>"` (from 26-00-SUMMARY.md).
    - `descendants` override map (optional — the Plan 26-00 build may have already placed "Home", "Blog", "Projects", "Contact" as the placeholder content; if so, NO override is needed and that is the EXACT label policy). If the Plan 26-00 placeholder used different text (e.g., "Find what you need" for heading-slot, generic link names), override with:
      - `<sectionNavBackHeadingSlotTextId>` (if user wants visible heading on 404 — STUB content; OR set `enabled: false` to suppress the heading on the 404 instance): per Claude's Discretion, leave heading-slot enabled with STUB content "Find what you need" (matches Plan 26-00 placeholder).
      - 4 link text-node ids from 26-00-SUMMARY (`<navBackHomeTextId>` ... `<navBackContactTextId>`): the structural labels are already "Home/Blog/Projects/Contact" from Plan 26-00; NO content override needed (EXACT structural labels per Claude's Discretion in CONTEXT).
    - If 26-00 placeholders are already the desired labels, this ref needs no descendants override at all. Document in 26-02-SUMMARY.md.

    Insert 2 — Section/Footer ref:
    - `type: "ref"`, `ref: "Xs0Hs"`.
    - NO descendants override per D-77.

    Record both ref ids in 26-02-SUMMARY.md scratch.
  </action>
  <verify>
    `batch_get(["<page404Id>"])` now shows 4 direct children in order: G0wNOc ref, message-section, sectionNavBackId ref, Xs0Hs ref. Section/NavBack instance renders with heading "Find what you need" (or override) + 4 links Home/Blog/Projects/Contact.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(["<page404Id>"])` returns 4 direct children in expected order.
    - Section/NavBack ref descendants override (or non-override, if Plan 26-00 placeholders sufficed) is documented in 26-02-SUMMARY.md.
    - Section/Footer ref has no descendants override (D-77 compliance).
    - All ref IDs recorded.
  </acceptance_criteria>
  <done>
    404 page frame is structurally complete with all 4 content blocks.
  </done>
</task>

<task type="auto">
  <name>Task 4: snapshot_layout sweep + page-frame fit_content adjustment if needed</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Open Questions #3 (fit_content) + § Pitfall 6 (text-clipping quirk)
    - `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` text-clipping quirk pattern
    - 26-01-SUMMARY.md snapshot_layout outcomes (pattern from Plan 26-01)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` — document level. Expected: `"No layout problems."`.

    `mcp__pencil__snapshot_layout({ rootId: "<page404Id>", problemsOnly: true })` — per-frame. Document text-clipping false-positives.

    `mcp__pencil__get_screenshot({ nodeId: "<page404Id>" })` — render full 404 page inline. Inspect for overflow.

    If overflow: `batch_design` Update `{ id: "<page404Id>", set: { height: "fit_content" } }`. Re-render.

    Record outcomes in 26-02-SUMMARY.md scratch.
  </action>
  <verify>
    Document-level snapshot returns "No layout problems." Inline render of 404 page is captured without overflow.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
    - Per-frame snapshot captured; text-clipping quirks documented (NOT failures).
    - `get_screenshot(<page404Id>)` produced an inline render.
    - Any `fit_content` Update logged with before/after dimensions.
  </acceptance_criteria>
  <done>
    Layout-clean; ready for user calibration gate.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Calibration spot-check user gate (joel-only branch — per-section token-usage)</name>
  <files>None (presentation-only — uses Task 4's inline screenshot)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-62 (joel-only token-usage check) + D-63 (joel-only fidelity semantics) + D-64 (description identifier) + D-65 (single AskUserQuestion at plan close) + D-83 (per-section labels)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Focus 5 (PAGE-11 INERT for joel-only) + § Focus 6 (AskUserQuestion format) + § Example 4 (inline pattern)
    - 26-01-SUMMARY.md Task 6 outcomes (FAQ calibration pattern — apply same shape)
  </read_first>
  <action>
    Render TWO inline screenshots for the calibration gate:
    - `mcp__pencil__get_screenshot({ nodeId: "<messageSectionId>" })` — message section (STUB text)
    - `mcp__pencil__get_screenshot({ nodeId: "<sectionNavBackRefId>" })` OR re-render `<page404Id>` cropped — NavBack instance

    Present AskUserQuestion per D-65 format:

    Question: "Calibration spot-check — 404 page (joel-only-no-crito-ref branch per D-62). Does this composition use the declared tokens correctly?"

    Description (markdown — both screenshots rendered above):

    ```
    ## Calibration Spot-Check — 404 Page (Joel-Only Branch)

    **Inline renders shown above:** message section + Section/NavBack instance

    ### Section 1: Message section (STUB text per D-81)
    Fidelity label proposal:
    - LAYOUT = **EXACT** (real frame + auto-layout + typography token bindings)
    - TEXT CONTENT = **STUB** (placeholder strings; brand-voice deferred per CONTEXT.md Deferred Ideas)

    **Semantic tokens consumed:**
    - Headline: `type-semantic-heading-1-family` (Plus Jakarta Sans) + `-size` (48px) + `-weight` (700) + `-lh` (1.4)
    - Body: `type-semantic-prose-paragraph-family` (Inter) + `-size` (16/18px) + `-weight` (400) + `-lh` (1.625)
    - Headline fill: `color-semantic-text-primary` #141f39ff
    - Body fill: `color-semantic-text-secondary` #52525bff
    - Section padding-y: [96, 0] bound to space-semantic-section-y x2 (generous 404 rhythm)
    - Stack gap: 24 bound to space-semantic-stack-md

    ### Section 2: Section/NavBack instance
    Fidelity label proposal: **EXACT** (Plan 26-00 build with placeholder labels Home/Blog/Projects/Contact — STRUCTURAL labels per CONTEXT.md Claude's Discretion, NOT microcopy)

    **Semantic tokens consumed (inherits Plan 26-00 NavBack build):**
    - Heading: heading-2 typography ({N}px Plus Jakarta Sans 700)
    - Links: body typography (Inter 16 400)
    - Padding-y: [32, 0]
    - Gap between links: 24

    ### Section 3 + 4: Section/Header + Section/Footer instances
    Fidelity label proposal: **EXACT** (Phase 25 shipped; D-77 Crito-source labels stay)

    ### PAGE-11 status (RESEARCH § Focus 5):
    INERT for this page — joel-only-no-crito-ref scope means no Crito 404 raster exists to remove (PEN-INVENTORY confirms; OPEN-23-06 confirms no Crito FAQ). PAGE-11 only applies to crito-source branch — Plan 26-03 CALIBRATION-PROTOCOL.md documents this carve-out.

    **Description identifiers (D-64 — inline only per OPEN-23-01, NOT disk-written):**
    - `26-404-message-section--token-usage.png`
    - `26-404-navback-section--token-usage.png`

    **Calibration target (D-62 joel-only branch):** `_Tokens & Foundations` reference frame `RpGbe`. Are the message and NavBack compositions using the declared tokens correctly?
    ```

    Options:
    - "APPROVE — both sections use tokens correctly; fidelity labels per D-83 are correct (message-LAYOUT=EXACT/TEXT=STUB; NavBack=EXACT)"
    - "REVISE — token mismatch or label mismatch (free-text describe)"
    - "GAP — composition reveals a missing token (raise OPEN-26-NN; specify)"

    Wait for response. Record in 26-02-SUMMARY.md.

    If REVISE: address via `batch_design` Update ops + re-gate.
    If GAP: log OPEN-26-NN and proceed.
    If APPROVE: proceed to Task 6.
  </action>
  <verify>
    User has responded; outcome recorded; any REVISE iterations resolved; any GAP logged.
  </verify>
  <acceptance_criteria>
    - Two `get_screenshot` calls produced inline renders.
    - AskUserQuestion presented with both screenshots + structured token-usage description + PAGE-11 INERT note + per-section fidelity proposal per D-83.
    - User chose APPROVE / APPROVE-after-revise / APPROVE-with-GAP-logged.
    - Per-section fidelity labels per D-83 recorded in 26-02-SUMMARY.md.
    - Any OPEN-26-NN raised has 1-line row ready for Task 6 PEN-INVENTORY.
  </acceptance_criteria>
  <done>
    User has spot-checked both sections — VALID-02 satisfied for joel-only branch on 404.
  </done>
</task>

<task type="auto">
  <name>Task 6: PEN-INVENTORY extensions + 26-02-SUMMARY.md close + PAGE-11 INERT note</name>
  <files>
    - .planning/research/PEN-INVENTORY.md
    - .planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-88 (PEN-INVENTORY extension pattern)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Focus 5 (PAGE-11 INERT for joel-only) + § PEN-INVENTORY Extensions Phase 26 Will Write table
    - 26-01-SUMMARY.md Task 7 PEN-INVENTORY edit pattern
    - 26-02-SUMMARY.md scratch — all IDs + fidelity labels from Task 5 + any OPEN-26-NN
  </read_first>
  <action>
    Edit `.planning/research/PEN-INVENTORY.md`:

    1. Frames Inventory table — add new row for `404`:
       - name: "404"
       - frame_id: `<page404Id>`
       - scope: `joel-only-no-crito-ref`
       - joel_page_map: `/404`
       - child_section_count: 4 (Header ref + message-section + NavBack ref + Footer ref)
       - status: `reconstructed-PHASE-26`
       - notes: "Fresh design in Crito vocab per D-58; STUB headline + body per D-81; Section/NavBack 4 EXACT structural labels Home/Blog/Projects/Contact per Claude's Discretion; PAGE-11 INERT for this row (joel-only-no-crito-ref — no raster exists to remove per RESEARCH § Focus 5)."

    2. Extend Variant Evidence (Phase 26) sub-section with rows for 404 page-frame structural literal-to-semantic bindings:
       - 404 page frame: fill #ffffffff ↔ color-semantic-bg-page; width 1440; vertical auto-layout + alignItems center + gap 0 + padding 0.
       - message-section: width 1200; padding [96, 0] ↔ space-semantic-section-y x2; gap 24 ↔ space-semantic-stack-md; alignItems center.
       - headline text: Plus Jakarta Sans 48/700/lh 1.4 ↔ heading-1 typography; STUB content "Headline placeholder"; fill #141f39ff ↔ color-semantic-text-primary.
       - body text: Inter 16-or-18/400/lh 1.625 ↔ body/prose-paragraph typography; STUB content; fill #52525bff ↔ color-semantic-text-secondary.
       - Section/NavBack ref: descendants override (or none if Plan 26-00 placeholders sufficed).
       - Section/Footer ref: no descendants override (D-77).

    3. If any OPEN-26-NN was raised in Task 5, add row to "## Open Flags — Phase 26 (OPEN-26-NN)" section.

    4. Add an explicit PAGE-11 INERT note under Frames Inventory or in a new "## PAGE-11 Status Notes" sub-section:
       - "Phase 26 ships joel-only-no-crito-ref pages (FAQ + 404). PAGE-11 (raster-removal-only-after-verify) is INERT for these rows — no Crito source raster exists per PEN-INVENTORY frame scope; no removal action needed. PAGE-11 applies only to crito-source branch per RESEARCH § Focus 5; Plan 26-03 CALIBRATION-PROTOCOL.md formally documents this carve-out in the branch matrix."

    Then write `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md` containing:
    - Pre-flight outcomes
    - All IDs created: `<page404Id>` + coords, `<messageSectionId>` + `<headlineTextId>` + `<bodyTextId>`, `<sectionNavBackRefId>`, `<sectionFooterRefId>`
    - Task 3 NavBack descendants override map (or note that Plan 26-00 placeholders sufficed)
    - snapshot_layout outcomes (document + 404 per-frame, with quirk notes)
    - Task 5 user response: APPROVE/REVISE/GAP + per-section fidelity labels per D-83 (message-LAYOUT=EXACT/TEXT=STUB; NavBack=EXACT; Header=EXACT; Footer=EXACT)
    - PEN-INVENTORY diff (Frames Inventory 404 row + Variant Evidence Phase 26 rows + PAGE-11 INERT note + any OPEN-26-NN)
    - Explicit PAGE-11 INERT status for Plan 26-03 input
    - Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/26-.../26-02-SUMMARY.md
    - VAL outcomes for VALID-01, VALID-02, VALID-03, PAGE-08, PAGE-09, PAGE-11 (INERT)
  </action>
  <verify>
    `cat .planning/research/PEN-INVENTORY.md | grep "404"` returns the new Frames Inventory row. `cat .planning/research/PEN-INVENTORY.md | grep "PAGE-11"` returns the INERT note. 26-02-SUMMARY.md exists.
  </verify>
  <acceptance_criteria>
    - PEN-INVENTORY Frames Inventory contains a new row for `404` with scope `joel-only-no-crito-ref`, joel_page_map `/404`, status `reconstructed-PHASE-26`, frame_id = `<page404Id>`.
    - PEN-INVENTORY Variant Evidence (Phase 26) sub-section contains rows for 404 page frame + message section + headline + body + NavBack ref + Footer ref bindings.
    - PAGE-11 INERT note is present (in Frames Inventory notes OR new section).
    - Any OPEN-26-NN raised in Task 5 has a row.
    - 26-02-SUMMARY.md exists with listed sections.
  </acceptance_criteria>
  <done>
    Plan 26-02 complete. 404 page frame shipped; calibration gated; PAGE-11 INERT carve-out documented. Plan 26-03 may proceed with the codify-what-worked step.
  </done>
</task>

</tasks>

<verification>
- Active editor remained `design/Crito.pen` throughout (D-87 pre-flight every mutating task).
- 404 frame is a direct child of the document root (Pitfall 3 compliance).
- Plan 26-00 outputs (Section/NavBack) and Plan 26-01 outputs (FAQ frame) are unmutated.
- Section/NavBack instance shows 4 structural labels Home/Blog/Projects/Contact per Claude's Discretion.
- Section/Header + Section/Footer instances render with Crito-source labels intact (D-77).
- Plan-close calibration spot-check was presented via AskUserQuestion with inline screenshots + structured token-usage description + PAGE-11 INERT note.
- PEN-INVENTORY Frames Inventory + Variant Evidence + PAGE-11 INERT note extended per D-88.
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
- 26-02-SUMMARY.md committed to git.
</verification>

<success_criteria>
- A top-level frame named `404` exists with 4 vertical children: Section/Header ref, message-section frame, Section/NavBack ref, Section/Footer ref.
- Message section contains headline + body text nodes with STUB content (per D-81) and EXACT typography bindings (heading-1 + prose-paragraph).
- Section/NavBack instance renders with 4 structural labels (Home / Blog / Projects / Contact) and a STUB heading.
- Plan-close calibration AskUserQuestion was presented with inline screenshots + token-usage description + PAGE-11 INERT note; user APPROVED.
- PEN-INVENTORY.md Frames Inventory row for 404 + Variant Evidence Phase 26 rows + PAGE-11 INERT note exist.
- VALID-01 satisfied (per-section labels per D-83); VALID-02 satisfied (joel-only-branch calibration via D-62); VALID-03 satisfied (gaps as OPEN flags).
- PAGE-08 satisfied (404 frame reconstructed); PAGE-09 satisfied (desktop-only, 1440 width); PAGE-11 documented as INERT (joel-only branch — no raster to remove).
</success_criteria>

<output>
After completion, create `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md` containing:
- Pre-flight outcomes (Task 0 — token count + Plan 26-00 NavBack intact + Plan 26-01 FAQ intact)
- Task 1: `<page404Id>` + returned `{x, y}` + directional anomaly notes (if any)
- Task 2 IDs: `<messageSectionId>`, `<headlineTextId>`, `<bodyTextId>` + STUB content strings used
- Task 3 IDs: `<sectionNavBackRefId>` + descendants override (if any), `<sectionFooterRefId>`
- Task 4 snapshot_layout outcomes (document + 404 per-frame, text-clipping quirks documented)
- Task 5 user response: APPROVE/REVISE/GAP + per-section fidelity labels per D-83 + any OPEN-26-NN
- Task 6 PEN-INVENTORY diff (Frames Inventory 404 row + Variant Evidence rows + PAGE-11 INERT note + OPEN-26-NN if any)
- Explicit PAGE-11 INERT note for Plan 26-03 CALIBRATION-PROTOCOL.md branch matrix
- VAL outcomes for VALID-01, VALID-02, VALID-03, PAGE-08, PAGE-09, PAGE-11
- Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/26-.../26-02-SUMMARY.md
</output>
