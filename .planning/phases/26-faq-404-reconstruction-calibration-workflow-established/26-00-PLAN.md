---
phase: 26-faq-404-reconstruction-calibration-workflow-established
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - PAGE-04
  - PAGE-08
  - VALID-01

must_haves:
  truths:
    - "After this plan, `mcp__pencil__get_variables({})` returns 100 tokens (was 95 at end of Phase 25)."
    - "A new primitive `type-primitive-size-32` exists and resolves to the literal value 32."
    - "Four new semantic aliases `type-semantic-heading-2-{family,size,weight,lh}` exist and resolve through primitives only (no literal values on semantic aliases, per Phase 23 D-14)."
    - "`_Components / Sections` parent frame `g9oRa5` contains two new reusable section components: `Section / CTA` and `Section / NavBack`, both alongside the existing `Section / Header` (G0wNOc) and `Section / Footer` (Xs0Hs)."
    - "`Section / CTA` exposes three named child slot frames (`headline-slot`, `body-slot`, `actions-slot`); each is `enabled: true` with placeholder content (D-53 carry-forward); `actions-slot` is typed with `slot: ['M7eUr','hIWuC']` per Plan 25-03 D-52 PREFERRED Pattern."
    - "`Section / NavBack` exposes two named child slot frames (`heading-slot`, `links-slot`); links-slot contains 4 text-node placeholders (Home / Blog / Projects / Contact) per Claude's Discretion default in CONTEXT.md."
    - "Both new sections have a sibling Pencil note inside `g9oRa5` documenting their slot signature (belt-and-suspenders per Phase 25 D-52)."
    - "ROADMAP.md Phase 26 entry lists 4 plans (26-00, 26-01, 26-02, 26-03), not the original 3."
    - "PEN-INVENTORY.md has a new Token Extensions (Phase 26) sub-section with rows for `type-primitive-size-32` + 4 heading-2 aliases; Variant Evidence (Phase 26) sub-section with rows for Section/CTA + Section/NavBack; OPEN-23-10 row updated with the heading-2-resolved-by-D-72 note; OPEN-23-11 row updated with re-pointing to Phase 28."
    - "Phase 25 baseline IDs (G0wNOc, Xs0Hs, t40xct, hIWuC, AzmgQ, M7eUr, avgor, g9oRa5, t67DU6, RpGbe and their children) are unmutated; `batch_get` on each returns the Phase 25 end-state x/y/children/properties."
  artifacts:
    - path: "design/Crito.pen"
      provides: "New primitive `type-primitive-size-32`; 4 new semantic aliases `type-semantic-heading-2-*`; 2 new reusable section components `Section / CTA` + `Section / NavBack` inside `g9oRa5`; 2 sibling Pencil notes documenting the slot signatures."
      contains: "Section / CTA, Section / NavBack, type-semantic-heading-2-family, -size, -weight, -lh, type-primitive-size-32"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Token Extensions (Phase 26) sub-section; Variant Evidence (Phase 26) sub-section; OPEN-23-10 + OPEN-23-11 update notes; Open Flags — Phase 26 (OPEN-26-NN) section initialized."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 entry updated from 3 plans (26-01/02/03) to 4 plans (26-00/01/02/03) with the foundation plan named and one-line objectives per plan."
  key_links:
    - from: "type-semantic-heading-2-size"
      to: "type-primitive-size-32"
      via: "$<primitive> reference in set_variables call"
      pattern: "set_variables.*type-semantic-heading-2-size.*\\$type-primitive-size-32"
    - from: "Section / CTA `actions-slot`"
      to: "Primitive / Button / Default (M7eUr) and Primitive / Button / Secondary (hIWuC)"
      via: "typed slot suggestion `slot: ['M7eUr','hIWuC']` (D-52 PREFERRED) + placeholder ref content"
      pattern: "slot:.*\\[.*M7eUr.*hIWuC.*\\]"
    - from: "Section / NavBack `links-slot`"
      to: "Text nodes styled with body typography (4 nav links: Home / Blog / Projects / Contact)"
      via: "frame containing 4 text child nodes (NOT Button refs — per RESEARCH § Don't Hand-Roll)"
      pattern: "links-slot.*contains:.*4.*text"
---

<objective>
Phase 26 foundation: extend the Phase 23 token surface with `type-semantic-heading-2-*` (and the supporting `type-primitive-size-32` primitive), then build two new reusable section components — `Section / CTA` (3-slot for general purpose CTAs) and `Section / NavBack` (2-slot, narrow-purpose for 404) — inside the existing `_Components / Sections` parent frame `g9oRa5`. Also update ROADMAP.md to reflect the 4-plan Phase 26 (originally listed as 3).

**Purpose:** Plans 26-01 (FAQ) and 26-02 (404) instance the Section/CTA and Section/NavBack components and reference the new heading-2 typography token. Without this foundation plan, those plans cannot proceed. Separating foundation from consumer-instance work mirrors the Phase 23 precedent (tokens-before-consumers) and lets PEN-INVENTORY's token surface settle before page-frame reconstruction begins.

**Output:** A `.pen` file with 100 tokens (was 95), 4 reusable components in `g9oRa5` (was 2), a ROADMAP.md correctly listing 4 plans for Phase 26, and PEN-INVENTORY.md extended with the audit-trail rows for the new tokens, components, and OPEN-flag updates. No user-facing visual gate at plan close (D-86 — agent-deterministic deliverable); ONE mid-plan AskUserQuestion gate at Task 1 for heading-2 value confirmation per D-72 + CONTEXT.md Claude's Discretion + RESEARCH.md Focus 3.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight active-editor + token surface baseline</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-87 (pre-flight active-editor enforcement)
    - `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` § Active-Editor Pre-flight Result (D-54) — pattern reference
    - `.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json` — expected baseline IDs for drift check
  </read_first>
  <action>
    Call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert `activeEditor` string ends with `design/Crito.pen`. If mismatch, halt and surface to user per D-87 + Phase 25 D-54 / Phase 24 D-35 protocol.

    Then call `mcp__pencil__get_variables({})` and assert the returned token count is exactly 95 (Phase 25 end-of-phase baseline). Record the call response in 26-00-SUMMARY.md scratch section. If count != 95, halt and surface (token-surface drift since Phase 25 close).

    Then call `mcp__pencil__batch_get` on baseline IDs: `G0wNOc` (Section/Header), `Xs0Hs` (Section/Footer), `t40xct` (Compound/Card), `hIWuC` (Button/Secondary), `AzmgQ` (Substack atomic-glyph), `M7eUr` (Button/Default), `avgor` (Primitives parent), `g9oRa5` (Sections parent), `t67DU6` (Compounds parent), `RpGbe` (Tokens & Foundations parent). Confirm each returns its expected name + structural fields matching end-of-phase-25/id-inventory.json `phase_25_additions` + Phase 24 baseline. No mutations in this task.
  </action>
  <verify>
    Single message to user: "Pre-flight PASS. Active editor: design/Crito.pen. Token surface: 95 (Phase 25 baseline). Baseline IDs intact: G0wNOc, Xs0Hs, t40xct, hIWuC, AzmgQ, M7eUr, avgor, g9oRa5, t67DU6, RpGbe."
  </verify>
  <acceptance_criteria>
    - `mcp__pencil__get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `mcp__pencil__get_variables({})` returned exactly 95 tokens.
    - `mcp__pencil__batch_get` on all 10 baseline IDs returned existing nodes with names matching expected (no node-missing or property-drift errors).
    - No mutations issued during Task 0 (only `get_editor_state`, `get_variables`, `batch_get`).
  </acceptance_criteria>
  <done>
    Pre-flight assertions all pass; baseline token count + structural IDs confirmed intact; subsequent tasks may safely mutate.
  </done>
</task>

<task type="checkpoint:decision">
  <name>Task 1: Heading-2 source-of-truth gate — .fig consult OR interpolation</name>
  <files>None (decision-only; no Pencil mutations this task)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-72 (heading-2 source-derivation), Claude's Discretion section
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Focus 3 (`.fig` zipped binary unreadable by agent), § Open Questions #1 + #2 (recommended interpolation + new primitive)
    - `.planning/research/PEN-INVENTORY.md` Tokens Written — Semantic Aliases (heading-1 4-part composite at lines 687-690)
    - `.planning/research/PITFALLS.md` Pitfall 1 (no inventing primitives without source)
  </read_first>
  <action>
    Per D-72 + Claude's Discretion: the executing agent cannot directly read the Crito `.fig` (it's a zipped Figma binary per RESEARCH § Focus 3). Surface the decision to the user via AskUserQuestion with this structure:

    Question: "Phase 26 needs new semantic token `type-semantic-heading-2-*` (4-part composite mirroring heading-1). The agent cannot read .fig files. Two options:"

    Options:
    - **Option A — Accept interpolation default** — `type-semantic-heading-2-size: 32` (Plus Jakarta Sans 700, lh 1.4). Adds new primitive `type-primitive-size-32`. Raises OPEN-26-01 noting "interpolation-derived, not .fig-sourced; verifier consumer = Phase 28 Blog."
    - **Option B — User supplies .fig-derived values** — User opens Figma desktop with Crito community template, reads heading-2 Variables panel (family, size, weight, line-height), reports back. No OPEN flag (source-confirmed). Agent will then proceed with user-supplied values for the 4-part composite.

    Description must include: the 4 token names that will be written (`type-semantic-heading-2-family`, `-size`, `-weight`, `-lh`); the recommended default values; the cite to D-72 + RESEARCH Focus 3; the implication for token count (95 → 100 if Option A with new primitive added).

    Wait for user response. Record the chosen option + the 4 values (family, size, weight, lh) and the new-primitive-needed flag (true if size is not in existing primitives 14/16/18/48/70) in 26-00-SUMMARY.md scratch. These values feed Task 2.
  </action>
  <verify>
    User has responded with Option A or Option B; if Option B, 4 numeric/string values for family/size/weight/lh are recorded.
  </verify>
  <acceptance_criteria>
    - AskUserQuestion was presented with both options + token name list + value defaults + OPEN-flag implication.
    - User response is captured in scratch with: option chosen, family value, size value (number), weight value, lh value, new-primitive-name (if applicable).
    - If Option A (default interpolation), OPEN-26-01 entry is drafted to be added to PEN-INVENTORY in Task 5.
  </acceptance_criteria>
  <done>
    Heading-2 values are decided; next task can write them via `set_variables`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write `type-primitive-size-32` (if needed) + 4 heading-2 semantic aliases via set_variables</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 1 scratch (chosen values for heading-2 family/size/weight/lh + whether a new primitive is needed)
    - `.planning/research/PEN-INVENTORY.md` § Tokens Written — Semantic Aliases (heading-1 surface pattern at lines 687-690)
    - `.planning/phases/23-audit-token-foundation/23-CONTEXT.md` D-12 (flat-dash naming), D-14 (semantic → primitive only)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Example 2 (`set_variables` shape for semantic-alias-to-primitive resolution)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state({ include_schema: false })` and re-assert active editor (D-87 — every mutating call gets a fresh pre-flight).

    Call `mcp__pencil__set_variables({ replace: false, variables: { ... } })` with the variables map derived from Task 1 outcome. If Option A or Option B with size value not in primitives 14/16/18/48/70: the map MUST include a new primitive (e.g., `type-primitive-size-32`) plus the 4 semantic aliases referencing primitives via `$<primitive>` syntax. Strict Phase 23 D-14 — semantic aliases resolve to primitives, never to literal values.

    Example shape (assuming Option A — size 32):
    - `type-primitive-size-32` → literal "32"
    - `type-semantic-heading-2-family` → `$type-primitive-family-display`
    - `type-semantic-heading-2-size` → `$type-primitive-size-32`
    - `type-semantic-heading-2-weight` → `$type-primitive-weight-700`
    - `type-semantic-heading-2-lh` → `$type-primitive-lh-heading`

    After the write, call `mcp__pencil__get_variables({})` and assert the returned token count is exactly 100 (was 95 + 1 primitive + 4 semantics) for Option A, OR 99 (was 95 + 4 semantics, no new primitive needed) for Option B if user supplied an existing primitive size. Record exact count + the 5 (or 4) new token entries in 26-00-SUMMARY.md.

    OPEN-23-13 reminder: `$<token>` syntax works in `set_variables` but NOT in `batch_design` (that's Task 3+4 — use literal values there).
  </action>
  <verify>
    `mcp__pencil__get_variables({})` returns 100 tokens (Option A) or 99 (Option B without new primitive); the 4 new semantic alias entries are present and each resolves to a `$<primitive>` reference (not a literal); the new primitive (if added) is present and resolves to its literal size value.
  </verify>
  <acceptance_criteria>
    - `mcp__pencil__get_editor_state` pre-flight passed before `set_variables` call (D-87 enforced).
    - `mcp__pencil__set_variables` call did not error.
    - `mcp__pencil__get_variables({})` returns 100 tokens (Option A) or matches the Option B count.
    - Inspecting the 4 heading-2 alias entries shows each value matches pattern `$type-primitive-*` (no raw literal values on semantics).
    - If a new primitive was added, its entry shows a literal numeric string (e.g., "32") and is named `type-primitive-size-<value>`.
  </acceptance_criteria>
  <done>
    Token surface extended by 4 or 5 entries; semantic aliases resolve through primitives per Phase 23 D-14; foundation for Section/CTA + NavBack typography in place.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build `Section / CTA` inside `g9oRa5` via batch_design (3 slots, sibling note)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-78 (Section/CTA 3-slot signature) + D-80 (lives in g9oRa5) + D-52 carry-forward (belt-and-suspenders documentation)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Pattern 2 (concrete `batch_design` shape for Section/CTA, including typed `actions-slot: ['M7eUr','hIWuC']`)
    - `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` § Sibling Pencil note `hkh26` (note pattern reference)
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` D-52 PREFERRED typed-slot Pattern + `enabled: true` placeholder pattern (D-53)
    - `.planning/research/PITFALLS.md` (relevant: Pitfall 4 — `batch_design` rejects `$<token>` references; use literal hex/px values + audit-trail via PEN-INVENTORY)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight). Then call `mcp__pencil__batch_design` to Insert a new reusable component `Section / CTA` as a child of `g9oRa5` (id: `g9oRa5`). Follow RESEARCH § Pattern 2 exactly.

    Required structure:
    - Outer frame: `type: "frame"`, `name: "Section / CTA"`, `reusable: true`, `width: 1200` (matches Header/Footer Phase 25 ship width), `layout: "vertical"`, `gap: 24` (bound to space-semantic-stack-md), `padding: [64, 0]` (section-y rhythm), `alignItems: "center"`, `fill: "#f2f2f7ff"` (bound to color-semantic-bg-surface — literal per OPEN-23-13).
    - Child 1 `headline-slot`: `type: "frame"`, `name: "headline-slot"`, `slot: []` (untyped — accepts text), `enabled: true`. Contains one text node with content "Still have questions?" (STUB per D-82), fontFamily "Plus Jakarta Sans" (heading-2-family), fontSize matching Task 1 value (32 if Option A), fontWeight "700", fill "#141f39ff" (color-semantic-text-primary), lineHeight 1.4.
    - Child 2 `body-slot`: `type: "frame"`, `name: "body-slot"`, `slot: []`, `enabled: true`. Contains one text node with content "Get in touch and we'll help you figure out next steps." (STUB), fontFamily "Inter" (prose-paragraph-family), fontSize 16, fontWeight "400", fill "#52525bff" (color-semantic-text-secondary), lineHeight 1.625.
    - Child 3 `actions-slot`: `type: "frame"`, `name: "actions-slot"`, `slot: ["M7eUr","hIWuC"]` (D-52 PREFERRED Pattern — typed suggestion), `enabled: true`, `layout: "horizontal"`, `gap: 12`. Contains one ref `{ type: "ref", ref: "M7eUr", descendants: { <ATJK9>: { content: "Get in touch" }, <V4Dx4i>: { enabled: true } } }`. Look up actual descendant text-node id + arrow-icon slot id from `end-of-phase-25/id-inventory.json` for Button/Default M7eUr; if unknown, do a `batch_get(["M7eUr"])` first to enumerate descendants.

    Sibling Pencil note (D-52 belt-and-suspenders): Insert another child of `g9oRa5` of `type: "note"`, content describing the slot signature:
    "Section / CTA — slot signature: headline-slot (text, heading-1 or heading-2 typography, REQUIRED); body-slot (text, prose-paragraph typography, OPTIONAL — enabled:true with placeholder per D-53); actions-slot (frame, accepts 1–2 Primitive/Button refs — typed slot suggestion M7eUr/hIWuC per Phase 25 D-52 PREFERRED Pattern). FAQ + future Homepage instance with overrides via descendants paths."
    Note width 1200, fontFamily "Inter", fontSize 12.

    Record the new component frame id (Pencil-assigned) + each slot child id + sibling note id in 26-00-SUMMARY.md (these feed Plan 26-01 descendants overrides per RESEARCH Example 3).

    Pitfall 4 reminder: all fill / fontSize / padding values are LITERAL (no `$<token>`). PEN-INVENTORY Variant Evidence row (Task 5) audit-trails each literal-to-semantic binding.
  </action>
  <verify>
    `mcp__pencil__batch_get(["<sectionCtaId>"])` returns a frame named "Section / CTA" with 3 direct frame children named "headline-slot", "body-slot", "actions-slot" + the sibling Pencil note as a g9oRa5 sibling. `actions-slot` shows `slot: ["M7eUr","hIWuC"]`. `headline-slot` text content == "Still have questions?". `actions-slot` contains 1 ref to M7eUr with descendants override for "Get in touch".
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(["<sectionCtaId>"])` returns a node named "Section / CTA" with `reusable: true`, parent `g9oRa5`, 3 child frames, fill "#f2f2f7ff", width 1200.
    - All 3 slots have `enabled: true` with placeholder content.
    - `actions-slot` has typed `slot: ["M7eUr","hIWuC"]` (D-52 PREFERRED Pattern).
    - Sibling Pencil note exists as a child of `g9oRa5` (NOT inside Section/CTA itself).
    - All component IDs (section + slots + note + descendant ids of M7eUr ref) recorded in 26-00-SUMMARY.md scratch.
  </acceptance_criteria>
  <done>
    Section/CTA is a first-class reusable library component instanceable from Plan 26-01 FAQ.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build `Section / NavBack` inside `g9oRa5` via batch_design (2 slots, sibling note)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-79 (Section/NavBack 2-slot, 404-only) + D-80 (lives in g9oRa5) + Claude's Discretion section (4 link labels: Home / Blog / Projects / Contact)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Pattern 3 (concrete `batch_design` shape for Section/NavBack) + § Don't Hand-Roll (NavBack links are text nodes, NOT Button refs)
    - `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` § Sibling Pencil note `x48zh` (note pattern)
    - `.planning/research/PITFALLS.md` Pitfall O5/O6 (component bloat — NavBack stays narrow, not generalized)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight). Then call `mcp__pencil__batch_design` to Insert a new reusable component `Section / NavBack` as a child of `g9oRa5`. Follow RESEARCH § Pattern 3 exactly.

    Required structure:
    - Outer frame: `type: "frame"`, `name: "Section / NavBack"`, `reusable: true`, `width: 1200`, `layout: "vertical"`, `gap: 16`, `padding: [32, 0]`, `alignItems: "center"`.
    - Child 1 `heading-slot`: `type: "frame"`, `name: "heading-slot"`, `slot: []`, `enabled: true`. Contains one text node with content "Find what you need" (STUB placeholder), heading-2 typography (Plus Jakarta Sans, Task 1 size, weight 700, lh 1.4), fill "#141f39ff".
    - Child 2 `links-slot`: `type: "frame"`, `name: "links-slot"`, `slot: []`, `enabled: true`, `layout: "horizontal"`, `gap: 24`. Contains 4 text-node children with body typography (Inter, 16, weight 400, fill "#141f39ff", lh 1.625), contents: "Home", "Blog", "Projects", "Contact" (per Claude's Discretion default matching Joel's v1.3 nav set). NOT Button refs — plain text nodes per RESEARCH Don't Hand-Roll.

    Sibling Pencil note inside `g9oRa5` (D-52 belt-and-suspenders): content describing the slot signature:
    "Section / NavBack — slot signature: heading-slot (text, heading-2 typography, REQUIRED); links-slot (frame, contains 3-4 text-node nav-back links — text nodes NOT Button primitives, per Pitfall O5/O6 narrow-scope design; 404 instances with Home/Blog/Projects/Contact). Built narrowly for 404; NOT generalized to Blog/Projects related-content (those need card grids — different signature, deferred to Phase 28/29 per D-79)."
    Note width 1200, fontFamily "Inter", fontSize 12.

    Record the new component frame id + heading-slot id + links-slot id + 4 link text-node ids + sibling note id in 26-00-SUMMARY.md scratch (feed Plan 26-02 descendants overrides).
  </action>
  <verify>
    `mcp__pencil__batch_get(["<sectionNavBackId>"])` returns a frame named "Section / NavBack" with 2 direct frame children named "heading-slot" and "links-slot". `links-slot` has 4 text-node children with contents "Home", "Blog", "Projects", "Contact". `heading-slot` text content == "Find what you need". Sibling Pencil note exists as child of `g9oRa5`.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(["<sectionNavBackId>"])` returns a node named "Section / NavBack" with `reusable: true`, parent `g9oRa5`, 2 child frames.
    - `links-slot` contains exactly 4 text-node children (no Button refs).
    - Link contents are "Home", "Blog", "Projects", "Contact" in left-to-right horizontal order.
    - Both slots have `enabled: true` with placeholder content.
    - Sibling Pencil note exists as a child of `g9oRa5` describing the 2-slot signature + narrow-scope rationale.
    - All component IDs recorded in 26-00-SUMMARY.md.
    - `mcp__pencil__batch_get(["g9oRa5"])` confirms g9oRa5 now has 4 reusable section children (Header G0wNOc + Footer Xs0Hs + new CTA + new NavBack) + their sibling notes.
  </acceptance_criteria>
  <done>
    Section/NavBack is a first-class reusable library component instanceable from Plan 26-02 404.
  </done>
</task>

<task type="auto">
  <name>Task 5: Plan close — PEN-INVENTORY extensions + ROADMAP update + snapshot_layout sweep</name>
  <files>
    - .planning/research/PEN-INVENTORY.md
    - .planning/ROADMAP.md
    - design/Crito.pen (read-only validation)
  </files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-88 (PEN-INVENTORY extension pattern) + D-84 (4-plan structure → ROADMAP update)
    - `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` § PEN-INVENTORY Updates (D-56 audit-trail discipline) — pattern reference
    - `.planning/research/PEN-INVENTORY.md` — current state, locate "Token Extensions (Phase 24)" and "Variant Evidence (Phase 24)" sections to extend with "(Phase 26)" sub-sections; locate OPEN-23-10 + OPEN-23-11 rows for update notes
    - `.planning/ROADMAP.md` Phase 26 section — replace 3-plan list with 4-plan list
  </read_first>
  <action>
    Step 1 — PEN-INVENTORY extensions (per D-88):

    Edit `.planning/research/PEN-INVENTORY.md` to add:
    - In Token Extensions section, new "(Phase 26)" sub-section with rows for:
      - `type-primitive-size-32` (if Option A or new-primitive case) — type: primitive, value: 32, source: "Phase 26 D-72 interpolation between heading-1 48 and body 16; OPEN-26-01 flag for Phase 28 Blog verification" OR ".fig-supplied by user".
      - `type-semantic-heading-2-family` → `type-primitive-family-display`
      - `type-semantic-heading-2-size` → `type-primitive-size-32` (or chosen size primitive)
      - `type-semantic-heading-2-weight` → `type-primitive-weight-700`
      - `type-semantic-heading-2-lh` → `type-primitive-lh-heading`
    - In Variant Evidence section, new "(Phase 26)" sub-section with rows for Section/CTA + Section/NavBack: outer-frame literal-to-semantic bindings (fill, padding, gap, width, alignItems) per Pitfall 4 audit-trail; per-slot literal-to-semantic bindings (typography family/size/weight/lh; text fill); sibling-note IDs.
    - Update OPEN-23-10 row: append "Phase 26 D-72 ships heading-2 only (`type-semantic-heading-2-*` 4-part composite + `type-primitive-size-32` primitive); heading-3/-4/-5/-6 remain open for Phase 28 (Blog post hierarchy) consumers."
    - Update OPEN-23-11 row: append "Phase 26 declined to add prose-link / prose-list / inline-code (no consumer — FAQ Q+A is plain paragraphs); re-pointed to Phase 28 per D-70 + D-73."
    - Create new section "## Open Flags — Phase 26 (OPEN-26-NN)" (if not already present): add OPEN-26-01 row if Task 1 selected Option A (interpolation): "heading-2 size 32 interpolation-derived from .fig-unreadable fallback; verifier consumer Phase 28 Blog when richer heading hierarchy surfaces."

    Step 2 — ROADMAP.md update (per D-84):

    Edit `.planning/ROADMAP.md` Phase 26 section. Replace the existing 3-plan list with 4 plans:
    - Update `**Plans**: TBD` to `**Plans**: 4 plans`.
    - Replace the plan checkbox list with:
      ```
      Plans:

      - [ ] 26-00-PLAN.md — Phase 26 foundation: `type-semantic-heading-2-*` 4-part composite + `type-primitive-size-32` primitive + new Section/CTA (3-slot) + Section/NavBack (2-slot) reusable components inside `g9oRa5`
      - [ ] 26-01-PLAN.md — FAQ page frame reconstruction: top-level `FAQ` frame at 1440 width via `find_empty_space_on_canvas`; instances Section/Header + page-intro + Q+A list (5 verbatim pairs from `src/pages/faq.astro`) + Section/CTA (STUB microcopy) + Section/Footer; plan-close calibration spot-check user gate
      - [ ] 26-02-PLAN.md — 404 page frame reconstruction: top-level `404` frame at 1440 width; instances Section/Header + message section (STUB text) + Section/NavBack + Section/Footer; plan-close calibration spot-check user gate
      - [ ] 26-03-PLAN.md — Codify `.planning/research/CALIBRATION-PROTOCOL.md` (branch matrix crito-source vs joel-only; per-step script per branch; EXACT/APPROXIMATE/STUB label definitions; OPEN-flag template; PAGE-11 crito-source-only rule; PAGE-09 desktop-only)
      ```

    Step 3 — snapshot_layout sweep per plan-close convention (D-87 cross-cutting):

    Call `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })`. Expected: `"No layout problems."` (per Phase 25 VAL-25-15 carry-forward). Then call `mcp__pencil__snapshot_layout({ rootId: "<sectionCtaId>", problemsOnly: true })` and `mcp__pencil__snapshot_layout({ rootId: "<sectionNavBackId>", problemsOnly: true })`. Document any text-clipping false-positives in 26-00-SUMMARY.md per Phase 24 24-05-SUMMARY quirk + Phase 25 carry-forward.

    Step 4 — Write 26-00-SUMMARY.md with: pre-flight result, Task 1 user-chosen heading-2 values, Token Extensions written + final token count, Section/CTA id + slot ids + descendants paths used, Section/NavBack id + slot ids + 4 link text-node ids, sibling note ids, PEN-INVENTORY edits diff, ROADMAP edits diff, snapshot_layout outcomes (document level + per-section), and a "Files Modified" list. No user gate at plan close per D-86 (Plan 26-00 is agent-deterministic delivery once Task 1 gate resolved).
  </action>
  <verify>
    - `cat .planning/research/PEN-INVENTORY.md | grep -A 5 "(Phase 26)"` returns the new Token Extensions + Variant Evidence sub-sections.
    - `cat .planning/research/PEN-INVENTORY.md | grep -E "OPEN-23-10|OPEN-23-11"` shows the appended Phase 26 update notes.
    - `cat .planning/ROADMAP.md | grep -A 8 "Phase 26"` shows 4 plan entries (26-00 through 26-03).
    - `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `"No layout problems."` at document level.
    - 26-00-SUMMARY.md exists at `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md`.
  </verify>
  <acceptance_criteria>
    - PEN-INVENTORY.md contains exactly one new "(Phase 26)" sub-section under Token Extensions with at least 4 alias rows + 1 primitive row (Option A) or 4 alias rows (Option B without new primitive).
    - PEN-INVENTORY.md contains exactly one new "(Phase 26)" sub-section under Variant Evidence with rows for both Section/CTA + Section/NavBack literal-to-semantic bindings + their sibling notes.
    - PEN-INVENTORY.md OPEN-23-10 + OPEN-23-11 rows have appended Phase 26 update notes (not new rows — appended notes).
    - If OPEN-26-01 was raised in Task 1, PEN-INVENTORY.md "## Open Flags — Phase 26 (OPEN-26-NN)" section exists with the OPEN-26-01 row.
    - ROADMAP.md Phase 26 section shows 4 plan checkboxes (26-00 through 26-03) and `**Plans**: 4 plans`.
    - `snapshot_layout` document-level returns "No layout problems." (per-section may show text-clipping false-positives — documented in 26-00-SUMMARY.md).
    - 26-00-SUMMARY.md exists and documents: pre-flight outcome, heading-2 user-chosen values, all new IDs (component + slot + note), PEN-INVENTORY + ROADMAP edits, snapshot outcomes.
  </acceptance_criteria>
  <done>
    Phase 26 foundation complete. Token surface extended, two new section components in place, audit trail updated, ROADMAP reflects 4-plan structure. Plan 26-01 may now proceed.
  </done>
</task>

</tasks>

<verification>
- Active editor remained `design/Crito.pen` for every mutating call (D-87).
- Token count moved from 95 → 100 (Option A) or 99 (Option B without new primitive) — exact count confirmed by `get_variables({})`.
- Both new section components are reusable library children of `g9oRa5` (not standalone) and follow the Phase 25 D-52 belt-and-suspenders documentation pattern (typed slots where applicable + sibling Pencil note).
- Phase 25 baseline IDs (G0wNOc, Xs0Hs, t40xct, hIWuC, AzmgQ + Phase 24 primitives) unmutated.
- PEN-INVENTORY.md and ROADMAP.md edits committed to git per `commit_docs: true` policy.
- 26-00-SUMMARY.md exists.
</verification>

<success_criteria>
- `get_variables({})` returns 100 tokens (Option A) — verifiable via Pencil MCP.
- `batch_get(["g9oRa5"])` shows 4 reusable section children (was 2 at Phase 25 close) — Header, Footer, CTA, NavBack.
- `batch_get(["<sectionCtaId>"])` shows 3 named slot frames with `enabled: true` + placeholder content.
- `batch_get(["<sectionNavBackId>"])` shows 2 named slot frames; `links-slot` contains 4 text nodes with the 4 nav labels.
- ROADMAP.md Phase 26 shows 4 plans listed; `.planning/research/PEN-INVENTORY.md` contains Phase 26 Token Extensions + Variant Evidence sub-sections + OPEN-23-10/11 update notes.
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
</success_criteria>

<output>
After completion, create `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` containing:
- Pre-flight outcomes (editor + token-count baseline + baseline IDs intact)
- Task 1 user-chosen heading-2 values (Option A or B; family, size, weight, lh; new-primitive-name if applicable; OPEN-26-01 flag if raised)
- New IDs: `<sectionCtaId>`, `<sectionCtaHeadlineSlotId>`, `<sectionCtaBodySlotId>`, `<sectionCtaActionsSlotId>`, `<sectionCtaNoteId>`, `<sectionNavBackId>`, `<sectionNavBackHeadingSlotId>`, `<sectionNavBackLinksSlotId>`, 4 `<navBackLink*Id>` text node ids, `<sectionNavBackNoteId>`
- M7eUr descendant ids used for actions-slot ref (label text node id; arrow-icon slot id)
- Final token count (100 or 99) + each new token's name + resolution path
- PEN-INVENTORY edits diff (new sub-sections; updated OPEN rows)
- ROADMAP edits diff (3 → 4 plans, plan-name list)
- snapshot_layout outcomes (document level + per-section, with text-clipping quirk documented per Phase 24/25 precedent)
- Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/ROADMAP.md, .planning/phases/26-.../26-00-SUMMARY.md
</output>
