---
phase: 26-faq-404-reconstruction-calibration-workflow-established
plan: 01
type: execute
wave: 1
depends_on:
  - 26-00
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-04
  - PAGE-09
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "A new top-level page frame named `FAQ` exists in `design/Crito.pen` as a direct child of the document root (NOT inside any library parent)."
    - "The `FAQ` frame is positioned to the right of the existing Crito page-frame cluster via `mcp__pencil__find_empty_space_on_canvas` (D-74) and has width 1440 (D-75)."
    - "The `FAQ` frame contains 5 stacked content blocks in a top-down vertical auto-layout: (1) Section/Header ref (G0wNOc), (2) page-intro section frame, (3) Q+A list section frame with 5 verbatim Q+A pairs from `src/pages/faq.astro` lines 11-32, (4) Section/CTA ref (from Plan 26-00) with STUB microcopy, (5) Section/Footer ref (Xs0Hs)."
    - "Q items use `type-semantic-heading-2-*` typography (the token added in Plan 26-00) bound via literal values; A items use `type-semantic-prose-paragraph-*` (Phase 23 surface)."
    - "The 5 Q+A pairs ship verbatim from `src/pages/faq.astro` lines 11-32 — no edits, no rewording, all apostrophes preserved."
    - "Section/CTA instance carries STUB microcopy (headline / body / button-label all placeholder strings per D-82) — fidelity label STUB."
    - "Per-section fidelity labels recorded in 26-01-SUMMARY.md per D-83: Q+A list = EXACT (real content + correct token usage), CTA = STUB (placeholder microcopy), Header instance = EXACT (Phase 25 shipped), Footer instance = EXACT (Phase 25 shipped), page-intro = EXACT or APPROXIMATE depending on copy choice."
    - "User has spot-checked the FAQ page frame via plan-close calibration AskUserQuestion gate (D-65 inline rendering pattern per RESEARCH § Focus 4 — `get_screenshot` is inline-only, NOT disk-written)."
    - "PEN-INVENTORY.md Frames Inventory has a new row for `FAQ` (frame_id assigned at insertion, scope: `joel-only-no-crito-ref`, joel_page_map: `/faq`, status: `reconstructed-PHASE-26`); any plan-surfaced flags added under Open Flags — Phase 26."
    - "D-58: Fresh design in Crito vocab, NOT v1.3 visual mirror"
    - "D-59: FAQ visual shape — stacked Q+A pairs, no accordion in design"
    - "D-61: FAQ CTA section included as layout, microcopy STUB'd per D-82"
    - "D-62: Calibration vs _Tokens & Foundations (token-usage axis)"
    - "D-70: Phase 26 ships only prose tokens FAQ uses; re-point prose-link/list to Phase 28"
    - "D-71: type-semantic-prose-paragraph-* consumed as-is; calibration spot-check is verification"
    - "D-76: Plain frame name `FAQ`, no prefix"
    - "D-77: Section/Header + Section/Footer instanced top + bottom; Crito-source labels stay"
    - "D-87: Pre-flight editor-state assertion (carry-forward)"
    - "D-88: PEN-INVENTORY extension (Frames Inventory row for FAQ)"
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `FAQ` with 5 vertical content blocks; section/instance refs and content text nodes."
      contains: "FAQ page frame, page-intro section, Q+A list section with 5 Q+A pairs, Section/CTA instance, Section/Header ref G0wNOc, Section/Footer ref Xs0Hs"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for FAQ; Variant Evidence (Phase 26) rows for page frame + page-intro + Q+A section structural literal-to-semantic bindings; any new OPEN-26-NN flags raised at calibration."
  key_links:
    - from: "FAQ page frame"
      to: "Section / Header (G0wNOc)"
      via: "ref child at top of auto-layout vertical stack"
      pattern: "FAQ.*ref.*G0wNOc"
    - from: "FAQ page frame"
      to: "Section / CTA (from Plan 26-00)"
      via: "ref child with descendants override for headline/body/actions-slot button label (STUB content per D-82)"
      pattern: "FAQ.*ref.*<sectionCtaId>"
    - from: "FAQ page frame"
      to: "Section / Footer (Xs0Hs)"
      via: "ref child at bottom of auto-layout vertical stack"
      pattern: "FAQ.*ref.*Xs0Hs"
    - from: "Q+A list section"
      to: "5 verbatim Q+A pairs from src/pages/faq.astro lines 11-32"
      via: "10 text nodes (5 Q + 5 A) in stacked Q+A frames using heading-2 / prose-paragraph typography"
      pattern: "How long does a typical project take"
---

<objective>
Create the FAQ page frame in `design/Crito.pen` as a fresh-design composition (D-58 — joel-only-no-crito-ref, NOT a v1.3 visual mirror) instancing Phase 25 Section/Header + Section/Footer + Plan 26-00 Section/CTA, plus a Q+A list section that ships Joel's 5 v1.3 FAQ Q+A pairs verbatim from `src/pages/faq.astro`. Plan-close calibration spot-check gate per D-65 confirms the composition uses declared tokens correctly.

**Purpose:** First real per-page reconstruction in v2.0. Validates that Phase 23 tokens + Phase 24 primitives + Phase 25 sections + Plan 26-00 additions compose correctly in a page context. Establishes the joel-only-branch calibration workflow that Plan 26-03 will codify into CALIBRATION-PROTOCOL.md.

**Output:** A working `FAQ` top-level page frame; an extended PEN-INVENTORY Frames Inventory with the FAQ row + Variant Evidence Phase 26 sub-section rows; per-section fidelity labels recorded; a plan-close user-approved (or revision-requested) state. ONE user gate at plan close (calibration spot-check per D-86).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
@.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json
@src/pages/faq.astro
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plan 26-00 outputs intact</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` — Section/CTA id + slot ids; Section/NavBack ids; new token names; final token count after Plan 26-00
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-87 (pre-flight) + D-88 (PEN-INVENTORY extension)
    - `.planning/research/exports/v2.0/end-of-phase-25/id-inventory.json` — Phase 25 + Phase 24 baseline IDs
  </read_first>
  <action>
    Call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert active editor ends with `design/Crito.pen`. Halt on mismatch per D-87.

    Then call `mcp__pencil__get_variables({})` and assert exact count from 26-00-SUMMARY.md (100 if Option A; 99 if Option B without new primitive). Halt if drift.

    Then `mcp__pencil__batch_get` on these IDs from 26-00-SUMMARY.md: `<sectionCtaId>`, `<sectionCtaHeadlineSlotId>`, `<sectionCtaBodySlotId>`, `<sectionCtaActionsSlotId>`. Confirm structure matches Plan 26-00 close state (3 slots, actions-slot has typed `slot: ["M7eUr","hIWuC"]`, all slots `enabled: true`, fill #f2f2f7ff on outer frame).

    Then `batch_get` on Phase 25 baseline IDs: `G0wNOc` (Section/Header), `Xs0Hs` (Section/Footer), `M7eUr` (Button/Default), `g9oRa5` (Sections parent). Confirm 4 reusable section children in g9oRa5 (Header + Footer + CTA + NavBack).

    Record in 26-01-SUMMARY.md scratch: pre-flight result + Plan 26-00 IDs intact + token count match.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token count: {N} matches Plan 26-00 close state. Section/CTA id `<sectionCtaId>` with 3 slots intact. Phase 25 baseline IDs intact."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `get_variables({})` count matches Plan 26-00 close-state value documented in 26-00-SUMMARY.md.
    - `batch_get` on Plan 26-00 IDs (Section/CTA + 3 slots) confirms structural intactness.
    - `batch_get` on g9oRa5 confirms 4 reusable section children present.
    - No mutations issued in Task 0.
  </acceptance_criteria>
  <done>
    Plan 26-00 outputs verified intact; Plan 26-01 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `FAQ` top-level page frame via find_empty_space_on_canvas + batch_design</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-74 (find_empty_space_on_canvas to the right of Crito cluster), D-75 (1440 width, Crito-rhythm height estimate), D-76 (plain name "FAQ")
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Focus 1 (page-frame creation mechanics) + § Pattern 1 (batch_design Insert shape) + § Open Questions #3 (fixed-height-then-fit_content) + #4 (placement directional preference unverified) + § Pitfall 3 (page frame as direct document child, NOT inside library parents) + § Pitfall 5 (page width 1440 + alignItems center for Section width 1200 inner content)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 1800 })` to find non-overlapping coordinates. (Height 1800 is an estimate: Header ~80 + page-intro ~200 + Q+A list 5×~180 + CTA ~250 + Footer ~500 = ~1930; round down for tighter rhythm — adjust at Task 6 fit_content step.)

    Inspect the returned `{x, y}`. If x is LESS THAN the right-edge of the existing Crito cluster (RESEARCH Open Question #4 caveat — tool may not expose directional preference), raise OPEN-26-NN noting the actual placement direction in the scratch summary; do NOT halt — proceed with returned coords. Document for CALIBRATION-PROTOCOL.md Plan 26-03.

    Then call `mcp__pencil__batch_design` to Insert a top-level frame:
    - Parent: document (NOT g9oRa5 — page frames sit at canvas root per Pitfall 3).
    - `type: "frame"`, `name: "FAQ"` (plain name per D-76).
    - Position: `{x, y}` from `find_empty_space_on_canvas`.
    - Dimensions: `width: 1440`, `height: 1800` (numeric for now; Update to fit_content in Task 6 if overflow).
    - Layout: `layout: "vertical"`, `gap: 0`, `padding: 0`, `alignItems: "center"` (per Pitfall 5 — outer 1440 + inner sections 1200 centered).
    - Fill: `"#ffffffff"` (color-semantic-bg-page literal per OPEN-23-13).

    Record `<faqPageId>` (Pencil-assigned), the returned x/y coords, and the height estimate in 26-01-SUMMARY.md scratch.
  </action>
  <verify>
    `mcp__pencil__batch_get(["<faqPageId>"])` returns a top-level frame named "FAQ", parent == document root, width 1440, layout vertical, alignItems center, fill #ffffffff.
  </verify>
  <acceptance_criteria>
    - Pre-flight `get_editor_state` PASSED.
    - `find_empty_space_on_canvas` returned `{x, y}` and any directional anomaly was noted in scratch (not blocking).
    - `batch_design` Insert completed without error.
    - `batch_get(["<faqPageId>"])` returns a frame at document root level (NOT inside g9oRa5, avgor, t67DU6, or RpGbe) named "FAQ" with width 1440 + vertical auto-layout + alignItems center.
    - No mutations to baseline IDs (`batch_get` on g9oRa5 / Xs0Hs / G0wNOc shows unchanged children counts vs Task 0).
    - `<faqPageId>` recorded in 26-01-SUMMARY.md scratch.
  </acceptance_criteria>
  <done>
    Empty FAQ page frame in place; ready for content insertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header ref + page-intro section as first 2 children of FAQ frame</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-77 (Header instanced as-is, Crito-source nav labels stay — Joel v1.3 4-link override deferred to Phase 31)
    - `.planning/phases/25-section-compound-components/25-01-SUMMARY.md` § Section/Header Build — G0wNOc structural + 6 Crito-source nav label text-node ids (used as descendants paths if any overrides needed — but per D-77, no overrides for FAQ instance)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Example 3 (ref + descendants pattern)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__batch_design` with 2 Insert operations as children of `<faqPageId>`:

    Insert 1 — Section/Header ref:
    - `type: "ref"`, `ref: "G0wNOc"` (Section/Header from Phase 25 Plan 25-01).
    - NO descendants overrides per D-77 (Crito-source nav labels stay — Joel v1.3 4-link override is Phase 31 work).
    - This is the first child of `<faqPageId>` in the vertical auto-layout.

    Insert 2 — Page-intro section frame:
    - `type: "frame"`, `name: "page-intro"`, `width: 1200`, `layout: "vertical"`, `gap: 16`, `padding: [64, 0]` (matches section-y rhythm), `alignItems: "center"`.
    - Child: one text node `{ type: "text", content: "Frequently Asked Questions", fontFamily: "Plus Jakarta Sans", fontSize: 48, fontWeight: "700", fill: "#141f39ff", lineHeight: 1.4 }` — uses heading-1 semantic typography (NOT heading-2 — page-level title is the largest tier).
    - (Optional second child) a body sub-headline text node with prose-paragraph typography (Inter 16 400) and content "Answers to common questions about working with Joel." Fidelity = APPROXIMATE (this is NEW content, but kept short and brand-neutral; user can revise at calibration gate).

    Record the page-intro frame id + text node ids in 26-01-SUMMARY.md.
  </action>
  <verify>
    `mcp__pencil__batch_get(["<faqPageId>"])` shows 2 direct children: a ref to G0wNOc and a frame named "page-intro". The page-intro contains 1-2 text children with the heading-1 typography on the title.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(["<faqPageId>"])` returns 2 children: Section/Header ref (G0wNOc) + page-intro frame, in that order.
    - Page-intro is a frame (width 1200, vertical auto-layout, alignItems center) with at least one text child whose content == "Frequently Asked Questions" and uses Plus Jakarta Sans 48/700/lh 1.4 (heading-1 typography).
    - No descendants override on the G0wNOc ref (D-77 compliance).
    - Page-intro IDs recorded in scratch.
  </acceptance_criteria>
  <done>
    Top of FAQ page complete: Header + page intro.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert Q+A list section with 5 verbatim Q+A pairs from src/pages/faq.astro</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-59 (stacked Q+A, no accordion) + D-82 (Q+A content shipped verbatim from src/pages/faq.astro lines 11-32)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Focus 8 (the 5 verbatim Q+A pairs extracted) + § Don't Hand-Roll (Q+A items are plain frames, NOT Compound/Card)
    - `src/pages/faq.astro` lines 11-32 (verify source — read directly to confirm no drift)
    - 26-00-SUMMARY.md — final heading-2 size value chosen at Plan 26-00 Task 1 (for Q items)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__batch_design` Insert one new child of `<faqPageId>` (third child of the page frame vertical stack):

    Outer frame `qa-list`:
    - `type: "frame"`, `name: "qa-list"`, `width: 1200`, `layout: "vertical"`, `gap: 32` (between Q+A pairs), `padding: [64, 0]`, `alignItems: "center"` (or "stretch" if Q+A pairs should fill 1200 width).

    Then for EACH of the 5 Q+A pairs, insert a child Q+A frame inside `qa-list`:
    - `type: "frame"`, `name: "qa-item-{1..5}"`, `width: 1200` (or 800 for narrower reading width — Claude's Discretion), `layout: "vertical"`, `gap: 12`, `padding: 0`.
    - Child A — Q text node: `{ type: "text", content: <Q text verbatim>, fontFamily: "Plus Jakarta Sans", fontSize: <heading-2 size from 26-00>, fontWeight: "700", fill: "#141f39ff", lineHeight: 1.4 }` — uses heading-2 typography per D-72.
    - Child B — A text node: `{ type: "text", content: <A text verbatim>, fontFamily: "Inter", fontSize: 16, fontWeight: "400", fill: "#52525bff", lineHeight: 1.625 }` — uses prose-paragraph typography per D-71.

    The 5 verbatim Q+A pairs (from `src/pages/faq.astro` lines 11-32 — DO NOT REWORD, preserve apostrophes verbatim):

    Q1: "How long does a typical project take?"
    A1: "Every project is different. Discovery and prototyping usually take 1-2 weeks, then we'll outline a timeline in the proposal based on scope."

    Q2: "Do you work with clients outside your area?"
    A2: "Absolutely. Most client communication happens over video calls and email. Location doesn't matter."

    Q3: "What if I'm not sure exactly what I need?"
    A3: "That's what discovery is for. We'll talk through your challenges and I'll help clarify what solution makes sense."

    Q4: "How do you handle changes during the project?"
    A4: "Small adjustments are normal. Larger scope changes are discussed together and may adjust the timeline or investment."

    Q5: "What happens after handover?"
    A5: "You get documentation and training. I'm available for questions and can provide ongoing support if needed."

    Pitfall 4 reminder: all literal hex/px values; PEN-INVENTORY Variant Evidence row in Task 6 audit-trails each binding.

    Pitfall reminder (D-59): NO accordion. Stacked, both Q and A always visible.

    Pitfall reminder (RESEARCH Don't Hand-Roll): NO Compound/Card — image-slot is wrong shape. Plain Q+A frames.

    Record the qa-list frame id + 5 qa-item frame ids + 10 text node ids in 26-01-SUMMARY.md.
  </action>
  <verify>
    `batch_get(["<qaListId>"])` shows 5 qa-item frames as children. For each qa-item, 2 text children: Q (heading-2 typography) + A (prose-paragraph typography). Q text contents match the 5 verbatim Q strings exactly (apostrophes preserved). A text contents match the 5 verbatim A strings exactly.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(["<qaListId>"])` returns a frame with 5 child Q+A frames.
    - Each Q+A item contains exactly 2 text children: heading-2 Q + prose-paragraph A.
    - All 5 Q strings + 5 A strings match the verbatim content from `src/pages/faq.astro` lines 11-32 — apostrophes (`'` in `I'm`, `That's`, `we'll`, etc.) preserved.
    - NO accordion mechanic (no toggle / details / summary nodes per D-59).
    - NO Compound/Card refs in the Q+A list (plain frames per RESEARCH Don't Hand-Roll).
    - All 17 IDs (qa-list + 5 qa-item + 10 text nodes) recorded in 26-01-SUMMARY.md.
  </acceptance_criteria>
  <done>
    Q+A list complete with 5 verbatim pairs; first real consumer of heading-2 token.
  </done>
</task>

<task type="auto">
  <name>Task 4: Insert Section/CTA ref (STUB microcopy) + Section/Footer ref</name>
  <files>design/Crito.pen</files>
  <read_first>
    - 26-00-SUMMARY.md — `<sectionCtaId>` + headline-slot / body-slot / actions-slot descendant text-node ids; M7eUr descendant ids for button label
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-61 (FAQ CTA layout EXACT, microcopy STUB) + D-77 (Footer Crito-source labels stay — no override) + D-82 (STUB CTA microcopy)
    - `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` § Section/Footer Build — Xs0Hs structural reference (3 link columns + social row)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Example 3 (descendants override pattern)
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight).

    Call `mcp__pencil__batch_design` Insert 2 children of `<faqPageId>` (4th and 5th of vertical stack):

    Insert 1 — Section/CTA ref with STUB microcopy override:
    - `type: "ref"`, `ref: "<sectionCtaId>"` (from 26-00-SUMMARY.md).
    - `descendants` override map:
      - `<sectionCtaHeadlineSlotTextId>` (look up exact text-node descendant id from 26-00 close state): `{ content: "Still have questions?" }` — STUB placeholder per D-82.
      - `<sectionCtaBodySlotTextId>`: `{ content: "Get in touch and we'll help you figure out next steps." }` — STUB placeholder.
      - M7eUr label text node id (e.g., ATJK9 per 24-05-SUMMARY): `{ content: "Get in touch" }` — STUB placeholder per D-82.

    Insert 2 — Section/Footer ref:
    - `type: "ref"`, `ref: "Xs0Hs"`.
    - NO descendants override per D-77 (Crito-source labels stay; Joel-brand override is Phase 31 work).

    Record the 2 ref node ids in 26-01-SUMMARY.md scratch.
  </action>
  <verify>
    `batch_get(["<faqPageId>"])` now shows 5 direct children in order: G0wNOc ref, page-intro frame, qa-list frame, sectionCtaId ref (with descendants override), Xs0Hs ref. Section/CTA instance descendants resolve to "Still have questions?" / "Get in touch and we'll help you figure out next steps." / button label "Get in touch".
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(["<faqPageId>"])` returns 5 direct children in expected order.
    - Section/CTA ref descendants override produces the 3 STUB strings on render.
    - Section/Footer ref has no descendants override (D-77 compliance).
    - All ref IDs recorded in 26-01-SUMMARY.md.
  </acceptance_criteria>
  <done>
    FAQ page frame is structurally complete with all 5 content blocks.
  </done>
</task>

<task type="auto">
  <name>Task 5: snapshot_layout sweep + page-frame fit_content adjustment if needed</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Open Questions #3 (fixed-height-then-fit_content pattern) + § Pitfall 6 (snapshot_layout text-clipping false-positive)
    - `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` — text-clipping quirk documentation pattern
    - `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` § snapshot_layout Audit (VAL-25-15) — pattern reference
  </read_first>
  <action>
    Re-call `mcp__pencil__get_editor_state` (D-87 pre-flight — even for read-mostly tasks because Task 5 may Update the page-frame height).

    Call `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` — document level. Expected: `"No layout problems."` per Phase 25 VAL-25-15 carry-forward.

    Call `mcp__pencil__snapshot_layout({ rootId: "<faqPageId>", problemsOnly: true })` — per-frame. Document any text-clipping false-positives (Phase 24/25 quirk — text nodes inside auto-layout frames sometimes report clipping but visually render correctly).

    Call `mcp__pencil__get_screenshot({ nodeId: "<faqPageId>" })` to render the full FAQ page inline. Inspect:
    - Does any content overflow the 1800px height estimate from Task 1?
    - Does any text node appear visually clipped (vs the false-positive from snapshot_layout)?

    If page frame overflows:
    - Call `mcp__pencil__batch_design` with an Update op: `{ id: "<faqPageId>", set: { height: "fit_content" } }`. Document the Update.
    - Re-run `get_screenshot` to confirm no overflow.

    Record snapshot_layout outcomes + any height adjustment + any text-clipping quirks in 26-01-SUMMARY.md.
  </action>
  <verify>
    `snapshot_layout({ maxDepth: 0 })` returns "No layout problems." at document level. `get_screenshot(faqPageId)` renders the full page without overflow.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
    - Per-frame snapshot was captured; any text-clipping quirks documented per Phase 24/25 precedent (NOT a failure — known false-positive).
    - `get_screenshot(<faqPageId>)` produced an inline render.
    - If page-frame height was Updated to `fit_content`, the Update was logged in 26-01-SUMMARY.md with before/after dimensions.
  </acceptance_criteria>
  <done>
    Layout-clean; ready for user calibration gate.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 6: Calibration spot-check user gate (per-section token-usage verification)</name>
  <files>None (presentation-only — uses Task 5's inline screenshot)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-62 (joel-only branch = token-usage check vs `_Tokens & Foundations` RpGbe) + D-63 (joel-only EXACT/APPROXIMATE/STUB semantics) + D-64 (filename identifier, NOT disk write) + D-65 (single AskUserQuestion at plan close presenting screenshot + token list) + D-83 (per-section fidelity labels)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Focus 4 (`get_screenshot` inline-only, D-64 filenames are description identifiers) + § Focus 6 (AskUserQuestion format) + § Example 4 (inline calibration screenshot pattern)
  </read_first>
  <action>
    Render TWO inline screenshots for the calibration gate:
    - `mcp__pencil__get_screenshot({ nodeId: "<qaListId>" })` — Q+A list section
    - `mcp__pencil__get_screenshot({ nodeId: "<sectionCtaRefId>" })` OR re-render with `<faqPageId>` cropped — Section/CTA instance section

    Present AskUserQuestion with the following structure (per RESEARCH § Example 4 + D-65):

    Question: "Calibration spot-check — FAQ page (joel-only-no-crito-ref branch per D-62). Does this composition use the declared tokens correctly?"

    Description (markdown — both screenshots rendered inline above the description via Pencil MCP image-content protocol):

    ```
    ## Calibration Spot-Check — FAQ Page (Joel-Only Branch)

    **Inline renders shown above:** Q+A list section + Section/CTA instance

    ### Section 1: Q+A List (5 verbatim pairs from src/pages/faq.astro)
    Fidelity label proposal: **EXACT** (real content + correct token usage per D-83)

    **Semantic tokens consumed (D-65 token-usage check vs `_Tokens & Foundations` RpGbe):**
    - Q items: `type-semantic-heading-2-family` (Plus Jakarta Sans) + `-size` ({N from 26-00 Task 1}px) + `-weight` (700) + `-lh` (1.4)
    - A items: `type-semantic-prose-paragraph-family` (Inter) + `-size` (16px) + `-weight` (400) + `-lh` (1.625)
    - Q fill: `color-semantic-text-primary` #141f39ff
    - A fill: `color-semantic-text-secondary` #52525bff
    - Section padding-y: literal [64, 0] bound to `space-semantic-section-y` rhythm
    - Q+A pair gap: 32 bound to `space-semantic-stack-lg`; Q→A gap: 12 bound to `space-semantic-stack-xs`

    ### Section 2: Section/CTA instance ("Still have questions?")
    Fidelity label proposal: **STUB** (microcopy placeholder per D-82, layout EXACT)

    **Semantic tokens consumed (same Q+A typography references; Section/CTA inherits Plan 26-00 build):**
    - Headline: heading-2-* tokens (same as Q above)
    - Body: prose-paragraph-* tokens (same as A above)
    - Actions: Primitive/Button (M7eUr) instance with STUB label "Get in touch"
    - CTA section fill: #f2f2f7ff (color-semantic-bg-surface)

    ### Section 3 + 4: Section/Header + Section/Footer instances
    Fidelity label proposal: **EXACT** (Phase 25 shipped; Crito-source labels stay per D-77)

    **Description identifiers (D-64 — inline only per OPEN-23-01, NOT disk-written):**
    - `26-faq-qa-section--token-usage.png`
    - `26-faq-cta-section--token-usage.png`

    **Calibration target (D-62 joel-only branch):**
    `_Tokens & Foundations` reference frame `RpGbe` at top of canvas shows the canonical visual register for these tokens. Are the Q+A and CTA compositions using them correctly?
    ```

    Options:
    - "APPROVE — both sections use tokens correctly; fidelity labels EXACT (Q+A) + STUB (CTA) per D-83 are correct"
    - "REVISE — token mismatch (free-text describe which section, which token, what's wrong)"
    - "GAP — composition reveals a missing token (raise OPEN-26-NN; specify the gap)"

    Wait for user response. Record outcome in 26-01-SUMMARY.md.

    If APPROVE: proceed to Task 7.
    If REVISE: address the specific feedback via additional `batch_design` Update ops, then re-present the gate. Loop until APPROVE.
    If GAP: add an OPEN-26-NN entry to PEN-INVENTORY § Open Flags — Phase 26 with the user's description; proceed to Task 7 with the gap documented (gap-declaration-not-gap-filling per VALID-03).
  </action>
  <verify>
    User has responded with APPROVE / REVISE / GAP; the response is recorded in 26-01-SUMMARY.md; any REVISE feedback has been resolved by additional Update ops + re-gate; any GAP has been logged as OPEN-26-NN.
  </verify>
  <acceptance_criteria>
    - Two `get_screenshot` calls produced inline renders (Q+A section + CTA section).
    - AskUserQuestion was presented with both screenshots + structured token-usage description per D-65 format.
    - User chose APPROVE or APPROVE-after-revision or APPROVE-with-GAP-logged.
    - Per-section fidelity labels per D-83 are recorded in 26-01-SUMMARY.md (Q+A = EXACT, CTA = STUB, Header = EXACT, Footer = EXACT, page-intro = EXACT or APPROXIMATE per user input).
    - Any OPEN-26-NN raised has a 1-line row in 26-01-SUMMARY.md ready for PEN-INVENTORY in Task 7.
  </acceptance_criteria>
  <done>
    User has spot-checked at least one section (likely both via the batched gate) — VALID-02 satisfied for joel-only branch.
  </done>
</task>

<task type="auto">
  <name>Task 7: PEN-INVENTORY extensions + 26-01-SUMMARY.md close</name>
  <files>
    - .planning/research/PEN-INVENTORY.md
    - .planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-88 (PEN-INVENTORY extension pattern for Phase 26)
    - `.planning/phases/25-section-compound-components/25-02-SUMMARY.md` § PEN-INVENTORY Updates — Frames Inventory row addition pattern
    - 26-01-SUMMARY.md scratch — all IDs created in Tasks 1-6 + Task 6 user-chosen fidelity labels + any OPEN-26-NN raised
  </read_first>
  <action>
    Edit `.planning/research/PEN-INVENTORY.md`:

    1. Frames Inventory table — add new row for `FAQ`:
       - name: "FAQ"
       - frame_id: `<faqPageId>` (Pencil-assigned from Task 1)
       - scope: `joel-only-no-crito-ref`
       - joel_page_map: `/faq`
       - child_section_count: 5 (Header ref + page-intro + qa-list + CTA ref + Footer ref)
       - status: `reconstructed-PHASE-26`
       - notes: "Fresh design in Crito vocab per D-58; 5 v1.3 Q+A pairs shipped verbatim from src/pages/faq.astro; per-section fidelity labels per D-83 (Q+A list = EXACT, CTA = STUB, Header/Footer/page-intro = EXACT or APPROXIMATE per Task 6 user input)."

    2. Extend Variant Evidence (Phase 26) sub-section (created in Plan 26-00) with rows for FAQ page-frame structural literal-to-semantic bindings:
       - FAQ page frame: fill #ffffffff ↔ color-semantic-bg-page; width 1440 ↔ literal page-rhythm; layout vertical + alignItems center + gap 0 + padding 0.
       - page-intro frame: width 1200 ↔ inner-content-width; gap 16 ↔ space-semantic-stack-sm; padding [64, 0] ↔ space-semantic-section-y; alignItems center; title text Plus Jakarta Sans 48/700/lh 1.4 ↔ heading-1 typography; fill #141f39ff ↔ color-semantic-text-primary.
       - qa-list frame: width 1200; gap 32 ↔ space-semantic-stack-lg; padding [64, 0] ↔ space-semantic-section-y; alignItems (center or stretch per Task 3 choice).
       - 5 qa-item frames: width inherits or set; gap 12 ↔ space-semantic-stack-xs; Q text heading-2 typography ↔ type-semantic-heading-2-*; A text prose-paragraph typography ↔ type-semantic-prose-paragraph-*; Q fill ↔ color-semantic-text-primary; A fill ↔ color-semantic-text-secondary.
       - Section/CTA ref descendants override: 3 STUB strings.
       - Section/Footer ref: no descendants override (D-77).

    3. If any OPEN-26-NN was raised in Task 6, add the row to "## Open Flags — Phase 26 (OPEN-26-NN)" section.

    4. If Task 6 confirmed Q+A typography renders correctly using `type-semantic-prose-paragraph-*`, remove the "provisional" flag from those tokens' rows per D-71 + PEN-INVENTORY Extensions table item.

    Then write `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` containing:
    - Pre-flight outcomes (Task 0)
    - All IDs created: `<faqPageId>`, `<faqXY>`, `<pageIntroId>`, `<pageIntroTitleTextId>`, `<pageIntroBodyTextId>`, `<qaListId>`, 5 `<qaItemId>`, 10 `<qaTextNodeIds>`, `<sectionCtaRefId>`, `<sectionFooterRefId>`
    - find_empty_space_on_canvas returned coords + directional anomaly notes (RESEARCH Open Question #4)
    - The 5 verbatim Q+A strings as shipped (for audit-trail completeness)
    - Section/CTA descendants override map used
    - snapshot_layout outcomes (document level + FAQ per-frame, with quirk notes)
    - Task 6 user response: APPROVE / REVISE / GAP + per-section fidelity labels per D-83
    - PEN-INVENTORY diff (Frames Inventory row + Variant Evidence rows + any OPEN-26-NN added)
    - Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/26-.../26-01-SUMMARY.md
    - VAL outcomes for VALID-01 (per-section labels) + VALID-02 (joel-only-branch calibration via D-62 token-usage check) + VALID-03 (any gaps declared as OPEN flags)
  </action>
  <verify>
    `cat .planning/research/PEN-INVENTORY.md | grep "FAQ"` returns the new Frames Inventory row. `cat .planning/research/PEN-INVENTORY.md | grep -A 5 "(Phase 26)"` shows Variant Evidence rows for FAQ. 26-01-SUMMARY.md exists and includes all listed sections.
  </verify>
  <acceptance_criteria>
    - PEN-INVENTORY Frames Inventory contains a new row for `FAQ` with scope `joel-only-no-crito-ref`, joel_page_map `/faq`, status `reconstructed-PHASE-26`, frame_id = `<faqPageId>`.
    - PEN-INVENTORY Variant Evidence (Phase 26) sub-section contains rows for FAQ page frame + page-intro + qa-list + 5 qa-items + Section/CTA + Section/Footer ref bindings.
    - Any OPEN-26-NN raised in Task 6 has a row under "## Open Flags — Phase 26".
    - If Task 6 confirmed prose-paragraph rendering, the "provisional" flag is removed from those token rows (D-71).
    - 26-01-SUMMARY.md exists at `.planning/phases/26-.../26-01-SUMMARY.md` and documents the listed sections.
  </acceptance_criteria>
  <done>
    Plan 26-01 complete. FAQ page frame shipped; calibration gated; audit trail extended. Plan 26-02 may proceed.
  </done>
</task>

</tasks>

<verification>
- Active editor remained `design/Crito.pen` throughout (D-87 pre-flight on every mutating task).
- FAQ frame is a direct child of the document root, NOT inside g9oRa5 / avgor / t67DU6 / RpGbe (Pitfall 3 compliance).
- The 5 Q+A pairs match `src/pages/faq.astro` lines 11-32 verbatim (apostrophes preserved).
- Section/CTA + Section/Footer instances render with appropriate STUB / EXACT fidelity per D-83.
- Plan-close calibration spot-check was presented via AskUserQuestion with inline screenshots + structured token-usage description (D-65 format).
- PEN-INVENTORY Frames Inventory + Variant Evidence sections extended per D-88.
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns clean at document level.
- 26-01-SUMMARY.md committed to git.
</verification>

<success_criteria>
- A top-level frame named `FAQ` exists with 5 vertical children in the order: Section/Header ref, page-intro frame, qa-list frame (with 5 qa-items), Section/CTA ref (with STUB descendants override), Section/Footer ref.
- `batch_get(["<qaListId>"])` shows exactly 5 qa-items; each contains a Q text node (heading-2 typography) + an A text node (prose-paragraph typography).
- Q+A text contents match the verbatim source from `src/pages/faq.astro` (verified by content equality check).
- Plan-close calibration AskUserQuestion was presented with inline `get_screenshot` renders + token-usage description; user APPROVED (possibly after REVISE iteration or with OPEN-26-NN gap logged).
- PEN-INVENTORY.md Frames Inventory row for FAQ exists; Variant Evidence Phase 26 sub-section contains FAQ-related rows.
- VALID-01 satisfied: per-section fidelity labels recorded in 26-01-SUMMARY.md.
- VALID-02 satisfied: joel-only-branch calibration artifact (inline screenshots + description identifier per D-64) was presented at the AskUserQuestion gate.
- VALID-03 satisfied: any gap declared as OPEN-26-NN (gap declaration, not gap filling).
- PAGE-04 satisfied: FAQ frame reconstructed as token-driven composition.
- PAGE-09 satisfied: desktop-only (1440 width, no mobile breakpoint).
</success_criteria>

<output>
After completion, create `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` containing:
- Pre-flight outcomes (Task 0 — token count + Plan 26-00 ID intactness)
- Task 1 placement: `<faqPageId>`, returned `{x, y}` from `find_empty_space_on_canvas`, any directional anomaly noted (for CALIBRATION-PROTOCOL.md feedback to Plan 26-03)
- Task 2 IDs: `<pageIntroId>`, `<pageIntroTitleTextId>`, optional `<pageIntroBodyTextId>`, Section/Header ref id
- Task 3 IDs: `<qaListId>`, 5 `<qaItem{1..5}Id>`, 10 `<qaQ{1..5}TextId>` + `<qaA{1..5}TextId>`; full 5 Q+A verbatim strings for audit
- Task 4 IDs: `<sectionCtaRefId>` + descendants override map used; `<sectionFooterRefId>`
- Task 5 snapshot_layout outcomes (document + FAQ per-frame), any fit_content Update applied, text-clipping quirks documented
- Task 6 user response: APPROVE / REVISE / GAP; per-section fidelity labels per D-83 (Q+A = EXACT, CTA = STUB, Header/Footer = EXACT, page-intro = EXACT/APPROXIMATE per user input); any OPEN-26-NN raised
- Task 7 PEN-INVENTORY edits diff (Frames Inventory FAQ row + Variant Evidence Phase 26 rows + OPEN-26-NN if any + "provisional" flag removed from prose-paragraph if confirmed)
- VAL outcomes for VALID-01, VALID-02, VALID-03, PAGE-04, PAGE-09
- Files Modified: design/Crito.pen, .planning/research/PEN-INVENTORY.md, .planning/phases/26-.../26-01-SUMMARY.md
</output>
