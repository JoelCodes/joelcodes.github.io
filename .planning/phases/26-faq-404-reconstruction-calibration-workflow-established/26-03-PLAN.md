---
phase: 26-faq-404-reconstruction-calibration-workflow-established
plan: 03
type: execute
wave: 1
depends_on:
  - 26-00
  - 26-01
  - 26-02
files_modified:
  - .planning/research/CALIBRATION-PROTOCOL.md
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - PAGE-09
  - PAGE-11
  - VALID-01
  - VALID-02
  - VALID-03

must_haves:
  truths:
    - "A new file `.planning/research/CALIBRATION-PROTOCOL.md` exists, written from the ACTUAL workflow that Plans 26-01 and 26-02 ran (codify-what-worked per D-67)."
    - "The protocol opens with a branch decision tree (crito-source-present vs joel-only-no-crito-ref) per D-66 + RESEARCH § Source-Branch Decision Tree."
    - "The protocol contains TWO parallel mini-protocols (one per branch) per D-66; each lists Pencil MCP call sequence, pairing target, naming convention, user-gate format."
    - "The protocol defines EXACT / APPROXIMATE / STUB semantics for BOTH branches per D-63; the joel-only definitions match what Plans 26-01 + 26-02 actually applied (per-section labels per D-83)."
    - "The protocol includes an OPEN-flag template (reusable across consumers per D-09 + D-68)."
    - "The protocol explicitly states PAGE-11 applies ONLY to crito-source branch (joel-only branch INERT per D-68 + RESEARCH § Focus 5); cross-references PEN-INVENTORY 404 row PAGE-11 INERT note from Plan 26-02."
    - "The protocol explicitly states the desktop-only constraint per PAGE-09 + D-68."
    - "The protocol documents the inline-screenshot substitution per RESEARCH § Pitfall 1 + § Focus 4 — `get_screenshot` cannot write to disk; D-64 filenames are description identifiers for the AskUserQuestion gate, NOT disk-write paths. Mirrors OPEN-23-01 substitution at Phase 23 D-19."
    - "The per-page phase consumer map is included (Phase 26 = both joel-only; Phase 27 = mixed; Phase 28 = mixed; Phase 29 = mixed; Phase 30 = joel-only; Phase 31 = crito-source) per RESEARCH § Source-Branch Decision Tree."
    - "`.planning/research/PEN-INVENTORY.md` has a new short `## Calibration Protocol` anchor (1-2 sentences) cross-referencing CALIBRATION-PROTOCOL.md per D-69."
    - "No Pencil mutations in this plan — it is a markdown-doc deliverable per D-86 (autonomous: true, no user gate)."
  artifacts:
    - path: ".planning/research/CALIBRATION-PROTOCOL.md"
      provides: "Unified calibration workflow doc with branch matrix + per-step scripts + label definitions + OPEN-flag template + PAGE-11 rule + PAGE-09 constraint + per-page phase consumer map. Inherited by Phases 27/28/29/30/31."
      min_lines: 200
      contains: "Branch matrix, EXACT, APPROXIMATE, STUB, joel-only, crito-source, PAGE-11, PAGE-09, OPEN-flag template, get_screenshot, AskUserQuestion"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "New `## Calibration Protocol` anchor section cross-referencing CALIBRATION-PROTOCOL.md per D-69."
  key_links:
    - from: ".planning/research/CALIBRATION-PROTOCOL.md"
      to: "Phase 27, 28, 29, 30, 31 per-page plans"
      via: "Definition-of-done framework inherited by all subsequent per-page phases"
      pattern: "inherited.*Phase 27"
    - from: ".planning/research/PEN-INVENTORY.md `## Calibration Protocol` anchor"
      to: ".planning/research/CALIBRATION-PROTOCOL.md"
      via: "1-2 sentence cross-reference"
      pattern: "see.*CALIBRATION-PROTOCOL"
---

<objective>
Write `.planning/research/CALIBRATION-PROTOCOL.md` — a unified calibration workflow doc with a branch matrix (crito-source-present vs joel-only-no-crito-ref) and per-branch scripts — codifying what Plans 26-01 (FAQ joel-only) and 26-02 (404 joel-only) actually did. Every subsequent per-page phase (27 Thank-you+Contact, 28 Blog, 29 Projects, 30 Design system, 31 Homepage) inherits this doc as its definition-of-done framework.

**Purpose:** Make the calibration-workflow shape reusable. The doc converts the in-context decisions D-58 through D-83 (CONTEXT.md) into an actionable, executable protocol future phases can apply mechanically. Codify-what-worked per D-67 — protocol reflects practice, not theory.

**Output:** A markdown protocol doc at `.planning/research/CALIBRATION-PROTOCOL.md` + a short `## Calibration Protocol` cross-reference anchor in PEN-INVENTORY.md per D-69. No Pencil mutations. No user gate at plan close (D-86 — agent-deterministic markdown deliverable).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md
@.planning/research/PEN-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight (read-only confirmation — no Pencil mutations this plan)</name>
  <files>None (read-only)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-66 (one unified doc) + D-67 (codify-what-worked) + D-68 (required contents) + D-69 (location + anchor) + D-86 (no user gate, agent-deterministic) + D-87 (pre-flight even for read-only is a habit)
    - 26-01-SUMMARY.md + 26-02-SUMMARY.md — actual workflow executed; user-chosen fidelity labels per D-83; any OPEN-26-NN raised; PAGE-11 INERT note from Plan 26-02
  </read_first>
  <action>
    Call `mcp__pencil__get_editor_state({ include_schema: false })` per D-87 (habit). Assert active editor ends with `design/Crito.pen`. Halt on mismatch.

    Read 26-01-SUMMARY.md + 26-02-SUMMARY.md to confirm:
    - Plan 26-01 FAQ user APPROVED (or APPROVED-with-revisions / APPROVED-with-GAP) — per-section fidelity labels per D-83 documented
    - Plan 26-02 404 user APPROVED — per-section fidelity labels per D-83 documented
    - PAGE-11 INERT note recorded in PEN-INVENTORY by Plan 26-02 Task 6
    - Any OPEN-26-NN flags raised in either plan
    - find_empty_space_on_canvas directional notes (if any anomaly per RESEARCH Open Question #4)

    Record the codify-what-worked input set in 26-03-SUMMARY.md scratch.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Plan 26-01 + 26-02 user-approval state recorded. Per-section fidelity labels available. PAGE-11 INERT note in PEN-INVENTORY. Codify inputs ready."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - 26-01-SUMMARY + 26-02-SUMMARY confirm user-approval state and per-section fidelity labels.
    - PAGE-11 INERT note exists in PEN-INVENTORY (from Plan 26-02).
    - No mutations issued.
  </acceptance_criteria>
  <done>
    Codify inputs assembled; Task 1 can draft the protocol from actuals.
  </done>
</task>

<task type="auto">
  <name>Task 1: Draft `.planning/research/CALIBRATION-PROTOCOL.md` from Plan 26-01/02 actuals</name>
  <files>.planning/research/CALIBRATION-PROTOCOL.md (new file)</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-66, D-67, D-68 (required contents)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-RESEARCH.md` § Source-Branch Decision Tree (pre-derived branch logic + per-page consumer map) + § Focus 4 (inline-screenshot substitution) + § Focus 5 (PAGE-11 INERT carve-out) + § Focus 6 (AskUserQuestion ergonomics) + § Pitfall 1 (filename-as-description-identifier)
    - 26-01-SUMMARY.md + 26-02-SUMMARY.md — actual per-task flow ran in production
    - `.planning/research/PEN-INVENTORY.md` — frame scope rows referenced by the branch decision tree (lookup mechanism)
  </read_first>
  <action>
    Create `.planning/research/CALIBRATION-PROTOCOL.md`. Structure (per D-68 required contents + RESEARCH § Source-Branch Decision Tree):

    **Top:** YAML frontmatter (version: 1.0; established: Phase 26; inherited-by: Phases 27/28/29/30/31).

    **Section 1: Purpose + Scope**
    - One paragraph: "Codifies the per-section calibration workflow established in Phase 26 (FAQ + 404 reconstructions, both joel-only-no-crito-ref). Every per-page phase from 26 onward inherits this protocol as its definition-of-done framework. The protocol has two branches because Crito source coverage is non-uniform: some Joel pages have a Crito raster reference (crito-source branch); others are joel-only fresh-design (joel-only branch). Both branches deliver the same outcome — VALID-01 fidelity label + VALID-02 calibration artifact + VALID-03 gap declaration — via branch-appropriate mechanisms."

    **Section 2: Branch Decision Tree**
    Insert the decision tree from RESEARCH § Source-Branch Decision Tree verbatim (adapted to flowing prose where helpful):
    - Look up the page name in `.planning/research/PEN-INVENTORY.md` Frames Inventory.
    - If scope ∈ {IN-SCOPE, IN-SCOPE token-mining-only} AND a Crito raster exists in `design/images/` → **crito-source-present branch**.
    - If scope == `joel-only-no-crito-ref` → **joel-only branch**.
    - Edge case: "IN-SCOPE token-mining-only" with no editable Crito frame (e.g., flat raster `cl8tt` Contact for Phase 27) → crito-source branch with raster-only sub-case; PAGE-11 evaluated case-by-case.

    **Section 3: crito-source-present branch protocol**
    - VALID-02 redefinition: side-by-side `get_screenshot(<sectionId>)` + matching raster from `design/images/image-import-NN.{jpg,png}` (or referenced Crito frame export).
    - VALID-01 fidelity definitions (per D-63):
      - **EXACT:** pixel-faithful to the matching raster.
      - **APPROXIMATE:** visually close; declared gaps documented as OPEN flag.
      - **STUB:** placeholder; full reconstruction deferred.
    - PAGE-11: ACTIVE — remove/hide/lock raster after reconstruction verified.
    - Per-step script:
      1. `mcp__pencil__get_editor_state` pre-flight.
      2. `mcp__pencil__get_screenshot({ nodeId: "<reconstructedSectionId>" })` — inline render.
      3. Locate matching raster filename in `design/images/`.
      4. AskUserQuestion presenting both images + structured "matches?" question.
      5. Options: APPROVE / REVISE / GAP.
      6. On APPROVE: `mcp__pencil__batch_design` Update sets raster node `enabled: false` (hidden archival) OR removes it; PEN-INVENTORY Frames Inventory `status_counts` flat:N decremented.
    - Description identifier convention: `<phase>-<page>-<section>--side-by-side.png` (D-64).

    **Section 4: joel-only-no-crito-ref branch protocol** (the branch Plan 26-01 + 26-02 actually used)
    - VALID-02 redefinition (D-62): pairs reconstructed `get_screenshot(<sectionId>)` against `_Tokens & Foundations` reference frame (RpGbe) — "does the composition use the declared tokens correctly?" is the right question when no visual source exists.
    - VALID-01 fidelity definitions (per D-63 joel-only column):
      - **EXACT:** every property references a semantic token AND user has spot-checked.
      - **APPROXIMATE:** uses primitives directly OR has inferred values flagged OPEN.
      - **STUB:** layout placeholder, token-binding or content deferred.
    - PAGE-11: INERT — no raster exists to remove. Cross-reference PEN-INVENTORY Frames Inventory rows for `joel-only-no-crito-ref` scope (flat:0, partial:0, factored:0 per PEN-INVENTORY).
    - Per-step script (as actually run by Plans 26-01 + 26-02 — codify-what-worked per D-67):
      1. `mcp__pencil__get_editor_state` pre-flight (D-87).
      2. `mcp__pencil__find_empty_space_on_canvas({ width, height })` for new page frame placement.
      3. `mcp__pencil__batch_design` Insert page frame at document root (NOT inside library parents — Pitfall 3).
      4. `mcp__pencil__batch_design` Insert content children (Section refs + content frames + text nodes).
      5. `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` + per-frame snapshot at plan close (text-clipping quirk documented per Phase 24/25 precedent).
      6. `mcp__pencil__get_screenshot({ nodeId: "<sectionId>" })` for each section needing spot-check — INLINE render only, NOT disk-written (Pitfall 1 + Focus 4 — `get_screenshot` cannot write bytes; OPEN-23-01 confirmed).
      7. AskUserQuestion presenting screenshots + structured token-usage description (RESEARCH § Example 4 format).
      8. Options: APPROVE / REVISE / GAP. REVISE iterates via `batch_design` Update; GAP logs OPEN-26-NN (or phase-appropriate OPEN-XX-NN).
      9. PEN-INVENTORY extensions per D-88: Frames Inventory row + Variant Evidence rows + any OPEN flags.
    - Description identifier convention: `<phase>-<page>-<section>--token-usage.png` (D-64) — IDENTIFIER ONLY, not a disk path. This is the most-misread part of the protocol; document it explicitly per RESEARCH § Pitfall 1.

    **Section 5: VALID-01 Per-Section Labels (D-83 reading)**
    - Fidelity labels attach to SECTIONS, not whole pages.
    - Mixed-fidelity pages are honest: FAQ shipped Q+A=EXACT + CTA=STUB; 404 shipped message-LAYOUT=EXACT/TEXT=STUB + NavBack=EXACT.
    - The calibration AskUserQuestion proposes per-section labels; user confirms or adjusts.
    - Label words come from D-63 (branch-specific semantics) — do not invent new fidelity vocabulary.

    **Section 6: VALID-02 Calibration Artifact Substitution (RESEARCH § Pitfall 1 + § Focus 4)**
    - `mcp__pencil__get_screenshot` returns inline image content (MCP image-content protocol) — does NOT save to disk. OPEN-23-01 confirmed across Phase 23/24/25.
    - `mcp__pencil__export_nodes` is broken for `.pen` files (MCP error -32603). OPEN-23-01 substitution mechanism: structural JSON (`id-inventory.json`) for archival.
    - D-64 filenames (`26-faq-qa-section--token-usage.png`, etc.) are **DESCRIPTION IDENTIFIERS** for the AskUserQuestion gate, NOT writable paths.
    - `.planning/ui-reviews/v2.0/` directory may exist but Phase 26 contributes NO files to it; subsequent phases follow the same inline-only convention.
    - Archival path for milestone-close: Phase 32 may attempt `export_nodes` again (test if Pencil fix lands) OR substitute `id-inventory.json` snapshot per Phase 23 D-19 precedent.

    **Section 7: VALID-03 Gap Declaration Template (D-09 + D-68)**
    - Format per OPEN flag (matches Phase 23 OPEN flags structure):
      ```
      OPEN-{PHASE}-{NN}: {one-line gap description}
      Surfaced by: {plan-id, task-name}
      Affected artifact: {file-path or pen-node-id}
      Resolution path: {which future phase + why} OR {user decision needed}
      ```
    - Examples from Phase 26: OPEN-26-01 heading-2 interpolation-derived (if Plan 26-00 Option A); any token-mismatch GAP from Plan 26-01/02 calibration gates.
    - Re-pointing is allowed: OPEN-23-11 prose-link / prose-list / inline-code re-pointed from Phase 26 to Phase 28 per D-70 + D-73.

    **Section 8: Cross-Cutting Constraints**
    - **Desktop-only (PAGE-09):** All v2.0 page-frame reconstruction is at 1440 outer × inner-content-width 1200. Mobile breakpoint frames deferred to a later milestone. Repeated here for clarity per D-68.
    - **PAGE-11 applicability:** ACTIVE on crito-source branch ONLY. INERT on joel-only branch (no raster exists to remove). Cross-reference PEN-INVENTORY PAGE-11 INERT note added by Plan 26-02.
    - **Pre-flight active-editor (D-87):** Every Pencil-mutating call gets fresh `get_editor_state` assertion. Carry-forward from Phase 23 OPEN-23-14 → Phase 24 D-35 → Phase 25 D-54.
    - **PEN-INVENTORY extension pattern (D-88):** Every plan that mutates `.pen` adds Frames Inventory row + Variant Evidence rows + any OPEN flags.
    - **Single-file strategy (PROJECT.md):** Everything in `design/Crito.pen`. Page frames at document root; library components inside `_Components / *` parent frames.

    **Section 9: Per-Page Phase Consumer Map**
    Insert the per-page phase consumer map from RESEARCH § Source-Branch Decision Tree:
    - Phase 26: FAQ (joel-only) + 404 (joel-only) — BOTH joel-only branch (Plans 26-01 + 26-02).
    - Phase 27: Thank-you (joel-only) + Contact (crito-source 09_Contact `cl8tt` — flat raster sub-case) — MIXED branches.
    - Phase 28: Blog index (crito-source 07_Blog `DzqTm`) + Blog post (crito-source 08_Blog Details `w1m3x`) + Blog tag-page (joel-only) — MIXED.
    - Phase 29: Projects index (joel-only) + Project detail (joel-only) — BOTH joel-only. (04_About + 05_Service + 06_Service Details are token-mining-only — NOT reconstructed.)
    - Phase 30: Design system (joel-only) — joel-only.
    - Phase 31: Homepage (crito-source `ujMLJ` — partially editable nuance) — crito-source.

    **Section 10: Codify-What-Worked Lessons from Phase 26**
    Brief retrospective:
    - Section/CTA + Section/NavBack as additions to g9oRa5 — pattern for future per-page phases that surface new section component needs.
    - heading-2 source-derivation via AskUserQuestion gate (Plan 26-00 Task 1) — pattern for future token extensions where source is `.fig`-unreadable.
    - Plan-close calibration gate batches all per-section gates in one AskUserQuestion (Phase 25 carry-forward + Plan 26-01 + Plan 26-02 demonstrated) — single user gate at plan close, multiple sections in one description.
    - Find_empty_space_on_canvas directional notes (any anomaly from Plan 26-01/02 documented here for tool-behavior reference).
    - PAGE-11 INERT carve-out for joel-only branch is the cleanest single-line rule for the protocol.

    **Bottom:** "*Established: Phase 26 (2026-06-06)*. *Inherited by: Phases 27/28/29/30/31*. *Substitutions: per OPEN-23-01 inline-screenshot pattern.*"

    Save to `.planning/research/CALIBRATION-PROTOCOL.md`. Target ≥ 200 lines.
  </action>
  <verify>
    `wc -l .planning/research/CALIBRATION-PROTOCOL.md` returns ≥ 200 lines. `grep -E "EXACT|APPROXIMATE|STUB|joel-only|crito-source|PAGE-11|PAGE-09|OPEN-flag|get_screenshot|AskUserQuestion" .planning/research/CALIBRATION-PROTOCOL.md` returns ≥ 1 hit per token.
  </verify>
  <acceptance_criteria>
    - File `.planning/research/CALIBRATION-PROTOCOL.md` exists.
    - File has ≥ 200 lines (lower-bound on substance per must_haves).
    - File contains all 10 sections listed above.
    - File explicitly defines EXACT / APPROXIMATE / STUB for BOTH branches per D-63.
    - File contains the branch decision tree, the OPEN-flag template, the PAGE-11 carve-out, the PAGE-09 constraint, the inline-screenshot substitution per OPEN-23-01.
    - File contains the per-page phase consumer map.
    - File codifies the actual Plan 26-01/02 workflow (not theory) per D-67.
  </acceptance_criteria>
  <done>
    Protocol drafted from actuals; ready for PEN-INVENTORY cross-reference and plan close.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `## Calibration Protocol` cross-reference anchor to PEN-INVENTORY.md (D-69)</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md` D-69 (short cross-reference anchor)
    - `.planning/research/PEN-INVENTORY.md` — locate insertion point (probably after Open Flags section or as a top-level navigation anchor)
  </read_first>
  <action>
    Edit `.planning/research/PEN-INVENTORY.md` to add a short `## Calibration Protocol` section. Content (1-2 sentences per D-69):

    ```
    ## Calibration Protocol

    The per-section calibration workflow for every v2.0 per-page reconstruction phase (26 onward) is codified at `.planning/research/CALIBRATION-PROTOCOL.md`. It has two branches — `crito-source-present` and `joel-only-no-crito-ref` — selected via this PEN-INVENTORY's Frames Inventory `scope` column. Established by Phase 26 Plan 26-03; inherited by Phases 27/28/29/30/31. PAGE-11 (raster-removal) applies ONLY to the crito-source branch; the joel-only branch's PAGE-11 status is INERT (no raster exists to remove — see also the PAGE-11 INERT note from Plan 26-02).
    ```

    Insert this section in a logical location — recommend immediately before "## Open Flags" sections or immediately after the Frames Inventory table (so readers find the protocol referenced near the scope lookup that drives the branch decision).
  </action>
  <verify>
    `grep -A 3 "## Calibration Protocol" .planning/research/PEN-INVENTORY.md` returns the new section with the CALIBRATION-PROTOCOL.md cross-reference + the joel-only/crito-source branch note + PAGE-11 INERT note.
  </verify>
  <acceptance_criteria>
    - PEN-INVENTORY.md contains a new `## Calibration Protocol` section.
    - Section length is 1-2 sentences per D-69 (short anchor, not a full duplicate).
    - Section cross-references `.planning/research/CALIBRATION-PROTOCOL.md` by exact path.
    - Section names both branches (crito-source / joel-only) and the PAGE-11 carve-out.
    - Section is positioned logically (near Frames Inventory or Open Flags).
  </acceptance_criteria>
  <done>
    PEN-INVENTORY cross-reference in place; readers using PEN-INVENTORY as the single source of truth can find the protocol.
  </done>
</task>

<task type="auto">
  <name>Task 3: Plan close + Phase 26 close — 26-03-SUMMARY.md + ROADMAP plan-checkbox marks + Phase 26 close marker</name>
  <files>
    - .planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-03-SUMMARY.md
    - .planning/ROADMAP.md
  </files>
  <read_first>
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` § Phase 25 Close pattern (multi-plan phase close reference)
    - `.planning/ROADMAP.md` Phase 26 section (4 plan checkboxes from Plan 26-00 Task 5)
    - `.planning/research/CALIBRATION-PROTOCOL.md` (Task 1 output)
  </read_first>
  <action>
    Step 1 — Write `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-03-SUMMARY.md` containing:
    - Pre-flight outcome (Task 0)
    - CALIBRATION-PROTOCOL.md contents summary (10 sections; line count; key cross-references) per Task 1
    - PEN-INVENTORY `## Calibration Protocol` anchor diff per Task 2
    - Codify-what-worked retrospective (Section 10 of CALIBRATION-PROTOCOL.md) restated in-line
    - VAL outcomes for PAGE-09, PAGE-11, VALID-01, VALID-02, VALID-03 (all formally documented in CALIBRATION-PROTOCOL.md; Phase 26 satisfies them via the protocol + Plans 26-01/02 applications)
    - Files Modified: `.planning/research/CALIBRATION-PROTOCOL.md` (new), `.planning/research/PEN-INVENTORY.md` (anchor section added), `.planning/phases/26-.../26-03-SUMMARY.md` (new)

    Step 2 — Mark plan checkboxes in ROADMAP.md:
    Locate Phase 26 section. For each of the 4 plans (26-00, 26-01, 26-02, 26-03), change `- [ ]` to `- [x]`.

    Step 3 — Phase 26 close indicators in ROADMAP.md:
    Locate the Progress table row for "26. FAQ + 404 Reconstruction": update `Plans Complete` from `0/TBD` (or current) to `4/4`; update `Status` from `Not started` to `Complete`; update `Completed` to today's date (use the current execution date).

    Step 4 — Update Phase 26 success-criteria checkmarks in ROADMAP.md:
    Locate the "Phase 26: FAQ + 404 Reconstruction (Calibration Workflow Established)" Success Criteria. For each criterion that this phase's plans satisfied (criteria 1, 2, 5; criterion 3 is satisfied by inline-screenshot substitution; criterion 4 is INERT for joel-only branch per Plan 26-02 PAGE-11 note), update with a brief "(satisfied via Plan 26-NN)" annotation if the ROADMAP convention supports it; otherwise leave criteria as-is and let the SUMMARY documents carry the satisfaction record.

    Step 5 — No user gate at plan close per D-86 (Plan 26-03 is agent-deterministic markdown deliverable).
  </action>
  <verify>
    - 26-03-SUMMARY.md exists.
    - `grep "\[x\] 26-0" .planning/ROADMAP.md` returns 4 lines (all 4 plans marked done).
    - `grep -A 1 "26\. FAQ + 404 Reconstruction" .planning/ROADMAP.md` shows status `Complete` and Plans Complete `4/4`.
  </verify>
  <acceptance_criteria>
    - 26-03-SUMMARY.md exists at `.planning/phases/26-.../26-03-SUMMARY.md` with the listed sections.
    - ROADMAP.md Phase 26 plan checkboxes all marked `[x]`.
    - ROADMAP.md Progress table row for Phase 26 shows `4/4`, `Complete`, and execution date.
    - No mutations to `design/Crito.pen` in this plan (read-only via Task 0 pre-flight only).
  </acceptance_criteria>
  <done>
    Plan 26-03 complete. Phase 26 complete. CALIBRATION-PROTOCOL.md ready for Phase 27+ consumers.
  </done>
</task>

</tasks>

<verification>
- `.planning/research/CALIBRATION-PROTOCOL.md` exists with ≥ 200 lines covering all 10 sections specified.
- The protocol's branch matrix matches RESEARCH § Source-Branch Decision Tree.
- The protocol's joel-only branch reflects Plans 26-01 + 26-02 actual workflow (codify-what-worked per D-67), not theoretical.
- PAGE-11 INERT note (joel-only branch) and PAGE-09 desktop-only constraint are explicit.
- The inline-screenshot substitution per OPEN-23-01 is explicit (D-64 filenames are identifiers, not paths).
- `.planning/research/PEN-INVENTORY.md` has the `## Calibration Protocol` anchor section per D-69.
- ROADMAP.md Phase 26 marked complete with 4/4 plans done.
- 26-03-SUMMARY.md exists.
- No `design/Crito.pen` mutations in this plan (read-only Task 0 pre-flight only).
</verification>

<success_criteria>
- `.planning/research/CALIBRATION-PROTOCOL.md` exists and contains the branch decision tree, both branch protocols, label definitions, OPEN-flag template, PAGE-11 carve-out, PAGE-09 constraint, inline-screenshot substitution, per-page consumer map, and codify-what-worked lessons.
- `.planning/research/PEN-INVENTORY.md` has the short `## Calibration Protocol` cross-reference anchor.
- `.planning/ROADMAP.md` Phase 26 marked Complete (4/4 plans, current date).
- No user gate at plan close (D-86 — autonomous: true).
- PAGE-09, PAGE-11, VALID-01, VALID-02, VALID-03 all satisfied via the protocol + Plans 26-01/02 applications (Phase 26 complete).
</success_criteria>

<output>
After completion, create `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-03-SUMMARY.md` containing:
- Pre-flight outcome (Task 0)
- CALIBRATION-PROTOCOL.md contents summary (10 sections; line count; key cross-references)
- PEN-INVENTORY anchor section diff (Task 2)
- Codify-what-worked retrospective restated in-line
- ROADMAP.md plan checkboxes + Phase 26 close marks diff (Task 3)
- VAL outcomes for PAGE-09, PAGE-11, VALID-01, VALID-02, VALID-03
- Files Modified: `.planning/research/CALIBRATION-PROTOCOL.md` (new), `.planning/research/PEN-INVENTORY.md` (anchor added), `.planning/ROADMAP.md` (plan close marks), `.planning/phases/26-.../26-03-SUMMARY.md` (new)
- Phase 26 close summary: 4 plans complete; foundation token + 2 new sections + 2 new page frames + protocol doc all shipped; CALIBRATION-PROTOCOL.md is the durable definition-of-done framework Phase 27+ inherits.
</output>
