---
phase: 25-leaf-page-migrations-faq-thank-you-404
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/pages/thank-you.astro
  - src/pages/404.astro
  - tests/accessibility/v2-leaf.spec.ts
  - .github/workflows/deploy.yml
  - design/design-system.pen
autonomous: true
requirements:
  - LEAF-02
  - LEAF-03

must_haves:
  truths:
    - "Visiting /thank-you renders on src/layouts/v2/BaseLayout.astro with a centered <Card elevated={true}> containing CheckCircle2 icon (size 64, --color-accent), h1 'Thanks for reaching out!' (NOT uppercase per D-25-09/D-26-04), body paragraph mentioning '48 hours', a primary Button 'Skip the wait — book a call' linking to the Calendly URL with target='_blank' rel='noopener noreferrer', and a secondary 'Return to homepage' text link (D-25-07..12)"
    - "The Calendly URL is sourced from `import.meta.env.PUBLIC_CALENDLY_URL` with placeholder fallback 'https://calendly.com/joelshinness' (D-25-11) — same pattern as PUBLIC_N8N_WEBHOOK_URL in ContactSection.astro"
    - "Visiting /404 renders on src/layouts/v2/BaseLayout.astro with no banner chrome (D-25-17): a big '404' numeral in --font-display (text-8xl baseline, text-9xl on md+), a tagline h2 'This page wandered off.' in --color-text, a 2x2 destination grid of anchor-wrapped Cards (Home / Projects / Blog / FAQ in that order, D-25-15), and a Button variant='link' 'Or get in touch' with auto-rendered ArrowRight icon linking to /#contact (D-25-16)"
    - "/404 includes `<meta slot=\"head\" name=\"robots\" content=\"noindex\">` so the page is excluded from search index (D-25-18); title is 'Page not found' (bare, SEO.astro appends ' | Joel Shinness')"
    - "Each /404 destination card uses the anchor-wraps-Card pattern from src/components/v2/feature/BlogCard.astro: outer <a class=\"dest-card\"> carries href + focus ring + hover-lift; inner <Card> does NOT use interactive={true} (per RESEARCH OQ-4 resolution — avoid double-tabindex)"
    - "Zero v1 imports remain in src/pages/thank-you.astro and src/pages/404.astro (no src/components/ui/, src/components/layout/, src/layouts/BaseLayout.astro)"
    - "Zero dark: utilities, zero FOUC script, zero Google Fonts CDN <link> tags in either page (D-25-19, Phase 23 D-22)"
    - "No uppercase text-transform on any heading on /thank-you or /404 (Phase 26 D-26-04, D-25-09)"
    - ".github/workflows/deploy.yml build step includes `PUBLIC_CALENDLY_URL: ${{ secrets.PUBLIC_CALENDLY_URL }}` in the `env:` block alongside PUBLIC_N8N_WEBHOOK_URL (D-25-11)"
    - "tests/accessibility/v2-leaf.spec.ts contains additional /thank-you and /404 describe blocks (Categories B + C) and the full suite passes with zero axe-core violations"
    - "design/design-system.pen contains a /404 page frame AND an FAQ accordion-row variant (closed + open state), added via Pencil MCP per D-25-22"
  artifacts:
    - path: "src/pages/thank-you.astro"
      provides: "v2 thank-you page on BaseLayoutV2 with env-var Calendly URL"
      contains: "import BaseLayout from '../layouts/v2/BaseLayout.astro'"
      contains_also:
        - "import Card from '../components/v2/ui/Card.astro'"
        - "import CardBody from '../components/v2/ui/CardBody.astro'"
        - "import Button from '../components/v2/ui/Button.astro'"
        - "import { CheckCircle2 } from '@lucide/astro'"
        - "import.meta.env.PUBLIC_CALENDLY_URL"
        - "Thanks for reaching out"
        - "48 hours"
        - "Skip the wait"
        - "Return to homepage"
    - path: "src/pages/404.astro"
      provides: "v2 404 page with destination grid + noindex"
      contains: "import BaseLayout from '../layouts/v2/BaseLayout.astro'"
      contains_also:
        - "import Card from '../components/v2/ui/Card.astro'"
        - "import CardBody from '../components/v2/ui/CardBody.astro'"
        - "import Button from '../components/v2/ui/Button.astro'"
        - "noindex"
        - "404"
        - "wandered off"
        - "/projects"
        - "/blog"
        - "/faq"
        - "/#contact"
    - path: "tests/accessibility/v2-leaf.spec.ts"
      provides: "Complete a11y coverage for /faq + /thank-you + /404"
      contains: "test.describe('v2 /thank-you"
      contains_also:
        - "test.describe('v2 /404"
        - "page.goto('/thank-you')"
        - "page.goto('/404')"
    - path: ".github/workflows/deploy.yml"
      provides: "CI build env wired for Calendly URL"
      contains: "PUBLIC_CALENDLY_URL"
  key_links:
    - from: "src/pages/thank-you.astro"
      to: "PUBLIC_CALENDLY_URL env var"
      via: "const calendlyUrl = import.meta.env.PUBLIC_CALENDLY_URL || 'https://calendly.com/joelshinness'"
      pattern: "import\\.meta\\.env\\.PUBLIC_CALENDLY_URL"
    - from: "src/pages/thank-you.astro"
      to: "Calendly external destination"
      via: "<Button variant=\"primary\" size=\"lg\" href={calendlyUrl} target=\"_blank\" rel=\"noopener noreferrer\">"
      pattern: "href=\\{calendlyUrl\\}[\\s\\S]*target=\"_blank\""
    - from: "src/pages/404.astro"
      to: "<head> noindex meta"
      via: "<meta slot=\"head\" name=\"robots\" content=\"noindex\" />"
      pattern: "slot=\"head\"[\\s\\S]*name=\"robots\"[\\s\\S]*content=\"noindex\""
    - from: "src/pages/404.astro destination cards"
      to: "['/', '/projects', '/blog', '/faq']"
      via: "destinations array mapped to <a class=\"dest-card\" href={d.href}>"
      pattern: "dest-card"
    - from: "src/pages/404.astro contact CTA"
      to: "/#contact"
      via: "<Button variant=\"link\" href=\"/#contact\">"
      pattern: "variant=\"link\"[\\s\\S]*href=\"/#contact\""
    - from: ".github/workflows/deploy.yml build step env"
      to: "PUBLIC_CALENDLY_URL secret"
      via: "PUBLIC_CALENDLY_URL: ${{ secrets.PUBLIC_CALENDLY_URL }}"
      pattern: "PUBLIC_CALENDLY_URL: \\$\\{\\{ secrets\\.PUBLIC_CALENDLY_URL \\}\\}"
---

<objective>
Migrate `/thank-you` to `BaseLayoutV2` (rewrite in place) and create new `/404` page on `BaseLayoutV2`. Wire `PUBLIC_CALENDLY_URL` into `.github/workflows/deploy.yml`. Append /thank-you and /404 describe blocks (Categories B + C) to the test scaffold Plan 25-01 created. End-of-plan: extend `design/design-system.pen` via Pencil MCP with a /404 page frame and an FAQ accordion-row variant (D-25-22, end-of-phase consolidation).

Purpose: Delivers LEAF-02 (/thank-you) and LEAF-03 (/404). Closes the v2 leaf-page migration set. The env-var wiring for Calendly mirrors the proven `PUBLIC_N8N_WEBHOOK_URL` pattern and closes one of the three deployment-time Pending Todos in STATE.md (code side; actual secret value remains a Joel-action).

Output:
- `src/pages/thank-you.astro` (rewritten in place; ~50 lines)
- `src/pages/404.astro` (new file; ~70-90 lines)
- `tests/accessibility/v2-leaf.spec.ts` (extended with /thank-you + /404 describes; 9-13 total tests)
- `.github/workflows/deploy.yml` (one-line env addition)
- `design/design-system.pen` (Pencil MCP edits — /404 frame + FAQ accordion-row variant)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-CONTEXT.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-UI-SPEC.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-VALIDATION.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-01-SUMMARY.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /thank-you on BaseLayoutV2 with elevated Card + env-var Calendly</name>

  <read_first>
    - src/pages/thank-you.astro (v1 — source of truth for copy ('Thanks for reaching out!', '48 hours' paragraph, 'Skip the wait — book a call', 'Return to homepage'), CheckCircle2 icon import, min-h-[70vh] vertical-center layout; discard all v1 imports, dark: classes, uppercase, neobrutalist Card/Button variants)
    - src/components/homepage/ContactSection.astro (line 214 — env-var pattern: `const webhookURL = import.meta.env.PUBLIC_N8N_WEBHOOK_URL || 'https://...';`; re-key for PUBLIC_CALENDLY_URL)
    - src/components/v2/ui/Card.astro + CardBody.astro (Phase 24 — confirm `elevated` prop API, default padding behavior, class slot)
    - src/components/v2/ui/Button.astro (Phase 24 — confirm polymorphic `href` renders `<a>`, `variant="primary"`, `size="lg"`, `target` + `rel` attribute passthrough)
    - src/layouts/v2/BaseLayout.astro (confirm bare `title` prop is correct — SEO.astro auto-appends ' | Joel Shinness'; v1 thank-you passed pre-suffixed title which is the existing bug being fixed)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md §`src/pages/thank-you.astro` — REWRITE (full markup composition + anti-patterns-to-drop list)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md §Example 2 (verified Card + Button composition)
  </read_first>

  <files>src/pages/thank-you.astro</files>

  <action>
  Rewrite `src/pages/thank-you.astro` in place.

  Frontmatter imports (all v2 paths):
  - `BaseLayout` from `../layouts/v2/BaseLayout.astro`
  - `Card` from `../components/v2/ui/Card.astro`
  - `CardBody` from `../components/v2/ui/CardBody.astro`
  - `Button` from `../components/v2/ui/Button.astro`
  - `{ CheckCircle2 }` from `@lucide/astro`

  Frontmatter logic: `const calendlyUrl = import.meta.env.PUBLIC_CALENDLY_URL || 'https://calendly.com/joelshinness';`

  Page wrapper: `<BaseLayout title="Thanks!" description="Thank you for reaching out. I'll respond within 48 hours.">` — bare title (no `| Joel Shinness` suffix, fixing the v1 double-suffix bug per RESEARCH Pitfall 2).

  Body markup (single section, vertical-centered Card):
  ```
  <div class="min-h-[70vh] flex items-center justify-center container mx-auto px-md">
    <Card elevated={true} class="max-w-2xl w-full">
      <CardBody class="flex flex-col items-center text-center gap-md px-lg py-xl">
        <CheckCircle2 size={64} strokeWidth={2} class="text-accent" aria-hidden="true" />
        <h1 class="font-display font-bold text-display text-text">Thanks for reaching out!</h1>
        <p class="font-text text-body text-text max-w-md">I'll email you within 48 hours with next steps. Looking forward to learning more about your project!</p>
        <Button variant="primary" size="lg" href={calendlyUrl} target="_blank" rel="noopener noreferrer">Skip the wait — book a call</Button>
        <a href="/" class="font-text text-small text-text-muted underline underline-offset-2 hover:text-accent transition-colors">Return to homepage</a>
      </CardBody>
    </Card>
  </div>
  ```

  Note: the em-dash in "Skip the wait — book a call" is U+2014 (`—`), not a hyphen — matches v1 source exactly. Use `&mdash;` or the literal Unicode character as preferred; both render identically.

  Drop entirely from v1: `BaseLayout` v1 import, v1 `Card`/`Button` imports, `title="Thanks! | Joel Shinness"` pre-suffix, `Card variant="turquoise"`, `text-turquoise dark:text-turquoise-dark` icon classes, `font-heading ... uppercase tracking-tight text-text-light dark:text-text-dark` h1 classes, `font-body text-lg text-text-light dark:text-text-dark` body classes, `Button variant="turquoise"`, hardcoded Calendly URL, `text-sm text-text-muted-light dark:text-text-muted-dark hover:text-turquoise dark:hover:text-turquoise-dark` secondary link classes, the trailing `<style>{ .container { max-width: 1280px; } }` block. Do NOT add `uppercase` anywhere.

  No scoped `<style>` block needed — every visual rule comes from Tailwind v4 utilities backed by the v2 @theme tokens.
  </action>

  <verify>
  - `npm run astro check` exits 0
  - `npm run build` exits 0 and emits `dist/thank-you/index.html`
  - `grep -c "import BaseLayout from '../layouts/v2/BaseLayout.astro'" src/pages/thank-you.astro` returns 1
  - `grep -c "import Card from '../components/v2/ui/Card.astro'" src/pages/thank-you.astro` returns 1
  - `grep -c "import CardBody from '../components/v2/ui/CardBody.astro'" src/pages/thank-you.astro` returns 1
  - `grep -c "import Button from '../components/v2/ui/Button.astro'" src/pages/thank-you.astro` returns 1
  - `grep -c "import { CheckCircle2 } from '@lucide/astro'" src/pages/thank-you.astro` returns 1
  - `grep -c "import\\.meta\\.env\\.PUBLIC_CALENDLY_URL" src/pages/thank-you.astro` returns 1
  - `grep -c "dark:" src/pages/thank-you.astro` returns 0
  - `grep -c "uppercase" src/pages/thank-you.astro` returns 0
  - `grep -c "text-turquoise\|font-heading\|font-body\|text-text-light\|variant=\"turquoise\"" src/pages/thank-you.astro` returns 0
  - `grep -c "Thanks for reaching out" src/pages/thank-you.astro` returns 1
  - `grep -c "48 hours" src/pages/thank-you.astro` returns 1
  - `grep -c "Skip the wait" src/pages/thank-you.astro` returns 1
  - `grep -c "Return to homepage" src/pages/thank-you.astro` returns 1
  - `grep -c 'target="_blank"' src/pages/thank-you.astro` returns 1
  - `grep -c 'rel="noopener noreferrer"' src/pages/thank-you.astro` returns 1
  - `node tests/check-token-collision.cjs` exits 0
  - With no PUBLIC_CALENDLY_URL set, view-source of `dist/thank-you/index.html` contains `href="https://calendly.com/joelshinness"` on the primary Button anchor
  </verify>

  <acceptance_criteria>
  - src/pages/thank-you.astro imports BaseLayout, Card, CardBody, Button from v2 paths and CheckCircle2 from @lucide/astro (5 imports, exact paths)
  - src/pages/thank-you.astro frontmatter contains `const calendlyUrl = import.meta.env.PUBLIC_CALENDLY_URL || 'https://calendly.com/joelshinness';` (verbatim string match)
  - src/pages/thank-you.astro uses `<BaseLayout title="Thanks!"` (NOT `title="Thanks! | Joel Shinness"`)
  - src/pages/thank-you.astro renders exactly one `<Card elevated={true}` element (or `<Card elevated>` shorthand — both equivalent)
  - src/pages/thank-you.astro renders `<CheckCircle2 size={64}` with `class="text-accent"` and `aria-hidden="true"`
  - src/pages/thank-you.astro renders `<h1` containing literal text `Thanks for reaching out!` with NO `uppercase` class
  - src/pages/thank-you.astro renders the body paragraph containing `48 hours` and `Looking forward`
  - src/pages/thank-you.astro renders `<Button variant="primary" size="lg" href={calendlyUrl} target="_blank" rel="noopener noreferrer">` (attributes in any order — all 5 attributes present)
  - src/pages/thank-you.astro Button label contains `Skip the wait` and either `—` (U+2014) or `&mdash;`
  - src/pages/thank-you.astro renders `<a href="/"` with text `Return to homepage`
  - `grep -c "dark:" src/pages/thank-you.astro` returns 0
  - `grep -c "uppercase" src/pages/thank-you.astro` returns 0
  - `grep -c "max-width: 1280px" src/pages/thank-you.astro` returns 0 (v1 `<style>` block dropped)
  - `npm run build && test -f dist/thank-you/index.html` exits 0
  - `node tests/check-token-collision.cjs` exits 0
  </acceptance_criteria>

  <done>
  /thank-you renders on BaseLayoutV2 with an elevated centered Card containing the green CheckCircle2 icon, sentence-case headline, body copy, primary Calendly Button (env-var driven), and secondary home link. No double-suffix in `<title>`, no `uppercase`, no v1 imports, no `dark:` utilities. Build succeeds. Local dev with no PUBLIC_CALENDLY_URL set still produces a working placeholder Calendly link.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /404 page with hero numeral, 2x2 destination grid, and noindex meta</name>

  <read_first>
    - src/components/v2/feature/BlogCard.astro (Phase 26 — anchor-wraps-Card pattern: outer `<a class="...">` carries hover-lift + focus ring, inner `<Card>` does NOT use `interactive={true}`. Copy the CSS for `.dest-card` from lines 73-89 style block, adapted from `.blog-card`. THIS PATTERN OVERRIDES UI-SPEC's stated `interactive={true}` per RESEARCH OQ-4 resolution and 25-PATTERNS critical note)
    - src/pages/design-system.astro (line 48 — `<meta slot="head" name="robots" content="noindex, follow" />`; copy pattern, drop ', follow' since /404 should be fully noindex)
    - src/components/v2/ui/Card.astro + CardBody.astro (Phase 24 — confirm default padding; confirm `interactive` is OPTIONAL and defaults to false)
    - src/components/v2/ui/Button.astro (Phase 24 — confirm `variant="link"` defaults `iconRight` to `ArrowRight` (Phase 24 D-04); do NOT pass `iconRight` prop, let the default apply)
    - src/layouts/v2/BaseLayout.astro (confirm `<slot name="head" />` on line 29 accepts `<meta slot="head">` elements)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md §`src/pages/404.astro` — NEW (full markup composition including hero, grid, contact CTA)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md §Example 3 (complete /404 skeleton)
  </read_first>

  <files>src/pages/404.astro</files>

  <action>
  Create new file `src/pages/404.astro`.

  Frontmatter imports (all v2 paths):
  - `BaseLayout` from `../layouts/v2/BaseLayout.astro`
  - `Card` from `../components/v2/ui/Card.astro`
  - `CardBody` from `../components/v2/ui/CardBody.astro`
  - `Button` from `../components/v2/ui/Button.astro`

  No icon imports needed — `ArrowRight` is auto-rendered by `Button` `variant="link"` per Phase 24 D-04.

  Frontmatter data — declare destinations array in exact order Home / Projects / Blog / FAQ (D-25-15 + UI-SPEC Copywriting Contract):
  - `{ href: '/',         label: 'Home',     desc: 'Back to the start.' }`
  - `{ href: '/projects', label: 'Projects', desc: "See work I've shipped." }`
  - `{ href: '/blog',     label: 'Blog',     desc: 'Read recent posts.' }`
  - `{ href: '/faq',      label: 'FAQ',      desc: 'Common questions, answered.' }`

  Page wrapper: `<BaseLayout title="Page not found" description="The page you're looking for has wandered off.">` — bare title.

  First child inside BaseLayout: `<meta slot="head" name="robots" content="noindex" />` — note `content="noindex"` only, NOT `noindex, follow` (which design-system.astro uses); a true 404 should fully suppress indexing.

  Section 1 — Hero numeral + tagline (D-25-14, D-25-17 no banner chrome):
  ```
  <div class="container mx-auto px-md">
    <section class="mt-2xl text-center">
      <h1 class="font-display font-bold text-8xl md:text-9xl text-primary leading-none">404</h1>
      <h2 class="mt-md font-display font-bold text-h2 text-text">This page wandered off.</h2>
    </section>
  ```

  Section 2 — 2x2 destination grid (D-25-15; anchor-wraps-Card per BlogCard precedent, NOT interactive Card):
  ```
    <div class="mt-xl mx-auto max-w-4xl">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-md">
        {destinations.map(d => (
          <a href={d.href} class="dest-card" aria-label={`Go to ${d.label}`}>
            <Card>
              <CardBody>
                <h3 class="font-display font-bold text-h3 text-text">{d.label}</h3>
                <p class="font-text text-small text-text-muted mt-xs">{d.desc}</p>
              </CardBody>
            </Card>
          </a>
        ))}
      </div>
    </div>
  ```

  Section 3 — Soft 5th destination, contact CTA (D-25-16):
  ```
    <div class="mt-xl mb-2xl text-center">
      <Button variant="link" href="/#contact">Or get in touch</Button>
    </div>
  </div>
  ```

  Scoped `<style>` for `.dest-card` (adapted from BlogCard.astro lines 73-89 — outer anchor carries focus ring + hover-lift since inner Card is NOT interactive):
  ```
  <style>
    .dest-card {
      display: block;
      text-decoration: none;
      color: inherit;
      transition: transform 200ms ease;
      border-radius: 0.5rem; /* match Card rounded-lg so focus ring traces the right shape */
    }
    .dest-card:hover { transform: translateY(-2px); }
    .dest-card:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
    }
  </style>
  ```

  Do NOT pass `interactive={true}` to the inner Card — that would cause the double-tabindex issue flagged in RESEARCH OQ-4 and resolved by BlogCard's CR-02 fix.

  Do NOT add any banner chrome (no `.blog-hero`, no `.page-hero`, no breadcrumbs) — D-25-17 explicit. The numeral itself is the page chrome.
  </action>

  <verify>
  - `ls src/pages/404.astro` succeeds
  - `npm run astro check` exits 0
  - `npm run build` exits 0 and emits `dist/404.html` (NOT `dist/404/index.html` — Astro convention for 404)
  - `grep -c "import BaseLayout from '../layouts/v2/BaseLayout.astro'" src/pages/404.astro` returns 1
  - `grep -c "import Card from '../components/v2/ui/Card.astro'" src/pages/404.astro` returns 1
  - `grep -c 'name="robots".*content="noindex"' src/pages/404.astro` returns 1
  - `grep -c '"slot="head"' src/pages/404.astro` returns 1 (the noindex meta has slot="head")
  - `grep -c '"/projects"' src/pages/404.astro` returns 1
  - `grep -c '"/blog"' src/pages/404.astro` returns 1
  - `grep -c '"/faq"' src/pages/404.astro` returns 1
  - `grep -c '"/#contact"' src/pages/404.astro` returns 1
  - `grep -c "wandered off" src/pages/404.astro` returns 1
  - `grep -c "text-8xl" src/pages/404.astro` returns 1
  - `grep -c "interactive={true}\|interactive=true\|interactive " src/pages/404.astro` returns 0 (inner Card is NOT interactive)
  - `grep -c "dark:" src/pages/404.astro` returns 0
  - `grep -c "uppercase" src/pages/404.astro` returns 0
  - `grep -c "blog-hero\|page-hero\|breadcrumb" src/pages/404.astro` returns 0 (no banner chrome per D-25-17)
  - `grep -c "@lucide/astro" src/pages/404.astro` returns 0 (no icon imports — ArrowRight is rendered by Button link variant)
  - `node tests/check-token-collision.cjs` exits 0
  - `npm run preview` → `curl -s http://localhost:4321/404` returns HTML containing `404` and `wandered off` and `noindex`
  - Manual: `npm run preview` → visit `http://localhost:4321/404` directly → verify visually: huge 404 numeral, tagline, 4 cards in 2x2 grid on md+ (single column on mobile), link Button at bottom with arrow icon
  </verify>

  <acceptance_criteria>
  - src/pages/404.astro exists at the absolute path /Users/joel/Desktop/Claude-Demos/joel-shinness-website/src/pages/404.astro
  - src/pages/404.astro imports BaseLayout, Card, CardBody, Button from v2 paths (4 imports, exact paths)
  - src/pages/404.astro does NOT import anything from @lucide/astro
  - src/pages/404.astro declares a `destinations` array (or equivalent) with exactly 4 entries in order: '/', '/projects', '/blog', '/faq'
  - src/pages/404.astro uses `<BaseLayout title="Page not found"` (bare title)
  - src/pages/404.astro contains `<meta slot="head" name="robots" content="noindex" />` (exact attributes; quoted attribute order may vary but all three attrs present)
  - src/pages/404.astro renders an `<h1>` with literal text `404` and classes including `text-8xl` and `text-primary`
  - src/pages/404.astro renders an `<h2>` containing literal text `This page wandered off.`
  - src/pages/404.astro renders exactly 4 `<a class="dest-card"` elements (one per destination), each wrapping a `<Card>` (no `interactive` prop) containing a `<CardBody>` with `<h3>` label and `<p>` description
  - src/pages/404.astro renders exactly one `<Button variant="link" href="/#contact">` with literal label `Or get in touch`
  - src/pages/404.astro contains a `<style>` block with `.dest-card:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }`
  - src/pages/404.astro contains a `.dest-card:hover { transform: translateY(-2px); }` rule
  - `grep -c "dark:" src/pages/404.astro` returns 0
  - `grep -c "uppercase" src/pages/404.astro` returns 0
  - `grep -c "blog-hero\|page-hero" src/pages/404.astro` returns 0
  - `npm run build && test -f dist/404.html` exits 0
  - `node tests/check-token-collision.cjs` exits 0
  </acceptance_criteria>

  <done>
  /404 renders on BaseLayoutV2 with the typographic hero (huge 404 numeral + tagline), a 2x2 grid of 4 destination cards (Home / Projects / Blog / FAQ), and a link-variant contact CTA. noindex meta is in `<head>`. The page has no banner chrome (per D-25-17). Each destination card is keyboard-reachable (single tab stop per card via outer `<a>`, not double-tabbed). Build succeeds; `dist/404.html` is emitted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend v2-leaf.spec.ts with /thank-you + /404 tests, add PUBLIC_CALENDLY_URL to deploy.yml, and Pencil-mirror /404 + FAQ accordion variant</name>

  <read_first>
    - tests/accessibility/v2-leaf.spec.ts (Plan 25-01 deliverable — confirm wcagTags constant + existing /faq describe; append new describes WITHOUT modifying the /faq block)
    - tests/accessibility/v2-blog.spec.ts (Phase 26 — pattern reference for additional test types)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md §Example 4 (full /thank-you + /404 test scaffolds with all assertions)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-VALIDATION.md §Test Categories B + C (defines required assertions per route)
    - .github/workflows/deploy.yml (locate the build step env block — sibling of PUBLIC_N8N_WEBHOOK_URL on line 36; insert PUBLIC_CALENDLY_URL directly under it without disturbing other env keys)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-CONTEXT.md (D-25-22 — Pencil mirror scope: /404 frame + FAQ accordion-row variant; /thank-you reuses Card + Button already factored)
    - design/design-system.pen (DO NOT use Read/Grep — use Pencil MCP tools only; see PATTERNS §`design/design-system.pen` workflow: open_document → find_empty_space_on_canvas → batch_design → snapshot_layout)
  </read_first>

  <files>tests/accessibility/v2-leaf.spec.ts, .github/workflows/deploy.yml, design/design-system.pen</files>

  <action>
  **Sub-task 3A — Extend test spec.** Append two `test.describe` blocks to `tests/accessibility/v2-leaf.spec.ts` (after the existing /faq describe from Plan 25-01). Do NOT modify the /faq describe or the wcagTags constant.

  `test.describe('v2 /thank-you (Plan 25-02)', () => { ... })` — implement Category B from VALIDATION.md (minimum 4 tests):
  1. **B1 render + axe**: goto /thank-you → assert h1 contains 'Thanks for reaching out!' → AxeBuilder withTags(wcagTags).analyze() → violations equal []
  2. **B5 no-uppercase (D-25-09)**: goto /thank-you → read computed `text-transform` on h1 via `evaluate(el => getComputedStyle(el).textTransform)` → assert equals `'none'`
  3. **B3 Calendly CTA**: getByRole('link', { name: /Skip the wait/ }) → assert href matches `/calendly\.com/` → assert target === '_blank' → assert rel === 'noopener noreferrer'
  4. **B4 secondary link**: getByRole('link', { name: 'Return to homepage' }) → assert href === '/'

  `test.describe('v2 /404 (Plan 25-02)', () => { ... })` — implement Category C from VALIDATION.md (minimum 4 tests):
  1. **C1 render + axe**: goto /404 → assert h1 contains '404' → assert h2 contains 'wandered off' → AxeBuilder withTags(wcagTags).analyze() → violations equal []
  2. **C4 noindex**: goto /404 → assert `meta[name="robots"]` has `content` matching `/noindex/`
  3. **C2 destination grid**: goto /404 → locate `a.dest-card` → assert count === 4 → evaluate hrefs in order, assert array equals `['/', '/projects', '/blog', '/faq']`
  4. **C3 contact CTA**: goto /404 → getByRole('link', { name: /Or get in touch/ }) → assert href === '/#contact' → assert it contains at least 1 `<svg>` child (auto-rendered ArrowRight from Button link variant)

  Optional fifth test on /404: **C5 no banner chrome** — assert `page.locator('.blog-hero, .page-hero')` count is 0 (D-25-17).

  Use the exact scaffold patterns from RESEARCH §Example 4 — they have been pre-verified against the Phase 26 axe-core invocation contract.

  **Sub-task 3B — Add workflow env.** Edit `.github/workflows/deploy.yml`. Locate the `Build site` step `env:` block (sibling of `PUBLIC_N8N_WEBHOOK_URL` on line 36). Insert one new line directly under the existing line:
  ```
            PUBLIC_CALENDLY_URL: ${{ secrets.PUBLIC_CALENDLY_URL }}
  ```
  Preserve YAML indentation exactly (2 spaces deeper than `env:`, matching the existing `PUBLIC_N8N_WEBHOOK_URL` line). Do NOT modify any other workflow file (`pr-preview.yml` etc. — only `deploy.yml` has a build-step env block per RESEARCH).

  **Sub-task 3C — Pencil mirror (D-25-22).** Use Pencil MCP tools ONLY (the .pen file is encrypted; never use Read/Grep/Edit on it). Workflow:
  1. `pencil__open_document` with path `design/design-system.pen`
  2. `pencil__find_empty_space_on_canvas` to locate insertion points
  3. `pencil__batch_design` to add TWO design artifacts:
     - **/404 page frame**: page-sized frame showing the 404 numeral (display font, ~text-8xl scale), tagline "This page wandered off.", 2x2 grid of 4 cards (Home / Projects / Blog / FAQ with description sub-lines), and a "Or get in touch →" link-style button at the bottom. Use existing Pencil variables for tokens (--color-primary, --color-text, --color-text-muted, --color-surface, --color-accent, --font-display, --font-text, spacing scale).
     - **FAQ accordion-row variant**: a frame within the existing component-variants area (or a new sibling frame) showing one borderless `<details>` row in two states: closed (question + ChevronDown right-aligned) and open (question + ChevronDown rotated 180deg + answer body below). 1px `--color-border` bottom divider visible.
  4. `pencil__snapshot_layout` on the new frames to verify rendering
  5. `pencil__get_screenshot` (optional) to confirm visual fidelity

  Frame naming convention should follow whatever Phase 23/24/26 established (look at existing frames via `pencil__get_editor_state` first). If naming convention is unclear, name frames `Page / 404` and `Component / FAQ Accordion Row` to match a likely existing pattern.

  Do NOT introduce new Pencil variables — D-25-22 explicit that Phase 25 ships zero new tokens. Reuse existing variables only.

  This sub-task is end-of-phase consolidation per RESEARCH OQ-3 — running it last means the test pass on /404 + /thank-you can verify rendering parity with the design before the Pencil mirror is updated. If Pencil MCP is unavailable in the execution environment, log the omission in the SUMMARY.md and flag for manual completion; do not block the phase.
  </action>

  <verify>
  Sub-task 3A — test spec:
  - `grep -c "test.describe('v2 /thank-you" tests/accessibility/v2-leaf.spec.ts` returns 1
  - `grep -c "test.describe('v2 /404" tests/accessibility/v2-leaf.spec.ts` returns 1
  - `grep -c "page.goto('/thank-you')" tests/accessibility/v2-leaf.spec.ts` returns at least 4
  - `grep -c "page.goto('/404')" tests/accessibility/v2-leaf.spec.ts` returns at least 4
  - `grep -c "Skip the wait" tests/accessibility/v2-leaf.spec.ts` returns at least 1
  - `grep -c "wandered off" tests/accessibility/v2-leaf.spec.ts` returns at least 1
  - `grep -c "noindex" tests/accessibility/v2-leaf.spec.ts` returns at least 1
  - `grep -c "dest-card" tests/accessibility/v2-leaf.spec.ts` returns at least 1
  - `npm run build && npx playwright test tests/accessibility/v2-leaf.spec.ts --reporter=line` exits 0 (all tests across /faq /thank-you /404 pass)

  Sub-task 3B — workflow env:
  - `grep -c "PUBLIC_CALENDLY_URL: \\$\\{\\{ secrets\\.PUBLIC_CALENDLY_URL \\}\\}" .github/workflows/deploy.yml` returns 1
  - `grep -c "PUBLIC_N8N_WEBHOOK_URL: \\$\\{\\{ secrets\\.PUBLIC_N8N_WEBHOOK_URL \\}\\}" .github/workflows/deploy.yml` returns 1 (sibling preserved, not modified)
  - `git diff .github/workflows/deploy.yml` shows exactly one added line (the PUBLIC_CALENDLY_URL line)
  - `npx yaml-lint .github/workflows/deploy.yml` (if available) OR `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` exits 0 (valid YAML)

  Sub-task 3C — Pencil mirror:
  - `pencil__get_editor_state` on `design/design-system.pen` shows a `/404` (or similarly-named) page frame and an FAQ accordion-row variant frame that did not exist before
  - `pencil__snapshot_layout` on the new frames returns successful layout (no missing variables, no broken refs)
  - Manual via Pencil MCP `pencil__get_screenshot`: /404 frame visually matches the implemented `/404.astro` page (numeral + tagline + 4-card grid + link button); FAQ accordion-row matches the divider-list aesthetic shipped in Plan 25-01

  End-of-plan integration:
  - `npm run astro check` exits 0
  - `npm run build` exits 0 emitting `dist/faq/index.html`, `dist/thank-you/index.html`, `dist/404.html`
  - `node tests/check-token-collision.cjs` exits 0
  - `npx playwright test tests/accessibility/v2-leaf.spec.ts --reporter=line` exits 0 (full suite green)
  </verify>

  <acceptance_criteria>
  - tests/accessibility/v2-leaf.spec.ts contains exactly one `test.describe('v2 /thank-you` block AND exactly one `test.describe('v2 /404` block (in addition to the pre-existing /faq describe)
  - The /thank-you describe contains at least 4 tests covering B1 (render+axe), B3 (Calendly href+target+rel), B4 (return home link), B5 (no-uppercase)
  - The /404 describe contains at least 4 tests covering C1 (render+axe), C2 (4 destination hrefs in declared order), C3 (contact CTA href), C4 (noindex meta)
  - The /thank-you axe-core test asserts `results.violations.toEqual([])` with `wcagTags`
  - The /404 axe-core test asserts `results.violations.toEqual([])` with `wcagTags`
  - The /404 destination test asserts hrefs equal `['/', '/projects', '/blog', '/faq']` (exact array)
  - .github/workflows/deploy.yml contains the literal line `PUBLIC_CALENDLY_URL: ${{ secrets.PUBLIC_CALENDLY_URL }}` indented to match `PUBLIC_N8N_WEBHOOK_URL`
  - .github/workflows/deploy.yml is valid YAML (parseable)
  - design/design-system.pen contains a /404 page frame (verified via `pencil__get_editor_state` showing the new frame name) AND an FAQ accordion-row variant frame
  - The Pencil frames use existing variables only (no new variables introduced — verified via `pencil__get_variables` showing the variable count unchanged from before Sub-task 3C)
  - Full test command `npx playwright test tests/accessibility/v2-leaf.spec.ts` exits 0 with all tests green
  - If Pencil MCP is unavailable at execution time, the SUMMARY.md explicitly notes "Pencil mirror deferred — Pencil MCP unavailable" and the rest of the plan still passes
  </acceptance_criteria>

  <done>
  Full a11y suite passes across /faq, /thank-you, /404 (axe 0 violations on each route under WCAG 2.x AA). CI workflow has PUBLIC_CALENDLY_URL wired alongside PUBLIC_N8N_WEBHOOK_URL (value owned by Joel post-merge). Pencil source-of-truth design mirrors what shipped: /404 frame and FAQ accordion-row variant present in design/design-system.pen. All three Phase 25 routes build to dist/.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks (verifies BOTH Plan 25-01 + Plan 25-02 deliverables together — Phase 25 closes here):
1. `npm run astro check` exits 0
2. `npm run build` exits 0; `ls dist/faq/index.html dist/thank-you/index.html dist/404.html` all succeed
3. `node tests/check-token-collision.cjs` exits 0
4. `npx playwright test tests/accessibility/v2-leaf.spec.ts --reporter=line` exits 0 (12+ tests across /faq /thank-you /404, zero failures)
5. `grep -rln "dark:\|components/ui/\|components/layout/\|layouts/BaseLayout.astro" src/pages/faq.astro src/pages/thank-you.astro src/pages/404.astro` returns no matches (E1, E2 from VALIDATION)
6. `grep -c "PUBLIC_CALENDLY_URL" .github/workflows/deploy.yml` returns 1 (E6)
7. Manual: `npm run preview` → visit `http://localhost:4321/faq`, `/thank-you`, `/404` → all render light-mode with v2 chrome
8. Manual: paste `dist/faq/index.html` JSON-LD into <https://validator.schema.org/> → zero errors (LEAF-01 SEO criterion)
9. Manual: with no PUBLIC_CALENDLY_URL set, `/thank-you` primary Button href === `https://calendly.com/joelshinness` (D-25-11 fallback)
10. Manual via Pencil MCP: `design/design-system.pen` shows the /404 frame and FAQ accordion-row variant (D-25-22)
11. Lighthouse CI (deferred to CI run on PR): expect 90+ across Performance / Accessibility / Best Practices / SEO on all three routes (Phase success criterion #4)
</verification>

<success_criteria>
- LEAF-02 satisfied: /thank-you migrated to BaseLayoutV2 with elevated Card, env-var Calendly URL, no `uppercase`, no v1 imports
- LEAF-03 satisfied: /404 created on BaseLayoutV2 with hero numeral + 4-destination grid + contact CTA + noindex meta
- All three Phase 25 routes (/faq, /thank-you, /404) pass axe-core with zero WCAG 2.x AA violations (Phase success criterion #4)
- Production build emits dist/faq/index.html, dist/thank-you/index.html, dist/404.html
- PUBLIC_CALENDLY_URL wired in .github/workflows/deploy.yml; deployment-side value remains a Joel-action
- design/design-system.pen extended with /404 page frame + FAQ accordion-row variant (D-25-22)
- All `must_haves.truths` are true (cite D-25-07..18, D-25-19, D-25-22, D-26-04)
- All `must_haves.artifacts` exist and contain the specified strings
- All `must_haves.key_links` resolve via grep patterns
</success_criteria>

<output>
After completion, create `.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-02-SUMMARY.md` summarizing:
- Files modified/created (5 paths)
- Decisions exercised (D-25-07..18, D-25-19, D-25-22, fix-as-you-touch on the title double-suffix bug)
- Test coverage added (/thank-you + /404 categories B + C)
- Workflow env addition (PUBLIC_CALENDLY_URL)
- Pencil mirror status (completed via MCP, or deferred with reason)
- Any deviations: the inner Card on /404 destination grid is NOT `interactive={true}` (overrides UI-SPEC line 222-223 per RESEARCH OQ-4 and 25-PATTERNS critical note; BlogCard precedent applied)
- Phase 25 status: ALL 3 LEAF requirements (LEAF-01, LEAF-02, LEAF-03) complete

Also update `.planning/STATE.md` to mark Phase 25 complete and advance the current position to Phase 27 (per ROADMAP execution order, Phase 26 already shipped).
</output>
