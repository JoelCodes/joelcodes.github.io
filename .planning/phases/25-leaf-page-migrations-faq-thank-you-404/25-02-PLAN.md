---
phase: 25-leaf-page-migrations-faq-thank-you-404
plan: 02
type: execute
wave: 2
depends_on: [25-01]
requirements: [LEAF-02, LEAF-03]
files_modified:
  - src/pages/thank-you.astro
  - src/pages/404.astro
autonomous: true
gap_closure: false

must_haves:
  truths:
    - "User landing on /thank-you (after submitting the contact form) sees the v2 layout with a centered elevated Card containing a green MailCheck icon, an H1 'Thanks for reaching out!', a confirmation paragraph, a primary 'Skip the wait — book a call' button (Calendly link), and a secondary 'Return to homepage' link"
    - "Visiting any unmatched URL on the deployed site (or in `npm run dev`) shows the v2 404 page with H1 'Page not found', a body paragraph, and a primary 'Return home' button linking to /"
    - "/thank-you and /404 render in light mode only — no dark-mode flash, no theme toggle, no localStorage.theme references"
    - "The Calendly placeholder URL `https://calendly.com/joelshinness` is preserved verbatim on /thank-you (the real-URL swap is a separately tracked v1.3 todo and is OUT OF SCOPE)"
    - "The /404 page is excluded from search engines via <meta name=\"robots\" content=\"noindex, follow\"> in <head>"
    - "After `npm run build`, dist/404.html exists and is non-empty and contains the string 'Page not found' (Astro static-build auto-emits the 404 file for GitHub Pages serving)"
    - "axe-core scans of /thank-you and /404 both return zero violations across WCAG 2.x AA tag set (the previously-failing tests in tests/accessibility/v2-leaf-pages.spec.ts created by Plan 25-01 are now GREEN)"
    - "Both /thank-you and /404 contain zero v1 layout/primitive imports, zero dark: utilities, zero localStorage.theme references"
  artifacts:
    - path: "src/pages/thank-you.astro"
      provides: "Migrated thank-you confirmation page on BaseLayoutV2 (v1 turquoise Card replaced by v2 elevated Card + MailCheck icon)"
      contains:
        - "import BaseLayout from '../layouts/v2/BaseLayout.astro';"
        - "import Button from '../components/v2/ui/Button.astro';"
        - "import Card from '../components/v2/ui/Card.astro';"
        - "import CardBody from '../components/v2/ui/CardBody.astro';"
        - "import { MailCheck } from '@lucide/astro';"
        - "min-h-[70vh] flex items-center justify-center"
        - "elevated={true}"
        - "<MailCheck size={64} class=\"text-accent\""
        - "Thanks for reaching out!"
        - "https://calendly.com/joelshinness"
        - "Skip the wait — book a call"
        - "Return to homepage"
    - path: "src/pages/404.astro"
      provides: "New 404 page on BaseLayoutV2 (Astro static build auto-emits dist/404.html for GitHub Pages)"
      contains:
        - "import BaseLayout from '../layouts/v2/BaseLayout.astro';"
        - "import Button from '../components/v2/ui/Button.astro';"
        - "import Card from '../components/v2/ui/Card.astro';"
        - "import CardBody from '../components/v2/ui/CardBody.astro';"
        - "<meta slot=\"head\" name=\"robots\" content=\"noindex, follow\""
        - "min-h-[70vh] flex items-center justify-center"
        - "elevated={true}"
        - "Page not found"
        - "Return home"
        - "href=\"/\""
  key_links:
    - from: "src/pages/thank-you.astro"
      to: "src/layouts/v2/BaseLayout.astro"
      via: "import default"
      pattern: "import BaseLayout from '\\.\\./layouts/v2/BaseLayout\\.astro'"
    - from: "src/pages/thank-you.astro"
      to: "src/components/v2/ui/Card.astro + CardBody.astro + Button.astro"
      via: "import defaults + <Card elevated={true}><CardBody>...primary + link Button...</CardBody></Card> centered composition"
      pattern: "elevated=\\{true\\}"
    - from: "src/pages/thank-you.astro"
      to: "External Calendly URL"
      via: "<Button variant=\"primary\" size=\"lg\" href=\"https://calendly.com/joelshinness\">"
      pattern: "https://calendly\\.com/joelshinness"
    - from: "src/pages/404.astro"
      to: "src/layouts/v2/BaseLayout.astro"
      via: "import default + <meta slot=\"head\" name=\"robots\" content=\"noindex, follow\" /> head injection"
      pattern: "import BaseLayout from '\\.\\./layouts/v2/BaseLayout\\.astro'"
    - from: "src/pages/404.astro"
      to: "Astro static build → dist/404.html"
      via: "Astro 5 file-based routing auto-detects src/pages/404.astro and emits dist/404.html for GitHub Pages"
      pattern: "Page not found"
    - from: "tests/accessibility/v2-leaf-pages.spec.ts (created by Plan 25-01)"
      to: "/thank-you and /404 runtime DOM"
      via: "page.goto + AxeBuilder — the failing tests from 25-01 turn green"
      pattern: "page\\.goto\\('/thank-you'\\)"
---

<objective>
Migrate `src/pages/thank-you.astro` from v1 BaseLayout + v1 turquoise Card to BaseLayoutV2 + v2 elevated Card + Lucide MailCheck icon. Create new `src/pages/404.astro` on BaseLayoutV2 with `<meta name="robots" content="noindex, follow">` head injection — Astro auto-emits `dist/404.html` for GitHub Pages serving (no `astro.config.mjs` change needed). Both pages reuse the centered-Card visual family established by the FAQ CTA in Plan 25-01. The two previously-failing axe tests in `tests/accessibility/v2-leaf-pages.spec.ts` (created by Plan 25-01) turn GREEN as the contract gate for this plan.

Purpose: LEAF-02 and LEAF-03 close out the leaf-page migration phase. /thank-you completes the contact-form lead-gen funnel surface (preserving the Calendly placeholder URL for the separately tracked v1.3 swap todo); /404 establishes the recoverable-nav surface for unmatched routes. Both pages exercise the centered-Card visual family on truly minimal content surfaces, finishing the dual-layout coexistence validation before Phase 26 begins migrating content-heavy blog pages.

Output:
- Rewritten `src/pages/thank-you.astro` (v2 layout, v2 primitives, MailCheck icon at text-accent green, Calendly placeholder URL preserved verbatim)
- New `src/pages/404.astro` (v2 layout, noindex meta in head, no icon per D-12, single "Return home" Button per D-11)
- `npm run astro check` and `npm run build` both green
- `dist/404.html` exists, non-empty, contains "Page not found"
- All 4 tests in `tests/accessibility/v2-leaf-pages.spec.ts` green (the /thank-you and /404 axe tests turn green; /faq tests already green from Plan 25-01)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-CONTEXT.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-UI-SPEC.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-VALIDATION.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-01-PLAN.md
@src/pages/thank-you.astro
@src/pages/design-system.astro
@src/pages/faq.astro
@src/layouts/v2/BaseLayout.astro
@src/components/v2/ui/Button.astro
@src/components/v2/ui/Card.astro
@src/components/v2/ui/CardBody.astro
@src/styles/v2/global.css
@tests/accessibility/v2-leaf-pages.spec.ts
@astro.config.mjs
</context>

<tasks>

<task type="execute">
  <name>Task 1: Rewrite src/pages/thank-you.astro on BaseLayoutV2 with elevated Card + MailCheck icon (Calendly URL preserved verbatim)</name>
  <files>src/pages/thank-you.astro</files>
  <read_first>
    - src/pages/thank-you.astro (current v1 file — read all 58 lines to identify exactly what is being preserved (page title, description, H1 text, paragraph copy, Calendly URL, `min-h-[70vh] flex items-center justify-center` container) vs. replaced (v1 BaseLayout import, v1 Card with `variant="turquoise"`, v1 Button, CheckCircle2 icon, all `dark:` utilities, the inline `<style>` container max-width override at lines 54-58))
    - src/pages/faq.astro (the rewritten v2 FAQ from Plan 25-01 — reference for the exact import block shape, the v2 SEO prop pattern, and the CTA Card composition that this page mirrors at the top level)
    - src/pages/design-system.astro (canonical v2 Card composition reference — lines 178-191 for `<Card elevated={true}><CardBody>...</CardBody></Card>` shape)
    - src/layouts/v2/BaseLayout.astro (confirm SEO props flow + default slot wrapping)
    - src/components/v2/ui/Button.astro (confirm `variant="link"` auto-injects `ArrowRight` as `iconRight` at lines 32-35 — the secondary "Return to homepage" link uses this; pages do NOT import ArrowRight directly)
    - src/components/v2/ui/Card.astro (confirm `elevated` boolean adds `shadow-md`)
    - src/components/v2/ui/CardBody.astro (confirm `px-md py-md` baked-in padding — pages MUST NOT override)
    - src/styles/v2/global.css (confirm tokens used: `--color-text`, `--color-accent`, `--spacing-lg/xl`, `--max-width-md/2xl`, `--text-h1`, `--text-body`, `--font-display`, `--font-text`)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-UI-SPEC.md (Component Composition `/thank-you` block lines 234-258; Copywriting Contract lines 130-142 for the locked verbatim copy and Calendly URL)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-CONTEXT.md (decisions D-14, D-15, D-16, D-17, D-18 for the thank-you-specific contract)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md (Section `src/pages/thank-you.astro` — verbatim composition block)
  </read_first>
  <action>
    Rewrite `src/pages/thank-you.astro` from scratch on BaseLayoutV2 — overwrite the existing v1 file in place (the current 58-line file is replaced wholesale; nothing in it survives except the listed verbatim strings).

    File structure (top to bottom):

    1. Frontmatter `---` block with imports:
       - `import BaseLayout from '../layouts/v2/BaseLayout.astro';` (v2 layout — replaces the v1 `'../layouts/BaseLayout.astro'` import at current v1 line 1)
       - `import Button from '../components/v2/ui/Button.astro';` (v2 Button primitive — replaces v1 Button at current v1 line 3)
       - `import Card from '../components/v2/ui/Card.astro';` (v2 Card primitive — replaces v1 Card with `variant="turquoise"` at current v1 line 2)
       - `import CardBody from '../components/v2/ui/CardBody.astro';` (Card composition piece per Phase 24 D-05)
       - `import { MailCheck } from '@lucide/astro';` (D-15 — replaces v1 `CheckCircle2` import at current v1 line 4)

    2. Body — `<BaseLayout>` invocation with verbatim v1 SEO props (D-16 + UI-SPEC Copywriting):
       - `title="Thanks! | Joel Shinness"` (verbatim from current v1 line 9)
       - `description="Thank you for reaching out. I'll respond within 48 hours."` (verbatim from current v1 line 10)

    3. Outer container — verbatim from current v1 line 12 (D-14): `<div class="container mx-auto px-lg py-xl min-h-[70vh] flex items-center justify-center">`. Note: `min-h-[70vh]` is the ONE permitted arbitrary `[…]` value in this phase per UI-SPEC Spacing Scale Exceptions; do NOT introduce other arbitrary values.

    4. Centered Card (locked composition from UI-SPEC Component Composition `/thank-you` block):
       ```
       <Card elevated={true} class="max-w-2xl w-full text-center">
         <CardBody>
           <div class="flex flex-col items-center gap-md">
             <MailCheck size={64} class="text-accent" aria-hidden="true" />
             <h1 class="font-display text-h1 font-bold text-text">
               Thanks for reaching out!
             </h1>
             <p class="font-text text-body text-text max-w-md">
               I'll email you within 48 hours with next steps. Looking forward to learning more about your project!
             </p>
             <Button variant="primary" size="lg" href="https://calendly.com/joelshinness">
               Skip the wait — book a call
             </Button>
             <Button variant="link" size="sm" href="/">Return to homepage</Button>
           </div>
         </CardBody>
       </Card>
       ```
       Notes on each element:
       - `elevated={true}` adds `shadow-md` per Card.astro line 21 — D-14.
       - `max-w-2xl` (672px) = `--max-width-2xl` token (Phase 24 24-05) — matches the centered-Card moment family.
       - `w-full` ensures the Card fills the container width on mobile (small screens).
       - `text-center` centers the inner stack horizontally.
       - `<CardBody>` already has `px-md py-md` padding — DO NOT override via class.
       - `flex flex-col items-center gap-md` inner stack — `--spacing-md` (24px) gap between icon, H1, paragraph, primary Button, secondary link Button.
       - `<MailCheck size={64} class="text-accent" aria-hidden="true" />` — D-15 explicit. The `class="text-accent"` is the ONE permitted text-accent application on this page (icons are non-text per WCAG 1.4.3; Phase 24 24-03 STATE entry confirms text-accent fails 4.5:1 for text). `aria-hidden="true"` because the H1 carries the accessible name.
       - H1 text: `Thanks for reaching out!` — verbatim from current v1 line 22.
       - Paragraph: `I'll email you within 48 hours with next steps. Looking forward to learning more about your project!` — verbatim from current v1 lines 26-28. `max-w-md` (448px) = `--max-width-md` token; constrains line length for readability.
       - Primary Button: `<Button variant="primary" size="lg" href="https://calendly.com/joelshinness">Skip the wait — book a call</Button>`. The `href` is preserved VERBATIM from current v1 line 35 per D-16 — this is the Calendly placeholder URL; the real-URL swap is a separately tracked v1.3 STATE.md todo and MUST NOT be folded into this phase. Label uses an em-dash (`—`, U+2014) per UI-SPEC Copywriting (locked) — note the current v1 uses a hyphen-minus; the UI-SPEC em-dash is the authoritative version.
       - Secondary Button: `<Button variant="link" size="sm" href="/">Return to homepage</Button>` — D-14 RESOLVED to `variant="link"`. The link variant auto-injects `ArrowRight` as `iconRight` per Button.astro lines 32-35 (pages do NOT import ArrowRight). `size="sm"` keeps it visually subordinate to the primary Calendly Button.

    DELETE everything else from the v1 file:
    - `import BaseLayout from '../layouts/BaseLayout.astro';` (line 1) — replaced with v2 path.
    - `import Card from '../components/ui/Card.astro';` (line 2) — replaced with v2 import.
    - `import Button from '../components/ui/Button.astro';` (line 3) — replaced with v2 import.
    - `import { CheckCircle2 } from '@lucide/astro';` (line 4) — replaced with `MailCheck` per D-15.
    - `<Card variant="turquoise">` wrapper (line 14) — v2 has no turquoise variant; the success-state cue is now carried by the MailCheck icon per D-14, D-15.
    - `<CheckCircle2 size={64} strokeWidth={2} class="text-turquoise mb-6" />` (line 17) — replaced by MailCheck per D-15.
    - All `dark:` utilities (lines 16, 21, 26, 45 of v1) — light-mode invariant per Phase 23 D-08.
    - All `font-heading` / `font-body` / `text-text-light` legacy classes — replaced by `font-display` / `font-text` / `text-text`.
    - The inline `<style>.container { max-width: 1280px; }</style>` block (lines 54-58 of v1) — Tailwind v4's default `container` class handles width; no override needed and inline `<style>` blocks add unnecessary CSS.
    - Any reference to `text-turquoise` or v1 colors — v2 palette only.

    DO NOT add: any `dark:` utility, any `<script>` (no JSON-LD on this page — UI-SPEC SEO Contract row 3 confirms no special head-slot tags for /thank-you), any inline `<style>` block, any v1 import, any new arbitrary `[…]` value beyond `min-h-[70vh]`.

    DO NOT modify: src/layouts/v2/BaseLayout.astro, src/components/v2/ui/* (locked from Phase 23/24), src/styles/v2/global.css (zero new tokens this phase per UI-SPEC Cross-Cutting Contract), tests/accessibility/v2-leaf-pages.spec.ts (Plan 25-01 owns; do not edit even if you want to add tests — the spec is complete).

    After writing, run the grep gates from VALIDATION.md to self-verify:
    - `! grep -E 'dark:|localStorage\.theme|prefers-color-scheme|\.dark[^a-z]' src/pages/thank-you.astro` — zero matches.
    - `! grep -F "from '../layouts/BaseLayout.astro'" src/pages/thank-you.astro` — zero matches.
    - `! grep -E "from ['\"]\\.\\./components/ui/" src/pages/thank-you.astro` — zero matches.
    - `grep -F 'https://calendly.com/joelshinness' src/pages/thank-you.astro` returns exactly 1 match (D-16 — Calendly URL preservation gate D8 from VALIDATION.md).

    Run `npm run astro check` and `npm run build` — both must exit 0.

    With `npm run dev` running, run `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts -g "/thank-you"` — must pass (the previously-failing /thank-you axe test from Plan 25-01 turns GREEN).

    Commit with message `feat(25-02): migrate /thank-you to BaseLayoutV2 with elevated Card + MailCheck icon (LEAF-02)`.
  </action>
  <verify>
    `test -f src/pages/thank-you.astro` — file exists.
    `grep -F "import BaseLayout from '../layouts/v2/BaseLayout.astro'" src/pages/thank-you.astro` returns 1 match.
    `grep -F "import Button from '../components/v2/ui/Button.astro'" src/pages/thank-you.astro` returns 1 match.
    `grep -F "import Card from '../components/v2/ui/Card.astro'" src/pages/thank-you.astro` returns 1 match.
    `grep -F "import CardBody from '../components/v2/ui/CardBody.astro'" src/pages/thank-you.astro` returns 1 match.
    `grep -F "import { MailCheck } from '@lucide/astro'" src/pages/thank-you.astro` returns 1 match.
    `! grep -F "CheckCircle2" src/pages/thank-you.astro` — zero matches (v1 icon removed per D-15).
    `! grep -F "variant=\"turquoise\"" src/pages/thank-you.astro` — zero matches (v1 turquoise Card variant removed per D-14).
    `grep -F 'min-h-[70vh] flex items-center justify-center' src/pages/thank-you.astro` returns 1 match (container preserved per D-14).
    `grep -F 'elevated={true}' src/pages/thank-you.astro` returns 1 match (D-14).
    `grep -F 'class="text-accent"' src/pages/thank-you.astro` returns 1 match (MailCheck stroke per D-15 — the ONE permitted text-accent application on this page).
    `grep -F 'size={64}' src/pages/thank-you.astro` returns 1 match (MailCheck size per UI-SPEC Component Composition).
    `grep -F 'Thanks for reaching out!' src/pages/thank-you.astro` returns 1 match (H1 verbatim).
    `grep -F "I'll email you within 48 hours with next steps. Looking forward to learning more about your project!" src/pages/thank-you.astro` returns 1 match (paragraph verbatim).
    `grep -F 'https://calendly.com/joelshinness' src/pages/thank-you.astro` returns exactly 1 match (D-16 + VALIDATION D8 gate).
    `grep -F 'Skip the wait — book a call' src/pages/thank-you.astro` returns 1 match (em-dash from UI-SPEC, U+2014 — note: the verify command checks the em-dash version per the locked spec).
    `grep -F 'variant="link"' src/pages/thank-you.astro` returns 1 match (D-14 RESOLVED secondary link).
    `grep -F 'Return to homepage' src/pages/thank-you.astro` returns 1 match.
    `! grep -E 'dark:|localStorage\.theme|prefers-color-scheme|\.dark[^a-z]' src/pages/thank-you.astro` — zero matches (light-mode invariant).
    `! grep -E 'font-body|font-heading|text-text-light|text-turquoise' src/pages/thank-you.astro` — zero matches (no v1 token names).
    `! grep -F "from '../layouts/BaseLayout.astro'" src/pages/thank-you.astro` — zero matches (no v1 layout import per D-17).
    `! grep -E "from ['\"]\\.\\./components/ui/" src/pages/thank-you.astro` — zero matches (no v1 primitive imports per D-17).
    `! grep -F '<style>' src/pages/thank-you.astro` — zero matches (inline style block deleted).
    `! grep -F 'is:global' src/pages/thank-you.astro` — zero matches (Phase 24 D-19 carry-forward).
    `npm run astro check` exits 0.
    `npm run build` exits 0.
    With `npm run dev` running: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts -g "/thank-you"` exits 0 (axe test green).
    Manual visual sanity (optional): `npm run dev` → visit `http://localhost:4321/thank-you` → centered elevated Card with green MailCheck icon at 64px, H1 "Thanks for reaching out!", confirmation paragraph, primary green "Skip the wait — book a call" button, secondary "Return to homepage →" link below.
  </verify>
  <done>
    `src/pages/thank-you.astro` is a complete from-scratch rewrite on BaseLayoutV2. The MailCheck icon (text-accent, size 64) replaces the v1 CheckCircle2 (D-15). The v2 elevated Card replaces the v1 turquoise Card (D-14). The Calendly placeholder URL `https://calendly.com/joelshinness` is preserved verbatim — exactly 1 grep match (D-16, VALIDATION D8). The secondary "Return to homepage" Button uses `variant="link"` (D-14 RESOLVED). Zero v1 imports, zero dark-mode artifacts, zero inline `<style>` block (D-17, D-18). The /thank-you axe test in `tests/accessibility/v2-leaf-pages.spec.ts` (created by Plan 25-01) is now GREEN.
  </done>
</task>

<task type="execute">
  <name>Task 2: Create new src/pages/404.astro on BaseLayoutV2 with noindex meta + centered Card + Return home Button</name>
  <files>src/pages/404.astro</files>
  <read_first>
    - src/pages/thank-you.astro (the rewritten v2 thank-you from Task 1 above — primary composition analog; the /404 layout is identical minus the icon and minus the secondary link Button per D-12)
    - src/pages/design-system.astro (line 49 — verbatim source for the `<meta slot="head" name="robots" content="noindex, follow" />` pattern)
    - src/pages/faq.astro (the rewritten v2 FAQ from Plan 25-01 — reference for the BaseLayout invocation shape and Card composition)
    - src/layouts/v2/BaseLayout.astro (confirm line 29 `<slot name="head" />` accepts the noindex meta)
    - src/components/v2/ui/Button.astro (confirm `variant="primary" size="md"` renders the green-fill polymorphic anchor when `href` is present)
    - src/components/v2/ui/Card.astro + src/components/v2/ui/CardBody.astro (composition primitives — same as Task 1)
    - src/styles/v2/global.css (confirm tokens used — same as Task 1)
    - astro.config.mjs (confirm static-only build, no SSR adapter — verifies that `src/pages/404.astro` will auto-emit `dist/404.html` per D-09 + RESEARCH "Don't Hand-Roll" row 4)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-UI-SPEC.md (Component Composition `/404` block lines 272-306; Copywriting Contract lines 144-152 for the locked H1, body, Button label; Cross-Cutting Contract for invariants)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-CONTEXT.md (decisions D-09, D-10, D-11, D-12, D-13 for the 404-specific contract — pay attention to D-11's deliberate scope tightening: single "Return home" Button, NOT a list of key pages, despite roadmap success-criterion 3 wording)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md (Section `src/pages/404.astro` — verbatim composition block + Astro static-build behavior note)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md (Pitfall 4 — sitemap exclusion; Common Pitfalls section confirms the `<meta name="robots" content="noindex, follow">` is the canonical fix)
  </read_first>
  <action>
    Create the new file `src/pages/404.astro` (the file does NOT currently exist; this is a from-scratch creation, NOT a rewrite). Astro's static build (`npm run build`) auto-detects `src/pages/404.astro` and emits `dist/404.html` for GitHub Pages serving — D-09 explicit, no `astro.config.mjs` change needed.

    File structure (top to bottom):

    1. Frontmatter `---` block with imports — same shape as the rewritten /thank-you, MINUS the Lucide icon (D-12: "No icon"):
       - `import BaseLayout from '../layouts/v2/BaseLayout.astro';`
       - `import Button from '../components/v2/ui/Button.astro';`
       - `import Card from '../components/v2/ui/Card.astro';`
       - `import CardBody from '../components/v2/ui/CardBody.astro';`

    2. Body — `<BaseLayout>` invocation with locked SEO props (UI-SPEC Copywriting):
       - `title="Page not found | Joel Shinness"`
       - `description="The page you're looking for doesn't exist or has moved."`

    3. Inside `<BaseLayout>`, FIRST child = head-slot noindex meta (D-13). Verbatim pattern from `src/pages/design-system.astro` line 49:
       - `<meta slot="head" name="robots" content="noindex, follow" />`
       - This lands inside `<head>` via `BaseLayoutV2` line 29's `<slot name="head" />`. `noindex` prevents search-engine indexing; `follow` allows link-juice flow through the "Return home" Button.

    4. Outer container — same shape as the rewritten /thank-you (D-12 explicit: "Visual layout matches the thank-you pattern"): `<div class="container mx-auto px-lg py-xl min-h-[70vh] flex items-center justify-center">`. The `min-h-[70vh]` is the ONE permitted arbitrary value (UI-SPEC Spacing Scale Exceptions).

    5. Centered Card (locked composition from UI-SPEC Component Composition `/404` block):
       ```
       <Card elevated={true} class="max-w-2xl w-full text-center">
         <CardBody>
           <div class="flex flex-col items-center gap-md">
             <h1 class="font-display text-h1 font-bold text-text">Page not found</h1>
             <p class="font-text text-body text-text max-w-md">
               This page doesn't exist or has moved. Head back home to find what you're looking for.
             </p>
             <Button variant="primary" size="md" href="/">Return home</Button>
           </div>
         </CardBody>
       </Card>
       ```
       Notes:
       - `elevated={true}` (D-12) adds `shadow-md`.
       - `max-w-2xl w-full text-center` — same composition family as /thank-you and the FAQ CTA.
       - `flex flex-col items-center gap-md` — 24px stack between H1, paragraph, Button.
       - H1: `Page not found` — D-10, locked. NO "Error 404" prefix; NO apology; straightforward tone.
       - Paragraph: `This page doesn't exist or has moved. Head back home to find what you're looking for.` — UI-SPEC Copywriting line 150 (D-10 RESOLVED). `max-w-md` (448px) for line-length readability.
       - Button: `<Button variant="primary" size="md" href="/">Return home</Button>` — D-12 RESOLVED to `variant="primary"` (filled green), `size="md"` matches the FAQ CTA Button (consistency across the centered-Card family). `href="/"` returns to the homepage.
       - **NO secondary link list** — D-11 deliberate scope tightening of roadmap success-criterion 3 (which says "navigation links back to the homepage and key pages"). The roadmap wording is INTENTIONALLY NOT followed literally. The Header is always visible on every page including /404, providing nav to Blog / Projects / FAQ / Contact; a second redundant link list would add visual noise without adding wayfinding. **DO NOT add a secondary "Browse our Blog/Projects/FAQ" link list to satisfy the literal wording — this is locked per D-11 and confirmed in CONTEXT.md "Specifics" 3rd bullet.**
       - **NO icon** — D-12 explicit ("No icon. No background art. Card contains heading + body + Button only.").
       - **NO secondary link Button** — only /thank-you has the secondary "Return to homepage" link; /404 has only the primary Button.

    DO NOT add: any `dark:` utility, any `<script>` block (no JSON-LD on /404 — UI-SPEC SEO Contract row 3), any inline `<style>` block, any v1 import, any icon import (D-12), any secondary link Button (D-11), any list of key pages (D-11), any new arbitrary `[…]` value beyond `min-h-[70vh]`.

    DO NOT modify: src/layouts/v2/BaseLayout.astro, src/components/v2/ui/* (locked from Phase 23/24), src/styles/v2/global.css (zero new tokens this phase), astro.config.mjs (Astro auto-detects /404 — no config change per D-09), tests/accessibility/v2-leaf-pages.spec.ts (Plan 25-01 owns; the /404 axe test is already in the spec waiting to be turned green by this task).

    After writing, run the grep gates and verifications:
    - `! grep -E 'dark:|localStorage\.theme|prefers-color-scheme|\.dark[^a-z]' src/pages/404.astro` — zero matches.
    - `! grep -E "from ['\"]\\.\\./components/ui/" src/pages/404.astro` — zero matches.
    - `! grep -F "from '../layouts/BaseLayout.astro'" src/pages/404.astro` — zero matches.
    - `! grep -E '@lucide/astro' src/pages/404.astro` — zero matches (D-12: no icon).

    Run `npm run astro check` and `npm run build` — both must exit 0.

    After build, verify static-404 emission (D7 from VALIDATION.md):
    - `test -s dist/404.html` — file exists and is non-empty.
    - `grep -q "Page not found" dist/404.html` — content marker present (verifies the v2 page content is in the emitted file, not a fallback shell).

    Belt-and-suspenders sitemap check (RESEARCH Pitfall 4 — Open Question 2):
    - After build, run: `if grep -F '/404' dist/sitemap-*.xml 2>/dev/null; then echo "WARN: /404 in sitemap — consider adding sitemap filter in astro.config.mjs as a follow-up"; else echo "OK: /404 not in sitemap"; fi`
    - This is informational only — if /404 IS in the sitemap, document it in the SUMMARY as a follow-up (do NOT block this plan; the noindex meta is the primary defense per D-13).

    With `npm run dev` running, run `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts -g "/404"` — must pass (the previously-failing /404 axe test from Plan 25-01 turns GREEN).

    Run the full leaf-pages spec to confirm all 4 tests are now green: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts` — exits 0.

    Commit with message `feat(25-02): create /404 on BaseLayoutV2 with centered Card + Return home Button + noindex meta (LEAF-03)`.
  </action>
  <verify>
    `test -f src/pages/404.astro` — file exists (NEW file).
    `grep -F "import BaseLayout from '../layouts/v2/BaseLayout.astro'" src/pages/404.astro` returns 1 match.
    `grep -F "import Button from '../components/v2/ui/Button.astro'" src/pages/404.astro` returns 1 match.
    `grep -F "import Card from '../components/v2/ui/Card.astro'" src/pages/404.astro` returns 1 match.
    `grep -F "import CardBody from '../components/v2/ui/CardBody.astro'" src/pages/404.astro` returns 1 match.
    `! grep -E '@lucide/astro' src/pages/404.astro` — zero matches (D-12 no icon).
    `grep -F '<meta slot="head" name="robots" content="noindex, follow"' src/pages/404.astro` returns 1 match (D-13).
    `grep -F 'min-h-[70vh] flex items-center justify-center' src/pages/404.astro` returns 1 match (centered-Card family per D-12).
    `grep -F 'elevated={true}' src/pages/404.astro` returns 1 match (D-12).
    `grep -F 'max-w-2xl w-full text-center' src/pages/404.astro` returns 1 match (composition family).
    `grep -F 'Page not found' src/pages/404.astro` returns at least 1 match (H1 + title — D-10).
    `grep -F "This page doesn't exist or has moved. Head back home to find what you're looking for." src/pages/404.astro` returns 1 match (locked body per D-10 RESOLVED).
    `grep -F 'variant="primary"' src/pages/404.astro` returns 1 match (D-12 RESOLVED).
    `grep -F 'href="/"' src/pages/404.astro` returns at least 1 match (Return home target).
    `grep -F 'Return home' src/pages/404.astro` returns 1 match (D-11 — single Button label, NOT "Browse our pages" or similar).
    `! grep -E 'dark:|localStorage\.theme|prefers-color-scheme|\.dark[^a-z]' src/pages/404.astro` — zero matches.
    `! grep -F "from '../layouts/BaseLayout.astro'" src/pages/404.astro` — zero matches.
    `! grep -E "from ['\"]\\.\\./components/ui/" src/pages/404.astro` — zero matches.
    `! grep -F '<style>' src/pages/404.astro` — zero matches.
    `! grep -F 'is:global' src/pages/404.astro` — zero matches.
    `npm run astro check` exits 0.
    `npm run build` exits 0.
    `test -s dist/404.html` exits 0 (D7 from VALIDATION.md).
    `grep -q 'Page not found' dist/404.html` exits 0 (D7 content marker).
    With `npm run dev` running: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts -g "/404"` exits 0 (axe test green).
    Final integration: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts` exits 0 (all 4 tests in the spec green — /faq axe + /faq JSON-LD + /thank-you axe + /404 axe).
    Optional sitemap check: after build, `dist/sitemap-*.xml` does NOT contain `/404` (informational; noindex meta is the primary defense per D-13).
    Manual visual sanity (optional): `npm run dev` → visit `http://localhost:4321/this-route-does-not-exist` → centered elevated Card with H1 "Page not found", body paragraph, primary green "Return home" Button. View page source — `<head>` contains `<meta name="robots" content="noindex, follow">`.
  </verify>
  <done>
    `src/pages/404.astro` exists as a new file on BaseLayoutV2 with the centered-Card composition (D-12), single "Return home" primary Button (D-11), and noindex meta in `<head>` (D-13). Astro static build emits `dist/404.html` non-empty containing "Page not found" (D-09, D7 from VALIDATION). Zero v1 imports, zero icon imports, zero dark-mode artifacts. The /404 axe test in `tests/accessibility/v2-leaf-pages.spec.ts` is now GREEN. ALL 4 tests in the spec are now green (the contract gate from Plan 25-01 fully satisfied).
  </done>
</task>

</tasks>

<verification>

**Build + typecheck:**
- `npm run astro check` exits 0
- `npm run build` exits 0
- `test -s dist/404.html` exits 0 (D7 from VALIDATION.md — Astro auto-emitted the static 404 file)
- `grep -q 'Page not found' dist/404.html` exits 0 (D7 content marker — verifies the v2 page content is in the emitted file)

**Light-mode invariant grep gates (D5 from VALIDATION.md):**
- `! grep -E 'dark:|localStorage\.theme|prefers-color-scheme|\.dark[^a-z]' src/pages/thank-you.astro` returns nothing
- `! grep -E 'dark:|localStorage\.theme|prefers-color-scheme|\.dark[^a-z]' src/pages/404.astro` returns nothing

**Migration completeness grep gates (D6 from VALIDATION.md):**
- `! grep -F "from '../layouts/BaseLayout.astro'" src/pages/thank-you.astro` returns nothing
- `! grep -F "from '../layouts/BaseLayout.astro'" src/pages/404.astro` returns nothing
- `! grep -E "from ['\"]\\.\\./components/ui/" src/pages/thank-you.astro` returns nothing
- `! grep -E "from ['\"]\\.\\./components/ui/" src/pages/404.astro` returns nothing

**Calendly URL preservation (D8 from VALIDATION.md, D-16):**
- `grep -F 'https://calendly.com/joelshinness' src/pages/thank-you.astro` returns exactly 1 match

**Accessibility (D1 from VALIDATION.md, LEAF-02 + LEAF-03):**
- With `npm run dev` running: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts -g "/thank-you"` exits 0
- With `npm run dev` running: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts -g "/404"` exits 0
- Final: `npx playwright test tests/accessibility/v2-leaf-pages.spec.ts` exits 0 (all 4 tests green — full Phase 25 spec green)

**Static 404 emission (D7 from VALIDATION.md, LEAF-03):**
- `test -s dist/404.html` exits 0
- `grep -q 'Page not found' dist/404.html` exits 0
- Optional sitemap belt-and-suspenders: `! grep -F '/404' dist/sitemap-*.xml 2>/dev/null` (informational only — if /404 appears in sitemap, document as follow-up; noindex meta is the primary defense)

**Plan 25-01 spec contract satisfied:**
- All 4 tests in `tests/accessibility/v2-leaf-pages.spec.ts` (created by Plan 25-01) are now green
- The contract gate handed off from Plan 25-01 is fully closed

**Manual UAT signal (LEAF-02):**
- `npm run dev` → visit `http://localhost:4321/thank-you` → centered elevated Card with green MailCheck icon (64px), H1 "Thanks for reaching out!", confirmation paragraph, primary green "Skip the wait — book a call" Button (links to Calendly placeholder), secondary "Return to homepage →" link below

**Manual UAT signal (LEAF-03):**
- `npm run dev` → visit `http://localhost:4321/this-route-does-not-exist` → v2 404 page renders (centered elevated Card, H1 "Page not found", body paragraph, primary green "Return home" Button); view-source confirms `<meta name="robots" content="noindex, follow">` in `<head>`

</verification>

<success_criteria>

LEAF-02 satisfied (per ROADMAP Phase 25 success-criterion 2): `/thank-you` renders on `BaseLayoutV2` with the post-submission message and Calendly placeholder link intact.

LEAF-03 satisfied (per ROADMAP Phase 25 success-criterion 3, applied per D-11's intentional scope tightening): `/404` renders on `BaseLayoutV2` with a single "Return home" Button (the always-visible HeaderV2 carries the rest of the recoverable nav per D-11 — DO NOT add a key-pages list).

ROADMAP Phase 25 success-criterion 4 satisfied across 25-01 + 25-02: All three pages pass axe-core with zero violations and Lighthouse 90+ across all categories (Lighthouse CI runs automatically on PR via `.github/workflows/deploy.yml`).

Specifically:
- [ ] `src/pages/thank-you.astro` imports BaseLayout exclusively from `'../layouts/v2/BaseLayout.astro'`
- [ ] `src/pages/thank-you.astro` imports Button + Card + CardBody exclusively from `'../components/v2/ui/'`
- [ ] `src/pages/thank-you.astro` uses `<MailCheck size={64} class="text-accent" aria-hidden="true" />` (D-15) — replaces v1 CheckCircle2
- [ ] `src/pages/thank-you.astro` uses `<Card elevated={true} class="max-w-2xl w-full text-center">` (D-14) — replaces v1 turquoise Card
- [ ] `src/pages/thank-you.astro` Calendly URL is `https://calendly.com/joelshinness` verbatim — exactly 1 grep match (D-16, VALIDATION D8)
- [ ] `src/pages/thank-you.astro` secondary "Return to homepage" uses `<Button variant="link" size="sm" href="/">` (D-14 RESOLVED)
- [ ] `src/pages/thank-you.astro` H1 + paragraph copy preserved verbatim from v1 (D-16)
- [ ] `src/pages/thank-you.astro` zero `dark:` utilities, zero v1 imports, zero inline `<style>` block (D-17, D-18)
- [ ] `src/pages/404.astro` is a NEW file (did not exist before this plan)
- [ ] `src/pages/404.astro` imports BaseLayout + Button + Card + CardBody from v2 paths only
- [ ] `src/pages/404.astro` does NOT import any Lucide icon (D-12 — no icon)
- [ ] `src/pages/404.astro` injects `<meta slot="head" name="robots" content="noindex, follow" />` (D-13)
- [ ] `src/pages/404.astro` uses `<Card elevated={true}>` + `<Button variant="primary" size="md" href="/">Return home</Button>` (D-12 RESOLVED)
- [ ] `src/pages/404.astro` does NOT contain a secondary link list to Blog/Projects/FAQ/Contact (D-11 — locked scope tightening)
- [ ] `src/pages/404.astro` H1 = "Page not found" (D-10) and body = locked copy from UI-SPEC line 150
- [ ] `src/pages/404.astro` zero `dark:` utilities, zero v1 imports
- [ ] `npm run build` produces `dist/404.html` (non-empty, contains "Page not found") — D7 from VALIDATION.md
- [ ] `npm run astro check` and `npm run build` both exit 0
- [ ] All 4 tests in `tests/accessibility/v2-leaf-pages.spec.ts` green (the /thank-you and /404 axe tests turn green; /faq tests already green from Plan 25-01)
- [ ] `astro.config.mjs` is unchanged (Astro auto-detects /404.astro — D-09)

CONTEXT decisions covered (from D-01..D-20, this plan owns):
- D-09 (Astro static-build auto-emits dist/404.html), D-10 (404 tone + headline + body locked copy), D-11 (single Return home Button — NOT a list of key pages; deliberate scope tightening of roadmap success-criterion 3), D-12 (404 visual layout matches thank-you pattern; no icon, no background art, primary Button), D-13 (404 noindex meta), D-14 (thank-you v2 elevated Card replaces v1 turquoise), D-15 (MailCheck icon replaces CheckCircle2), D-16 (Calendly URL preserved verbatim), D-17 (v2-only imports across both files), D-18 (legacy thank-you scaffold deletion), D-19 (axe tests for /thank-you + /404 turn green in shared spec), D-20 (plan split — this plan owns thank-you + 404).

</success_criteria>

<output>
After completion, create `.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-02-SUMMARY.md` following the standard summary template, including:
- Decisions made during execution (any planner discretion items resolved at write-time)
- Tech-stack additions (none expected — no new packages)
- Files created/modified (the 2 files: src/pages/thank-you.astro modified, src/pages/404.astro created)
- Tests turned green (the previously-failing /thank-you and /404 axe tests in v2-leaf-pages.spec.ts)
- Subsystem: leaf-page-migration
- Affects: Phase 26 (blog migration — sets the precedent for content-page v2 migration)
- Requires: Phase 23 (BaseLayoutV2 + tokens), Phase 24 (Button + Card + CardBody primitives + max-width tokens), Plan 25-01 (spec file + FAQ migration that established the centered-Card visual family)
- Patterns established: Astro 5 static-build 404.html auto-emission for GitHub Pages (D-09 verified empirically); `<meta slot="head" name="robots" content="noindex, follow" />` for hidden-page indexing exclusion (verbatim from /design-system precedent); v2 success-state visual cue via Lucide icon at text-accent + elevated Card (replaces v1 turquoise variant pattern — D-14, D-15)
- Carry-forward todos: v1.3 STATE.md "Update Calendly booking link on /thank-you page with real URL" remains pending (NOT folded into this phase per D-16 — placeholder URL preserved verbatim)
- Optional follow-up (informational): if `dist/sitemap-*.xml` was found to contain `/404` after build, document recommendation to add a sitemap filter in `astro.config.mjs` as a 2-line follow-up. The noindex meta (D-13) is the primary defense; sitemap filtering is belt-and-suspenders.
- Phase 25 completion gate: with this plan green, all 3 leaf-page migrations (LEAF-01, LEAF-02, LEAF-03) are complete; Phase 25 ready for `/gsd:verify-work` and Lighthouse CI sign-off.
</output>
