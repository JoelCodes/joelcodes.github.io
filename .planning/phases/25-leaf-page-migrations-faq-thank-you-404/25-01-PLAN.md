---
phase: 25-leaf-page-migrations-faq-thank-you-404
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/faq.astro
  - tests/accessibility/v2-leaf.spec.ts
autonomous: true
requirements:
  - LEAF-01

must_haves:
  truths:
    - "Visiting /faq renders on src/layouts/v2/BaseLayout.astro with the Crito-faithful page-header banner (D-25-01): surface-muted band, breadcrumbs (Home / FAQ), centered <h1>Frequently Asked Questions</h1> in --font-display"
    - "All 5 question/answer pairs from v1 faq.astro are present verbatim as native <details>/<summary> rows (D-25-03, D-25-21)"
    - "Each accordion row is a borderless divider-list row separated by a 1px --color-border bottom rule (D-25-04); no Card per row; no v1 neobrutalist borders"
    - "Each <summary> shows a Lucide ChevronDown on the right that rotates 180deg via CSS when details[open] (D-25-05); chevron color shifts --color-text-muted -> --color-primary"
    - "The native <summary> disclosure marker is suppressed on Safari (::-webkit-details-marker) and Firefox (::marker + list-style:none) so only the ChevronDown icon shows"
    - "An FAQPage JSON-LD <script> exists in the rendered <head> with @context=schema.org, @type=FAQPage, 5 mainEntity Questions each with acceptedAnswer (D-25-21)"
    - "A bottom CTA banner appears with --color-surface-muted background symmetric to the top banner: <h2>Still have questions? Let's talk.</h2> + v2 Button variant=primary size=lg label 'Get in touch' href='/#contact' (D-25-02)"
    - "Zero v1 imports remain in src/pages/faq.astro (no src/components/ui/, src/components/layout/, src/layouts/BaseLayout.astro)"
    - "Zero dark: utilities, zero FOUC script, zero Google Fonts CDN <link> tags in src/pages/faq.astro (D-25-19, Phase 23 D-22)"
    - "No uppercase text-transform on any heading on /faq (Phase 26 D-26-04)"
    - "tests/accessibility/v2-leaf.spec.ts exists and the /faq describe block (Category A) passes with zero axe-core violations under wcagTags=['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']"
  artifacts:
    - path: "src/pages/faq.astro"
      provides: "v2 FAQ page on BaseLayoutV2"
      contains: "import BaseLayout from '../layouts/v2/BaseLayout.astro'"
      contains_also:
        - "import { ChevronDown } from '@lucide/astro'"
        - "import Button from '../components/v2/ui/Button.astro'"
        - "@type"
        - "FAQPage"
        - "Frequently Asked Questions"
        - "Still have questions"
        - "Get in touch"
    - path: "tests/accessibility/v2-leaf.spec.ts"
      provides: "Wave 0 a11y test scaffold + /faq Category A tests"
      contains: "const wcagTags = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa']"
      contains_also:
        - "import AxeBuilder from '@axe-core/playwright'"
        - "test.describe('v2 /faq"
        - "page.goto('/faq')"
  key_links:
    - from: "src/pages/faq.astro"
      to: "src/layouts/v2/BaseLayout.astro"
      via: "BaseLayout import + <BaseLayout title='FAQ' description='...'>"
      pattern: "import BaseLayout from '\\.\\./layouts/v2/BaseLayout\\.astro'"
    - from: "src/pages/faq.astro"
      to: "<head> JSON-LD"
      via: "<script slot=\"head\" type=\"application/ld+json\" set:html={JSON.stringify(faqSchema)} />"
      pattern: "slot=\"head\"[\\s\\S]*application/ld\\+json"
    - from: "src/pages/faq.astro (CTA Button)"
      to: "/#contact"
      via: "<Button variant=\"primary\" size=\"lg\" href=\"/#contact\">Get in touch</Button>"
      pattern: "href=\"/#contact\""
    - from: "tests/accessibility/v2-leaf.spec.ts"
      to: "/faq route + axe-core"
      via: "page.goto('/faq') + AxeBuilder withTags wcagTags"
      pattern: "page\\.goto\\('/faq'\\)"
---

<objective>
Migrate `/faq` from v1 to `BaseLayoutV2`. Rewrite `src/pages/faq.astro` in place: drop all v1 imports, `dark:` utilities, FOUC script, and Google Fonts CDN. Compose three sections under `<BaseLayout>`: (1) Crito-faithful page-header banner (verbatim pattern from `src/pages/blog/index.astro`, string-swap "Blog" -> "FAQ"), (2) native `<details>/<summary>` divider-list accordion with Lucide ChevronDown rotation, (3) bottom CTA banner with v2 primary Button -> `/#contact`. Preserve the 5 v1 FAQ entries and the FAQPage JSON-LD schema verbatim (inject via `<script slot="head">`). Ship Wave 0 dependency `tests/accessibility/v2-leaf.spec.ts` with the `wcagTags` constant + the `/faq` Category A test cases (smoke, JSON-LD shape, accordion keyboard toggle, chevron rotation, CTA href).

Purpose: Delivers LEAF-01. This is the first leaf-page migration in Phase 25 and produces the page-header banner reuse pattern for Phase 27's `/projects` index. The scoped accordion CSS is intentionally NOT factored into a v2 primitive (single consumer rule, D-25-20).

Output:
- `src/pages/faq.astro` (rewritten in place; ~140-160 lines expected)
- `tests/accessibility/v2-leaf.spec.ts` (new file; Wave 0 dependency; /faq describe block populated, /thank-you and /404 describes left for Plan 25-02 to fill in)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-CONTEXT.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-UI-SPEC.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md
@.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-VALIDATION.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /faq on BaseLayoutV2 with banner + accordion + CTA + JSON-LD</name>

  <read_first>
    - src/pages/faq.astro (v1 — source of truth for FAQ entries + JSON-LD schema generation; copy data + schema generator verbatim; discard everything else)
    - src/pages/blog/index.astro (Phase 26 — source of truth for the page-header banner pattern: imports, `<section class="blog-hero">` markup lines 32-44, scoped `<style>` block lines 168-214; copy pattern verbatim with class-prefix rename to `page-hero` and string swap "Blog" -> "FAQ")
    - src/layouts/v2/BaseLayout.astro (Phase 23 — confirm `title` prop signature, `<slot name="head" />` exposure on line 29; pass BARE `title="FAQ"` per PATTERNS Pattern 1 / RESEARCH Pitfall 2)
    - src/components/v2/ui/Button.astro (Phase 24 — confirm polymorphic `href` renders `<a>`; confirm `variant="primary"` + `size="lg"` API)
    - src/components/SEO.astro (line 30 — confirms `| Joel Shinness` is auto-appended; do not double-suffix the title)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-PATTERNS.md (canonical recipe for the accordion divider-list CSS in §`src/pages/faq.astro` — REWRITE)
  </read_first>

  <files>src/pages/faq.astro</files>

  <action>
  Rewrite `src/pages/faq.astro` in place from scratch.

  Frontmatter imports (drop all v1 imports — no `src/components/ui/`, no `src/components/layout/`, no `src/layouts/BaseLayout.astro`, no v1 SEO component):
  - `BaseLayout` from `../layouts/v2/BaseLayout.astro`
  - `Button` from `../components/v2/ui/Button.astro`
  - `ChevronDown` from `@lucide/astro`

  Frontmatter data: copy the `faqs` array (5 question/answer pairs) verbatim from v1 `src/pages/faq.astro` lines 11-32. Copy the `faqSchema` object generator verbatim from v1 lines 34-46 (uses `faqs.map(faq => ({...}))` to produce `mainEntity`). See PATTERNS §`src/pages/faq.astro` — REWRITE for exact data.

  Page wrapper: `<BaseLayout title="FAQ" description="Frequently asked questions about working with Joel Shinness">` — bare title, no `| Joel Shinness` suffix (SEO.astro auto-appends). Inside, first child: `<script slot="head" type="application/ld+json" set:html={JSON.stringify(faqSchema)} />`. The `slot="head"` attribute targets BaseLayoutV2's `<slot name="head" />` on line 29.

  Section 1 — Top page-header banner: copy the markup and scoped `<style>` from `src/pages/blog/index.astro` verbatim, with two edits: (a) rename CSS class prefix `blog-hero` -> `page-hero` (so Phase 27 `/projects` index can reuse without renaming again — per RESEARCH OQ-1 resolution to keep `blog-hero` on /faq for Phase 25; planner override per UI-SPEC: use `page-hero` since the planner is the same agent that will revisit in Phase 27 and the rename is one-line risk). NOTE: OQ-1 in RESEARCH says keep `blog-hero`; this plan defers to RESEARCH and uses `blog-hero` (no rename) to match the proven shipped CSS. Verbatim copy means verbatim class names too. (b) String swap "Blog" -> "FAQ" in three places: breadcrumb `<li aria-current="page">FAQ</li>`, `<h1 class="blog-hero__title">Frequently Asked Questions</h1>`, and (no subtitle on /faq per UI-SPEC — do NOT include a subtitle even if blog/index has one). Use `›` chevron OR `/` separator — match `src/pages/blog/index.astro` verbatim (currently `/`).

  Section 2 — Accordion divider-list: render `{faqs.map(faq => (<details class="faq-row"><summary><span class="faq-row__question">{faq.question}</span><ChevronDown class="faq-row__chevron" size={20} aria-hidden="true" /></summary><div class="faq-row__answer">{faq.answer}</div></details>))}`. Wrap in a container `<section class="container mx-auto px-4 py-12 lg:py-16 max-w-3xl">` for centered narrow column.

  Section 3 — Bottom CTA banner: `<section class="blog-hero">` (same class as top, picks up the same background + border styling) with inner container, `<h2 class="blog-hero__title">Still have questions? Let&rsquo;s talk.</h2>` (re-use the existing `.blog-hero__title` styles — they apply equally to h2; or duplicate the styling under `.blog-hero h2` if h1-specific in the copy), and a single `<Button variant="primary" size="lg" href="/#contact">Get in touch</Button>` centered below. Use `text-center` on the inner container.

  Scoped `<style>` block — TWO blocks acceptable (or one combined). Block 1 = verbatim banner CSS from `src/pages/blog/index.astro` lines 168-214 (selectors `.blog-hero`, `.blog-hero__breadcrumbs ol`, `.blog-hero__breadcrumbs a`, `.blog-hero__title`). Block 2 = accordion divider-list recipe from 25-PATTERNS lines 122-160 (selectors `.faq-row`, `.faq-row:last-child`, `.faq-row > summary`, `.faq-row > summary::-webkit-details-marker`, `.faq-row > summary::marker`, `.faq-row__chevron`, `.faq-row[open] > summary .faq-row__chevron`, `.faq-row__answer`, `.faq-row > summary:focus-visible`). Use `--color-*`, `--font-*`, `--spacing-*`, `--text-*`, `--leading-*` tokens only — no v1 token names.

  Drop entirely from v1: the inline `<!doctype html>`/`<html>`/`<head>`/`<body>` shell, the `<link rel="preconnect">` Google Fonts chain, the `<script is:inline>` FOUC script, the `body class="font-body bg-bg-light dark:bg-bg-dark"`, the `border-[3px] border-text-light dark:border-text-dark rounded` neobrutalist accordion chrome, the `+` plus-rotate, and the `border-t-[3px]` answer divider.
  </action>

  <verify>
  - `npm run astro check` exits 0 (no type errors on the rewritten file)
  - `npm run build` exits 0
  - `ls dist/faq/index.html` succeeds
  - `grep -c "import BaseLayout from '../layouts/v2/BaseLayout.astro'" src/pages/faq.astro` returns 1
  - `grep -c "import { ChevronDown } from '@lucide/astro'" src/pages/faq.astro` returns 1
  - `grep -c "import Button from '../components/v2/ui/Button.astro'" src/pages/faq.astro` returns 1
  - `grep -c "dark:" src/pages/faq.astro` returns 0
  - `grep -c "components/ui/\|components/layout/\|layouts/BaseLayout.astro" src/pages/faq.astro` returns 0 (no v1 imports — note the v2 path `layouts/v2/BaseLayout.astro` will NOT match this v1 grep)
  - `grep -c "localStorage\|prefers-color-scheme\|fonts.googleapis" src/pages/faq.astro` returns 0
  - `grep -c "@type\".*\"FAQPage\"" src/pages/faq.astro` returns 1 (schema preserved)
  - `grep -c "Frequently Asked Questions" src/pages/faq.astro` returns 1 (h1)
  - `grep -c "Still have questions" src/pages/faq.astro` returns 1 (CTA h2)
  - `grep -c "Get in touch" src/pages/faq.astro` returns 1 (CTA button label)
  - `grep -c 'href="/#contact"' src/pages/faq.astro` returns 1 (CTA href)
  - `grep -c "::-webkit-details-marker" src/pages/faq.astro` returns 1 (Safari marker suppression)
  - `grep -c "::marker" src/pages/faq.astro` returns 1 (Firefox fallback)
  - `grep -c "rotate(180deg)" src/pages/faq.astro` returns 1 (chevron rotation rule)
  - `node tests/check-token-collision.cjs` exits 0
  - `curl -s http://localhost:4321/faq` (during `npm run dev`) returns HTML containing `Frequently Asked Questions` and `application/ld+json`
  - View-source of `dist/faq/index.html` contains exactly one `<script type="application/ld+json">` block with all 5 questions
  </verify>

  <acceptance_criteria>
  - src/pages/faq.astro imports BaseLayout from '../layouts/v2/BaseLayout.astro' (literal string match)
  - src/pages/faq.astro imports Button from '../components/v2/ui/Button.astro' (literal string match)
  - src/pages/faq.astro imports ChevronDown from '@lucide/astro' (literal string match)
  - src/pages/faq.astro contains exactly 5 entries in the `faqs` array with question/answer keys
  - src/pages/faq.astro builds a `faqSchema` object with @context='https://schema.org', @type='FAQPage', mainEntity derived from faqs.map(...)
  - src/pages/faq.astro renders `<script slot="head" type="application/ld+json" set:html={JSON.stringify(faqSchema)} />` (exact attributes)
  - src/pages/faq.astro contains `<BaseLayout title="FAQ"` (NOT `title="FAQ | Joel Shinness"` — see RESEARCH Pitfall 2)
  - src/pages/faq.astro renders breadcrumb `<a href="/">Home</a>` and `<li aria-current="page">FAQ</li>`
  - src/pages/faq.astro renders `<h1 class="blog-hero__title">Frequently Asked Questions</h1>` (or equivalent — class name must match the scoped `<style>` selector)
  - src/pages/faq.astro contains the literal string `Still have questions?` and `Get in touch` for the CTA banner
  - src/pages/faq.astro renders `<Button variant="primary" size="lg" href="/#contact">Get in touch</Button>` (props in any order)
  - The scoped `<style>` block contains `.faq-row { border-bottom: 1px solid var(--color-border); }` and `.faq-row:last-child { border-bottom: none; }`
  - The scoped `<style>` block contains `.faq-row > summary::-webkit-details-marker { display: none; }` AND `.faq-row > summary::marker { display: none; }` (both marker resets)
  - The scoped `<style>` block contains `.faq-row[open] > summary .faq-row__chevron { transform: rotate(180deg); ... }` with a color shift to `var(--color-primary)` in the open state
  - The scoped `<style>` block contains a `:focus-visible` rule with `outline: 2px solid var(--color-accent)` (Phase 24 D-18)
  - `grep -c "dark:" src/pages/faq.astro` returns 0
  - `grep -c "uppercase" src/pages/faq.astro` returns 0
  - `grep -c "text-turquoise\|font-heading\|font-body\|bg-bg-light\|text-text-light" src/pages/faq.astro` returns 0 (no v1 token classes)
  - `npm run build && test -f dist/faq/index.html` exits 0
  - `node tests/check-token-collision.cjs` exits 0
  </acceptance_criteria>

  <done>
  /faq renders on BaseLayoutV2 in light mode with the page-header banner, divider-list accordion, FAQPage JSON-LD, and bottom CTA banner. All 5 FAQ entries are present and toggleable via Enter/Space/Click on the summary. Chevron rotates 180° on open. Native disclosure marker is suppressed in WebKit and Firefox. Zero v1 imports, zero `dark:` utilities, zero FOUC script. Build succeeds, JSON-LD validates structurally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/accessibility/v2-leaf.spec.ts (Wave 0) with /faq Category A tests</name>

  <read_first>
    - tests/accessibility/v2-blog.spec.ts (Phase 26 — pattern source: imports, `wcagTags` constant on line 4, `test.describe` blocks, axe-core invocation pattern; copy structure verbatim)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-RESEARCH.md §Example 4 (full Playwright scaffold with all 13 assertions — the /faq describe block goes in this plan; the /thank-you + /404 describe blocks land in Plan 25-02)
    - .planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-VALIDATION.md §Test Categories (Category A defines: A1 render, A2 JSON-LD, A3 accordion keyboard, A4 CTA banner — implement all 4)
    - playwright.config.ts (confirm baseURL is `http://localhost:4321` and test files glob includes `tests/accessibility/**/*.spec.ts`)
    - src/pages/faq.astro (the file under test — produced by Task 1)
  </read_first>

  <files>tests/accessibility/v2-leaf.spec.ts</files>

  <action>
  Create new file `tests/accessibility/v2-leaf.spec.ts`. Use `tests/accessibility/v2-blog.spec.ts` as the structural template.

  File header (verbatim from v2-blog.spec.ts lines 1-4):
  - `import { test, expect } from '@playwright/test';`
  - `import AxeBuilder from '@axe-core/playwright';`
  - `const wcagTags = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'];`

  Add ONE `test.describe('v2 /faq (Plan 25-01)', () => { ... })` block containing the /faq Category A tests. Leave the file ready for Plan 25-02 to append `test.describe('v2 /thank-you (Plan 25-02)', ...)` and `test.describe('v2 /404 (Plan 25-02)', ...)` blocks — do NOT add stub describes for /thank-you or /404 here (they would fail since neither page is migrated in Wave 1).

  Tests to include in the /faq describe block (4 tests minimum; expand with more cases from RESEARCH Example 4 if useful):
  1. **A1 page render + axe**: `await page.goto('/faq');` → `await expect(page.locator('h1')).toContainText('Frequently Asked Questions');` → `const results = await new AxeBuilder({ page }).withTags(wcagTags).analyze();` → `expect(results.violations).toEqual([]);`
  2. **A2 JSON-LD shape**: navigate to /faq → `const ld = await page.locator('script[type="application/ld+json"]').nth(1).textContent();` (nth=1 because SEO.astro emits a WebSite or BreadcrumbList JSON-LD as index 0; planner should verify the actual ordinal — if SEO emits no JSON-LD, use `.nth(0)`; document the chosen index in a comment) → `const data = JSON.parse(ld);` → assert `data['@type'] === 'FAQPage'`, `data['@context'] === 'https://schema.org'`, `data.mainEntity.length === 5`, `data.mainEntity[0]['@type'] === 'Question'`, `data.mainEntity[0].acceptedAnswer['@type'] === 'Answer'`
  3. **A3 accordion keyboard toggle**: navigate to /faq → focus first `<summary>` → `await page.keyboard.press('Enter');` → assert first `<details>` has `[open]` attribute → press Enter again → assert `[open]` removed (toggle works in both directions)
  4. **A4 CTA banner**: navigate to /faq → `const cta = page.getByRole('link', { name: 'Get in touch' });` → `await expect(cta).toHaveAttribute('href', '/#contact');`

  Optional 5th test (recommended): **A3b chevron rotation** — click first summary → locate `.faq-row__chevron` → read computed `transform` → assert `transform` contains `matrix` (any non-identity matrix proves rotation applied; computed `rotate(180deg)` resolves to `matrix(-1, 0, 0, -1, 0, 0)`).

  Do not assert anything about /thank-you or /404 in this file yet.
  </action>

  <verify>
  - `ls tests/accessibility/v2-leaf.spec.ts` succeeds
  - `npm run astro check` exits 0
  - `grep -c "const wcagTags = \[" tests/accessibility/v2-leaf.spec.ts` returns 1
  - `grep -c "import AxeBuilder from '@axe-core/playwright'" tests/accessibility/v2-leaf.spec.ts` returns 1
  - `grep -c "test.describe('v2 /faq" tests/accessibility/v2-leaf.spec.ts` returns 1
  - `grep -c "page.goto('/faq')" tests/accessibility/v2-leaf.spec.ts` returns at least 4 (one per test)
  - `grep -c "test.describe('v2 /thank-you\|test.describe('v2 /404" tests/accessibility/v2-leaf.spec.ts` returns 0 (Plan 25-02 adds these)
  - `npm run build && npx playwright test tests/accessibility/v2-leaf.spec.ts -g "/faq" --reporter=line` exits 0 with zero failures (all /faq tests pass)
  - `npx playwright test tests/accessibility/v2-leaf.spec.ts -g "/faq"` reports zero axe-core violations across all /faq tests
  </verify>

  <acceptance_criteria>
  - tests/accessibility/v2-leaf.spec.ts exists at the absolute path /Users/joel/Desktop/Claude-Demos/joel-shinness-website/tests/accessibility/v2-leaf.spec.ts
  - tests/accessibility/v2-leaf.spec.ts imports `test` and `expect` from `@playwright/test`
  - tests/accessibility/v2-leaf.spec.ts imports `AxeBuilder` from `@axe-core/playwright`
  - tests/accessibility/v2-leaf.spec.ts declares `const wcagTags = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'];` (exact match)
  - tests/accessibility/v2-leaf.spec.ts contains exactly ONE `test.describe('v2 /faq` block (no /thank-you or /404 describes)
  - The /faq describe block contains at least 4 tests covering A1, A2, A3, A4 from VALIDATION.md
  - At least one test calls `new AxeBuilder({ page }).withTags(wcagTags).analyze()` and asserts `results.violations` equals `[]`
  - At least one test parses an `application/ld+json` script and asserts `@type === 'FAQPage'` AND `mainEntity.length === 5`
  - At least one test asserts the CTA Button's `href` attribute equals `/#contact`
  - `npx playwright test tests/accessibility/v2-leaf.spec.ts -g "/faq"` exits 0 (all tests pass, zero axe violations)
  </acceptance_criteria>

  <done>
  tests/accessibility/v2-leaf.spec.ts exists with the wcagTags constant and a single /faq describe block containing 4+ passing tests. /faq passes axe-core with zero WCAG violations. JSON-LD structural integrity verified. CTA link href verified. Plan 25-02 can append /thank-you and /404 describe blocks without restructuring.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks:
1. `npm run astro check` exits 0
2. `npm run build` exits 0 and emits `dist/faq/index.html`
3. `node tests/check-token-collision.cjs` exits 0
4. `npx playwright test tests/accessibility/v2-leaf.spec.ts -g "/faq" --reporter=line` exits 0
5. Manual: `npm run dev` → open `http://localhost:4321/faq` → verify visually:
   - Top banner has surface-muted background, breadcrumbs, centered display-font H1
   - Each accordion row toggles open/close on click and Enter/Space; chevron rotates 180°; no native triangle visible on Safari (manual cross-browser if available)
   - Bottom CTA banner has surface-muted background, h2 headline, primary green Button
   - No flash of dark styles, no theme toggle visible, no Google Fonts network requests in DevTools
6. View-source `dist/faq/index.html` → confirm a single `<script type="application/ld+json">` block with all 5 questions in `mainEntity`
7. Optional: paste JSON-LD into <https://validator.schema.org/> → expect zero errors
</verification>

<success_criteria>
- LEAF-01 satisfied: /faq migrated to BaseLayoutV2; FAQPage JSON-LD preserved; CTA block added at bottom; axe-core passes
- src/pages/faq.astro is the v2 page (zero v1 imports, zero dark: utilities, zero FOUC, zero Google Fonts CDN)
- tests/accessibility/v2-leaf.spec.ts exists as the Wave 0 dependency for Plan 25-02 (provides the wcagTags constant + /faq Category A coverage)
- All `must_haves.truths` are true (cite D-25-01..06, D-25-19, D-25-21, D-26-04)
- All `must_haves.artifacts` exist and contain the specified strings
- All `must_haves.key_links` resolve via grep patterns
</success_criteria>

<output>
After completion, create `.planning/phases/25-leaf-page-migrations-faq-thank-you-404/25-01-SUMMARY.md` summarizing: files modified/created, decisions exercised (D-25-01..06, D-25-19, D-25-21), test coverage added, and any deviations from the plan (e.g., banner class name kept as `blog-hero` per RESEARCH OQ-1, or any JSON-LD nth-locator index chosen in the spec).
</output>
