---
phase: 39-utility-pages-dev-hidden-pages
plan: 05
type: execute
wave: 4
depends_on: ["39-02", "39-04"]
files_modified:
  - scripts/fidelity-screenshots.mjs
  - .planning/phases/39-utility-pages-dev-hidden-pages/fidelity/
autonomous: false
requirements: [PAGE-03, PAGE-04, PAGE-05, IA-02]

must_haves:
  truths:
    - "A full production build passes and dev-hidden pages are provably absent from the sitemap"
    - "Both dev-hidden pages carry noindex + canonical; the area page carries valid ProfessionalService JSON-LD in the built output"
    - "All three new pages pass axe zero-violations and the contrast gate in light + dark"
    - "Joel has approved the Figma-vs-rendered fidelity comparison for /services/web, /areas/abbotsford, and /404"
  artifacts:
    - path: "scripts/fidelity-screenshots.mjs"
      provides: "Playwright rendered-page screenshots for all three pages at their breakpoints, light + dark"
      contains: "rendered-services-web"
    - path: ".planning/phases/39-utility-pages-dev-hidden-pages/fidelity/"
      provides: "Permanent fidelity artifacts: Figma PNGs + rendered screenshots"
  key_links:
    - from: "scripts/fidelity-screenshots.mjs"
      to: ".planning/phases/39-utility-pages-dev-hidden-pages/fidelity/"
      via: "page.screenshot output path"
      pattern: "fidelity"

security:
  threat_model:
    - threat: "A regression reintroduces sitemap leakage or drops the noindex meta before merge, exposing unfinished pages to indexing"
      mitigation: "The pre-merge verification gate runs npm run build + the sitemap grep + the noindex/canonical grep + JSON-LD grep, all asserting the expected result before Joel approves. This is the IA-02 'verified in production build output' requirement."
      severity: "medium"
    - threat: "Placeholder Abbotsford copy or 404 derived copy is shipped as if final"
      mitigation: "The gate presents the derived-value log: 404 copy is locked-final (D-05, not flagged), Abbotsford copy is flagged as scaffold (D-03), service/area dark treatment is flagged as derived (D-04). Joel reviews before approval; FUT-03 gates real Abbotsford publish."
      severity: "low"
---

<objective>
Close Phase 39: run the automated verification gate (build + sitemap grep + noindex/canonical grep + JSON-LD grep + a11y + contrast), capture the fidelity screenshots, and get Joel's approval on the Figma-vs-rendered comparison for all three pages.

Purpose: PAGE-03/04/05 fidelity + the IA-02 "verified in the production build output" requirement (ROADMAP SC-4). Follows the Phase 34–38 fidelity-gate precedent: manual Figma PNG export (export_nodes broken), Playwright rendered screenshots, side-by-side, Joel approval.

Output: `scripts/fidelity-screenshots.mjs`, fidelity artifacts under the phase `fidelity/` dir, and recorded gate approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-utility-pages-dev-hidden-pages/39-CONTEXT.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-FRAME-DRAFT.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md
@scripts/blog-fidelity-screenshots.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated verification gate</name>
  <read_first>
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md ("Fidelity Gate — Pre-gate checklist" — note the PROD-redirect checklist item is SUPERSEDED by D-02: pages must NOT redirect in prod)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-RESEARCH.md (RQ-1 sitemap grep; RQ-3 JSON-LD grep; RQ-8 sitemap filenames)
    - astro.config.mjs (confirm the 39-02 filter is present)
  </read_first>
  <action>
    Run `npm run build`, then execute each gate check and record pass/fail: (1) `grep -r "services/web\|areas/abbotsford" dist/sitemap-*.xml` returns zero (sitemap exclusion — IA-02); (2) rendered `<head>` of `dist/services/web/index.html` and `dist/areas/abbotsford/index.html` each contain `name="robots" content="noindex, nofollow"` AND `<link rel="canonical"`; (3) `grep -l "ProfessionalService" dist/areas/abbotsford/index.html` non-empty AND `grep "Abbotsford, BC" dist/areas/abbotsford/index.html` matches (PAGE-04 JSON-LD in build output); (4) `grep -rn "import.meta.env.PROD\|Astro.redirect" src/pages/services/web.astro src/pages/areas/abbotsford.astro src/pages/404.astro` returns zero (D-02 — no page redirects; 404 stays public); (5) `npm run preview` + an unmatched URL returns HTTP 404 (PAGE-05); (6) `npm run test:a11y` — 404, services-web, areas-abbotsford specs zero violations light + dark; (7) `node scripts/check-contrast.mjs` passes; (8) `grep -rn "bg-yellow\|text-turquoise\|shadow-neo\|iso-shadow" src/pages/404.astro src/pages/services/web.astro src/pages/areas/abbotsford.astro` returns zero (no old-token leakage). Any failing check blocks the phase — fix before proceeding to fidelity capture.
  </action>
  <acceptance_criteria>
    - `npm run build` succeeds
    - Sitemap grep returns zero; noindex+canonical present on both dev-hidden pages; ProfessionalService + "Abbotsford, BC" present in built area HTML
    - Zero PROD-redirect matches across all three pages; unmatched URL returns HTTP 404
    - `npm run test:a11y` zero violations; `node scripts/check-contrast.mjs` passes; no old-token matches
  </acceptance_criteria>
  <done>Every automated gate check passes; dev-hidden pages are provably out of the sitemap, noindexed, canonical-intact, with valid area JSON-LD, and no page redirects.</done>
</task>

<task type="auto">
  <name>Task 2: Capture rendered fidelity screenshots + export Figma PNGs</name>
  <read_first>
    - scripts/blog-fidelity-screenshots.mjs (exact clone target — capture loop, dark handling, font/animation settle)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("scripts/fidelity-screenshots.mjs" section: shots array + capture loop)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md (Fidelity Gate table: breakpoints per surface)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-FRAME-DRAFT.md (approved frame node IDs for Figma PNG export)
  </read_first>
  <action>
    Create `scripts/fidelity-screenshots.mjs` as a clone of `blog-fidelity-screenshots.mjs`, writing to `.planning/phases/39-utility-pages-dev-hidden-pages/fidelity/`, with a `shots` array covering: `/404` at 1440 + 390 (light + dark); `/services/web` at 390/768/1440 light + 1440 dark; `/areas/abbotsford` at 390/768/1440 light + 1440 dark. Reuse the capture loop verbatim (newContext viewport + colorScheme, waitUntil networkidle, dark html-class wait, document.fonts.ready + rAF + settled finite animations, fullPage screenshot). Start `npm run dev` (background), run `node scripts/fidelity-screenshots.mjs`, confirm all PNGs written. Then export the Figma reference PNGs for frames `85:103` and `85:104` at 1440 + 390 MANUALLY via the Figma editor (`export_nodes` is broken — MEMORY.md; use figma-desktop `get_screenshot` to guide, then manual export) into the same `fidelity/` dir. (404 has no Figma frame — reviewed as brand-voice composition per D-05.)
  </action>
  <acceptance_criteria>
    - `scripts/fidelity-screenshots.mjs` exists and writes rendered PNGs for all three pages at the specified breakpoints/themes into `.planning/phases/.../fidelity/`
    - Figma reference PNGs for 85:103 and 85:104 (1440 + 390) present in the same dir
    - Rendered and Figma PNGs are side-by-side comparable per page
  </acceptance_criteria>
  <done>All rendered + Figma fidelity artifacts are captured and stored for Joel's review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Joel approves the fidelity comparison</name>
  <read_first>
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md ("Fidelity Gate — Pre-approved deviations to log")
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-COPY-GAPS.md (if created during 39-04)
  </read_first>
  <what-built>
    Three pages complete and gate-verified: /404 (branded, locked copy), /services/web (dev-hidden, noindex, from frame 85:103), /areas/abbotsford (dev-hidden, noindex + ProfessionalService JSON-LD, from frame 85:104). All automated gates pass (sitemap exclusion, noindex+canonical, JSON-LD in build, a11y, contrast, no PROD redirect). Fidelity artifacts (rendered vs Figma) are in the phase fidelity/ dir.
  </what-built>
  <how-to-verify>
    1. Review fidelity/ side-by-side: /services/web + /areas/abbotsford rendered vs Figma frames 85:103/85:104 at 390/768/1440 light + 1440 dark; /404 rendered light + dark (no frame — brand-voice review).
    2. Read the derived-value log presented alongside: (a) /404 copy is LOCKED-FINAL per D-05 (not a gap); (b) Abbotsford copy is SCAFFOLD per D-03 (FUT-03 gates real rewrite); (c) service/area dark treatment is DERIVED (no dark frames, Phase 38 D-04 recipe); (d) any [COPY GAP] entries from 39-COPY-GAPS.md.
    3. Confirm the pages match the approved frames and the brand.
  </how-to-verify>
  <action>
    Present the fidelity artifacts and the derived-value log to Joel. If Joel requests visual fixes, apply them to the page(s), re-run Task 1 gate + Task 2 capture, and re-present until approved. Record the approval (date + any deviations accepted) in the plan summary. Do NOT delete fidelity artifacts — they are permanent gate records (Phase 36 precedent).
  </action>
  <resume-signal>Type "approved" to close Phase 39, or describe the fidelity fixes needed.</resume-signal>
</task>

</tasks>

<verification>
- All Task 1 gate checks pass on a fresh `npm run build`
- Fidelity artifacts present in `.planning/phases/39-utility-pages-dev-hidden-pages/fidelity/`
- Joel's approval recorded
</verification>

<success_criteria>
Phase 39 is verified end-to-end: dev-hidden pages provably out of the sitemap, noindexed, canonical-intact, area JSON-LD valid in build output, all three pages a11y-clean, and Joel has approved the fidelity comparison.
</success_criteria>

<output>
After completion, create `.planning/phases/39-utility-pages-dev-hidden-pages/39-05-SUMMARY.md` recording every gate-check result, the fidelity approval outcome, and any deviations Joel accepted.
</output>
