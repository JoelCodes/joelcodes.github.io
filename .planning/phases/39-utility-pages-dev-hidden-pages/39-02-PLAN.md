---
phase: 39-utility-pages-dev-hidden-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/404.astro
  - astro.config.mjs
  - tests/accessibility/404.spec.ts
autonomous: true
requirements: [PAGE-05, IA-02]

must_haves:
  truths:
    - "Visiting any unmatched URL renders a branded 404 page with the locked copy"
    - "The 404 page uses BaseLayout chrome (header + footer) and Wavelength tokens, in light + dark"
    - "The 404 back-to-home CTA navigates to /"
    - "The sitemap filter excludes /services/ and /areas/ URLs from generated sitemap XML"
  artifacts:
    - path: "src/pages/404.astro"
      provides: "Branded not-found page — h1 'Page not found', locked lead, CTAButton solid href='/'"
      contains: "Page not found"
    - path: "astro.config.mjs"
      provides: "Sitemap filter extended to exclude /services/ and /areas/"
      contains: "!page.includes('/services/')"
    - path: "tests/accessibility/404.spec.ts"
      provides: "axe spec for /404 in light + dark, zero violations"
      contains: "404 page in dark mode"
  key_links:
    - from: "src/pages/404.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import + wrap"
      pattern: "import BaseLayout"
    - from: "src/pages/404.astro"
      to: "src/components/wl/CTAButton.astro"
      via: "CTAButton href='/'"
      pattern: "CTAButton.*href=\"/\""
    - from: "astro.config.mjs filter()"
      to: "@astrojs/sitemap"
      via: "filter clause"
      pattern: "!page.includes\\('/areas/'\\)"

security:
  threat_model:
    - threat: "Dev-hidden /services/ and /areas/ URLs leak into the production sitemap and get crawled/indexed before content is final (info-exposure)"
      mitigation: "astro.config.mjs sitemap filter() gains !page.includes('/services/') && !page.includes('/areas/'). Verified by npm run build && grep -r 'services/web|areas/abbotsford' dist/sitemap-*.xml returning zero (this is the IA-02 half owned here; the noindex meta half is 39-04)."
      severity: "medium"
    - threat: "404 page exposes internal routes or stack info"
      mitigation: "404 is a fully static branded page with locked copy — no dynamic route data, no error detail rendered. Astro's static 404.astro convention only serves dist/404.html."
      severity: "low"
---

<objective>
Build the branded `/404` page and extend the sitemap `filter()` to exclude `/services/` and `/areas/`. Both build in parallel with Wave 1 frame drafting — the 404 has NO Figma frame dependency (copy locked D-05, layout recipe locked in UI-SPEC), and the sitemap filter is a config-only additive diff.

Purpose: PAGE-05 (404 rebrand) and the sitemap half of IA-02 (dev-hidden URLs out of the sitemap). This plan is deliberately NOT gated by the frame-approval checkpoint so it makes progress while frames await Joel's review.

Output: `src/pages/404.astro`, extended `astro.config.mjs` sitemap filter, and `tests/accessibility/404.spec.ts`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-CONTEXT.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md
@src/pages/showcase.astro
@src/pages/index.astro
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend astro.config.mjs sitemap filter</name>
  <read_first>
    - astro.config.mjs (current filter at line 148: excludes /blog and /showcase)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-RESEARCH.md (RQ-1 exact diff; RQ-8 sitemap output filenames; Pitfall 5 trailing-slash rationale)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("astro.config.mjs (modify)" section)
  </read_first>
  <action>
    In `astro.config.mjs`, replace the current sitemap `filter` line (currently `filter: (page) => !page.includes('/blog') && !page.includes('/showcase'),`) with a four-clause filter that ALSO excludes `/services/` and `/areas/`: it must AND together `!page.includes('/blog')`, `!page.includes('/showcase')`, `!page.includes('/services/')`, and `!page.includes('/areas/')`. Use the trailing slash on `/services/` and `/areas/` (matches `/services/web/` and future sub-pages without false-positives per Pitfall 5). Leave `changefreq`, `priority`, `lastmod` in the sitemap() block untouched.
  </action>
  <acceptance_criteria>
    - `astro.config.mjs` filter() contains `!page.includes('/services/')` AND `!page.includes('/areas/')`
    - `npm run build && grep -r "services/web\|areas/abbotsford" dist/sitemap-*.xml` returns zero lines (exit code 1 from grep)
    - Existing `/blog` and `/showcase` exclusions remain present
  </acceptance_criteria>
  <done>Sitemap filter excludes /blog, /showcase, /services/, /areas/; a fresh build produces no service/area URLs in dist/sitemap-*.xml.</done>
</task>

<task type="auto">
  <name>Task 2: Create branded 404 page with locked copy</name>
  <read_first>
    - src/pages/showcase.astro (page-shell + paper-bg section pattern; but do NOT copy its import.meta.env.PROD redirect — 404 must be public)
    - src/pages/index.astro (lines 39-54: gradient section + FrequencyWave absolute placement pattern, if FrequencyWave is added)
    - src/components/wl/CTAButton.astro (solid variant, built-in focus ring)
    - src/components/wl/FrequencyWave.astro (class passthrough, aria-hidden decoration)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md ("Page Contract — /404": layout, 640px max-width, 112px padding, copy)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("src/pages/404.astro" section: exact shell + heading + lead + CTA patterns)
  </read_first>
  <action>
    Create `src/pages/404.astro`. Import `BaseLayout` from `../layouts/BaseLayout.astro` and `CTAButton` from `../components/wl/CTAButton.astro`. NO `import.meta.env.PROD` redirect — the 404 must stay publicly reachable (D-05). Wrap in `<BaseLayout title="Page Not Found | Joel Shinness Solutions" description="This page has moved or doesn't exist.">` with NO `slot="head"` noindex (404 is not dev-hidden). One `<section>` with the standard gutter class `px-[24px] sm:px-[32px] lg:px-[160px] min-[1920px]:px-[400px]`, 112px top/bottom padding, `--wl-paper` background (via token, semantic dark flip — not a gradient unless FrequencyWave is added). Inner `<div style="max-width: 640px; margin: 0 auto; text-align: center;">`. Content, copy VERBATIM (D-05, locked — do NOT flag as derived at gate, it is approved): `<h1 class="wl-heading-h1-interior" style="color: var(--color-wl-ink); margin: 0;">Page not found</h1>`; then `<p class="wl-text-lead" style="color: var(--color-wl-sub);">This page has moved or doesn't exist. Head back to the homepage to find what you're looking for.</p>`; then `<CTAButton href="/" variant="solid">Back to home</CTAButton>` (internal link — no target/rel). FrequencyWave is Claude's discretion (D-06): a single 640px column on a full-viewport page tends to feel sparse — if included, wrap the section `position: relative; overflow: hidden;`, render `<FrequencyWave class="absolute pointer-events-none" ...>` in the background BEFORE content, make the content wrapper `relative z-10`, and switch the section bg to the gradient-in-class pattern `[background:linear-gradient(to_bottom,#E6F1F1,#D2E7E7)] dark:[background:linear-gradient(to_bottom,#123640,#0C2228)]` (never inline `style` for gradients — Pitfall 3). Do NOT invent new SVG decoration either way.
  </action>
  <acceptance_criteria>
    - `src/pages/404.astro` exists, imports BaseLayout + CTAButton, contains NO `import.meta.env.PROD` and NO `Astro.redirect`
    - Rendered page contains exactly the strings "Page not found", "This page has moved or doesn't exist. Head back to the homepage to find what you're looking for.", and a CTA "Back to home" with `href="/"`
    - No `slot="head"` robots meta on this page
    - `npm run build && npm run preview` — requesting an unmatched URL serves the 404 page with HTTP 404 status
    - Any gradient background is in `class` arbitrary-value syntax, never inline `style`
  </acceptance_criteria>
  <done>/404 renders the locked branded not-found page in light + dark via BaseLayout chrome, with a working Back-to-home CTA and correct HTTP 404 status.</done>
</task>

<task type="auto">
  <name>Task 3: Add 404 accessibility spec</name>
  <read_first>
    - tests/accessibility/showcase.spec.ts (exact clone target — 59-line structure)
    - tests/accessibility/helpers.ts (settleAnimations import)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("tests/accessibility/404.spec.ts" section: full file template)
  </read_first>
  <action>
    Create `tests/accessibility/404.spec.ts` as an exact clone of `tests/accessibility/showcase.spec.ts` with: `test.describe('404 Page Accessibility', ...)`; a light-mode test that does `page.goto('/404')` → `settleAnimations(page)` → `new AxeBuilder({ page }).withTags(wcagTags).analyze()` → `expect(results.violations).toEqual([])`; a dark-mode test using `browser.newContext({ colorScheme: 'dark' })`, asserting `html` has class `/dark/`, then the same axe analysis. Reuse the `wcagTags` constant `['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa']` and import `settleAnimations` from `./helpers`. Note in a comment that `page.goto('/404')` hits the 404.astro route directly (200 in dev) — HTTP-404 status is verified separately via preview.
  </action>
  <acceptance_criteria>
    - `tests/accessibility/404.spec.ts` exists with a light test and a dark test, both asserting `results.violations` equals `[]`
    - Dark test asserts `html` has the `dark` class before running axe
    - `npm run test:a11y` passes the 404 specs with zero violations in both themes
  </acceptance_criteria>
  <done>404 axe spec passes zero violations in light + dark.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds; `grep -r "services/web\|areas/abbotsford" dist/sitemap-*.xml` returns zero
- `npm run preview` + unmatched URL → 404 page, HTTP 404 status
- `npm run test:a11y` — 404 specs zero violations light + dark
- `grep -r "import.meta.env.PROD\|Astro.redirect" src/pages/404.astro` returns zero (404 stays public)
</verification>

<success_criteria>
The branded /404 ships with locked copy and correct 404 status; the sitemap filter provably excludes /services/ and /areas/. Neither depends on the Figma frames.
</success_criteria>

<output>
After completion, create `.planning/phases/39-utility-pages-dev-hidden-pages/39-02-SUMMARY.md` recording the 404 FrequencyWave include/omit decision (D-06 resolution) and the final sitemap filter clause.
</output>
