---
phase: 39-utility-pages-dev-hidden-pages
plan: 04
type: execute
wave: 3
depends_on: ["39-03"]
files_modified:
  - .planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md
  - src/pages/services/web.astro
  - src/pages/areas/abbotsford.astro
  - tests/accessibility/services-web.spec.ts
  - tests/accessibility/areas-abbotsford.spec.ts
  - scripts/check-contrast.mjs
autonomous: true
requirements: [PAGE-03, PAGE-04, IA-02]

must_haves:
  truths:
    - "User visiting /services/web by direct URL sees the full Service Web page (no redirect) in light + dark"
    - "User visiting /areas/abbotsford by direct URL sees the full Area page (no redirect) in light + dark"
    - "Both pages carry <meta name=\"robots\" content=\"noindex, nofollow\"> in the rendered <head>"
    - "The /areas/abbotsford <head> contains ProfessionalService JSON-LD with areaServed 'Abbotsford, BC'"
    - "Both pages' <link rel=\"canonical\"> remains present and correct despite noindex"
  artifacts:
    - path: ".planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md"
      provides: "Verbatim frame extraction: section geometry, copy, backgrounds, components per section for 85:103 and 85:104"
      min_lines: 30
    - path: "src/pages/services/web.astro"
      provides: "Service Web page — noindex head slot, wl/ composition, one h1, book-a-call CTA, NO PROD redirect"
      contains: "noindex, nofollow"
    - path: "src/pages/areas/abbotsford.astro"
      provides: "Area Abbotsford page — noindex + ProfessionalService JSON-LD head slot, wl/ composition, NO PROD redirect"
      contains: "ProfessionalService"
    - path: "tests/accessibility/services-web.spec.ts"
      provides: "axe spec /services/web light + dark"
      contains: "Service Web Page Accessibility"
    - path: "tests/accessibility/areas-abbotsford.spec.ts"
      provides: "axe spec /areas/abbotsford light + dark"
      contains: "Area Abbotsford Page Accessibility"
  key_links:
    - from: "src/pages/services/web.astro"
      to: "src/layouts/BaseLayout.astro <slot name='head'>"
      via: "meta slot='head' robots noindex"
      pattern: "slot=\"head\" name=\"robots\""
    - from: "src/pages/areas/abbotsford.astro"
      to: "BaseLayout head slot"
      via: "script slot='head' application/ld+json set:html"
      pattern: "application/ld\\+json.*set:html"
    - from: "both pages CTAButton"
      to: "src/lib/constants.ts BOOKING_URL"
      via: "href={BOOKING_URL}"
      pattern: "href=\\{BOOKING_URL\\}"

security:
  threat_model:
    - threat: "Dev-hidden pages get indexed because noindex meta is missing or a PROD redirect wrongly removes them from the build (violating ROADMAP SC-2/SC-3)"
      mitigation: "noindex meta injected via BaseLayout head slot on both pages (verified by grep on rendered <head>). NO import.meta.env.PROD redirect (D-02) — pages ship into dist/ reachable by direct URL. Sitemap exclusion handled in 39-02. This is the noindex half of IA-02."
      severity: "medium"
    - threat: "ProfessionalService JSON-LD contains placeholder/sensitive PII (address, phone) that misleads local-SEO or exposes data"
      mitigation: "JSON-LD is MINIMAL per D-04: only name, url, areaServed 'Abbotsford, BC', serviceType, provider (Person: Joel Shinness). NO address, NO telephone. Real GBP-consistent NAP is deferred to FUT-03 while the page stays noindexed."
      severity: "low"
    - threat: "External book-a-call CTA opens a tab with window.opener access (reverse tabnabbing)"
      mitigation: "CTAButton to BOOKING_URL uses target=\"_blank\" rel=\"noopener\" (Phase 35/37 convention)."
      severity: "low"
---

<objective>
Extract the Joel-approved frames `85:103` / `85:104` and build the two dev-hidden pages — `/services/web` and `/areas/abbotsford` — composing only from `src/components/wl/`, wiring the noindex head-slot mechanism and the ProfessionalService JSON-LD, and adding their axe specs + any net-new contrast pairs.

Purpose: PAGE-03 (Service Web dev-hidden), PAGE-04 (Area page + ProfessionalService JSON-LD, dev-hidden), and the noindex half of IA-02. The single most important constraint: NO `import.meta.env.PROD` redirect on either page (D-02) — that showcase pattern must NOT be copied; the pages ship into the prod build reachable by direct URL and hidden only via noindex meta + the sitemap filter (from 39-02).

Output: `39-EXTRACTION.md`, `src/pages/services/web.astro`, `src/pages/areas/abbotsford.astro`, two axe specs, and contrast-script additions for any net-new bg literals.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-CONTEXT.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-RESEARCH.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md
@.planning/phases/39-utility-pages-dev-hidden-pages/39-FRAME-DRAFT.md
@src/pages/showcase.astro
@src/pages/index.astro
@src/layouts/BaseLayout.astro
@src/components/SEO.astro
@src/components/wl/ServiceCard.astro
@src/components/wl/CTAButton.astro
@src/lib/constants.ts
</context>

<critical_constraint>
NO `if (import.meta.env.PROD) return Astro.redirect('/')` on either page. The 39-UI-SPEC.md "Dev gate redirect" Interaction-Contracts row and the per-page dev-gate frontmatter are SUPERSEDED by CONTEXT D-02. The hiding mechanism is ONLY: unlinked in nav + noindex meta (this plan) + sitemap filter (39-02). A page returning 301→/ in prod is a FAILURE.
</critical_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Extract approved frames 85:103 + 85:104 to 39-EXTRACTION.md</name>
  <read_first>
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-FRAME-DRAFT.md ("Approval" section — final approved node IDs)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-UI-SPEC.md (FIDELITY-GAP table: what to extract per frame — header padding, section padding/bg, card grid columns/gap at each breakpoint)
    - .planning/phases/37-landing-page/37-01-* extraction artifact if present (extraction-artifact format precedent)
    - src/pages/showcase.astro (how prior extracted values were consumed into a page)
  </read_first>
  <action>
    Using figma-desktop MCP (`get_design_context` / `get_screenshot`) on the approved frames `85:103` and `85:104` (node IDs from 39-FRAME-DRAFT.md Approval section), create `.planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md` recording VERBATIM per frame: (a) every section in order with its heading text, eyebrow/label text, body copy, and CTA copy; (b) each section's background (token `--wl-paper` / `--wl-sea-glass` or gradient literal); (c) each section's vertical padding at 390/768/1440/1920 (or the standard 112px if it matches); (d) any card grid column count + gap per breakpoint; (e) which wl/ component maps to each block (ServiceCard/Step/Callout/LinkCard/Eyebrow/CTAButton/FrequencyWave/Breadcrumb). Follow the v1.4 lesson: never invent — any copy/geometry that cannot be read is flagged `[COPY GAP]` / `FIDELITY-GAP` and logged to `39-COPY-GAPS.md`. Note any type size that has no ramp match (record as local-style-with-source-node, Phase 36 precedent). For Abbotsford, note copy is scaffold (D-03).
  </action>
  <acceptance_criteria>
    - `.planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md` exists covering BOTH frames, section-by-section, with copy + bg + padding + component mapping
    - Every unreadable copy/geometry point is flagged as [COPY GAP] / FIDELITY-GAP (none silently invented)
    - Net-new background literals (not already in check-contrast.mjs) are listed explicitly for Task 4
  </acceptance_criteria>
  <done>Both approved frames are extracted verbatim into 39-EXTRACTION.md as the authoritative build input; gaps are flagged, not invented.</done>
</task>

<task type="auto">
  <name>Task 2: Build /services/web (noindex, wl/ composition, no PROD redirect)</name>
  <read_first>
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md (Service Web section data from Task 1 — authoritative)
    - src/pages/showcase.astro (page-shell + section patterns — but NOT its import.meta.env.PROD redirect)
    - src/pages/index.astro (wl/ component composition + gradient-in-class pattern lines 39-58)
    - src/layouts/BaseLayout.astro (line 38 <slot name="head" /> injection point)
    - src/components/wl/ServiceCard.astro (props: variant, kicker, benefit, title, headingLevel, body; slots icon/default)
    - src/components/wl/CTAButton.astro (variant, icon, href, target, rel; always renders <a>)
    - src/components/wl/Eyebrow.astro (onDark prop)
    - src/lib/constants.ts (BOOKING_URL)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("src/pages/services/web.astro" section: exact composition patterns)
  </read_first>
  <action>
    Create `src/pages/services/web.astro`. Import `BaseLayout` from `../../layouts/BaseLayout.astro`, `{ BOOKING_URL }` from `../../lib/constants`, and only the wl/ components that frame 85:103 actually uses per 39-EXTRACTION.md (ServiceCard, Eyebrow, CTAButton, and Step/Callout/LinkCard/FrequencyWave/Breadcrumb ONLY if the frame shows them — build only what the frame shows, v1.4 lesson). Do NOT add `import.meta.env.PROD` / `Astro.redirect` (D-02 — critical). Wrap in `<BaseLayout title="Web Development Services | Joel Shinness Solutions" description="[from extraction]">` and inject `<meta slot="head" name="robots" content="noindex, nofollow" />` as the first child (Option A head-slot; renders after SEO's canonical, which is unaffected). Build each section from 39-EXTRACTION.md verbatim: standard gutter class `px-[24px] sm:px-[32px] lg:px-[160px] min-[1920px]:px-[400px]`; content wrapper `max-width: 1120px; margin: 0 auto;`; section padding + bg from extraction (gradient backgrounds go in `class` arbitrary-value syntax with `dark:` pair from the UI-SPEC dark-recipe table, NEVER inline style). Heading hierarchy: exactly one `<h1>` (`.wl-heading-h1-interior` or ramp-matched), section `<h2>`, ServiceCard `headingLevel={3}`. Book-a-call CTA: `<CTAButton href={BOOKING_URL} variant="solid" icon="calendar" target="_blank" rel="noopener">[CTA copy]</CTAButton>`. All copy verbatim from extraction; any gap rendered as the `[COPY GAP]` yellow-bg/red-outline marker and logged to `39-COPY-GAPS.md`. Dark treatment follows the Phase 38 D-04 landing-recipe (semantic tokens flip automatically; gradient literals map to the UI-SPEC dark pairs).
  </action>
  <acceptance_criteria>
    - `src/pages/services/web.astro` exists; `grep -c "import.meta.env.PROD\|Astro.redirect" src/pages/services/web.astro` returns 0
    - Contains `<meta slot="head" name="robots" content="noindex, nofollow" />`
    - Rendered `<head>` of /services/web (after build) contains `name="robots" content="noindex, nofollow"` AND still contains `<link rel="canonical">`
    - Exactly one `<h1>` in the page; ServiceCard instances use `headingLevel={3}`
    - Book-a-call CTA uses `href={BOOKING_URL}` with `target="_blank" rel="noopener"`
    - No gradient in inline `style=`; all gradients in `class` arbitrary-value syntax
    - `npm run build` produces `dist/services/web/index.html` (page ships to prod, not redirected)
  </acceptance_criteria>
  <done>/services/web renders the full Service Web page from frame 85:103, noindexed via head slot, with no PROD redirect and canonical intact.</done>
</task>

<task type="auto">
  <name>Task 3: Build /areas/abbotsford (noindex + ProfessionalService JSON-LD, no PROD redirect)</name>
  <read_first>
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md (Area Abbotsford section data — authoritative, copy is scaffold D-03)
    - src/pages/services/web.astro (sibling built in Task 2 — identical shell)
    - src/components/SEO.astro (line 75 set:html={JSON.stringify(personSchema)} — exact JSON-LD injection pattern to mirror)
    - src/layouts/BaseLayout.astro (line 38 head slot)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-CONTEXT.md (D-04 minimal JSON-LD — no address/telephone)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("src/pages/areas/abbotsford.astro" section: JSON-LD + head slot patterns)
  </read_first>
  <action>
    Create `src/pages/areas/abbotsford.astro` mirroring the services/web.astro shell (same imports two levels up, same section-build discipline from 39-EXTRACTION.md for frame 85:104). NO `import.meta.env.PROD` / `Astro.redirect` (D-02). In frontmatter define `professionalServiceSchema` = an object with EXACTLY these keys (D-04 minimal — NO address, NO telephone): `"@context": "https://schema.org"`, `"@type": "ProfessionalService"`, `"name": "Joel Shinness Solutions"`, `"url": "https://joelshinness.com"`, `"areaServed": "Abbotsford, BC"`, `"serviceType": "Web Development"`, `"provider": { "@type": "Person", "name": "Joel Shinness" }`. Wrap in `<BaseLayout title="Web Development in Abbotsford, BC | Joel Shinness Solutions" description="[from extraction]">` and inject TWO head-slot elements: `<meta slot="head" name="robots" content="noindex, nofollow" />` and `<script slot="head" type="application/ld+json" set:html={JSON.stringify(professionalServiceSchema)} />` (mirrors SEO.astro line 75 exactly — `set:html` avoids HTML-escaping). Build sections verbatim from extraction (scaffold copy per D-03); same gutter/max-width/heading-hierarchy/CTA rules as services/web. Book-a-call CTA to BOOKING_URL with target=_blank rel=noopener.
  </action>
  <acceptance_criteria>
    - `src/pages/areas/abbotsford.astro` exists; `grep -c "import.meta.env.PROD\|Astro.redirect" src/pages/areas/abbotsford.astro` returns 0
    - Frontmatter JSON-LD object has areaServed "Abbotsford, BC" and provider Person "Joel Shinness"; contains NO `address` and NO `telephone` keys
    - Head slot injects both the robots noindex meta and the application/ld+json script via `set:html`
    - After build: `grep -l "ProfessionalService" dist/areas/abbotsford/index.html` returns the path (non-empty) AND `grep "Abbotsford, BC" dist/areas/abbotsford/index.html` matches
    - Rendered `<head>` contains `name="robots" content="noindex, nofollow"` AND `<link rel="canonical">`
    - `npm run build` produces `dist/areas/abbotsford/index.html` (ships to prod, not redirected)
  </acceptance_criteria>
  <done>/areas/abbotsford renders from frame 85:104, noindexed, with minimal valid ProfessionalService JSON-LD (no NAP) present in the built <head>, and no PROD redirect.</done>
</task>

<task type="auto">
  <name>Task 4: Add axe specs for both pages + net-new contrast pairs</name>
  <read_first>
    - tests/accessibility/showcase.spec.ts (exact clone target)
    - tests/accessibility/helpers.ts (settleAnimations)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-EXTRACTION.md (Task 1 list of net-new bg literals, if any)
    - scripts/check-contrast.mjs (existing pair structure; existing pairs: ink/sub on paper, ink/sub on sea-glass, accent on sea-glass)
    - .planning/phases/39-utility-pages-dev-hidden-pages/39-PATTERNS.md ("services-web.spec.ts" / "areas-abbotsford.spec.ts" sections)
  </read_first>
  <action>
    Create `tests/accessibility/services-web.spec.ts` (describe `'Service Web Page Accessibility'`, route `/services/web`) and `tests/accessibility/areas-abbotsford.spec.ts` (describe `'Area Abbotsford Page Accessibility'`, route `/areas/abbotsford`) as exact clones of `showcase.spec.ts` — light test + dark-mode test (colorScheme dark, assert html.dark class), `wcagTags = ['wcag2a','wcag2aa','wcag21a','wcag21aa','wcag22aa']`, `settleAnimations` from `./helpers`, `expect(results.violations).toEqual([])`. Add a comment on each: `// Page is dev-hidden (noindex) but reachable in dev — no redirect (D-02)`. Then, ONLY for background literals surfaced net-new by 39-EXTRACTION.md (not already covered), append the corresponding text/bg pairs to `scripts/check-contrast.mjs` (target 4.5:1). If extraction surfaced no net-new bg literal, add nothing and note that in the summary.
  </action>
  <acceptance_criteria>
    - `tests/accessibility/services-web.spec.ts` and `tests/accessibility/areas-abbotsford.spec.ts` exist with light + dark tests asserting zero violations
    - `npm run test:a11y` passes both new specs zero violations light + dark
    - `node scripts/check-contrast.mjs` passes with any Phase 39 net-new pairs added (or unchanged if none)
  </acceptance_criteria>
  <done>Both dev-hidden pages pass axe zero-violations in light + dark, and every net-new bg literal passes the contrast gate.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds; both `dist/services/web/index.html` and `dist/areas/abbotsford/index.html` exist
- `grep -r "services/web\|areas/abbotsford" dist/sitemap-*.xml` returns zero (sitemap filter from 39-02 holds)
- `grep -rl "ProfessionalService" dist/areas/abbotsford/index.html` non-empty; `grep "Abbotsford, BC" ...` matches
- Rendered `<head>` of both pages contains `noindex, nofollow` AND `<link rel="canonical">`
- `grep -rn "import.meta.env.PROD\|Astro.redirect" src/pages/services/web.astro src/pages/areas/abbotsford.astro` returns zero
- `npm run test:a11y` — both new specs zero violations; `node scripts/check-contrast.mjs` passes
</verification>

<success_criteria>
Both dev-hidden pages are reachable by direct URL, noindexed, out of the sitemap, with the area page carrying minimal ProfessionalService JSON-LD — and neither page has a PROD redirect. Copy is verbatim-from-frame (gaps flagged, not invented).
</success_criteria>

<output>
After completion, create `.planning/phases/39-utility-pages-dev-hidden-pages/39-04-SUMMARY.md` recording: section count landed per page, any [COPY GAP] entries logged, net-new contrast pairs added (or none), and confirmation that neither page has a PROD redirect.
</output>
