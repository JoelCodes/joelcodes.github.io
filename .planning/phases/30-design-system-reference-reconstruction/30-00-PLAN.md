---
phase: 30-design-system-reference-reconstruction
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - PAGE-07
  - COMP-08
  - COMP-09
  - VALID-03

must_haves:
  truths:
    - "Section / TokenSwatchGrid component lives in g9oRa5 with declared slot signature"
    - "Section / TypeSpecimen component lives in g9oRa5 with declared slot signature"
    - "Section / ComponentShowcase component lives in g9oRa5 with code-snippet-slot per D-150"
    - "3 sibling Pencil notes ship declaring slot signatures per D-149 belt-and-suspenders"
    - "Token surface drift = 0 net-new tokens (default per D-151; mid-plan token escalation only if showcase-internal padding/gap surfaces a gap)"
    - "Library count moves 16 → 19"
    - "PEN-INVENTORY extended with 3 new Frames Inventory rows + Variant Evidence rows + OPEN-30-NN seed"
    - "snapshot_layout at document root returns 'No layout problems.'"
    - "All Phase 23-29 baseline component IDs remain unchanged"
  artifacts:
    - path: "design/Crito.pen — Section / TokenSwatchGrid"
      provides: "Reusable color-swatch grid pattern (heading-slot + grid-of-swatch-tiles auto-layout horizontal-wrap)"
      contains: "frame type, layout:vertical, child heading-slot text + grid-container frame with default swatch-tile"
    - path: "design/Crito.pen — Section / TypeSpecimen"
      provides: "Reusable type-specimen tile pattern (heading-slot + grid-of-specimen-tiles)"
      contains: "frame type, layout:vertical, child heading-slot text + grid-container with default specimen-tile (sample-text + token-name + size/weight/lh caption)"
    - path: "design/Crito.pen — Section / ComponentShowcase"
      provides: "Reusable component-instance pattern (label-slot + live-instance-slot + code-snippet-slot)"
      contains: "frame type, layout:vertical, label-slot text + live-instance-slot frame + code-snippet-slot styled with type-semantic-mono-primitive + type-semantic-prose-code-block per D-150"
    - path: "design/Crito.pen — 3 sibling notes at document root"
      provides: "Slot signature declarations per D-149"
      contains: "note type with text describing slot signature for each of the 3 new sections"
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "3 new Frames Inventory rows + 3 Variant Evidence rows + OPEN-30-NN seed"
      contains: "TokenSwatchGrid + TypeSpecimen + ComponentShowcase entries"
  key_links:
    - from: "Section / TokenSwatchGrid"
      to: "g9oRa5 (_Components / Sections)"
      via: "batch_design Insert with parent=g9oRa5"
      pattern: "I(\"g9oRa5\", { ... })"
    - from: "Section / TypeSpecimen"
      to: "g9oRa5 (_Components / Sections)"
      via: "batch_design Insert with parent=g9oRa5"
      pattern: "I(\"g9oRa5\", { ... })"
    - from: "Section / ComponentShowcase"
      to: "g9oRa5 (_Components / Sections)"
      via: "batch_design Insert with parent=g9oRa5"
      pattern: "I(\"g9oRa5\", { ... })"
    - from: "Section / ComponentShowcase code-snippet-slot"
      to: "type-semantic-mono-primitive + type-semantic-prose-code-block tokens (Phase 28)"
      via: "literal hex/family/size values per OPEN-23-13 (batch_design rejects $token refs)"
      pattern: "fontFamily literal + fontSize literal + Variant Evidence row documents binding"
---

<objective>
Plan 30-00 ships 3 new Section components (`Section / TokenSwatchGrid`, `Section / TypeSpecimen`, `Section / ComponentShowcase`) inside `_Components / Sections` (g9oRa5) per D-149, each with a sibling Pencil note declaring slot signature per D-52 + D-93 belt-and-suspenders. This is the foundation layer for Plan 30-01's `Design system` page frame composition; without these 3 components, Plan 30-01 would inline ~58 instances of structural tile/showcase composition.

Purpose: Avoid duplicating ~58 structural tile-decisions inside Plan 30-01 by factoring the 3 repeating patterns (color-swatch grid × ~30 instances, type-specimen tile × ~12 instances, component-showcase block × ~16 instances) as reusable Section components. INTENTIONAL DEPARTURE from Phase 26/28/29 narrow-scoping discipline per D-149 — bounded at the SECTION layer only, justified by in-phase repetition density + future v3 design-system milestone plausibility. Library count moves 16 → 19.

Output: 3 new component frames in `design/Crito.pen` library + 3 sibling Pencil notes + PEN-INVENTORY extension (NEW Frames Inventory rows + Variant Evidence rows + OPEN-30-NN seed).

Foundation plan closes WITHOUT a user-calibration gate per D-152 + D-86 + D-102 + D-124 + D-139 chain (agent-deterministic foundation work; user gate only at the page-frame composition layer in Plan 30-01).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# Phase 30 LOCKED decisions + research (MANDATORY)
@.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md
@.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md
@.planning/phases/30-design-system-reference-reconstruction/30-VALIDATION.md

# Calibration protocol (joel-only branch) — Plan 30-01 consumer, but Plan 30-00 component design must satisfy
@.planning/research/CALIBRATION-PROTOCOL.md

# Single source of truth for component IDs + status
@.planning/research/PEN-INVENTORY.md

# Open flags + pitfalls + stack
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

# Direct predecessor — D-128 in-place reclassification pattern (D-154 carry-forward)
@.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md

# Phase 28 — D-150 mono-primitive + prose-code-block token consumers
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md

# Phase 25 — D-52 typed-slot suggestion-only + sibling-note belt-and-suspenders convention
@.planning/phases/25-section-compound-components/25-CONTEXT.md

# Project instructions
@./CLAUDE.md
</context>

<threat_model>
Phase 30 ships ZERO `src/` code changes (v2.0 `.pen`-file-only milestone per PROJECT.md + REQUIREMENTS.md Out of Scope). No HTTP endpoints, no user input handling, no auth flows, no database operations, no external API calls. Pencil MCP server runs locally with file-system access to `design/Crito.pen`. Threat surface: NONE (trivially empty). Pre-flight `get_editor_state` assertion (D-153) prevents wrong-file mutation (OPEN-23-14 carry-forward) — this is a correctness gate, not a security threat.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight active-editor assertion + baseline read + library probe</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-153 pre-flight rule)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Standard Stack baseline tool calls)
    @.planning/research/PEN-INVENTORY.md (baseline IDs: g9oRa5, avgor, t67DU6, RpGbe, M7eUr, hIWuC, nwJk7, j0FxQZ, kJQmJ, u7NmaS, G0wNOc, Xs0Hs, Hs5rc, N1jo3i, O1IwyS, t40xct, ZSxZU, DnsRs, y4RORu, OLSa0)
    @.planning/research/PITFALLS.md (Pitfall 6 active-editor switch — OPEN-23-14)
  </read_first>
  <action>
    1. Call `mcp__pencil__get_editor_state({ include_schema: false })`. Assert `activeEditor.file == "design/Crito.pen"`. If mismatch, HALT and surface to user per D-153.
    2. Call `mcp__pencil__get_guidelines({ topic: "design-system" })` — refresh slot mechanics + component-authoring guidance for the 3 new section components.
    3. Call `mcp__pencil__get_variables({})` — record token count (expected: 127 post-Phase 28 surface; Phase 27 + 29 added 0 net new). Cache value for Task 6 drift check.
    4. Call `mcp__pencil__batch_get({ nodeIds: ["g9oRa5", "avgor", "t67DU6", "RpGbe"], readDepth: 2 })` — confirm 3 library parent frames + RpGbe `_Tokens & Foundations` exist and unchanged. Record g9oRa5 child count for Task 5 delta check.
    5. Call `mcp__pencil__batch_get({ nodeIds: ["M7eUr", "hIWuC", "nwJk7", "j0FxQZ", "kJQmJ", "u7NmaS", "G0wNOc", "Xs0Hs", "Hs5rc", "N1jo3i", "O1IwyS", "t40xct", "ZSxZU", "DnsRs", "y4RORu", "OLSa0"], readDepth: 1 })` — confirm all 16 Phase 23-29 library entries exist (baseline for Task 6 zero-mutation regression check).
    6. Log baseline state to working memory (no file write at this task).
  </action>
  <verify>
    `mcp__pencil__get_editor_state({ include_schema: false })` returns activeEditor path containing `design/Crito.pen`.
    `mcp__pencil__get_variables({})` returns a `variables` array (default expected 127 entries).
    `mcp__pencil__batch_get({ nodeIds: ["g9oRa5", "avgor", "t67DU6", "RpGbe"], readDepth: 2 })` returns 4 frames, each with a `children` array.
  </verify>
  <acceptance_criteria>
    - After Task 1, `mcp__pencil__get_editor_state({ include_schema: false })` returns activeEditor with `file` field containing the literal string `"design/Crito.pen"` (D-153 satisfied — halt otherwise).
    - After Task 1, `mcp__pencil__get_variables({})` returns a count of 127 ± 0 (drift baseline; will recheck at Task 5).
    - After Task 1, `mcp__pencil__batch_get({ nodeIds: ["g9oRa5"], readDepth: 1 })` returns a frame with current child count N (baseline N stored for Task 6 — expected N + 3 at plan close after Tasks 2/3/4).
    - After Task 1, all 16 baseline IDs (M7eUr, hIWuC, nwJk7, j0FxQZ, kJQmJ, u7NmaS, G0wNOc, Xs0Hs, Hs5rc, N1jo3i, O1IwyS, t40xct, ZSxZU, DnsRs, y4RORu, OLSa0) return non-null frames from `batch_get` (Phase 23-29 baseline verified intact).
    - HALT condition: if `activeEditor.file` does NOT contain `"design/Crito.pen"`, surface error to user per D-153 chain (OPEN-23-14 wrong-file mutation prevented).
  </acceptance_criteria>
  <done>Pre-flight assertion passes; baseline IDs verified; token count recorded; ready for component builds.</done>
</task>

<task type="auto">
  <name>Task 2: Build Section / TokenSwatchGrid + Section / TypeSpecimen library components</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-149 slot signatures for TokenSwatchGrid + TypeSpecimen; Claude's Discretion section on tile internal layouts)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Code Examples — Section/TokenSwatchGrid Component Build Plan 30-00 Task 1 reference example; § Don't Hand-Roll color swatch grid / type specimen tile rows)
    @.planning/phases/25-section-compound-components/25-CONTEXT.md (D-52 Pencil 2.13 typed-slot suggestion-only; D-53 enabled:true placeholder slots)
    @.planning/research/PITFALLS.md (Pitfall 2 — batch_design rejects $token refs; use literal hex/size/family values)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153 — every mutating call gets pre-flight assertion).

    **Sub-step A — Build Section / TokenSwatchGrid** inside g9oRa5 per D-149 slot signature. Use `mcp__pencil__batch_design` with Insert ops matching the structural sketch in 30-RESEARCH.md § Code Examples "Section/TokenSwatchGrid Component Build". Required structure:
    - Parent frame `Section / TokenSwatchGrid` — `layout: vertical, gap: 24 (space-semantic-stack-md literal), padding: [32, 0], width: 1200, height: fit_content, fill: "transparent", reusable: true` as direct child of `g9oRa5`.
    - heading-slot child (type:text, name "heading-slot", placeholder text "Color tokens", font Plus Jakarta Sans 24/700, fill `#141f39ff` literal per OPEN-23-13, enabled:true).
    - grid-container child (type:frame, name "grid-container", `layout: horizontal, layoutWrap: wrap, gap: 16 (space-semantic-inline-md literal), width: 1200, height: fit_content, fill: "transparent"`).
    - Default swatch-tile inside grid-container (type:frame, name "swatch-tile-default", `layout: vertical, gap: 8, padding: 12, width: 200, height: fit_content, fill: "#ffffffff"`).
    - Swatch rectangle inside default tile (type:rectangle, name "swatch", `fill: "#fdba09ff"` placeholder, `width: 176, height: 80, cornerRadius: 4 (radius-semantic-sm literal)`).
    - token-name text (Inter 14/500, fill `#141f39ff`, text "color-semantic-bg-accent" placeholder).
    - resolved-value text (Inter 12/400, fill `#52525bff`, text "#fdba09" placeholder).

    **Sub-step B — Build Section / TypeSpecimen** inside g9oRa5 per D-149 slot signature. Required structure:
    - Parent frame `Section / TypeSpecimen` — same chassis as TokenSwatchGrid (1200 width, vertical layout, transparent fill, reusable:true) as direct child of `g9oRa5`.
    - heading-slot child (text "Type Scale" placeholder, Plus Jakarta Sans 24/700).
    - grid-container child (auto-layout horizontal-wrap OR vertical per Claude's Discretion in 30-CONTEXT.md — default vertical for type specimens reads cleanest).
    - Default specimen-tile (auto-layout vertical frame, ~360px wide; padding 16; gap 4) containing:
      - sample-text child: rendered in the token-being-specimen'd; placeholder text "The quick brown fox jumps over the lazy dog" rendered Plus Jakarta Sans 32/700 (placeholder = display-heading specimen).
      - token-name caption (Inter 12/500, fill `#52525bff`, text "type-semantic-display" placeholder).
      - metadata caption (Inter 11/400, fill `#52525bff`, text "32px / 700 / 1.1 lh" placeholder).

    Reference 30-RESEARCH.md § Code Examples for full literal token values to use (OPEN-23-13 — batch_design rejects `$<token>` refs; use literal hex/size/family throughout).

    Each Insert op in batch_design returns a node id — capture all ids (`tokenSwatchGridId`, `typeSpecimenId`, plus child ids) for Task 3 (ComponentShowcase build references mono-primitive token bindings) + Task 4 (sibling notes reference these ids by name) + Task 7 (PEN-INVENTORY rows record the new ids).
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<tokenSwatchGridId>", "<typeSpecimenId>"], readDepth: 3 })` returns 2 frames, each with parent=`g9oRa5`, with the heading-slot + grid-container + default-tile child structure.
    `mcp__pencil__snapshot_layout({ rootId: "g9oRa5", problemsOnly: true })` returns either `"No layout problems."` or only text-clipping false-positives per Pitfall 3.
  </verify>
  <acceptance_criteria>
    - After Task 2, `mcp__pencil__batch_get({ nodeIds: ["<tokenSwatchGridId>"], readDepth: 3 })` returns a frame with `parent: "g9oRa5"`, `name: "Section / TokenSwatchGrid"`, `layout: "vertical"`, `width: 1200`, `reusable: true`, and at least 2 direct children (heading-slot text + grid-container frame).
    - After Task 2, `mcp__pencil__batch_get({ nodeIds: ["<typeSpecimenId>"], readDepth: 3 })` returns a frame with `parent: "g9oRa5"`, `name: "Section / TypeSpecimen"`, similar chassis (1200 width, vertical, reusable:true), with heading-slot + grid-container children.
    - After Task 2, `mcp__pencil__batch_get({ nodeIds: ["g9oRa5"], readDepth: 1 })` returns g9oRa5's child count = baseline + 2 (TokenSwatchGrid + TypeSpecimen added).
    - After Task 2, `mcp__pencil__snapshot_layout({ rootId: "g9oRa5", problemsOnly: true })` returns either `"No layout problems."` or only text-clipping false-positives on the placeholder text nodes (Pitfall 3 carry-forward — document, do not mitigate).
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran before the mutating `batch_design` call and returned `activeEditor.file` containing `"design/Crito.pen"` (D-153 — halt otherwise).
  </acceptance_criteria>
  <done>TokenSwatchGrid + TypeSpecimen library components present in g9oRa5 with declared slot signatures; node ids captured for downstream tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Build Section / ComponentShowcase library component with code-snippet-slot per D-150</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-149 + D-150 ComponentShowcase slot signature; code-snippet-slot styled with Phase 28 mono-primitive + prose-code-block tokens)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Code Examples — ComponentShowcase Instance with Code Snippet Plan 30-01 Task 6 reference; § Don't Hand-Roll component showcase block row)
    @.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md (D-105 + D-108 mono-primitive + prose-code-block token surface — code-snippet-slot literal values must match these tokens' resolved values)
    @.planning/research/PITFALLS.md (Pitfall 2 — batch_design $token rejection; use literal hex/family/size)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    **Build Section / ComponentShowcase** inside g9oRa5 per D-149 + D-150 slot signature. Required structure:
    - Parent frame `Section / ComponentShowcase` — `layout: vertical, gap: 16, padding: [24, 0], width: 1200, height: fit_content, fill: "transparent", reusable: true` as direct child of `g9oRa5`.
    - label-slot child (type:text, name "label-slot", placeholder text "Primitive / Button / Default", Inter 14/600 — caption-tier for component identifier, fill `#141f39ff` literal).
    - live-instance-slot child (type:frame, name "live-instance-slot", `layout: vertical, padding: 24, fill: "#fafafaff"` for showcase background, `cornerRadius: 4 (radius-semantic-sm literal), width: 1200, height: fit_content, enabled: true`). Inner placeholder child can be a simple rectangle or text "[component instance lives here]" — consumer overrides at instance time with a ref to the actual component.
    - code-snippet-slot child (type:text OR type:frame containing text, name "code-snippet-slot", `enabled: true` per D-150).
      - Styled with `fontFamily: "JetBrains Mono"` literal (per Phase 28 type-semantic-mono-primitive resolved value — verify against `get_variables({})` output from Task 1 if uncertain).
      - `fontSize: 14` literal (per type-semantic-mono-primitive size).
      - Background fill `#f4f4f5ff` literal (per type-semantic-prose-code-block bg per Phase 28 D-108).
      - Padding `[12, 16]` literal (prose-code-block padding per Phase 28).
      - Border 1px solid `#e4e4e7ff` literal (prose-code-block border per Phase 28).
      - `cornerRadius: 4` literal.
      - Placeholder text: `<Button variant="yellow">Click</Button>` (canonical default-variant snippet per D-150).

    IMPORTANT: Per OPEN-23-13 + Pitfall 2, batch_design rejects `$<token>` references. ALL token bindings use literal resolved values. Variant Evidence rows in Task 7 document the binding (literal value → token name) for downstream agents + code-milestone CSS export.

    If your Task 1 `get_variables({})` output disagrees with the literal mono-primitive / prose-code-block values listed here (e.g., the resolved JetBrains Mono family changed or prose-code-block bg shifted), use the resolved values from the actual `get_variables({})` output and update Task 7 PEN-INVENTORY notes accordingly.

    Capture `componentShowcaseId` + slot child ids (`labelSlotId`, `liveInstanceSlotId`, `codeSnippetSlotId`) for Task 4 sibling note + Task 7 PEN-INVENTORY Variant Evidence row.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<componentShowcaseId>"], readDepth: 3 })` returns a frame with `parent: "g9oRa5"`, with 3 children matching label-slot + live-instance-slot + code-snippet-slot.
    Code-snippet-slot has `enabled: true` per D-150.
    `mcp__pencil__snapshot_layout({ rootId: "g9oRa5", problemsOnly: true })` returns either `"No layout problems."` or only text-clipping false-positives.
  </verify>
  <acceptance_criteria>
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["<componentShowcaseId>"], readDepth: 3 })` returns a frame with `parent: "g9oRa5"`, `name: "Section / ComponentShowcase"`, `reusable: true`, and exactly 3 direct child slots named `label-slot`, `live-instance-slot`, and `code-snippet-slot`.
    - After Task 3, the `code-snippet-slot` child returns `enabled: true` per D-150 default.
    - After Task 3, the `code-snippet-slot` has fontFamily literal value matching the resolved `type-semantic-mono-primitive` family (default expected: `"JetBrains Mono"` per Phase 28 D-108; reconcile with Task 1 `get_variables({})` output if shifted).
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["g9oRa5"], readDepth: 1 })` returns g9oRa5's child count = baseline + 3 (all 3 new sections present after Tasks 2 + 3).
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran before the mutating `batch_design` call and returned active editor `design/Crito.pen` (D-153).
  </acceptance_criteria>
  <done>ComponentShowcase library component present in g9oRa5 with code-snippet-slot bound to Phase 28 mono-primitive + prose-code-block literal values; library count now 16 → 19 verified.</done>
</task>

<task type="auto">
  <name>Task 4: Ship 3 sibling Pencil notes declaring slot signatures (D-149 belt-and-suspenders)</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-149 sibling notes for 3 new sections; D-93 belt-and-suspenders convention)
    @.planning/phases/25-section-compound-components/25-CONTEXT.md (D-52 Pencil 2.13 typed-slot suggestion-only — sibling notes ship regardless)
    @.planning/research/PITFALLS.md (Pitfall 7 — sibling Pencil notes are LOAD-BEARING, NOT optional)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    Use `mcp__pencil__batch_design` to Insert 3 sibling Pencil notes at document root. Each note is `type: "note"` with the slot signature in human-readable form per Phase 25 D-52 belt-and-suspenders convention:

    **Note A — TokenSwatchGrid slot signature:**
    Name: `"Section/TokenSwatchGrid slot signature"`
    Text: `"Slot signature: heading-slot (text) + grid-of-swatch-tiles (auto-layout horizontal-wrap frame). Each tile is a child frame with swatch (color-fill rectangle) + token-name (label) + resolved-value (caption). Consumer overrides tile contents via descendants. Default tile is the consumer-replaceable template. Tile width default 200px; grid auto-layout horizontal-wrap; consumer may pivot to 240px for ~5 tiles/row vs ~6 tiles/row at 1200 content-width per 30-RESEARCH.md Open Question 4."`

    **Note B — TypeSpecimen slot signature:**
    Name: `"Section/TypeSpecimen slot signature"`
    Text: `"Slot signature: heading-slot (text) + grid-of-specimen-tiles (auto-layout vertical or horizontal-wrap frame). Each tile is a child frame with sample-text (rendered IN the token being specimen'd) + token-name (caption) + size/weight/line-height metadata (caption). Default tile renders display-heading specimen. Consumer overrides per-tile via descendants — typical token-gallery use: 1 tile per type-semantic-* role. Plan-execution may render specimens vertically (default) or horizontally based on visual density."`

    **Note C — ComponentShowcase slot signature:**
    Name: `"Section/ComponentShowcase slot signature"`
    Text: `"Slot signature: label-slot (text — component name + variant identifier) + live-instance-slot (frame — consumer Inserts a ref to the actual component) + code-snippet-slot (text — canonical default-variant snippet per D-150). code-snippet-slot is enabled:true by default per D-150 + D-53 placeholder-slot pattern. Snippet styled with type-semantic-mono-primitive (family JetBrains Mono + size 14) + type-semantic-prose-code-block (bg #f4f4f5 + border #e4e4e7 + padding 12/16 + radius 4). Per OPEN-23-13 + Pitfall 2: literal values used in batch_design; variable resolution works in Pencil UI + downstream code-milestone CSS export."`

    Note: per D-93 + D-52 chain, these notes ship REGARDLESS of typed-slot adoption — Pencil 2.13 typed-slot is suggestion-only (confirmed Plan 25-03 schema check). Sibling notes are the authoritative slot declaration for downstream agents reading the file.

    Capture sibling note ids for Task 7 PEN-INVENTORY notes-reference field.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<noteAId>", "<noteBId>", "<noteCId>"], readDepth: 1 })` returns 3 notes at document root with the expected names + text content.
  </verify>
  <acceptance_criteria>
    - After Task 4, `mcp__pencil__batch_get({ nodeIds: ["<noteAId>", "<noteBId>", "<noteCId>"], readDepth: 1 })` returns 3 note-type nodes.
    - Note A's `name` field equals `"Section/TokenSwatchGrid slot signature"` and `text` field contains the substrings `"heading-slot"`, `"grid-of-swatch-tiles"`, `"swatch"`, `"token-name"`, `"resolved-value"`.
    - Note B's `name` field equals `"Section/TypeSpecimen slot signature"` and `text` field contains the substrings `"heading-slot"`, `"sample-text"`, `"token-name"`, `"metadata"`.
    - Note C's `name` field equals `"Section/ComponentShowcase slot signature"` and `text` field contains the substrings `"label-slot"`, `"live-instance-slot"`, `"code-snippet-slot"`, `"enabled:true"`, `"type-semantic-mono-primitive"`, `"type-semantic-prose-code-block"`.
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran before the mutating `batch_design` call and returned active editor `design/Crito.pen` (D-153).
  </acceptance_criteria>
  <done>3 sibling Pencil notes ship at document root declaring slot signatures for the 3 new sections per D-149 belt-and-suspenders.</done>
</task>

<task type="auto">
  <name>Task 5: Token surface drift verification + snapshot_layout + zero-mutation baseline regression check</name>
  <files>design/Crito.pen (read-only validation)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-151 default no new tokens; D-153 pre-flight)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Validation Architecture Dim 1 + Dim 3; Pitfall 3 text-clipping false-positives)
    @.planning/research/PITFALLS.md (Pitfall 3 + Pitfall 6)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    **Step A — Token surface drift check:**
    Call `mcp__pencil__get_variables({})`. Compare count against Task 1 cached baseline (expected: 127 ± 0).
    - If count == baseline: PASS (default per D-151 — no new tokens shipped).
    - If count > baseline by 1-2: MID-PLAN TOKEN ESCALATION occurred during Tasks 2/3 (D-72/D-106/D-135 precedent). Document the new token name(s) + rationale in Task 7 PEN-INVENTORY Token Extensions row.
    - If count differs unexpectedly (drift > 2 or count < baseline): HALT and surface to user — unintended mutation or measurement error.

    **Step B — snapshot_layout document root check:**
    Call `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root.
    Expected: `"No layout problems."` (matches Phase 24-29 chain precedent).
    If text-clipping false-positives appear on the 3 new components' placeholder text nodes (Pitfall 3 carry-forward), document them as benign — no mitigation per Phase 24-29 precedent. Document the false-positive node names for Task 7 PEN-INVENTORY notes.

    **Step C — Zero-mutation baseline regression check (Dimension 3):**
    Call `mcp__pencil__batch_get({ nodeIds: ["M7eUr", "hIWuC", "nwJk7", "j0FxQZ", "kJQmJ", "u7NmaS", "G0wNOc", "Xs0Hs", "Hs5rc", "N1jo3i", "O1IwyS", "t40xct", "ZSxZU", "DnsRs", "y4RORu", "OLSa0", "RpGbe", "avgor", "t67DU6"], readDepth: 1 })`.
    Compare each frame's child count + name against Task 1 baseline.
    - g9oRa5 child count: baseline + 3 (TokenSwatchGrid + TypeSpecimen + ComponentShowcase added — EXPECTED MUTATION).
    - All other 19 baseline IDs: child counts + names UNCHANGED.
    - If any baseline ID mutated unexpectedly: HALT and surface to user — regression detected.

    Record all 3 checks' outcomes in working memory for Task 6 SUMMARY narrative.
  </action>
  <verify>
    `mcp__pencil__get_variables({})` returns count matching Task 1 baseline (default 127; may be 128-129 if mid-plan token escalation occurred per D-72/D-106/D-135).
    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `"No layout problems."` at document root (text-clipping false-positives on placeholder text nodes per Pitfall 3 are benign and documented).
    `mcp__pencil__batch_get({ nodeIds: [...baseline...], readDepth: 1 })` returns 19 baseline frames with unchanged child counts (except g9oRa5 +3).
  </verify>
  <acceptance_criteria>
    - After Task 5, `mcp__pencil__get_variables({})` returns count == Task 1 baseline (default 127) OR baseline + 1-2 (only if mid-plan token escalation occurred per D-72/D-106/D-135 — must be documented in Task 7).
    - After Task 5, `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root returns the literal string `"No layout problems."` (text-clipping false-positives on placeholder text nodes inside the 3 new components are benign per Pitfall 3 carry-forward).
    - After Task 5, `mcp__pencil__batch_get({ nodeIds: ["RpGbe", "avgor", "t67DU6"], readDepth: 1 })` returns child counts UNCHANGED from Task 1 baseline (RpGbe = 6 children per Phase 23 Plan 23-05; avgor + t67DU6 child counts unchanged).
    - After Task 5, `mcp__pencil__batch_get({ nodeIds: ["g9oRa5"], readDepth: 1 })` returns child count == Task 1 baseline + 3 (TokenSwatchGrid + TypeSpecimen + ComponentShowcase). If +2 or +4 (instead of +3): HALT.
    - After Task 5, all 16 Phase 23-29 baseline component IDs (Button/Input/Badge/Icon/Header/Footer/CTA/NavBack/TagFilter/RelatedPosts/ResultsMetrics/RelatedProjects/Card/BlogCard/ProjectCard/CheckboxGroup) return non-null + structurally unchanged from Task 1 baseline.
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran before the validation calls and returned active editor `design/Crito.pen` (D-153 — even read-only validation runs the assertion per D-153 chain).
  </acceptance_criteria>
  <done>Token surface drift = 0 net-new (or documented escalation); document root snapshot clean; Phase 23-29 baseline unmutated; ready to commit PEN-INVENTORY extension.</done>
</task>

<task type="auto">
  <name>Task 6: PEN-INVENTORY extension — NEW Frames Inventory + Variant Evidence + OPEN-30-NN seed + commit</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    @.planning/research/PEN-INVENTORY.md (existing structure: Frames Inventory table, Variant Evidence sections per phase, Open Flags sections per phase, Token Extensions if applicable)
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-154 PEN-INVENTORY extension pattern)
    @.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md (D-141 prior-phase extension pattern reference)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Code Examples — PEN-INVENTORY In-Place Reclassification reference; uses similar Frames Inventory row format)
  </read_first>
  <action>
    Read PEN-INVENTORY.md current state. Append (or edit-in-place per existing structure) the following sections per D-154 Phase 30 Plan 30-00 contributions:

    **Sub-step A — NEW Frames Inventory rows** for the 3 new sections (append to Frames Inventory table, in a Phase 30 Plan 30-00 sub-section if the table is split by phase, OR appended at the end if flat):

    ```
    | Section / TokenSwatchGrid | <tokenSwatchGridId from Task 2> | reusable-section-component | parent: g9oRa5 (_Components / Sections) | 3 (heading-slot + grid-container + default-tile) | factored:1 | n/a (Phase 30 plan 30-00 — new library entry per D-149) | Slot signature note: <noteAId from Task 4>; Variant Evidence Phase 30 row |
    | Section / TypeSpecimen | <typeSpecimenId from Task 2> | reusable-section-component | parent: g9oRa5 | 3 (heading-slot + grid-container + default-tile) | factored:1 | n/a (Phase 30 plan 30-00 — new library entry per D-149) | Slot signature note: <noteBId>; Variant Evidence Phase 30 row |
    | Section / ComponentShowcase | <componentShowcaseId from Task 3> | reusable-section-component | parent: g9oRa5 | 3 (label-slot + live-instance-slot + code-snippet-slot) | factored:1 | n/a (Phase 30 plan 30-00 — new library entry per D-149 + D-150) | Slot signature note: <noteCId>; Variant Evidence Phase 30 row (code-snippet-slot token binding) |
    ```

    **Sub-step B — Variant Evidence (Phase 30) section** (create new sub-section if doesn't exist):

    Document:
    - TokenSwatchGrid slot signature evidence (heading-slot text default "Color tokens"; grid-container layoutWrap; default-tile dimensions 200×fit_content; consumer override mechanism via descendants).
    - TypeSpecimen slot signature evidence (heading-slot text default "Type Scale"; default-tile internal layout vertical sample-text → token-name → metadata; tile width ~360 default).
    - ComponentShowcase slot signature evidence (label-slot caption tier; live-instance-slot showcase bg #fafafa + radius-semantic-sm; code-snippet-slot enabled:true per D-150).
    - **ComponentShowcase code-snippet-slot token binding** (Phase 30 D-150 + Phase 28 D-108): literal values used per OPEN-23-13 → font family `"JetBrains Mono"` resolves to `type-semantic-mono-primitive` family; font size 14 resolves to `type-semantic-mono-primitive` size; bg `#f4f4f5` resolves to `type-semantic-prose-code-block` background; border `#e4e4e7` resolves to `type-semantic-prose-code-block` border; padding `[12, 16]` resolves to `type-semantic-prose-code-block` padding; cornerRadius 4 resolves to `radius-semantic-sm`. Phase 30 ComponentShowcase is the SECONDARY validation surface for `type-semantic-prose-code-block` per D-150 + Phase 28 D-110 'provisional' flag verification chain.

    **Sub-step C — IF mid-plan token escalation occurred** (Task 5 Step A revealed count > baseline): add Token Extensions (Phase 30 plan 30-00) row(s) per D-154. Default expectation: NO mid-plan token escalation; this sub-step is conditional.

    **Sub-step D — Open Flags — Phase 30 (OPEN-30-NN)** seed section (new section per D-154):

    Pre-populate likely-candidates from 30-CONTEXT D-154:
    - **OPEN-30-01 (seed):** Utilities iso-glow STUB labeling — Plan 30-01 calibration gate confirms or escalates per D-144 / Open Question 3.
    - **OPEN-30-02 (seed):** Utilities iso-rotate STUB labeling — Plan 30-01 calibration gate confirms.
    - **OPEN-30-03 (seed, conditional):** `type-semantic-prose-code-block` provisional-flag SECONDARY-validation outcome — populated post-Plan-30-01 calibration gate per D-150 + Phase 28 D-110 verification chain.

    Real OPEN-30-NN entries get appended as Plan 30-01 calibration gate surfaces them.

    **Sub-step E — Carry-forward status updates:**
    - OPEN-25-07 partial-resolution status note: Phase 30 Component gallery instances Card (t40xct) + BlogCard (ZSxZU) + ProjectCard (DnsRs) as 4th cross-phase consumer of the sibling-component pattern (after Phase 25 single-Card + Phase 28 BlogCard + Phase 29 ProjectCard). Final disposition deferred to Phase 32 close.

    Save PEN-INVENTORY.md.

    **Sub-step F — Commit** (per planning workflow `commit_docs: true` — check `.planning/config.json`):
    `git add design/Crito.pen .planning/research/PEN-INVENTORY.md .planning/phases/30-design-system-reference-reconstruction/30-00-PLAN.md`
    `git commit -m "exec(30-00): Tasks 2-6 — Section/TokenSwatchGrid + TypeSpecimen + ComponentShowcase library (library 16→19) + PEN-INVENTORY extension"`

    (The plan execute workflow will handle final SUMMARY commit at plan close — this commit is Tasks 2-6 ride-along per Phase 23-29 carry-forward convention.)
  </action>
  <verify>
    `grep -c "Section / TokenSwatchGrid" .planning/research/PEN-INVENTORY.md` returns ≥ 1.
    `grep -c "Section / TypeSpecimen" .planning/research/PEN-INVENTORY.md` returns ≥ 1.
    `grep -c "Section / ComponentShowcase" .planning/research/PEN-INVENTORY.md` returns ≥ 1.
    `grep -c "OPEN-30-" .planning/research/PEN-INVENTORY.md` returns ≥ 1 (seeded entries).
    `git log -1 --format=%s` returns a commit message starting with `exec(30-00):`.
  </verify>
  <acceptance_criteria>
    - After Task 6, `grep -q "Section / TokenSwatchGrid" .planning/research/PEN-INVENTORY.md` exits 0 (row present in Frames Inventory).
    - After Task 6, `grep -q "Section / TypeSpecimen" .planning/research/PEN-INVENTORY.md` exits 0.
    - After Task 6, `grep -q "Section / ComponentShowcase" .planning/research/PEN-INVENTORY.md` exits 0.
    - After Task 6, `grep -q "Variant Evidence" .planning/research/PEN-INVENTORY.md && grep -q "Phase 30" .planning/research/PEN-INVENTORY.md` exits 0 (Phase 30 Variant Evidence sub-section ships).
    - After Task 6, `grep -q "OPEN-30-01" .planning/research/PEN-INVENTORY.md` exits 0 (Open Flags section seeded; entries may be re-numbered if there are conflicts — accept any OPEN-30-NN seed entry presence).
    - After Task 6, `git log -1 --format=%s` returns a commit subject line starting with `exec(30-00):`.
    - After Task 6, `git status --porcelain` returns no unstaged changes to `.planning/research/PEN-INVENTORY.md` (changes committed).
  </acceptance_criteria>
  <done>PEN-INVENTORY.md extended with 3 NEW Frames Inventory rows + Variant Evidence Phase 30 sub-section + OPEN-30-NN seed; changes committed per planning workflow.</done>
</task>

</tasks>

<verification>
**Plan 30-00 overall verification (run at plan close per 30-VALIDATION.md Per-Task Verification Map):**

1. `mcp__pencil__get_editor_state({ include_schema: false })` returns `activeEditor.file` containing `"design/Crito.pen"` (D-153).
2. `mcp__pencil__batch_get({ nodeIds: ["<tokenSwatchGridId>", "<typeSpecimenId>", "<componentShowcaseId>"], readDepth: 3 })` returns 3 frames, each with `parent: "g9oRa5"`, declared slot signatures, and the structural depth declared in their respective Task 2/3 actions.
3. `mcp__pencil__batch_get({ nodeIds: ["<noteAId>", "<noteBId>", "<noteCId>"], readDepth: 1 })` returns 3 sibling notes at document root with the expected names + slot signature text content per Task 4.
4. `mcp__pencil__get_variables({})` returns count matching Task 1 baseline (default 127; up to +2 if mid-plan token escalation occurred per D-72/D-106/D-135).
5. `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root returns `"No layout problems."` (text-clipping false-positives on the 3 new components' placeholder text nodes are benign per Pitfall 3 — document, do not mitigate).
6. `mcp__pencil__batch_get({ nodeIds: [...19 baseline IDs...], readDepth: 1 })` returns all baseline IDs unchanged (g9oRa5 child count +3 expected; all others +0).
7. `grep -q "Section / TokenSwatchGrid" .planning/research/PEN-INVENTORY.md && grep -q "Section / TypeSpecimen" .planning/research/PEN-INVENTORY.md && grep -q "Section / ComponentShowcase" .planning/research/PEN-INVENTORY.md` exits 0.
8. `git log --oneline -5` shows a Plan 30-00 commit with subject starting `exec(30-00):` per Phase 23-29 carry-forward convention.
</verification>

<success_criteria>
Plan 30-00 succeeds when:
- [ ] All 6 tasks complete with pre-flight `get_editor_state` assertion passing before each mutating call (D-153)
- [ ] Library count moves 16 → 19 (verified via g9oRa5 child count delta)
- [ ] 3 new section components present in g9oRa5 with declared slot signatures matching D-149 + D-150
- [ ] 3 sibling Pencil notes ship at document root per D-149 belt-and-suspenders (D-93 + D-52 + Pitfall 7 honored)
- [ ] ComponentShowcase code-snippet-slot binds to literal values for Phase 28 mono-primitive + prose-code-block tokens (Variant Evidence row documents the binding per OPEN-23-13 dual-track convention)
- [ ] Token surface drift = 0 net-new (default per D-151) OR documented mid-plan token escalation per D-72/D-106/D-135 precedent
- [ ] All Phase 23-29 baseline component IDs structurally unchanged (Dimension 3 regression check passes)
- [ ] PEN-INVENTORY.md extended with 3 NEW Frames Inventory rows + Variant Evidence Phase 30 sub-section + OPEN-30-NN seed
- [ ] Document root `snapshot_layout` returns `"No layout problems."` (text-clipping false-positives documented as benign)
- [ ] All changes committed per planning workflow (`commit_docs: true`)
- [ ] Plan 30-00 closes WITHOUT a user-calibration gate per D-152 + D-86 + D-102 + D-124 + D-139 chain (foundation work; no user verification step at the library layer)
</success_criteria>

<output>
After completion, create `.planning/phases/30-design-system-reference-reconstruction/30-00-SUMMARY.md` documenting:
- Pre-flight assertion outcome at each mutating call (Tasks 2, 3, 4, 5, 6)
- Captured node ids for the 3 new sections (TokenSwatchGrid + TypeSpecimen + ComponentShowcase) + 3 sibling notes
- Token surface drift outcome (default 0 net-new; document mid-plan escalation if it occurred)
- snapshot_layout document-root outcome + per-frame text-clipping false-positives (Pitfall 3 documented as benign per phase precedent)
- Baseline regression check outcome (Phase 23-29 IDs unmutated except g9oRa5 +3)
- PEN-INVENTORY rows added (Frames Inventory + Variant Evidence + OPEN-30-NN seed)
- Tier-2 fallback readiness statement for Plan 30-01 calibration gate (~58-instance density expected per Pitfall 1)
- Frontmatter per Phase 23-29 SUMMARY conventions: `affects`, `subsystem`, `requires`, `provides`, `tech_stack.added`
</output>
