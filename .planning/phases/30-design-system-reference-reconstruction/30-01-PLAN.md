---
phase: 30-design-system-reference-reconstruction
plan: 01
type: execute
wave: 1
depends_on:
  - "30-00"
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-07
  - PAGE-09
  - PAGE-11
  - VALID-01
  - VALID-02
  - VALID-03
  - COMP-05
  - COMP-06

must_haves:
  truths:
    - "A `Design system` page frame exists in design/Crito.pen at 1440 width as a top-level frame"
    - "The page frame anchors on Phase 29 Project frame s5k41l via find_empty_space_on_canvas nodeId pattern per § 10.4"
    - "Section/Header (G0wNOc) instances at the TOP of the page frame as the first child (no override per D-77)"
    - "Section/Footer (Xs0Hs) instances at the BOTTOM of the page frame as the last child (no override per D-77)"
    - "The page frame contains 7 child sections in vertical-stack order: Header → page-intro → Colors → Typography → Components → Utilities → Footer (D-145 + D-146)"
    - "page-intro section ships v1.3 verbatim content per D-143 (H1 + body + JSON-link secondary line) + 2 sibling Pencil notes (noindex + JSON endpoint)"
    - "Colors section contains TokenSwatchGrid instances grouped per v1.3 sub-grouping (Primary Accent / Text Variants / Neutral / Usage Guidelines)"
    - "Typography section contains TypeSpecimen instances (or inline tables) for Font Families + Type Scale (+ Weights + Line Heights)"
    - "Components section contains ~16 ComponentShowcase instances ordered Primitives → Compounds → Sections per D-148"
    - "Utilities section ships 3 sub-sections (iso-shadow APPROXIMATE + iso-glow STUB + iso-rotate STUB per D-144 + Open Q3) + sibling CSS notes"
    - "Sibling Pencil notes ship on each H2 frame per D-144 (v1.3 anchor IDs: #introduction #colors #typography #components #utilities)"
    - "Plan-close calibration AskUserQuestion returns APPROVE (joel-only branch § 4.5 format; token-usage check against RpGbe)"
    - "On APPROVE: PEN-INVENTORY line 77 placeholder row reclassified in-place to real reconstructed `Design system` frame row (D-128 + D-154 pattern)"
    - "PAGE-11 INERT — no Crito source raster to hide per § 4.3 joel-only branch"
    - "Token surface drift = 0 net-new (no new tokens shipped in composition plan)"
    - "All Phase 23-29 baseline component IDs + page frame IDs remain unchanged (Dimension 3 regression)"
    - "D-142: Utilitarian internal-docs register — no hero band, no decorative bg fills, no Section/CTA marketing close (Crito-vocab section components only; honest to v1.3 noindex internal-reference framing)"
    - "D-147: Token gallery shows ALL ~107 tokens exhaustively (Colors + Typography + Spacing + Radii); Component gallery is CURATED to 1-2 variants per component per D-148"
    - "D-152: Plan 30-01 closes WITH single user-calibration AskUserQuestion gate per CALIBRATION-PROTOCOL § 4.5 joel-only branch (page-frame composition layer is where the user gate fires; foundation layer 30-00 closes without gate)"
    - "D-153: Every Pencil-mutating task in Plan 30-01 first calls mcp__pencil__get_editor_state and asserts active editor == design/Crito.pen (Task 1 + precondition on Tasks 2-7)"
  artifacts:
    - path: "design/Crito.pen — Design system page frame"
      provides: "Top-level page frame at 1440 width, layout vertical, gap 0, padding 0, alignItems center, fill color-semantic-bg-page literal"
      contains: "7 child sections in vertical-stack order; Header + Footer pure-ref instances per D-77; fit_content height after Task 9 settle"
    - path: "design/Crito.pen — page-intro sub-section"
      provides: "v1.3 verbatim H1 + body + JSON-link secondary line per D-143"
      contains: "H1 'Design System' + body 'Internal reference for Joel and AI coding agents...' + 'Machine-readable format: /design-system.json' + 2 sibling notes (noindex preservation + JSON endpoint preservation)"
    - path: "design/Crito.pen — Colors sub-section"
      provides: "Section/TokenSwatchGrid instances for v1.3 sub-groupings (Primary Accent / Text Variants / Neutral / Usage Guidelines)"
      contains: "4 TokenSwatchGrid instances OR a coherent token-gallery composition per Open Question 1 RpGbe probe outcome at Task 1"
    - path: "design/Crito.pen — Typography sub-section"
      provides: "Section/TypeSpecimen instances or inline tables for Font Families / Type Scale / Font Weights / Line Heights"
      contains: "TypeSpecimen instances for token-renderable surfaces (Font Families + Type Scale); inline tables OR additional TypeSpecimen for non-renderable metadata (Font Weights + Line Heights)"
    - path: "design/Crito.pen — Components sub-section"
      provides: "~16 Section/ComponentShowcase instances per D-148 full v2.0 library coverage"
      contains: "Primitives sub-heading (Button Default M7eUr + Button Secondary hIWuC + Input Default nwJk7 + Focus + Error + Badge Default j0FxQZ + Badge Outline kJQmJ + Icon u7NmaS + 16/20/32 size variants) + Compounds sub-heading (Card t40xct + BlogCard ZSxZU + ProjectCard DnsRs + CheckboxGroup) + Sections sub-heading (Header G0wNOc + Footer Xs0Hs + CTA Hs5rc + NavBack N1jo3i + TagFilter O1IwyS + RelatedPosts + ResultsMetrics y4RORu + RelatedProjects OLSa0)"
    - path: "design/Crito.pen — Utilities sub-section"
      provides: "3 sub-sections per D-144 with visual approximations + sibling CSS notes"
      contains: "iso-shadow APPROXIMATE (offset filled shape behind Card placeholder) + iso-glow STUB + iso-rotate STUB + 3 sibling notes documenting src/styles/global.css mechanics"
    - path: ".planning/research/PEN-INVENTORY.md (line 77)"
      provides: "In-place reclassification of (joel-only: Design System) placeholder → real Design system frame row"
      contains: "frame_id from Task 2; status_counts: factored:7; reconstruction_priority n/a; open_flag_ids per Plan 30-01 calibration outcome"
  key_links:
    - from: "Design system page frame"
      to: "Phase 29 Project frame s5k41l (canvas (7547.27, −4111.55))"
      via: "find_empty_space_on_canvas nodeId anchor per § 10.4"
      pattern: "FindEmptySpace({ width: 1440, direction: \"right\", padding: 80, nodeId: \"s5k41l\" })"
    - from: "Design system page frame top child"
      to: "Section/Header G0wNOc"
      via: "batch_design Insert type:ref ref:G0wNOc, no descendants override (D-77)"
      pattern: "I(<designSystemPageFrameId>, { type: \"ref\", ref: \"G0wNOc\", name: \"Section/Header instance\" })"
    - from: "Design system page frame bottom child"
      to: "Section/Footer Xs0Hs"
      via: "batch_design Insert type:ref ref:Xs0Hs, no descendants override (D-77)"
      pattern: "I(<designSystemPageFrameId>, { type: \"ref\", ref: \"Xs0Hs\", name: \"Section/Footer instance\" })"
    - from: "Colors sub-section TokenSwatchGrid instances"
      to: "_Tokens & Foundations RpGbe (PRIMARY calibration target per D-62)"
      via: "Tile bindings to semantic color tokens — calibration AskUserQuestion verifies bindings match RpGbe canonical surface"
      pattern: "TokenSwatchGrid descendants override swatch fill + token-name + resolved-value per swatch"
    - from: "Components sub-section ComponentShowcase instances"
      to: "Phase 23-29 library entries (M7eUr, hIWuC, nwJk7, j0FxQZ, kJQmJ, u7NmaS, G0wNOc, Xs0Hs, Hs5rc, N1jo3i, O1IwyS, t40xct, ZSxZU, DnsRs, y4RORu, OLSa0, plus CheckboxGroup + RelatedPosts)"
      via: "ComponentShowcase live-instance-slot consumer-Inserts ref to actual component; code-snippet-slot text styled with Phase 28 mono-primitive + prose-code-block tokens"
      pattern: "I(<componentShowcaseInstanceId>/<liveInstanceSlotId>, { type: \"ref\", ref: \"M7eUr\" })"
    - from: "PEN-INVENTORY line 77 placeholder row"
      to: "Real Design system frame row (post-APPROVE in-place reclassification per D-128 + D-154)"
      via: "Edit-in-place markdown table row update"
      pattern: "Line 77 row name change + frame_id field populated + status_counts: factored:7 + reconstruction_priority: n/a"
---

<objective>
Plan 30-01 composes the `Design system` page frame inside `design/Crito.pen` as the FIFTH production use of the CALIBRATION-PROTOCOL § 4 joel-only-no-crito-ref branch (after Plan 26-01 FAQ + Plan 26-02 404 + Plan 27-01 Thank-you + Plan 28-03 Tag), CLOSING the PEN-INVENTORY's last `joel-only-no-crito-ref` placeholder row (line 77 `(joel-only: Design System)`) via the Phase 29 D-128 in-place reclassification pattern. The page frame instances Plan 30-00's 3 new Section components + Phase 23-29 library entries to ship a token gallery + component gallery + utilities section, ending with a single user-calibration AskUserQuestion gate per § 4.5 (joel-only branch token-usage format).

Purpose: Ship Phase 30's success criteria 1-4 (Design-system reference page frame exists with token + component gallery; Phase 25 Section/Header + Section/Footer instanced as cross-page consistency proof; per-section fidelity labels + side-by-side calibration artifact + user spot-check before phase close). This is the broadest single-plan calibration scope in v2.0 — ~58 visual artifacts across 5 sections — and IS the v2.0 library reference (the only IN-SCOPE surface that exercises every primitive + compound + section component built in Phases 23-29).

Output: 1 new top-level page frame in `design/Crito.pen` (`Design system`) + PEN-INVENTORY in-place reclassification of line 77 placeholder + Variant Evidence rows + any plan-surfaced OPEN-30-NN flags. PAGE-11 INERT (no Crito source raster to remove per § 4.3 joel-only branch).

Single user-calibration gate at Task 14 (joel-only branch § 4.5 format — token-usage check against `_Tokens & Foundations` RpGbe per D-62). Plan-execution may invoke § 6.4 Tier-2 stale-cache fallback for inline screenshots given the HIGH-likelihood prediction on ~58-instance density (Pitfall 1).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md

# Phase 30 LOCKED decisions + research + validation (MANDATORY)
@.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md
@.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md
@.planning/phases/30-design-system-reference-reconstruction/30-VALIDATION.md

# Calibration protocol — § 4 joel-only branch (PRIMARY calibration framework for this plan)
@.planning/research/CALIBRATION-PROTOCOL.md

# Plan 30-00 foundation (depends_on) — node ids for the 3 new sections
@.planning/phases/30-design-system-reference-reconstruction/30-00-PLAN.md

# Single source of truth for baseline IDs + line 77 placeholder
@.planning/research/PEN-INVENTORY.md

# Open flags + pitfalls + stack
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

# Phase 29 — in-place reclassification pattern (D-128 → D-154 carry-forward) + s5k41l anchor
@.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md

# Phase 28 — prose tokens (D-150 code-snippet consumer); BlogCard + TagFilter + RelatedPosts library
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md

# Phase 26 — joel-only branch PRIMARY ancestor; § 4 calibration protocol established here
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md

# Phase 25 — D-77 Header/Footer pure-ref pattern (no override carry-forward); component IDs
@.planning/phases/25-section-compound-components/25-CONTEXT.md

# Phase 24 — primitive component IDs (M7eUr, hIWuC, nwJk7, j0FxQZ, u7NmaS); D-29 Icon size taxonomy
@.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md

# Phase 23 — _Tokens & Foundations RpGbe (PRIMARY calibration target)
@.planning/phases/23-audit-token-foundation/23-CONTEXT.md

# v1.3 content extraction source (read directly via Read tool — NOT a .pen file)
@src/pages/design-system.astro
@src/styles/global.css

# Project instructions
@./CLAUDE.md
</context>

<threat_model>
Phase 30 ships ZERO `src/` code changes (v2.0 `.pen`-file-only milestone per PROJECT.md + REQUIREMENTS.md Out of Scope). No HTTP endpoints, no user input handling, no auth flows, no database operations, no external API calls. Pencil MCP server runs locally with file-system access to `design/Crito.pen`. Threat surface: NONE (trivially empty). Pre-flight `get_editor_state` assertion (D-153) prevents wrong-file mutation (OPEN-23-14 carry-forward) — this is a correctness gate, not a security threat. PAGE-11 INERT per § 4.3 joel-only branch — no raster-hide operation runs in this plan (would require user APPROVE first per Pitfall 4; INERT branch has no raster to hide regardless).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight + baseline read + RpGbe sub-grouping probe (Open Question 1 resolution)</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-153 pre-flight; Claude's Discretion — Token-gallery sub-grouping mirrors RpGbe sub-groupings)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Open Questions Q1 — RpGbe sub-grouping for token-gallery alignment; § Validation Architecture Dim 3 baseline regression list)
    @.planning/phases/23-audit-token-foundation/23-CONTEXT.md (Phase 23 D-19 RpGbe canonical sub-grouping — Colors / Typography / Spacing Scale / Radius / Dark Mode Deferred)
    @.planning/phases/30-design-system-reference-reconstruction/30-00-PLAN.md (must_haves — depends_on node ids: tokenSwatchGridId, typeSpecimenId, componentShowcaseId from 30-00 close)
  </read_first>
  <action>
    1. Pre-flight: `mcp__pencil__get_editor_state({ include_schema: false })` — assert `activeEditor.file` contains `"design/Crito.pen"` (D-153). HALT on mismatch.

    2. Read Plan 30-00 SUMMARY (when published) for the 3 new section IDs: `<tokenSwatchGridId>`, `<typeSpecimenId>`, `<componentShowcaseId>` + 3 sibling note IDs. If SUMMARY not yet published, derive via `mcp__pencil__batch_get({ nodeIds: ["g9oRa5"], readDepth: 2 })` and find children with names `"Section / TokenSwatchGrid"`, `"Section / TypeSpecimen"`, `"Section / ComponentShowcase"`.

    3. **RpGbe sub-grouping probe (Open Question 1 resolution):**
    Call `mcp__pencil__batch_get({ nodeIds: ["RpGbe"], readDepth: 3 })`. Inspect the child structure. Phase 23 Plan 23-05 baseline: 6 children (Header, 1. Colors with 12 primitive + 14 semantic swatches, 2. Typography with 7 specimens, 3. Spacing Scale, 4. Radius, 5. Dark Mode — Deferred).
    - If RpGbe sub-grouping is well-structured + matches v1.3 sub-groupings (Primary Accent / Text / Neutral / Usage; Family / Scale / Weight / LH): MIRROR it in Plan 30-01 token-gallery sub-sections (Tasks 5-6).
    - If RpGbe sub-grouping is partial (heading-3 / heading-4 / mono-primitive / prose tokens NOT yet rendered as specimens): proceed with v1.3 sub-groupings per 30-CONTEXT D-144 + `<code_context>` defaults. Plan-close SUMMARY documents the partial-state observation.
    - If RpGbe structure is unclear or mutated unexpectedly: HALT and surface to user.

    4. Baseline regression read — call `mcp__pencil__batch_get` on all Phase 23-29 baseline IDs (4 library parents + 16 components + page frames). Cache child counts + names for Task 11 zero-mutation diff:
    - **Library parents (4):** RpGbe, avgor, t67DU6, g9oRa5
    - **Primitives (4):** M7eUr (Button/Default), hIWuC (Button/Secondary), nwJk7 (Input/Default), j0FxQZ (Badge/Default), kJQmJ (Badge/Outline), u7NmaS (Icon/24)
    - **Compounds (4):** t40xct (Card), ZSxZU (BlogCard), DnsRs (ProjectCard), plus CheckboxGroup id (read from PEN-INVENTORY or g9oRa5/t67DU6 child enumeration)
    - **Sections (8):** G0wNOc (Header), Xs0Hs (Footer), Hs5rc (CTA), N1jo3i (NavBack), O1IwyS (TagFilter), RelatedPosts id, y4RORu (ResultsMetrics), OLSa0 (RelatedProjects)
    - **Page frames (Phase 23-29):** b7Hgy (FAQ), csXky (404), XsDab (Thank-you), n0QqTd (Contact), EDAf1, aQ8FL (Blog Post), FUctJ (Tag), SCcln, s5k41l (Project), cl8tt, w1m3x, DzqTm, cYlRH, Y2isa, ujMLJ — confirm IDs from PEN-INVENTORY (some may be table-only references; capture actual frame ids that exist in the file)

    5. Token surface read — `mcp__pencil__get_variables({})` — record count (expected: matches Plan 30-00 close = 127 default; up to +2 if mid-plan token escalation occurred in 30-00 per D-72/D-106/D-135 precedent). Cache for Task 11 drift check.

    6. **Confirm s5k41l anchor existence + canvas position** — `mcp__pencil__batch_get({ nodeIds: ["s5k41l"], readDepth: 1 })`. Expected canvas position: (7547.27, −4111.55). This is the FindEmptySpace anchor for Task 2 per § 10.4 + D-145.
  </action>
  <verify>
    `mcp__pencil__get_editor_state({ include_schema: false })` returns activeEditor path `"design/Crito.pen"`.
    `mcp__pencil__batch_get({ nodeIds: ["RpGbe"], readDepth: 3 })` returns RpGbe with ≥ 5 children (Phase 23 baseline 6 children; ± Phase 24-29 augmentation).
    `mcp__pencil__batch_get({ nodeIds: ["s5k41l"], readDepth: 1 })` returns a frame at canvas position approximately (7547.27, −4111.55) ± tolerance.
    `mcp__pencil__get_variables({})` returns count matching Plan 30-00 close (default 127).
  </verify>
  <acceptance_criteria>
    - After Task 1, `mcp__pencil__get_editor_state({ include_schema: false })` returns active editor `design/Crito.pen` (D-153 satisfied — HALT otherwise).
    - After Task 1, `mcp__pencil__batch_get({ nodeIds: ["RpGbe"], readDepth: 3 })` returns a frame with `children` array of length ≥ 5 — sub-grouping structure observed + documented in working memory + Plan 30-01 SUMMARY notes (Open Question 1 resolution per § Open Questions).
    - After Task 1, `mcp__pencil__batch_get({ nodeIds: ["s5k41l"], readDepth: 1 })` returns a frame with `x` near 7547.27 and `y` near −4111.55 (Phase 29 Project frame anchor existence confirmed for Task 2 § 10.4 FindEmptySpace call).
    - After Task 1, `mcp__pencil__get_variables({})` returns count matching Plan 30-00 close baseline (default 127; document discrepancy if differs).
    - After Task 1, the 3 new section IDs from Plan 30-00 (`<tokenSwatchGridId>`, `<typeSpecimenId>`, `<componentShowcaseId>`) are captured + non-null.
    - After Task 1, all baseline IDs (RpGbe, avgor, t67DU6, g9oRa5, M7eUr, hIWuC, nwJk7, j0FxQZ, kJQmJ, u7NmaS, t40xct, ZSxZU, DnsRs, G0wNOc, Xs0Hs, Hs5rc, N1jo3i, O1IwyS, y4RORu, OLSa0, and page frames b7Hgy/csXky/XsDab/n0QqTd/aQ8FL/FUctJ/SCcln/s5k41l/cl8tt/w1m3x/DzqTm/cYlRH/Y2isa) return non-null + child counts cached for Task 11 zero-mutation regression check.
  </acceptance_criteria>
  <done>Pre-flight passes; RpGbe sub-grouping probed and documented; baseline cached; s5k41l anchor confirmed; ready for page-frame chassis build.</done>
</task>

<task type="auto">
  <name>Task 2: Build page-frame chassis (1440 width, vertical layout, placeholder:true, FindEmptySpace nodeId:s5k41l anchor)</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-145 linear long-scroll architecture; D-146 1440 outer / 1200 content-width conventions)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Code Examples — find_empty_space_on_canvas with nodeId Anchor Plan 30-01 Task 1; § Architecture Patterns — Pattern 2 nodeId anchor; Pattern 3 fit_content settle)
    @.planning/research/CALIBRATION-PROTOCOL.md (§ 10.4 FindEmptySpace nodeId anchor pattern)
    @.planning/research/PITFALLS.md (Pitfall 2 — batch_design rejects $token refs; use literal hex)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    Build the page-frame chassis via `mcp__pencil__batch_design` per the structural sketch in 30-RESEARCH.md § Code Examples "find_empty_space_on_canvas with nodeId Anchor (Plan 30-01 Task 1)". Required structure:

    ```js
    batch_design({ operations: `
      pos = FindEmptySpace({
        width: 1440,
        height: 2400,  // estimate; will switch to fit_content at Task 10
        direction: "right",
        padding: 80,
        nodeId: "s5k41l"  // Phase 29 Project frame anchor — directs placement to page-frame row y ≈ −4111
      });
      designSystemPageFrame = I(document, {
        type: "frame",
        name: "Design system",
        x: pos.x,
        y: pos.y,
        width: 1440,
        height: 2400,
        layout: "vertical",
        gap: 0,         // sub-section frames carry their own internal padding/gap per D-146
        padding: 0,
        alignItems: "center",  // sub-section frames width 1200 center inside 1440 outer
        fill: "#ffffffff",  // color-semantic-bg-page literal per OPEN-23-13 + D-146 Crito-vocab default
        placeholder: true  // cleared at Task 10 fit_content settle
      });
    ` });
    ```

    Capture `designSystemPageFrameId` — required for Tasks 3-10 child Insert operations.

    Per § 10.4 + Pitfall 2: the `nodeId: "s5k41l"` anchor is MANDATORY — without it, FindEmptySpace picks the library row (y ≈ −11711) instead of the page-frame row (y ≈ −4111). Without anchor: HALT. The literal fill `#ffffffff` is the resolved value of `color-semantic-bg-page` per OPEN-23-13 — Variant Evidence row at Task 15 documents the binding.

    Per D-146: `layout: vertical, gap: 0, padding: 0, alignItems: center`. Sub-section frames at 1200 content-width auto-center inside the 1440 outer per `alignItems: center`. Between-section rhythm flows from each sub-section frame's own internal padding (consistent with Phase 26-29 chain — no outer gap on page frame).

    Per D-146: `placeholder: true` during build — cleared at Task 10 (`fit_content` settle) so the page frame doesn't render its (estimated) 2400 height during construction. Phase 26-29 chain established the placeholder pattern (Plan 26-02 Task 4 contribution).
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<designSystemPageFrameId>"], readDepth: 1 })` returns a top-level frame named `"Design system"` with `width: 1440`, `layout: "vertical"`, `alignItems: "center"`, `placeholder: true`, located on the page-frame row (y near −4111 per § 10.4 anchor).
  </verify>
  <acceptance_criteria>
    - After Task 2, `mcp__pencil__batch_get({ nodeIds: ["<designSystemPageFrameId>"], readDepth: 1 })` returns a frame with `name: "Design system"`, `parent: document` (top-level), `width: 1440`, `layout: "vertical"`, `gap: 0`, `padding: 0`, `alignItems: "center"`, `fill: "#ffffffff"` (literal per OPEN-23-13), `placeholder: true`.
    - After Task 2, the page frame's `y` coordinate is in the range [−4500, −3500] (page-frame row per § 10.4; anchor on s5k41l directs FindEmptySpace to this row).
    - After Task 2, the page frame's `x` coordinate is GREATER than s5k41l.x (7547.27) — FindEmptySpace direction "right" with padding 80 places the frame to the right of s5k41l.
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran and returned `design/Crito.pen` (D-153).
    - HALT condition: if FindEmptySpace returns position with y < −10000 (library row), the nodeId anchor failed — HALT and surface to user.
  </acceptance_criteria>
  <done>Page-frame chassis ships at correct position; placeholder:true during build; ready for child Insert operations.</done>
</task>

<task type="auto">
  <name>Task 3: Insert Section/Header instance + page-intro section with v1.3 verbatim content + 2 sibling notes</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-77 Header pure-ref no-override; D-143 v1.3 verbatim page-intro content + 2 sibling notes; D-145 vertical-stack order; D-146 1200 content-width)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Architecture Patterns — Pattern 1 Pure-Ref Instance + Pattern 6 Sibling-Note Belt-and-Suspenders; § Page-Frame Architecture page-intro structure)
    @.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md (D-82 v1.3 content verbatim discipline)
    @.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md (D-93 sibling-note belt-and-suspenders convention)
    @src/pages/design-system.astro (read lines 14-46 directly via Read tool — v1.3 verbatim content extraction source)
    @.planning/research/PITFALLS.md (Pitfall 2 — literal hex; Pitfall 7 — sibling notes load-bearing)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    Read `src/pages/design-system.astro` lines 14-46 to confirm verbatim content for D-143.

    **Sub-step A — Insert Section/Header instance as first child of `<designSystemPageFrameId>`** per D-77 pure-ref no-override pattern (Plan 26-02 Task 3 contribution):
    ```js
    batch_design({ operations: `
      headerInstance = I("<designSystemPageFrameId>", {
        type: "ref",
        ref: "G0wNOc",
        name: "Section/Header instance"
      });
    ` });
    ```
    No descendants override per D-77 carry-forward (Joel-brand override deferred to Phase 31 Homepage). Capture `headerInstanceId`.

    **Sub-step B — Insert page-intro section frame** as second child of `<designSystemPageFrameId>` at 1200 content-width per D-146:
    ```js
    pageIntroFrame = I("<designSystemPageFrameId>", {
      type: "frame",
      name: "page-intro",
      layout: "vertical",
      gap: 16,  // space-semantic-stack-md literal
      padding: [80, 0],  // space-semantic-section-y literal per Phase 26-29 chain
      width: 1200,
      height: "fit_content",
      fill: "transparent"
    });
    ```

    **Sub-step C — Insert page-intro content per D-143 v1.3 verbatim:**
    - H1 child: `type: "text"`, name `"H1"`, text `"Design System"` (v1.3 line 32-34), fontFamily literal for `type-semantic-heading-1` family (likely Plus Jakarta Sans per Phase 23), fontSize literal for heading-1 size, fontWeight 700, fill `#141f39ff` literal (color-semantic-text-primary).
    - Body child: `type: "text"`, name `"body"`, text `"Internal reference for Joel and AI coding agents. Components, tokens, and utilities for consistent development."` (v1.3 lines 35-37 verbatim), fontFamily literal for `type-semantic-prose-paragraph` family, fontSize literal, fill `#141f39ff`.
    - Secondary line child: `type: "text"`, name `"json-link-secondary"`, text `"Machine-readable format: /design-system.json"` (v1.3 lines 38-46 condensed per D-143), fontFamily literal for caption tier, fontSize literal, fill `#52525bff` for secondary-text role; styled to imply link affordance (underline or color shift for the `/design-system.json` substring per `type-semantic-prose-link` literal binding — Variant Evidence row at Task 15 documents).

    **Sub-step D — Insert 2 sibling Pencil notes at document root (NOT inside the page frame) per D-143 + D-93 belt-and-suspenders:**

    Note A — noindex preservation:
    ```js
    I(document, {
      type: "note",
      name: "Design system page — noindex preservation (v1.3 line 15-16)",
      text: "v1.3 src/pages/design-system.astro line 15-16 ships <meta name=\"robots\" content=\"noindex, follow\" />. Internal-only page; preserve in code milestone. The /design-system route is for Joel + AI coding agents, not lead generation."
    });
    ```

    Note B — JSON endpoint preservation:
    ```js
    I(document, {
      type: "note",
      name: "Design system page — JSON endpoint preservation (v1.3 lines 38-46)",
      text: "v1.3 src/pages/design-system.astro lines 38-46 ship a machine-readable /design-system.json link served by src/pages/design-system.json.ts. Preserve endpoint in code milestone for AI coding agents consuming the design system programmatically."
    });
    ```

    **Sub-step E — Insert v1.3 sidebar-nav DROPPED sibling note** at document root per D-145:
    ```js
    I(document, {
      type: "note",
      name: "Design system page — v1.3 sidebar nav DROPPED (D-145)",
      text: "v1.3 src/pages/design-system.astro lines 22-26 ship a lg:grid-cols-[250px_1fr] sticky sidebar nav (DesignSystemNav.astro). Phase 30 drops the sidebar per D-145 — sticky positioning doesn't render in Pencil; Pitfall O5/O6 single-consumer rule applies at sidebar layer. Code milestone may re-introduce or remove; documented here for handoff."
    });
    ```

    Capture all child + sibling note IDs for Task 11 + Task 15 PEN-INVENTORY.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<headerInstanceId>"] })` returns a ref-type node with `ref: "G0wNOc"` and parent `<designSystemPageFrameId>`.
    `mcp__pencil__batch_get({ nodeIds: ["<pageIntroFrameId>"], readDepth: 2 })` returns a 1200-wide vertical frame with 3 text children (H1 + body + secondary line) containing verbatim v1.3 content.
    `mcp__pencil__batch_get({ nodeIds: ["<noteAId>", "<noteBId>", "<sidebarNoteId>"], readDepth: 1 })` returns 3 sibling notes at document root with the expected text content.
  </verify>
  <acceptance_criteria>
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["<headerInstanceId>"] })` returns a ref node with `ref: "G0wNOc"`, `name: "Section/Header instance"`, parent `<designSystemPageFrameId>`, NO descendants override (D-77 pure-ref pattern).
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["<pageIntroFrameId>"], readDepth: 2 })` returns a frame with `name: "page-intro"`, `width: 1200`, `layout: "vertical"`, parent `<designSystemPageFrameId>`, containing at least 3 text children.
    - After Task 3, the H1 child has `text` field exactly equal to `"Design System"` (D-143 verbatim).
    - After Task 3, the body child has `text` field exactly equal to `"Internal reference for Joel and AI coding agents. Components, tokens, and utilities for consistent development."` (D-143 verbatim from v1.3 lines 35-37).
    - After Task 3, the secondary-line child has `text` field containing the substring `"/design-system.json"` (D-143 condensed verbatim).
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["<noteAId>"], readDepth: 1 })` returns a note with `name` containing `"noindex preservation"` and `text` containing the substring `"noindex"`.
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["<noteBId>"], readDepth: 1 })` returns a note with `name` containing `"JSON endpoint preservation"` and `text` containing the substring `"/design-system.json"`.
    - After Task 3, `mcp__pencil__batch_get({ nodeIds: ["<sidebarNoteId>"], readDepth: 1 })` returns a note with `name` containing `"sidebar nav DROPPED"` and `text` containing the substring `"D-145"`.
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran and returned `design/Crito.pen` (D-153).
  </acceptance_criteria>
  <done>Header instance + page-intro section ship at top of page frame; 3 sibling notes document v1.3 carry-forward intent for code milestone.</done>
</task>

<task type="auto">
  <name>Task 4: Build Colors section + Typography section (token gallery surface)</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-144 v1.3 mirror Colors/Typography sub-sections; D-147 ~107 tokens; D-148 not applicable to token gallery; Claude's Discretion — TokenSwatchGrid + TypeSpecimen tile internal layouts)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Page-Frame Architecture Colors + Typography sub-section structure; § Open Questions Q1 RpGbe sub-grouping resolution + Q4 tile internal layout)
    @.planning/phases/30-design-system-reference-reconstruction/30-00-PLAN.md (must_haves — TokenSwatchGrid + TypeSpecimen IDs from Plan 30-00 Tasks 2)
    @src/pages/design-system.astro (read lines 50-376 directly — Colors + Typography sub-section content for sub-grouping reference)
    @.planning/research/PITFALLS.md (Pitfall 2 — literal hex for fills; Pitfall 5 — DO NOT add new primitive variants)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    Read v1.3 design-system.astro lines 50-376 for Colors + Typography sub-section structure reference.

    **Sub-step A — Insert Colors section frame** as third child of `<designSystemPageFrameId>`:
    ```js
    colorsSection = I("<designSystemPageFrameId>", {
      type: "frame",
      name: "Colors",
      layout: "vertical",
      gap: 32,  // space-semantic-stack-lg literal — between H2 and sub-sections
      padding: [80, 0],  // space-semantic-section-y literal
      width: 1200,
      height: "fit_content",
      fill: "transparent"
    });
    ```

    Inside Colors section, insert:
    - H2 text child: `text: "Colors"`, font Plus Jakarta Sans literal, fontSize literal for type-semantic-heading-2, fontWeight 700, fill `#141f39ff` literal.
    - Sibling Pencil note on H2 (per D-144 anchor IDs sibling-note convention): `I(document, { type: "note", name: "Colors H2 anchor ID", text: "v1.3 section anchor ID: #colors" })`.

    **Sub-step B — Insert TokenSwatchGrid instances inside Colors section** per D-147 ~107 tokens curated by D-144 v1.3 sub-grouping (Primary Accent / Text Variants / Neutral / Usage Guidelines). Per Open Question 1 Task 1 RpGbe probe outcome:
    - If RpGbe sub-grouping mirrors v1.3 cleanly: insert 4 TokenSwatchGrid instances (one per sub-grouping).
    - If RpGbe is partial: insert 3-4 TokenSwatchGrid instances per `<code_context>` v1.3 default — Primary Accent (~3-5 swatches: yellow / turquoise / magenta brand) + Text Variants (~2-4 text-on-light + text-on-dark) + Neutral (~6 bg / text / border) + Usage Guidelines (decorative inline notes; may not be a TokenSwatchGrid — plan-execution discretion).

    For each TokenSwatchGrid instance:
    ```js
    primaryAccentGrid = I("<colorsSection>", {
      type: "ref",
      ref: "<tokenSwatchGridId>",  // from Plan 30-00 Task 2
      name: "Colors — Primary Accent TokenSwatchGrid",
      descendants: {
        "<headingSlotId>": { text: "Primary Accent" }
        // grid-container tile overrides happen at instance time per tile
      }
    });
    ```

    Then for each swatch tile (~3-30 swatches across all 4 sub-groupings combined per D-147), insert tile-override children. Each tile gets:
    - swatch rectangle `fill` literal hex (e.g., `#fdba09ff` for yellow brand) per token resolved value
    - token-name text update (e.g., `"color-semantic-bg-accent"`)
    - resolved-value text update (e.g., `"#fdba09"`)

    Per OPEN-23-13: ALL fill values are LITERAL hex (no `$<token>` refs). Variant Evidence rows at Task 15 document literal → token bindings.

    Per Pitfall 5: DO NOT invent new color tokens for the gallery. The token gallery RENDERS the existing surface; if a token doesn't exist in `get_variables({})`, do NOT add it (default per D-151 zero net-new tokens; mid-plan gate ONLY if structural escalation surfaces).

    Tile width: 200 default per Open Question 4 + 30-CONTEXT Claude's Discretion. ~6 tiles per row at 1200 content-width.

    **Sub-step C — Insert Typography section frame** as fourth child of `<designSystemPageFrameId>` per identical chassis pattern (1200 width, vertical, padding [80, 0], gap 32, transparent fill). H2 text child `"Typography"` + sibling Pencil note `"v1.3 section anchor ID: #typography"`.

    **Sub-step D — Insert TypeSpecimen instances inside Typography section** per D-144 v1.3 sub-grouping (Font Families / Type Scale / Font Weights / Line Heights):
    - Font Families: 1 TypeSpecimen instance with 2-3 specimen tiles (display family — likely Plus Jakarta Sans; body family — likely Inter; mono family — JetBrains Mono per Phase 28 D-108).
    - Type Scale: 1 TypeSpecimen instance with ~7-12 specimen tiles (display + heading-1 + heading-2 + heading-3 + heading-4 + body + body-sm + caption + button — token-renderable surface). Each tile: sample-text rendered IN the token + token-name caption + size/weight/lh metadata caption.
    - Font Weights: inline table OR TypeSpecimen instance (plan-execution discretion per D-144). Default: small inline table — 4 rows × 2 cols (weight value + sample "Sample text" rendered at that weight).
    - Line Heights: inline table OR TypeSpecimen instance (same discretion). Default: small inline table — 4 rows × 2 cols.

    Per Pitfall 5: DO NOT invent new typography tokens. Render the existing surface from Task 1 `get_variables({})` output.

    **Sub-step E — Tier-1 stale-cache mitigation in advance** (per Pitfall 1 + § 6.4):
    After inserting Colors + Typography subtrees, perform a cross-row Update on `<designSystemPageFrameId>` to clear render cache:
    ```js
    U("<designSystemPageFrameId>", { y: -8000 });
    U("<designSystemPageFrameId>", { y: <originalY> });
    ```
    This is Tier-1 mitigation per § 6.4 — pre-applied to reduce stale-cache risk before per-section screenshots at Task 12. May fail; Tier-2 fallback runs at Task 12 if needed.

    Capture `colorsSectionId`, `typographySectionId`, all child IDs for Task 11 + Task 15.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<colorsSectionId>"], readDepth: 3 })` returns a 1200-wide frame with H2 child + 3-4 TokenSwatchGrid ref children.
    `mcp__pencil__batch_get({ nodeIds: ["<typographySectionId>"], readDepth: 3 })` returns a 1200-wide frame with H2 child + 2-4 TypeSpecimen ref children (depending on inline-table vs TypeSpecimen discretion for Weights + LH).
    Each TokenSwatchGrid instance has a `ref` field equal to `<tokenSwatchGridId>` from Plan 30-00.
    Each TypeSpecimen instance has a `ref` field equal to `<typeSpecimenId>` from Plan 30-00.
  </verify>
  <acceptance_criteria>
    - After Task 4, `mcp__pencil__batch_get({ nodeIds: ["<colorsSectionId>"], readDepth: 3 })` returns a frame named `"Colors"` with width 1200, layout vertical, parent `<designSystemPageFrameId>`, containing 1 H2 text child + 3-4 ref children with `ref: "<tokenSwatchGridId>"`.
    - After Task 4, `mcp__pencil__batch_get({ nodeIds: ["<typographySectionId>"], readDepth: 3 })` returns a frame named `"Typography"` with width 1200, layout vertical, parent `<designSystemPageFrameId>`, containing 1 H2 text child + at least 2 ref children with `ref: "<typeSpecimenId>"` (Font Families + Type Scale minimum; Weights + LH may be inline OR additional TypeSpecimen per discretion).
    - After Task 4, the Colors H2 text child has `text: "Colors"` exactly.
    - After Task 4, the Typography H2 text child has `text: "Typography"` exactly.
    - After Task 4, `mcp__pencil__batch_get` on the Colors sibling Pencil note returns a note with text containing the substring `"#colors"` (D-144 v1.3 anchor ID).
    - After Task 4, `mcp__pencil__batch_get` on the Typography sibling Pencil note returns a note with text containing the substring `"#typography"`.
    - After Task 4, total swatch tile count across all TokenSwatchGrid instances in Colors section is 15-35 (per D-147 ~30 brand+text+bg+border+decorative token surface, accommodating sub-grouping discretion).
    - After Task 4, total specimen tile count across all TypeSpecimen instances in Typography section is 7-15 (per D-147 ~12 display/heading/caption/prose roles + Phase 28's 7 prose tokens; depends on inline-vs-specimen choice for Weights + LH).
    - After Task 4, `mcp__pencil__get_variables({})` returns count UNCHANGED from Task 1 baseline (per Pitfall 5 — no new tokens invented for the gallery; Plan 30-01 ships ZERO net-new tokens).
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran and returned `design/Crito.pen` (D-153).
  </acceptance_criteria>
  <done>Colors + Typography token-gallery sections ship inside the page frame; ~22-50 total tiles across both sections; no new tokens invented.</done>
</task>

<task type="auto">
  <name>Task 5: Build Components section — ~16 ComponentShowcase instances ordered Primitives → Compounds → Sections per D-148</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-147 curated 1-2 variants per component; D-148 full v2.0 library coverage; D-150 ComponentShowcase code-snippet canonical default-variant)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Code Examples — ComponentShowcase Instance with Code Snippet Plan 30-01 Task 6 reference; § Page-Frame Architecture Components sub-heading structure)
    @.planning/phases/30-design-system-reference-reconstruction/30-00-PLAN.md (must_haves — ComponentShowcase ID + slot IDs from Plan 30-00 Task 3)
    @.planning/phases/24-layout-primitives-primitive-components/24-CONTEXT.md (Primitive IDs)
    @.planning/phases/25-section-compound-components/25-CONTEXT.md (Card t40xct + Section/Header G0wNOc + Section/Footer Xs0Hs IDs)
    @.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-CONTEXT.md (Section/CTA Hs5rc + Section/NavBack N1jo3i IDs)
    @.planning/phases/27-thank-you-contact-reconstruction/27-CONTEXT.md (CheckboxGroup compound + Select primitive IDs — read SUMMARY for exact IDs)
    @.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md (BlogCard ZSxZU + Section/TagFilter O1IwyS + Section/RelatedPosts IDs + Badge/Outline kJQmJ)
    @.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md (ProjectCard DnsRs + Section/ResultsMetrics y4RORu + Section/RelatedProjects OLSa0 IDs)
    @.planning/research/PITFALLS.md (Pitfall 5 — D-147 curation; NO new variants invented)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    **Sub-step A — Insert Components section frame** as fifth child of `<designSystemPageFrameId>` per identical 1200-wide chassis (vertical, padding [80, 0], gap 48 to accommodate 3 sub-headings, transparent fill). H2 text child `"Components"` + sibling Pencil note `"v1.3 section anchor ID: #components"`.

    **Sub-step B — Insert 3 sub-section frames** inside Components section per D-148 Primitives → Compounds → Sections ordering. Each sub-section frame: 1200 width, vertical layout, gap 24, transparent fill. Each gets an H3 sub-heading text child (`"Primitives"`, `"Compounds"`, `"Sections"`).

    **Sub-step C — Insert ~16 ComponentShowcase instances** per D-148 + D-147 curation:

    **Primitives sub-section (~10 ComponentShowcase instances per D-148 curation):**
    - Button/Default (M7eUr) — code snippet `<Button variant="yellow">Click</Button>` (D-150 canonical default)
    - Button/Secondary (hIWuC) — code snippet `<Button variant="secondary">Click</Button>`
    - Input/Default (nwJk7) — code snippet `<Input label="Name" />`
    - Input/Focus state (use nwJk7 in focus-state styling — descendants override on the Showcase live-instance-slot OR a separate Focus-state primitive id if Phase 27 ships one; Task 1 batch_get findings confirm) — code snippet `<Input label="Name" state="focus" />`
    - Input/Error state (similar — Phase 27 ships Error state per D-89) — code snippet `<Input label="Name" error="Required" />`
    - Badge/Default (j0FxQZ) — code snippet `<Badge>Tag</Badge>`
    - Badge/Outline (kJQmJ) — code snippet `<Badge variant="outline">Tag</Badge>`
    - Icon (u7NmaS + 16/20/32 sizes side-by-side per D-148 Icon-sizes-are-canonical-taxonomy — single Showcase with 4 live instances side-by-side; code snippet `<Icon name="check" size={24} />`)

    For each ComponentShowcase instance:
    ```js
    buttonDefaultShowcase = I("<primitivesSubsectionId>", {
      type: "ref",
      ref: "<componentShowcaseId>",  // from Plan 30-00 Task 3
      name: "Button/Default ComponentShowcase",
      descendants: {
        "<labelSlotId>": { text: "Primitive / Button / Default" },
        "<codeSnippetSlotId>": { text: "<Button variant=\"yellow\">Click</Button>", enabled: true }
      }
    });

    // Live instance inside the live-instance-slot
    buttonLiveInstance = I("<buttonDefaultShowcaseId>/<liveInstanceSlotId>", {
      type: "ref",
      ref: "M7eUr",
      name: "Button/Default live instance"
    });
    ```

    **Compounds sub-section (~4 ComponentShowcase instances):**
    - Card (t40xct) — populated default slots (image + title + body + footer-actions) per D-148; code snippet `<Card variant="default">...</Card>`
    - BlogCard (ZSxZU) — populated default per Phase 28 ZSxZU defaults; code snippet `<BlogCard post={post} />`
    - ProjectCard (DnsRs) — populated default per Phase 29 DnsRs defaults; code snippet `<ProjectCard project={project} />`
    - CheckboxGroup (id from Task 1 batch_get of t67DU6 children) — populated default with ~3-5 options; code snippet `<CheckboxGroup options={options} />`

    **Sections sub-section (8 ComponentShowcase instances):**
    - Header (G0wNOc) ComponentShowcase + live instance ref G0wNOc; code snippet `<Header />`
    - Footer (Xs0Hs) + live ref Xs0Hs; code snippet `<Footer />`
    - CTA (Hs5rc) + live ref Hs5rc; code snippet `<CTA heading="..." body="..." actions={[...]} />`
    - NavBack (N1jo3i) + live ref N1jo3i; code snippet `<NavBack heading="..." links={[...]} />`
    - TagFilter (O1IwyS) + live ref O1IwyS; code snippet `<TagFilter tags={[...]} active={...} />`
    - RelatedPosts (Phase 28 id) + live ref; code snippet `<RelatedPosts posts={[...]} />`
    - ResultsMetrics (y4RORu) + live ref y4RORu; code snippet `<ResultsMetrics metrics={[...]} />`
    - RelatedProjects (OLSa0) + live ref OLSa0; code snippet `<RelatedProjects projects={[...]} />`

    Per D-147 + Pitfall 5: do NOT add new primitive variants or component variants for the gallery — instance existing surface only. Variant matrices already live at the primitive level for `batch_get` discovery.

    Per D-150: each code-snippet-slot has `enabled: true` with the canonical default-variant snippet only (NOT exhaustive snippets across variants).

    Per OPEN-23-13 + Pitfall 2: all descendants overrides use literal values, not `$<token>` refs. Variant Evidence rows at Task 15 document literal → token bindings (especially the code-snippet-slot mono-primitive + prose-code-block bindings).

    **Sub-step D — Tier-1 stale-cache mitigation in advance** (per Pitfall 1 + § 6.4):
    After inserting ~16 ComponentShowcase instances, perform cross-row Update on `<designSystemPageFrameId>`:
    ```js
    U("<designSystemPageFrameId>", { y: -8000 });
    U("<designSystemPageFrameId>", { y: <originalY> });
    ```
    Pre-applied per § 6.4. HIGH-likelihood Tier-2 fallback expected at Task 12.

    Capture `componentsSectionId`, sub-section IDs, ~16 ComponentShowcase instance IDs + their live-instance-slot child IDs for Task 11 + Task 15.

    NOTE: Per § 4.5 step 6 + Pitfall 1, plan-execution may need to chunk Task 5 across multiple `batch_design` calls (Pencil 2.13 ~25-op cap per call; ~16 ComponentShowcase instances × ~3 ops each = ~50 ops total).
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<componentsSectionId>"], readDepth: 3 })` returns a 1200-wide frame with H2 child + 3 sub-section frames (Primitives + Compounds + Sections), each containing ComponentShowcase ref children.
    Total ComponentShowcase instances across the 3 sub-sections = ~16 (count must be ≥ 14 and ≤ 20 to accommodate Input states + Icon sizes consolidation discretion).
    Each ComponentShowcase instance has `ref: "<componentShowcaseId>"` from Plan 30-00.
  </verify>
  <acceptance_criteria>
    - After Task 5, `mcp__pencil__batch_get({ nodeIds: ["<componentsSectionId>"], readDepth: 3 })` returns a frame named `"Components"` with width 1200, layout vertical, parent `<designSystemPageFrameId>`, containing 1 H2 text child + 3 sub-section frames named `"Primitives"`, `"Compounds"`, `"Sections"`.
    - After Task 5, total ComponentShowcase ref children (recursive count under `<componentsSectionId>`) is ≥ 14 and ≤ 20 (target ~16 per D-148; Input states + Icon sizes may consolidate or expand).
    - After Task 5, every ComponentShowcase instance has `ref: "<componentShowcaseId>"` from Plan 30-00 Task 3.
    - After Task 5, each ComponentShowcase has its `code-snippet-slot` descendant overridden with a canonical default-variant snippet (D-150) — verify any single sample: `mcp__pencil__batch_get` on the Button/Default ComponentShowcase descendants returns code-snippet-slot text containing `"<Button"` substring.
    - After Task 5, each ComponentShowcase live-instance-slot contains a ref child pointing to the corresponding Phase 23-29 library component ID (M7eUr, hIWuC, nwJk7, j0FxQZ, kJQmJ, u7NmaS, t40xct, ZSxZU, DnsRs, G0wNOc, Xs0Hs, Hs5rc, N1jo3i, O1IwyS, y4RORu, OLSa0, plus CheckboxGroup + RelatedPosts).
    - After Task 5, the Sections sub-section ComponentShowcase instances DO include Header (G0wNOc) and Footer (Xs0Hs) — satisfies ROADMAP success criterion 3 cross-page consistency proof at the gallery level (the chassis instances in Task 3 + 6 satisfy it at the page-chassis level).
    - After Task 5, `mcp__pencil__get_variables({})` returns count UNCHANGED from Task 1 baseline (NO new tokens invented for the gallery per Pitfall 5).
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran (D-153) before each chunked `batch_design` call.
  </acceptance_criteria>
  <done>Components section ships with ~16 ComponentShowcase instances + 3 sub-section sub-headings; live instances ref Phase 23-29 library entries; canonical default-variant code snippets per D-150.</done>
</task>

<task type="auto">
  <name>Task 6: Build Utilities section + Insert Section/Footer instance (page-frame bottom)</name>
  <files>design/Crito.pen (mutated via mcp__pencil__batch_design)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-77 Footer pure-ref no-override; D-144 Utilities section iso-shadow APPROXIMATE + iso-glow STUB + iso-rotate STUB per Open Q3)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Page-Frame Architecture Utilities section structure; § Open Questions Q3 Utilities approximation depth)
    @src/styles/global.css (read lines 140-231 directly — actual CSS mechanics for iso-shadow + iso-glow + iso-rotate sibling notes)
    @.planning/research/PITFALLS.md (Pitfall 7 — sibling CSS notes load-bearing)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    Read `src/styles/global.css` lines 140-231 to confirm the actual CSS mechanic literals for sibling notes:
    - iso-rotate: `transform: rotateX(45deg) rotateZ(45deg)` (variants: subtle 30°/30°, steep 60°/45°) — global.css lines 146-148
    - iso-shadow: `box-shadow: 5px 5px 0 currentColor` (variants: sm 3px, lg 8px; dark-mode glow override) — global.css lines 193-218
    - iso-glow: `box-shadow: 0 0 20px color-mix(in oklch, currentColor 40%, transparent)` (variants: subtle 15px/20%, strong 30px/60%) — global.css lines 221-231

    **Sub-step A — Insert Utilities section frame** as sixth child of `<designSystemPageFrameId>` per identical 1200-wide chassis (vertical, padding [80, 0], gap 32, transparent fill). H2 text child `"Utilities"` + sibling Pencil note `"v1.3 section anchor ID: #utilities"`.

    **Sub-step B — Insert 3 utility sub-section frames** inside Utilities section (each 1200 width, vertical layout, gap 16, transparent fill). Each gets an H3 sub-heading text child (`"iso-shadow"`, `"iso-glow"`, `"iso-rotate"`).

    **Sub-step C — Insert iso-shadow APPROXIMATE composition** inside iso-shadow sub-section per D-144 Claude's Discretion + Open Q3 default:
    - Visual approximation: a small bordered card placeholder (rectangle, ~240×160, white fill, 1px border `#e4e4e7ff` literal, cornerRadius 4) WITH an offset filled shape behind it (another rectangle, same dimensions, fill `#141f39ff` literal for color-semantic-shadow-default OR `#52525bff` for neutral-700, positioned 5px right + 5px down — z-order: behind the white card).
    - Implementation: insert background-rectangle FIRST (z-order back), then card-rectangle SECOND (z-order front) — Pencil 2.13 sibling order = z-order.
    - Sibling Pencil note inside iso-shadow sub-section: `I("<isoShadowSubsectionId>", { type: "note", name: "iso-shadow CSS mechanic", text: "src/styles/global.css lines 193-218: box-shadow: 5px 5px 0 currentColor (variants: iso-shadow-sm = 3px 3px, iso-shadow-lg = 8px 8px). Dark mode override: glow effect via color-mix. Phase 30 APPROXIMATE visualization is offset filled shape behind a Card placeholder; downstream code milestone re-implements via box-shadow." })`.

    **Sub-step D — Insert iso-glow STUB composition** inside iso-glow sub-section per D-144 + Open Q3 default:
    - STUB visualization: text node "STUB — iso-glow renders as CSS filter; not faithfully representable in Pencil" + a placeholder rectangle (same Card-like 240×160 white fill + 1px border) with NO glow rendering.
    - Sibling Pencil note inside iso-glow sub-section: `I("<isoGlowSubsectionId>", { type: "note", name: "iso-glow CSS mechanic", text: "src/styles/global.css lines 221-231: box-shadow: 0 0 20px color-mix(in oklch, currentColor 40%, transparent). Variants: subtle = 15px/20%, strong = 30px/60%. Phase 30 STUB — CSS filter behavior cannot be faithfully rendered in Pencil design tool; downstream code milestone re-implements via box-shadow + color-mix." })`.

    **Sub-step E — Insert iso-rotate STUB composition** inside iso-rotate sub-section per D-144 + Open Q3 default:
    - STUB visualization: text node "STUB — iso-rotate uses 3D CSS transforms; not faithfully representable in Pencil" + a placeholder rectangle.
    - Sibling Pencil note inside iso-rotate sub-section: `I("<isoRotateSubsectionId>", { type: "note", name: "iso-rotate CSS mechanic", text: "src/styles/global.css lines 146-148: transform: rotateX(45deg) rotateZ(45deg). Variants: subtle = 30°/30°, steep = 60°/45°. Phase 30 STUB — 3D CSS transforms cannot be faithfully rendered in Pencil design tool; downstream code milestone re-implements via CSS transform." })`.

    Per D-144 + Open Q3 Recommendation: iso-shadow APPROXIMATE is the calibration-gate test — user APPROVE confirms or REVISE re-labels to STUB. iso-glow + iso-rotate ship as STUB by default.

    **Sub-step F — Insert Section/Footer instance as last (seventh) child of `<designSystemPageFrameId>`** per D-77 pure-ref no-override pattern:
    ```js
    footerInstance = I("<designSystemPageFrameId>", {
      type: "ref",
      ref: "Xs0Hs",
      name: "Section/Footer instance"
    });
    ```
    No descendants override per D-77 carry-forward.

    Capture `utilitiesSectionId`, 3 utility sub-section IDs, 3 sibling note IDs, `footerInstanceId` for Task 11 + Task 15.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<utilitiesSectionId>"], readDepth: 3 })` returns a 1200-wide frame with H2 child + 3 sub-section frames (iso-shadow + iso-glow + iso-rotate) + 3 sibling notes.
    `mcp__pencil__batch_get({ nodeIds: ["<footerInstanceId>"] })` returns a ref node with `ref: "Xs0Hs"` and parent `<designSystemPageFrameId>` (last child position).
  </verify>
  <acceptance_criteria>
    - After Task 6, `mcp__pencil__batch_get({ nodeIds: ["<utilitiesSectionId>"], readDepth: 3 })` returns a frame named `"Utilities"` with width 1200, layout vertical, parent `<designSystemPageFrameId>`, containing 1 H2 text child + 3 sub-section frames named `"iso-shadow"`, `"iso-glow"`, `"iso-rotate"`.
    - After Task 6, each utility sub-section contains exactly 1 sibling Pencil note documenting the CSS mechanic from `src/styles/global.css` (3 notes total) — verify presence of substrings `"box-shadow: 5px 5px"`, `"box-shadow: 0 0 20px"`, `"rotateX(45deg) rotateZ(45deg)"` across the 3 notes.
    - After Task 6, iso-shadow sub-section contains BOTH an offset background-rectangle AND a foreground card-rectangle (APPROXIMATE rendering per Open Q3 default; calibration gate confirms or re-labels to STUB).
    - After Task 6, iso-glow + iso-rotate sub-sections contain STUB indicators (text node saying "STUB" OR clearly-labeled placeholder rectangle without effect-rendering).
    - After Task 6, `mcp__pencil__batch_get({ nodeIds: ["<footerInstanceId>"] })` returns a ref node with `ref: "Xs0Hs"`, `name: "Section/Footer instance"`, parent `<designSystemPageFrameId>`, NO descendants override (D-77 pure-ref pattern).
    - After Task 6, `mcp__pencil__batch_get({ nodeIds: ["<designSystemPageFrameId>"], readDepth: 1 })` returns the page frame with `children` array of length 7 — Header instance + page-intro + Colors + Typography + Components + Utilities + Footer instance (D-145 + D-146 vertical-stack order).
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran (D-153).
  </acceptance_criteria>
  <done>Utilities section + Footer instance ship; page frame now has all 7 child sections; sibling CSS notes document actual src/styles/global.css mechanics for code milestone.</done>
</task>

<task type="auto">
  <name>Task 7: fit_content settle + snapshot_layout + zero-mutation baseline regression check + per-section screenshots for calibration</name>
  <files>design/Crito.pen (mutated via batch_design + validation reads)</files>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-146 fit_content settle; D-153 pre-flight; D-152 Tier-2 fallback for tall page frames)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Architecture Patterns Pattern 3 fit_content settle + Pattern 4 Tier-1/Tier-2 stale-cache; § Validation Architecture Dim 1 + Dim 3 + Dim 8; Pitfall 1 + Pitfall 3)
    @.planning/research/CALIBRATION-PROTOCOL.md (§ 6.4 Tier-1/Tier-2 stale-cache workaround)
    @.planning/research/PITFALLS.md (Pitfall 1 stale-cache HIGH likelihood at ~58 instances; Pitfall 3 text-clipping false-positive)
  </read_first>
  <action>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    **Sub-step A — fit_content settle** (Plan 26-02 Task 4 contribution pattern):
    ```js
    batch_design({ operations: `
      U("<designSystemPageFrameId>", {
        placeholder: false,
        height: "fit_content"
      });
    ` });
    ```
    This switches the page frame from fixed 2400 estimate to auto-sizing based on child content.

    **Sub-step B — snapshot_layout document root check:**
    Call `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })`.
    Expected: `"No layout problems."` (matches Phase 24-29 chain precedent).
    Per Pitfall 3: text-clipping false-positives anticipated to EXCEED Phase 26-29 counts due to ~58-instance density (especially Components section). Document false-positive node names for Plan 30-01 SUMMARY + PEN-INVENTORY notes; do NOT mitigate.

    Also call per-frame: `mcp__pencil__snapshot_layout({ rootId: "<designSystemPageFrameId>", problemsOnly: true })`. Document text-clipping outcomes.

    **Sub-step C — Token surface drift check:**
    Call `mcp__pencil__get_variables({})`. Compare against Task 1 baseline (expected: count UNCHANGED — Plan 30-01 composition plan ships ZERO net-new tokens per D-151 default).
    - If unchanged: PASS.
    - If +1: investigate — mid-plan unexpected token escalation (NOT expected for composition plan; HALT and surface to user).

    **Sub-step D — Zero-mutation baseline regression check (Dimension 3):**
    Call `mcp__pencil__batch_get` on all Phase 23-29 baseline IDs (cached from Task 1). Compare child counts + names:
    - **Library parents:** g9oRa5 unchanged (the 3 Plan 30-00 sections are already in g9oRa5 — child count = Plan 30-00 close baseline). avgor + t67DU6 + RpGbe child counts UNCHANGED.
    - **All 16 Phase 23-29 component IDs:** structurally UNCHANGED.
    - **All Phase 23-29 page frame IDs (b7Hgy, csXky, XsDab, n0QqTd, aQ8FL, FUctJ, SCcln, s5k41l, cl8tt, w1m3x, DzqTm, cYlRH, Y2isa):** UNCHANGED.
    - **Document root child count:** Plan 30-00 baseline + 1 (new Design system page frame at top level).
    - If any baseline ID mutated unexpectedly: HALT and surface to user.

    **Sub-step E — Per-section get_screenshot (Tier-1 stale-cache mitigation per § 6.4):**
    For each of the 5 content sections (page-intro, Colors, Typography, Components, Utilities) + Header + Footer + the whole page frame:
    1. Call `mcp__pencil__get_screenshot({ nodeId: "<sectionId>" })`.
    2. If returned image is blank-white (Pitfall 1 trigger):
       - Apply Tier-1 mitigation: `U("<designSystemPageFrameId>", { y: -8000 })` then `U("<designSystemPageFrameId>", { y: <originalY> })`. Retry screenshot.
       - If still blank: flag for Tier-2 fallback at Task 8 calibration gate (description references canvas coordinates instead of inline image).
    3. Document outcome per section in Plan 30-01 SUMMARY.

    Per RESEARCH HIGH-likelihood prediction at ~58-instance density: PRE-COMMIT to Tier-2 fallback for at least some sections — do NOT spend > 2 Tier-1 retry attempts per section.
  </action>
  <verify>
    `mcp__pencil__batch_get({ nodeIds: ["<designSystemPageFrameId>"], readDepth: 1 })` returns the page frame with `placeholder: false` and `height: "fit_content"` (or a numeric value approximating the auto-content sum).
    `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root returns `"No layout problems."` OR documented benign text-clipping false-positives.
    `mcp__pencil__get_variables({})` returns count == Task 1 baseline (Plan 30-01 ships zero net-new tokens).
    Per-section `get_screenshot` returns inline images OR Tier-2 fallback flag set per section (document in working memory for Task 8).
  </verify>
  <acceptance_criteria>
    - After Task 7, `mcp__pencil__batch_get({ nodeIds: ["<designSystemPageFrameId>"], readDepth: 1 })` returns the page frame with `placeholder: false` (fit_content settled).
    - After Task 7, `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root returns the literal string `"No layout problems."` (text-clipping false-positives on text nodes inside Components section ComponentShowcase + page-intro/Colors/Typography text nodes are benign per Pitfall 3 — documented in SUMMARY, no mitigation applied).
    - After Task 7, `mcp__pencil__get_variables({})` returns count == Task 1 baseline (NO net-new tokens shipped in composition plan).
    - After Task 7, all 16 Phase 23-29 library component IDs + all Phase 23-29 page frame IDs return structurally UNCHANGED from Task 1 baseline (Dimension 3 regression check passes).
    - After Task 7, document root child count = Task 1 baseline + 1 (only Design system page frame added at top level).
    - After Task 7, per-section get_screenshot outcomes are documented (inline image received OR Tier-2 fallback flag set per section). At least 5 of 7 sections should yield inline images at Tier-1 — Tier-2 fallback for 3+ sections triggers calibration gate description structure adjustment at Task 8.
    - PRE-FLIGHT: `mcp__pencil__get_editor_state({ include_schema: false })` ran (D-153).
  </acceptance_criteria>
  <done>Page frame settled to fit_content; document root snapshot clean; Phase 23-29 baseline regression check passed; per-section screenshots captured (with Tier-2 fallback for any blank-white subtrees).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Plan-close calibration AskUserQuestion (joel-only branch § 4.5 token-usage check vs RpGbe)</name>
  <read_first>
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-152 single plan-close gate; D-62 joel-only branch token-usage calibration target)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Architecture Patterns Pattern 5 AskUserQuestion Single Plan-Close Gate — markdown template; § Open Questions Q1 RpGbe sub-grouping resolution per Task 1; Q3 Utilities approximation outcome; Q5 prose-code-block secondary validation)
    @.planning/research/CALIBRATION-PROTOCOL.md (§ 4.5 joel-only branch AskUserQuestion format; § 4.6 description identifiers convention)
  </read_first>
  <what-built>
    Plan 30-01 has built the `Design system` page frame inside `design/Crito.pen` as the FIFTH production use of the CALIBRATION-PROTOCOL § 4 joel-only-no-crito-ref branch. The page frame contains 7 child sections in vertical-stack order: Section/Header (G0wNOc pure-ref) → page-intro (v1.3 verbatim H1 + body + JSON-link + 2 sibling notes for noindex + JSON endpoint preservation) → Colors (TokenSwatchGrid instances for Primary Accent / Text / Neutral / Usage Guidelines, ~15-35 swatch tiles total) → Typography (TypeSpecimen instances for Font Families + Type Scale + Font Weights + Line Heights, ~7-15 specimen tiles total) → Components (~16 ComponentShowcase instances ordered Primitives → Compounds → Sections per D-148 full v2.0 library coverage) → Utilities (3 sub-sections: iso-shadow APPROXIMATE + iso-glow STUB + iso-rotate STUB + 3 sibling CSS notes documenting src/styles/global.css mechanics) → Section/Footer (Xs0Hs pure-ref). All semantic tokens consumed via literal value bindings per OPEN-23-13; Variant Evidence rows at Task 15 will document literal → token bindings.
  </what-built>
  <how-to-verify>
    Pre-flight: re-run `mcp__pencil__get_editor_state({ include_schema: false })` (D-153).

    Construct and run AskUserQuestion per CALIBRATION-PROTOCOL § 4.5 joel-only branch markdown template. Reference 30-RESEARCH.md § Architecture Patterns Pattern 5 for the EXACT structure. Description includes:

    **Section 1: page-intro** — Fidelity label proposal: APPROXIMATE per D-83 (v1.3 verbatim text + new sibling-note structural docs for noindex + JSON endpoint + sidebar-dropped). Semantic tokens consumed: type-semantic-heading-1, type-semantic-prose-paragraph, type-semantic-prose-caption, type-semantic-prose-link (inline), color-semantic-bg-page, color-semantic-text-primary.

    **Section 2: Colors** — Fidelity label proposal: EXACT (TokenSwatchGrid tiles bind directly to color-semantic-* tokens declared in RpGbe per D-62). Sub-groupings: Primary Accent / Text Variants / Neutral / Usage Guidelines per v1.3. Open Question 1 resolution outcome (from Task 1 RpGbe probe): [document whether RpGbe sub-grouping mirrored or v1.3 default applied].

    **Section 3: Typography** — Fidelity label proposal: EXACT (TypeSpecimen tiles bind to type-semantic-* tokens; Font Families + Type Scale render the token surface; Font Weights + Line Heights ship as inline tables OR additional TypeSpecimen instances per plan-execution discretion). Semantic tokens consumed: type-semantic-display, heading-1..4, body, body-sm, caption, button, mono-primitive, prose-paragraph, prose-link, prose-list, prose-inline-code, prose-code-block.

    **Section 4: Components** — Fidelity label proposal: EXACT (instances bind to canonical primitives + compounds + sections from Phase 23-29; D-147 curated 1-2 variants per primitive; D-148 full v2.0 library coverage; D-150 ComponentShowcase code-snippet-slot SECONDARY validation for type-semantic-prose-code-block per Q5).

    **Section 5: Utilities** — Fidelity label proposal: APPROXIMATE (iso-shadow offset-shape behind Card placeholder per Open Q3 default) + STUB (iso-glow, iso-rotate per Open Q3 default). Sibling CSS notes attached for code-milestone re-implementation. **Question for user:** Does iso-shadow approximation read cleanly or misleadingly? If misleading, re-label to STUB.

    **Section 6: Header/Footer (chassis)** — Fidelity label proposal: EXACT (pure-ref instances, no override per D-77 carry-forward). This satisfies ROADMAP success criterion 3 — Header + Footer composed correctly in a page context other than the homepage.

    **Calibration target (D-62 joel-only branch token-usage check vs RpGbe):**
    "Does the Design system page frame's TokenSwatchGrid + TypeSpecimen + ComponentShowcase tile-bindings match the canonical surface declared in `_Tokens & Foundations` (RpGbe)?"

    **Description identifiers (D-64 + § 4.6, inline only per OPEN-23-01, NOT disk-written):**
    - 30-design-system-page-intro--token-usage.png
    - 30-design-system-colors--token-usage.png
    - 30-design-system-typography--token-usage.png
    - 30-design-system-components--token-usage.png
    - 30-design-system-utilities--token-usage.png

    **Inline screenshots (Task 7 outcomes):**
    - For each section where Tier-1 succeeded: include inline image content in description.
    - For each section where Tier-2 fallback triggered: description references canvas coordinates `(<x>, ~−4111)` and instructs user to verify in Pencil editor.

    **Provisional-flag verification chain (Q5):**
    Plan 30-01 IS the SECONDARY validation surface for `type-semantic-prose-code-block` per D-150. If user finds the code-snippet-slot rendering reads correctly in the ComponentShowcase context (different from Plan 28-02 Blog Post body PRIMARY validation), the provisional flag advances toward Phase 32 close.

    **Options:**
    - "APPROVE — all sections use tokens correctly; fidelity labels per D-83 as proposed (page-intro APPROXIMATE; Colors EXACT; Typography EXACT; Components EXACT; Utilities iso-shadow APPROXIMATE + iso-glow STUB + iso-rotate STUB; Header/Footer EXACT)"
    - "REVISE — token mismatch (free-text describe which section + which token binding)"
    - "GAP — composition reveals a missing token or fidelity-label adjustment (raise OPEN-30-NN; specify the gap)"

    User reads inline screenshots (Tier-1 successes) + verifies Tier-2 fallback sections in Pencil editor at canvas coordinates + selects APPROVE / REVISE / GAP.
  </how-to-verify>
  <resume-signal>
    User selects an option. On APPROVE: proceed to Task 9 PEN-INVENTORY in-place reclassification. On REVISE: address the token mismatch (in-task corrective batch_design) + re-run Task 8 calibration gate. On GAP: raise OPEN-30-NN entries in PEN-INVENTORY at Task 9 + proceed to Task 9 reclassification (gap declaration ≠ blocking per D-83 + VALID-03 carry-forward).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 9: Post-APPROVE — PEN-INVENTORY in-place reclassification (line 77 placeholder) + Variant Evidence + OPEN-30-NN extension</name>
  <files>.planning/research/PEN-INVENTORY.md</files>
  <read_first>
    @.planning/research/PEN-INVENTORY.md (line 77 placeholder row format; existing Frames Inventory + Variant Evidence + Open Flags structure)
    @.planning/phases/30-design-system-reference-reconstruction/30-CONTEXT.md (D-154 reclassification pattern; PAGE-11 INERT for joel-only)
    @.planning/phases/30-design-system-reference-reconstruction/30-RESEARCH.md (§ Code Examples — PEN-INVENTORY In-Place Reclassification BEFORE/AFTER reference)
    @.planning/phases/29-projects-reconstruction-index-project-detail/29-CONTEXT.md (D-128 in-place reclassification pattern precedent — Y2isa + cYlRH carry-forward to line 77 placeholder)
  </read_first>
  <action>
    GATE CONDITION: This task ONLY runs if Task 8 returned APPROVE (or GAP — non-blocking per D-83 + VALID-03). If REVISE returned, do NOT run this task; Task 8 re-runs with corrective batch_design first.

    Read PEN-INVENTORY.md current state. Apply the following changes per D-154 Phase 30 Plan 30-01 contributions:

    **Sub-step A — In-place reclassification of line 77 placeholder row** per D-128 + D-154 pattern (Y2isa + cYlRH precedent from Phase 29):

    Find the row at PEN-INVENTORY line 77 with the placeholder pattern. Per 30-RESEARCH.md § Code Examples "PEN-INVENTORY In-Place Reclassification" — BEFORE state:
    ```
    | (joel-only: Design System) | n/a (no Crito source) | joel-only-no-crito-ref | /design-system page (per D-08) | 0 | flat:0, partial:0, factored:0 | medium (Phase 30) | — |
    ```

    AFTER state (post-APPROVE):
    ```
    | Design system | <designSystemPageFrameId from Task 2> | joel-only-no-crito-ref | /design-system (per Plan 30-01) | 7 | flat:0, partial:0, factored:7 | n/a (Phase 30 plan 30-01 — reconstructed; FIFTH joel-only-no-crito-ref branch use in v2.0; PAGE-11 INERT per § 4.3; first Phase 30 cross-page consistency proof for Header + Footer per ROADMAP success criterion 3) | OPEN-30-NN (Utilities iso-glow/iso-rotate STUB labeling + any plan-surfaced flags from Task 8) |
    ```

    Use Edit (NOT Write) to modify ONLY line 77 — preserve all other PEN-INVENTORY content.

    **Sub-step B — Variant Evidence (Phase 30) Plan 30-01 sub-section** (append to Variant Evidence section, ideally under the same Phase 30 Variant Evidence heading created by Plan 30-00 Task 6):

    Document:
    - Design system page frame architecture per D-145 + D-146 (1440 outer, 1200 content-width, vertical layout, alignItems center, fill #ffffffff literal).
    - find_empty_space_on_canvas nodeId:s5k41l anchor outcome (§ 10.4 + Plan 26-02 precedent inherited — placement at canvas (~7547+1520+, ~−4111)).
    - Header + Footer pure-ref instances per D-77 (no override; cross-page consistency proof for ROADMAP success criterion 3 — Phase 30 establishes; Phase 31 Homepage extends).
    - page-intro D-143 v1.3 verbatim content extraction + 3 sibling Pencil notes (noindex + JSON endpoint + sidebar-dropped) per D-93 belt-and-suspenders.
    - Colors TokenSwatchGrid instances: per-sub-grouping tile-binding inventory (Primary Accent: ~3 brand swatches yellow/turquoise/magenta; Text Variants: ~2 text-on-light/dark; Neutral: ~6 bg/text/border; Usage Guidelines: inline notes). Document literal → token bindings: e.g., `#fdba09 → color-semantic-bg-accent`; `#141f39 → color-semantic-text-primary`.
    - Typography TypeSpecimen instances: token-renderable specimen inventory (Font Families: display + body + mono; Type Scale: display + heading-1..4 + body + body-sm + caption + button + prose-paragraph + prose-link + prose-code-block + prose-list + prose-inline-code; Font Weights + Line Heights: inline-table OR specimen choice).
    - Components ComponentShowcase instances: full library coverage per D-148 (Primitives 10 + Compounds 4 + Sections 8 = ~22 total but consolidated to ~16 via Icon-sizes-as-single-showcase per D-148). Each live-instance-slot binding documented (e.g., Button/Default showcase live-instance → M7eUr; Card showcase live-instance → t40xct). Each code-snippet-slot canonical default snippet documented.
    - Utilities section: iso-shadow APPROXIMATE composition evidence (offset filled shape behind Card placeholder; literal shadow color `#141f39ff` or `#52525bff`); iso-glow + iso-rotate STUB evidence; 3 sibling CSS notes from src/styles/global.css lines 140-231 inventoried.

    **Sub-step C — Open Flags — Phase 30 extension** (append to OPEN-30-NN section created by Plan 30-00 Task 6, replacing seed entries with real entries from Task 8 calibration outcome):

    Real OPEN-30-NN entries:
    - **OPEN-30-01:** Utilities iso-shadow APPROXIMATE confirmation (CALIBRATION-PROTOCOL Task 8 outcome — APPROVE confirms APPROXIMATE; REVISE re-labels to STUB; document final disposition).
    - **OPEN-30-02:** Utilities iso-glow STUB confirmation.
    - **OPEN-30-03:** Utilities iso-rotate STUB confirmation.
    - **OPEN-30-04 (conditional):** `type-semantic-prose-code-block` provisional-flag SECONDARY-validation outcome per D-150 + Phase 28 D-110 chain. APPROVE at Task 8 advances toward Phase 32 close; GAP raises specific concern.
    - **OPEN-30-NN (any other gaps from Task 8 calibration gate):** as raised by user during APPROVE / REVISE / GAP option text.

    **Sub-step D — Carry-forward status updates:**
    - OPEN-25-07 partial-resolution status: Phase 30 Component gallery instances Card (t40xct) + BlogCard (ZSxZU) + ProjectCard (DnsRs) as 4th cross-phase consumer of sibling-component pattern. Final disposition deferred to Phase 32 close. Status: PARTIAL-RESOLUTION RIDE-ALONG.
    - OPEN-23-12 (`radius-semantic-pill`): Phase 30 token-gallery DID/DID-NOT surface new pill consumers (per Task 4 outcome). If new pill consumers surfaced, document; else default leaves OPEN-23-12 carry-forward unchanged.
    - OPEN-23-10 (heading-5/-6 token addition): Phase 30 DID/DID-NOT require new heading-5/-6 tokens (per Task 4 + Task 5 outcomes). If specimen-tile metadata captions reused heading-3/-4, OPEN-23-10 carry-forward unchanged.

    **Sub-step E — PAGE-11 INERT status documented** in the reclassified line 77 row's `reconstruction_priority` field per Sub-step A AFTER state ("PAGE-11 INERT per § 4.3").

    Save PEN-INVENTORY.md.

    **Sub-step F — Commit** (per planning workflow `commit_docs: true`):
    `git add design/Crito.pen .planning/research/PEN-INVENTORY.md .planning/phases/30-design-system-reference-reconstruction/30-01-PLAN.md`
    `git commit -m "exec(30-01): Tasks 2-9 — Design system page frame composition + APPROVE calibration + PEN-INVENTORY line 77 reclassification (FIFTH joel-only-no-crito-ref consumer; PAGE-11 INERT; library 19→19; tokens 127→127)"`

    (Final SUMMARY commit handled at plan close by execute-plan workflow.)
  </action>
  <verify>
    `grep -q "^| Design system " .planning/research/PEN-INVENTORY.md` exits 0 (reclassified row present).
    `grep -q "(joel-only: Design System)" .planning/research/PEN-INVENTORY.md` exits 1 (placeholder row reclassified-away).
    `grep -q "factored:7" .planning/research/PEN-INVENTORY.md` exits 0 (status_counts updated).
    `grep -q "OPEN-30-01" .planning/research/PEN-INVENTORY.md` exits 0 (real OPEN-30-NN entries appended).
    `git log -1 --format=%s` returns a commit message starting with `exec(30-01):`.
  </verify>
  <acceptance_criteria>
    - After Task 9, `grep -q "^| Design system " .planning/research/PEN-INVENTORY.md` exits 0 (reclassified row present at the line previously occupied by the placeholder).
    - After Task 9, `grep -q "(joel-only: Design System)" .planning/research/PEN-INVENTORY.md` exits 1 (placeholder row content reclassified-away per D-128 + D-154 pattern; in-place edit preserved row position).
    - After Task 9, the reclassified row contains the substring `"<designSystemPageFrameId>"` (frame_id field populated from Task 2 capture).
    - After Task 9, the reclassified row contains the substring `"factored:7"` (status_counts updated to reflect 7 sub-sections all reconstructed).
    - After Task 9, the reclassified row contains the substring `"PAGE-11 INERT per § 4.3"` (joel-only branch documented).
    - After Task 9, the reclassified row contains the substring `"FIFTH joel-only-no-crito-ref branch use"` (production-use count documented).
    - After Task 9, `grep -q "OPEN-30-01" .planning/research/PEN-INVENTORY.md && grep -q "OPEN-30-02" .planning/research/PEN-INVENTORY.md && grep -q "OPEN-30-03" .planning/research/PEN-INVENTORY.md` exits 0 (3 minimum Utilities OPEN flags + any additional from Task 8).
    - After Task 9, `grep -q "Variant Evidence" .planning/research/PEN-INVENTORY.md && grep -q "Plan 30-01" .planning/research/PEN-INVENTORY.md` exits 0 (Phase 30 Plan 30-01 Variant Evidence sub-section ships).
    - After Task 9, `git log -1 --format=%s` returns a commit subject starting with `exec(30-01):`.
    - After Task 9, `git status --porcelain` returns no unstaged changes to `.planning/research/PEN-INVENTORY.md`.
    - GATE: this task does NOT run if Task 8 returned REVISE (corrective batch_design + Task 8 re-run first); GAP responses DO proceed to this task (OPEN-30-NN extensions absorb the gap declaration per VALID-03 carry-forward).
  </acceptance_criteria>
  <done>PEN-INVENTORY line 77 reclassified in-place; Variant Evidence Phase 30 Plan 30-01 sub-section appended; real OPEN-30-NN entries replace seed; carry-forward status updates applied; commit shipped.</done>
</task>

</tasks>

<verification>
**Plan 30-01 overall verification (run at plan close per 30-VALIDATION.md Per-Task Verification Map):**

1. `mcp__pencil__get_editor_state({ include_schema: false })` returns `activeEditor.file` containing `"design/Crito.pen"` (D-153).
2. `mcp__pencil__batch_get({ nodeIds: ["<designSystemPageFrameId>"], readDepth: 2 })` returns a top-level frame named `"Design system"` with width 1440, layout vertical, alignItems center, fill `#ffffffff` literal, `placeholder: false`, `height: "fit_content"` settled, containing EXACTLY 7 children in vertical-stack order: Header instance + page-intro + Colors + Typography + Components + Utilities + Footer instance.
3. `mcp__pencil__batch_get` on Header instance + Footer instance returns ref nodes with `ref: "G0wNOc"` + `ref: "Xs0Hs"` and NO descendants override (D-77 pure-ref pattern; satisfies ROADMAP success criterion 3 cross-page consistency proof at the chassis layer).
4. page-intro section contains 3 text children (H1 + body + JSON-link) matching D-143 v1.3 verbatim content; 3 sibling Pencil notes at document root document noindex + JSON endpoint + sidebar-dropped intent.
5. Colors section contains 3-4 TokenSwatchGrid ref instances + ~15-35 total swatch tiles across all sub-groupings (Primary Accent / Text Variants / Neutral / Usage Guidelines per v1.3 + D-144).
6. Typography section contains ≥ 2 TypeSpecimen ref instances + ~7-15 total specimen tiles (Font Families + Type Scale minimum; Weights + LH per discretion).
7. Components section contains 3 sub-section frames (Primitives + Compounds + Sections) + ≥ 14 and ≤ 20 ComponentShowcase ref instances total per D-148; each ComponentShowcase has a code-snippet-slot with canonical default-variant snippet per D-150; each ComponentShowcase live-instance-slot binds to the correct Phase 23-29 library component ID.
8. Utilities section contains 3 sub-sections (iso-shadow APPROXIMATE + iso-glow STUB + iso-rotate STUB per Open Q3 + Task 8 calibration confirmation) + 3 sibling CSS notes documenting src/styles/global.css mechanics.
9. `mcp__pencil__get_variables({})` returns count == Task 1 baseline (Plan 30-01 ships ZERO net-new tokens per D-151 composition-plan default).
10. `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root returns `"No layout problems."` (text-clipping false-positives on the ~58-instance density text nodes documented as benign per Pitfall 3).
11. All Phase 23-29 baseline component IDs + page frame IDs return structurally UNCHANGED from Task 1 baseline (Dimension 3 regression check passes).
12. Task 8 calibration AskUserQuestion returned APPROVE (per § 4.5 joel-only branch format; token-usage check against RpGbe satisfied per D-62).
13. PEN-INVENTORY line 77 placeholder reclassified in-place to real `Design system` row with frame_id, status_counts factored:7, PAGE-11 INERT, FIFTH joel-only-no-crito-ref branch use documented (D-128 + D-154 pattern).
14. Plan 30-01 commit shipped per planning workflow (`commit_docs: true`).
</verification>

<success_criteria>
Plan 30-01 succeeds when:
- [ ] All 9 tasks complete with pre-flight `get_editor_state` assertion passing before each mutating call (D-153)
- [ ] `Design system` page frame ships at top-level in design/Crito.pen at 1440 width, anchored on s5k41l per § 10.4
- [ ] Page frame contains 7 child sections in vertical-stack order per D-145 + D-146
- [ ] Section/Header (G0wNOc) + Section/Footer (Xs0Hs) instance as pure-ref no-override (D-77) — satisfies ROADMAP success criterion 3
- [ ] page-intro ships v1.3 verbatim content per D-143 + 3 sibling notes (noindex + JSON + sidebar-dropped) per D-93 belt-and-suspenders
- [ ] Colors section instances Plan 30-00 TokenSwatchGrid + Typography section instances Plan 30-00 TypeSpecimen + Components section instances Plan 30-00 ComponentShowcase (D-148 ~16 instances)
- [ ] Components section includes Header + Footer + all 6 other Phase 25-29 sections (D-148 full v2.0 library coverage)
- [ ] Utilities section ships iso-shadow APPROXIMATE + iso-glow STUB + iso-rotate STUB per D-144 + Open Q3 + 3 sibling CSS notes
- [ ] Token surface drift = 0 net-new (composition plan ships zero new tokens per D-151 default)
- [ ] All Phase 23-29 baseline IDs structurally unchanged (Dimension 3 regression)
- [ ] Document root `snapshot_layout` returns `"No layout problems."` (text-clipping false-positives benign per Pitfall 3)
- [ ] Page frame `fit_content` settled (placeholder: false) per Plan 26-02 Task 4 pattern
- [ ] Task 8 calibration AskUserQuestion returned APPROVE (joel-only branch § 4.5 format; token-usage check vs RpGbe per D-62)
- [ ] PEN-INVENTORY line 77 placeholder reclassified in-place to real `Design system` row (D-128 + D-154 pattern)
- [ ] PAGE-11 INERT documented in reclassified row per § 4.3 joel-only branch (no raster-hide operation runs)
- [ ] Variant Evidence Phase 30 Plan 30-01 sub-section appended documenting literal → token bindings + structural choices
- [ ] Real OPEN-30-NN entries appended to PEN-INVENTORY (replacing Plan 30-00 seeds with calibration-gate outcomes)
- [ ] Commit shipped per planning workflow (`commit_docs: true`)
- [ ] Phase 30 success criteria 1-4 satisfied (token gallery + component gallery exist; Header + Footer composed correctly; per-section fidelity labels + calibration artifact + user spot-check before phase close)
</success_criteria>

<output>
After completion, create `.planning/phases/30-design-system-reference-reconstruction/30-01-SUMMARY.md` documenting:
- Pre-flight assertion outcome at each mutating call (Tasks 2-7, 9)
- RpGbe sub-grouping probe outcome (Open Question 1 resolution)
- Captured `designSystemPageFrameId` + sub-section IDs + ComponentShowcase instance IDs + sibling note IDs
- Per-section get_screenshot Tier-1 / Tier-2 outcomes (Pitfall 1 stale-cache severity at ~58-instance density; document actual behavior for Phase 31 carry-forward)
- snapshot_layout document-root outcome + per-frame text-clipping false-positives inventory (Pitfall 3 documented as benign)
- Baseline regression check outcome (Phase 23-29 IDs unmutated; document root +1 page frame only)
- Token surface drift outcome (default 0 net-new)
- Task 8 calibration AskUserQuestion outcome (APPROVE / REVISE / GAP) + per-section fidelity labels final disposition (page-intro APPROXIMATE / Colors EXACT / Typography EXACT / Components EXACT / Utilities iso-shadow APPROXIMATE-or-STUB + iso-glow STUB + iso-rotate STUB / Header-Footer EXACT)
- Open Question 3 resolution (Utilities iso-shadow APPROXIMATE final disposition post-calibration)
- Open Question 5 resolution (type-semantic-prose-code-block SECONDARY validation outcome per D-150 + Phase 28 D-110 chain)
- PEN-INVENTORY line 77 reclassification BEFORE/AFTER quotes
- Variant Evidence rows added (literal → token bindings inventory)
- OPEN-30-NN entries (Plan 30-00 seeds → real Plan 30-01 entries)
- Carry-forward status updates (OPEN-25-07 4th-consumer / OPEN-23-12 pill / OPEN-23-10 heading-5-6)
- Phase 30 success criteria 1-4 verification per ROADMAP
- Frontmatter per Phase 23-29 SUMMARY conventions: `affects`, `subsystem`, `requires`, `provides`, `tech_stack.added`
</output>
