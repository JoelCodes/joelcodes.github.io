---
phase: 38-showcase-page-blog-restyle
plan: 04
type: execute
wave: 2
depends_on: ["38-02"]
requirements: [PAGE-06]
files_modified:
  - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Joel has reviewed the four drafted blog frames in Figma and explicitly approved them (or provided edits that are reflected in the file)"
    - "The approved frames are recorded as canonical build references before any blog build plan runs"
    - "No blog build work (Plans 05, 06) proceeds until this approval is recorded"
  artifacts:
    - path: ".planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md"
      provides: "Approval status flipped to APPROVED with the final canonical node IDs and any Joel edits noted"
      contains: "APPROVED"
  key_links:
    - from: "38-BLOG-FRAMES.md approval"
      to: "Plans 05 and 06 (blog build)"
      via: "hard checkpoint gate — build waves depend on this approval"
      pattern: "APPROVED"
---

<objective>
Present the four drafted blog Figma frames (from Plan 02) to Joel for review and approval. This is the HARD CHECKPOINT (D-08): no blog build work (`.wl-prose`, BlogCard, blog page restyles, expressive-code) may proceed until Joel approves the frames. Once approved, the frames become canonical build references.

Purpose: Enforces D-05/D-06/D-08 — Figma stays the literal single source of truth; the blog is not built from "derived" assumptions but from Joel-approved frames.
Output: `38-BLOG-FRAMES.md` with approval status flipped to APPROVED (and any Joel edits reflected in Figma + noted).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md
@.planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md
@.planning/phases/38-showcase-page-blog-restyle/38-02-SUMMARY.md
</context>

<threat_model>
Approval-gate plan; touches only the record markdown.
- **Build-before-approval (the core risk this plan exists to prevent):** starting blog build without approval violates D-05/D-06/D-08 and could ship a non-canonical blog. Mitigation: this checkpoint is a blocking gate; Plans 05 and 06 declare `depends_on` on it (wave ordering) and must not run until approval is recorded.
- **Stale record:** if Joel edits frames in Figma, the recorded node IDs must still resolve to the edited frames. Mitigation: re-fetch and re-record final node IDs after approval.
No code/build/dependency vectors.
</threat_model>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Blog frame approval checkpoint (hard gate for all blog build work)</name>
  <read_first>
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (drafted frame node IDs, composition notes, COPY GAP log from Plan 02)
    - .planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md (D-06 Claude drafts / Joel approves; D-08 hard checkpoint; D-11 editorial index; D-12 title-first post)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Blog frame drafting protocol; HARD CHECKPOINT note lines ~260-261)
  </read_first>
  <decision>Approve the four drafted blog Figma frames (Blog Index + Blog Post at 1440 and 390) as canonical build references, or request edits before build begins.</decision>
  <context>
    Per D-05/D-06/D-08, Figma is the literal single source of truth and blog frames did not exist. Plan 02 drafted four light-only frames in file `1tg8wIPcvOVC5tPZ8pkGO2` (new "Blog" page), composed from Components page `36:5`. Dark is derived at build via the D-04 landing recipe. This approval is a HARD GATE: the blog build plans (05: .wl-prose + expressive-code + BlogCard; 06: blog page restyles + fidelity gate) cannot start until you approve. The showcase work (Plans 01/03) proceeds in parallel regardless, so the chain does not idle.
  </context>
  <how-to-review>
    1. Open Figma file `1tg8wIPcvOVC5tPZ8pkGO2`, "Blog" page.
    2. Review `Blog Index / 1440` and `Blog Index / 390`: single-column editorial list (title, date, description, tags, optional thumbnail) — not an image grid (D-11).
    3. Review `Blog Post / 1440` and `Blog Post / 390`: title → date/tags meta → featured image → prose order (D-12).
    4. Edit anything directly in Figma (⌘S to save), or note requested changes.
    5. Review the `[COPY GAP]` log in 38-BLOG-FRAMES.md and supply any missing copy.
  </how-to-review>
  <acceptance_criteria>
    - Joel states approval OR provides a specific edit list
    - If edits requested: frames updated in Figma (re-drafted via use_figma or edited by Joel), then re-presented until approved
    - On approval, 38-BLOG-FRAMES.md `Approval status:` is set to APPROVED with the final canonical node IDs and the date
    - Any COPY GAP items resolved with Joel-supplied copy or explicitly deferred with a `[COPY GAP]` note carried to the fidelity gate
  </acceptance_criteria>
  <resume-signal>Type "approved" to unlock blog build (Plans 05, 06), or list the frame edits to make first.</resume-signal>
</task>

</tasks>

<verification>
- `38-BLOG-FRAMES.md` shows `Approval status: APPROVED` with final canonical node IDs and date
- Any Joel-requested frame edits are reflected in Figma and re-approved
- COPY GAP items resolved or explicitly deferred to the fidelity gate
</verification>

<success_criteria>
- Blog frames approved by Joel BEFORE any blog build commit exists (D-08 hard checkpoint honored)
- Canonical build references recorded — Plans 05 and 06 may now proceed
</success_criteria>

<output>
After completion, create `.planning/phases/38-showcase-page-blog-restyle/38-04-SUMMARY.md` recording the approval (or edit-and-reapprove cycle), the final canonical frame node IDs, and any resolved/deferred COPY GAP items.
</output>
