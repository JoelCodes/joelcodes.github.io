---
phase: 38-showcase-page-blog-restyle
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [PAGE-02]
files_modified:
  - .planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md
  - src/data/projects.json
  - src/pages/showcase.astro
  - src/components/layout/SiteHeader.astro
  - astro.config.mjs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "In dev, /showcase renders a Client Work section and a Craft & Experiments section, each showing ProjectCard entries closed by default"
    - "Each ProjectCard on /showcase expands to reveal problem/built/result when opened"
    - "The number of cards in each section matches frame 12:3 (D-03)"
    - "In prod, /showcase redirects to / and is absent from dist and the sitemap"
    - "In prod, the SiteHeader Showcase link is not rendered (desktop and mobile)"
    - "In dev, the SiteHeader Showcase link renders and carries aria-current on /showcase"
  artifacts:
    - path: ".planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md"
      provides: "Frame 12:3 extracted values: card counts per section, section labels/treatment, h1+intro geometry, section backgrounds, gaps, CTA, breakpoint deltas, COPY GAP log"
      min_lines: 40
    - path: "src/pages/showcase.astro"
      provides: "Dev-gated showcase page composing ProjectCard from getCollection('projects')"
      min_lines: 40
      contains: "import.meta.env.PROD"
    - path: "src/data/projects.json"
      provides: "Placeholder entries duplicated to match frame 12:3 card counts per section"
    - path: "src/components/layout/SiteHeader.astro"
      provides: "Showcase nav link gated dev-only (desktop + mobile)"
      contains: "isDev"
    - path: "astro.config.mjs"
      provides: "Sitemap filter excluding /showcase"
      contains: "/showcase"
  key_links:
    - from: "src/pages/showcase.astro"
      to: "getCollection('projects')"
      via: "astro:content section filter on entry.data.section"
      pattern: "getCollection\\(['\"]projects['\"]\\)"
    - from: "src/pages/showcase.astro"
      to: "src/components/wl/ProjectCard.astro"
      via: "import + render per project with headingLevel={3}"
      pattern: "ProjectCard"
    - from: "src/pages/showcase.astro"
      to: "prod exclusion"
      via: "import.meta.env.PROD redirect guard"
      pattern: "import\\.meta\\.env\\.PROD"
---

<objective>
Extract frame `12:3` and build the dev-gated `/showcase` page: Client Work + Craft & Experiments sections composing the already-built `ProjectCard.astro`, plus the dev-gate wiring (page redirect guard, sitemap filter, SiteHeader nav-link gate) and the `projects.json` placeholder duplication to match the frame's card count.

Purpose: Delivers PAGE-02 — the full project-card experience on `/showcase`, dev-gated per D-01/D-02 until real project data exists.
Output: `38-EXTRACTION.md` (frame `12:3` values), `src/pages/showcase.astro` (new), extended `projects.json`, gated `SiteHeader.astro`, sitemap filter in `astro.config.mjs`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md
@.planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md
@.planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md
@.planning/phases/38-showcase-page-blog-restyle/38-RESEARCH.md
</context>

<threat_model>
Static Astro site, no server-side processing, no auth, no user input. Realistic vectors for this plan:
- **Dev-gate leakage to prod:** placeholder project data (D-09/D-10 lock) must never ship publicly. Mitigation: `import.meta.env.PROD` redirect guard on the page + sitemap filter exclusion. Verified by `grep -r "showcase" dist/` returning zero page output after a prod build.
- **Dead link in prod:** the currently-ungated Showcase nav link points at a non-existent prod page. Mitigation: `{isDev && ...}` gate on desktop + mobile links.
- **Supply-chain:** no new npm dependencies introduced this plan (verify `package.json` unchanged).
No other server-side or injection vectors apply (static output only).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract frame 12:3 into 38-EXTRACTION.md</name>
  <read_first>
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Showcase FIDELITY-GAP table lines ~115-127; Page Contract /showcase lines ~229-254; Copywriting Contract)
    - .planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md (D-01..D-04, D-14..D-16; canonical_refs — frame 12:3 is CANONICAL)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (FIDELITY-GAP / FRAME-PENDING table lines ~812-828; showcase.astro pattern section lines ~29-129)
    - src/pages/index.astro (landing precedent: gutter classes, 112px section padding, 1120px column, gradient class pattern, BOOKING_URL usage)
  </read_first>
  <action>
    Open Figma file `1tg8wIPcvOVC5tPZ8pkGO2` in Figma desktop and extract frame `12:3` via the figma-desktop MCP (`get_design_context` / `get_screenshot` — NOT export_nodes, which is broken). Extract every Showcase FIDELITY-GAP item from the UI-SPEC table and record to a new file `.planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md`. Required extracted values: card count per section (Client Work, Craft & Experiments) — this drives Task 2's placeholder count; section label treatment (Eyebrow via `.wl-label-eyebrow` vs `<h2 class="wl-heading-h2">` — state which, with node id); page `<h1>` text + class (likely `.wl-heading-h1-interior` 61px) and intro/lead text + class; gap from header to first section; card-to-card vertical gap; section-to-section gap; which section sits on `--color-wl-paper` vs a sea-glass gradient (map each to the light gradient literal, then to its dark pair from the UI-SPEC Showcase dark recipe table); any CTA / closing block (record copy + whether it is a CTAButton to BOOKING_URL); breakpoint deltas at 390 / 768 / 1920. Copy all page copy verbatim (h1, intro, section labels, CTA). Any missing copy gets a `[COPY GAP]` marker. Record each value with its source node id.
  </action>
  <verify>Read back 38-EXTRACTION.md — confirms non-zero integer card counts for both sections, a section-label treatment decision, h1 copy + class, and a section-background-to-gradient mapping. Any unresolved value is explicitly marked `[COPY GAP]` or `[FIDELITY-GAP]`, never left blank or invented.</verify>
  <acceptance_criteria>
    - File `.planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md` exists with at least 40 lines
    - Contains an integer card count for "client-work" and an integer card count for "craft-experiments" (grep for both section labels finds a numeric value)
    - Contains a line stating section-label treatment as either eyebrow or h2 with a node id reference
    - Contains the page h1 copy string and its ramp class name
    - Every section background is mapped to a light gradient literal (or `var(--color-wl-paper)`) AND its dark counterpart
    - Any unavailable copy is present as a `[COPY GAP]` marker (grep), zero invented copy
  </acceptance_criteria>
  <done>38-EXTRACTION.md holds all frame 12:3 values needed to build showcase.astro with zero invented geometry or copy.</done>
</task>

<task type="auto">
  <name>Task 2: Duplicate projects.json placeholders + gate SiteHeader nav link + sitemap filter</name>
  <read_first>
    - src/data/projects.json (current 2 entries: bakery-order-system → client-work; inventory-sync-automation → craft-experiments; note full v2 schema incl. thumbLabel)
    - src/content.config.ts (projects collection file() loader; entry.id vs entry.data.slug pitfall lines ~18-20)
    - src/components/layout/SiteHeader.astro (isDev at line 5; ungated Showcase link desktop lines ~38-41, mobile lines ~59-62; Blog link gate pattern lines ~46-51 is the model)
    - astro.config.mjs (sitemap filter line ~100: `filter: (page) => !page.includes('/blog')`)
    - .planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md (card counts from Task 1)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (SiteHeader section lines ~389-439; sitemap section lines ~596-614)
  </read_first>
  <action>
    (a) In `src/data/projects.json`, duplicate the two existing placeholder entries so `client-work` count and `craft-experiments` count each equal the counts recorded in 38-EXTRACTION.md (D-03). Each duplicate must be a valid entry against the projects schema in `src/content.config.ts` (unique `slug`, all required fields incl. `thumbLabel`), keeping the D-09 placeholder content verbatim. Do NOT edit `content.config.ts`. (b) In `src/components/layout/SiteHeader.astro`, wrap the desktop Showcase `<a>` (lines ~38-41) and the mobile Showcase `<a>` (lines ~59-62) each in `{isDev && ( ... )}` using the exact Blog-link gate pattern already at lines ~46-51 — preserve the existing `aria-current` logic on the desktop link. `isDev` already exists at line 5; do not add a new variable. (c) In `astro.config.mjs`, change the sitemap filter to `(page) => !page.includes('/blog') && !page.includes('/showcase')`. Do NOT touch the redirects block (D-15).
  </action>
  <verify>Run `npm run astro check` (types pass). Grep `src/data/projects.json` confirms the section counts match 38-EXTRACTION.md. Grep `src/components/layout/SiteHeader.astro` shows both Showcase links inside `isDev` guards. Grep `astro.config.mjs` shows the `/showcase` filter clause.</verify>
  <acceptance_criteria>
    - `grep -c '"section": "client-work"' src/data/projects.json` equals the client-work count in 38-EXTRACTION.md
    - `grep -c '"section": "craft-experiments"' src/data/projects.json` equals the craft-experiments count in 38-EXTRACTION.md
    - All `slug` values in projects.json are unique (no duplicate slug strings)
    - `src/content.config.ts` is unmodified (git diff shows no change)
    - Both `href="/showcase"` anchors in SiteHeader.astro are inside a `{isDev &&` block
    - `astro.config.mjs` sitemap filter contains `!page.includes('/showcase')`
    - `astro.config.mjs` redirects block is unmodified
    - `npm run astro check` exits 0
  </acceptance_criteria>
  <done>projects.json matches frame card counts; the Showcase nav link is dev-only; the sitemap excludes /showcase.</done>
</task>

<task type="auto">
  <name>Task 3: Build src/pages/showcase.astro (dev-gated, composes ProjectCard)</name>
  <read_first>
    - .planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md (all frame 12:3 geometry, copy, section backgrounds, label treatment from Task 1)
    - src/pages/index.astro (imports pattern; gutter classes `px-[24px] sm:px-[32px] lg:px-[160px] min-[1920px]:px-[400px]`; 112px section padding; 1120px centered column; Rule 1 gradient-in-class pattern; BOOKING_URL usage)
    - src/pages/blog/index.astro (dev-gate guard lines ~8-10: `if (import.meta.env.PROD) return Astro.redirect('/');`)
    - src/components/wl/ProjectCard.astro (props interface: eyebrow, title, outcome, summary, tags, problem, built, result, thumbLabel, headingLevel — compose, do NOT modify)
    - src/components/wl/Eyebrow.astro, src/components/wl/CTAButton.astro (available primitives)
    - src/lib/constants.ts (BOOKING_URL)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (showcase.astro pattern lines ~29-129: imports, getCollection filter, section+gradient class pattern, ProjectCard usage, heading hierarchy)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Page Contract /showcase lines ~229-254; heading hierarchy row; Interaction Contracts — all cards closed D-16)
  </read_first>
  <action>
    Create `src/pages/showcase.astro`. Frontmatter: dev-gate guard `if (import.meta.env.PROD) { return Astro.redirect('/'); }` as the first statement; import BaseLayout, `getCollection` from `astro:content`, ProjectCard + Eyebrow + CTAButton from `src/components/wl/`, BOOKING_URL from `src/lib/constants`. Load `const projects = await getCollection('projects')` and split into `clientWork` (filter `p.data.section === 'client-work'`) and `craftExperiments` (filter `p.data.section === 'craft-experiments'`). Template: single `<h1>` using the class + copy recorded in 38-EXTRACTION.md; two `<section>` elements (Client Work, Craft & Experiments) each with the section-label treatment recorded in 38-EXTRACTION.md (Eyebrow or `<h2 class="wl-heading-h2">`), rendering one `<ProjectCard headingLevel={3} .../>` per entry passing all props from `entry.data`. NO `open` attribute on any card (D-16, all closed). Use `entry.data.slug` for any URL, `entry.id` only for keying. Section backgrounds use the Rule 1 class pattern (`class="[background:...] dark:[background:...]"`) with the light+dark gradient pairs from 38-EXTRACTION.md — gradients in `class`, never inline `style`. Gutters/padding/max-width use the landing values verbatim (`px-[24px] sm:px-[32px] lg:px-[160px] min-[1920px]:px-[400px]`, 112px vertical padding, 1120px centered column). If 38-EXTRACTION.md records a CTA/closing block, render it via CTAButton to BOOKING_URL with the extracted copy; if it is `[COPY GAP]`, render the marker as a comment and flag at the gate. Any heading size with no ramp match: local style with a source-node comment (ProjectCard precedent), never a guessed value.
  </action>
  <verify>`npm run dev` then load `http://localhost:4321/showcase`: page renders with two labelled sections, N cards each matching 38-EXTRACTION.md counts, all cards closed on load, each expands on click revealing problem/built/result. `npm run astro check` passes. Grep confirms zero old neobrutalist tokens (`text-turquoise`, `font-heading`, `text-text-muted`) in showcase.astro.</verify>
  <acceptance_criteria>
    - `src/pages/showcase.astro` exists, ≥40 lines, first frontmatter statement matches `if (import.meta.env.PROD)` redirect to '/'
    - Contains exactly one `<h1>` element
    - Contains two `<section>` elements with the section labels from 38-EXTRACTION.md
    - Renders `<ProjectCard` with `headingLevel={3}` and NO `open` attribute anywhere in the file
    - Contains `getCollection('projects')` and two `.filter(` calls on `p.data.section`
    - Every gradient background is on a `class` attribute (arbitrary-value syntax), zero `style="background:...gradient` inline
    - `grep -E "text-turquoise|font-heading|text-text-muted" src/pages/showcase.astro` returns nothing
    - `npm run astro check` exits 0
    - In a running dev server, `/showcase` returns HTTP 200 and renders both sections; `/showcase` cards are closed (no `open`) on initial load
  </acceptance_criteria>
  <done>/showcase renders both sections composing ProjectCard closed-by-default, dev-gated, using landing layout precedent and frame 12:3 geometry.</done>
</task>

</tasks>

<verification>
- `npm run astro check` exits 0
- Dev server: `/showcase` renders two labelled sections; card counts per section match 38-EXTRACTION.md; all cards closed on load and expand on click
- `npm run build && grep -rl "showcase" dist/**/*.html 2>/dev/null` returns no page output (prod exclusion works)
- `dist/sitemap*.xml` contains no `/showcase` URL
- Prod build HTML for the header contains no `/showcase` link
- `git diff --stat src/content.config.ts` is empty (schema untouched)
- No new entries in `package.json` dependencies
</verification>

<success_criteria>
- PAGE-02 build complete: `/showcase` delivers Client Work + Craft & Experiments sections via ProjectCard, closed by default, expandable, at frame 12:3 geometry (dark derived per D-04)
- Dev-gate fully wired: page redirect guard, sitemap filter, dev-only nav link (desktop + mobile)
- projects.json placeholder count matches frame 12:3 per section (D-03)
- Zero old neobrutalist tokens in showcase.astro; zero new npm dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/38-showcase-page-blog-restyle/38-01-SUMMARY.md` recording: frame 12:3 card counts per section, section-label treatment chosen, any `[COPY GAP]` / `[FIDELITY-GAP]` markers for the fidelity gate, and the exact section-background gradient pairs applied.
</output>
