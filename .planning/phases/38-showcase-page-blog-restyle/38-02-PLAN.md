---
phase: 38-showcase-page-blog-restyle
plan: 02
type: execute
wave: 1
depends_on: []
requirements: [PAGE-06]
files_modified:
  - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Four blog frames exist in Figma file 1tg8wIPcvOVC5tPZ8pkGO2: Blog Index / 1440, Blog Index / 390, Blog Post / 1440, Blog Post / 390"
    - "The frames are composed from the existing Components page 36:5 components and brand variables (no invented primitives)"
    - "The blog index frame is a single-column editorial list (title, date, description, tags, optional thumbnail) — not an image-led card grid"
    - "The blog post frame orders content title-first, then date/tags meta, then featured image, then prose"
    - "The drafted frame node IDs are recorded for downstream approval and build reference"
  artifacts:
    - path: ".planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md"
      provides: "Record of drafted blog frame node IDs, frame names, composition notes, and the [COPY GAP] log — pending Joel approval"
      min_lines: 25
  key_links:
    - from: "38-BLOG-FRAMES.md"
      to: "Figma file 1tg8wIPcvOVC5tPZ8pkGO2"
      via: "recorded node IDs of the four drafted frames"
      pattern: "1tg8wIPcvOVC5tPZ8pkGO2"
---

<objective>
Draft the four blog Figma frames (Blog Index + Blog Post, each at 1440 and 390, light-only) in the brand file `1tg8wIPcvOVC5tPZ8pkGO2` using the claude.ai Figma MCP `use_figma`, composed from the existing Components page `36:5` components and brand variables. Record the drafted node IDs so Joel can review and approve them (approval is Plan 04, the hard checkpoint gating all blog build work per D-08).

Purpose: Closes the blog design gap IN Figma (D-05) rather than deriving a whole page surface in code — keeps Figma the literal single source of truth before PAGE-06 build work begins.
Output: Four drafted frames in Figma + `38-BLOG-FRAMES.md` recording their node IDs and composition notes for approval.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md
@.planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md
</context>

<threat_model>
This plan writes to an external Figma file only — no code, no build output, no site surface changes. Vectors:
- **Wrong-file / wrong-page write:** drafting into the wrong Figma file or overwriting existing frames would corrupt the design source of truth. Mitigation: create a NEW "Blog" page in file `1tg8wIPcvOVC5tPZ8pkGO2`; never modify existing frames (`12:2`, `12:3`, `36:5`, `117:103`, `85:103`, `85:104`); record node IDs of only the newly created frames.
- **Skill-prerequisite bypass:** calling `use_figma` without loading its mandatory skill risks malformed writes. Mitigation: load `/figma-use` (fallback `skill://figma/figma-use/SKILL.md`) and `/figma-generate-design` before any `use_figma` call.
No supply-chain, injection, or dev-gate vectors (no repo files touched except the record markdown).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Load Figma skills and draft the four blog frames</name>
  <read_first>
    - skill://figma/figma-use/SKILL.md (MANDATORY before any use_figma call — per MCP server instructions)
    - skill://figma/figma-generate-design/SKILL.md (translating a page layout into Figma)
    - .planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md (D-05, D-06, D-07, D-08, D-11, D-12; canonical_refs — compose from Components page 36:5; frames DO NOT EXIST yet)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Blog frame drafting protocol lines ~263-274; Blog index structural contracts lines ~276-287; Blog post structural contracts lines ~289-299; light-only discretion call — dark derived via D-04)
  </read_first>
  <action>
    First, read the `/figma-use` skill (fallback `skill://figma/figma-use/SKILL.md`) and `/figma-generate-design` skill in full — this is mandatory before any `use_figma` call. Then draft four frames via `use_figma` in Figma file `1tg8wIPcvOVC5tPZ8pkGO2` on a NEW page named "Blog" (do not modify existing pages/frames). Frame names and sizes: `Blog Index / 1440` (1440 wide), `Blog Index / 390` (390 wide), `Blog Post / 1440` (1440 wide), `Blog Post / 390` (390 wide). Draft LIGHT-ONLY (dark is derived at build time via the D-04 landing recipe). Compose only from the existing Components page `36:5` components and brand variables — do not invent new primitives. Blog Index layout: single-column editorial list of full-width stacked entries — each entry has title (Fraunces), date, description (Hanken), and tags; a small thumbnail is optional (decide presence + size in the frame). NOT an image-led card grid. Blog Post layout: title first, then date/tags meta row, then the featured image, then prose body (Fraunces headings / Hanken body, wl-accent links, blockquote, lists per the .wl-prose contract). Use the brand type ramp, sea-cool palette, and 8-point spacing. Any copy that is not derivable from real post data gets a `[COPY GAP]` marker in the frame or the record file — never invent post copy.
  </action>
  <verify>Retrieve the file's page/frame list via the Figma MCP and confirm the four named frames exist on a "Blog" page with the correct widths. Confirm no existing frame node IDs (`12:2`, `12:3`, `36:5`, `117:103`) were modified.</verify>
  <acceptance_criteria>
    - A "Blog" page exists in file `1tg8wIPcvOVC5tPZ8pkGO2` containing four frames named exactly `Blog Index / 1440`, `Blog Index / 390`, `Blog Post / 1440`, `Blog Post / 390`
    - `Blog Index / 1440` and `Blog Index / 390` present a single-column vertical stack of entries (no multi-column grid)
    - `Blog Post / 1440` and `Blog Post / 390` order elements: title → meta (date/tags) → featured image → prose
    - The `/figma-use` skill was read before the first `use_figma` call
    - No pre-existing frame (12:2 / 12:3 / 36:5 / 117:103 / 85:103 / 85:104) was modified
  </acceptance_criteria>
  <done>Four light-only blog frames exist in the brand file, composed from 36:5, ready for Joel's review.</done>
</task>

<task type="auto">
  <name>Task 2: Record drafted frame node IDs in 38-BLOG-FRAMES.md</name>
  <read_first>
    - .planning/phases/38-showcase-page-blog-restyle/38-CONTEXT.md (D-06 approval flow — node IDs recorded in plan/extraction artifact; D-09 fidelity gate needs canonical refs)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Blog frame drafting protocol — "node IDs recorded in the plan/extraction artifact")
  </read_first>
  <action>
    Create `.planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md`. Record for each of the four frames: its exact frame name, its Figma node ID (from the Figma MCP), the file key `1tg8wIPcvOVC5tPZ8pkGO2`, and a one-line composition note stating which `36:5` components it used. Add an "Approval status: PENDING JOEL" line (approval happens in Plan 04). Add a `[COPY GAP]` log listing any copy that was not derivable and needs Joel's input. State explicitly that these frames become canonical build references only after approval.
  </action>
  <verify>Read back 38-BLOG-FRAMES.md — confirms four frame entries each with a node ID, the file key, an "Approval status: PENDING JOEL" line, and a COPY GAP section.</verify>
  <acceptance_criteria>
    - File `.planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md` exists, ≥25 lines
    - Contains four distinct Figma node IDs, one per frame name
    - Contains the string `1tg8wIPcvOVC5tPZ8pkGO2`
    - Contains a line matching `Approval status:` with value PENDING (or equivalent)
    - Contains a `[COPY GAP]` section (may be "none" if all copy was derivable)
  </acceptance_criteria>
  <done>The four frame node IDs and composition notes are recorded and marked pending approval.</done>
</task>

</tasks>

<verification>
- Figma file `1tg8wIPcvOVC5tPZ8pkGO2` has a "Blog" page with the four named frames at correct widths
- `38-BLOG-FRAMES.md` records all four node IDs, the file key, pending-approval status, and a COPY GAP log
- No existing Figma frames modified
- No repo source/build files touched (only the record markdown)
</verification>

<success_criteria>
- Four light-only blog frames drafted in the brand file, composed from Components page 36:5 (D-05/D-06/D-07)
- Node IDs recorded for downstream approval and build reference
- Frames follow D-11 (editorial single-column index) and D-12 (title-first post) structural contracts
- Zero invented post copy — gaps flagged as `[COPY GAP]`
</success_criteria>

<output>
After completion, create `.planning/phases/38-showcase-page-blog-restyle/38-02-SUMMARY.md` recording the four frame node IDs, composition sources from 36:5, the light-only + derived-dark decision, and the COPY GAP log — noting that Plan 04 (approval checkpoint) must clear before any blog build plan runs.
</output>
