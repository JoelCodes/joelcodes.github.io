---
phase: 38-showcase-page-blog-restyle
plan: 06
type: execute
wave: 4
depends_on: ["38-05"]
requirements: [PAGE-06]
files_modified:
  - src/pages/blog/index.astro
  - src/pages/blog/tags/[tag].astro
  - src/pages/blog/[slug].astro
  - tests/accessibility/blog.spec.ts
  - scripts/check-contrast.mjs
autonomous: false
user_setup: []

must_haves:
  truths:
    - "In dev, /blog renders a single-column editorial list of BlogCard entries in the Wavelength brand (no tag-filter JS, no card grid)"
    - "In dev, /blog/[slug] renders title-first, then date/tags meta, then featured image, then .wl-prose content in a single centered column (no TOC, no 3-column grid)"
    - "In dev, /blog/tags/[tag] renders the index layout with a filtered heading announcing the tag"
    - "The blog post featured image uses loading='eager' at the LCP position and Lighthouse lcp-lazy-loaded passes"
    - "Blog URLs are unchanged and all three page types return 200 in dev; existing PROD guards are preserved"
    - "The three blog pages and the .wl-prose scope contain zero old neobrutalist tokens"
    - "Blog pages have zero axe violations light + dark; new blog contrast pairs pass 4.5:1"
  artifacts:
    - path: "src/pages/blog/index.astro"
      provides: "Wavelength editorial single-column blog index using wl/BlogCard"
      contains: "wl/BlogCard.astro"
    - path: "src/pages/blog/[slug].astro"
      provides: "Wavelength blog post: title-first, .wl-prose, Breadcrumb, loading=eager"
      contains: "loading=\"eager\""
    - path: "src/pages/blog/tags/[tag].astro"
      provides: "Restyled tag page (index layout + filtered heading)"
      contains: "wl/BlogCard.astro"
    - path: "tests/accessibility/blog.spec.ts"
      provides: "axe-core spec for /blog and a /blog/[slug] in light + dark"
      contains: "/blog"
  key_links:
    - from: "src/pages/blog/index.astro"
      to: "src/components/wl/BlogCard.astro"
      via: "import + map posts to BlogCard in a ul role=list"
      pattern: "wl/BlogCard"
    - from: "src/pages/blog/[slug].astro"
      to: ".wl-prose scope"
      via: "content wrapper class .wl-prose"
      pattern: "wl-prose"
    - from: "src/pages/blog/[slug].astro"
      to: "src/components/wl/Breadcrumb.astro"
      via: "Breadcrumb back-link replacing custom SVG anchor"
      pattern: "Breadcrumb"
---

<objective>
Restyle the three blog pages in place to the Wavelength brand using the Plan 05 foundations (`.wl-prose`, `wl/BlogCard.astro`, rethemed expressive-code): editorial single-column index, title-first post with `.wl-prose`, derived tag page, plus the `loading="eager"` LCP fix — then verify with an axe spec, contrast additions, and the blog fidelity gate. Gated behind frame approval (Plan 04, via Plan 05 dependency).

Purpose: Completes PAGE-06 — the blog restyle to the new brand with URLs unchanged and the featured-image LCP bug fixed.
Output: three restyled blog pages, `tests/accessibility/blog.spec.ts`, extended contrast matrix, and a Joel-approved blog fidelity gate.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md
@.planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md
@.planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md
@.planning/phases/38-showcase-page-blog-restyle/38-05-SUMMARY.md
</context>

<threat_model>
Static, dev-only blog surfaces; no server/auth/input.
- **Dev-gate preservation:** the existing `import.meta.env.PROD` guards in all three blog pages MUST be preserved — removing them would leak dev-only blog pages to prod (D-13). Mitigation: explicit "do not remove the guard" in each task + verify `grep import.meta.env.PROD` still present in all three, and a prod build shows no /blog output.
- **LCP regression:** the fix changes `loading="lazy"` to `eager` on the LCP image (roadmap criterion 3 / FOUND-04). Verified by Lighthouse `lcp-lazy-loaded` passing on a post URL.
- **Content injection via MDX:** repo-authored content only; `.wl-prose` applies to trusted MDX — no new sanitization concern.
- **Contrast:** new blog text/background pairs (accent link on paper, inline-code on sea-glass, code-block text on rethemed bg) must pass AA before ship.
No supply-chain change (no new deps).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Restyle blog index + tag page (editorial single-column, wl/BlogCard)</name>
  <read_first>
    - src/pages/blog/index.astro (current: BlogCard old import line 3, Button import line 4, allTags line ~22, filter buttons block lines ~47-67, grid section lines ~70-83, load-more lines ~86-96, inline script lines ~100-179; PROD guard lines ~6-10 MUST be preserved; getCollection+sort lines ~13-28 preserved)
    - src/pages/blog/tags/[tag].astro (current: BlogCard old import line 3; getStaticPaths PROD guard lines ~7-9 preserved; getCollection+tag filter+displayTag lines ~9-35 preserved; 3-col grid to replace)
    - src/components/wl/BlogCard.astro (the new component from Plan 05 — props: title, slug, pubDate, description, tags, featuredImage, readingTime)
    - src/components/wl/Breadcrumb.astro (for the tag page back-link)
    - src/pages/index.astro (section structure: gutter classes, 112px padding, 1120px column, paper background)
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (approved Blog Index frame: h1/intro copy + geometry, entry gaps, page header treatment)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (blog/index pattern lines ~211-267; tags/[tag] pattern lines ~356-386)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Blog index structural contracts; Tag page derived surface D-10)
  </read_first>
  <action>
    In `src/pages/blog/index.astro`: PRESERVE the `import.meta.env.PROD` redirect guard (lines ~6-10) and the getCollection+sort logic (lines ~13-28). Replace `import BlogCard from '../../components/BlogCard.astro'` with `import BlogCard from '../../components/wl/BlogCard.astro'`. REMOVE: the old `ui/Button.astro` import (line 4), the `allTags` const (line ~22), the entire filter-buttons block (lines ~47-67), the load-more block (lines ~86-96), and the entire `<script is:inline>` block (lines ~100-179) — D-11 editorial list has zero JS. Replace the `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3` section with a `<main>` using the landing section pattern (gutters `px-[24px] sm:px-[32px] lg:px-[160px] min-[1920px]:px-[400px]`, `background: var(--color-wl-paper)`, 112px vertical padding, 1120px centered column) containing a page `<h1>` (copy + class from the approved Blog Index frame) and a `<ul role="list">` (list-style none, no margin/padding) mapping each post to `<li><BlogCard title=... slug={post.id} pubDate=... description=... tags=... featuredImage=... readingTime=... /></li>`. In `src/pages/blog/tags/[tag].astro`: PRESERVE the getStaticPaths PROD guard (lines ~7-9) and the getCollection+tag-filter+displayTag logic (lines ~9-35). Replace the old BlogCard import with `../../../components/wl/BlogCard.astro`; add `import Breadcrumb from '../../../components/wl/Breadcrumb.astro'`. Rebuild the page as the index layout + a filtered heading announcing the tag (e.g. "Posts tagged '{displayTag}'" — exact wording from the approved index heading pattern; `[COPY GAP]` if none), a `<Breadcrumb items={[{ label: 'Blog', href: '/blog' }, { label: displayTag }]} />`, and the same `<ul role="list">` BlogCard stack. Remove all old neobrutalist tokens (`text-turquoise`, `font-heading`, `text-text-muted-light dark:text-text-muted-dark`, `font-bold uppercase`) from both files.
  </action>
  <verify>`npm run astro check` passes. `npm run dev`: `/blog` renders a single-column list of restyled entries (no filter buttons, no grid); `/blog/tags/{existing-tag}` renders the index layout with a tag heading + breadcrumb. Grep both files for old tokens (zero) and for `wl/BlogCard.astro` (present). Grep confirms PROD guards still present in both.</verify>
  <acceptance_criteria>
    - `src/pages/blog/index.astro` imports `../../components/wl/BlogCard.astro` and contains a `<ul role="list">` mapping posts to `<BlogCard`
    - `src/pages/blog/index.astro` contains NO `<script`, no `filter-container`, no `grid-cols` grid section, no `ui/Button` import
    - `src/pages/blog/index.astro` still contains `import.meta.env.PROD`
    - `src/pages/blog/tags/[tag].astro` imports `wl/BlogCard.astro` and `wl/Breadcrumb.astro`, still contains `import.meta.env.PROD` in getStaticPaths, and renders a heading referencing the tag
    - `grep -E "text-turquoise|font-heading|text-text-muted|font-bold uppercase" src/pages/blog/index.astro src/pages/blog/tags/[tag].astro` returns nothing
    - `npm run astro check` exits 0
    - In dev, `/blog` and `/blog/tags/{existing-tag}` return HTTP 200
  </acceptance_criteria>
  <done>Blog index + tag pages are Wavelength editorial single-column lists using wl/BlogCard, JS-free, guards intact.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle blog post page (title-first, .wl-prose, Breadcrumb, LCP fix)</name>
  <read_first>
    - src/pages/blog/[slug].astro (current: TableOfContents import line 4, getStaticPaths PROD guard lines ~7-8 preserved, headings destructure line ~20, 3-col grid line ~44, TOC aside lines ~46-49, custom back-link lines ~36-42 and ~101-107, tag pills lines ~70-79, loading="lazy" line ~89, .prose wrapper line ~95, empty right col lines ~110-112)
    - src/components/wl/Breadcrumb.astro (items prop: [{label, href}, {label}] — last item current page, no href)
    - src/components/wl/Tag.astro (tag pill primitive)
    - src/styles/global.css (the .wl-prose scope from Plan 05)
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (approved Blog Post frame: article column width, image treatment/radius/margin, meta-row styling, heading rhythm, back-link label copy)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (blog/[slug] pattern lines ~271-354: preserve/remove/replace list, token replacement table, new layout order)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Blog post structural contracts lines ~289-299: title→meta→image→prose, LCP fix, Breadcrumb, guard preserved)
  </read_first>
  <action>
    In `src/pages/blog/[slug].astro`: PRESERVE the getStaticPaths PROD guard (lines ~7-8) and `Astro.props`/`render(post)`/`readingTime`/date formatting (lines ~19-30). REMOVE: `import TableOfContents` (line 4), the `headings` item from the `render()` destructure (line ~20), the 3-column grid wrapper (line ~44), the TOC `<aside>` (lines ~46-49), and the empty right column (lines ~110-112). Add `import Breadcrumb from '../../components/wl/Breadcrumb.astro'` and `import Tag from '../../components/wl/Tag.astro'`. Replace the custom back-link SVG anchors (lines ~36-42 and ~101-107) with `<Breadcrumb items={[{ label: 'Blog', href: '/blog' }, { label: post.data.title }]} />` (label copy from the approved frame; `[COPY GAP]` if it specifies different chrome text). Replace the neobrutalist tag anchors (lines ~70-79) with `<a href={`/blog/tags/${tag}`}><Tag>{tag}</Tag></a>` for each tag. FIX the LCP bug: change `loading="lazy"` to `loading="eager"` on the featured image (line ~89). Change the content wrapper class from `prose` to `wl-prose` (line ~95). Restructure the article to a single centered column ordered: `<h1>` title (copy = post title; class from approved frame) → date/tags meta row (`<time datetime>` + Tag pills) → featured image (`loading="eager"`, alt = post title, radius/margin from frame) → `<div class="wl-prose"><Content /></div>`. Article column width from the approved frame (default `max-width: 65ch` with a source-node comment until confirmed). Replace every remaining old neobrutalist token per the PATTERNS replacement table (`font-heading`→ramp class, `text-turquoise`→`text-wl-accent`, `text-text-muted-*`→`var(--color-wl-sub)`, etc.).
  </action>
  <verify>`npm run astro check` passes. `npm run dev`: a `/blog/{slug}` post renders title-first, then meta, then image, then prose (single centered column, no TOC/sidebar); Breadcrumb back-link works; code blocks styled via the Plan 05 retheme. Grep line ~89 region shows `loading="eager"` and no `loading="lazy"` remains in the file; grep shows `wl-prose` (not bare `prose`) on the content wrapper and zero old neobrutalist tokens. `import.meta.env.PROD` still present.</verify>
  <acceptance_criteria>
    - `src/pages/blog/[slug].astro` contains `loading="eager"` and `grep 'loading="lazy"' src/pages/blog/[slug].astro` returns nothing
    - Content wrapper uses `class="wl-prose"` (not `class="prose"`)
    - Contains `import Breadcrumb` and renders `<Breadcrumb items={` with a `/blog` back-link
    - Contains NO `import TableOfContents`, no `grid-cols-[200px_1fr_200px]`, no `<aside`
    - Layout order in the template is h1 → meta → img → wl-prose content
    - Still contains `import.meta.env.PROD` in getStaticPaths
    - `grep -E "text-turquoise|font-heading|text-text-muted" src/pages/blog/[slug].astro` returns nothing
    - `npm run astro check` exits 0
  </acceptance_criteria>
  <done>Blog post is a title-first single centered column with .wl-prose + Breadcrumb; LCP image is eager; guard intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blog axe spec + contrast additions, then blog fidelity gate</name>
  <read_first>
    - tests/accessibility/landing.spec.ts (axe spec pattern to mirror)
    - tests/accessibility/helpers.ts (settleAnimations)
    - scripts/check-contrast.mjs (PAIRS array; existing color constants; PHASE 38 block if Plan 03 already added one — append to it, do not duplicate)
    - .planning/phases/38-showcase-page-blog-restyle/38-05-SUMMARY.md (expressive-code literals chosen — need contrast pairs for code text vs block bg)
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (approved frames — the fidelity comparison authority)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Accessibility Contract; Fidelity Gate matrix lines ~417-439; PATTERNS check-contrast additions lines ~627-649)
  </read_first>
  <action>
    First (autonomous portion): create `tests/accessibility/blog.spec.ts` mirroring landing.spec.ts — a light + dark axe test for `/blog` and for one `/blog/{first-post-slug}` (four tests total, dev context, dark test asserts `html` has class `dark`). Extend `scripts/check-contrast.mjs` PHASE 38 block with the blog pairs from the PATTERNS additions: `.wl-prose` accent link on paper (light + dark), inline-code text (`--wl-sub`) on `--wl-sea-glass` tint (light + dark), and code-block text vs the rethemed block background literals recorded in 38-05-SUMMARY (light + dark) — each threshold 4.5, textUse true. Fix any failure by adjusting the offending token/literal (record it), never by lowering the threshold. Run `npx playwright test tests/accessibility/blog.spec.ts` (all pass) and `node scripts/check-contrast.mjs` (exits 0). Then present the fidelity gate below.
  </action>
  <what-built>
    The three restyled blog pages (index editorial list, title-first post with .wl-prose, derived tag page), the LCP `loading="eager"` fix, brand-themed code blocks, axe specs passing light + dark, and the contrast matrix extended and passing.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Capture rendered `/blog` and a `/blog/{slug}` at 1440 and 390, light + dark (Playwright `page.screenshot()`; store under `.planning/phases/38-showcase-page-blog-restyle/fidelity/blog/`).
    3. Manually export the approved Blog Index + Blog Post frames from Figma (node IDs in 38-BLOG-FRAMES.md) at 1440 and 390.
    4. Present side-by-side: rendered vs approved frame for index + post at 1440 + 390, light. Dark is derived (D-04) — flag it; no dark frame comparison. Tag page reviewed as a derived surface (spot-check, no frame comparison).
    5. Confirm pre-gate checklist: `loading="eager"` on the post image and Lighthouse `lcp-lazy-loaded` passes on a post URL; `npm run build` shows no /blog or /showcase pages in dist and sitemap excludes both; blog URLs all 200 in dev; code blocks follow the `.dark` toggle; `node scripts/check-contrast.mjs` passes; grep for old neobrutalist tokens across the three blog pages + `.wl-prose` scope returns zero; `[COPY GAP]` log presented.
  </how-to-verify>
  <acceptance_criteria>
    - `tests/accessibility/blog.spec.ts` exists, covers `/blog` and a `/blog/{slug}` in light + dark; `npx playwright test tests/accessibility/blog.spec.ts` reports all passed
    - `node scripts/check-contrast.mjs` exits 0 with blog pairs added (accent-link-on-paper, inline-code-on-sea-glass, code-block-text-on-bg, light + dark)
    - Lighthouse `lcp-lazy-loaded` audit passes on a blog post URL
    - `npm run build` produces no `/blog` or `/showcase` page HTML in dist; sitemap excludes both
    - Rendered vs approved-frame screenshots captured for index + post at 1440 + 390 (light) plus dark rendered (derived)
    - Joel approves the blog fidelity comparison OR provides a specific gap list
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the blog fidelity gate, or list specific blog fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npm run astro check` exits 0
- `npx playwright test tests/accessibility/blog.spec.ts` → all passed (light + dark, index + post)
- `node scripts/check-contrast.mjs` → exits 0 with Phase 38 blog pairs
- Lighthouse `lcp-lazy-loaded` passes on a post URL
- `npm run build && grep -rl "/blog" dist/**/*.html; grep -rl "showcase" dist/**/*.html` → no page output; sitemap excludes both
- Blog URLs (`/blog`, `/blog/[slug]`, `/blog/tags/[tag]`) all 200 in dev; PROD guards intact in all three files
- grep for old neobrutalist tokens across the three blog pages + `.wl-prose` scope → zero
- Joel approval recorded for the blog fidelity gate
</verification>

<success_criteria>
- PAGE-06 complete: blog index/post/tag restyled to Wavelength, URLs unchanged, LCP bug fixed, code blocks brand-themed, axe-clean both themes, contrast passing
- Blog fidelity gate approved (index + post at 1440/390 light; dark derived; tag page derived)
- Roadmap criterion 3 satisfied (loading="eager" + Lighthouse LCP passes)
</success_criteria>

<output>
After completion, create `.planning/phases/38-showcase-page-blog-restyle/38-06-SUMMARY.md` recording: the restyle changes per page, the LCP fix + Lighthouse result, blog contrast additions, fidelity screenshot locations, and Joel's approval (or resolved gap list).
</output>
