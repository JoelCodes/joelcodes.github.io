---
phase: 38-showcase-page-blog-restyle
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
requirements: [PAGE-02]
files_modified:
  - tests/accessibility/showcase.spec.ts
  - scripts/check-contrast.mjs
autonomous: false
user_setup: []

must_haves:
  truths:
    - "The /showcase page has zero axe-core violations in both light and dark modes (dev context)"
    - "Every new showcase text/background color pair passes the contrast check at 4.5:1"
    - "The rendered /showcase (closed + expanded card states) matches frame 12:3 at 390/768/1440/1920, with dark reviewed as derived (D-04)"
  artifacts:
    - path: "tests/accessibility/showcase.spec.ts"
      provides: "axe-core Playwright spec for /showcase in light + dark"
      min_lines: 25
      contains: "/showcase"
    - path: "scripts/check-contrast.mjs"
      provides: "PAIRS extended with Phase 38 showcase section-band pairs from frame 12:3 extraction"
      contains: "PHASE 38"
  key_links:
    - from: "tests/accessibility/showcase.spec.ts"
      to: "/showcase dev page"
      via: "page.goto('/showcase') + AxeBuilder analyze"
      pattern: "page\\.goto\\(['\"]/showcase['\"]\\)"
---

<objective>
Verify the `/showcase` page built in Plan 01: add an axe-core accessibility spec (light + dark), extend the contrast matrix with any showcase section-band pairs from the frame `12:3` extraction, then run the Figma-vs-rendered fidelity gate (frame `12:3`, closed + expanded card states, four breakpoints) for Joel's approval.

Purpose: Enforces the per-phase design-fidelity gate (roadmap criterion 5) and the accessibility contract for PAGE-02.
Output: `tests/accessibility/showcase.spec.ts`, extended `scripts/check-contrast.mjs`, and a Joel-approved fidelity comparison.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md
@.planning/phases/38-showcase-page-blog-restyle/38-01-SUMMARY.md
@.planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md
</context>

<threat_model>
Verification-only plan (test + contrast script + screenshot gate); no site surface or dependency changes.
- **False-pass accessibility:** a spec that does not actually load the dev-only page would silently pass. Mitigation: assert `/showcase` returns rendered content and `html` carries `.dark` in the dark test; run against the dev server (showcase is dev-only, absent from prod build).
- **Contrast regression shipped unchecked:** new section-band literals from frame 12:3 must be added to the PAIRS matrix before the gate. Mitigation: `node scripts/check-contrast.mjs` must pass with the Phase 38 additions.
No other vectors (static site, no auth/input).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add showcase axe spec + extend contrast matrix</name>
  <read_first>
    - tests/accessibility/landing.spec.ts (the exact axe spec pattern: wcagTags, light test, dark test via browser.newContext colorScheme dark, settleAnimations helper)
    - tests/accessibility/helpers.ts (settleAnimations)
    - scripts/check-contrast.mjs (PAIRS array lines ~171-277; existing color constant names L_ACCENT/L_PAPER/L_SUB/L_SEA_GLASS/D_* etc.)
    - .planning/phases/38-showcase-page-blog-restyle/38-EXTRACTION.md (showcase section-background gradient literals — any new band colors need contrast pairs)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Accessibility Contract lines ~360-368; showcase /showcase row: one h1, sections as h2, ProjectCard headingLevel={3}, axe zero violations both themes)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (Axe test pattern lines ~767-802; check-contrast PAIRS extension lines ~618-649)
  </read_first>
  <action>
    Create `tests/accessibility/showcase.spec.ts` mirroring `tests/accessibility/landing.spec.ts` exactly: a `test.describe('Showcase Page Accessibility')` with a light test (`page.goto('/showcase')`, `settleAnimations`, `AxeBuilder({ page }).withTags(wcagTags).analyze()`, expect zero violations) and a dark test (`browser.newContext({ colorScheme: 'dark' })`, assert `html` has class `dark`, then same axe analyze). Use the same `wcagTags` array as landing.spec.ts. Then extend `scripts/check-contrast.mjs`: in a labelled `// ── PHASE 38 ADDITIONS` block appended after the last existing PAIRS row (before the closing `];`), add contrast pairs for any showcase section-band text/background combinations recorded in 38-EXTRACTION.md that are not already in the matrix — each row `[foreground_hex_or_const, background_hex_or_const, 'descriptive label', 4.5, true]`. If frame 12:3 uses only sea-glass/paper backgrounds already covered by existing L_INK/L_SUB pairs, state that in a comment and add no redundant rows. Fix any violation surfaced (adjust the section band or text token in showcase.astro from Plan 01 — record the change) rather than lowering the threshold.
  </action>
  <verify>Run `npx playwright test tests/accessibility/showcase.spec.ts` — both tests pass (zero violations light + dark). Run `node scripts/check-contrast.mjs` — exits 0 with Phase 38 pairs included.</verify>
  <acceptance_criteria>
    - `tests/accessibility/showcase.spec.ts` exists, ≥25 lines, contains `page.goto('/showcase')` and a dark-mode test asserting `html` class `dark`
    - `npx playwright test tests/accessibility/showcase.spec.ts` reports 2 passed, 0 failed
    - `scripts/check-contrast.mjs` contains a `PHASE 38` labelled block
    - `node scripts/check-contrast.mjs` exits 0
    - No contrast threshold below 4.5 was introduced for text pairs
  </acceptance_criteria>
  <done>Showcase passes axe in both themes and all new contrast pairs meet 4.5:1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Showcase fidelity gate (frame 12:3 vs rendered)</name>
  <read_first>
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Fidelity Gate section lines ~417-439: comparison matrix + pre-gate checklist)
    - .planning/phases/38-showcase-page-blog-restyle/38-01-SUMMARY.md (COPY GAP / FIDELITY-GAP markers, section-label + background decisions)
    - .planning/phases/36-content-components-expandable-cards/fidelity/ (Phase 36 gate artifact precedent for screenshot storage)
  </read_first>
  <what-built>
    The /showcase page (Plan 01): Client Work + Craft & Experiments sections composing ProjectCard closed-by-default and expandable, dev-gated, dark derived from the landing recipe (D-04). Axe passes light + dark; contrast matrix extended and passing (Task 1).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Capture rendered `/showcase` screenshots at 390, 768, 1440, 1920 in BOTH closed and expanded card states, light + dark (Playwright `page.screenshot()`; store under `.planning/phases/38-showcase-page-blog-restyle/fidelity/`).
    3. Manually export frame `12:3` PNGs from Figma at the same breakpoints (export_nodes is broken — manual export).
    4. Present side-by-side: rendered vs frame `12:3` for closed + expanded states at each breakpoint (light). Dark is reviewed as DERIVED (D-04) — flag it, no dark frame comparison.
    5. Present the `[COPY GAP]` / `[FIDELITY-GAP]` log from 38-01-SUMMARY with the screenshots.
    6. Confirm the pre-gate checklist items owned by this plan: card count per section matches 12:3 (D-03); all cards render closed (D-16); prod build has no /showcase page or sitemap entry and no prod nav link.
  </how-to-verify>
  <acceptance_criteria>
    - Rendered vs frame 12:3 screenshots captured for closed + expanded at 390/768/1440/1920, light, plus dark rendered (derived)
    - `[COPY GAP]` / `[FIDELITY-GAP]` log presented alongside
    - Joel approves the comparison OR provides a specific gap list to address
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the showcase fidelity gate, or list specific fidelity gaps to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npx playwright test tests/accessibility/showcase.spec.ts` → 2 passed
- `node scripts/check-contrast.mjs` → exits 0 with Phase 38 pairs
- Fidelity screenshots stored under `.planning/phases/38-showcase-page-blog-restyle/fidelity/`
- Joel approval recorded for the showcase fidelity gate
</verification>

<success_criteria>
- PAGE-02 verified: /showcase axe-clean both themes, contrast pairs pass, frame 12:3 fidelity approved (dark flagged derived)
- Roadmap criterion 5 satisfied (Figma-vs-rendered comparison approved before phase close)
</success_criteria>

<output>
After completion, create `.planning/phases/38-showcase-page-blog-restyle/38-03-SUMMARY.md` recording axe results, contrast additions, fidelity screenshot locations, and Joel's approval (or the gap list resolved).
</output>
