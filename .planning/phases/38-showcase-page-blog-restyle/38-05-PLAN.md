---
phase: 38-showcase-page-blog-restyle
plan: 05
type: execute
wave: 3
depends_on: ["38-04"]
requirements: [PAGE-06]
files_modified:
  - src/styles/global.css
  - astro.config.mjs
  - src/components/wl/BlogCard.astro
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A .wl-prose scope exists in global.css styling MDX headings/paragraphs/links/blockquote/lists/code/hr with Wavelength tokens, flipping automatically in dark"
    - "The existing neobrutalist .prose scope in global.css is unchanged"
    - "Code blocks respond to the .dark class toggle, not prefers-color-scheme"
    - "A new src/components/wl/BlogCard.astro renders a single-column editorial blog entry (title as h2, date, description, tags) linking to /blog/{slug}"
  artifacts:
    - path: "src/styles/global.css"
      provides: "New .wl-prose scope authored below the untouched .prose scope"
      contains: ".wl-prose"
    - path: "astro.config.mjs"
      provides: "expressiveCode configured with themeCssSelector + useDarkModeMediaQuery false, brand styleOverrides"
      contains: "themeCssSelector"
    - path: "src/components/wl/BlogCard.astro"
      provides: "Editorial blog list entry component (props-for-data, Tag primitive for pills)"
      min_lines: 30
      contains: "interface Props"
  key_links:
    - from: "src/components/wl/BlogCard.astro"
      to: "src/components/wl/Tag.astro"
      via: "import + render Tag per tag pill"
      pattern: "import Tag"
    - from: "astro.config.mjs expressiveCode"
      to: ".dark class"
      via: "themeCssSelector returning '.dark' for github-dark"
      pattern: "\\.dark"
    - from: ".wl-prose scope"
      to: "--wl-* tokens"
      via: "token-based color/font properties that flip in .dark"
      pattern: "var\\(--color-wl-"
---

<objective>
Build the three shared blog-build foundations gated behind the frame approval (Plan 04): the `.wl-prose` CSS scope in `global.css`, the `astro-expressive-code` retheme aligned to the `.dark` class, and the new `src/components/wl/BlogCard.astro` editorial list-entry component. These are the pieces the blog pages (Plan 06) import.

Purpose: PAGE-06 / D-13 — Wavelength prose treatment, brand-themed code blocks, and the single-column editorial entry component (D-11) that replaces the old neobrutalist BlogCard.
Output: `.wl-prose` scope, rethemed expressive-code config, `wl/BlogCard.astro`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md
@.planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md
@.planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md
@.planning/phases/38-showcase-page-blog-restyle/38-04-SUMMARY.md
</context>

<threat_model>
Static site foundations; no server/auth/input.
- **Content injection via MDX:** `.wl-prose` styles author-controlled MDX; no untrusted input (blog content is repo-authored). No sanitization concern beyond existing Astro/MDX defaults.
- **Prod-scope leakage:** `.wl-prose` and expressive-code changes are global styling — they apply site-wide but only render on blog pages (which are dev-only). The `.prose` neobrutalist scope must remain untouched (Phase 41 cleanup) to avoid breaking any other surface still using it.
- **Supply-chain:** no new npm dependencies — config-only change to already-installed `astro-expressive-code` 0.41.6 (verify `package.json` unchanged).
- **Contrast:** rethemed code-block text vs background must meet AA in both themes (validated in Plan 06's contrast pass; record chosen literals here).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author the .wl-prose scope in global.css</name>
  <read_first>
    - src/styles/global.css (the existing .prose scope lines ~489-642 — the structural template to parallel; the .dark token block lines ~44-57 showing which --wl-* tokens flip; the .wl-* type ramp definitions)
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (approved Blog Post frame node ID — the authority for prose heading sizes, blockquote size, image radius)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (.wl-prose scope contract lines ~307-322: per-selector treatment table)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (.wl-prose scope pattern lines ~443-564: per-element rule template with token usage)
  </read_first>
  <action>
    Append a new `.wl-prose` scope in `src/styles/global.css` BELOW the last existing `.prose` rule (below line ~642) — do NOT edit the `.prose` scope above (Phase 41 owns it). Style, per the UI-SPEC contract, using `--wl-*` tokens throughout (tokens flip in `.dark` automatically — add `.dark .wl-prose ...` rules ONLY where a literal non-token value is introduced): `h2,h3,h4` Fraunces via `var(--font-wl-heading)` with `font-variation-settings: "SOFT" 0, "WONK" 1` and `color: var(--color-wl-ink)`, sizes from the approved Blog Post frame (default to the `.wl-heading-*` ramp values, with a source-node comment if a local size is needed — ProjectCard precedent); `p, li` Hanken via `var(--font-wl-body)` 16px/1.6 `color: var(--color-wl-sub)`; `a` `color: var(--color-wl-accent)` underlined, hover to `var(--color-wl-ink)`, focus-visible outline `2px solid var(--color-wl-accent)` offset 2px; `strong` `var(--color-wl-ink)` weight 600; `blockquote` left border `4px solid var(--color-wl-accent)`, Fraunces italic, `color: var(--color-wl-sub)`; `ul/ol/li` standard indentation; inline `code:not(pre code)` `background-color: var(--color-wl-sea-glass)` small radius; `pre` margin + radius 8px; `img` radius 8px (confirm from frame) + block margin; `hr` `1px solid var(--color-wl-line)`. Values requiring the frame that were deferred at approval carry a source-node comment; never invent a size.
  </action>
  <verify>Grep `src/styles/global.css` for `.wl-prose` (present) and confirm the `.prose` block above is byte-unchanged (git diff shows only additions below line ~642). `npm run astro check` passes.</verify>
  <acceptance_criteria>
    - `src/styles/global.css` contains a `.wl-prose` selector block
    - The `.wl-prose` block appears AFTER the last `.prose`/`.dark .prose` rule (additions only below the existing scope)
    - `git diff src/styles/global.css` shows zero deletions/modifications within the original `.prose` scope lines
    - `.wl-prose a` uses `var(--color-wl-accent)` and has a `:focus-visible` outline rule
    - `.wl-prose blockquote` uses `var(--color-wl-accent)` for its left border and Fraunces italic
    - `.wl-prose code:not(pre code)` uses `var(--color-wl-sea-glass)`
    - `npm run astro check` exits 0
  </acceptance_criteria>
  <done>.wl-prose scope exists, token-based (dark flips automatically), .prose untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme expressive-code to the .dark class + brand palette</name>
  <read_first>
    - astro.config.mjs (current `expressiveCode()` bare call line ~97; sitemap block for context — do not touch it here, Plan 01 owns the /showcase filter)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (expressive-code retheme lines ~324-341: two-step config, styleOverrides toward --wl-sea-glass / --wl-line, AA check on code text)
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (approved Blog Post frame — code-block chrome authority if specified)
    - .planning/phases/38-showcase-page-blog-restyle/38-RESEARCH.md (expressive-code config lines ~156-186, verified from official docs)
  </read_first>
  <action>
    Replace the bare `expressiveCode()` in `astro.config.mjs` (line ~97) with a configured call: `themes: ['github-light', 'github-dark']`; `themeCssSelector: (theme) => theme.name === 'github-dark' ? '.dark' : ':not(.dark)'`; `useDarkModeMediaQuery: false` — this makes code blocks follow the site `.dark` class instead of `prefers-color-scheme`. Then add `styleOverrides` mapping code-block chrome toward brand values: block/frame background toward the `--wl-sea-glass` family (light `#E6F1F1`, dark `#123640`), borders toward `--wl-line`; keep syntax-token colors from the base themes unless the approved Blog Post frame specifies otherwise. Record each final literal used with an inline comment. Verify the code text vs block background meets WCAG AA (4.5:1) in both light and dark; if a chosen background fails AA against the base theme's foreground, keep the base theme background rather than shipping a failing pair (record the decision). Do NOT modify the sitemap filter here (Plan 01 owns it).
  </action>
  <verify>`npm run astro check` passes. Run `npm run dev`, open a blog post with a code block in dev; toggle dark mode — the code block switches theme with the `.dark` class (not system preference). Grep `astro.config.mjs` shows `themeCssSelector` and `useDarkModeMediaQuery: false`.</verify>
  <acceptance_criteria>
    - `astro.config.mjs` `expressiveCode(...)` call contains `themeCssSelector` and `useDarkModeMediaQuery: false`
    - Config contains `themes: ['github-light', 'github-dark']`
    - `styleOverrides` present with brand background literals recorded as comments
    - `package.json` dependencies unchanged (config-only)
    - `npm run astro check` exits 0
    - In dev, a rendered code block's theme follows the `.dark` class toggle (verified by manual toggle)
  </acceptance_criteria>
  <done>Code blocks follow the .dark class and use brand-leaning chrome that meets AA.</done>
</task>

<task type="auto">
  <name>Task 3: Build src/components/wl/BlogCard.astro (editorial list entry)</name>
  <read_first>
    - src/components/wl/LinkCard.astro (nearest analog: props interface, article-wraps-anchor pattern, geometry-in-style / color-in-class WR-02 pattern, focus treatment)
    - src/components/wl/Tag.astro (the tag-pill primitive to compose)
    - src/components/BlogCard.astro (OLD neobrutalist — read for semantic structure of date/time only; DO NOT import or reuse its tokens; it is a Phase 41 cleanup target)
    - .planning/phases/38-showcase-page-blog-restyle/38-BLOG-FRAMES.md (approved Blog Index frame node ID — authority for entry geometry: padding, gaps, thumbnail presence/size, meta styling, click-target)
    - .planning/phases/38-showcase-page-blog-restyle/38-UI-SPEC.md (Blog index structural contracts lines ~276-287; Component API row; Interaction Contracts — BlogCard click target FRAME-PENDING, no nested-interactive violations)
    - .planning/phases/38-showcase-page-blog-restyle/38-PATTERNS.md (BlogCard.astro pattern lines ~132-207: Props interface, wrapper element, title as h2, date, tag row, dark = token flip)
  </read_first>
  <action>
    Create `src/components/wl/BlogCard.astro` with `interface Props { title: string; slug: string; pubDate: Date; description: string; tags?: string[]; featuredImage?: string; readingTime?: number; class?: string }` (props-for-data, Phase 35 D-08; no slot). Structure per the approved Blog Index frame: an `<article>` wrapping an `<a href={`/blog/${slug}`}>` (article has no href/tabindex — avoid nested-interactive violations; whole-entry click area only if the approved frame implies it, following ProjectCard's whole-card-summary a11y precedent). Title renders as `<h2>` (heading hierarchy: entry title under the page `<h1>`) with Fraunces weight via `wl-heading-h3` class and `color` in a `class` (not inline) so `group-hover:text-wl-accent` can win. Date in `<time datetime={pubDate.toISOString()}>` styled with `var(--color-wl-sub)`. Description in Hanken `var(--color-wl-sub)`. Tags rendered by mapping `<Tag>{tag}</Tag>` (import from `./Tag.astro`) — no custom spans. Optional thumbnail only if the approved frame includes it, at the frame's size. Focus treatment `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-wl-accent` on the link. All colors via `--wl-*` tokens (flip automatically in dark; no `dark:` pairs, no literal hex). Geometry (padding, gaps) from the approved frame; any deferred value carries a source-node comment, never a guess.
  </action>
  <verify>`npm run astro check` passes. Grep confirms `import Tag` and no old neobrutalist tokens (`text-turquoise`, `font-heading`, `text-text-muted`, `bg-turquoise`) in the file. Confirm the title element is `<h2>` and the article does not carry `href`/`tabindex`.</verify>
  <acceptance_criteria>
    - `src/components/wl/BlogCard.astro` exists, ≥30 lines, contains `interface Props`
    - Contains `import Tag from './Tag.astro'` and renders `<Tag>` for each tag
    - Entry title element is `<h2>`; the wrapping `<article>` has no `href` or `tabindex`
    - Link `href` is `` `/blog/${slug}` ``
    - Date uses `<time datetime={pubDate.toISOString()}>`
    - `grep -E "text-turquoise|font-heading|text-text-muted|bg-turquoise" src/components/wl/BlogCard.astro` returns nothing
    - The link element has the `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-wl-accent` treatment
    - `npm run astro check` exits 0
  </acceptance_criteria>
  <done>wl/BlogCard.astro renders an editorial entry (h2 title, date, description, Tag pills) linking to /blog/{slug}, token-based dark flip.</done>
</task>

</tasks>

<verification>
- `npm run astro check` exits 0
- `.wl-prose` scope present in global.css; original `.prose` scope byte-unchanged
- `astro.config.mjs` expressiveCode uses `themeCssSelector` + `useDarkModeMediaQuery: false`; sitemap filter untouched by this plan
- `wl/BlogCard.astro` composes `<Tag>`, title is `<h2>`, zero old tokens
- `package.json` dependencies unchanged
</verification>

<success_criteria>
- D-13 delivered: `.wl-prose` scope + brand-aligned expressive-code (following `.dark` class)
- D-11 entry component (`wl/BlogCard.astro`) built from the approved frame, token-based dark flip
- Blog build foundations ready for the page restyles (Plan 06)
</success_criteria>

<output>
After completion, create `.planning/phases/38-showcase-page-blog-restyle/38-05-SUMMARY.md` recording: the `.wl-prose` selectors authored and any local sizes with source-node comments, the expressive-code literals chosen (+ AA results), and the BlogCard API + click-target decision from the approved frame.
</output>
