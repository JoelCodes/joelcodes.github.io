---
phase: 23-design-system-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "23-01"
  - "23-02"
files_modified:
  - src/layouts/v2/BaseLayout.astro
  - design/design-system.pen
autonomous: true
requirements:
  - FOUND-02
  - FOUND-05
gap_closure: false
threat_model:
  surface: "Static layout shell + design source file. Single non-inline <link rel=preload> tag for one woff2 file. No inline scripts (deliberately — light-mode-only avoids the FOUC <script is:inline> attack surface present in v1). No user input."
  risks:
    - "Missing <slot name=\"head\"> signature compatibility with v1 → downstream phases break when passing JSON-LD or per-page preloads → mitigated by explicit slot signature parity check in acceptance criteria."
    - "Accidentally importing v1 global.css alongside v2/global.css would break D-08 isolation → mitigated by explicit grep guard in <verify>."
    - "Font preload uses Vite's `?url` import → asset path is build-hashed and same-origin (no third-party preconnect) → CSP simpler than v1's Google Fonts setup."
    - "Pencil .pen file write via Pencil MCP only — Read/Grep tools cannot edit encrypted files."
  mitigations:
    - "No <script is:inline> tag in v2 BaseLayout — light-mode-only is the exact reason there's no FOUC to prevent."
    - "Body class uses v2 utility names only (bg-surface, text-text, font-text); no `dark:` variants exist in v2 token namespace."
    - "design-system.pen factors Header + Footer ONLY (D-15/D-16); primitives are out of scope until Phase 24."
must_haves:
  truths:
    - "src/layouts/v2/BaseLayout.astro exists, imports src/styles/v2/global.css, renders an HTML5 shell with <head> + <body> + slot architecture identical to v1 in slot signature, but with NO dark-mode FOUC script, NO #theme-toggle, NO localStorage.theme reference, and NO Google Fonts CDN preconnect/stylesheet links"
    - "design/design-system.pen exists as a Pencil document containing the full v2 token variable set, factored Header and Footer reusable components, and an inline 'Token Reference' frame mapping Pencil variables to CSS custom properties"
    - "v1 BaseLayout.astro and v1 global.css are byte-identical to their pre-phase state (FOUND-06 coexistence)"
  artifacts:
    - path: "src/layouts/v2/BaseLayout.astro"
      provides: "v2 layout shell consumed by every v2 page; light-mode-only; self-hosted-font-aware; SEO + slot 'head' compatible with v1"
      contains: "import '../../styles/v2/global.css', interface Props, <slot />, <slot name=\"head\" />"
    - path: "design/design-system.pen"
      provides: "Pencil source of truth for v2 tokens + factored Header/Footer components"
      contains: "Token Reference frame, Header component, Footer component, color/space/radius/text/font variables"
  key_links:
    - from: "src/layouts/v2/BaseLayout.astro"
      to: "src/styles/v2/global.css (Plan 23-02 output)"
      via: "import in frontmatter"
      pattern: "import '../../styles/v2/global.css'"
    - from: "src/layouts/v2/BaseLayout.astro"
      to: "src/components/v2/layout/Header.astro and Footer.astro (Plan 23-04 outputs — referenced ahead of existence)"
      via: "import in frontmatter; Plan 23-04 builds these so this layout becomes runnable"
      pattern: "import Header from '../../components/v2/layout/Header.astro'"
    - from: "design/design-system.pen"
      to: "src/styles/v2/global.css (provides the human-readable mapping to the machine-readable tokens)"
      via: "Token Reference frame inside the .pen file lists every Pencil variable name → CSS custom property name pair"
      pattern: "Token Reference"
---

<objective>
Create the v2 layout shell at `src/layouts/v2/BaseLayout.astro` (light-mode-only, no FOUC script, no theme toggle, self-hosted-font preload) and create the `design/design-system.pen` source of truth containing v2 variables plus factored Header and Footer components and the inline token-mapping table.

Purpose: BaseLayoutV2 is the "page wrapper" every later phase composes against. It must lock in the exact slot signature v1 uses (so downstream phases passing JSON-LD or per-page preload tags via `<slot name="head" />` work without surprise) while purging every dark-mode artefact. The .pen file is the design-system source-of-truth promised by FOUND-02 — pen variables and factored Pencil components for Header/Footer matched 1:1 with what Plan 23-04 will ship in code.

Output: `src/layouts/v2/BaseLayout.astro` (importable but not yet rendered until Plan 23-04 ships Header/Footer components); `design/design-system.pen` (Pencil-encrypted document with variables, components, and the token-mapping frame).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-design-system-foundation/23-CONTEXT.md
@.planning/phases/23-design-system-foundation/23-RESEARCH.md
@.planning/phases/23-design-system-foundation/23-VALIDATION.md
@.planning/phases/23-design-system-foundation/23-01-SUMMARY.md
@.planning/phases/23-design-system-foundation/23-01-CRITO-INSPECTION.md
@src/layouts/BaseLayout.astro
@src/components/SEO.astro
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/layouts/v2/BaseLayout.astro (light-mode-only shell)</name>
  <files>src/layouts/v2/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (v1 — for slot signature parity; do NOT carry over its `<script is:inline>` block, Google Fonts links, or `dark:*` body classes)
    - src/components/SEO.astro (confirm props interface — v2 layout passes `title` and `description` through unchanged)
    - .planning/phases/23-design-system-foundation/23-RESEARCH.md (§7 font-preload pattern via Vite `?url`; §8 BaseLayoutV2 structure with explicit "Keep" / "Drop" / "Add" lists)
    - .planning/phases/23-design-system-foundation/23-CONTEXT.md (D-01 namespaced path; D-08 no v1 token references; FOUND-05 no dark-mode artefacts of any kind)
    - .planning/phases/23-design-system-foundation/23-01-CRITO-INSPECTION.md (## npm Package Confirmation → exact font slug for the heading-font Vite `?url` import path)
  </read_first>
  <action>
Sub-step A — create the directory:
  - `mkdir -p src/layouts/v2/`

Sub-step B — write `src/layouts/v2/BaseLayout.astro`. The file MUST contain (exactly these structural elements, in this order):

  Frontmatter:
  - `import '../../styles/v2/global.css';` (the v2 stylesheet from Plan 23-02 — relative path is critical: `../../`)
  - One `import <varName> from '@fontsource-variable/<heading-slug>/files/<heading-slug>-latin-wght-normal.woff2?url';` line where `<heading-slug>` is the kebab-case slug of the HEADING font from 23-01-CRITO-INSPECTION.md (NOT the body font — only the above-the-fold critical font is preloaded per RESEARCH §7)
  - `import Header from '../../components/v2/layout/Header.astro';` (referencing the file Plan 23-04 will create — Astro permits forward import declarations as long as the referenced file exists at build time, which it will after Wave 3)
  - `import Footer from '../../components/v2/layout/Footer.astro';`
  - `import SEO from '../../components/SEO.astro';`
  - `interface Props { title: string; description?: string; }`
  - `const { title, description = 'Web apps, automation, and AI development for small businesses' } = Astro.props;` (description default copied verbatim from v1 for content parity)

  Template:
  - `<!doctype html>`
  - `<html lang="en">`
  - `<head>`:
    - `<meta charset="utf-8" />`
    - `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`
    - `<link rel="icon" href="/favicon.ico" />`
    - `<meta name="viewport" content="width=device-width" />`
    - `<meta name="generator" content={Astro.generator} />`
    - `<link rel="preload" href={<varName>} as="font" type="font/woff2" crossorigin />` (the single critical font woff2 from the Vite `?url` import)
    - `<SEO title={title} description={description} />`
    - `<slot name="head" />` (slot signature MUST be byte-identical to v1)
  - `<body class="font-text bg-surface text-text min-h-screen flex flex-col">` (utility classes use v2 token names ONLY — no `bg-bg-light`, no `text-text-light`, no `dark:*`)
  - `<Header />`
  - `<main class="flex-grow"><slot /></main>`
  - `<Footer />`
  - `</body></html>`

  EXPLICITLY ABSENT (these MUST NOT appear anywhere in the file — verify with negated grep):
  - No `<script is:inline>` tag
  - No `<script>` tag of any kind
  - No `localStorage` reference
  - No `prefers-color-scheme` reference
  - No `theme-toggle` reference
  - No `dark:` Tailwind variant
  - No `fonts.googleapis.com` or `fonts.gstatic.com` URL
  - No `<link rel="preconnect">` (Google Fonts is gone — self-hosted only per RESEARCH §7)
  - No `<noscript>` block (was a Google Fonts fallback in v1)

Sub-step C — confirm the file parses cleanly: `npm run astro check` MUST exit 0. Note: at this point Header/Footer components do not exist yet, so Astro `check` may emit a "module not found" diagnostic for `../../components/v2/layout/Header.astro` — this is EXPECTED and will resolve once Plan 23-04 lands. Acceptance criterion: the build error message must contain only "module not found" and reference Header/Footer/MobileNav specifically; no syntax errors, no other diagnostics.

NOTE on missing-Header build behaviour: To allow Plan 23-02/23-03 to verify in parallel without Plan 23-04 having shipped, do NOT yet wire any v2 page that imports this layout. The layout file existing on disk does NOT cause a build failure unless something imports it. Confirm: `grep -rE "import .* from .*layouts/v2/BaseLayout" src/pages/` returns nothing yet (no v2 page consumes it in Phase 23).
  </action>
  <verify>
    - `test -f src/layouts/v2/BaseLayout.astro` exits 0
    - `grep -c "import '../../styles/v2/global.css'" src/layouts/v2/BaseLayout.astro` returns 1
    - `grep -cE "@fontsource-variable/[a-z-]+/files/.*woff2\?url" src/layouts/v2/BaseLayout.astro` returns 1 (single critical font preload only)
    - `grep -c "<slot name=\"head\" />" src/layouts/v2/BaseLayout.astro` returns 1
    - `grep -c "<slot />" src/layouts/v2/BaseLayout.astro` returns 1 (the default slot for page content)
    - `grep -cE "<script\b" src/layouts/v2/BaseLayout.astro` returns 0
    - `grep -cE "localStorage|prefers-color-scheme|theme-toggle|fonts\.(googleapis|gstatic)\.com" src/layouts/v2/BaseLayout.astro` returns 0
    - `grep -cE "\bdark:" src/layouts/v2/BaseLayout.astro` returns 0
    - `grep -cE "<link rel=\"preconnect\"|<noscript>" src/layouts/v2/BaseLayout.astro` returns 0
    - `grep -c "interface Props" src/layouts/v2/BaseLayout.astro` returns 1
    - `git diff --stat src/layouts/BaseLayout.astro` shows no changes (v1 layout untouched — FOUND-06)
    - `git diff --stat src/styles/global.css` shows no changes (v1 styles untouched — FOUND-06)
    - `npm run build` exits 0 (no v2 page consumes this layout yet, so the missing Header import does not error)
  </verify>
  <acceptance_criteria>
    - File `src/layouts/v2/BaseLayout.astro` exists with the structure described above
    - Slot signature parity with v1: contains both `<slot />` (default) and `<slot name="head" />` (named)
    - Exactly one woff2 preload `<link>` for the heading font (not the body font; not multiple weights)
    - Body class string is `font-text bg-surface text-text min-h-screen flex flex-col` (v2 utilities only — verifiable by `grep "font-text bg-surface text-text" src/layouts/v2/BaseLayout.astro` returning 1)
    - Zero `<script>` tags, zero `localStorage`, zero `prefers-color-scheme`, zero `theme-toggle`, zero `dark:`, zero `fonts.googleapis.com`, zero `fonts.gstatic.com`, zero `<noscript>`, zero `<link rel="preconnect">` (eight separate negated greps)
    - v1 BaseLayout.astro is byte-identical to its pre-task state
    - `npm run build` exits 0 with no errors (warnings about un-imported layout are fine)
    - `npm run astro check` exits 0 OR emits only a single "module not found" class of diagnostic for Header/Footer/MobileNav imports (which will resolve in Plan 23-04)
  </acceptance_criteria>
  <done>v2 BaseLayout exists, is light-mode-pure, has slot parity with v1, and is ready for Plan 23-04 to drop in Header/Footer components and make it fully renderable.</done>
</task>

<task type="auto">
  <name>Task 2: Create design/design-system.pen with v2 variables, factored Header + Footer, and Token Reference frame</name>
  <files>design/design-system.pen</files>
  <read_first>
    - .planning/phases/23-design-system-foundation/23-01-CRITO-INSPECTION.md (## v2 Token Mapping Table → exact Pencil variable name + CSS custom property + value for every token)
    - .planning/phases/23-design-system-foundation/23-CONTEXT.md (D-15 .pen scope: variables + Header + Footer + Token Reference frame ONLY; D-16 NO primitives until Phase 24; D-17 token mapping inline; D-18 canonical Crito source nodes for Header/Footer; D-13 Footer is 2-column not 3; D-14 NO newsletter bar)
    - .planning/phases/23-design-system-foundation/23-RESEARCH.md (§12 design-system.pen creation sequence)
    - design/Consulting & Agency Website Template I Crito (Community).pen (read-only reference via Pencil MCP `batch_get` only — encrypted)
  </read_first>
  <action>
Use Pencil MCP tools EXCLUSIVELY for all .pen file work. Read/Grep tools cannot read OR write encrypted .pen files.

Step 1 (orient): `mcp__pencil__get_editor_state` to know the current active document.

Step 2 (open new doc): `mcp__pencil__open_document` with `filePathOrNew: "new"`. The MCP server will create an unsaved new document.

Step 3 (load guidelines): `mcp__pencil__get_guidelines` with `topic: "design-system"`. Use the returned conventions to organise frames (e.g. dedicated "Tokens" frame, dedicated "Components" frame, dedicated "Token Reference" text frame).

Step 4 (create variables): `mcp__pencil__batch_design` to create document variables for every entry in the 23-01-CRITO-INSPECTION.md "## v2 Token Mapping Table". Use slash-namespaced names matching the CSS structure:
  - `color/primary`, `color/primary-hover`, `color/surface`, `color/surface-muted`, `color/text`, `color/text-muted`, `color/border`, `color/accent` — 8 color variables, values from the inspection report
  - `space/xs`, `space/sm`, `space/md`, `space/lg`, `space/xl`, `space/2xl` — 6 numeric variables (px values from RESEARCH §3 or whatever 23-01 corrected)
  - `radius/sm`, `radius/md`, `radius/lg`, `radius/full` — 4 numeric variables
  - `text/display`, `text/h1`, `text/h2`, `text/h3`, `text/h4`, `text/body`, `text/small`, `text/caption` — 8 numeric variables
  - `font/display`, `font/text` — 2 string variables (NOT `font/heading` / `font/body` — naming override per 23-01 critical-naming-override section)
  - `font-weight/display`, `font-weight/text`, `font-weight/text-bold` — 3 numeric variables
  - `leading/display`, `leading/text` — 2 numeric variables
  Total: 33 variables minimum.

Step 5 (inspect Crito Header for factoring): `mcp__pencil__batch_get` with `nodeIds: ["ULZiU"]` at readDepth 3. Capture: layout direction (likely horizontal), child positions, logo size, nav-text styling, CTA button styling. (The Crito version has 6 links per CONTEXT D-18; ours has 4 — record both for the factoring step.)

Step 6 (factor Header component): Use `mcp__pencil__batch_design` to create a new component frame named `Header` with `reusable: true`. Compose it from:
  - Logo on the left (re-use Crito's logo treatment, replace text with "Joel Shinness")
  - 4 nav-text links in the center: "Blog", "Projects", "FAQ", "Contact" (NOT Crito's 6 links per CONTEXT D-18 adapt-and-override note)
  - One CTA button on the right with text "Let's Talk", filled with `var(color/accent)`, `cornerRadius: var(radius/md)`, padding from the Crito CTA reference
  - Apply v2 variables for ALL colors, type sizes, spacing, font families (no raw hex/px allowed inside the component — everything is a variable reference)
  - Sticky-bar positioning hint via the Pencil layout properties (top alignment, full width)

Step 7 (inspect Crito Footer for factoring): `mcp__pencil__batch_get` with `nodeIds: ["Y1ldm"]` at readDepth 3.

Step 8 (factor Footer component): Use `mcp__pencil__batch_design` to create a new component frame named `Footer` with `reusable: true`. Compose it as a 2-column layout (per CONTEXT D-13, NOT Crito's 3-column):
  - Left column: brand text "Joel Shinness" + 1-line description + social icons row (LinkedIn, Substack)
  - Right column: a single "Links" subgroup containing Blog, Projects, FAQ, Contact
  - DO NOT include a newsletter bar (D-14 NO newsletter)
  - DO NOT include a 3rd column (consolidate per D-13)
  - Footer-bottom row: copyright "© <year> Joel Shinness. All rights reserved."
  - Apply v2 variables for ALL colors, type sizes, spacing

Step 9 (Token Reference frame): Create a text frame titled "Token Reference" containing a 2-column markdown-style table OR a Pencil text-table primitive (whichever the design-system guidelines from step 3 recommend) with columns "Pencil variable" and "CSS custom property". Each row maps a slash-name (e.g. `color/primary`) to its CSS-custom-property name (e.g. `--color-primary`). Cover ALL 33 variables created in step 4. This frame is the human-readable bridge between Pencil source and CSS — D-17 mandates it lives inline here, not in a separate markdown file.

Step 10 (save): Use `mcp__pencil__batch_design` save semantics to save the document at path `design/design-system.pen`. Verify by calling `mcp__pencil__get_editor_state` and confirming the active document path is `design/design-system.pen`.

Step 11 (DO NOT add): NO Button frame, NO Card frame, NO Input frame, NO Badge frame, NO hero/services/projects frames. Per CONTEXT D-16, primitives land in Phase 24; sections land in Phases 26–29. Adding them now violates the phase boundary.

Step 12 (verify): Re-open `design/design-system.pen` (or use the active session) and `mcp__pencil__batch_get` to confirm:
  - At least 33 variables exist (matching step 4)
  - A frame/component named `Header` with `reusable: true` exists
  - A frame/component named `Footer` with `reusable: true` exists
  - A frame/text-block titled "Token Reference" exists with at least 33 mapping rows
  - NO frames named `Button`, `Card`, `Input`, `Badge`, `Hero`, `Services`, `Projects`, etc.
  </action>
  <verify>
    - `test -f design/design-system.pen` exits 0
    - File size is non-trivial: `test $(stat -f%z design/design-system.pen 2>/dev/null || stat -c%s design/design-system.pen) -gt 1024` (> 1KB indicates real content, not an empty new doc)
    - `mcp__pencil__get_editor_state` shows `design/design-system.pen` was the most recent active document
    - `mcp__pencil__batch_get` with a wildcard variable pattern returns at least 33 variables
    - `mcp__pencil__batch_get` finds a frame/component named `Header` with `reusable: true`
    - `mcp__pencil__batch_get` finds a frame/component named `Footer` with `reusable: true`
    - `mcp__pencil__batch_get` finds a text frame containing the literal string "Token Reference"
    - `mcp__pencil__batch_get` does NOT find any frame named `Button`, `Card`, `Input`, or `Badge` (Phase 24 work)
    - `git diff --stat design/Consulting\ \&\ Agency\ Website\ Template\ I\ Crito\ \(Community\).pen` shows zero changes (Crito source untouched)
  </verify>
  <acceptance_criteria>
    - File `design/design-system.pen` exists and is > 1KB
    - The .pen contains at least 33 document variables in slash-namespace form (8 colors + 6 spacing + 4 radii + 8 sizes + 2 font families + 3 weights + 2 leadings)
    - Variable names use `font/display` and `font/text` (NOT `font/heading` / `font/body`)
    - A reusable component frame named `Header` exists with logo + 4 nav links + "Let's Talk" CTA, all using variable references for colors/sizes/spacing
    - A reusable component frame named `Footer` exists with 2-column layout (brand+social left, links right) + copyright row + NO newsletter bar
    - A "Token Reference" text frame exists with a 2-column variable→CSS-property mapping covering all 33 variables
    - NO primitive component frames (Button, Card, Input, Badge) — those are Phase 24 scope
    - Original Crito .pen file is byte-identical (`git diff` clean)
    - All Pencil MCP `batch_get` verification calls succeed
  </acceptance_criteria>
  <done>The design-system .pen is the source of truth for v2 tokens and Header/Footer factoring; Token Reference frame inside it provides the human-readable variable→CSS-property mapping per D-17.</done>
</task>

</tasks>

<verification>
- `test -f src/layouts/v2/BaseLayout.astro && test -f design/design-system.pen` exits 0
- `npm run build` exits 0
- `git diff --stat src/layouts/BaseLayout.astro src/styles/global.css design/Consulting\ \&\ Agency\ Website\ Template\ I\ Crito\ \(Community\).pen` shows zero changes (v1 + Crito reference all untouched)
- All negated greps in Task 1's verify section pass (no dark-mode contamination)
- Pencil MCP confirms .pen contents per Task 2's acceptance criteria
</verification>

<success_criteria>
- FOUND-02 satisfied: `design/design-system.pen` exists with extracted variables and factored Header + Footer components.
- FOUND-05 satisfied: `src/layouts/v2/BaseLayout.astro` has zero dark-mode FOUC script, zero `localStorage.theme`, zero `#theme-toggle` reference; v2 pages will render light-mode-only regardless of OS preference.
- v1 surfaces (BaseLayout, global.css, Crito .pen) are byte-identical to pre-phase state.
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-system-foundation/23-03-SUMMARY.md` per `./.claude/get-shit-done/templates/summary.md`. Summary frontmatter MUST include:
  - `affects: [23-04, 24-04, 25-01, 25-02, 26-01, 26-04, 27-01, 27-02, 27-03, 28-01, 29-01, 29-02, 29-03]` (every later phase that mounts a page on BaseLayoutV2 or extends design-system.pen)
  - `subsystem: layout-shell`
  - `requires: [23-01, 23-02]` (consumed inspection report + v2 stylesheet existence)
  - artifacts list including the woff2 preload font slug
</output>
