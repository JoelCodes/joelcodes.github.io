---
phase: 23-design-system-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - "23-02"
  - "23-03"
files_modified:
  - src/components/v2/layout/Header.astro
  - src/components/v2/layout/Footer.astro
  - src/components/v2/layout/MobileNav.astro
  - src/pages/v2-smoke.astro
  - tests/accessibility/v2-layout.spec.ts
autonomous: true
requirements:
  - COMP-05
  - COMP-06
  - FOUND-06
gap_closure: false
threat_model:
  surface: "Three Astro components + one smoke-test page + one Playwright spec. The MobileNav contains the only inline JS in v2 (focus-trap + ESC + backdrop-tap handlers — ~30 lines). The smoke page exists only to give axe-core something to render against; it is intentionally trivial and contains no user input or external data."
  risks:
    - "Inline <script> in MobileNav.astro could be an XSS vector if it ever interpolated user data → mitigated by the script having ZERO data interpolation: it operates purely on DOM IDs `mobile-menu-toggle` and `mobile-menu-overlay` and known class names. No `innerHTML`, no eval, no string concatenation from input."
    - "Focus trap that escapes the overlay → axe-core spec asserts focus stays inside; manual test in checkpoint covers the ESC + backdrop-tap paths."
    - "External social URLs (LinkedIn, Substack) → already trusted v1 URLs reused; rel=\"noopener noreferrer\" + target=\"_blank\" prevents tabnabbing."
    - "FOUND-06 visual regression on v1 pages → the dual-entry-point design from RESEARCH §6 makes regression structurally impossible (v1 pages never load v2 CSS), but we still verify with `npm run build` + manual smoke + the token-collision script."
  mitigations:
    - "MobileNav script has zero data interpolation; pure DOM lookups by ID and known class strings."
    - "External anchor tags use rel=\"noopener noreferrer\" + target=\"_blank\"."
    - "axe-core spec gates the v2 smoke page for WCAG 2.2 AA compliance before any later phase mounts a real page on BaseLayoutV2."
    - "FOUND-06 verification is the FINAL task of this plan — the phase does not complete until v1 pages are confirmed unchanged."
must_haves:
  truths:
    - "Decisions implemented by this plan: D-09 (Let's Talk CTA href = /#contact, single href works for both same-page scroll and cross-page navigation), D-10 (mobile pattern is full-screen overlay with focus trap, ESC and backdrop close — NOT side drawer or dropdown), D-11 (MobileNav.astro is built fresh in v2/ namespace; v1 MobileNav is NOT copied or adapted because it carries dark-mode toggle code), D-12 (active link highlighting via Astro.url.pathname + aria-current='page' + visual cue), D-13 (footer is 2-column — left brand+social, right consolidated links — NOT Crito's 3-column layout), D-14 (no newsletter bar)"
    - "Visiting any page on BaseLayoutV2 (e.g. the throwaway /v2-smoke page) renders the v2 Header sticky at the top with 4 nav links and the 'Let's Talk' CTA, and renders the v2 Footer 2-column with social icons sized 44x44px or larger"
    - "No #theme-toggle element appears in the DOM of any v2 page (verifiable in browser console: `document.querySelectorAll('#theme-toggle').length === 0`)"
    - "Mobile hamburger opens a full-screen overlay that traps Tab focus, closes on ESC, and closes on backdrop tap; the overlay starts in the [hidden] state on initial load"
    - "All existing v1.3 pages render unchanged — verified by `npm run build` succeeding and a manual visit to /, /faq, /projects, /blog confirming visual parity with the pre-phase baseline"
    - "axe-core finds zero WCAG 2.2 AA violations on the v2 smoke page covering Header + Footer + MobileNav"
  artifacts:
    - path: "src/components/v2/layout/Header.astro"
      provides: "Sticky v2 header with logo, 4 nav links (Blog, Projects, FAQ, Contact), Let's Talk CTA, and mobile hamburger; no theme toggle; active link via aria-current"
      contains: "<header, sticky, top-0, aria-current, Let's Talk, /#contact, MobileNav"
    - path: "src/components/v2/layout/Footer.astro"
      provides: "2-column footer with brand+social left, links right, 44x44px social touch targets; no newsletter bar"
      contains: "<footer, grid-cols, min-w-[44px], min-h-[44px], LinkedIn, Substack"
    - path: "src/components/v2/layout/MobileNav.astro"
      provides: "Hamburger button + full-screen overlay nav with focus trap, ESC-to-close, backdrop-tap-to-close, body-scroll lock"
      contains: "id=\"mobile-menu-toggle\", id=\"mobile-menu-overlay\", aria-expanded, hidden, <script>, Escape"
    - path: "src/pages/v2-smoke.astro"
      provides: "Throwaway page mounted on BaseLayoutV2 used only by axe-core spec and FOUND-06 verification; deleted in Phase 25 once a real page (e.g. /faq) migrates"
      contains: "import BaseLayout from '../layouts/v2/BaseLayout.astro'"
    - path: "tests/accessibility/v2-layout.spec.ts"
      provides: "Playwright + axe-core spec asserting zero WCAG 2.2 AA violations on the v2 smoke page; asserts no #theme-toggle in DOM; asserts mobile-nav focus-trap behavior at narrow viewport"
      contains: "AxeBuilder, withTags, wcag22aa, mobile-menu-toggle"
  key_links:
    - from: "src/components/v2/layout/Header.astro"
      to: "src/components/v2/layout/MobileNav.astro"
      via: "Header imports MobileNav and renders it inside the mobile breakpoint container"
      pattern: "import MobileNav from './MobileNav.astro'"
    - from: "src/components/v2/layout/Header.astro AND Footer.astro"
      to: "src/styles/v2/global.css (Plan 23-02)"
      via: "Components use v2 utility classes (font-display, bg-surface, text-text, font-text) which resolve from v2 token block"
      pattern: "font-display|bg-surface|text-text|font-text|text-accent"
    - from: "src/pages/v2-smoke.astro"
      to: "src/layouts/v2/BaseLayout.astro (Plan 23-03)"
      via: "Page imports BaseLayout and wraps trivial content"
      pattern: "import BaseLayout from '../layouts/v2/BaseLayout.astro'"
---

<objective>
Build the three v2 layout components (Header, Footer, MobileNav), ship a throwaway smoke page that mounts BaseLayoutV2 so the components can be exercised by axe-core and a manual visual check, add a Playwright accessibility spec that gates v2 layout for WCAG 2.2 AA, and execute the FOUND-06 coexistence verification as the final task of the phase.

Purpose: Header/Footer/MobileNav are the components every later v2 page consumes. They must be light-mode-only, accessible, and self-contained — built fresh per CONTEXT D-11 (NOT copied from v1's MobileNav which carries dark-mode toggle code we deliberately exclude). The smoke page makes the whole foundation testable end-to-end without committing to any real page migration (those are Phase 25–29 scope). FOUND-06 is the phase exit gate: v1 pages must visually pass through Phase 23 unchanged.

Output: 3 v2 components + 1 smoke page + 1 axe-core spec; FOUND-06 verified via `npm run build` + manual visual smoke check on the v1.3 page set.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-design-system-foundation/23-CONTEXT.md
@.planning/phases/23-design-system-foundation/23-RESEARCH.md
@.planning/phases/23-design-system-foundation/23-VALIDATION.md
@.planning/phases/23-design-system-foundation/23-02-SUMMARY.md
@.planning/phases/23-design-system-foundation/23-03-SUMMARY.md
@.planning/phases/23-design-system-foundation/23-01-CRITO-INSPECTION.md
@src/components/layout/Header.astro
@src/components/layout/Footer.astro
@src/components/layout/MobileNav.astro
@src/layouts/v2/BaseLayout.astro
@tests/accessibility/axe-tests.spec.ts
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build v2 Header + MobileNav components (sticky, 4 links, CTA, focus-trap overlay)</name>
  <files>
    - src/components/v2/layout/Header.astro
    - src/components/v2/layout/MobileNav.astro
  </files>
  <read_first>
    - src/components/layout/Header.astro (v1 — for nav-link list reference; do NOT carry over `#theme-toggle` button, dark-mode toggle script, or `dark:*` classes)
    - src/components/layout/MobileNav.astro (v1 — REFERENCE FOR STYLE ONLY; per CONTEXT D-11 do NOT copy implementation; the file carries dark-mode-toggle code that v2 must not inherit. Read to understand what to AVOID, not to fork from.)
    - .planning/phases/23-design-system-foundation/23-RESEARCH.md (§9 mobile overlay pattern with focus-trap pseudocode; §10 active link detection pattern with edge-case table)
    - .planning/phases/23-design-system-foundation/23-CONTEXT.md (D-09 CTA href=/#contact; D-10 full-screen overlay; D-11 build MobileNav fresh; D-12 active link via aria-current + visual cue)
    - .planning/phases/23-design-system-foundation/23-01-CRITO-INSPECTION.md (## v2 Token Mapping Table for utility class names: font-display, bg-surface, text-text, text-accent, etc.)
  </read_first>
  <action>
Sub-step A — create directory: `mkdir -p src/components/v2/layout/`

Sub-step B — write `src/components/v2/layout/Header.astro`:
  Frontmatter:
    - `import MobileNav from './MobileNav.astro';`
    - Build the nav-link array as `const links = [{ href: '/blog', label: 'Blog' }, { href: '/projects', label: 'Projects' }, { href: '/faq', label: 'FAQ' }, { href: '/#contact', label: 'Contact' }];`
    - Compute current path: `const path = Astro.url.pathname.replace(/\/$/, '');`
    - Implement the `isActive(href, current)` helper exactly per RESEARCH §10: anchor-only links (`startsWith('/#')`) return false; otherwise current === href OR current.startsWith(href + '/').

  Template:
    - `<header class="sticky top-0 z-30 bg-surface border-b border-border">` (sticky retained from v1 convention; uses v2 tokens — no border-3px neobrutalist border, no dark-mode classes)
    - Inside, a `<div class="container mx-auto px-4 h-16 flex items-center justify-between">` (16 = 4rem header height; adjust per Crito inspection if 23-01 found different)
    - Logo on the left: `<a href="/" class="font-display text-xl font-bold text-text hover:text-accent transition-colors">Joel Shinness</a>`
    - Desktop nav: `<nav class="hidden md:flex items-center gap-6" aria-label="Primary">` mapping the links array. Each link is `<a href={link.href} aria-current={isActive(link.href, path) ? 'page' : undefined} class:list={['font-text font-medium text-text hover:text-accent transition-colors', { 'text-accent underline underline-offset-[6px]': isActive(link.href, path) }]}>{link.label}</a>` (active style: accent color + underline per RESEARCH §10 visual styling recommendation).
    - "Let's Talk" CTA after the nav links: `<a href="/#contact" class="hidden md:inline-flex items-center px-5 py-2.5 rounded-md bg-accent text-text font-text font-semibold hover:opacity-90 transition-opacity">Let's Talk</a>` — href=`/#contact` per CONTEXT D-09.
    - `<MobileNav links={links} />` — pass the same links array as a prop so the overlay nav stays in sync with the desktop nav.

  Explicit absences (negated grep guards):
    - NO `id="theme-toggle"` button
    - NO `<script>` for dark mode
    - NO `dark:` Tailwind variants
    - NO `localStorage` references
    - NO `border-[3px]` neobrutalist styling

Sub-step C — write `src/components/v2/layout/MobileNav.astro` FROM SCRATCH (do not copy v1):
  Frontmatter:
    - `import { Menu, X } from '@lucide/astro';`
    - `interface Props { links: { href: string; label: string }[] }`
    - `const { links } = Astro.props;`
    - Compute `const path = Astro.url.pathname.replace(/\/$/, '');` and the same `isActive` helper as Header.

  Template:
    - The hamburger button (visible at `md:hidden`):
      ```
      <button
        id="mobile-menu-toggle"
        type="button"
        aria-expanded="false"
        aria-controls="mobile-menu-overlay"
        aria-label="Open navigation menu"
        class="md:hidden inline-flex items-center justify-center min-w-[44px] min-h-[44px] text-text hover:text-accent"
      >
        <Menu size={24} aria-hidden="true" />
      </button>
      ```
    - The overlay (initially `hidden` attribute set so screen readers skip it):
      ```
      <div
        id="mobile-menu-overlay"
        hidden
        class="fixed inset-0 z-50 bg-surface flex flex-col items-center justify-center gap-6"
        role="dialog"
        aria-modal="true"
        aria-label="Navigation menu"
      >
        <button
          id="mobile-menu-close"
          type="button"
          aria-label="Close navigation menu"
          class="absolute top-4 right-4 min-w-[44px] min-h-[44px] inline-flex items-center justify-center text-text hover:text-accent"
        >
          <X size={24} aria-hidden="true" />
        </button>
        <nav class="flex flex-col items-center gap-6" aria-label="Mobile primary">
          { links.map(link => {
              const active = isActive(link.href, path);
              return (
                <a
                  href={link.href}
                  aria-current={active ? 'page' : undefined}
                  class:list={['font-display text-2xl font-semibold text-text hover:text-accent', { 'text-accent underline underline-offset-[6px]': active }]}
                >{link.label}</a>
              );
          }) }
          <a href="/#contact" class="mt-4 inline-flex items-center px-6 py-3 rounded-md bg-accent text-text font-text font-semibold">Let's Talk</a>
        </nav>
      </div>
      ```
    - Inline `<script>` (regular, NOT `is:inline` per RESEARCH §9 — Astro will bundle and TS-check it):
      Implement the focus-trap pattern exactly per RESEARCH §9 pseudocode. Required behaviour: open() sets `lastFocused = document.activeElement`, removes the `hidden` attribute on the overlay, adds `document.body.style.overflow = 'hidden'`, sets `aria-expanded='true'` on toggle, focuses the first focusable inside the overlay; close() reverses all of that and restores focus to `lastFocused`. Bind toggle click → open/close; close-button click → close; backdrop click (e.target === overlay) → close; document keydown ESC → close while overlay open; document keydown Tab → wrap from last to first focusable and Shift+Tab from first to last.
    - The script MUST select elements by ID `mobile-menu-toggle`, `mobile-menu-overlay`, `mobile-menu-close` and use the FOCUSABLE selector `'a[href], button:not([disabled]), [tabindex]:not([tabindex="-1"])'`.

  Explicit absences:
    - NO theme-toggle button anywhere
    - NO `dark:*` classes
    - NO localStorage references in the script
    - NO data interpolation in the script (XSS guard — script must reference no Astro.props or template-injected strings)
  </action>
  <verify>
    - `test -f src/components/v2/layout/Header.astro && test -f src/components/v2/layout/MobileNav.astro` exits 0
    - `grep -c "theme-toggle" src/components/v2/layout/Header.astro` returns 0
    - `grep -c "theme-toggle" src/components/v2/layout/MobileNav.astro` returns 0
    - `grep -cE "\bdark:" src/components/v2/layout/Header.astro` returns 0
    - `grep -cE "\bdark:" src/components/v2/layout/MobileNav.astro` returns 0
    - `grep -c "localStorage" src/components/v2/layout/Header.astro` returns 0
    - `grep -c "localStorage" src/components/v2/layout/MobileNav.astro` returns 0
    - `grep -cE "href=\"/blog\"|href=\"/projects\"|href=\"/faq\"|href=\"/#contact\"" src/components/v2/layout/Header.astro` returns at least 4 (the four nav targets) — adjust regex if links rendered via map
    - `grep -c "Let's Talk" src/components/v2/layout/Header.astro` returns at least 1
    - `grep -c "sticky top-0" src/components/v2/layout/Header.astro` returns 1
    - `grep -c "aria-current" src/components/v2/layout/Header.astro` returns at least 1
    - `grep -c "id=\"mobile-menu-toggle\"" src/components/v2/layout/MobileNav.astro` returns 1
    - `grep -c "id=\"mobile-menu-overlay\"" src/components/v2/layout/MobileNav.astro` returns 1
    - `grep -c "aria-expanded" src/components/v2/layout/MobileNav.astro` returns at least 1
    - `grep -cE "Escape|key === 'Escape'" src/components/v2/layout/MobileNav.astro` returns at least 1
    - `grep -c "min-w-\[44px\]" src/components/v2/layout/MobileNav.astro` returns at least 2 (toggle + close)
    - `npm run astro check` exits 0
  </verify>
  <acceptance_criteria>
    - Header component is sticky (`sticky top-0` class present)
    - Header contains exactly 4 nav links with hrefs `/blog`, `/projects`, `/faq`, `/#contact`
    - Header contains a "Let's Talk" anchor/button with href `/#contact`
    - Header has zero `theme-toggle`, zero `dark:` variants, zero `localStorage` references
    - Header active link logic uses `aria-current="page"` for the current route
    - MobileNav was authored fresh (NOT copied from src/components/layout/MobileNav.astro — verifiable: 23-04 commit shows the file as new addition, not a copy/move)
    - MobileNav has elements with IDs `mobile-menu-toggle`, `mobile-menu-overlay`, `mobile-menu-close`
    - MobileNav overlay starts with `hidden` attribute (not in a11y tree)
    - MobileNav script handles toggle click, ESC keydown, backdrop tap, Tab/Shift+Tab focus wrap
    - Both hamburger and close button meet 44x44 touch target
    - No `theme-toggle` / `dark:` / `localStorage` strings in either file
    - `npm run astro check` exits 0
  </acceptance_criteria>
  <done>v2 Header is sticky with the right nav set + CTA; v2 MobileNav is a fresh, accessible focus-trapped overlay; both are light-mode-only and ready to be rendered inside BaseLayoutV2.</done>
</task>

<task type="auto">
  <name>Task 2: Build v2 Footer (2-column, 44x44 social icons) + smoke page + axe-core spec</name>
  <files>
    - src/components/v2/layout/Footer.astro
    - src/pages/v2-smoke.astro
    - tests/accessibility/v2-layout.spec.ts
  </files>
  <read_first>
    - src/components/layout/Footer.astro (v1 — for social URLs and the 44x44 touch-target pattern; do NOT carry over `dark:*` classes or the `accent-yellow` color references)
    - .planning/phases/23-design-system-foundation/23-CONTEXT.md (D-13 2-column not 3; D-14 NO newsletter bar)
    - .planning/phases/23-design-system-foundation/23-RESEARCH.md (§5 reuse simple-icons-astro Substack icon and inline LinkedIn SVG; §11 FOUND-06 verification approach)
    - tests/accessibility/axe-tests.spec.ts (existing axe-core spec — match its style exactly: same withTags array, same expectation pattern)
    - src/layouts/v2/BaseLayout.astro (Plan 23-03 output — confirm import path the smoke page must use)
  </read_first>
  <action>
Sub-step A — write `src/components/v2/layout/Footer.astro`:
  Frontmatter:
    - `import Substack from 'simple-icons-astro/Substack';`
    - `const currentYear = new Date().getFullYear();`

  Template (2-column responsive grid per CONTEXT D-13, NOT 3-column):
    ```
    <footer class="border-t border-border bg-surface mt-auto">
      <div class="container mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-2 gap-8">
        <!-- Left column: brand + social -->
        <div class="flex flex-col gap-4">
          <p class="font-display text-xl font-bold text-text">Joel Shinness</p>
          <p class="font-text text-text-muted max-w-md">
            Web apps, automation, and AI development for small businesses.
          </p>
          <div class="flex gap-4 mt-2">
            <a
              href="https://www.linkedin.com/in/joel-shinness/"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="Connect with Joel on LinkedIn (opens in new tab)"
              class="inline-flex items-center justify-center min-w-[44px] min-h-[44px] text-text-muted hover:text-accent transition-colors"
            >
              <!-- Inline LinkedIn SVG copied verbatim from v1 Footer (path data only; no class colors) -->
              <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor" role="img" aria-hidden="true">
                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
              </svg>
            </a>
            <a
              href="https://joelshinness.substack.com"
              target="_blank"
              rel="noopener noreferrer"
              aria-label="Follow Joel on Substack (opens in new tab)"
              class="inline-flex items-center justify-center min-w-[44px] min-h-[44px] text-text-muted hover:text-accent transition-colors"
            >
              <Substack size={24} />
            </a>
          </div>
        </div>

        <!-- Right column: links -->
        <nav aria-label="Footer navigation" class="flex flex-col gap-2 md:items-end">
          <p class="font-display text-sm uppercase tracking-wide text-text-muted">Links</p>
          <a href="/blog" class="font-text text-text hover:text-accent transition-colors">Blog</a>
          <a href="/projects" class="font-text text-text hover:text-accent transition-colors">Projects</a>
          <a href="/faq" class="font-text text-text hover:text-accent transition-colors">FAQ</a>
          <a href="/#contact" class="font-text text-text hover:text-accent transition-colors">Contact</a>
        </nav>
      </div>
      <div class="border-t border-border">
        <div class="container mx-auto px-4 py-6 flex flex-col md:flex-row items-center justify-between gap-2">
          <p class="font-text text-sm text-text-muted">© {currentYear} Joel Shinness. All rights reserved.</p>
          <p class="font-text text-sm text-text-muted">
            Built with <a href="https://astro.build" target="_blank" rel="noopener noreferrer" class="underline hover:text-accent">Astro</a>
          </p>
        </div>
      </div>
    </footer>
    ```

  Explicit absences:
    - NO newsletter input/form (D-14)
    - NO 3rd column (D-13 — consolidated into 2 columns)
    - NO `dark:*` classes
    - NO v1 token references like `accent-yellow`, `bg-bg-light`, `text-text-light`

Sub-step B — write `src/pages/v2-smoke.astro` (throwaway page; deleted in Phase 25 when /faq migrates):
  ```
  ---
  import BaseLayout from '../layouts/v2/BaseLayout.astro';
  ---
  <BaseLayout title="v2 Smoke Test" description="Throwaway page used to exercise the v2 layout shell during Phase 23 verification. Deleted in Phase 25.">
    <section class="container mx-auto px-4 py-16">
      <h1 class="font-display text-h1 font-bold text-text mb-4">v2 Layout Smoke Test</h1>
      <p class="font-text text-body text-text-muted max-w-2xl">
        This page exists solely to verify that BaseLayoutV2 + HeaderV2 + FooterV2 render correctly during Phase 23.
        It is not linked from any nav and will be deleted once /faq migrates to BaseLayoutV2 in Phase 25.
      </p>
    </section>
  </BaseLayout>
  ```
  Add `// @phase-23-smoke-page — DELETE in Phase 25 when /faq migrates` as the first line of the file (HTML comment `<!-- ... -->` actually since this is at top of an Astro file; place inside the frontmatter as a `//` comment OR before the frontmatter as `<!-- -->`).

Sub-step C — write `tests/accessibility/v2-layout.spec.ts` (mirror style of `tests/accessibility/axe-tests.spec.ts`):
  ```
  import { test, expect } from '@playwright/test';
  import AxeBuilder from '@axe-core/playwright';

  const wcagTags = ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa', 'wcag22aa'];

  test.describe('v2 Layout Accessibility (Phase 23)', () => {
    test('v2 smoke page has zero axe-core violations', async ({ page }) => {
      await page.goto('/v2-smoke');
      const results = await new AxeBuilder({ page }).withTags(wcagTags).analyze();
      expect(results.violations).toEqual([]);
    });

    test('v2 smoke page contains no #theme-toggle in DOM', async ({ page }) => {
      await page.goto('/v2-smoke');
      const count = await page.locator('#theme-toggle').count();
      expect(count).toBe(0);
    });

    test('v2 smoke page mobile overlay is hidden on initial load', async ({ page }) => {
      await page.setViewportSize({ width: 375, height: 800 });
      await page.goto('/v2-smoke');
      const overlay = page.locator('#mobile-menu-overlay');
      await expect(overlay).toHaveAttribute('hidden', '');
    });

    test('v2 smoke page mobile overlay opens on hamburger click', async ({ page }) => {
      await page.setViewportSize({ width: 375, height: 800 });
      await page.goto('/v2-smoke');
      await page.locator('#mobile-menu-toggle').click();
      const overlay = page.locator('#mobile-menu-overlay');
      await expect(overlay).not.toHaveAttribute('hidden', '');
      const toggle = page.locator('#mobile-menu-toggle');
      await expect(toggle).toHaveAttribute('aria-expanded', 'true');
    });

    test('v2 smoke page mobile overlay closes on Escape', async ({ page }) => {
      await page.setViewportSize({ width: 375, height: 800 });
      await page.goto('/v2-smoke');
      await page.locator('#mobile-menu-toggle').click();
      await page.keyboard.press('Escape');
      const overlay = page.locator('#mobile-menu-overlay');
      await expect(overlay).toHaveAttribute('hidden', '');
    });
  });
  ```

Sub-step D — run the suite:
  - `npm run build` — MUST exit 0 (everything wires together now: smoke page imports BaseLayoutV2 which imports Header/Footer/MobileNav and v2/global.css)
  - `npm run astro check` — MUST exit 0
  - `npm run test:a11y` — MUST exit 0 with all 5 v2-layout spec cases passing AND all existing axe-core cases still passing
  - `node tests/check-token-collision.cjs` — MUST exit 0 ("OK: no token name collisions")
  </action>
  <verify>
    - `test -f src/components/v2/layout/Footer.astro && test -f src/pages/v2-smoke.astro && test -f tests/accessibility/v2-layout.spec.ts` exits 0
    - `grep -c "grid-cols-1 md:grid-cols-2" src/components/v2/layout/Footer.astro` returns 1
    - `grep -c "min-w-\[44px\]" src/components/v2/layout/Footer.astro` returns at least 2 (LinkedIn + Substack)
    - `grep -c "min-h-\[44px\]" src/components/v2/layout/Footer.astro` returns at least 2
    - `grep -c "newsletter\|Subscribe\|Sign up for" src/components/v2/layout/Footer.astro` returns 0 (no newsletter — D-14)
    - `grep -cE "\bdark:" src/components/v2/layout/Footer.astro` returns 0
    - `grep -c "import BaseLayout from '../layouts/v2/BaseLayout.astro'" src/pages/v2-smoke.astro` returns 1
    - `grep -c "DELETE in Phase 25" src/pages/v2-smoke.astro` returns 1
    - `grep -c "AxeBuilder" tests/accessibility/v2-layout.spec.ts` returns at least 1
    - `grep -c "wcag22aa" tests/accessibility/v2-layout.spec.ts` returns 1
    - `grep -c "#theme-toggle\|#mobile-menu-toggle\|#mobile-menu-overlay" tests/accessibility/v2-layout.spec.ts` returns at least 3
    - `npm run build` exits 0
    - `npm run astro check` exits 0
    - `npm run test:a11y` exits 0
    - `node tests/check-token-collision.cjs` exits 0
  </verify>
  <acceptance_criteria>
    - Footer is a 2-column grid at md+ breakpoints (collapses to 1 column on mobile)
    - Footer contains LinkedIn anchor (href=https://www.linkedin.com/in/joel-shinness/) and Substack anchor (href=https://joelshinness.substack.com), both with min-w-[44px] min-h-[44px]
    - Footer contains NO newsletter input, NO subscribe form, NO 3rd column
    - Footer uses v2 utility classes only (font-display, font-text, text-text, text-text-muted, bg-surface, border-border, text-accent) — zero `dark:*`, zero v1 token names
    - `src/pages/v2-smoke.astro` mounts BaseLayoutV2 and contains a "DELETE in Phase 25" marker for traceability
    - `tests/accessibility/v2-layout.spec.ts` contains 5 test cases: axe-core pass, no theme-toggle, overlay hidden initially, overlay opens on click + aria-expanded toggles, overlay closes on Escape
    - `npm run build` exits 0
    - `npm run astro check` exits 0
    - `npm run test:a11y` exits 0 (all v2 specs + all existing v1 specs pass)
    - Token-collision script still exits 0
  </acceptance_criteria>
  <done>v2 Footer renders 2-column with accessible social touch targets; the smoke page proves BaseLayoutV2 + Header + MobileNav + Footer all wire together; axe-core gates the layout for WCAG 2.2 AA; existing v1 a11y tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: FOUND-06 verification — confirm v1.3 pages render unchanged after v2 foundation lands</name>
  <files>
    - .planning/phases/23-design-system-foundation/23-FOUND-06-VERIFICATION.md
  </files>
  <read_first>
    - .planning/phases/23-design-system-foundation/23-RESEARCH.md (§11 FOUND-06 coexistence verification — recommended 2-step gate)
    - .planning/phases/23-design-system-foundation/23-VALIDATION.md (Manual-Only Verifications row for FOUND-06 — list of routes to visit)
    - .planning/phases/23-design-system-foundation/23-CONTEXT.md (FOUND-06 success criterion: "v1.3 pages and BaseLayout.astro continue to render unchanged during the transition")
    - src/styles/global.css (must be byte-identical to its pre-phase state)
    - src/layouts/BaseLayout.astro (must be byte-identical to its pre-phase state)
  </read_first>
  <action>
This task is the phase exit gate. It executes the FOUND-06 verification protocol from RESEARCH §11 and records the result in a markdown report.

Step 1 (build gate): Run `npm run build`. MUST exit 0. If it fails, halt — do NOT mark FOUND-06 verified.

Step 2 (token collision gate): Run `node tests/check-token-collision.cjs`. MUST exit 0 with "OK: no token name collisions" output. If it fails, halt.

Step 3 (existing axe-core gate): Run `npm run test:a11y`. MUST exit 0 — all v1 a11y specs and the new v2-layout spec must all pass.

Step 4 (v1 byte-identity gate): Run `git diff --stat src/styles/global.css src/layouts/BaseLayout.astro src/components/layout/`. MUST output zero changes for these v1 surfaces. (Phase 23 is allowed to add v2 files; it is NOT allowed to modify v1 files.)

Step 5 (manual visual smoke): Start `npm run dev` (in background) on a free port. Visit each route below in a browser tab and visually confirm it renders identical to its pre-Phase-23 baseline:
  - `/` (homepage)
  - `/blog`
  - `/blog/<any existing slug>` (use `ls src/content/blog/` to find one)
  - `/projects`
  - `/projects/<any existing slug>` (use `ls src/data/projects.json` and pick a slug)
  - `/faq`
  - `/thank-you`
  - `/design-system`

  For each route, confirm: header is the v1 neobrutalist sticky bar with theme toggle present; footer is the v1 footer with social icons; headings render in Bricolage Grotesque (v1 heading font); accent colors yellow/turquoise/magenta appear correctly. The test is binary: does it look like v1.3 looked yesterday? If ANY page regresses, halt and report the regression — DO NOT mark FOUND-06 complete.

Step 6 (also visit v2 smoke page): In the same `npm run dev` session, visit `/v2-smoke`. Confirm: header is the new v2 sticky bar with NO theme toggle; footer is the v2 2-column layout; fonts are the new self-hosted variable fonts (NOT Bricolage Grotesque). Confirm via DevTools console: `document.querySelectorAll('#theme-toggle').length === 0` returns true. Confirm: page renders in light mode regardless of OS dark-mode preference (toggle the OS preference if convenient; otherwise inspect that no `<html>` `dark` class is applied).

Step 7 (record result): Write `.planning/phases/23-design-system-foundation/23-FOUND-06-VERIFICATION.md` with:
  - Date / commit SHA at time of verification (`git rev-parse HEAD`)
  - Section: "Build gate" — output of `npm run build` (PASS / FAIL + last 5 lines of output)
  - Section: "Token collision gate" — output of `node tests/check-token-collision.cjs`
  - Section: "axe-core gate" — `npm run test:a11y` summary (X tests, Y passed, Z failed)
  - Section: "v1 byte-identity gate" — `git diff --stat` output for v1 paths (must be empty)
  - Section: "v1.3 page visual smoke" — table with columns Route | Visited | Visual parity vs v1.3 baseline (yes/no) | Notes; one row per route in step 5 (8 rows)
  - Section: "v2 smoke page" — confirmation table: theme-toggle absent? light-mode-only? new fonts? (yes/no per row)
  - Section: "FOUND-06 status" — single line `VERIFIED` or `FAILED — see notes`

Step 8 (kill dev server): If you started `npm run dev` in the background, terminate it cleanly.

NOTE on rendering verification: This task DOES require human eyes for the visual-smoke step (RESEARCH §11 explicitly recommends this; CONTEXT defers approach to implementer; VALIDATION marks it as a "Manual-Only Verification"). The executing Claude MUST: (1) run all automated gates (build, test:a11y, collision script, git diff) and record results in 23-FOUND-06-VERIFICATION.md; (2) write the "v1.3 page visual smoke" section table with columns populated for Route, Visited (no — pending user), Visual parity (— pending), Notes; (3) write `FOUND-06 status: PENDING USER VISUAL SMOKE CHECK` (NOT `VERIFIED`); (4) HALT and surface to the user the exact list of 8 routes to visit plus the smoke-check checklist (theme-toggle absent? light-mode-only? new fonts on /v2-smoke? v1 routes visually identical?). The status MUST NOT be flipped to `VERIFIED` by the executor under any circumstance — that flip is the user's signal that visual parity was confirmed by human eyes. Automated gates passing is necessary but NOT sufficient for FOUND-06.
  </action>
  <verify>
    - `test -f .planning/phases/23-design-system-foundation/23-FOUND-06-VERIFICATION.md` exits 0
    - `grep -c "VERIFIED\|FAILED" .planning/phases/23-design-system-foundation/23-FOUND-06-VERIFICATION.md` returns at least 1
    - `grep -c "^| /" .planning/phases/23-design-system-foundation/23-FOUND-06-VERIFICATION.md` returns at least 8 (one row per smoke-checked v1 route)
    - `git diff --stat src/styles/global.css src/layouts/BaseLayout.astro src/components/layout/` returns no changed-line counts (output is empty or shows zero modifications for v1 files)
    - `npm run build` exits 0
    - `npm run test:a11y` exits 0
    - `node tests/check-token-collision.cjs` exits 0
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/23-design-system-foundation/23-FOUND-06-VERIFICATION.md` exists
    - Report contains all 8 sections from step 7 above
    - "Build gate" records `npm run build` PASS
    - "Token collision gate" records `node tests/check-token-collision.cjs` PASS
    - "axe-core gate" records `npm run test:a11y` PASS with explicit pass count
    - "v1 byte-identity gate" records git diff is empty for v1 paths
    - "v1.3 page visual smoke" table has 8 rows covering /, /blog, /blog/<slug>, /projects, /projects/<slug>, /faq, /thank-you, /design-system; rows are pre-populated with `pending user check` for "Visited" and "Visual parity" columns
    - "v2 smoke page" section is pre-populated with `pending user check` for the three confirmation lines
    - Final status line written by the executor is `PENDING USER VISUAL SMOKE CHECK` — NOT `VERIFIED`. The user is the only authorised actor who can write `VERIFIED`. The executor MUST surface the smoke-check checklist to the user as a final action.
  </acceptance_criteria>
  <done>FOUND-06 automated gates have all passed (build, token-collision, axe-core, git-diff). The verification report exists with status `PENDING USER VISUAL SMOKE CHECK`. The user has been handed the smoke-check checklist for the 8 v1 routes plus the v2 smoke page. Phase 23 cannot ship until the user flips the status to `VERIFIED`.</done>
</task>

</tasks>

<verification>
- All three component files exist with v2 utility classes and zero dark-mode contamination
- v2 smoke page renders BaseLayoutV2 successfully
- Playwright a11y suite shows 5 new v2-layout tests passing PLUS all existing v1 tests still passing
- Token collision script exits 0
- v1 source files (global.css, BaseLayout.astro, components/layout/*) are byte-identical
- FOUND-06 verification report exists and is marked VERIFIED
</verification>

<success_criteria>
- COMP-05 satisfied: HeaderV2 is sticky, has 4 nav links, has "Let's Talk" CTA, has mobile hamburger; no theme toggle
- COMP-06 satisfied: FooterV2 is 2-column with 44x44 social icons; no newsletter bar
- FOUND-06 satisfied: v1.3 pages render unchanged; verified via build + tests + visual smoke; report archived in phase folder
- All 8 of phase 23's requirement IDs (FOUND-01..06, COMP-05, COMP-06) are now delivered
- Phase 23 is ready to close out via `/gsd:verify-work` and SUMMARY generation
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-system-foundation/23-04-SUMMARY.md` per `./.claude/get-shit-done/templates/summary.md`. Summary frontmatter MUST include:
  - `affects: [24-04, 25-01, 25-02, 26-01, 26-04, 27-01, 27-02, 27-03, 28-01, 29-01, 29-02, 29-03]`
  - `subsystem: layout-components`
  - `requires: [23-01, 23-02, 23-03]`
  - tech-stack.added: `tests/accessibility/v2-layout.spec.ts`
  - patterns: `mobile-overlay-focus-trap`, `aria-current-active-link`, `2-column-footer-with-44px-touch-targets`
  - Note that `src/pages/v2-smoke.astro` is throwaway and scheduled for deletion in Phase 25 when /faq migrates
</output>
