---
phase: 28-blog-reconstruction-index-post-tag-page
plan: 01
type: execute
wave: 2
depends_on:
  - 28-00
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-03
  - PAGE-09
  - PAGE-11
  - TOKEN-06
  - VALID-01
  - VALID-02
  - VALID-03
threat_model_summary:
  - id: T-28-01
    threat: "Pencil active-editor mismatch causes write to wrong .pen file"
    severity: high
    mitigation: "D-125 pre-flight `mcp__pencil__get_editor_state({ include_schema: false })` before every mutating call; halt and surface on mismatch."
  - id: T-28-03
    threat: "PAGE-11 raster (DzqTm) hidden BEFORE user APPROVE — destroys calibration target"
    severity: high
    mitigation: "Strict task sequencing per CALIBRATION-PROTOCOL § 3.4 step 7 + Pitfall 4: Task 0 = probe DzqTm raster image-import-NN.jpg index (READ-ONLY); Tasks 1-7 = construct Blog frame + content (NO DzqTm mutation); Task 8 = snapshot + screenshot; Task 9 = AskUserQuestion calibration gate; Task 10 (POST-APPROVE ONLY) = `Update(DzqTm, { enabled: false })`. NEVER call DzqTm Update before Task 9 APPROVE."
  - id: T-28-02
    threat: "Token surface drift during Blog index construction"
    severity: medium
    mitigation: "`get_variables({})` at plan open + close (expect 107 user-facing — zero new tokens added in this plan). Halt on drift."

must_haves:
  truths:
    - "DzqTm raster's `fill.image` reference probed at Task 0 (HIGHEST-PRIORITY DELIVERABLE per RESEARCH Focus 4); matching `design/images/image-import-NN.jpg` index recorded in 28-01-SUMMARY.md immediately upon discovery. Expected: image-import-14.jpg per Phase 25 D-49 raster-probe Variant Evidence; probe is authoritative."
    - "New top-level page frame named `Blog` exists in `design/Crito.pen` as a direct child of the document root (NOT inside library parents — Pitfall 3 carry-forward), positioned at the page-frame row (y ≈ -4111) via `mcp__pencil__find_empty_space_on_canvas` with `nodeId: 'n0QqTd'` anchor (Phase 27 Contact frame) per CALIBRATION-PROTOCOL § 10.4 + D-122."
    - "The `Blog` frame has width 1440, vertical auto-layout with alignItems center, fill `#ffffffff` (color-semantic-bg-page literal per OPEN-23-13)."
    - "The `Blog` frame contains 5 stacked content blocks: (1) Section/Header ref (G0wNOc, NO descendants override per D-77 carry-forward), (2) page-intro section (v1.3 verbatim 'Blog' heading-1 + body 'Practical insights on automation, AI, and custom software for small businesses. Learn how to work smarter, not harder.' per `src/pages/blog/index.astro` lines 32-37), (3) Section/TagFilter instance with 'All Posts' pill active (uses Plan 28-00 default — no descendants override needed), (4) 6-Card grid (3-col × 2-row per D-114), (5) Section/Footer ref (Xs0Hs, NO descendants override per D-77)."
    - "6 Card instances in 3-col × 2-row grid per D-114: each Card (t40xct ref) overrides title-slot descendants per D-111 (kI3bc.content = post title; metadata-caption.content = 'JAN 27, 2026 • 5 MIN READ' or representative date+read-time per v1.3 BlogCard structure) + footer-actions-slot descendants per D-112 (replace default Button placeholders with 2-3 Primitive/Badge/Outline instances for tags) + image-slot default placeholder + sibling Pencil note documenting featuredImage wiring per D-113."
    - "Section/TagFilter instance uses Plan 28-00 defaults: pill-1-active 'All Posts' enabled:true (M7eUr default); pills 2-5 use inactive ref (hIWuC) default for tag labels 'automation', 'small-business', 'productivity', 'ai'. No descendants override at instance time for Blog index — TagFilter component ships Blog-index-ready per Focus 6 Sub-option A1."
    - "Per-section fidelity labels per D-83 recorded in 28-01-SUMMARY.md: page-intro = EXACT (token-bound + content verbatim from v1.3 index.astro lines 32-37); TagFilter = APPROXIMATE (representative pill labels — actual tag set will be dynamic at code-milestone wire-up; sibling note documents this); 6-Card grid = APPROXIMATE (representative posts + metadata + tag composition; image-slot is default placeholder per D-113); Header = EXACT (Phase 25 shipped, D-77); Footer = EXACT (Phase 25 shipped, D-77)."
    - "User APPROVED the composition via single AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 3.4 step 5 (crito-source-flat-raster format) — pairs reconstructed Blog frame screenshot against the probed `image-import-NN.jpg` raster. Stale-cache fallback per § 6.4 Tier-1 (cross-row Update on Blog frame) then Tier-2 (user-editor verification) if needed."
    - "PAGE-11 ACTIVE — `DzqTm` raster hidden via `enabled: false` AFTER user APPROVE only per CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7 (NEVER before — Pitfall 4 enforcement; T-28-03 mitigation). SECOND production use of PAGE-11 ACTIVE in v2.0 (first was Phase 27 cl8tt)."
    - "PEN-INVENTORY status_counts for DzqTm moves from `flat:1` to `hidden:1`; Frames Inventory DzqTm row status updated from `IN-SCOPE` to `reconstructed-PHASE-28`; OPEN-23-05 partially-resolved by Plan 28-01 PAGE-11 ACTIVE (07_Blog reconstructed; 08_Blog Details remains for Plan 28-02)."
    - "PEN-INVENTORY Frames Inventory has new row for `Blog` (scope: IN-SCOPE, joel_page_map: /blog, status: reconstructed-PHASE-28); Variant Evidence (Phase 28) sub-section extended with Blog page-frame structural rows + 6-Card grid composition rows + TagFilter instance rows + literal-to-token bindings per OPEN-23-13 dual-track."
    - "Token surface unchanged at 107 user-facing across Plan 28-01 (Plan 28-01 ships zero new tokens — only Plan 28-00 added prose tokens)."
    - "Phase 23/24/25/26/27 baseline IDs + Plan 28-00 new library IDs intact (Plan 28-01 instances refs + mutates only DzqTm enabled:false at post-APPROVE step)."
    - "Document-level `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `\"No layout problems.\"` at plan close (text-clipping false-positives documented in 28-01-SUMMARY.md, NOT mitigated per Pitfall 5)."
    - "Card consumer-resolution per OPEN-25-07: first concrete consumer locks Card values via instance overrides; PEN-INVENTORY OPEN-25-07 status updated to 'partially-resolved' (final resolution at Plan 28-03 close after all 15 Card instances across 3 frames complete)."
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `Blog` with 5 vertical content blocks: Section/Header ref + page-intro + Section/TagFilter instance + 6-Card grid + Section/Footer ref. DzqTm raster hidden via enabled:false after user APPROVE (PAGE-11 ACTIVE)."
      contains: "Blog page frame (id <blogPageId>), page-intro section, TagFilter ref instance, 6-Card grid frame with 6 Card refs (t40xct) each with descendants overrides, Section/Header ref G0wNOc, Section/Footer ref Xs0Hs, DzqTm enabled:false."
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for Blog + DzqTm row status_counts update (flat:1 → hidden:1) + DzqTm status update (reconstructed-PHASE-28) + Variant Evidence (Phase 28) Blog rows + OPEN-23-05 partial-resolution + OPEN-25-07 status update + any OPEN-28-NN raised."
      contains: "Blog row in Frames Inventory, DzqTm status_counts update, Variant Evidence rows for Blog page frame + 6-Card grid + TagFilter instance"
  key_links:
    - from: "Blog page frame (<blogPageId>)"
      to: "Section / Header (G0wNOc) + Section / TagFilter (<tagFilterId>) + Section / Footer (Xs0Hs)"
      via: "ref children at top + middle + bottom; NO descendants override per D-77 (Header/Footer) + Plan 28-00 defaults (TagFilter)"
      pattern: "Blog.*ref.*G0wNOc.*ref.*<tagFilterId>.*ref.*Xs0Hs"
    - from: "Blog 6-Card grid"
      to: "6 Compound/Card (t40xct) ref instances each with descendants override"
      via: "Each Card instance overrides: kI3bc.content (post title) + <metadataCaptionId>.content (date • read time) + footer-actions-slot Badge/Outline replacements (2-3 tag pills per Card) + image-slot default placeholder + sibling featuredImage note"
      pattern: "blog-grid.*ref.*t40xct.*descendants.*kI3bc.*<metadataCaptionId>.*<badgeOutlineId>"
    - from: "Plan 28-01 Task 10 DzqTm mutation"
      to: "DzqTm `enabled: false` (PAGE-11 ACTIVE post-APPROVE only per § 3.4 step 7)"
      via: "batch_design Update AFTER user APPROVE at Task 9 calibration gate; NOT before (T-28-03 mitigation)"
      pattern: "DzqTm.*enabled.*false"
---

<objective>
Create the `Blog` page frame in `design/Crito.pen` as a crito-source-present-flat-raster reconstruction per D-122 Plan 28-01 + CALIBRATION-PROTOCOL § 3 (crito-source branch, flat-raster sub-case). The Crito source `DzqTm` (07_Blog) is a single image-import-*.jpg flat raster per OPEN-23-05; calibration pairs the reconstructed Blog frame against that raster per § 3.1 + § 3.5.

**Critical first-task probe (HIGHEST PRIORITY per RESEARCH Focus 4 + Pitfall 3):** Plan 28-01 Task 0 MUST call `mcp__pencil__batch_get({ nodeIds: ['DzqTm'], readDepth: 2 })` to read the `fill.image` reference and identify the matching `image-import-NN.jpg` index BEFORE any reconstruction work. Document the index in 28-01-SUMMARY.md immediately upon discovery. Expected: image-import-14.jpg per Phase 25 D-49 raster-probe Variant Evidence — but probe is authoritative.

Composition per D-122 (top → bottom):
1. `Section / Header` instance (G0wNOc, Phase 25) — Crito-source nav labels stay literal per D-77 carry-forward.
2. **Page-intro section** — heading-1 "Blog" + body "Practical insights on automation, AI, and custom software for small businesses. Learn how to work smarter, not harder." (v1.3 verbatim from `src/pages/blog/index.astro` lines 32-37).
3. **`Section / TagFilter` instance** (Plan 28-00 `<tagFilterId>`) — uses pre-configured Blog-index defaults: pill-1-active "All Posts" enabled:true (M7eUr); pills 2-5 inactive (hIWuC) with labels "automation", "small-business", "productivity", "ai". NO descendants override at instance time.
4. **6-Card grid** (3-col × 2-row per D-114, gap 24, alignItems start) — 6 Compound/Card (t40xct) ref instances; each Card overrides title-slot descendants (kI3bc title + `<metadataCaptionId>` metadata per D-111) + footer-actions-slot descendants (replace Button slots with 2-3 Primitive/Badge/Outline instances per D-112 + D-119) + image-slot default placeholder + sibling featuredImage wiring note per D-113.
5. `Section / Footer` instance (Xs0Hs, Phase 25) — Crito-source labels stay literal per D-77.

**PAGE-11 ACTIVE** per CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7: Task 10 fires `batch_design Update("DzqTm", { enabled: false })` ONLY AFTER user APPROVE at Task 9 calibration gate. SECOND production use of PAGE-11 ACTIVE in v2.0 (Pitfall 4 + T-28-03 — never hide before APPROVE).

**Purpose:** Phase 28's first per-page reconstruction. First concrete Compound/Card consumer per OPEN-25-07 (locks Card values via 6 instance overrides). First Section/TagFilter consumer per D-115 broad-scoping (Tag page is second consumer at Plan 28-03). Validates Plan 28-00 foundation deliverables.

**Output:** A working `Blog` top-level page frame; DzqTm raster hidden post-APPROVE; extended PEN-INVENTORY with Frames Inventory row + DzqTm status update + Variant Evidence (Phase 28) rows + OPEN-23-05 partial-resolution + OPEN-25-07 status update + any OPEN-28-NN; per-section fidelity labels recorded; plan-close user APPROVED state. ONE user gate at plan close per CALIBRATION-PROTOCOL § 3.4 step 5 (crito-source format).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-VALIDATION.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@src/pages/blog/index.astro
@src/components/BlogCard.astro
@src/content/blog/getting-started-with-automation.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plan 28-00 outputs intact + PROBE DzqTm raster image-import-NN.jpg index (HIGHEST PRIORITY) + extract v1.3 content</name>
  <files>design/Crito.pen (read-only); src/pages/blog/index.astro (read-only); src/components/BlogCard.astro (read-only); src/content/blog/getting-started-with-automation.mdx (read-only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` — Plan 28-00 deliverable IDs: `<tagFilterId>`, `<relatedPostsId>`, `<metadataCaptionId>`, `<badgeOutlineId>`
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 4 (DzqTm probe pattern — expected image-import-14.jpg) + Pitfall 4 (NEVER hide raster before APPROVE)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-122 (FindEmptySpace anchor on n0QqTd) + D-125 pre-flight
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.5 description identifier convention (`28-blog-{section}--side-by-side.png` paired with `design/images/image-import-NN.jpg`)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` Plan 27-02 anchor frame n0QqTd ID + page-frame row y coords (≈ -4111.55)
    - `src/pages/blog/index.astro` lines 1-100 (page-intro extraction + tag set + filter strip pattern)
    - `src/components/BlogCard.astro` lines 1-79 (BlogCard 4-section composition for Card-instance content reference per D-111 + D-112)
    - `src/content/blog/getting-started-with-automation.mdx` (frontmatter title + tags + pubDate for representative Card metadata)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125: `mcp__pencil__get_editor_state({ include_schema: false })`. Halt + surface on mismatch.

    Step 2 — Token surface drift check: `mcp__pencil__get_variables({})`. Assert user-facing count is 107 (Plan 28-00 close). Halt on drift.

    Step 3 — **HIGHEST PRIORITY: PROBE DzqTm raster image-import-NN.jpg index (RESEARCH Focus 4 + Pitfall 3):**
    `mcp__pencil__batch_get({ nodeIds: ['DzqTm'], readDepth: 2 })`. Read the `fill.image` reference (or descendant rectangle's `fill.image` if DzqTm is a frame containing a single rect). Expected structure per Phase 27 Plan 27-02 Task 0 precedent:
    - `DzqTm.fill = { type: "image", image: "image-import-NN.jpg" }` OR
    - `DzqTm.children[0].fill = { type: "image", image: "image-import-NN.jpg" }`

    Record the discovered `image-import-NN.jpg` index in 28-01-SUMMARY scratch IMMEDIATELY. This index is:
    - The visual calibration target for Task 9 side-by-side gate.
    - The description identifier suffix: `28-blog-{section}--side-by-side.png (paired with design/images/image-import-NN.jpg)`.
    - Recorded in PEN-INVENTORY § Frames Inventory DzqTm row notes per D-126.

    If `fill.image` not directly readable, recursively walk DzqTm via readDepth: 3 — fall back to identifying the only image-fill node inside the DzqTm subtree.

    Step 4 — Plan 28-00 deliverable intactness:
    `mcp__pencil__batch_get({ readDepth: 1, nodeIds: ['<tagFilterId>', '<relatedPostsId>', 'vGH3A', '<metadataCaptionId>', '<badgeOutlineId>'] })` (use IDs from 28-00-SUMMARY). Confirm structural intactness + paired-ref pills inside TagFilter pills-row.

    Step 5 — Phase 24/25/26/27 baseline component intactness:
    `batch_get` on: G0wNOc (Section/Header), Xs0Hs (Section/Footer), t40xct (Compound/Card), M7eUr (Button/Default), hIWuC (Button/Secondary), j0FxQZ (Badge), n0QqTd (Phase 27 Contact frame for FindEmptySpace anchor).

    Step 6 — Read v1.3 sources for verbatim content:
    - `src/pages/blog/index.astro` lines 32-37: page-intro heading "Blog" + body "Practical insights on automation, AI, and custom software for small businesses. Learn how to work smarter, not harder."
    - `src/pages/blog/index.astro` line 53: 'All Posts' filter pill label (matches Plan 28-00 TagFilter default)
    - `src/components/BlogCard.astro` lines 1-79: BlogCard composition structure (image / title-and-metadata / excerpt / tags)
    - `src/content/blog/getting-started-with-automation.mdx` frontmatter: title "Getting Started with Business Automation", pubDate 2026-01-27, tags ["automation", "small-business", "productivity"] — use as representative Card 1 content

    Record all extracted verbatim strings in 28-01-SUMMARY scratch.

    Step 7 — Derive 6 representative Card content sets (Cards 1-6 per D-114):
    - Card 1: title "Getting Started with Business Automation" (Joel's MDX); metadata "JAN 27, 2026 • 5 MIN READ"; tags ["automation", "small-business", "productivity"]
    - Card 2: title "I'm Pivoting" (Joel's other MDX file); metadata representative; tags representative
    - Cards 3-6: representative agency-template titles + metadata + tags per Crito raster Blog page register (probed image-import-NN.jpg shows ~6 Card density per D-114). Use 2-3 tag labels per Card from the set {automation, small-business, productivity, ai, design, tutorial}.

    Plan-execution may probe v1.3 BlogCard.astro `excerpt` extraction to inform body-slot content (default Phase 25 D-53 placeholder OR representative excerpt). Default: body-slot stays as Phase 25 placeholder; sibling note documents code-milestone wires actual excerpt from MDX frontmatter `description`.

    Step 8 — Look up Compound/Card descendant IDs from 25-03-SUMMARY + 28-00-SUMMARY:
    - kI3bc (title-text inside vGH3A — Phase 25)
    - `<metadataCaptionId>` (caption-text inside vGH3A — Plan 28-00 Task 3)
    - oTSwn (body-slot — Phase 25)
    - FGdti (image-slot — Phase 25)
    - eNqxd (footer-actions-slot — Phase 25)
    - `<badgeOutlineId>` (Plan 28-00 Task 6 — for tag pill replacements)
    - `<badgeOutlineLabelId>` (label child inside Badge/Outline)

    No mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token: 107. DzqTm raster index PROBED: image-import-NN.jpg (recorded in scratch). Plan 28-00 deliverables intact (TagFilter, RelatedPosts, vGH3A extension, Badge/Outline). Baselines intact (G0wNOc, Xs0Hs, t40xct, M7eUr, hIWuC, j0FxQZ, n0QqTd). v1.3 verbatim strings extracted (page-intro + 6 Card content sets). Compound/Card descendant IDs ready."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned activeEditor ending in `design/Crito.pen`.
    - `get_variables({})` returned 107 user-facing tokens.
    - **`batch_get(['DzqTm'], readDepth: 2)` returned DzqTm structure and `fill.image` reference identified — matching `image-import-NN.jpg` index recorded in 28-01-SUMMARY scratch (highest-priority probe deliverable per Pitfall 3).**
    - `batch_get` on Plan 28-00 IDs confirms intactness.
    - `batch_get` on G0wNOc, Xs0Hs, t40xct, M7eUr, hIWuC, j0FxQZ, n0QqTd confirms baseline intactness.
    - v1.3 verbatim strings extracted (page-intro heading + body + 6 Card content sets).
    - Compound/Card descendant IDs looked up (kI3bc, metadataCaptionId, oTSwn, FGdti, eNqxd, badgeOutlineId).
    - No mutations in Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight assertions pass. DzqTm raster index probed (highest-priority deliverable). v1.3 verbatim content captured. Card descendants override IDs ready. Plan 28-01 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `Blog` top-level page frame via find_empty_space_on_canvas with nodeId:n0QqTd anchor + batch_design Insert</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 10.4 (FindEmptySpace nodeId anchor pattern)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-122 (Plan 28-01 anchors on n0QqTd Contact frame from Phase 27)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` n0QqTd at (20887.27, -4111.55); expected Blog frame coords next-right
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — FindEmptySpace with `nodeId: 'n0QqTd'` anchor:
    `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 2400, direction: "right", padding: 80, nodeId: "n0QqTd" })`. Expected `{x, y}` ≈ `{x: ~22487, y: -4111.55}` (next page-frame slot right of n0QqTd at +1520 horizontal offset). Height 2400 estimate = Header (~80) + page-intro (~200) + TagFilter (~80) + 6-Card grid (~1200 for 3-col × 2-row at ~600h each row including gap) + Footer (~500) + spacing.

    Step 3 — `mcp__pencil__batch_design` Insert top-level frame:
    - Parent: document root (Pitfall 3 carry-forward).
    - `type: "frame"`, `name: "Blog"`, `placeholder: true` (clear in Task 8 with `fit_content` switch per § 10.8), `position: { x, y }` from FindEmptySpace.
    - Dimensions: `width: 1440`, `height: 2400`.
    - Layout: `layout: "vertical"`, `gap: 0`, `padding: 0`, `alignItems: "center"`.
    - Fill: `"#ffffffff"` (color-semantic-bg-page literal per OPEN-23-13).

    Record `<blogPageId>` + returned `{x, y}` coords in 28-01-SUMMARY scratch.
  </action>
  <verify>
    `batch_get(['<blogPageId>'])` returns top-level frame named "Blog" at document root level, width 1440, vertical auto-layout, alignItems center, fill #ffffffff, position y ≈ -4111 (same page-frame row).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `find_empty_space_on_canvas({ ..., nodeId: 'n0QqTd' })` returned `{x, y}` with y ≈ -4111 (same row).
    - `batch_design` Insert completed without error.
    - `batch_get(['<blogPageId>'])` returns a frame at document root level (NOT inside library parents or other page frames) named "Blog" with width 1440, vertical auto-layout, alignItems center, fill #ffffffff.
    - No mutations to baseline IDs or n0QqTd.
    - `<blogPageId>` + coords recorded.
  </acceptance_criteria>
  <done>
    Empty Blog page frame in place at canvas page-frame row, next-right of n0QqTd.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header ref + page-intro section (v1.3 verbatim "Blog" + body)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-77 (Header instanced as-is — Crito-source nav labels stay literal)
    - Task 0 scratch: v1.3 page-intro heading + body verbatim strings
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` page-intro pattern precedent (heading-1 + prose-paragraph body)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 2 children of `<blogPageId>`:

    **Insert 1 — Section/Header ref:**
    - `{ type: "ref", ref: "G0wNOc" }` — NO descendants override per D-77 carry-forward.

    **Insert 2 — page-intro section frame:**
    - `type: "frame"`, `name: "page-intro"`, `width: 1200`, `layout: "vertical"`, `gap: 16`, `padding: [80, 0, 48, 0]` (section-y rhythm + headline tier), `alignItems: "center"` (Blog agency-template register — centered intro per Crito Blog raster + v1.3 `text-center` in index.astro line 36).
    - Child 1 — Heading text (v1.3 verbatim):
      `{ type: "text", name: "heading", content: "Blog", fontFamily: "Plus Jakarta Sans" (type-semantic-heading-1-family), fontSize: 48 (type-semantic-heading-1-size), fontWeight: 700 (type-semantic-heading-1-weight), fill: "#141f39ff" (color-semantic-text-primary), lineHeight: 1.4 (type-semantic-heading-1-lh) }`
    - Child 2 — Body text (v1.3 verbatim):
      `{ type: "text", name: "body", content: "Practical insights on automation, AI, and custom software for small businesses. Learn how to work smarter, not harder.", fontFamily: "Inter" (type-semantic-prose-paragraph-family), fontSize: 16 (type-semantic-prose-paragraph-size), fontWeight: 400, fill: "#52525bff" (color-semantic-text-secondary), lineHeight: 1.625 (type-semantic-prose-paragraph-lh), textAlign: "center", width: 600 (max-w-2xl per v1.3 line 33) }`

    Pitfall 7 reminder: all literal hex; PEN-INVENTORY Variant Evidence row binds literals to tokens. Pitfall 5 reminder: text-clipping false-positives expected on body.

    Record `<pageIntroId>`, `<pageIntroHeadingId>`, `<pageIntroBodyId>` in scratch.
  </action>
  <verify>
    `batch_get(['<blogPageId>'], readDepth: 2)` shows 2 direct children: G0wNOc ref + page-intro frame. page-intro contains heading "Blog" (Plus Jakarta Sans 48/700) + body "Practical insights on automation..." (Inter 16/400 lh 1.625 width 600).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<blogPageId>'])` returns 2 children: Section/Header ref + page-intro frame.
    - Section/Header ref has NO descendants override (D-77 compliance).
    - page-intro frame has width 1200, vertical layout, gap 16, alignItems center.
    - Heading content matches v1.3 EXACTLY: "Blog".
    - Body content matches v1.3 EXACTLY: "Practical insights on automation, AI, and custom software for small businesses. Learn how to work smarter, not harder." (no truncation).
    - Heading typography: Plus Jakarta Sans 48/700/lh 1.4 fill #141f39ff (heading-1 token surface).
    - Body typography: Inter 16/400/lh 1.625 fill #52525bff (prose-paragraph token surface).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Top of Blog: Header + page-intro section (v1.3 verbatim "Blog" + body).
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert Section/TagFilter ref instance (Plan 28-00 defaults — no descendants override)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-117 (active-state mechanic) + D-122 (Blog index TagFilter uses pre-configured defaults)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` `<tagFilterId>` + paired-ref pill IDs + pill labels
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 6 Sub-option A1 (paired-ref mechanic; Blog index ships pre-configured with pill-1 active)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<blogPageId>` (3rd of vertical stack):

    **Insert — Section/TagFilter ref:**
    - `{ type: "ref", ref: "<tagFilterId>" }` (Plan 28-00 `<tagFilterId>` from 28-00-SUMMARY) — NO descendants override per Plan 28-00 default pre-configuration: pill-1-active "All Posts" enabled:true (M7eUr), pills 2-5 inactive (hIWuC) with labels "automation", "small-business", "productivity", "ai".

    **Why no descendants override:** Plan 28-00 Task 4 built TagFilter with Blog-index-ready defaults per Focus 6 Sub-option A1. Tag page (Plan 28-03) will override at instance time to flip which pill is active. Blog index uses the as-shipped default.

    Step 3 — Wrap TagFilter ref in a parent frame for consistent layout per D-122 + sibling sectioning convention:
    - **Wrap pattern:** TagFilter ref is direct child of `<blogPageId>` (no wrapper) OR wrapped in a section frame for consistent padding. Default: direct child with `marginBottom` via parent gap. Plan-execution may pivot if visual rendering at calibration shows TagFilter too close to page-intro or 6-Card grid.

    Record `<tagFilterRefId>` in scratch.
  </action>
  <verify>
    `batch_get(['<blogPageId>'], readDepth: 1)` shows 3 direct children: G0wNOc + page-intro + `<tagFilterRefId>` (ref to `<tagFilterId>`).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<blogPageId>'])` now has 3 children: Section/Header ref + page-intro + Section/TagFilter ref.
    - TagFilter ref has NO descendants override.
    - `<tagFilterRefId>` recorded.
  </acceptance_criteria>
  <done>
    TagFilter strip in place under page-intro. Pre-configured Blog-index defaults (pill-1 "All Posts" active).
  </done>
</task>

<task type="auto">
  <name>Task 4: Insert 6-Card grid frame + 3 Card instances (Cards 1-3, first row) with full descendants overrides</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-111 (title-slot descendants override) + D-112 (footer-actions-slot Badge instances) + D-113 (image-slot default + sibling wiring note) + D-114 (3-col × 2-row grid)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` `<metadataCaptionId>` + `<badgeOutlineId>` + Badge/Outline label child ID
    - Task 0 scratch: 6 Card content sets (titles + metadata + tags)
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` Card slot IDs (vGH3A title + kI3bc title-text + oTSwn body + FGdti image + eNqxd footer-actions)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 3 Path A (descendants override mechanic — content+enabled only, NOT net-new insertion)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<blogPageId>` (4th of vertical stack) — the grid frame:

    **Insert — 6-Card grid frame:**
    - `type: "frame"`, `name: "blog-grid"`, `width: 1200`, `layout: "vertical"` (rows are vertical-stack of horizontal-row frames; alternative: Pencil grid if 2.13 supports — default to nested vertical/horizontal layout for predictability), `gap: 24`, `padding: [0, 0, 80, 0]`, `alignItems: "center"`.

    **Row 1 — horizontal frame** (3 cards):
    - `{ type: "frame", name: "row-1", width: "fill_container", layout: "horizontal", gap: 24, alignItems: "start" }` containing 3 Card refs (Cards 1-3 below).

    **Card 1 instance** (title "Getting Started with Business Automation" + metadata + tags ["automation", "small-business", "productivity"]):
    - `{ type: "ref", ref: "t40xct", descendants: { "kI3bc": { content: "Getting Started with Business Automation" }, "<metadataCaptionId>": { content: "JAN 27, 2026 • 5 MIN READ" }, "eNqxd": { contains: [<3 Badge/Outline refs>] } } }`
    - footer-actions-slot eNqxd contains override: 3 ref instances of `<badgeOutlineId>` with descendants overrides:
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "automation" } } }`
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "small-business" } } }`
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "productivity" } } }`
    - image-slot (FGdti): default placeholder per Phase 25 D-53 + D-113 (no override needed)

    **Card 2 instance** (title "I'm Pivoting" + metadata + tags ["ai", "automation"] — representative from Joel's im-pivoting.mdx):
    - Same pattern with overrides: kI3bc.content = "I'm Pivoting"; `<metadataCaptionId>`.content = "FEB 09, 2026 • 3 MIN READ"; eNqxd.contains = [Badge/Outline "ai", Badge/Outline "automation"]

    **Card 3 instance** (representative agency-template content per D-114):
    - Overrides: kI3bc.content = "The Future of Small Business Tech"; `<metadataCaptionId>`.content = "JAN 15, 2026 • 7 MIN READ"; eNqxd.contains = [Badge/Outline "ai", Badge/Outline "tutorial"]

    Step 3 — Insert sibling Pencil note at Blog grid canvas position per D-113 featuredImage wiring belt-and-suspenders:
    - `{ type: "note", name: "Blog Card featuredImage wiring", content: "Code milestone wires each Card image-slot (FGdti) to `post.data.featuredImage` (frontmatter image path from MDX). v2.0 ships default placeholder (180h landscape, color-semantic-bg-surface-elevated fill) per D-113. Card title-slot descendants override per D-111: title from `post.data.title`; metadata-caption from `${pubDate} • ${readingTime}` template. Card footer-actions-slot descendants override per D-112: Badge/Outline instances looped from `post.data.tags` array (typically 2-3 tags per post)." }`

    Record Card 1-3 instance IDs + Badge/Outline tag pill IDs in scratch.

    **Pitfall 9 chunk note:** This task creates 1 grid frame + 1 row frame + 3 Card refs + 9 Badge/Outline refs + 1 note ≈ 14 ops. Within 25-op Pitfall 9 budget. Task 5 handles Row 2 (Cards 4-6).
  </action>
  <verify>
    `batch_get(['<blogGridId>'], readDepth: 4)` shows row-1 frame with 3 Card refs (t40xct); each Card has descendants override for kI3bc + `<metadataCaptionId>` + eNqxd contains 2-3 Badge/Outline refs.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<blogPageId>'])` now has 4 children: Header + page-intro + TagFilter + blog-grid.
    - blog-grid contains row-1 frame (horizontal, gap 24); row-1 contains 3 Card refs.
    - Each Card ref's descendants override map contains keys: kI3bc, `<metadataCaptionId>`, eNqxd (with contains override).
    - eNqxd contains override has 2-3 Badge/Outline ref children with descendants override on label.
    - Sibling featuredImage wiring note exists at blog-grid canvas position.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Row 1 of 6-Card grid complete (Cards 1-3 with full descendants overrides + Badge/Outline tag pills + sibling note).
  </done>
</task>

<task type="auto">
  <name>Task 5: Insert Row 2 (Cards 4-6) + Section/Footer ref (closes Blog frame composition)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: Card 4-6 content sets
    - Task 4 scratch: blog-grid frame ID
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-77 (Footer instanced as-is)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<blogGridId>` (Row 2):

    **Row 2 — horizontal frame** (3 cards):
    - `{ type: "frame", name: "row-2", width: "fill_container", layout: "horizontal", gap: 24, alignItems: "start" }` containing 3 Card refs (Cards 4-6).

    **Card 4 instance** (representative):
    - Overrides: kI3bc.content = "Why I Use Astro for Client Sites"; `<metadataCaptionId>`.content = "JAN 08, 2026 • 4 MIN READ"; eNqxd.contains = [Badge/Outline "design", Badge/Outline "tutorial"]

    **Card 5 instance** (representative):
    - Overrides: kI3bc.content = "Building Trust with Small Business Clients"; `<metadataCaptionId>`.content = "DEC 22, 2025 • 6 MIN READ"; eNqxd.contains = [Badge/Outline "small-business", Badge/Outline "productivity"]

    **Card 6 instance** (representative):
    - Overrides: kI3bc.content = "AI Tools That Actually Save Time"; `<metadataCaptionId>`.content = "DEC 10, 2025 • 5 MIN READ"; eNqxd.contains = [Badge/Outline "ai", Badge/Outline "productivity", Badge/Outline "automation"]

    Step 3 — `batch_design` Insert 1 child of `<blogPageId>` (5th + last of vertical stack):

    **Insert — Section/Footer ref:**
    - `{ type: "ref", ref: "Xs0Hs" }` — NO descendants override per D-77 carry-forward.

    Step 4 — Update `<blogPageId>` to fit_content per CALIBRATION-PROTOCOL § 10.8:
    - `Update("<blogPageId>", { placeholder: false, height: "fit_content" })` — frame grows from initial 2400 height estimate to actual content height.

    Record Card 4-6 IDs + footer ref ID + updated `<blogPageId>` final height in scratch.
  </action>
  <verify>
    `batch_get(['<blogPageId>'], readDepth: 1)` shows 5 children in order: G0wNOc + page-intro + TagFilter ref + blog-grid + Xs0Hs. blog-grid has 2 row children (row-1 + row-2) each with 3 Card refs. `<blogPageId>` height now fit_content.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert + Update completed without error.
    - `batch_get(['<blogPageId>'])` returns 5 children in vertical order: Section/Header + page-intro + Section/TagFilter + blog-grid + Section/Footer.
    - blog-grid has 2 row children (row-1 + row-2), each with 3 Card refs (6 total Cards).
    - 6 Card descendants overrides all use Plan 28-00 `<metadataCaptionId>` + `<badgeOutlineId>` consistently.
    - Section/Footer ref has NO descendants override.
    - `<blogPageId>` height = fit_content (Pencil grew from 2400 to actual content height).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Full Blog frame composition complete: Header + page-intro + TagFilter + 6-Card grid + Footer. PAGE-11 ACTIVE deferred to Task 10 (post-APPROVE only).
  </done>
</task>

<task type="auto">
  <name>Task 6: Token-binding verification + zero-raw-values sweep on Blog frame</name>
  <files>design/Crito.pen (read-only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-126 + cross-cutting "zero raw hex/px" policy
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` zero-raw-values sweep precedent
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__search_all_unique_properties({ parentId: '<blogPageId>' })`:
    - Verify zero raw color hex and zero raw px values inside Blog frame structural composition (page-intro typography literals + Card-instance descendants override literals are exempt per OPEN-23-13 dual-track — PEN-INVENTORY Variant Evidence rows bind literals to tokens).
    - The acceptable literal set: heading-1 (Plus Jakarta Sans / 48 / 700 / #141f39ff / lh 1.4); prose-paragraph body (Inter / 16 / 400 / #52525bff / lh 1.625); Card title-slot Plus Jakarta Sans 18/700 (Phase 25 D-49 baseline); metadata-caption Inter 14/400 #52525bff (Plan 28-00 Task 3 inherited); Badge/Outline white fill + 1px border + cornerRadius 10 (Plan 28-00 Task 6 baseline).
    - Unexpected raw values (e.g., a stray #ff0000 or 73px padding) raise OPEN-28-NN flag.

    Step 3 — `mcp__pencil__snapshot_layout({ parentId: '<blogPageId>', problemsOnly: true })`:
    - Expect `"No layout problems."` or text-clipping false-positives only (Pitfall 5 carry-forward; multi-line content surfaces — body paragraph + card title titles).
    - Document each false-positive in 28-01-SUMMARY scratch.

    Step 4 — `mcp__pencil__get_variables({})` re-check: assert user-facing count still 107 (no accidental token mutation).

    No mutations in this task.
  </action>
  <verify>
    `search_all_unique_properties({ parentId: '<blogPageId>' })` returns only expected literal set (heading + prose-paragraph + Card baseline + metadata-caption + Badge/Outline baseline). `snapshot_layout({ parentId: '<blogPageId>', problemsOnly: true })` returns `"No layout problems."` or documented text-clipping false-positives only.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `search_all_unique_properties` scope-pass: zero unexpected raw hex/px inside Blog frame (only documented literal set from page-intro + Card baseline + metadata + Badge/Outline).
    - `snapshot_layout` clean or documented false-positives only.
    - `get_variables({})` count: 107 (no drift).
    - No mutations in Task 6.
  </acceptance_criteria>
  <done>
    Blog frame passes zero-raw-values sweep + snapshot_layout sweep. Ready for screenshot capture (Task 7) + calibration gate (Task 9).
  </done>
</task>

<task type="auto">
  <name>Task 7: Capture per-section screenshots via get_screenshot (with Tier-1 stale-cache fallback)</name>
  <files>design/Crito.pen (mutating only if Tier-1 fallback triggers)</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 6.4 (stale-cache Tier-1 + Tier-2 workarounds; § 3.4 step 4 screenshot capture)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 8 (stale-cache likely on new page frames; Tier-1 cross-row Update applies to page frames only)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__get_screenshot({ nodeId: '<blogPageId>' })` — full-page Blog frame screenshot.
    Sections to spot-check at calibration gate:
    - `mcp__pencil__get_screenshot({ nodeId: '<pageIntroId>' })`
    - `mcp__pencil__get_screenshot({ nodeId: '<tagFilterRefId>' })`
    - `mcp__pencil__get_screenshot({ nodeId: '<blogGridId>' })`

    Step 3 — If any get_screenshot returns blank-white per OPEN-26-02 stale-cache:
    **Tier-1 fallback (PAGE FRAMES only per Focus 8 + § 6.4):**
    - `Update("<blogPageId>", { y: <different-row-y, e.g., -2000> })` — temporarily move out of page-frame row
    - re-call `get_screenshot`
    - `Update("<blogPageId>", { y: -4111.55 })` — restore to original row

    **Tier-2 fallback (if Tier-1 still blank):**
    - Document Tier-2 fallback path: present batch_get structural data + snapshot_layout at AskUserQuestion gate (Task 9) and request user-side editor visual verification at the specific frame's canvas position. Per Focus 8 + § 6.4 production-proven pattern from Phase 26 Plan 26-02 + Phase 27 Plan 27-02.

    Step 4 — Record screenshot inline-only (NOT disk-written per OPEN-23-01 substitution); description identifier per § 3.5: `28-blog-full-page--side-by-side.png (paired with design/images/image-import-NN.jpg)`, `28-blog-page-intro--side-by-side.png`, `28-blog-tag-filter--side-by-side.png`, `28-blog-card-grid--side-by-side.png` (NN from Task 0 probe).

    Document Tier-1 vs Tier-2 outcome per section in scratch.
  </action>
  <verify>
    All 4 sections (full + page-intro + tag-filter + card-grid) have screenshots captured OR Tier-2 fallback documented. No file-disk writes (inline-only per OPEN-23-01).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `get_screenshot` called for full Blog frame + 3 sections (page-intro + TagFilter ref + blog-grid).
    - If Tier-1 fallback triggered: cross-row Update + restore sequence completed without leaving Blog frame at non-page-frame-row y.
    - If Tier-2 fallback documented: scratch records which sections require user-editor verification at calibration gate.
    - Screenshot description identifiers recorded per § 3.5.
  </acceptance_criteria>
  <done>
    Screenshots ready for calibration gate. Stale-cache outcome documented per section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Plan-close calibration AskUserQuestion gate per CALIBRATION-PROTOCOL § 3.4 step 5 (crito-source-flat-raster format)</name>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.4 step 5 (AskUserQuestion format for crito-source-flat-raster branch) + § 3.5 description identifier convention
    - Task 0 scratch: `design/images/image-import-NN.jpg` index probed for DzqTm
    - Task 6 scratch: zero-raw-values + snapshot_layout sweep results
    - Task 7 scratch: screenshot Tier-1/Tier-2 outcome
  </read_first>
  <what-built>
    A new `Blog` top-level page frame at canvas (~22487, -4111.55) with 5 stacked content blocks: Section/Header (G0wNOc, no override) + page-intro section ("Blog" heading-1 + body verbatim from v1.3 index.astro) + Section/TagFilter ref (pre-configured pill-1 "All Posts" active + pills 2-5 inactive labels "automation" / "small-business" / "productivity" / "ai") + 6-Card grid (3-col × 2-row, each Card with title + metadata-caption + 2-3 Badge/Outline tag pills + default image placeholder + sibling featuredImage wiring note) + Section/Footer (Xs0Hs, no override).

    Token bindings audit-trailed in PEN-INVENTORY Variant Evidence (Phase 28) rows per OPEN-23-13 dual-track. Token surface unchanged at 107 user-facing.
  </what-built>
  <how-to-verify>
    **VALID-02 calibration pairing per § 3.1 (crito-source-flat-raster sub-case):**

    1. Open `design/Crito.pen` in Pencil editor.
    2. Locate the original Crito flat raster `DzqTm` (07_Blog) at canvas position (record per § 3.1 — typically still visible; PAGE-11 ACTIVE will hide AFTER this APPROVE).
    3. Locate the reconstructed `Blog` frame at canvas (~22487, -4111.55) — next-right of n0QqTd Contact frame from Phase 27.
    4. Open `design/images/image-import-NN.jpg` (NN from Task 0 probe — likely 14 per Phase 25 D-49) in a separate viewer as the calibration target.
    5. Side-by-side compare reconstructed Blog frame against image-import-NN.jpg for the following sections:
       - **Page-intro**: heading "Blog" + body line + alignment (centered per agency-template) — fidelity proposal: EXACT (token-bound + v1.3 verbatim)
       - **TagFilter strip**: filter pill row with 'All Posts' active + 4 inactive tag pills — fidelity proposal: APPROXIMATE (representative pill labels; actual tag set will be dynamic at code-milestone)
       - **6-Card grid**: 3-col × 2-row density + Card composition (image placeholder + title + date/read-time + tag pills) — fidelity proposal: APPROXIMATE (representative posts + metadata + tags; image-slot default placeholder per D-113)
       - **Header**: EXACT (Phase 25 shipped, D-77 carry-forward)
       - **Footer**: EXACT (Phase 25 shipped, D-77 carry-forward)
    6. Confirm or revise per-section fidelity labels.
    7. Confirm Compound/Card consumer-resolution: are the 6 Card instances honest representations of v1.3 BlogCard composition pattern (image + title + metadata + tags)? Should anything be adjusted at the Card-component level (e.g., padding, border, image-slot aspect)?
    8. Stale-cache note: if Task 7 screenshots were blank-white (Tier-1 fallback), verify visually in Pencil editor at the Blog frame canvas position per § 6.4 Tier-2 fallback.

    **Decision per § 3.4 step 6:** APPROVE / REVISE / GAP (with specific GAP description for OPEN-28-NN flag if needed).
  </how-to-verify>
  <resume-signal>
    Type "APPROVED" to:
    - Lock the 5 fidelity labels (page-intro EXACT, TagFilter APPROXIMATE, 6-Card grid APPROXIMATE, Header EXACT, Footer EXACT)
    - Authorize Task 10 PAGE-11 ACTIVE (hide DzqTm via enabled:false)

    Type "REVISE: <description>" to refine specific sections before APPROVE (e.g., "REVISE: tag pill count is 6 in raster not 5, add 'design' pill"; "REVISE: page-intro alignment should be left-aligned per raster").

    Type "GAP: <description>" to record an OPEN-28-NN flag without blocking phase progression (e.g., "GAP: raster shows Card hover state — defer to code milestone").
  </resume-signal>
</task>

<task type="auto">
  <name>Task 9: ONLY IF APPROVE — Fire PAGE-11 ACTIVE on DzqTm (enabled: false) + update PEN-INVENTORY</name>
  <files>design/Crito.pen; .planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.3 (PAGE-11 ACTIVE: hide raster via enabled:false AFTER APPROVE only) + § 3.4 step 7 (post-APPROVE mutation step)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-126 (PEN-INVENTORY DzqTm status_counts update post-APPROVE)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` Phase 27 cl8tt PAGE-11 ACTIVE precedent (first production use)
    - Task 8 user decision: APPROVE / REVISE / GAP
  </read_first>
  <action>
    **PRECONDITION: User APPROVED at Task 8. If REVISE: loop back to relevant tasks (do not fire DzqTm Update). If GAP: record OPEN-28-NN and either fire DzqTm Update (if APPROVE-with-GAP) or defer (if GAP-without-APPROVE).**

    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__batch_design` Update DzqTm raster (PAGE-11 ACTIVE per § 3.3):
    - `Update("DzqTm", { enabled: false })` — hide the original Crito flat raster. SECOND production use of PAGE-11 ACTIVE in v2.0 (first was cl8tt at Phase 27).
    - **CRITICAL (Pitfall 4 + T-28-03):** This Update fires ONLY because Task 8 user APPROVE was recorded. NEVER call this Update before APPROVE.

    Step 3 — Verify via `mcp__pencil__batch_get({ nodeIds: ['DzqTm'], readDepth: 0 })`:
    - `enabled: false` confirmed
    - Frame structure otherwise preserved (DzqTm not deleted, just hidden — structural archive persists per § 3.3)

    Step 4 — Update `.planning/research/PEN-INVENTORY.md`:

    **Update DzqTm row in Frames Inventory:**
    - Status: `IN-SCOPE` → `reconstructed-PHASE-28`
    - status_counts: `flat:1` → `hidden:1`
    - Notes: "Plan 28-01 — reconstructed as editable Blog frame (`<blogPageId>`) with 6-Card grid + TagFilter + page-intro; DzqTm raster hidden via enabled:false per PAGE-11 ACTIVE (CALIBRATION-PROTOCOL § 3.3) post-APPROVE at calibration gate. Structural archive persists at canvas (DzqTm.x, DzqTm.y). Source image-import-NN.jpg index: NN (from Task 0 probe)."

    **Add Blog row to Frames Inventory:**
    - Frame: Blog (`<blogPageId>`)
    - Scope: IN-SCOPE
    - joel_page_map: /blog
    - Status: reconstructed-PHASE-28
    - Notes: "Blog index reconstruction per D-122 Plan 28-01 + CALIBRATION-PROTOCOL § 3.4. 5 content blocks: Header + page-intro + TagFilter + 6-Card grid + Footer. First concrete Compound/Card consumer per OPEN-25-07 partial-resolution. First Section/TagFilter consumer per D-115."

    **Update OPEN-23-05:**
    - Add Plan 28-01 partial-resolution note: "07_Blog (DzqTm = image-import-NN.jpg) reconstructed as editable Blog frame. Remaining flat-raster frames (`08_Blog Details`, `06_Service Details`, `05_Service`, `04_About`) continue. Phase 28 Plan 28-02 will reconstruct 08_Blog Details."

    **Update OPEN-25-07:**
    - Status: "consumer-ready" → "partially-resolved" (6 Card instances at Plan 28-01 close; full resolution at Plan 28-03 close after all 15 instances complete)

    **Add Variant Evidence (Phase 28) rows for Plan 28-01:**
    - Blog page frame structural composition (5 children + width 1440 + vertical layout)
    - 6-Card grid layout (3-col × 2-row + gap 24)
    - Card descendants override pattern (title + metadata + footer-actions Badge/Outline + image default + sibling note)
    - TagFilter instance default-position (pill-1 active "All Posts")
    - Page-intro literal-to-token bindings (heading-1 + prose-paragraph)

    **Add OPEN-28-NN rows** (if Task 8 user-decision raised any GAP flags).

    Step 5 — Plan-close sweep:
    - `mcp__pencil__get_variables({})` — assert 107 user-facing tokens (no drift).
    - `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` — expect `"No layout problems."` at document root.
    - `mcp__pencil__batch_get({ nodeIds: ['<blogPageId>', 'DzqTm', '<tagFilterId>', '<relatedPostsId>', '<metadataCaptionId>', '<badgeOutlineId>'], readDepth: 1 })` — final intactness check.

    Step 6 — Write `28-01-SUMMARY.md` per template (objective + outcome + 5 sections fidelity labels + DzqTm image-import-NN.jpg index probed + Card consumer-resolution + OPEN-23-05 partial + OPEN-25-07 status update + Tier-1/Tier-2 stale-cache outcome + plan-close sweep + acceptance criteria match + carry-forward IDs for Plan 28-02 + 28-03).
  </action>
  <verify>
    `batch_get(['DzqTm'], readDepth: 0)` returns `enabled: false`. PEN-INVENTORY DzqTm row + Blog row + OPEN-23-05 + OPEN-25-07 updated. Final sweep all PASS. 28-01-SUMMARY.md written.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - User APPROVE recorded at Task 8 (precondition — Task 9 does NOT fire on REVISE).
    - `batch_design Update("DzqTm", { enabled: false })` completed without error.
    - `batch_get(['DzqTm'], readDepth: 0)` confirms `enabled: false`; DzqTm structure otherwise preserved (NOT deleted).
    - PEN-INVENTORY DzqTm row: status updated, status_counts updated, Plan 28-01 notes added.
    - PEN-INVENTORY Blog row added in Frames Inventory.
    - OPEN-23-05: Plan 28-01 partial-resolution note added.
    - OPEN-25-07: status updated to "partially-resolved".
    - Variant Evidence (Phase 28) rows added for Plan 28-01 deliverables.
    - OPEN-28-NN rows added (if any GAP flags raised at Task 8).
    - Plan-close sweep all PASS (token surface 107 + snapshot_layout clean + batch_get intactness).
    - 28-01-SUMMARY.md written.
  </acceptance_criteria>
  <done>
    Plan 28-01 closes. DzqTm raster hidden post-APPROVE (PAGE-11 ACTIVE 2nd production use). PEN-INVENTORY extended. Plans 28-02 + 28-03 unblocked; carry-forward `<blogPageId>` becomes Plan 28-02 FindEmptySpace anchor.
  </done>
</task>

</tasks>

<verification>
- DzqTm raster image-import-NN.jpg probed at Task 0; index recorded in SUMMARY (highest-priority deliverable per Pitfall 3).
- `Blog` page frame created at document root, page-frame row y ≈ -4111, width 1440, vertical layout.
- 5 vertical children: Section/Header + page-intro + Section/TagFilter + 6-Card grid + Section/Footer.
- 6 Card instances with full descendants overrides (title + metadata-caption + 2-3 Badge/Outline tag pills + default image placeholder).
- TagFilter uses Plan 28-00 defaults (pill-1 "All Posts" active; no descendants override).
- Token surface unchanged at 107 user-facing.
- User APPROVED at Task 8 calibration gate (single AskUserQuestion per § 3.4 step 5).
- DzqTm hidden via enabled:false ONLY AFTER APPROVE (Task 9 post-precondition).
- PEN-INVENTORY extended: DzqTm status update + Blog row + Variant Evidence Phase 28 rows + OPEN-23-05 partial + OPEN-25-07 partially-resolved + OPEN-28-NN (if any).
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `"No layout problems."` at document root.
- 28-01-SUMMARY.md written.
</verification>

<success_criteria>
- 07_Blog flat raster (DzqTm) reconstructed as editable Blog frame; OPEN-23-05 partially resolved (Plan 28-01).
- First concrete Compound/Card consumer per OPEN-25-07 (6 instances; OPEN status → partially-resolved).
- First Section/TagFilter consumer per D-115 broad-scoping.
- PAGE-11 ACTIVE 2nd production use successful (DzqTm hidden post-APPROVE).
- Per-section fidelity labels recorded + user APPROVED.
- Carry-forward `<blogPageId>` enables Plan 28-02 FindEmptySpace anchor.
</success_criteria>

<output>
After completion, create `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md` per `./.claude/get-shit-done/templates/summary.md` with:
- DzqTm image-import-NN.jpg index probed (NN value)
- `<blogPageId>` + canvas coords
- 5 content block IDs (Header ref + `<pageIntroId>` + `<tagFilterRefId>` + `<blogGridId>` + Footer ref)
- 6 Card instance IDs (Cards 1-6) + Badge/Outline tag pill IDs (~15 total)
- Per-section fidelity labels (5: page-intro EXACT, TagFilter APPROXIMATE, 6-Card grid APPROXIMATE, Header EXACT, Footer EXACT) + user APPROVE recorded
- Tier-1 / Tier-2 stale-cache outcome per section
- PAGE-11 ACTIVE outcome (DzqTm enabled:false post-APPROVE; 2nd production use in v2.0)
- PEN-INVENTORY changes (DzqTm row update + Blog row added + OPEN-23-05 partial + OPEN-25-07 partially-resolved + Variant Evidence Phase 28 rows + OPEN-28-NN if any)
- Plan-close sweep results
- Token surface (107 user-facing — no drift)
- Carry-forward IDs for Plan 28-02 (`<blogPageId>` as FindEmptySpace anchor)
</output>
</content>
</invoke>