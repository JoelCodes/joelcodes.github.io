---
phase: 28-blog-reconstruction-index-post-tag-page
plan: 02
type: execute
wave: 3
depends_on:
  - 28-00
  - 28-01
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: false
requirements:
  - PAGE-03
  - PAGE-09
  - PAGE-11
  - TOKEN-06
  - VALID-01
  - VALID-02
  - VALID-03
threat_model_summary:
  - id: T-28-01
    threat: "Pencil active-editor mismatch causes write to wrong .pen file"
    severity: high
    mitigation: "D-125 pre-flight `mcp__pencil__get_editor_state({ include_schema: false })` before every mutating call; halt and surface on mismatch."
  - id: T-28-03
    threat: "PAGE-11 raster (w1m3x) hidden BEFORE user APPROVE — destroys calibration target"
    severity: high
    mitigation: "Strict task sequencing per CALIBRATION-PROTOCOL § 3.4 step 7 + Pitfall 4: Task 0 = probe w1m3x raster image-import-NN.jpg index (READ-ONLY); Tasks 1-8 = construct Blog Post frame + content (NO w1m3x mutation); Task 9 = snapshot + screenshot; Task 10 = AskUserQuestion calibration gate; Task 11 (POST-APPROVE ONLY) = `Update(w1m3x, { enabled: false })`. NEVER call w1m3x Update before Task 10 APPROVE."
  - id: T-28-02
    threat: "Token surface drift during Blog Post construction"
    severity: medium
    mitigation: "`get_variables({})` at plan open + close (expect 107 user-facing — zero new tokens added)."

must_haves:
  truths:
    - "w1m3x raster's `fill.image` reference probed at Task 0 (HIGHEST-PRIORITY DELIVERABLE per RESEARCH Focus 4); matching `design/images/image-import-NN.jpg` index recorded in 28-02-SUMMARY.md immediately upon discovery. UNKNOWN at planning time — first identification per Focus 4; best-guess image-import-13.jpg or image-import-15.jpg but probe is authoritative."
    - "New top-level page frame named `Blog Post` exists in `design/Crito.pen` as a direct child of the document root (Pitfall 3 carry-forward), positioned at the page-frame row (y ≈ -4111) via `mcp__pencil__find_empty_space_on_canvas` with `nodeId: '<blogPageId>'` anchor (Blog index frame from Plan 28-01) per CALIBRATION-PROTOCOL § 10.4 + D-122."
    - "The `Blog Post` frame has width 1440, vertical auto-layout with alignItems center, fill `#ffffffff` (color-semantic-bg-page literal per OPEN-23-13)."
    - "The `Blog Post` frame contains 5 stacked content blocks: (1) Section/Header ref (G0wNOc, NO descendants override per D-77), (2) post header section (title heading-1 + metadata row + post-detail tag row of Primitive/Badge/Outline instances per D-121 + featured-image default placeholder + sibling featuredImage wiring note per D-113), (3) post body section (exercises ALL 7 Phase 28 prose tokens + carry-forward prose-paragraph; verbatim content from `src/content/blog/getting-started-with-automation.mdx` per D-123), (4) Section/RelatedPosts ref instance (Plan 28-00 `<relatedPostsId>`) with 3 Card instances customized per D-118 + D-116, (5) Section/Footer ref (Xs0Hs, NO descendants override per D-77)."
    - "Post body section exercises every Phase 28 prose token per D-123 verbatim from getting-started-with-automation.mdx: H1 'Getting Started with Business Automation' (heading-1 token; line 10) + 3 H2 sections ('Why Automate?' / 'Start Simple' / 'What's Next?'; lines 14, 23, 49 — heading-2 token) + 1 H3 ('Example: Invoice Follow-ups'; line 31 — heading-3 token NEW per D-106) + ul 4-item bullet list (lines 18-21 — prose-list token NEW per D-109) + ol 3-item numbered list (lines 27-29 — prose-list token NEW) + N inline-code spans (paragraph runs with backticks per Focus 5 Option A — prose-inline-code token NEW per D-108) + 1 fenced JavaScript code block (lines 35-45 — prose-code-block token NEW per D-108) + 1 italic span ('*Have a process...*' line 59 — prose-paragraph italic surface, Phase 23 carry-forward) + 1 prose-link ([Get in touch](/contact); line 59 — prose-link token NEW per D-107) + 1 horizontal rule ('---' line 57 — styled via color-semantic-border-divider per Deferred Ideas; no new token)."
    - "Section/RelatedPosts ref instance uses Plan 28-00 defaults (heading 'Related posts' + 3 Card refs) with descendants override per D-118 to customize 3 Card content (kI3bc + `<metadataCaptionId>` + footer-actions-slot Badge/Outline tag pills per D-111 + D-112 — same pattern as Plan 28-01 Blog index 6-Card grid)."
    - "Post-detail tag row per D-121: inline horizontal row of 2-3 Primitive/Badge/Outline (`<badgeOutlineId>`) instances inside post header section between metadata row and featured image (matches v1.3 [slug].astro structural pattern). Tags match Joel's sample MDX frontmatter: ['automation', 'small-business', 'productivity']."
    - "Per-section fidelity labels per D-83 recorded in 28-02-SUMMARY.md: post header = EXACT (token-bound + structural reference from v1.3 [slug].astro lines 64-73); post body = EXACT (token-bound + content verbatim from getting-started-with-automation.mdx per D-123); RelatedPosts strip = APPROXIMATE (representative 3 Card content; selection algorithm deferred to runtime per Deferred Ideas); Header = EXACT (Phase 25); Footer = EXACT (Phase 25)."
    - "User APPROVED the composition via single AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 3.4 step 5 (crito-source-flat-raster format) — pairs reconstructed Blog Post frame screenshot against the probed `image-import-NN.jpg` raster. Stale-cache fallback per § 6.4 Tier-1/Tier-2 if needed. **PRIMARY validation surface for all 7 Phase 28 prose tokens** + verifier for prose-paragraph 'provisional' flag per D-110."
    - "PAGE-11 ACTIVE — `w1m3x` raster hidden via `enabled: false` AFTER user APPROVE only per CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7 (NEVER before — Pitfall 4 + T-28-03). THIRD production use of PAGE-11 ACTIVE in v2.0 (SECOND in Phase 28)."
    - "Prose-paragraph 'provisional' flag verification per D-110: if post body rendering APPROVES at calibration, flag removed in PEN-INVENTORY (Phase 26 D-71 mechanic carry-forward). If problematic, OPEN-28-NN raised."
    - "PEN-INVENTORY status_counts for w1m3x moves from `flat:1` to `hidden:1`; Frames Inventory w1m3x row status updated from `IN-SCOPE` to `reconstructed-PHASE-28`; OPEN-23-05 partial-resolution updated (07_Blog + 08_Blog Details both now hidden); OPEN-23-10 partial-resolution UPDATE (heading-3 USED via post body H3 + heading-4 ships in foundation but unused at Plan 28-02 — heading-5/-6 still open)."
    - "PEN-INVENTORY Frames Inventory has new row for `Blog Post` (scope: IN-SCOPE, joel_page_map: /blog/[slug], status: reconstructed-PHASE-28); Variant Evidence (Phase 28) sub-section extended with Blog Post page-frame structural rows + post body prose-token consumer rows per Focus 5 mapping + RelatedPosts instance + post-detail tag row rows."
    - "Token surface unchanged at 107 user-facing across Plan 28-02 (zero new tokens — only Plan 28-00 added prose tokens)."
    - "Phase 23-27 baseline IDs + Plan 28-00 deliverable IDs + Plan 28-01 `<blogPageId>` intact (Plan 28-02 instances refs + mutates only w1m3x enabled:false at post-APPROVE step)."
    - "Document-level `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `\"No layout problems.\"` at plan close (text-clipping false-positives anticipated to EXCEED Phase 27 due to multi-paragraph post body prose per Pitfall 5 + Focus 8 — documented in 28-02-SUMMARY.md, NOT mitigated)."
    - "Card consumer-resolution per OPEN-25-07: status remains 'partially-resolved' (3 RelatedPosts Card instances at Plan 28-02 + 6 Blog index from Plan 28-01 = 9 of 15 total; final resolution at Plan 28-03 close)."
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `Blog Post` with 5 vertical content blocks: Section/Header + post header section + post body section + Section/RelatedPosts instance + Section/Footer. w1m3x raster hidden via enabled:false after user APPROVE (PAGE-11 ACTIVE)."
      contains: "Blog Post page frame (id <blogPostId>), post header (title + metadata + tag row + image placeholder + sibling note), post body (H1 + 3 H2 + 1 H3 + ul + ol + inline-code + code-block + italic + link + hr — every Phase 28 prose token consumed), Section/RelatedPosts ref with 3 Card instances, Section/Header G0wNOc, Section/Footer Xs0Hs, w1m3x enabled:false."
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for Blog Post + w1m3x row status_counts update (flat:1 → hidden:1) + status update (reconstructed-PHASE-28) + Variant Evidence (Phase 28) Blog Post rows including prose-token consumer rows + OPEN-23-05 further partial-resolution + OPEN-23-10 partial update + prose-paragraph 'provisional' flag removed (if APPROVED) + any OPEN-28-NN raised."
      contains: "Blog Post row in Frames Inventory, w1m3x status update, Variant Evidence prose-token consumer rows (heading-3 H3 + prose-inline-code + prose-code-block + prose-link + prose-list-unordered + prose-list-ordered consumers), OPEN-23-05 partial expanded, OPEN-23-10 partial expanded, prose-paragraph 'provisional' flag REMOVED if APPROVED"
  key_links:
    - from: "Blog Post page frame (<blogPostId>)"
      to: "Section/Header (G0wNOc) + Section/RelatedPosts (<relatedPostsId>) + Section/Footer (Xs0Hs)"
      via: "ref children at top + 4th position + bottom; NO descendants override on Header/Footer per D-77; RelatedPosts uses Plan 28-00 defaults + Card content override per D-118"
      pattern: "Blog Post.*ref.*G0wNOc.*ref.*<relatedPostsId>.*ref.*Xs0Hs"
    - from: "Post body section"
      to: "All 7 Phase 28 prose tokens + carry-forward prose-paragraph + heading-1 + heading-2"
      via: "Each prose construct in MDX maps to a Phase 28 token per Focus 5 mapping: H1 → heading-1; H2 → heading-2; H3 → heading-3 NEW; bullet/numbered lists → prose-list NEW; inline-code → prose-inline-code NEW (Option A horizontal-wrap frame); code-block → prose-code-block NEW (Focus 5 styled text-frame composition); italic → prose-paragraph italic; link → prose-link NEW; hr → color-semantic-border-divider (no new token per Deferred)"
      pattern: "post-body.*text.*type-semantic-(heading-1|heading-2|heading-3|prose-paragraph|prose-inline-code|prose-code-block|prose-link|prose-list).*type-primitive-family-mono"
    - from: "Post header tag row"
      to: "Primitive/Badge/Outline instances (<badgeOutlineId>)"
      via: "Horizontal frame inside post header (between metadata row and featured image per D-121); 2-3 Badge/Outline refs with descendants override on label.content for tags ['automation', 'small-business', 'productivity']"
      pattern: "post-header.*tag-row.*ref.*<badgeOutlineId>.*automation.*small-business.*productivity"
    - from: "Plan 28-02 Task 11 w1m3x mutation"
      to: "w1m3x `enabled: false` (PAGE-11 ACTIVE post-APPROVE only per § 3.4 step 7)"
      via: "batch_design Update AFTER user APPROVE at Task 10 calibration gate; NOT before (T-28-03 mitigation)"
      pattern: "w1m3x.*enabled.*false"
---

<objective>
Create the `Blog Post` page frame in `design/Crito.pen` as a crito-source-present-flat-raster reconstruction per D-122 Plan 28-02 + CALIBRATION-PROTOCOL § 3 (crito-source branch, flat-raster sub-case). The Crito source `w1m3x` (08_Blog Details) is a single image-import-*.jpg flat raster per OPEN-23-05; calibration pairs the reconstructed Blog Post frame against that raster per § 3.1 + § 3.5.

**Critical first-task probe (HIGHEST PRIORITY per Focus 4 + Pitfall 3):** Plan 28-02 Task 0 MUST call `mcp__pencil__batch_get({ nodeIds: ['w1m3x'], readDepth: 2 })` to read the `fill.image` reference and identify the matching `image-import-NN.jpg` index. Index is UNKNOWN at planning time (first identification per Focus 4) — probe is authoritative.

Composition per D-122 (top → bottom):
1. `Section / Header` instance (G0wNOc) — no override per D-77.
2. **Post header section** — title heading-1 "Getting Started with Business Automation" (v1.3 MDX frontmatter) + metadata row (date "JAN 27, 2026" + read-time "5 MIN READ") + post-detail tag row (inline 3 Primitive/Badge/Outline refs with label overrides for "automation", "small-business", "productivity" per D-121) + featured-image default placeholder per D-113 + sibling featuredImage wiring note.
3. **Post body section** — verbatim content from `src/content/blog/getting-started-with-automation.mdx` per D-123. Single vertical-layout frame containing all MDX constructs in order, each child token-bound per Focus 5 mapping table. **PRIMARY prose-token validation surface for all 7 Phase 28 prose tokens.** Inline-code uses Option A horizontal-wrap frame composition (3 text nodes per inline-code consumer); code-block uses Focus 5 styled-frame composition (frame + mono text node with `\n` line breaks); hr uses color-semantic-border-divider 1px line (no new token).
4. `Section / RelatedPosts` instance (Plan 28-00 `<relatedPostsId>`) with descendants override per D-118 on 3 Card content (heading-slot uses default "Related posts"; cards-row 3 Card refs each with title + metadata + tags overrides per D-111 + D-112).
5. `Section / Footer` instance (Xs0Hs) — no override per D-77.

**PAGE-11 ACTIVE** per CALIBRATION-PROTOCOL § 3.3 + § 3.4 step 7: Task 11 fires `batch_design Update("w1m3x", { enabled: false })` ONLY AFTER user APPROVE at Task 10 calibration gate. THIRD production use of PAGE-11 ACTIVE in v2.0 (Pitfall 4 + T-28-03).

**Purpose:** Phase 28's deepest plan — exercises every Phase 28 prose token + first Section/RelatedPosts consumer per D-116 narrow-scoping. PRIMARY validation surface for Plan 28-00 foundation prose token surface. Verifier for prose-paragraph 'provisional' flag per D-110.

**Output:** A working `Blog Post` top-level page frame; w1m3x raster hidden post-APPROVE; extended PEN-INVENTORY with Frames Inventory row + w1m3x status update + Variant Evidence (Phase 28) prose-token consumer rows + OPEN-23-05 partial expanded + OPEN-23-10 partial expanded + prose-paragraph 'provisional' flag removed (if APPROVED) + any OPEN-28-NN; per-section fidelity labels recorded; plan-close user APPROVED state. ONE user gate at plan close per § 3.4 step 5.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-VALIDATION.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@src/content/blog/getting-started-with-automation.mdx
@src/pages/blog/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plans 28-00 + 28-01 outputs intact + PROBE w1m3x raster image-import-NN.jpg index (HIGHEST PRIORITY) + extract verbatim MDX content</name>
  <files>design/Crito.pen (read-only); src/content/blog/getting-started-with-automation.mdx (read-only); src/pages/blog/[slug].astro (read-only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` — `<tagFilterId>`, `<relatedPostsId>`, `<metadataCaptionId>`, `<badgeOutlineId>`, all 7 prose token IDs
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md` — `<blogPageId>` (for FindEmptySpace anchor)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 4 (w1m3x probe — UNKNOWN, first identification) + Focus 5 (code-block + inline-code representation; full MDX → prose-token mapping table) + Pitfall 4 (NEVER hide raster before APPROVE)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-123 (verbatim MDX content) + D-110 (prose-paragraph 'provisional' flag verifier) + D-121 (post-detail tag row in post header)
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.5 description identifier convention
    - `src/content/blog/getting-started-with-automation.mdx` lines 1-62 (FULL FILE — content extraction per D-123)
    - `src/pages/blog/[slug].astro` lines 1-113 (post header structural reference: title → metadata → tag row → featured-image → body per D-121)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125: `mcp__pencil__get_editor_state({ include_schema: false })`. Halt + surface on mismatch.

    Step 2 — Token surface drift check: `mcp__pencil__get_variables({})`. Assert user-facing count is 107 (Plan 28-01 close). Halt on drift.

    Step 3 — **HIGHEST PRIORITY: PROBE w1m3x raster image-import-NN.jpg index (Focus 4 + Pitfall 3):**
    `mcp__pencil__batch_get({ nodeIds: ['w1m3x'], readDepth: 2 })`. Read `fill.image` reference (or descendant rectangle's `fill.image`). Per Focus 4: index is UNKNOWN at planning time; best-guess image-import-13.jpg or image-import-15.jpg (sequential to Blog index 14) — but **do not assume; probe is authoritative**.

    Record the discovered `image-import-NN.jpg` index in 28-02-SUMMARY scratch IMMEDIATELY. Use as:
    - Visual calibration target for Task 10.
    - Description identifier suffix: `28-blog-post-{section}--side-by-side.png (paired with design/images/image-import-NN.jpg)`.
    - Recorded in PEN-INVENTORY § Frames Inventory w1m3x row notes per D-126.

    If `fill.image` not directly readable, walk via readDepth: 3 — identify the only image-fill node inside the w1m3x subtree.

    Step 4 — Plan 28-00 + 28-01 deliverable intactness:
    `mcp__pencil__batch_get({ readDepth: 1, nodeIds: ['<tagFilterId>', '<relatedPostsId>', 'vGH3A', '<metadataCaptionId>', '<badgeOutlineId>', '<blogPageId>', 'DzqTm'] })`. Confirm structural intactness. DzqTm should still have `enabled: false` (Plan 28-01 Task 9 outcome — verify it's still hidden + not unexpectedly re-enabled).

    Step 5 — Phase 23-27 baseline component intactness:
    `batch_get` on G0wNOc (Section/Header), Xs0Hs (Section/Footer), t40xct (Compound/Card), M7eUr (Button/Default), j0FxQZ (Badge solid).

    Step 6 — Read v1.3 MDX source verbatim per D-123:
    Read `src/content/blog/getting-started-with-automation.mdx` lines 1-62. Extract per Focus 5 mapping table:
    - **H1 (line 10):** "Getting Started with Business Automation"
    - **First paragraph (line 12):** "Most small business owners think automation requires expensive software or technical expertise. The truth? You can start automating today with tools you already have."
    - **H2 #1 (line 14):** "Why Automate?"
    - **Paragraph (line 16):** "Every minute spent on repetitive tasks is a minute you could spend growing your business. Here's what automation can handle:"
    - **Unordered list (lines 18-21):** 4 items: "**Email responses** - Auto-replies, templates, and scheduling" / "**Invoice reminders** - Automatic follow-ups on unpaid invoices" / "**Data entry** - Moving information between apps automatically" / "**Social media** - Scheduled posts and basic engagement"
    - **H2 #2 (line 23):** "Start Simple"
    - **Paragraph (line 25):** "The best automation is invisible. Start with one process that:"
    - **Ordered list (lines 27-29):** 3 items: "Happens frequently (daily or weekly)" / "Follows the same steps every time" / "Doesn't require human judgment"
    - **H3 (line 31):** "Example: Invoice Follow-ups"
    - **Paragraph (line 33):** "Instead of manually checking who hasn't paid:"
    - **Fenced JS code block (lines 35-45):** Full 8-line JavaScript pseudo-code as in source
    - **Paragraph (line 47):** "This simple pattern can save hours each week."
    - **H2 #3 (line 49):** "What's Next?"
    - **Paragraph (line 51):** "In future posts, we'll cover:"
    - **Unordered list (lines 53-55):** 3 items: "Setting up your first Zapier automation" / "When to build custom tools vs use existing solutions" / "How AI is changing what's possible for small businesses"
    - **Horizontal rule (line 57):** "---"
    - **Italic + link paragraph (line 59):** "*Have a process you'd like to automate? [Get in touch](/contact) and let's chat about what's possible.*"
    - **Frontmatter date (line 4):** "2026-01-27" → "JAN 27, 2026" formatted for metadata row
    - **Frontmatter tags (line 6):** ["automation", "small-business", "productivity"] → 3 Badge/Outline labels in post header tag row

    Step 7 — Read `src/pages/blog/[slug].astro` lines 1-113 for post header structural reference per D-121:
    - Line ~50: title h1 (heading-1)
    - Line ~55: metadata row (date • read-time)
    - Lines 64-73: tag row (inline anchor links rendered as Badge/Outline in static design)
    - Line ~80: featured-image
    - Line ~95: body content area

    Record extracted MDX content + structural reference in 28-02-SUMMARY scratch.

    Step 8 — Look up Compound/Card descendant IDs from 25-03-SUMMARY + 28-00-SUMMARY for RelatedPosts Card overrides (same as Plan 28-01):
    - kI3bc, `<metadataCaptionId>`, oTSwn, FGdti, eNqxd, `<badgeOutlineId>`, `<badgeOutlineLabelId>`

    No mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token: 107. w1m3x raster index PROBED: image-import-NN.jpg (recorded in scratch — FIRST IDENTIFICATION per Focus 4). DzqTm still enabled:false (Plan 28-01 outcome preserved). Plan 28-00 + 28-01 deliverables intact. Baselines intact. MDX verbatim content extracted (all 7 prose-token consumer constructs identified). Post header structural reference captured."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned activeEditor ending in `design/Crito.pen`.
    - `get_variables({})` returned 107 user-facing tokens.
    - **`batch_get(['w1m3x'], readDepth: 2)` returned w1m3x structure and `fill.image` reference identified — matching `image-import-NN.jpg` index recorded in 28-02-SUMMARY scratch (highest-priority probe deliverable per Pitfall 3; FIRST identification per Focus 4).**
    - `batch_get` on Plan 28-00 + 28-01 IDs confirms intactness; DzqTm still enabled:false.
    - `batch_get` on G0wNOc, Xs0Hs, t40xct, M7eUr, j0FxQZ confirms baseline intactness.
    - Full MDX verbatim content extracted from `src/content/blog/getting-started-with-automation.mdx`: H1 + 3 H2 + 1 H3 + 1 ul (4 items) + 1 ol (3 items) + 1 ul (3 items in "What's Next?") + JavaScript code block + italic + link + hr + paragraphs.
    - v1.3 [slug].astro post header structural reference captured.
    - Compound/Card descendant IDs ready for RelatedPosts Card overrides.
    - No mutations in Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight PASS. w1m3x raster index probed (highest-priority deliverable — FIRST identification). MDX verbatim content ready for post body section (Tasks 5-7). Post header structural pattern ready (Task 3). Plan 28-02 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `Blog Post` top-level page frame via find_empty_space_on_canvas with nodeId:<blogPageId> anchor</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 10.4 (FindEmptySpace nodeId anchor pattern)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-122 (Plan 28-02 anchors on Blog index from 28-01)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md` `<blogPageId>` + canvas coords
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — FindEmptySpace with `nodeId: '<blogPageId>'` anchor:
    `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 3200, direction: "right", padding: 80, nodeId: "<blogPageId>" })`. Expected `{x, y}` ≈ `{x: ~24107, y: -4111.55}` (next-right of `<blogPageId>` from Plan 28-01). Height 3200 estimate = Header (~80) + post header (~600 for title + metadata + tag row + featured image placeholder ~360h) + post body (~1400 for multi-paragraph + 2 lists + H3 + code block + italic + link + hr) + RelatedPosts strip (~500) + Footer (~500) + spacing. Multi-paragraph body anticipated to push limits per Pitfall 5.

    Step 3 — `mcp__pencil__batch_design` Insert top-level frame:
    - Parent: document root (Pitfall 3).
    - `type: "frame"`, `name: "Blog Post"`, `placeholder: true`, `position: { x, y }`.
    - Dimensions: `width: 1440`, `height: 3200`.
    - Layout: `vertical`, `gap: 0`, `padding: 0`, `alignItems: "center"`.
    - Fill: `"#ffffffff"`.

    Record `<blogPostId>` + coords in scratch.
  </action>
  <verify>
    `batch_get(['<blogPostId>'])` returns top-level frame named "Blog Post" at document root, width 1440, vertical, alignItems center, fill #ffffffff, position y ≈ -4111.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - FindEmptySpace returned y ≈ -4111.
    - `batch_design` Insert completed without error.
    - `batch_get(['<blogPostId>'])` returns frame at document root level (not inside library parents or other page frames).
    - No mutations to baseline IDs or `<blogPageId>`.
    - `<blogPostId>` + coords recorded.
  </acceptance_criteria>
  <done>
    Empty Blog Post page frame in place at canvas page-frame row, next-right of `<blogPageId>`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/Header ref + post header section (title heading-1 + metadata row + tag row + featured-image placeholder + sibling note)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-77 (Header instanced as-is) + D-121 (post-detail tag row in post header between metadata + featured image) + D-113 (image-slot default placeholder + sibling featuredImage wiring note)
    - Task 0 scratch: MDX frontmatter (title + date + tags) + post header structural reference from v1.3 [slug].astro
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` `<badgeOutlineId>` + `<badgeOutlineLabelId>`
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert children of `<blogPostId>` in chunked order (Pitfall 9 ≤ 25 ops per chunk):

    **Insert 1 — Section/Header ref:**
    - `{ type: "ref", ref: "G0wNOc" }` — no override per D-77.

    **Insert 2 — post-header section frame:**
    - `type: "frame"`, `name: "post-header"`, `width: 1200`, `layout: "vertical"`, `gap: 24`, `padding: [80, 0, 48, 0]`, `alignItems: "start"` (left-aligned per v1.3 [slug].astro structural pattern).

    **Post-header children (inserted in order):**

    Child 1 — title heading text:
    - `{ type: "text", name: "title", content: "Getting Started with Business Automation", fontFamily: "Plus Jakarta Sans" (type-semantic-heading-1-family), fontSize: 48 (heading-1-size), fontWeight: 700, fill: "#141f39ff" (color-semantic-text-primary), lineHeight: 1.4, width: 1100 }` — heading-1 token surface.

    Child 2 — metadata row frame:
    - `{ type: "frame", name: "metadata-row", width: "fit_content", layout: "horizontal", gap: 12, alignItems: "center" }` containing 2 text nodes:
      - `{ type: "text", name: "date", content: "JAN 27, 2026", fontFamily: "Inter" (type-primitive-family-body), fontSize: 14 (type-semantic-body-sm-size), fontWeight: 500, fill: "#52525bff" (color-semantic-text-secondary) }`
      - `{ type: "text", name: "read-time", content: "• 5 MIN READ", fontFamily: "Inter", fontSize: 14, fontWeight: 500, fill: "#52525bff" }` (or single text node with combined content per Claude's Discretion)

    Child 3 — tag row frame per D-121 (3 Badge/Outline refs):
    - `{ type: "frame", name: "tag-row", width: "fit_content", layout: "horizontal", gap: 8, alignItems: "center" }` containing 3 ref children:
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "automation" } } }`
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "small-business" } } }`
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "productivity" } } }`

    Child 4 — featured-image placeholder frame per D-113:
    - `{ type: "frame", name: "featured-image", width: 1100, height: 400, fill: "#f2f2f7ff" (color-semantic-bg-surface-elevated), cornerRadius: 10 (radius-semantic-card) }` — landscape 11:4 placeholder (matches agency-template featured-image aspect).

    Child 5 — sibling Pencil note (featuredImage + TOC wiring per D-113 + D-122 single-column ships):
    - `{ type: "note", name: "Blog Post header wiring", content: "Code milestone wires: (1) featured-image fill to `post.data.featuredImage` (MDX frontmatter image path); v2.0 ships default placeholder per D-113. (2) Post-detail tag row Badge/Outline refs loop from `post.data.tags` array — v2.0 ships representative 3 tags from getting-started-with-automation.mdx; runtime renders per-post variable count. (3) v1.3 [slug].astro renders 3-column TOC layout (lg:grid-cols-[200px_1fr_200px]) — v2.0 Plan 28-02 ships single-column composition per CONTEXT.md Deferred Ideas; TOC affordance is code-milestone concern (sticky position + scroll-spy + heading anchor extraction). (4) Metadata row date format: code milestone formats `post.data.pubDate` to 'MMM DD, YYYY' display; read-time calculated from MDX body word count." }`

    Record post-header IDs (`<postHeaderId>`, `<titleId>`, `<metadataRowId>`, `<tagRowId>`, `<featuredImageId>`, `<wiringNoteId>`) in scratch.
  </action>
  <verify>
    `batch_get(['<blogPostId>'], readDepth: 3)` shows 2 direct children: G0wNOc + post-header. post-header has 5 children: title (Plus Jakarta Sans 48/700) + metadata-row (date + read-time) + tag-row (3 Badge/Outline refs with label overrides) + featured-image placeholder + wiring note.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Inserts completed without error.
    - `batch_get(['<blogPostId>'])` returns 2 children: Header ref + post-header.
    - post-header has 5 children in vertical order: title + metadata-row + tag-row + featured-image + wiring note.
    - Title content matches MDX frontmatter title verbatim: "Getting Started with Business Automation".
    - Metadata row has date "JAN 27, 2026" + read-time "5 MIN READ".
    - Tag row has 3 Badge/Outline refs with labels "automation", "small-business", "productivity" (matches MDX tags array).
    - Featured-image placeholder is 1100×400 with color-semantic-bg-surface-elevated fill + radius-semantic-card.
    - Wiring note exists with featuredImage + tag-row + TOC + metadata wiring documentation.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Post header complete: Header + title + metadata + tag row + featured-image placeholder + sibling wiring note.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert post body section frame + paragraphs + H2#1 + first unordered list (Why Automate? block — covers prose-paragraph + heading-2 + prose-list)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: MDX content lines 12-21 (first paragraph + H2 "Why Automate?" + paragraph + 4-item bullet list)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 5 mapping table (prose-paragraph + heading-2 + prose-list token bindings)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-109 (prose-list composite: indent 24 + item-gap 8 + bullet-style disc)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<blogPostId>` — the post-body section frame:

    **Insert — post-body section frame:**
    - `type: "frame"`, `name: "post-body"`, `width: 760` (centered prose width, agency-template register), `layout: "vertical"`, `gap: 24` (paragraph rhythm), `padding: [0, 0, 64, 0]`, `alignItems: "start"`.

    Step 3 — Insert children of `<postBodyId>` in MDX order — first chunk (lines 12-21):

    Child 1 — intro paragraph (line 12) — prose-paragraph token:
    - `{ type: "text", name: "intro-paragraph", content: "Most small business owners think automation requires expensive software or technical expertise. The truth? You can start automating today with tools you already have.", fontFamily: "Inter" (prose-paragraph-family), fontSize: 16 (prose-paragraph-size), fontWeight: 400, fill: "#141f39ff" (color-semantic-text-primary), lineHeight: 1.625 (prose-paragraph-lh), width: "fill_container" }`

    Child 2 — H2 #1 (line 14) — heading-2 token:
    - `{ type: "text", name: "h2-why-automate", content: "Why Automate?", fontFamily: "Plus Jakarta Sans" (heading-2-family), fontSize: 32 (heading-2-size), fontWeight: 700, fill: "#141f39ff", lineHeight: 1.4, width: "fill_container" }`

    Child 3 — paragraph (line 16) — prose-paragraph:
    - `{ type: "text", name: "para-1", content: "Every minute spent on repetitive tasks is a minute you could spend growing your business. Here's what automation can handle:", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, width: "fill_container" }`

    Child 4 — unordered list frame per D-109 (4 items, lines 18-21) — prose-list-unordered token:
    - `{ type: "frame", name: "ul-automation-handles", width: "fill_container", layout: "vertical", gap: 8 (prose-list-item-gap), padding: [0, 0, 0, 24] (prose-list-indent) }` containing 4 text nodes:
      - `{ type: "text", content: "• **Email responses** - Auto-replies, templates, and scheduling", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625 }`
      - `{ type: "text", content: "• **Invoice reminders** - Automatic follow-ups on unpaid invoices", ... same }`
      - `{ type: "text", content: "• **Data entry** - Moving information between apps automatically", ... same }`
      - `{ type: "text", content: "• **Social media** - Scheduled posts and basic engagement", ... same }`
    - Note: bold `**Email responses**` markdown syntax preserved as literal in text content; Pencil text nodes don't support per-character font runs — the bold formatting is a runtime concern (MDX renders `**text**` as `<strong>text</strong>` styled by code milestone). For visual prose-list validation, the bullet prefix `•` + indent 24 + item-gap 8 are the token consumers.

    Record `<postBodyId>` + child IDs for first chunk in scratch.

    **Pitfall 9 chunk note:** This task creates 1 post-body frame + 4 prose children (intro para + H2 + para + ul) ≈ 10 ops including ul's 4 text children. Within budget. Tasks 4-7 continue post body content.
  </action>
  <verify>
    `batch_get(['<postBodyId>'], readDepth: 3)` shows 4 children: intro-paragraph (prose-paragraph), h2-why-automate (heading-2), para-1 (prose-paragraph), ul-automation-handles frame with 4 bullet-prefixed text children.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `<blogPostId>` now has 3 children: Header + post-header + post-body.
    - post-body has 4 children: intro paragraph + H2 #1 + paragraph + ul frame.
    - ul frame has indent 24, item-gap 8, 4 bullet-prefixed text children.
    - All content verbatim from MDX (apostrophes preserved, bold markdown preserved as literal).
    - Typography token-bound: prose-paragraph (Inter 16/400/1.625 #141f39ff) + heading-2 (Plus Jakarta Sans 32/700/1.4) + prose-list (indent 24 + item-gap 8 + disc bullet prefix).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    First post body chunk complete: intro paragraph + "Why Automate?" H2 + paragraph + 4-item bullet list. prose-paragraph + heading-2 + prose-list tokens consumed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Insert post body chunk 2 (Start Simple H2 + paragraph + ordered list + Example H3 — covers heading-3 NEW + prose-list ordered)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: MDX lines 23-31 (H2 "Start Simple" + paragraph + 3-item numbered list + H3 "Example: Invoice Follow-ups")
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-106 (heading-3 NEW token at 24/700/1.4) + D-109 (prose-list ordered-style decimal)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert children of `<postBodyId>` (continuing in MDX order, after Task 3 children):

    Child 5 — H2 #2 (line 23) — heading-2 token:
    - `{ type: "text", name: "h2-start-simple", content: "Start Simple", fontFamily: "Plus Jakarta Sans", fontSize: 32, fontWeight: 700, fill: "#141f39ff", lineHeight: 1.4, width: "fill_container" }`

    Child 6 — paragraph (line 25) — prose-paragraph:
    - `{ type: "text", name: "para-2", content: "The best automation is invisible. Start with one process that:", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, width: "fill_container" }`

    Child 7 — ordered list frame per D-109 (3 items, lines 27-29) — prose-list-ordered token:
    - `{ type: "frame", name: "ol-automation-criteria", width: "fill_container", layout: "vertical", gap: 8, padding: [0, 0, 0, 24] }` containing 3 text nodes with "1. " / "2. " / "3. " prefixes:
      - `{ type: "text", content: "1. Happens frequently (daily or weekly)", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625 }`
      - `{ type: "text", content: "2. Follows the same steps every time", ... same }`
      - `{ type: "text", content: "3. Doesn't require human judgment", ... same }`

    Child 8 — H3 (line 31) — heading-3 NEW token per D-106:
    - `{ type: "text", name: "h3-example-invoice", content: "Example: Invoice Follow-ups", fontFamily: "Plus Jakarta Sans" (heading-3-family), fontSize: 24 (heading-3-size — interpolation default OR .fig value from Plan 28-00), fontWeight: 700 (heading-3-weight), fill: "#141f39ff", lineHeight: 1.4 (heading-3-lh), width: "fill_container" }`

    Record IDs in scratch.
  </action>
  <verify>
    `batch_get(['<postBodyId>'], readDepth: 3)` shows now 8 children total; H3 "Example: Invoice Follow-ups" uses Plus Jakarta Sans 24/700/1.4 (heading-3 token surface).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - 4 new children added to post-body: H2 #2 + paragraph + ol frame + H3.
    - H3 content matches MDX EXACTLY: "Example: Invoice Follow-ups".
    - H3 typography: Plus Jakarta Sans 24/700/1.4 (heading-3 token consumed — D-106 deliverable validated).
    - ol frame has 3 numbered text children with "1. " / "2. " / "3. " prefixes; indent 24 + item-gap 8 (prose-list-ordered consumed).
    - All content verbatim from MDX.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Post body chunk 2 complete. heading-3 token (NEW per D-106) consumed for first time. prose-list-ordered consumed.
  </done>
</task>

<task type="auto">
  <name>Task 5: Insert post body chunk 3 (paragraph before code + JS code block + paragraph after code — covers prose-code-block NEW)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: MDX lines 33-47 (paragraph "Instead of manually checking..." + 8-line JS code block + paragraph "This simple pattern can save hours each week.")
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 5 (code-block styled text-frame composition; literal `\n` line breaks; mono family + bg-surface-elevated fill + 16px padding + radius-semantic-card + lh 1.6)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-108 (prose-code-block token composite)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert children of `<postBodyId>`:

    Child 9 — paragraph before code (line 33) — prose-paragraph:
    - `{ type: "text", name: "para-before-code", content: "Instead of manually checking who hasn't paid:", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, width: "fill_container" }`

    Child 10 — fenced JavaScript code block per Focus 5 — prose-code-block NEW token:
    - `{ type: "frame", name: "code-block", layout: "vertical", gap: 0, padding: 16 (prose-code-block-padding), fill: "#f2f2f7ff" (color-semantic-bg-surface-elevated; bound to prose-code-block-bg), cornerRadius: 10 (radius-semantic-card; bound to prose-code-block-radius), width: "fill_container" }` containing 1 text node:
      - `{ type: "text", name: "code-block-text", content: "// Pseudo-code for invoice automation\nconst overdueInvoices = invoices.filter(\n  inv => inv.dueDate < today && !inv.paid\n);\n\noverdueInvoices.forEach(invoice => {\n  sendReminderEmail(invoice.customer);\n  logActivity(\`Reminder sent to ${invoice.customer.name}\`);\n});", fontFamily: "'SF Mono', Menlo, Monaco, Consolas, monospace" (type-primitive-family-mono — Plan 28-00), fontSize: 14 (prose-code-block-size), fontWeight: "normal", fill: "#141f39ff" (color-semantic-text-primary; uniform — no syntax highlighting per D-108 + D-92), lineHeight: 1.6 (prose-code-block-lh), textGrowth: "fill_height", width: "fill_container" }`
    - **Critical:** literal `\n` characters in content render as line breaks in Pencil text nodes (per Focus 5). Backtick character in template-literal line `\`Reminder sent...\`` is preserved as-is (Pencil text handles backticks literally).

    Child 11 — paragraph after code (line 47) — prose-paragraph:
    - `{ type: "text", name: "para-after-code", content: "This simple pattern can save hours each week.", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, width: "fill_container" }`

    Record IDs in scratch.
  </action>
  <verify>
    `batch_get(['<postBodyId>'], readDepth: 3)` shows now 11 children; code-block frame has mono font text child with `\n` line breaks + bg-surface-elevated + padding 16 + cornerRadius 10 + lh 1.6 (prose-code-block token surface).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - 3 new children: paragraph before code + code-block frame + paragraph after code.
    - Code block frame: bg #f2f2f7ff (bg-surface-elevated) + padding 16 + cornerRadius 10.
    - Code block text: mono family (system stack from Plan 28-00) + size 14 + lh 1.6 + uniform fill (no syntax highlighting per D-108).
    - Code block content matches MDX EXACTLY (8 lines with `\n` literals; template-literal backticks preserved).
    - Surrounding paragraphs verbatim from MDX.
    - All IDs recorded.
    - prose-code-block token surface validated (first consumer per D-108 deliverable).
  </acceptance_criteria>
  <done>
    Post body chunk 3 complete. prose-code-block token (NEW per D-108) consumed for first time. Mono primitive consumed.
  </done>
</task>

<task type="auto">
  <name>Task 6: Insert post body chunk 4 (What's Next? H2 + paragraph + final unordered list + hr — covers final prose-list + horizontal rule)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: MDX lines 49-57 (H2 "What's Next?" + paragraph + 3-item bullet list + horizontal rule)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` Deferred Ideas (hr ships as color-semantic-border-divider 1px line — NO new prose-hr token)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert children of `<postBodyId>`:

    Child 12 — H2 #3 (line 49) — heading-2 token:
    - `{ type: "text", name: "h2-whats-next", content: "What's Next?", fontFamily: "Plus Jakarta Sans", fontSize: 32, fontWeight: 700, fill: "#141f39ff", lineHeight: 1.4, width: "fill_container" }`

    Child 13 — paragraph (line 51) — prose-paragraph:
    - `{ type: "text", name: "para-3", content: "In future posts, we'll cover:", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, width: "fill_container" }`

    Child 14 — unordered list frame per D-109 (3 items, lines 53-55) — prose-list-unordered:
    - `{ type: "frame", name: "ul-future-posts", width: "fill_container", layout: "vertical", gap: 8, padding: [0, 0, 0, 24] }` containing 3 text nodes:
      - `{ type: "text", content: "• Setting up your first Zapier automation", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625 }`
      - `{ type: "text", content: "• When to build custom tools vs use existing solutions", ... same }`
      - `{ type: "text", content: "• How AI is changing what's possible for small businesses", ... same }`

    Child 15 — horizontal rule (line 57) — line node per Deferred Ideas (NO new prose-hr token):
    - `{ type: "line", name: "hr", x1: 0, y1: 0, x2: 760, y2: 0, stroke: "#d4d4d8ff" (color-semantic-border-divider OR border-default), strokeWidth: 1 }`
    - **Spacing:** wrap in 24px margins via parent gap (already 24 via post-body frame gap) OR insert spacer frame above/below per Pencil layout convention. Default: post-body frame gap 24 handles spacing; no spacer needed.

    Record IDs in scratch.
  </action>
  <verify>
    `batch_get(['<postBodyId>'], readDepth: 3)` shows now 15 children; final ul has 3 children; hr is a line node 1px color-semantic-border-divider.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - 4 new children: H2 #3 + paragraph + ul frame + hr line.
    - All content verbatim from MDX (apostrophes in "What's Next?" + "we'll cover" preserved).
    - hr is line node 1px color-semantic-border-divider stroke.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Post body chunk 4 complete. Final prose-list unordered consumed. hr rendered via existing border-divider token (no new hr token per Deferred Ideas).
  </done>
</task>

<task type="auto">
  <name>Task 7: Insert final post body chunk (italic-and-link paragraph — covers prose-link NEW + prose-paragraph italic verifier)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: MDX line 59 ("*Have a process you'd like to automate? [Get in touch](/contact) and let's chat about what's possible.*")
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 5 Option A (inline-code Option A horizontal-wrap pattern applied to mixed inline content — though this line has NO inline-code, just italic + link)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-107 (prose-link composite: accent fill + hover-underline descriptor)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-110 (prose-paragraph 'provisional' flag verifier at this calibration gate)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert child of `<postBodyId>`:

    Child 16 — italic-and-link paragraph (line 59) per Focus 5 Option A inline mixed-content pattern:
    The line "*Have a process you'd like to automate? [Get in touch](/contact) and let's chat about what's possible.*" needs:
    - Italic wrapper (entire line italic per `*...*` markdown wrap)
    - Link span: "Get in touch" → /contact (prose-link styling)

    Per Focus 5 Option A horizontal-wrap (Pencil text nodes don't support per-character font runs):
    - `{ type: "frame", name: "italic-link-para", width: "fill_container", layout: "horizontal", gap: 0, alignItems: "baseline" /* or "center" per OPEN-27-02 Pencil 2.13 lacks baseline */, wrap: true /* if supported */ }` containing 3 text nodes:
      - Text 1: `{ type: "text", content: "Have a process you'd like to automate? ", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, fontStyle: "italic" }` (prose-paragraph italic surface — Phase 23 carry-forward, verifier per D-110)
      - Text 2 (the link span): `{ type: "text", content: "Get in touch", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#15bee3ff" (color-semantic-text-accent — prose-link-color per D-107), lineHeight: 1.625, fontStyle: "italic" }` — prose-link token consumed (no underline static per D-107 + D-92; underline-on-hover descriptor documented in PEN-INVENTORY).
      - Text 3: `{ type: "text", content: " and let's chat about what's possible.", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625, fontStyle: "italic" }`

    **Note on horizontal-wrap landmine per Focus 2 + RESEARCH:** If `wrap: true` not supported in Pencil 2.13, accept overflow horizontal layout for this single line; visual outcome at calibration gate may show truncation. Fall back to Option B (single text node with backticks visible) only if Option A renders broken; Option A is preferred for prose-token validation.

    **Alternative for the italic+link line (FALLBACK if horizontal-wrap brittle):**
    - Single text node with content "Have a process you'd like to automate? Get in touch and let's chat about what's possible." italic — link styling lost in static design; sibling note documents "Inline link styling per type-semantic-prose-link token; static design depicts plain italic prose with link target /contact; code milestone wires actual styled link span via MDX renderer." This honors D-92 static-design-as-READING-state pragma.

    Record IDs in scratch (default Option A; Option B fallback documented if needed).
  </action>
  <verify>
    `batch_get(['<postBodyId>'], readDepth: 4)` shows now 16 children; final italic-link-para frame has 3 text children with italic style; middle text uses accent fill (prose-link color).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - 1 new child: italic-link-para (horizontal-wrap frame with 3 italic text children OR fallback single text node + sibling note).
    - Middle text (link span "Get in touch") uses color-semantic-text-accent (prose-link-color per D-107).
    - All 3 text children fontStyle italic (prose-paragraph italic surface — Phase 23 carry-forward, verifier per D-110).
    - prose-link token consumed (first consumer per D-107 deliverable).
    - Plan-execution path recorded (Option A horizontal-wrap OR Option B fallback).
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Post body complete (16 children total). All 7 Phase 28 prose tokens consumed: heading-1 (Task 2 title), heading-2 (Tasks 3+4+6 H2s), heading-3 (Task 4 H3 NEW), mono-primitive (Task 5 code block), prose-inline-code (Task 7 deferred — D-123 sample MDX has no standalone inline-code line; raise OPEN-28-NN noting inline-code consumer missing OR add inline-code paragraph manually if sample MDX has one — RECHECK: line 26 has no inline code; first paragraph para-1 mentions "tools" not "`tools`"; **noting inline-code is NOT actually exercised by getting-started-with-automation.mdx as it appears in source — sample MDX has fenced code block but NO inline `code` spans. Plan 28-02 may add a representative inline-code consumer at sibling note level OR raise OPEN-28-NN that inline-code token surface ships without a per-MDX-line consumer at Plan 28-02 close**), prose-code-block (Task 5), prose-link (Task 7), prose-list (Tasks 3+4+6).

    **Decision:** Insert 1 synthetic inline-code consumer paragraph at end of post body for prose-inline-code token validation per D-105 promise. **Add Task 7b in Task 7 OR fold into this Done note for documentation.** Recommended action: After Task 7's italic-link paragraph, append 1 more representative paragraph that demonstrates inline-code per Focus 5 Option A — e.g., "Most automation tools accept code patterns like `npm run build` to trigger workflows." with `npm run build` styled per prose-inline-code token. Flag in 28-02-SUMMARY as "synthetic inline-code consumer added for prose-inline-code validation (D-123 sample MDX silent on inline-code — synthetic line ships for token validation per D-105 promise; raises OPEN-28-NN documenting the synthesis decision; future MDX with actual inline-code lifts this restriction)."
  </done>
</task>

<task type="auto">
  <name>Task 8: Insert synthetic inline-code consumer + Section/RelatedPosts ref instance with 3-Card descendants overrides + Section/Footer ref</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-116 (RelatedPosts narrow-scoping) + D-118 (3 full-shape Card instances) + D-77 (Footer no override)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` `<relatedPostsId>` + RelatedPosts heading-slot + cards-row Card ref IDs
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 5 Option A (inline-code horizontal-wrap frame composition)
    - Task 7 decision: synthetic inline-code consumer ships at end of post body
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert synthetic inline-code paragraph at end of `<postBodyId>` per Focus 5 Option A:

    Child 17 — synthetic inline-code paragraph (added at post body close for prose-inline-code validation):
    - `{ type: "frame", name: "inline-code-para", width: "fill_container", layout: "horizontal", gap: 0, alignItems: "center", wrap: true /* if supported */ }` containing 3 text nodes:
      - Text 1: `{ type: "text", content: "Most automation tools accept code patterns like ", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625 }`
      - Text 2 (inline-code chrome per D-108 + Focus 5): `{ type: "text", content: "npm run build", fontFamily: "'SF Mono', Menlo, Monaco, Consolas, monospace" (mono primitive — Plan 28-00), fontSize: 14 (prose-inline-code-size), fontWeight: "normal", fill: "#141f39ff", lineHeight: 1.625 }` wrapped in a small frame `{ type: "frame", layout: "horizontal", padding: [0, 4], fill: "#f2f2f7ff" (prose-inline-code-bg per D-108), stroke: "#d4d4d8ff" (prose-inline-code-stroke), strokeWidth: 1, cornerRadius: 4 (prose-inline-code-radius) }` containing the mono text.
      - Text 3: `{ type: "text", content: " to trigger workflows.", fontFamily: "Inter", fontSize: 16, fontWeight: 400, fill: "#141f39ff", lineHeight: 1.625 }`

    Step 3 — `batch_design` Insert Section/RelatedPosts ref + Section/Footer ref as children of `<blogPostId>` (4th + 5th of vertical stack):

    **Insert — Section/RelatedPosts ref instance per D-118:**
    - `{ type: "ref", ref: "<relatedPostsId>", descendants: { /* override 3 Card content per D-111 + D-112 — same pattern as Plan 28-01 Blog index Cards */ } }`
    - Descendants override map per Card ref inside RelatedPosts cards-row (use card-1 / card-2 / card-3 names from 28-00-SUMMARY):
      - Card 1: override `kI3bc.content`, `<metadataCaptionId>.content`, `eNqxd.contains` (3 Badge/Outline tag pills)
      - Card 2: same pattern with different content
      - Card 3: same pattern with different content
    - Representative Card content (per D-122 + D-58 — Crito vocab not v1.3 mirror):
      - Card 1: title "I'm Pivoting" (Joel's other MDX) + metadata "FEB 09, 2026 • 3 MIN READ" + tags ["ai", "automation"]
      - Card 2: title "The Future of Small Business Tech" + metadata "JAN 15, 2026 • 7 MIN READ" + tags ["ai", "tutorial"]
      - Card 3: title "Why I Use Astro for Client Sites" + metadata "JAN 08, 2026 • 4 MIN READ" + tags ["design", "tutorial"]

    **Insert — Section/Footer ref:**
    - `{ type: "ref", ref: "Xs0Hs" }` — no override per D-77.

    Step 4 — Update `<blogPostId>` to fit_content per CALIBRATION-PROTOCOL § 10.8:
    - `Update("<blogPostId>", { placeholder: false, height: "fit_content" })` — frame grows from initial 3200 estimate to actual height.

    Record `<relatedPostsRefId>` + footer ref ID + Card override IDs + final `<blogPostId>` height in scratch.

    **Pitfall 9 chunk note:** This task creates 1 synthetic inline-code frame (5 child nodes) + 1 RelatedPosts ref with 3 Card descendants overrides + 1 Footer ref + 1 frame Update ≈ 12 ops. Within budget.
  </action>
  <verify>
    `batch_get(['<blogPostId>'], readDepth: 1)` shows 5 children in order: G0wNOc + post-header + post-body (17 children including synthetic inline-code) + RelatedPosts ref + Xs0Hs. `<blogPostId>` height = fit_content.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert + Update completed without error.
    - `batch_get(['<blogPostId>'])` returns 5 children in vertical order.
    - post-body has 17 children including synthetic inline-code paragraph at end.
    - Synthetic inline-code paragraph: 3 text nodes (with mono-styled middle text inside framed chrome per Focus 5 + D-108).
    - RelatedPosts ref instance with descendants override on 3 Card children (kI3bc + metadataCaption + footer-actions Badge/Outline overrides) — same pattern as Plan 28-01 Blog index Cards.
    - Section/Footer ref has NO descendants override.
    - `<blogPostId>` height = fit_content.
    - All IDs recorded.
    - prose-inline-code token consumed (synthetic — flagged in OPEN-28-NN).
  </acceptance_criteria>
  <done>
    Full Blog Post frame composition complete: Header + post-header + post-body (all 7 prose tokens consumed) + RelatedPosts + Footer. PAGE-11 ACTIVE deferred to Task 11 (post-APPROVE).
  </done>
</task>

<task type="auto">
  <name>Task 9: Zero-raw-values sweep + snapshot_layout + capture per-section screenshots (Tier-1 fallback if stale-cache)</name>
  <files>design/Crito.pen (read-only; mutating only if Tier-1 fallback triggers)</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 6.4 (stale-cache Tier-1/Tier-2)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 8 (stale-cache likely on new Blog Post frame; Pitfall 5 multi-paragraph false-positives expected to exceed Phase 27)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__search_all_unique_properties({ parentId: '<blogPostId>' })`:
    - Verify only expected literal set (heading-1/-2/-3 typography + prose-paragraph + prose-link accent + prose-inline-code chrome + prose-code-block chrome + Card baseline + Badge/Outline baseline). Unexpected raw values → OPEN-28-NN.

    Step 3 — `mcp__pencil__snapshot_layout({ parentId: '<blogPostId>', problemsOnly: true })`:
    - Expect text-clipping false-positives EXCEEDING Phase 27 (per Pitfall 5 + Focus 8 — multi-paragraph post body with prose tokens). Document each per Phase 24-27 quirk carry-forward.

    Step 4 — `mcp__pencil__get_variables({})` re-check: assert 107 user-facing (no drift).

    Step 5 — Capture screenshots per § 3.4 step 4:
    - Full Blog Post frame: `get_screenshot({ nodeId: '<blogPostId>' })`
    - Sections: post-header + post-body + RelatedPosts
    - Per-prose-token spot checks (informs Task 10 calibration gate): heading-3 H3 + code-block + inline-code-para + italic-link-para (prose-link demonstration) + ul + ol

    Step 6 — If blank-white per OPEN-26-02 stale-cache:
    - **Tier-1 fallback (PAGE FRAME only):** cross-row Update on `<blogPostId>` then restore. Per Focus 8 — NOT applicable to library subtrees.
    - **Tier-2 fallback:** Document Tier-2 path for calibration gate (Task 10 presents batch_get structural data + snapshot_layout; asks user to verify visually in Pencil editor).

    Step 7 — Description identifiers per § 3.5: `28-blog-post-{section}--side-by-side.png (paired with design/images/image-import-NN.jpg)` where NN from Task 0 probe. Sections: full-page + post-header + post-body + related-posts. Per-prose-token spot identifiers also: heading-3 + code-block + inline-code + prose-link.

    Document outcomes in scratch.
  </action>
  <verify>
    Zero unexpected raw values; snapshot_layout clean (text-clipping false-positives documented); screenshots captured (or Tier-2 fallback documented).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `search_all_unique_properties` scope-pass: zero unexpected raw values inside Blog Post frame.
    - `snapshot_layout` clean or text-clipping false-positives documented (likely EXCEEDS Phase 27 per Pitfall 5).
    - `get_variables({})` count: 107.
    - All screenshots captured (or Tier-2 fallback documented per section).
    - No mutations except optional Tier-1 cross-row Update (restored).
  </acceptance_criteria>
  <done>
    Blog Post frame passes sweep + screenshots captured. Ready for calibration gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: Plan-close calibration AskUserQuestion gate per § 3.4 step 5 (crito-source-flat-raster; PRIMARY prose-token validation surface)</name>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.4 step 5 + § 3.5
    - Task 0 scratch: `design/images/image-import-NN.jpg` index probed for w1m3x
    - Task 9 scratch: sweep results + screenshot Tier-1/Tier-2 outcomes
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-110 (prose-paragraph 'provisional' flag verifier)
  </read_first>
  <what-built>
    A new `Blog Post` top-level page frame at canvas (~24107, -4111.55) with 5 stacked content blocks: Section/Header + post-header (title "Getting Started with Business Automation" heading-1 + metadata row "JAN 27, 2026 • 5 MIN READ" + 3 Badge/Outline tag pills "automation"/"small-business"/"productivity" + featured-image 1100×400 placeholder + sibling featuredImage+TOC+tags+metadata wiring note) + post-body (17 children — all 7 Phase 28 prose tokens consumed: heading-1/-2/-3 + prose-paragraph + prose-inline-code synthetic + prose-code-block + prose-link + prose-list unordered/ordered) + Section/RelatedPosts (Plan 28-00 `<relatedPostsId>` with 3 Card content overrides) + Section/Footer.

    Token bindings audit-trailed in PEN-INVENTORY Variant Evidence (Phase 28) rows. Token surface unchanged at 107.

    **This is the PRIMARY validation surface for all 7 Phase 28 prose tokens + verifier for prose-paragraph 'provisional' flag per D-110.**
  </what-built>
  <how-to-verify>
    **VALID-02 calibration pairing per § 3.1 (crito-source-flat-raster sub-case):**

    1. Open `design/Crito.pen` in Pencil editor.
    2. Locate `w1m3x` (08_Blog Details) flat raster at its canvas position (still visible; PAGE-11 ACTIVE hides AFTER APPROVE).
    3. Locate reconstructed `Blog Post` frame at canvas (~24107, -4111.55) — next-right of Plan 28-01 Blog index frame.
    4. Open `design/images/image-import-NN.jpg` (NN from Task 0 probe — FIRST identification per Focus 4) in a separate viewer.
    5. Side-by-side compare for fidelity labels:
       - **Post header**: title heading-1 + metadata row + 3-tag row + featured-image placeholder + sibling note presence — fidelity proposal: EXACT (token-bound + structural reference from v1.3 [slug].astro)
       - **Post body**: PRIMARY prose-token validation — spot-check each prose token consumer:
         - heading-1 title rendering (carry-forward Phase 23 verifier)
         - heading-2 3 instances ("Why Automate?" / "Start Simple" / "What's Next?") — Phase 26 D-72 verifier carry-forward
         - **heading-3 NEW**: "Example: Invoice Follow-ups" — D-106 first consumer; does interpolation default 24/700 read distinct from heading-2 32/700? If too similar, raise OPEN-28-NN.
         - **prose-list NEW**: 3 lists (2 ul + 1 ol) — D-109 first consumer; do indent 24 + item-gap 8 + bullet/number prefixes render correctly?
         - **prose-inline-code NEW**: synthetic consumer "npm run build" — D-108 first consumer (synthetic per Task 7 decision); does mono + bg-surface-elevated + 1px border + radius 4 render correctly?
         - **prose-code-block NEW**: JS code block — D-108 first consumer; does mono + bg + 16px padding + radius 10 + lh 1.6 read correctly? No syntax highlighting per D-108.
         - **prose-link NEW**: "Get in touch" in italic-link paragraph — D-107 first consumer; does accent color render correctly without always-on underline?
         - **prose-paragraph PROVISIONAL flag verifier (D-110)**: multi-paragraph post body rendering — does Inter 16/400 lh 1.625 read correctly for body content? **If APPROVED, prose-paragraph 'provisional' flag removed in PEN-INVENTORY per D-71 carry-forward. If problematic, raise OPEN-28-NN.**
         - hr (line 57): does color-semantic-border-divider 1px render as a clean horizontal rule?
         - italic span: does Inter italic render correctly across the closing paragraph?
       - **RelatedPosts strip**: heading "Related posts" + 3 Card instances with overridden content — fidelity proposal: APPROXIMATE (representative 3 Card content; selection algorithm deferred to runtime)
       - **Header**: EXACT carry-forward.
       - **Footer**: EXACT carry-forward.
    6. Confirm or revise per-section fidelity labels.
    7. **Decision on prose-paragraph 'provisional' flag (D-110):** APPROVE → flag removed in PEN-INVENTORY at Task 11. REVISE → raise OPEN-28-NN with revision proposal.
    8. Stale-cache note: if Task 9 screenshots blank-white (Tier-1 fallback), verify visually in Pencil editor at Blog Post canvas position per § 6.4 Tier-2.

    **Decision per § 3.4 step 6:** APPROVE / REVISE / GAP.
  </how-to-verify>
  <resume-signal>
    Type "APPROVED" to:
    - Lock the 5 fidelity labels (post-header EXACT, post-body EXACT, RelatedPosts APPROXIMATE, Header EXACT, Footer EXACT)
    - Authorize Task 11 PAGE-11 ACTIVE (hide w1m3x via enabled:false)
    - Remove prose-paragraph 'provisional' flag in PEN-INVENTORY per D-110

    Type "APPROVED-EXCEPT-PROSE-PARAGRAPH" if all sections APPROVE but prose-paragraph rendering needs revision (PAGE-11 ACTIVE fires; flag stays provisional; OPEN-28-NN raised).

    Type "REVISE: <description>" to refine specific sections before APPROVE.

    Type "GAP: <description>" to record OPEN-28-NN without blocking progression.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 11: ONLY IF APPROVE — Fire PAGE-11 ACTIVE on w1m3x + update PEN-INVENTORY + remove prose-paragraph 'provisional' flag</name>
  <files>design/Crito.pen; .planning/research/PEN-INVENTORY.md</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 3.3 + § 3.4 step 7
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-126 + D-110 (prose-paragraph flag removal)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md` Plan 28-01 PEN-INVENTORY pattern (DzqTm + OPEN-23-05 partial-resolution precedent)
    - Task 10 user decision: APPROVE / APPROVED-EXCEPT-PROSE-PARAGRAPH / REVISE / GAP
  </read_first>
  <action>
    **PRECONDITION:** User APPROVED (or APPROVED-EXCEPT-PROSE-PARAGRAPH) at Task 10. If REVISE: loop back. If GAP-without-APPROVE: defer w1m3x Update.

    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design Update("w1m3x", { enabled: false })` per PAGE-11 ACTIVE § 3.3:
    - THIRD production use of PAGE-11 ACTIVE in v2.0 (SECOND in Phase 28).
    - **CRITICAL (Pitfall 4 + T-28-03):** Fires ONLY because Task 10 APPROVE was recorded.

    Step 3 — Verify `batch_get(['w1m3x'], readDepth: 0)` confirms `enabled: false`; w1m3x structure preserved.

    Step 4 — Update PEN-INVENTORY.md:

    **Update w1m3x row in Frames Inventory:**
    - Status: `IN-SCOPE` → `reconstructed-PHASE-28`
    - status_counts: `flat:1` → `hidden:1`
    - Notes: "Plan 28-02 — reconstructed as editable Blog Post frame (`<blogPostId>`) with post header + post body (all 7 Phase 28 prose tokens validated) + RelatedPosts strip + Header/Footer; w1m3x raster hidden via enabled:false per PAGE-11 ACTIVE post-APPROVE at calibration gate. Structural archive persists. Source image-import-NN.jpg index: NN (from Task 0 probe — FIRST identification)."

    **Add Blog Post row to Frames Inventory:**
    - Frame: Blog Post (`<blogPostId>`)
    - Scope: IN-SCOPE
    - joel_page_map: /blog/[slug]
    - Status: reconstructed-PHASE-28
    - Notes: "Blog post detail reconstruction per D-122 Plan 28-02. 5 content blocks: Header + post-header + post-body + RelatedPosts + Footer. PRIMARY prose-token validation surface — all 7 Phase 28 prose tokens consumed (heading-3 first use, prose-list 3 instances, prose-inline-code 1 synthetic consumer, prose-code-block 1 JS code block, prose-link 1 'Get in touch' span, mono primitive via inline+block). First Section/RelatedPosts consumer per D-116. Second Blog-domain Card consumer (3 instances; cumulative with Plan 28-01 = 9 of 15 Phase 28 total)."

    **Update OPEN-23-05:**
    - Expand Plan 28-01 partial-resolution note to include Plan 28-02: "07_Blog (DzqTm) + 08_Blog Details (w1m3x = image-import-NN.jpg) both reconstructed. Remaining: 06_Service Details, 05_Service, 04_About (Phase 29 token-mining-only per D-07)."

    **Update OPEN-23-10:**
    - Mark partial expanded: "heading-3 SHIPPED + USED at Plan 28-02 (H3 'Example: Invoice Follow-ups'); heading-4 SHIPPED + UNUSED at Plan 28-02 (sample MDX doesn't reach H4 hierarchy); heading-5/-6 remain open."

    **Update OPEN-25-07:**
    - Status remains "partially-resolved" (9 of 15 Card instances complete; final resolution at Plan 28-03 close after 6 more Tag page Cards).

    **prose-paragraph 'provisional' flag (D-110):**
    - If Task 10 APPROVED (full): **REMOVE 'provisional' flag** from `type-semantic-prose-paragraph-*` in PEN-INVENTORY Token Extensions (or wherever the flag lives). Note: "Provisional flag removed by Plan 28-02 APPROVE (D-110 verifier; multi-paragraph post body rendering passed user spot-check)."
    - If APPROVED-EXCEPT-PROSE-PARAGRAPH: **Flag stays provisional + raise OPEN-28-NN** with revision proposal from user.

    **Add Variant Evidence (Phase 28) rows for Plan 28-02:**
    - Blog Post page frame structural composition
    - Post header section structure (title + metadata + tag row + featured image + wiring note)
    - Post body prose-token consumer rows per Focus 5 mapping (heading-1/-2/-3/prose-paragraph/prose-list/prose-inline-code/prose-code-block/prose-link/mono-primitive consumers — each consumer's content + token binding)
    - RelatedPosts instance with 3 Card descendants override
    - Post-detail tag row (3 Badge/Outline refs)
    - Synthetic inline-code consumer note (per Task 7 decision — flagged in OPEN-28-NN)

    **Add OPEN-28-NN rows** if Task 10 raised GAP flags or APPROVED-EXCEPT-PROSE-PARAGRAPH.

    Step 5 — Plan-close sweep:
    - `get_variables({})` = 107.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` = `"No layout problems."` at document root.
    - `batch_get` final intactness on Plan 28-00 + 28-01 + 28-02 deliverables.

    Step 6 — Write `28-02-SUMMARY.md` per template (objective + outcome + 5 sections fidelity labels + w1m3x index probed + all 7 prose-token consumer validations + prose-paragraph 'provisional' flag outcome + OPEN-23-05 expanded + OPEN-23-10 expanded + plan-close sweep + carry-forward IDs for Plan 28-03 + synthetic inline-code consumer note).
  </action>
  <verify>
    `batch_get(['w1m3x'], readDepth: 0)` returns `enabled: false`. PEN-INVENTORY updated. prose-paragraph flag removed (if APPROVED) or OPEN-28-NN raised (if APPROVED-EXCEPT). Final sweep all PASS. 28-02-SUMMARY.md written.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - User APPROVE (or APPROVED-EXCEPT-PROSE-PARAGRAPH) recorded at Task 10.
    - `batch_design Update("w1m3x", { enabled: false })` completed.
    - `batch_get(['w1m3x'])` confirms enabled:false; structure preserved.
    - PEN-INVENTORY: w1m3x row updated; Blog Post row added; OPEN-23-05 expanded; OPEN-23-10 expanded; OPEN-25-07 status update; Variant Evidence Phase 28 rows added including all prose-token consumer rows; OPEN-28-NN if any.
    - prose-paragraph 'provisional' flag removed (if APPROVED full) OR flag stays + OPEN-28-NN raised (if APPROVED-EXCEPT).
    - Plan-close sweep all PASS (107 tokens + snapshot_layout clean + batch_get intact).
    - 28-02-SUMMARY.md written.
  </acceptance_criteria>
  <done>
    Plan 28-02 closes. w1m3x raster hidden post-APPROVE (PAGE-11 ACTIVE 3rd production use). All 7 Phase 28 prose tokens validated. prose-paragraph 'provisional' flag resolved. Plan 28-03 unblocked; `<blogPostId>` becomes FindEmptySpace anchor.
  </done>
</task>

</tasks>

<verification>
- w1m3x raster image-import-NN.jpg probed at Task 0 (FIRST identification per Focus 4).
- `Blog Post` page frame created at document root, page-frame row y ≈ -4111, width 1440.
- 5 vertical children: Header + post-header + post-body + RelatedPosts + Footer.
- Post body has 17 children consuming all 7 Phase 28 prose tokens.
- Token surface unchanged at 107.
- User APPROVED at Task 10 calibration gate (PRIMARY prose-token validation).
- w1m3x hidden via enabled:false ONLY AFTER APPROVE.
- prose-paragraph 'provisional' flag resolved (removed if full APPROVE; stays + OPEN-28-NN if APPROVED-EXCEPT).
- PEN-INVENTORY extended: w1m3x update + Blog Post row + Variant Evidence Phase 28 prose-token consumer rows + OPEN-23-05 expanded + OPEN-23-10 expanded + OPEN-25-07 update + OPEN-28-NN.
- `snapshot_layout` clean at document root.
- 28-02-SUMMARY.md written.
</verification>

<success_criteria>
- 08_Blog Details flat raster (w1m3x) reconstructed as editable Blog Post frame; OPEN-23-05 further partial-resolved.
- All 7 Phase 28 prose tokens consumed by post body section (D-105 + D-123 deliverable validated).
- First Section/RelatedPosts consumer per D-116 narrow-scoping.
- 9 of 15 total Phase 28 Card instances complete (cumulative with Plan 28-01).
- PAGE-11 ACTIVE 3rd production use successful.
- prose-paragraph 'provisional' flag resolved per D-110.
- Per-section fidelity labels recorded + user APPROVED.
- Carry-forward `<blogPostId>` enables Plan 28-03 FindEmptySpace anchor.
</success_criteria>

<output>
After completion, create `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-02-SUMMARY.md` per template with:
- w1m3x image-import-NN.jpg index probed (FIRST identification)
- `<blogPostId>` + canvas coords
- 5 content block IDs (Header ref + `<postHeaderId>` + `<postBodyId>` + `<relatedPostsRefId>` + Footer ref)
- 17 post-body child IDs mapped to prose tokens per Focus 5 table
- 3 RelatedPosts Card instance IDs + descendants override summaries
- Per-section fidelity labels (5: post-header EXACT, post-body EXACT, RelatedPosts APPROXIMATE, Header EXACT, Footer EXACT) + user APPROVE
- prose-paragraph 'provisional' flag outcome (removed / stays + OPEN-28-NN)
- PAGE-11 ACTIVE outcome (w1m3x enabled:false; 3rd production use)
- PEN-INVENTORY changes (w1m3x update + Blog Post row + Variant Evidence Phase 28 prose-token consumer rows + OPEN-23-05 expanded + OPEN-23-10 expanded + OPEN-25-07 update + OPEN-28-NN)
- Synthetic inline-code consumer decision documented (Task 7 → Task 8)
- Tier-1 / Tier-2 stale-cache outcomes
- Plan-close sweep results (107 tokens + snapshot + batch_get)
- Carry-forward IDs for Plan 28-03 (`<blogPostId>` as FindEmptySpace anchor)
</output>
</content>
</invoke>