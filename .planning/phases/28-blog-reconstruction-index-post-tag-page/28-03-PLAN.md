---
phase: 28-blog-reconstruction-index-post-tag-page
plan: 03
type: execute
wave: 4
depends_on:
  - 28-00
  - 28-01
  - 28-02
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
  - .planning/research/exports/v2.0/end-of-phase-28/id-inventory.json
autonomous: false
requirements:
  - PAGE-03
  - PAGE-09
  - PAGE-11
  - VALID-01
  - VALID-02
  - VALID-03
threat_model_summary:
  - id: T-28-01
    threat: "Pencil active-editor mismatch causes write to wrong .pen file"
    severity: high
    mitigation: "D-125 pre-flight `mcp__pencil__get_editor_state({ include_schema: false })` before every mutating call; halt and surface on mismatch."
  - id: T-28-02
    threat: "Token surface drift during Tag page construction"
    severity: medium
    mitigation: "`get_variables({})` at plan open + close (expect 107 — zero new tokens added)."
  - id: T-28-05
    threat: "TagFilter descendants override breaks Plan 28-01 Blog index TagFilter instance (different active-pill index could shuffle Plan 28-01 state)"
    severity: medium
    mitigation: "Descendants override at INSTANCE level only (Tag page TagFilter ref instance). Plan 28-01 Blog index TagFilter ref instance retains its own descendants state (Pencil 2.13 descendants override is per-instance, not component-level). Verify Plan 28-01 Blog index TagFilter unchanged via batch_get at Task 0."

must_haves:
  truths:
    - "Pre-flight assertion (D-125) succeeded; active editor == design/Crito.pen confirmed at each mutating call."
    - "New top-level page frame named `Tag` exists in `design/Crito.pen` as a direct child of document root (Pitfall 3 carry-forward), positioned at the page-frame row (y ≈ -4111) via `mcp__pencil__find_empty_space_on_canvas` with `nodeId: '<blogPostId>'` anchor (Blog Post frame from Plan 28-02) per CALIBRATION-PROTOCOL § 10.4 + D-122."
    - "The `Tag` frame has width 1440, vertical auto-layout with alignItems center, fill `#ffffffff` (color-semantic-bg-page literal per OPEN-23-13)."
    - "The `Tag` frame contains 5 stacked content blocks: (1) Section/Header ref (G0wNOc, NO descendants override per D-77), (2) page-intro section with representative content per D-122 Claude's Discretion + STUB-acceptable per D-83 mixed-fidelity ('Posts tagged \"automation\"' heading-1 + '3 posts about automation' subhead) + sibling Pencil note documenting dynamic generation pattern for code-milestone wiring, (3) Section/TagFilter instance with current-tag pill 'automation' marked active via descendants override (DIFFERENT active-pill index from Blog index — validates D-117 paired-ref descendants flexibility), (4) 6-Card grid (3-col × 2-row per D-114 — same composition as Plan 28-01 Blog index; filtered representation), (5) Section/Footer ref (Xs0Hs, NO descendants override per D-77)."
    - "Section/TagFilter instance descendants override per D-117 paired-ref Sub-option A1: flip pill-1-active.enabled:false + pill-1-inactive.enabled:true + pill-2-active.enabled:true + pill-2-inactive.enabled:false (move active state from 'All Posts' position 1 to 'automation' position 2). This is a DIFFERENT active-pill index from Plan 28-01 Blog index (which uses Plan 28-00 default position 1 active)."
    - "6 Card instances in 3-col × 2-row grid per D-114: each Card overrides title-slot descendants per D-111 (kI3bc + `<metadataCaptionId>`) + footer-actions-slot descendants per D-112 (2-3 Primitive/Badge/Outline tag pills, all including 'automation' since this is the automation-tagged filter view) + image-slot default placeholder + sibling featuredImage wiring note per D-113. Cards 1-3 may overlap content with Plan 28-01 Blog index (same automation-tagged posts) — that's expected since Tag page is the filtered subset."
    - "Per-section fidelity labels per D-83 recorded in 28-03-SUMMARY.md: page-intro = STUB (representative content per D-122 Claude's Discretion — actual content dynamic at code-milestone); TagFilter (active-state different from Blog index) = APPROXIMATE (Plan 28-00 paired-ref override works as expected); 6-Card grid = APPROXIMATE (representative filtered post set; image-slot default); Header = EXACT (Phase 25); Footer = EXACT (Phase 25)."
    - "User APPROVED the composition via single AskUserQuestion calibration gate per CALIBRATION-PROTOCOL § 4.5 (joel-only token-usage format — pairs reconstructed Tag frame token-usage against `_Tokens & Foundations` RpGbe; NO Crito source raster to side-by-side compare per joel-only-no-crito-ref scope)."
    - "PAGE-11 INERT per CALIBRATION-PROTOCOL § 4.3 + § 4.4 (no Crito source raster to remove — Crito's blog architecture has no Tag page frame per Phase 23 audit). This is the THIRD INERT-branch consumer of CALIBRATION-PROTOCOL after Phase 26 FAQ + 404 + Phase 27 Thank-you."
    - "PEN-INVENTORY Frames Inventory has new row for `Tag` (scope: joel-only-no-crito-ref, joel_page_map: /blog/tags/[tag], status: reconstructed-PHASE-28); Variant Evidence (Phase 28) sub-section extended with Tag page-frame structural rows + 6-Card grid composition + TagFilter active-state-flip override rows (validates D-117 paired-ref flexibility)."
    - "OPEN-25-07 marked RESOLVED at Plan 28-03 close — 15 Card instances total across 3 Phase 28 frames (6 Blog index + 3 RelatedPosts + 6 Tag page) = first concrete consumer validation of Phase 25 D-49 single-Card claim."
    - "Token surface unchanged at 107 user-facing across Plan 28-03."
    - "Phase 23-27 baseline IDs + Plan 28-00 + 28-01 + 28-02 deliverable IDs intact (Plan 28-03 instances refs only)."
    - "Document-level `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `\"No layout problems.\"` at plan close (text-clipping false-positives documented per Phase 24-27 carry-forward)."
    - "id-inventory.json archival at `.planning/research/exports/v2.0/end-of-phase-28/id-inventory.json` per OPEN-23-01 substitution pattern + Phase 23/24/25/26/27 precedent."
  artifacts:
    - path: "design/Crito.pen"
      provides: "New top-level page frame `Tag` with 5 vertical content blocks: Section/Header + page-intro (STUB representative) + Section/TagFilter instance (active-state flipped to 'automation') + 6-Card grid + Section/Footer."
      contains: "Tag page frame (id <tagPageId>), page-intro section (heading-1 + subhead + sibling dynamic-generation note), TagFilter ref instance with descendants override flipping active-pill index from 1 to 2, 6-Card grid with 6 Card refs (t40xct) each with descendants overrides (same pattern as Plan 28-01 + 28-02 Cards), Section/Header ref G0wNOc, Section/Footer ref Xs0Hs."
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Frames Inventory row for Tag + Variant Evidence (Phase 28) Tag rows + OPEN-25-07 RESOLVED note + any OPEN-28-NN raised at calibration gate."
      contains: "Tag row in Frames Inventory, Variant Evidence rows for Tag page-frame + TagFilter active-state-flip override (validates D-117 paired-ref flexibility) + 6-Card grid, OPEN-25-07 RESOLVED"
    - path: ".planning/research/exports/v2.0/end-of-phase-28/id-inventory.json"
      provides: "Structural snapshot of Crito.pen after Plan 28-03 close per OPEN-23-01 substitution + Phase 23/24/25/26/27 archival precedent."
      contains: "Document-tree snapshot via batch_get readDepth 4 from document root; all Phase 28 new IDs (7 prose tokens + 2 Section components + Card title-slot extension + Badge/Outline variant + 3 page frames) included."
  key_links:
    - from: "Tag page frame (<tagPageId>)"
      to: "Section/Header (G0wNOc) + Section/TagFilter (<tagFilterId> with active-state flipped) + Section/Footer (Xs0Hs)"
      via: "ref children at top + middle + bottom; Header/Footer no override per D-77; TagFilter descendants override flips active-pill index per D-117"
      pattern: "Tag.*ref.*G0wNOc.*ref.*<tagFilterId>.*descendants.*pill-1-active.*enabled.*false.*pill-2-active.*enabled.*true.*ref.*Xs0Hs"
    - from: "Tag 6-Card grid"
      to: "6 Compound/Card (t40xct) ref instances with descendants overrides (same pattern as Plan 28-01)"
      via: "Each Card overrides: kI3bc.content + <metadataCaptionId>.content + footer-actions-slot Badge/Outline tag pills (all include 'automation' since filtered view)"
      pattern: "tag-grid.*ref.*t40xct.*descendants.*kI3bc.*<metadataCaptionId>.*<badgeOutlineId>.*automation"
    - from: "Tag page-intro sibling note"
      to: "Dynamic generation pattern for code-milestone wiring"
      via: "Sibling Pencil text node documenting: 'Code milestone generates page-intro heading from `Posts tagged \"${tag}\"` template + subhead from post count'"
      pattern: "Posts tagged.*Code milestone"
---

<objective>
Create the `Tag` page frame in `design/Crito.pen` as a joel-only-no-crito-ref reconstruction per D-122 Plan 28-03 + CALIBRATION-PROTOCOL § 4 (joel-only branch). No Crito source frame exists for Tag pages — Crito's blog architecture has Blog index + Blog detail only per Phase 23 audit. Calibration is token-usage check vs `_Tokens & Foundations` (RpGbe) per § 4.1 — NOT side-by-side with a raster.

**No probe needed** (joel-only branch — no source raster to identify). Tag page is the FOURTH INERT-branch consumer of CALIBRATION-PROTOCOL (Phase 26 FAQ + 404 + Phase 27 Thank-you + Phase 28 Tag).

Composition per D-122 + D-58 (fresh in Crito vocab):
1. `Section / Header` instance (G0wNOc) — no override per D-77.
2. **Page-intro section** — representative content per D-122 Claude's Discretion + STUB-acceptable per D-83 mixed-fidelity: heading-1 'Posts tagged "automation"' + body subhead "3 posts about automation" (or per Joel's preference — surface at calibration gate). Sibling Pencil note documents dynamic generation pattern.
3. **`Section / TagFilter` instance** (Plan 28-00 `<tagFilterId>`) with descendants override per D-117 paired-ref Sub-option A1: flip active-pill index from position 1 ("All Posts") to position 2 ("automation"). **DIFFERENT active-pill index from Plan 28-01 Blog index validates D-117 paired-ref flexibility.**
4. **6-Card grid** (3-col × 2-row per D-114) — same composition pattern as Plan 28-01 Blog index, filtered to 'automation'-tagged posts. Each Card includes 'automation' in its tag pills (footer-actions-slot Badge/Outline overrides).
5. `Section / Footer` instance (Xs0Hs) — no override per D-77.

**PAGE-11 INERT** per CALIBRATION-PROTOCOL § 4.3: no Crito source raster to remove. No batch_design Update on any flat raster at Plan 28-03 close.

**Purpose:** Phase 28's joel-only branch contrast — validates that Phase 28 tools work across BOTH branches in one phase (Phase 27 dual-branch precedent extended). Final OPEN-25-07 Card consumer-resolution (15 total Card instances across 3 Phase 28 frames). End-of-phase id-inventory.json archival per OPEN-23-01 substitution.

**Output:** A working `Tag` top-level page frame; PEN-INVENTORY extended with Frames Inventory row + Variant Evidence rows + OPEN-25-07 RESOLVED + OPEN-28-NN if any; end-of-phase id-inventory.json archived; per-section fidelity labels recorded; plan-close user APPROVED state. ONE user gate at plan close per CALIBRATION-PROTOCOL § 4.5 (joel-only token-usage format).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-VALIDATION.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-02-SUMMARY.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@src/pages/blog/tags/[tag].astro
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + verify Plans 28-00 + 28-01 + 28-02 outputs intact + read v1.3 Tag page source for structural reference</name>
  <files>design/Crito.pen (read-only); src/pages/blog/tags/[tag].astro (read-only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` — `<tagFilterId>` + paired-ref pill IDs (pill-1-active, pill-1-inactive, pill-2-active, pill-2-inactive...) + `<relatedPostsId>` + `<metadataCaptionId>` + `<badgeOutlineId>`
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md` — `<blogPageId>` (Blog index reference; Plan 28-03 Card grid pattern mirror)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-02-SUMMARY.md` — `<blogPostId>` (for FindEmptySpace anchor)
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4 (joel-only branch protocol; § 4.5 AskUserQuestion format)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-117 (active-state mechanic) + D-122 (Tag page structure) + D-58 (fresh in Crito vocab)
    - `src/pages/blog/tags/[tag].astro` lines 1-95 (v1.3 Tag page structural reference: 'Back to all posts' link + 'Posts tagged "X"' heading + N posts subhead + Card grid)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125: `mcp__pencil__get_editor_state({ include_schema: false })`. Halt + surface on mismatch.

    Step 2 — Token surface drift check: `mcp__pencil__get_variables({})`. Assert user-facing count is 107 (Plan 28-02 close). Halt on drift.

    Step 3 — Plan 28-00 + 28-01 + 28-02 deliverable intactness:
    `mcp__pencil__batch_get({ readDepth: 2, nodeIds: ['<tagFilterId>', '<relatedPostsId>', 'vGH3A', '<metadataCaptionId>', '<badgeOutlineId>', '<blogPageId>', '<blogPostId>', 'DzqTm', 'w1m3x'] })`. Confirm:
    - `<tagFilterId>` paired-ref pills intact (10 children: 5 positions × 2 refs each)
    - `<blogPageId>` (Blog index) intact + 5 children unchanged
    - `<blogPostId>` (Blog Post) intact + 5 children unchanged
    - DzqTm + w1m3x both `enabled: false` (Plans 28-01 + 28-02 PAGE-11 ACTIVE outcomes preserved)
    - vGH3A still has 2 children (kI3bc + metadata-caption) — Plan 28-00 extension preserved

    Step 4 — Identify paired-ref pill IDs from 28-00-SUMMARY for descendants override at Task 3:
    - `<pill-1-active-id>` (M7eUr ref at position 1, default enabled:true — "All Posts")
    - `<pill-1-inactive-id>` (hIWuC ref at position 1, default enabled:false — "All Posts")
    - `<pill-2-active-id>` (M7eUr ref at position 2, default enabled:false — "automation")
    - `<pill-2-inactive-id>` (hIWuC ref at position 2, default enabled:true — "automation")

    These IDs feed the descendants override in Task 3 (flip enabled values).

    Step 5 — Phase 23-27 baseline component intactness:
    `batch_get` on G0wNOc, Xs0Hs, t40xct, M7eUr, hIWuC, j0FxQZ, csXky (404 frame), n0QqTd (Contact frame).

    Step 6 — Read v1.3 Tag page source for structural reference:
    `src/pages/blog/tags/[tag].astro` lines 1-95:
    - 'Back to all posts' link (line ~30 — deferred to runtime per D-122 Claude's Discretion)
    - 'Posts tagged "<tag>"' heading template (line ~47)
    - N posts count subhead (line ~50)
    - Card grid (line ~62)

    Per D-122 Plan 28-03 + D-122 Claude's Discretion + D-58: ship representative content ('Posts tagged "automation"' + '3 posts about automation') with sibling Pencil note documenting dynamic generation.

    Step 7 — Plan-derive 6 Card content sets (filtered to 'automation' tag — overlap with Plan 28-01 expected since same posts):
    - Card 1: title "Getting Started with Business Automation" (Joel's MDX); metadata "JAN 27, 2026 • 5 MIN READ"; tags ["automation", "small-business", "productivity"] (includes automation)
    - Card 2: title "I'm Pivoting" (Joel's MDX); metadata representative; tags ["ai", "automation"] (includes automation)
    - Cards 3-6: representative agency-template titles + metadata; all tags include 'automation' since this is the automation filter view. E.g., "Automating Customer Onboarding" / "From Spreadsheets to Automated Workflows" / "AI + Automation: A Practical Guide" / "Small Business Automation Stack 2026"

    No mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token: 107. Plan 28-00 + 28-01 + 28-02 deliverables intact. DzqTm + w1m3x still enabled:false. TagFilter paired-ref pill IDs identified for active-state flip. v1.3 Tag page structural reference captured. 6 representative Card content sets derived (all include 'automation' tag — filter view)."
  </verify>
  <acceptance_criteria>
    - `get_editor_state` returned activeEditor ending in `design/Crito.pen`.
    - `get_variables({})` returned 107 user-facing tokens.
    - `batch_get` on Plan 28-00/01/02 IDs confirms intactness; DzqTm + w1m3x both enabled:false.
    - `batch_get` on baselines (G0wNOc, Xs0Hs, t40xct, M7eUr, hIWuC, j0FxQZ, csXky, n0QqTd) confirms intactness.
    - TagFilter paired-ref pill IDs identified for Task 3 descendants override.
    - 6 representative Card content sets derived.
    - v1.3 Tag page structural reference captured.
    - No mutations in Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight PASS. Paired-ref pill IDs ready for active-state flip. Plan 28-03 may safely mutate.
  </done>
</task>

<task type="auto">
  <name>Task 1: Create `Tag` top-level page frame via find_empty_space_on_canvas with nodeId:<blogPostId> anchor + page-intro section</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 10.4 (FindEmptySpace nodeId anchor pattern)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-122 (Plan 28-03 anchors on Blog Post frame from 28-02)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-02-SUMMARY.md` `<blogPostId>` + canvas coords
    - Task 0 scratch: representative page-intro content
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — FindEmptySpace with `nodeId: '<blogPostId>'` anchor:
    `mcp__pencil__find_empty_space_on_canvas({ width: 1440, height: 2200, direction: "right", padding: 80, nodeId: "<blogPostId>" })`. Expected `{x, y}` ≈ `{x: ~25727, y: -4111.55}` (next-right of `<blogPostId>` from Plan 28-02). Height 2200 estimate = Header (~80) + page-intro (~200) + TagFilter (~80) + 6-Card grid (~1200) + Footer (~500) + spacing.

    Step 3 — `mcp__pencil__batch_design` Insert top-level frame:
    - Parent: document root.
    - `type: "frame"`, `name: "Tag"`, `placeholder: true`, `position: { x, y }`.
    - Dimensions: `width: 1440`, `height: 2200`.
    - Layout: `vertical`, `gap: 0`, `padding: 0`, `alignItems: "center"`.
    - Fill: `"#ffffffff"`.

    Step 4 — `batch_design` Insert children of `<tagPageId>` — Header + page-intro:

    **Insert 1 — Section/Header ref:**
    - `{ type: "ref", ref: "G0wNOc" }` — no override per D-77.

    **Insert 2 — page-intro section frame:**
    - `type: "frame"`, `name: "page-intro"`, `width: 1200`, `layout: "vertical"`, `gap: 12`, `padding: [80, 0, 48, 0]`, `alignItems: "center"` (matches Blog index page-intro alignment + agency-template register).

    **Page-intro children:**

    Child 1 — Back-link affordance (per D-122 Claude's Discretion + v1.3 structural reference):
    - **DECISION:** Skip Back-link in v2.0 static design — deferred to runtime per D-122 + Deferred Ideas (similar to D-99 "Return to homepage" link in Plan 27-01). Code milestone wires the Back link.
    - Alternative: ship as plain text-link styled per prose-link token. **Default: skip** (matches Phase 27 D-99 Claude's Discretion for static-design economy).

    Child 2 — Heading text (representative per D-122 + D-83 STUB-acceptable):
    - `{ type: "text", name: "heading", content: "Posts tagged \"automation\"", fontFamily: "Plus Jakarta Sans" (heading-1-family), fontSize: 48 (heading-1-size), fontWeight: 700, fill: "#141f39ff" (color-semantic-text-primary), lineHeight: 1.4, textAlign: "center" }` — heading-1 token surface.

    Child 3 — Body subhead (representative per D-122 + D-83 STUB-acceptable):
    - `{ type: "text", name: "subhead", content: "3 posts about automation", fontFamily: "Inter" (prose-paragraph-family), fontSize: 16, fontWeight: 400, fill: "#52525bff" (color-semantic-text-secondary), lineHeight: 1.625, textAlign: "center" }` — prose-paragraph token surface.

    Child 4 — sibling Pencil note documenting dynamic generation per D-122 Claude's Discretion:
    - `{ type: "note", name: "Tag page dynamic-generation wiring", content: "Code milestone generates page-intro from /blog/tags/[tag].astro template: heading content via `Posts tagged \"${tag}\"` template literal; subhead via `${posts.length} posts about ${tag}` (or similar pluralized phrasing) — see v1.3 src/pages/blog/tags/[tag].astro lines 47-62 for reference structure. v2.0 ships representative 'automation' tag with 3-posts count as STUB content; runtime renders per-tag dynamic values. v1.3 'Back to all posts' link affordance also deferred to runtime per CONTEXT Deferred Ideas." }`

    Record `<tagPageId>`, `<pageIntroId>`, `<headingId>`, `<subheadId>`, `<wiringNoteId>` in scratch.
  </action>
  <verify>
    `batch_get(['<tagPageId>'], readDepth: 3)` shows 2 direct children: G0wNOc + page-intro. page-intro contains heading "Posts tagged \"automation\"" + subhead "3 posts about automation" + sibling wiring note.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `find_empty_space_on_canvas({ ..., nodeId: '<blogPostId>' })` returned y ≈ -4111.
    - `batch_design` Insert completed without error.
    - `batch_get(['<tagPageId>'])` returns frame at document root level (NOT inside library parents).
    - `<tagPageId>` has 2 children: Section/Header ref + page-intro frame.
    - page-intro has 3 children: heading + subhead + sibling wiring note.
    - Heading content "Posts tagged \"automation\"" (quotes preserved exactly).
    - Subhead content "3 posts about automation".
    - Typography token-bound: heading-1 + prose-paragraph.
    - Wiring note documents dynamic generation pattern + 'Back to all posts' deferral.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Tag page frame in place. Header + page-intro complete (STUB representative content + sibling wiring note documents dynamic generation).
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert Section/TagFilter ref instance with descendants override flipping active-pill index from position 1 ("All Posts") to position 2 ("automation")</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-117 (active-state mechanic — paired-ref enabled toggle) + D-122 (Tag page TagFilter active-state different from Blog index)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 6 Sub-option A1 (paired-ref descendants override flip for Tag page)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` paired-ref pill IDs (pill-1-active, pill-1-inactive, pill-2-active, pill-2-inactive)
    - Task 0 scratch: paired-ref pill IDs ready for descendants override
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<tagPageId>` (3rd of vertical stack) — Section/TagFilter ref instance per D-117 active-state flip:

    **Insert — Section/TagFilter ref with descendants override:**
    - `{ type: "ref", ref: "<tagFilterId>", descendants: { "<pill-1-active-id>": { enabled: false }, "<pill-1-inactive-id>": { enabled: true }, "<pill-2-active-id>": { enabled: true }, "<pill-2-inactive-id>": { enabled: false } } }`

    **Mechanic per Focus 6 Sub-option A1:**
    - Position 1 "All Posts": flip from default active (M7eUr enabled:true) to inactive (hIWuC enabled:true) — by setting pill-1-active.enabled:false + pill-1-inactive.enabled:true
    - Position 2 "automation": flip from default inactive (hIWuC enabled:true) to active (M7eUr enabled:true) — by setting pill-2-active.enabled:true + pill-2-inactive.enabled:false
    - Positions 3-5 ("small-business" / "productivity" / "ai"): unchanged (inactive state preserved from Plan 28-00 defaults)

    **Validation of D-117 paired-ref flexibility:**
    - Plan 28-01 Blog index uses Plan 28-00 default (pill-1 "All Posts" active).
    - Plan 28-03 Tag page flips to pill-2 "automation" active via descendants override.
    - Both consumers use the SAME `<tagFilterId>` component with different per-instance descendants overrides — validates Focus 6 Sub-option A1 + D-117.
    - Plan 28-01 Blog index TagFilter ref instance is UNCHANGED (descendants overrides are per-instance per Pencil 2.13).

    Step 3 — Verify Plan 28-01 Blog index TagFilter ref instance unchanged per T-28-05 mitigation:
    - `batch_get(['<blogPageId>'], readDepth: 4)` — locate Blog index TagFilter ref child; confirm NO descendants override on it (uses Plan 28-00 defaults: pill-1 active).
    - If Blog index TagFilter ref shows unexpected enabled state on pills, halt + surface (descendants override is per-instance — Plan 28-03 should NOT affect Plan 28-01 instance).

    Record `<tagPageTagFilterRefId>` in scratch.
  </action>
  <verify>
    `batch_get(['<tagPageTagFilterRefId>'], readDepth: 2)` shows TagFilter ref with descendants override map flipping pill-1 pair (active→inactive) + pill-2 pair (inactive→active). `batch_get(['<blogPageId>'], readDepth: 4)` confirms Blog index TagFilter ref unchanged.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<tagPageId>'])` now has 3 children: Header + page-intro + TagFilter ref.
    - TagFilter ref has descendants override: pill-1-active enabled:false, pill-1-inactive enabled:true, pill-2-active enabled:true, pill-2-inactive enabled:false.
    - Plan 28-01 Blog index TagFilter ref UNCHANGED (verifies Pencil per-instance descendants behavior).
    - `<tagPageTagFilterRefId>` recorded.
  </acceptance_criteria>
  <done>
    TagFilter active-state flip in place. D-117 paired-ref flexibility validated. Active pill is now "automation" (position 2) for Tag page; Blog index still has "All Posts" (position 1) active.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insert 6-Card grid frame + Cards 1-3 (Row 1) with descendants overrides — same pattern as Plan 28-01</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-111 + D-112 + D-113 + D-114 (Card-instance pattern; same as Plan 28-01)
    - Task 0 scratch: 6 representative Card content sets (all include 'automation' tag)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-01-SUMMARY.md` Plan 28-01 6-Card grid composition pattern (mirror)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` `<metadataCaptionId>` + `<badgeOutlineId>` + Compound/Card descendant IDs
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<tagPageId>` (4th of vertical stack):

    **Insert — 6-Card grid frame:**
    - `type: "frame"`, `name: "tag-grid"`, `width: 1200`, `layout: "vertical"`, `gap: 24`, `padding: [0, 0, 80, 0]`, `alignItems: "center"`.

    **Row 1 — horizontal frame:**
    - `{ type: "frame", name: "row-1", width: "fill_container", layout: "horizontal", gap: 24, alignItems: "start" }` containing 3 Card refs.

    **Card 1 instance** (Joel's MDX — "Getting Started with Business Automation"):
    - `{ type: "ref", ref: "t40xct", descendants: { "kI3bc": { content: "Getting Started with Business Automation" }, "<metadataCaptionId>": { content: "JAN 27, 2026 • 5 MIN READ" }, "eNqxd": { contains: [<3 Badge/Outline refs for 'automation', 'small-business', 'productivity'>] } } }`
    - footer-actions-slot 3 Badge/Outline refs:
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "automation" } } }`
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "small-business" } } }`
      - `{ type: "ref", ref: "<badgeOutlineId>", descendants: { "<badgeOutlineLabelId>": { content: "productivity" } } }`

    **Card 2 instance** ("I'm Pivoting" — also automation-tagged):
    - Overrides: kI3bc.content = "I'm Pivoting"; `<metadataCaptionId>`.content = "FEB 09, 2026 • 3 MIN READ"; eNqxd.contains = [Badge/Outline "ai", Badge/Outline "automation"]

    **Card 3 instance** ("Automating Customer Onboarding"):
    - Overrides: kI3bc.content = "Automating Customer Onboarding"; `<metadataCaptionId>`.content = "JAN 15, 2026 • 6 MIN READ"; eNqxd.contains = [Badge/Outline "automation", Badge/Outline "tutorial"]

    Step 3 — Insert sibling Pencil note at tag-grid canvas position per D-113 + Plan 28-01 precedent:
    - `{ type: "note", name: "Tag page Card filtering wiring", content: "Code milestone filters /blog post collection by current tag (URL param) — see v1.3 src/pages/blog/tags/[tag].astro line ~30 for `posts.filter(p => p.data.tags.includes(tag))` pattern. v2.0 ships representative 6 Cards (all include 'automation' tag); runtime renders actual filtered subset (length may differ). Each Card image-slot (FGdti) wires to `post.data.featuredImage` per Card-consumer pattern (carry-forward from Plan 28-01 Blog index sibling note)." }`

    Record Card 1-3 instance IDs + Badge/Outline tag pill IDs in scratch.

    **Pitfall 9 chunk note:** This task creates 1 grid frame + 1 row frame + 3 Card refs + 8 Badge/Outline refs + 1 note ≈ 14 ops. Within budget.
  </action>
  <verify>
    `batch_get(['<tagGridId>'], readDepth: 4)` shows row-1 frame with 3 Card refs (t40xct); each Card has descendants override for kI3bc + `<metadataCaptionId>` + eNqxd contains 2-3 Badge/Outline refs (all including 'automation' label).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `batch_get(['<tagPageId>'])` now has 4 children: Header + page-intro + TagFilter + tag-grid.
    - tag-grid contains row-1 with 3 Card refs.
    - Each Card descendants override contains kI3bc, `<metadataCaptionId>`, eNqxd (with Badge/Outline children including 'automation' tag label).
    - Sibling Card filtering wiring note exists at tag-grid canvas position.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Row 1 of Tag page 6-Card grid complete (Cards 1-3, all automation-tagged).
  </done>
</task>

<task type="auto">
  <name>Task 4: Insert Row 2 (Cards 4-6) + Section/Footer ref (closes Tag frame composition)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: Card 4-6 content sets (all automation-tagged)
    - Task 3 scratch: tag-grid frame ID
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-77 (Footer no override)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `batch_design` Insert 1 child of `<tagGridId>` (Row 2):

    **Row 2 — horizontal frame:**
    - `{ type: "frame", name: "row-2", width: "fill_container", layout: "horizontal", gap: 24, alignItems: "start" }` containing 3 Card refs.

    **Card 4 instance** ("From Spreadsheets to Automated Workflows"):
    - Overrides: kI3bc.content = "From Spreadsheets to Automated Workflows"; `<metadataCaptionId>`.content = "DEC 20, 2025 • 5 MIN READ"; eNqxd.contains = [Badge/Outline "automation", Badge/Outline "productivity"]

    **Card 5 instance** ("AI + Automation: A Practical Guide"):
    - Overrides: kI3bc.content = "AI + Automation: A Practical Guide"; `<metadataCaptionId>`.content = "DEC 05, 2025 • 7 MIN READ"; eNqxd.contains = [Badge/Outline "ai", Badge/Outline "automation"]

    **Card 6 instance** ("Small Business Automation Stack 2026"):
    - Overrides: kI3bc.content = "Small Business Automation Stack 2026"; `<metadataCaptionId>`.content = "NOV 28, 2025 • 8 MIN READ"; eNqxd.contains = [Badge/Outline "automation", Badge/Outline "small-business", Badge/Outline "tutorial"]

    Step 3 — `batch_design` Insert 1 child of `<tagPageId>` (5th + last of vertical stack):

    **Insert — Section/Footer ref:**
    - `{ type: "ref", ref: "Xs0Hs" }` — no override per D-77.

    Step 4 — Update `<tagPageId>` to fit_content per CALIBRATION-PROTOCOL § 10.8:
    - `Update("<tagPageId>", { placeholder: false, height: "fit_content" })`.

    Record Card 4-6 IDs + footer ref ID + final `<tagPageId>` height in scratch.
  </action>
  <verify>
    `batch_get(['<tagPageId>'], readDepth: 1)` shows 5 children in vertical order: Header + page-intro + TagFilter + tag-grid + Footer. tag-grid has 2 row children (row-1 + row-2) each with 3 Card refs. `<tagPageId>` height = fit_content.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert + Update completed without error.
    - `batch_get(['<tagPageId>'])` returns 5 children in vertical order.
    - tag-grid has 2 row children, each with 3 Card refs (6 total).
    - All 6 Cards include 'automation' tag in footer-actions-slot Badge/Outline overrides.
    - Section/Footer ref has NO descendants override.
    - `<tagPageId>` height = fit_content.
    - All IDs recorded.
  </acceptance_criteria>
  <done>
    Full Tag frame composition complete: Header + page-intro (STUB) + TagFilter (active-state flipped) + 6-Card grid (all automation-tagged) + Footer.
  </done>
</task>

<task type="auto">
  <name>Task 5: Zero-raw-values sweep + snapshot_layout + capture per-section screenshots (Tier-1 fallback if stale-cache)</name>
  <files>design/Crito.pen (read-only; mutating only if Tier-1 fallback triggers)</files>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 6.4 (stale-cache Tier-1/Tier-2)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 8 (stale-cache likely on new page frames)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__search_all_unique_properties({ parentId: '<tagPageId>' })`:
    - Verify only expected literal set (same as Plan 28-01 — heading-1 + prose-paragraph + Card baseline + metadata-caption + Badge/Outline). Unexpected raw values → OPEN-28-NN.

    Step 3 — `mcp__pencil__snapshot_layout({ parentId: '<tagPageId>', problemsOnly: true })`:
    - Expect `"No layout problems."` or text-clipping false-positives per Pitfall 5.

    Step 4 — `mcp__pencil__get_variables({})` re-check: 107 (no drift).

    Step 5 — Capture screenshots per § 4.4 step 9 (joel-only branch — inline-only per OPEN-23-01):
    - `get_screenshot({ nodeId: '<tagPageId>' })` (full Tag frame)
    - `get_screenshot({ nodeId: '<pageIntroId>' })`
    - `get_screenshot({ nodeId: '<tagPageTagFilterRefId>' })` (capture active-state flip rendering)
    - `get_screenshot({ nodeId: '<tagGridId>' })`

    Step 6 — Stale-cache fallback per § 6.4:
    - Tier-1: cross-row Update on `<tagPageId>` if blank-white; restore.
    - Tier-2: document fallback path for Task 6 calibration gate (joel-only branch uses token-usage check against RpGbe instead of side-by-side raster comparison).

    Step 7 — Description identifiers per § 4.5 (joel-only — NOT paired with raster):
    - `28-tag-{section}--token-usage-check.png` (NOT `--side-by-side` per joel-only branch convention)
    - Sections: full-page + page-intro + tag-filter (active-flip) + card-grid

    Document outcomes in scratch.
  </action>
  <verify>
    Zero unexpected raw values; snapshot_layout clean; screenshots captured (or Tier-2 documented).
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `search_all_unique_properties` scope-pass: zero unexpected raw values.
    - `snapshot_layout` clean or text-clipping false-positives documented.
    - `get_variables({})`: 107.
    - Screenshots captured (or Tier-2 fallback documented).
    - Description identifiers per joel-only convention (`--token-usage-check.png` not `--side-by-side.png`).
  </acceptance_criteria>
  <done>
    Tag frame passes sweep. Screenshots ready for joel-only token-usage calibration gate.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Plan-close calibration AskUserQuestion gate per CALIBRATION-PROTOCOL § 4.5 (joel-only token-usage format)</name>
  <read_first>
    - `.planning/research/CALIBRATION-PROTOCOL.md` § 4.5 (joel-only AskUserQuestion format — token-usage check vs RpGbe `_Tokens & Foundations`)
    - Task 5 scratch: sweep results + screenshot outcomes
    - `.planning/phases/27-thank-you-contact-reconstruction/27-01-SUMMARY.md` Thank-you joel-only calibration precedent
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-01-SUMMARY.md` FAQ joel-only calibration precedent
  </read_first>
  <what-built>
    A new `Tag` top-level page frame at canvas (~25727, -4111.55) with 5 stacked content blocks: Section/Header (G0wNOc, no override) + page-intro (STUB representative 'Posts tagged "automation"' heading-1 + '3 posts about automation' subhead + sibling dynamic-generation wiring note) + Section/TagFilter ref instance with descendants override flipping active-pill index from position 1 ("All Posts") to position 2 ("automation") — DIFFERENT active-pill index than Plan 28-01 Blog index (validates D-117 paired-ref flexibility) + 6-Card grid (3-col × 2-row, all 6 Cards include 'automation' tag in footer-actions-slot Badge/Outline overrides + sibling filtering wiring note) + Section/Footer (Xs0Hs, no override).

    Token bindings audit-trailed in PEN-INVENTORY Variant Evidence (Phase 28). Token surface unchanged at 107.

    **15 of 15 total Phase 28 Card instances complete (6 Blog index + 3 RelatedPosts + 6 Tag page).** OPEN-25-07 RESOLVES at this plan close.

    PAGE-11 INERT per § 4.3 — no Crito source raster to remove (Tag page is joel-only-no-crito-ref).
  </what-built>
  <how-to-verify>
    **VALID-02 calibration per § 4.1 (joel-only branch — token-usage check, NOT raster side-by-side):**

    1. Open `design/Crito.pen` in Pencil editor.
    2. Locate reconstructed `Tag` frame at canvas (~25727, -4111.55) — next-right of `<blogPostId>` Blog Post frame from Plan 28-02.
    3. Open `_Tokens & Foundations` reference frame (RpGbe) in same Pencil canvas (top of file per Phase 23 D-19 archival).
    4. Token-usage check per § 4.1: for each section, verify the typography + color + spacing + radii used match a semantic token in RpGbe (no raw values; no unmapped hex; no unfamiliar weights).
    5. Per-section fidelity proposals:
       - **Page-intro**: heading 'Posts tagged "automation"' (heading-1 token) + subhead '3 posts about automation' (prose-paragraph token) + sibling wiring note — fidelity proposal: STUB (representative content; actual content dynamic at code-milestone per D-83 mixed-fidelity)
       - **TagFilter strip**: active-state DIFFERENT from Blog index — "automation" pill (position 2) is now active (solid M7eUr style); "All Posts" pill (position 1) is now inactive (outline hIWuC style). Validates D-117 paired-ref flexibility. — fidelity proposal: APPROXIMATE (paired-ref descendants override mechanic works as expected)
       - **6-Card grid**: 3-col × 2-row density; all 6 Cards include 'automation' Badge/Outline tag pill in footer-actions-slot — fidelity proposal: APPROXIMATE (representative filtered post set; image-slot default placeholder per D-113)
       - **Header**: EXACT carry-forward.
       - **Footer**: EXACT carry-forward.
    6. **Phase 28 Card consumer-resolution validation:** 15 Card instances across 3 frames (Blog index 6 + RelatedPosts 3 + Tag page 6). All use slot-content variation per Phase 25 D-49 (single Card component serves all three blog surfaces) without variant-axis proliferation. — Does this validate Phase 25 D-49 single-Card claim?
    7. Stale-cache note: if Task 5 screenshots blank-white (Tier-1 fallback), verify visually in Pencil editor at Tag canvas position per § 6.4 Tier-2.

    **Decision per § 4.5 step 6:** APPROVE / REVISE / GAP.
  </how-to-verify>
  <resume-signal>
    Type "APPROVED" to:
    - Lock the 5 fidelity labels (page-intro STUB, TagFilter APPROXIMATE active-flipped, 6-Card grid APPROXIMATE, Header EXACT, Footer EXACT)
    - Confirm Phase 25 D-49 single-Card claim validated by 15 instances across 3 Phase 28 frames
    - RESOLVE OPEN-25-07 in PEN-INVENTORY
    - Trigger Plan 28-03 close + end-of-phase id-inventory.json archival

    Type "REVISE: <description>" to refine specific sections before APPROVE.

    Type "GAP: <description>" to record OPEN-28-NN without blocking phase close.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 7: ONLY IF APPROVE — Update PEN-INVENTORY + archive id-inventory.json + write 28-03-SUMMARY.md (Phase 28 close)</name>
  <files>.planning/research/PEN-INVENTORY.md; .planning/research/exports/v2.0/end-of-phase-28/id-inventory.json (NEW); design/Crito.pen (read-only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-126 (PEN-INVENTORY extension pattern)
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` end-of-phase id-inventory.json archival precedent
    - `.planning/research/exports/v2.0/end-of-phase-27/id-inventory.json` archival pattern reference
    - Task 6 user decision: APPROVE / REVISE / GAP
  </read_first>
  <action>
    **PRECONDITION:** User APPROVED at Task 6. If REVISE: loop back. If GAP: record OPEN-28-NN.

    Step 1 — Pre-flight per D-125.

    Step 2 — Update PEN-INVENTORY.md:

    **Add Tag row to Frames Inventory:**
    - Frame: Tag (`<tagPageId>`)
    - Scope: joel-only-no-crito-ref
    - joel_page_map: /blog/tags/[tag]
    - Status: reconstructed-PHASE-28
    - PAGE-11 status: INERT (no Crito source raster — joel-only branch per § 4.3)
    - Notes: "Tag page reconstruction per D-122 Plan 28-03 + CALIBRATION-PROTOCOL § 4 joel-only branch. 5 content blocks: Header + page-intro (STUB representative) + TagFilter active-flipped + 6-Card grid (all 'automation' tagged) + Footer. THIRD INERT-branch consumer of CALIBRATION-PROTOCOL after Phase 26 FAQ + 404 + Phase 27 Thank-you. Validates D-117 paired-ref active-state flexibility (different active-pill index than Plan 28-01 Blog index). FINAL Phase 28 Card consumer-resolution site (6 Tag instances + 6 Blog index + 3 RelatedPosts = 15 total Cards = OPEN-25-07 RESOLVED)."

    **Update OPEN-25-07:** RESOLVED — "First concrete Compound/Card consumer-resolution complete at Phase 28 close. 15 Card instances across 3 frames (Blog index 6 + RelatedPosts 3 + Tag page 6) all use slot-content variation (title + metadata + tags + image) without variant-axis proliferation. Phase 25 D-49 single-Card claim validated. Card values locked: 320×fit_content base; image-slot 180h default placeholder; title-slot Plus Jakarta Sans 18/700 + metadata-caption Inter 14/400 #52525bff; body-slot default placeholder; footer-actions-slot multi-purpose (Blog/Tag instances use 2-3 Badge/Outline; Phase 27 Sidebar Card used Button — D-52 validated). Phase 29 Projects inherits same pattern for Project Card composition."

    **Add Variant Evidence (Phase 28) rows for Plan 28-03:**
    - Tag page frame structural composition (5 children, joel-only-no-crito-ref scope)
    - Page-intro STUB content + sibling dynamic-generation wiring note
    - TagFilter active-state-flip override (validates D-117 paired-ref flexibility) — descendants override map specifics
    - 6-Card grid layout (3-col × 2-row, mirror of Plan 28-01 pattern; all Cards include 'automation' tag)

    **Add OPEN-28-NN rows** if Task 6 raised GAP flags.

    **Final Phase 28 PEN-INVENTORY summary at end of Open Flags section:**
    - "Phase 28 close (Plan 28-03 APPROVE 2026-06-08+):
      - 3 new page frames: Blog (DzqTm hidden), Blog Post (w1m3x hidden), Tag (joel-only PAGE-11 INERT)
      - 7 new prose tokens (D-105 deliverable): heading-3 + heading-4 + mono-primitive + inline-code + code-block + prose-link + prose-list
      - 2 new Section components: Section/TagFilter (broad-scoping D-115) + Section/RelatedPosts (narrow-scoping D-116)
      - 1 Card title-slot extension (vGH3A + metadata-caption per Focus 3 Path A)
      - 1 Badge variant (Badge/Outline per D-119)
      - 15 total Card instances (6 Blog index + 3 RelatedPosts + 6 Tag page — OPEN-25-07 RESOLVED)
      - Token surface: 100 → 107 user-facing (raw Pencil ~125)
      - OPEN-23-05 partial: 07_Blog + 08_Blog Details both hidden (4 remaining: 04_About / 05_Service / 06_Service Details / 09_Contact already done in Phase 27)
      - OPEN-23-10 partial: heading-3 + heading-4 shipped; heading-5/-6 remain
      - OPEN-23-11 RESOLVED: prose-link + prose-list + prose-inline-code shipped + bonus prose-code-block
      - OPEN-25-07 RESOLVED: 15 Card instances validate D-49 single-Card claim
      - prose-paragraph 'provisional' flag resolved per D-110 at Plan 28-02 APPROVE (or OPEN-28-NN if APPROVED-EXCEPT)
      - PAGE-11 ACTIVE 2 production uses (DzqTm + w1m3x; FIRST multi-application in v2.0)"

    Step 3 — Archive id-inventory.json per OPEN-23-01 substitution + Phase 23/24/25/26/27 precedent:
    - Create directory: `.planning/research/exports/v2.0/end-of-phase-28/`
    - Archive structural snapshot via `batch_get({ readDepth: 4, nodeIds: ['<document-root>'] })` (or specific subtree IDs if root-level call is too heavy)
    - Write JSON containing all Phase 28 new IDs (7 token IDs + `<tagFilterId>` + `<relatedPostsId>` + `<metadataCaptionId>` + `<badgeOutlineId>` + `<blogPageId>` + `<blogPostId>` + `<tagPageId>`) + carry-forward Phase 23-27 baselines (avgor, g9oRa5, t67DU6, RpGbe, vGH3A, t40xct, M7eUr, hIWuC, j0FxQZ, G0wNOc, Xs0Hs, csXky, n0QqTd) + DzqTm + w1m3x enabled:false status
    - Matches Phase 27 end-of-phase-27 archival shape

    Step 4 — Plan-close + Phase-close sweep:
    - `get_variables({})` = 107 user-facing.
    - `snapshot_layout({ maxDepth: 0, problemsOnly: true })` = `"No layout problems."` at document root.
    - `batch_get` final intactness on all Phase 28 deliverables (3 page frames + 2 Section components + Card extension + Badge/Outline + 7 prose tokens via spot-check + DzqTm/w1m3x enabled:false preserved).

    Step 5 — Write `28-03-SUMMARY.md` per template:
    - Objective + outcome + 5 sections fidelity labels + user APPROVE
    - Token surface unchanged at 107
    - 15 Card instances total (final tally; OPEN-25-07 RESOLVED)
    - D-117 paired-ref flexibility validation outcome
    - PEN-INVENTORY changes summary
    - id-inventory.json archival path
    - Phase 28 close final summary (all deliverables shipped per CONTEXT D-105 through D-126)
    - Carry-forward to Phase 29 (Projects)

    Step 6 — Update STATE.md:
    - Phase 28 complete; phases completed 5 → 6; total plans completed 20 → 24
    - Current focus: Phase 29 (Projects Reconstruction)
    - Stopped at: Phase 28 complete
    - Resume file: `.planning/phases/29-projects-reconstruction/29-CONTEXT.md` (when created)
    - Next action: `/gsd:plan-phase 29` after `/gsd:discuss-phase 29` + `/gsd:research-phase 29`
  </action>
  <verify>
    PEN-INVENTORY updated with Tag row + OPEN-25-07 RESOLVED + Phase 28 close summary. id-inventory.json archived at end-of-phase-28. 28-03-SUMMARY.md written. STATE.md updated.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - User APPROVE recorded at Task 6.
    - PEN-INVENTORY: Tag row added in Frames Inventory; Variant Evidence Phase 28 rows for Tag; OPEN-25-07 RESOLVED; final Phase 28 close summary added; OPEN-28-NN if any.
    - id-inventory.json archived at `.planning/research/exports/v2.0/end-of-phase-28/id-inventory.json` per OPEN-23-01 substitution + carry-forward pattern.
    - Plan-close sweep PASS (107 tokens + snapshot_layout clean + batch_get intact + DzqTm/w1m3x enabled:false preserved).
    - 28-03-SUMMARY.md written.
    - STATE.md updated (Phase 28 complete; current focus Phase 29).
  </acceptance_criteria>
  <done>
    Phase 28 closes. 3 new blog page frames shipped (Blog + Blog Post + Tag). 7 new prose tokens shipped. 2 new Section components shipped. Card title-slot extended + Badge/Outline variant added. 15 Card instances validate D-49 single-Card claim (OPEN-25-07 RESOLVED). PAGE-11 ACTIVE applied twice (DzqTm + w1m3x). All 4 CALIBRATION-PROTOCOL branches consumed in v2.0 (Phase 26 + 27 + 28). Phase 29 unblocked.
  </done>
</task>

</tasks>

<verification>
- `Tag` page frame created at document root, page-frame row y ≈ -4111, width 1440.
- 5 vertical children: Header + page-intro (STUB) + TagFilter (active-flipped) + 6-Card grid + Footer.
- TagFilter descendants override flips active-pill from position 1 ("All Posts") to position 2 ("automation") per D-117.
- 6 Card instances all include 'automation' tag in footer-actions-slot Badge/Outline overrides.
- Token surface unchanged at 107 user-facing.
- User APPROVED at Task 6 calibration gate (joel-only token-usage format).
- PAGE-11 INERT (no Crito source raster to remove).
- PEN-INVENTORY extended: Tag row + Variant Evidence Phase 28 Tag rows + OPEN-25-07 RESOLVED + Phase 28 close summary + OPEN-28-NN if any.
- id-inventory.json archived at end-of-phase-28.
- 28-03-SUMMARY.md written.
- STATE.md updated.
</verification>

<success_criteria>
- Tag page reconstructed as joel-only-no-crito-ref scope (3rd INERT-branch consumer in v2.0 after Phase 26 FAQ + 404 + Phase 27 Thank-you).
- D-117 paired-ref active-state flexibility validated (different active-pill index than Plan 28-01 Blog index).
- Final 6 Card instances complete (15 of 15 total Phase 28 — OPEN-25-07 RESOLVED).
- Phase 25 D-49 single-Card claim validated by 15 instances across 3 blog surfaces.
- All Phase 28 success criteria from ROADMAP achieved (3 blog frames reconstructed; post body exercises every prose token; TagFilter + RelatedPosts factored as Section components; per-section fidelity labels + user spot-check).
- id-inventory.json archived at end-of-phase-28.
- Phase 29 ready to plan.
</success_criteria>

<output>
After completion, create `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-03-SUMMARY.md` per template with:
- `<tagPageId>` + canvas coords
- 5 content block IDs (Header ref + `<pageIntroId>` + `<tagPageTagFilterRefId>` + `<tagGridId>` + Footer ref)
- 6 Card instance IDs (Cards 1-6) + Badge/Outline tag pill IDs
- Per-section fidelity labels (5: page-intro STUB, TagFilter APPROXIMATE active-flipped, 6-Card grid APPROXIMATE, Header EXACT, Footer EXACT) + user APPROVE
- D-117 paired-ref flexibility validation outcome (different active-pill index works as expected)
- 15 Card instances final tally (6 Blog index + 3 RelatedPosts + 6 Tag page) — OPEN-25-07 RESOLVED
- PEN-INVENTORY changes (Tag row + Variant Evidence Phase 28 + OPEN-25-07 RESOLVED + Phase 28 close summary + OPEN-28-NN if any)
- id-inventory.json archived at end-of-phase-28
- Plan-close + Phase-close sweep results
- STATE.md update outcome (Phase 28 complete)
- Carry-forward to Phase 29 (Projects)
</output>
</content>
</invoke>