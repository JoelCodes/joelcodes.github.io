---
phase: 28-blog-reconstruction-index-post-tag-page
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - design/Crito.pen
  - .planning/research/PEN-INVENTORY.md
autonomous: true
requirements:
  - PAGE-03
  - TOKEN-06
threat_model_summary:
  - id: T-28-01
    threat: "Pencil active-editor mismatch causes write to wrong .pen file"
    severity: high
    mitigation: "D-125 pre-flight `mcp__pencil__get_editor_state({ include_schema: false })` before every mutating call; halt and surface on mismatch."
  - id: T-28-02
    threat: "Token surface drift — accidental token addition/deletion beyond the 7 prose tokens"
    severity: high
    mitigation: "`get_variables({})` snapshot at plan open (expect 100) + after each `set_variables` batch + at plan close (expect 107)."
  - id: T-28-04
    threat: "Card title-slot (vGH3A) descendants override breaks existing Phase 27 Sidebar Card consumer"
    severity: high
    mitigation: "Task 5 extends vGH3A by ADDING a `metadata-caption` text node child (Path A per RESEARCH Focus 3); Phase 27 sidebar Card already overrides `kI3bc` content — no removal of kI3bc; new caption ships with `enabled: true` default. Backward-compatible: Phase 27 sidebar Card consumer can override `<metadata-caption-id>.enabled: false` post-hoc if visual collision surfaces (none expected — Phase 27 sidebar Card has no canvas re-render until next user edit)."

must_haves:
  truths:
    - "Pre-flight assertion (D-125) succeeded before every mutating call in this plan; active editor == design/Crito.pen confirmed at each pre-flight check."
    - "Token surface grew from exactly 100 (carried from Plan 27-02) to exactly 107 — 7 new prose tokens added (D-105): type-semantic-heading-3-* (4-part composite per D-106), type-semantic-heading-4-* (4-part composite per D-106), type-primitive-family-mono (mono primitive per D-108), type-semantic-prose-inline-code-* (composite per D-108), type-semantic-prose-code-block-* (composite per D-108), type-semantic-prose-link-* (composite per D-107), type-semantic-prose-list-* (composite per D-109)."
    - "Crito .fig consult attempted FIRST for heading-3 + heading-4 values per D-106; if .fig is zipped-binary unreadable per Phase 26 OPEN-26-01 precedent, interpolation defaults ship (heading-3 = 24/700/lh 1.4; heading-4 = 20/700/lh 1.4) and OPEN-28-NN flag raised with proposed values + escalation note."
    - "Crito .fig consult attempted FIRST for mono primitive font choice per D-108; if .fig silent, default system mono stack `'SF Mono', Menlo, Monaco, Consolas, monospace` ships."
    - "`Section / TagFilter` component (broad-scoping per D-115) exists inside `_Components / Sections` (g9oRa5) with 2 slots: heading-slot (frame, enabled:false default) + pills-row (horizontal frame, gap 12, alignItems center) containing 10 paired-ref children per RESEARCH Focus 6 Sub-option A1 (5 positions × 2 refs each: M7eUr active + hIWuC inactive)."
    - "Sibling Pencil note at TagFilter canvas position inside g9oRa5 documents slot signature + active-state mechanic per D-117 + D-52 belt-and-suspenders."
    - "`Section / RelatedPosts` component (narrow-scoping per D-116) exists inside `_Components / Sections` (g9oRa5) with 2 slots: heading-slot (frame, enabled:true default, heading text 'Related posts' using heading-2 token surface) + cards-row (horizontal frame, gap 24, alignItems start) containing 3 Compound/Card refs (t40xct)."
    - "Sibling Pencil note at RelatedPosts canvas position inside g9oRa5 documents slot signature + 3-card density per D-114 + D-118 + D-52 belt-and-suspenders."
    - "`Compound / Card` title-slot (vGH3A) extended per RESEARCH Focus 3 Path A: layout switched to vertical with gap 4; existing `kI3bc` title text node preserved; NEW `metadata-caption` text node child inserted (`enabled: true` default, body-sm typography, color-semantic-text-secondary, content 'DATE • READ TIME' placeholder). Phase 28 Card consumers override this caption content per D-111."
    - "Badge variant evaluation completed per D-119 plan-execution decision: either `Primitive / Badge / Outline` variant added inside avgor (RECOMMENDED per RESEARCH Focus 7: white/light bg + 1-2px border + cornerRadius 10) OR Badge surface confirmed sufficient via descendants-override path with documented rationale. Outcome recorded in 28-00-SUMMARY.md."
    - "Phase 23-27 baseline IDs intact (verified via batch_get): avgor, g9oRa5, t67DU6, RpGbe, nwJk7, vGH3A (extended, NOT replaced), oTSwn, FGdti, eNqxd, M7eUr, hIWuC, u7NmaS, G0wNOc, Xs0Hs, t40xct, Hs5rc, csXky, n0QqTd, j0FxQZ."
    - "PEN-INVENTORY extended per D-126: NEW `## Token Extensions (Phase 28)` sub-section with 7 prose-token rows + source-derivation audit-trail; NEW `## Variant Evidence (Phase 28)` sub-section with Section/TagFilter slot signature + Section/RelatedPosts slot signature + Card title-slot extension row + Badge/Outline variant row (if added); `## Open Flags — Phase 28 (OPEN-28-NN)` section seeded for any plan-surfaced flags (heading-3/-4 .fig escalation if needed, mono-family choice escalation if needed)."
    - "`snapshot_layout({ maxDepth: 0, problemsOnly: true })` at document root returns `\"No layout problems.\"`; per-frame text-clipping false-positives documented per Phase 24/25/26/27 quirk in 28-00-SUMMARY.md."
    - "No user-calibration gate fires at plan close per D-124 + D-86 carry-forward (foundation plan is agent-deterministic); mid-plan user gate fires ONLY IF heading-3/-4 .fig escalation per D-106 + Phase 26 D-72 precedent."
  artifacts:
    - path: "design/Crito.pen"
      provides: "7 new prose tokens (heading-3 composite + heading-4 composite + mono primitive + inline-code composite + code-block composite + prose-link composite + prose-list composite); Section/TagFilter component inside g9oRa5 with 10 paired-ref pills + heading-slot disabled; Section/RelatedPosts component inside g9oRa5 with 3 Card refs + heading-slot enabled 'Related posts'; Compound/Card title-slot (vGH3A) extension with metadata-caption text node child; optional Badge/Outline variant inside avgor."
      contains: "type-semantic-heading-3-* (4 vars), type-semantic-heading-4-* (4 vars), type-primitive-family-mono (1 var), type-semantic-prose-inline-code-* (composite), type-semantic-prose-code-block-* (composite), type-semantic-prose-link-* (composite), type-semantic-prose-list-* (composite); TagFilter id <tagFilterId>, RelatedPosts id <relatedPostsId>, vGH3A extended with <metadataCaptionId>, optional <badgeOutlineId>."
    - path: ".planning/research/PEN-INVENTORY.md"
      provides: "Token Extensions (Phase 28) sub-section + Variant Evidence (Phase 28) sub-section + Open Flags Phase 28 section seeded; OPEN-23-11 marked RESOLVED (prose-link + prose-list + prose-inline-code shipped + bonus prose-code-block); OPEN-23-10 marked partial (heading-3 + heading-4 shipped; heading-5/-6 remain)."
      contains: "## Token Extensions (Phase 28), ## Variant Evidence (Phase 28), ## Open Flags — Phase 28 (OPEN-28-NN), OPEN-23-11 RESOLVED, OPEN-23-10 partial"
  key_links:
    - from: "Section / TagFilter pills-row"
      to: "Primitive / Button / Default (M7eUr) + Primitive / Button / Secondary (hIWuC)"
      via: "Paired-ref children at 5 positions per RESEARCH Focus 6 Sub-option A1; M7eUr default-active at pill-1, hIWuC default-active at pills 2-5"
      pattern: "tagFilter.*pills-row.*ref.*M7eUr.*ref.*hIWuC"
    - from: "Section / RelatedPosts cards-row"
      to: "Compound / Card (t40xct)"
      via: "3 ref children (typed-slot suggestion `slot: ['t40xct']` per D-52)"
      pattern: "relatedPosts.*cards-row.*ref.*t40xct"
    - from: "Compound / Card title-slot (vGH3A)"
      to: "metadata-caption text node (NEW per Focus 3 Path A)"
      via: "Insert as sibling text node below kI3bc; layout vertical gap 4"
      pattern: "vGH3A.*layout.*vertical.*metadata-caption"
    - from: "7 new prose tokens"
      to: "Phase 28 post body section consumers (Plan 28-02)"
      via: "Composite token references inside post body section text/frame nodes"
      pattern: "type-semantic-heading-3.*type-semantic-prose-(inline-code|code-block|link|list).*type-primitive-family-mono"
---

<objective>
Phase 28 foundation plan (D-122 + D-101 foundation-first carry-forward). Ships the long-promised prose token surface (OPEN-23-11 RESOLVED + OPEN-23-10 partial) + 2 new Section components + Compound/Card title-slot extension + possible Badge/Outline variant.

Scope (D-105 + D-115 + D-116 + Focus 3 Path A + Focus 7):
1. Probe Crito .fig for prose-token source evidence per D-106 + D-107 + D-108 (Phase 26 D-72 mechanic carry-forward).
2. Write 7 new prose tokens via `set_variables` (D-105):
   - `type-semantic-heading-3-*` composite (size + weight + lh + family per D-106)
   - `type-semantic-heading-4-*` composite (size + weight + lh + family per D-106)
   - `type-primitive-family-mono` primitive (D-108)
   - `type-semantic-prose-inline-code-*` composite (D-108)
   - `type-semantic-prose-code-block-*` composite (D-108)
   - `type-semantic-prose-link-*` composite (D-107)
   - `type-semantic-prose-list-*` composite (D-109)
3. Extend Compound/Card title-slot (vGH3A) per RESEARCH Focus 3 Path A: switch layout vertical gap 4 + insert metadata-caption text node child.
4. Build `Section / TagFilter` inside g9oRa5 per D-115 + Focus 6 Sub-option A1 (paired-ref pills).
5. Build `Section / RelatedPosts` inside g9oRa5 per D-116 + Focus 2 slot signature (3 Card refs + 'Related posts' heading).
6. Evaluate Badge surface per D-119 + Focus 7 — ship `Primitive / Badge / Outline` variant inside avgor IF Phase 24 surface doesn't cover outline-pill style.
7. Extend PEN-INVENTORY per D-126 + plan-close sweep.

**No user-calibration gate at plan close** per D-124 + D-86 (foundation work agent-deterministic). Mid-plan user gate ONLY IF heading-3/-4 .fig escalation per D-106 + Phase 26 D-72 mechanic.

**Output:** 7 new prose tokens (100 → 107); 2 new Section components; Card title-slot extended; possible Badge/Outline variant; PEN-INVENTORY extended with Phase 28 sub-sections; foundation plan-close.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md
@.planning/phases/28-blog-reconstruction-index-post-tag-page/28-VALIDATION.md
@.planning/research/CALIBRATION-PROTOCOL.md
@.planning/research/PEN-INVENTORY.md
@.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md
@.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md
@.planning/phases/25-section-compound-components/25-01-SUMMARY.md
@.planning/phases/25-section-compound-components/25-02-SUMMARY.md
@.planning/phases/25-section-compound-components/25-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md
@.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md
@.planning/phases/23-audit-token-foundation/23-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight + Crito .fig probe + baseline ID intactness + Phase 24 Badge ID confirmation</name>
  <files>design/Crito.pen (read-only); design/images/Consulting & Agency Website Template I Crito (Community).fig (probe-attempt only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-125 pre-flight + D-106 heading values + D-107 prose-link + D-108 mono + inline-code + code-block + D-119 Badge evaluation
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 1 (.fig consult flow) + Focus 7 (Badge ID `j0FxQZ`)
    - `.planning/phases/23-audit-token-foundation/23-04-SUMMARY.md` Phase 23 semantic-token writing patterns (heading composite shape, prose-paragraph composite shape)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` heading-2 composite shape + D-72 .fig escalation precedent
    - `.planning/research/PEN-INVENTORY.md` token surface count (expect 100); existing baseline IDs (avgor, g9oRa5, t67DU6, RpGbe, vGH3A, t40xct, j0FxQZ)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125: `mcp__pencil__get_editor_state({ include_schema: false })`. Halt + surface on mismatch (active editor must end in `design/Crito.pen`).

    Step 2 — Token surface drift check: `mcp__pencil__get_variables({})`. Assert count is exactly 100 (Phase 27 closed at 100 per Plan 27-02 SUMMARY). Halt on drift.

    Step 3 — Crito .fig consult attempt per D-106 + D-107 + D-108 + RESEARCH Focus 1:
    Attempt to read `design/images/Consulting & Agency Website Template I Crito (Community).fig`. Expected outcome per RESEARCH Focus 1 + Phase 26 OPEN-26-01: zipped Figma binary unreadable by executing agent. Record outcome in 28-00-SUMMARY scratch:
    - `.fig consult attempted: PASS|FAIL`
    - If FAIL: fallback to interpolation defaults per D-106 (heading-3 = 24/700/lh 1.4; heading-4 = 20/700/lh 1.4) + system-mono-stack default per D-108 + accent-fill default per D-107. Record OPEN-28-NN candidates for user surfacing at Task 1-3 mid-plan gate.

    Step 4 — Baseline ID intactness via `batch_get`:
    - Library parents: `avgor` (Primitives), `g9oRa5` (Sections), `t67DU6` (Compounds), `RpGbe` (_Tokens & Foundations)
    - Card slots: `vGH3A` (title-slot), `oTSwn` (body-slot), `FGdti` (image-slot), `eNqxd` (footer-actions-slot), `kI3bc` (title-text node inside vGH3A)
    - Button refs for TagFilter: `M7eUr` (Button/Default), `hIWuC` (Button/Secondary)
    - Compound/Card: `t40xct` (for RelatedPosts cards-row slot)
    - Phase 24 Badge: `j0FxQZ` (per RESEARCH Focus 7 + Plan 24-03 SUMMARY line 62) — confirm ID + read structure (layout, padding, fill, cornerRadius, label child `xSve5`)
    - Phase 26 + 27 page frames: csXky (404), n0QqTd (Contact) — read-only intact check
    - Phase 23 token reference: `RpGbe` _Tokens & Foundations parent

    If `j0FxQZ` returns 404, fall back to `batch_get(['avgor'], readDepth: 2)` to find Badge child ID. Record discovered Badge ID in scratch.

    Step 5 — Read existing prose-paragraph token shape via `get_variables({})` to confirm composite pattern for new prose tokens (heading-3/-4 follow heading-2 composite shape per Phase 26; inline-code + code-block + prose-link + prose-list follow heading composite shape adapted for prose-specific properties per D-105 + D-107 + D-108 + D-109).

    Step 6 — Plan-derive token values per D-106 + D-107 + D-108 + D-109 + RESEARCH Focus 1 (record in scratch for Tasks 1-3 batch_design):
    - heading-3: size 24, weight 700, lh 1.4, family Plus Jakarta Sans (heading family per Phase 26 carry-forward)
    - heading-4: size 20, weight 700, lh 1.4, family Plus Jakarta Sans
    - mono primitive: `'SF Mono', Menlo, Monaco, Consolas, monospace` (D-108 system stack)
    - prose-inline-code composite: family mono + size 14 + bg `color-semantic-bg-surface-elevated` + stroke 1px `color-semantic-border-default` + cornerRadius 4 + horizontal padding 4
    - prose-code-block composite: family mono + size 14 + bg `color-semantic-bg-surface-elevated` + padding 16 + cornerRadius 10 (radius-semantic-card reuse) + lh 1.6
    - prose-link composite: color `color-semantic-text-accent` + font-decoration descriptor (hover-underline rendered as static per D-107 + D-92)
    - prose-list composite: indent 24 + item-gap 8 + bullet-style disc + ordered-style decimal

    NO mutations in this task.
  </action>
  <verify>
    Single message: "Pre-flight PASS. Active editor: design/Crito.pen. Token surface: 100 (matches Phase 27 close). Crito .fig consult attempted: <PASS|FAIL>. Baseline IDs intact: avgor, g9oRa5, t67DU6, RpGbe, vGH3A, kI3bc, t40xct, M7eUr, hIWuC, j0FxQZ, csXky, n0QqTd. Badge ID confirmed: j0FxQZ. Token values derived per D-106+107+108+109; values recorded in scratch."
  </verify>
  <acceptance_criteria>
    - `mcp__pencil__get_editor_state` returned `activeEditor` ending in `design/Crito.pen`.
    - `mcp__pencil__get_variables({})` returned exactly 100 tokens.
    - Crito .fig consult outcome recorded (`PASS` or `FAIL`).
    - `mcp__pencil__batch_get` on all baseline IDs (avgor, g9oRa5, t67DU6, RpGbe, vGH3A, oTSwn, FGdti, eNqxd, kI3bc, M7eUr, hIWuC, t40xct, j0FxQZ, csXky, n0QqTd) returned structures (no 404s); Badge ID `j0FxQZ` confirmed by Phase 24 SUMMARY line 62 match.
    - 7 prose token values derived and recorded in scratch (heading-3 + heading-4 + mono + inline-code + code-block + prose-link + prose-list).
    - No mutations in Task 0.
  </acceptance_criteria>
  <done>
    Pre-flight assertions pass. Crito .fig consult outcome known. Baseline IDs intact. 7 prose token values ready for set_variables in Tasks 1-3. Badge ID confirmed for Focus 7 evaluation in Task 6.
  </done>
</task>

<task type="auto">
  <name>Task 1: set_variables — Write heading-3 + heading-4 composites + mono primitive (3 token sets)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: heading-3 + heading-4 + mono primitive values (interpolation defaults if .fig FAIL)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` heading-2 composite shape precedent (4 vars: family + size + weight + lh)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-106 (heading values) + D-108 (mono primitive)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 1 (.fig fallback flow)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — IF Task 0 .fig consult outcome FAIL AND interpolation defaults feel ambiguous (per Claude judgment), surface mid-plan user gate per D-106 + Phase 26 D-72 mechanic:
    - AskUserQuestion: "Crito .fig is unreadable (zipped binary). Proposed heading-3 = 24/700/lh 1.4 (between heading-2 32 and body 16); proposed heading-4 = 20/700/lh 1.4. APPROVE or specify different values?"
    - On APPROVE: proceed. On REVISE: use user-specified values.
    - Skip this step if .fig consult PASS or values are obvious enough to proceed (interpolation is a sound default per D-106).

    Step 3 — `mcp__pencil__set_variables` for heading-3 composite (4 vars):
    - `type-semantic-heading-3-family`: alias to `type-primitive-family-heading` (Plus Jakarta Sans)
    - `type-semantic-heading-3-size`: alias to `type-primitive-size-24` (or write `type-primitive-size-24` primitive first if it doesn't exist; check Phase 23 primitive set)
    - `type-semantic-heading-3-weight`: alias to `type-primitive-weight-700`
    - `type-semantic-heading-3-lh`: alias to `type-primitive-lh-tight` (lh 1.4)

    Step 4 — `mcp__pencil__set_variables` for heading-4 composite (4 vars):
    - `type-semantic-heading-4-family`: alias to `type-primitive-family-heading`
    - `type-semantic-heading-4-size`: alias to `type-primitive-size-20` (or write primitive first if missing)
    - `type-semantic-heading-4-weight`: alias to `type-primitive-weight-700`
    - `type-semantic-heading-4-lh`: alias to `type-primitive-lh-tight`

    Step 5 — `mcp__pencil__set_variables` for mono primitive (1 var):
    - `type-primitive-family-mono`: literal value `'SF Mono', Menlo, Monaco, Consolas, monospace` (D-108 system stack)

    Step 6 — Verify via `mcp__pencil__get_variables({})`: count should now be 109 (100 + 4 heading-3 + 4 heading-4 + 1 mono = 109). If primitives `type-primitive-size-24` or `type-primitive-size-20` had to be created, count will be higher (record exact delta). Confirm new variable names present.

    Record token IDs + final count in scratch.
  </action>
  <verify>
    `mcp__pencil__get_variables({})` returns ≥ 109 tokens; new variables `type-semantic-heading-3-family/size/weight/lh`, `type-semantic-heading-4-family/size/weight/lh`, `type-primitive-family-mono` are present and resolve correctly.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - If mid-plan user gate fired: user APPROVE or REVISE recorded.
    - `set_variables` calls completed without error.
    - `get_variables({})` count: 109 (or higher if missing primitives were created — exact delta recorded in scratch).
    - New variables resolve: heading-3 composite (4 vars), heading-4 composite (4 vars), mono primitive (1 var).
    - No accidental token deletion.
  </acceptance_criteria>
  <done>
    Heading-3 + heading-4 + mono primitive tokens shipped. Ready for prose composite tokens in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: set_variables — Write prose-inline-code + prose-code-block + prose-link + prose-list composites (4 composite token sets)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - Task 0 scratch: prose composite values per D-107 + D-108 + D-109
    - `.planning/phases/23-audit-token-foundation/23-04-SUMMARY.md` existing prose-paragraph composite shape (mirror for new prose tokens)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-107 prose-link + D-108 inline-code/code-block + D-109 prose-list
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__set_variables` for prose-inline-code composite (per D-108):
    - `type-semantic-prose-inline-code-family`: alias to `type-primitive-family-mono` (from Task 1)
    - `type-semantic-prose-inline-code-size`: alias to `type-primitive-size-14` (existing per Phase 23)
    - `type-semantic-prose-inline-code-bg`: alias to `color-semantic-bg-surface-elevated`
    - `type-semantic-prose-inline-code-stroke`: alias to `color-semantic-border-default`
    - `type-semantic-prose-inline-code-stroke-width`: literal 1
    - `type-semantic-prose-inline-code-radius`: alias to `radius-primitive-4` (or radius-semantic-card if 10 chosen — confirm primitive availability; if `radius-primitive-4` missing, write it OR alias to existing radius-semantic-card; record decision)
    - `type-semantic-prose-inline-code-padding-h`: literal 4

    Step 3 — `mcp__pencil__set_variables` for prose-code-block composite (per D-108):
    - `type-semantic-prose-code-block-family`: alias to `type-primitive-family-mono`
    - `type-semantic-prose-code-block-size`: alias to `type-primitive-size-14`
    - `type-semantic-prose-code-block-bg`: alias to `color-semantic-bg-surface-elevated`
    - `type-semantic-prose-code-block-padding`: literal 16
    - `type-semantic-prose-code-block-radius`: alias to `radius-semantic-card` (10 — radius-primitive-10 carry-forward)
    - `type-semantic-prose-code-block-lh`: alias to `type-primitive-lh-body` (1.6 — confirm exists; if not, write `type-primitive-lh-loose` 1.6 primitive)

    Step 4 — `mcp__pencil__set_variables` for prose-link composite (per D-107):
    - `type-semantic-prose-link-color`: alias to `color-semantic-text-accent`
    - `type-semantic-prose-link-decoration`: literal `"underline-on-hover"` (descriptor per D-107 + D-92 static-design-as-READING-state)

    Step 5 — `mcp__pencil__set_variables` for prose-list composite (per D-109):
    - `type-semantic-prose-list-indent`: literal 24
    - `type-semantic-prose-list-item-gap`: literal 8
    - `type-semantic-prose-list-bullet-style`: literal `"disc"`
    - `type-semantic-prose-list-ordered-style`: literal `"decimal"`

    Step 6 — Verify via `mcp__pencil__get_variables({})`: expected count 109 (post-Task 1) + 7 (inline-code) + 6 (code-block) + 2 (prose-link) + 4 (prose-list) = 128 (range may vary if missing primitives were created).

    **CONTEXT.md token target is "~125" per phase-specific guidance (100 → ~125).** Exact final count = 107 if we count by D-105's 7 token PROMISES (heading-3 + heading-4 + mono + inline-code + code-block + prose-link + prose-list = 7 tokens by user-facing count), even though each is a composite of multiple Pencil variables. **CONTEXT D-105 reads "7 new tokens" where each composite = 1 user-facing token.** The Pencil-variable count may be ~125-128 (composites expand to multiple variables); record both metrics in scratch:
    - "user-facing token count" (per D-105 promise): 100 + 7 = 107
    - "raw Pencil variable count" (per `get_variables({})` actual): 100 + delta (~25-28)

    Both metrics document the same truth at different granularities. PEN-INVENTORY Token Extensions sub-section uses user-facing count.
  </action>
  <verify>
    `mcp__pencil__get_variables({})` confirms 4 new composite token surfaces (inline-code + code-block + prose-link + prose-list) all resolve correctly; Pencil-variable count ~125-128.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `set_variables` for 4 prose composites completed without error.
    - `get_variables({})` confirms all 4 composite surfaces present.
    - User-facing token count documented in scratch (100 → 107 per D-105) AND raw Pencil-variable count documented separately.
    - No accidental token deletion.
  </acceptance_criteria>
  <done>
    All 7 D-105 prose tokens shipped (heading-3 + heading-4 + mono-primitive + inline-code + code-block + prose-link + prose-list). Token-writing phase complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend Compound/Card title-slot (vGH3A) per Focus 3 Path A — switch to vertical layout + insert metadata-caption text node child</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 3 Path A (RECOMMENDED) — title-slot extension mechanic
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` Compound/Card structural details (vGH3A title-slot ID, kI3bc existing title text node, layout currently)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-111 (descendants override mechanic; metadata-caption pre-creation requirement)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__batch_design` Update + Insert on vGH3A:
    - **Update** `vGH3A`: `{ layout: "vertical", gap: 4 }` (switch from current single-child layout to stack)
    - **Insert** as child of `vGH3A` (after existing `kI3bc` title text):
      - `{ type: "text", name: "metadata-caption", content: "DATE • READ TIME", fontFamily: "Inter" (or `type-semantic-body-sm-family` alias), fontSize: 14 (or `type-semantic-body-sm-size`), fontWeight: "normal", fill: <color-semantic-text-secondary literal e.g. "#52525bff" per OPEN-23-13>, lineHeight: 1.625, textGrowth: "fixed-width", width: "fill_container", enabled: true }`

    Per RESEARCH Focus 3 Path A: this pre-creates the metadata caption child so Phase 28 Card consumers can override its content via descendants at instance time (the Phase 27 label-slot pattern applied to Card title-slot).

    Step 3 — Verify via `mcp__pencil__batch_get({ nodeIds: ['vGH3A'], readDepth: 2 })`:
    - vGH3A now has layout `vertical`, gap 4
    - vGH3A has 2 children: kI3bc (title text — unchanged) + `<metadataCaptionId>` (NEW)
    - `<metadataCaptionId>` has `enabled: true`, content "DATE • READ TIME"

    Step 4 — Verify Phase 27 sidebar Card (n0QqTd → sidebar Card descendant) is NOT broken by this change:
    - `mcp__pencil__batch_get({ nodeIds: ['n0QqTd'], readDepth: 4 })` — confirm sidebar Card descendants override map still resolves; `kI3bc` content override "Ready to chat?" still applies.
    - **Expected:** new metadata-caption child shows default "DATE • READ TIME" content on Phase 27 sidebar Card. **This is acceptable** per CONTEXT D-111 + the threat model T-28-04: Phase 27 sidebar Card has no visual re-render until next user edit; if Joel notices the unintended "DATE • READ TIME" on Contact-page sidebar Card, raise OPEN-28-NN with descendants override fix (`metadata-caption.enabled: false` in Phase 27 sidebar Card descendants map). Document this as ACCEPTED-RISK in 28-00-SUMMARY.md.

    Record `<metadataCaptionId>` in scratch.
  </action>
  <verify>
    `batch_get(['vGH3A'], readDepth: 2)` returns vGH3A with layout vertical + gap 4 + 2 children (kI3bc preserved + new metadata-caption text node); metadata-caption has enabled:true.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Update + Insert completed without error.
    - vGH3A layout = vertical, gap = 4.
    - vGH3A has exactly 2 children: kI3bc (preserved) + metadata-caption (NEW with id `<metadataCaptionId>`).
    - metadata-caption has content "DATE • READ TIME", enabled:true, body-sm typography, color-semantic-text-secondary fill.
    - Phase 27 sidebar Card structural intactness verified via batch_get on n0QqTd readDepth 4 (descendants override on kI3bc still resolves).
    - `<metadataCaptionId>` recorded.
  </acceptance_criteria>
  <done>
    Card title-slot extended per Focus 3 Path A. Phase 28 Card consumers (Plans 28-01 + 28-02 + 28-03) can now override metadata-caption.content + .enabled via descendants at instance time.
  </done>
</task>

<task type="auto">
  <name>Task 4: Build Section/TagFilter component (g9oRa5 child) with paired-ref pills + sibling note</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-115 (broad-scoping rationale) + D-117 (active-state mechanic) + D-120 (cornerRadius override)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 2 slot signature + Focus 6 Sub-option A1 paired-ref children pattern + Focus 6 sibling note verbatim
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` Section/CTA + Section/NavBack structural precedent (parent frame placement inside g9oRa5)
    - `.planning/phases/24-layout-primitives-primitive-components/24-05-SUMMARY.md` Button refs M7eUr (Default) + hIWuC (Secondary)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `mcp__pencil__find_empty_space_on_canvas({ width: 600, height: 80, direction: 'right', padding: 80, nodeId: <last-component-in-g9oRa5-id> })` to find placement spot for Section/TagFilter inside g9oRa5 (or pass `nodeId: 'g9oRa5'` parent if Pencil supports child-relative empty space). Record placement coords.

    Step 3 — `mcp__pencil__batch_design` Insert as child of `g9oRa5`:
    - **Parent frame** `Section / TagFilter`:
      - `type: "frame"`, `name: "Section / TagFilter"`, `reusable: true`, `width: "fit_content"` or 1200 (fill_container at instance time), `layout: "vertical"`, `gap: 16`, `padding: 0`, `alignItems: "center"` (Blog index pattern; Tag page also centered).

    - **Child 1 — heading-slot frame:**
      - `type: "frame"`, `name: "heading-slot"`, `slot: []`, `enabled: false` (default per D-115 — Blog index + Tag page both have page-intro headings above strip), `layout: "horizontal"`, `alignItems: "center"`, `width: "fit_content"`.

    - **Child 2 — pills-row frame:**
      - `type: "frame"`, `name: "pills-row"`, `slot: ['M7eUr', 'hIWuC']` (typed-slot suggestion per D-52), `enabled: true`, `layout: "horizontal"`, `gap: 12`, `alignItems: "center"`, `width: "fit_content"`, `wrap: true` (allow wrap if Pencil 2.13 supports; else `false` and accept overflow at consumer).

    - **Pills (5 positions × 2 refs each = 10 paired-ref children per Focus 6 Sub-option A1):**
      - Position 1 — "All Posts":
        - `{ type: "ref", name: "pill-1-active", ref: "M7eUr", enabled: true, descendants: { "ATJK9": { content: "All Posts" }, "<button-frame-id>": { cornerRadius: 9999 } } }` (override cornerRadius per D-120)
        - `{ type: "ref", name: "pill-1-inactive", ref: "hIWuC", enabled: false, descendants: { "<secondary-label-id>": { content: "All Posts" }, "<secondary-frame-id>": { cornerRadius: 9999 } } }`
      - Position 2 — "automation":
        - `{ type: "ref", name: "pill-2-active", ref: "M7eUr", enabled: false, descendants: { "ATJK9": { content: "automation" }, "<button-frame-id>": { cornerRadius: 9999 } } }`
        - `{ type: "ref", name: "pill-2-inactive", ref: "hIWuC", enabled: true, descendants: { "<secondary-label-id>": { content: "automation" }, "<secondary-frame-id>": { cornerRadius: 9999 } } }`
      - Position 3 — "small-business": same pattern (active enabled:false, inactive enabled:true)
      - Position 4 — "productivity": same pattern
      - Position 5 — "ai": same pattern

    **At Blog index instance time** (Plan 28-01): pre-configured Blog index defaults (pill-1-active enabled:true, pill-1-inactive enabled:false; pills 2-5 inverted). No descendants override needed.

    **At Tag page instance time** (Plan 28-03): override `pill-1-active.enabled:false + pill-1-inactive.enabled:true + pill-2-active.enabled:true + pill-2-inactive.enabled:false` (flip pair 1 + pair 2 for "automation" active).

    Step 4 — Insert sibling Pencil note inside g9oRa5 at TagFilter's canvas-y position per RESEARCH Focus 2 verbatim convention:
    - `{ type: "note", name: "Section/TagFilter notes", content: "Section/TagFilter — 2 slots: heading-slot (enabled:false default; instance enable + content if page needs section heading), pills-row (horizontal row of paired Button refs; position 1 default active 'All Posts'; pills 2-5 default inactive tag labels; instance overrides via descendants to toggle pill-N-active.enabled:true + pill-N-inactive.enabled:false at the position being marked active; cornerRadius 9999 override per pill per D-120). Phase 24 Button refs: M7eUr (Default), hIWuC (Secondary). 10 paired-ref children total per Focus 6 Sub-option A1." }`

    Step 5 — Verify via `mcp__pencil__batch_get({ nodeIds: ['<tagFilterId>'], readDepth: 3 })`:
    - 2 slot children: heading-slot (enabled:false) + pills-row (enabled:true)
    - pills-row has 10 ref children (5 positions × 2 refs)
    - pill-1-active enabled:true, pill-1-inactive enabled:false (Blog index defaults)
    - Sibling note exists at canvas-y position

    Step 6 — Verify `snapshot_layout({ parentId: '<tagFilterId>', problemsOnly: true })` returns `"No layout problems."` (text-clipping false-positives documented).

    Record `<tagFilterId>` + child IDs (pill IDs) in scratch.
  </action>
  <verify>
    `batch_get(['<tagFilterId>'], readDepth: 3)` returns Section/TagFilter parent frame with 2 slot children; pills-row has 10 paired-ref children; sibling note exists inside g9oRa5; snapshot_layout clean.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `Section / TagFilter` exists inside g9oRa5 with `reusable: true`.
    - 2 slot children present: heading-slot (enabled:false) + pills-row (enabled:true) per Focus 2 signature.
    - pills-row contains exactly 10 ref children (5 positions × 2 refs M7eUr + hIWuC).
    - Position 1 ("All Posts"): pill-1-active enabled:true (M7eUr), pill-1-inactive enabled:false (hIWuC). Positions 2-5 inverted (inactive default).
    - Sibling Pencil note inside g9oRa5 at canvas position adjacent to TagFilter per Focus 2 verbatim content.
    - `search_all_unique_properties({ parentId: '<tagFilterId>' })` returns zero raw hex/px inside TagFilter component frame (cornerRadius 9999 is a token-bound override at descendants level, not a raw value inside the component itself — verify).
    - `snapshot_layout({ parentId: '<tagFilterId>', problemsOnly: true })` returns `"No layout problems."`.
    - `<tagFilterId>` + pill IDs recorded.
  </acceptance_criteria>
  <done>
    Section/TagFilter shipped with paired-ref active/inactive mechanic. Ready for Blog index (Plan 28-01) + Tag page (Plan 28-03) instances.
  </done>
</task>

<task type="auto">
  <name>Task 5: Build Section/RelatedPosts component (g9oRa5 child) with 3 Card refs + 'Related posts' heading + sibling note</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-116 (narrow-scoping rationale) + D-118 (3 full-shape Card instances) + D-114 (3-card density)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 2 slot signature (heading-slot enabled:true + cards-row with 3 t40xct refs) + verbatim sibling note text
    - `.planning/phases/25-section-compound-components/25-03-SUMMARY.md` Compound/Card t40xct ID + Card slot signature (4 slots)
    - `.planning/phases/26-faq-404-reconstruction-calibration-workflow-established/26-00-SUMMARY.md` Section/CTA precedent for heading-slot with enabled:true + default heading text
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — `find_empty_space_on_canvas({ width: 1200, height: 500, direction: 'right', padding: 80, nodeId: '<tagFilterId>' })` to find placement spot adjacent to Section/TagFilter inside g9oRa5.

    Step 3 — `mcp__pencil__batch_design` Insert as child of `g9oRa5`:
    - **Parent frame** `Section / RelatedPosts`:
      - `type: "frame"`, `name: "Section / RelatedPosts"`, `reusable: true`, `width: 1200` (fill_container at instance time), `layout: "vertical"`, `gap: 24`, `padding: [64, 0]` (section-y rhythm per Phase 23 space-semantic-section-y), `alignItems: "start"`.

    - **Child 1 — heading-slot frame** (enabled:true default per D-53 Phase 25 carry-forward):
      - `type: "frame"`, `name: "heading-slot"`, `slot: []`, `enabled: true`, `layout: "horizontal"`, `alignItems: "center"`, `width: "fill_container"`.
      - **Child of heading-slot — heading text node:**
        - `{ type: "text", name: "heading", content: "Related posts", fontFamily: "Plus Jakarta Sans" (or `type-semantic-heading-2-family` alias), fontSize: 32 (or `type-semantic-heading-2-size`), fontWeight: 700, fill: <color-semantic-text-primary literal e.g. "#141f39ff">, lineHeight: 1.4 }` — uses Phase 26 D-72 heading-2 token surface.

    - **Child 2 — cards-row frame:**
      - `type: "frame"`, `name: "cards-row"`, `slot: ['t40xct']` (typed-slot suggestion per D-52), `enabled: true`, `layout: "horizontal"`, `gap: 24`, `alignItems: "start"`, `width: "fill_container"`.
      - **3 Card refs (cards-row children) per D-118 + D-114:**
        - `{ type: "ref", name: "card-1", ref: "t40xct" }` (default placeholders inherited from Phase 25 D-53)
        - `{ type: "ref", name: "card-2", ref: "t40xct" }`
        - `{ type: "ref", name: "card-3", ref: "t40xct" }`

    Step 4 — Insert sibling Pencil note inside g9oRa5 at RelatedPosts canvas-y position per RESEARCH Focus 2 verbatim convention:
    - `{ type: "note", name: "Section/RelatedPosts notes", content: "Section/RelatedPosts — 2 slots: heading-slot (default 'Related posts' heading-2 surface; enabled:true per D-53; instance may override content if page needs alternate heading), cards-row (horizontal row of 3 Compound/Card refs; instances override per D-111 + D-112 + D-113 Card-consumer pattern — metadata-caption content + footer-actions-slot Badge instances + image-slot default placeholder + sibling wiring note). Phase 25 Card ref: t40xct. Single consumer Phase 28 Plan 28-02 Blog post detail." }`

    Step 5 — Verify via `mcp__pencil__batch_get({ nodeIds: ['<relatedPostsId>'], readDepth: 3 })`:
    - 2 slot children: heading-slot (enabled:true, contains heading text "Related posts") + cards-row (enabled:true, contains 3 Card refs t40xct)
    - Sibling note exists at canvas-y position

    Step 6 — Verify `snapshot_layout({ parentId: '<relatedPostsId>', problemsOnly: true })` returns `"No layout problems."`.

    Record `<relatedPostsId>` + child IDs in scratch.
  </action>
  <verify>
    `batch_get(['<relatedPostsId>'], readDepth: 3)` returns Section/RelatedPosts parent frame with 2 slot children; cards-row has exactly 3 Card refs (t40xct); heading-slot contains "Related posts" text; sibling note exists.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `Section / RelatedPosts` exists inside g9oRa5 with `reusable: true`.
    - 2 slot children present: heading-slot (enabled:true, "Related posts" heading-2) + cards-row (enabled:true).
    - cards-row contains exactly 3 ref children (all t40xct).
    - Sibling Pencil note inside g9oRa5 per Focus 2 verbatim content.
    - `search_all_unique_properties({ parentId: '<relatedPostsId>' })` returns zero raw hex/px inside RelatedPosts component frame.
    - `snapshot_layout({ parentId: '<relatedPostsId>', problemsOnly: true })` returns `"No layout problems."`.
    - `<relatedPostsId>` + child IDs recorded.
  </acceptance_criteria>
  <done>
    Section/RelatedPosts shipped with 3 Card refs. Ready for Blog post detail (Plan 28-02) consumer.
  </done>
</task>

<task type="auto">
  <name>Task 6: Badge variant evaluation + ship Primitive/Badge/Outline IF needed (D-119 plan-execution decision)</name>
  <files>design/Crito.pen</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-119 (mix-per-consumer hybrid strategy) + D-121 (post-detail tag row Badge usage)
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-RESEARCH.md` Focus 7 (Badge variant evaluation — recommends shipping Badge/Outline; default visual: white/light bg + 1-2px border + cornerRadius 10)
    - `.planning/phases/24-layout-primitives-primitive-components/24-03-SUMMARY.md` Badge structural details (j0FxQZ + xSve5 label child; current solid cyan fill #15bee3ff)
    - Task 0 scratch: confirmed Badge ID `j0FxQZ` + Badge structure
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125.

    Step 2 — Evaluate Badge surface per D-119 + Focus 7. Decision criteria:
    - Phase 24 Badge (j0FxQZ) ships SOLID cyan fill `#15bee3ff` (color-semantic-bg-accent-secondary or equivalent).
    - D-119 requires outline-pill style for Card tags (footer-actions-slot per D-112) + post-detail tag row (per D-121).
    - **DECISION:** Ship `Primitive / Badge / Outline` variant inside avgor per Focus 7 (RECOMMENDED — both Card tags + post-detail tags need outline style; ~15 instances at Plan 28-01 + ~3-4 at Plan 28-02 = ~18-19 consumers).

    Step 3 — `mcp__pencil__batch_design` Insert as child of `avgor`:
    - **Parent frame** `Primitive / Badge / Outline`:
      - `type: "frame"`, `name: "Primitive / Badge / Outline"`, `reusable: true`, `width: "fit_content"`, `layout: "horizontal"`, `gap: 8` (matches Phase 24 Badge default), `padding: [4, 12]` (matches Phase 24 Badge default), `fill: "#ffffffff"` (color-semantic-bg-page literal per OPEN-23-13; matches v1.3 white/light bg), `stroke: <color-semantic-border-accent literal e.g. "#15bee3ff" or color-semantic-border-default>` (matches v1.3 turquoise border; if accent-border token doesn't exist, use color-semantic-border-default per Focus 7), `strokeWidth: 1` (1-2px range per Focus 7; ship 1 first; user gate revises to 2 if visually too light at Plan 28-01 calibration), `cornerRadius: 10` (radius-semantic-button reuse per Phase 24 D-32 Case B carry-forward).

    - **Child — label text node** (matches Phase 24 Badge label child structure):
      - `{ type: "text", name: "label", content: "Badge", fontFamily: "Inter" (or `type-primitive-family-body`), fontSize: 14 (or `type-semantic-body-sm-size`), fontWeight: 500, fill: <color-semantic-text-primary literal e.g. "#141f39ff"> }` (NOT white per Focus 7 — fill is light, so text is dark).

    Step 4 — Verify via `mcp__pencil__batch_get({ nodeIds: ['<badgeOutlineId>'], readDepth: 2 })`:
    - Badge/Outline exists inside avgor with reusable:true
    - Label child exists with content "Badge"
    - Fill white, stroke 1px border, cornerRadius 10

    Step 5 — Verify `snapshot_layout({ parentId: '<badgeOutlineId>', problemsOnly: true })` returns `"No layout problems."`.

    Step 6 — Verify `search_all_unique_properties({ parentId: '<badgeOutlineId>' })` returns the token-binding inventory (literal hex per OPEN-23-13 + PEN-INVENTORY Variant Evidence row binds literals to tokens — same precedent as Phase 24 Badge j0FxQZ + Phase 25 Card t40xct).

    Record `<badgeOutlineId>` in scratch.
  </action>
  <verify>
    `batch_get(['<badgeOutlineId>'], readDepth: 2)` returns Badge/Outline frame with white fill, 1px stroke, cornerRadius 10, single label child; snapshot_layout clean.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - `batch_design` Insert completed without error.
    - `Primitive / Badge / Outline` exists inside avgor with `reusable: true`.
    - Layout horizontal, gap 8, padding [4, 12] (matches Phase 24 Badge structural baseline).
    - Fill white (color-semantic-bg-page literal), stroke 1px (color-semantic-border-default or accent variant), cornerRadius 10 (radius-semantic-button reuse).
    - Label child Inter 14/500, color-semantic-text-primary fill (NOT white).
    - `snapshot_layout({ parentId: '<badgeOutlineId>', problemsOnly: true })` returns `"No layout problems."`.
    - `<badgeOutlineId>` recorded.
    - PEN-INVENTORY Variant Evidence row to be added at Task 7 binds literals to tokens per OPEN-23-13 dual-track.
  </acceptance_criteria>
  <done>
    Primitive/Badge/Outline shipped. Ready for Card tag instances (Plans 28-01 + 28-02 + 28-03) + post-detail tag row instances (Plan 28-02).
  </done>
</task>

<task type="auto">
  <name>Task 7: Extend PEN-INVENTORY with Phase 28 Token Extensions + Variant Evidence + Open Flags sub-sections; plan-close sweep</name>
  <files>.planning/research/PEN-INVENTORY.md; design/Crito.pen (read-only)</files>
  <read_first>
    - `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-CONTEXT.md` D-126 (PEN-INVENTORY extension pattern)
    - `.planning/research/PEN-INVENTORY.md` existing structure (Token Extensions Phase 26 + Variant Evidence Phase 25/26/27 precedent)
    - Task 0-6 scratch: all token IDs + component IDs + label/pill/Card IDs
    - `.planning/phases/27-thank-you-contact-reconstruction/27-02-SUMMARY.md` PEN-INVENTORY extension precedent (Calendly Wiring Map + Variant Evidence Phase 27)
  </read_first>
  <action>
    Step 1 — Pre-flight per D-125 (final pre-flight before .md mutation — verify no concurrent Pencil mutations occurred).

    Step 2 — Edit `.planning/research/PEN-INVENTORY.md`:

    **Add new sub-section `## Token Extensions (Phase 28)`:**
    - Header: phase 28 ships 7 prose tokens; resolves OPEN-23-11 + partial OPEN-23-10
    - Rows for each of 7 tokens with: token name, composite shape, source-evidence (.fig PASS|FAIL + interpolation/system-default), real consumer (D-123 sample MDX line references)

    **Add new sub-section `## Variant Evidence (Phase 28)`:**
    - Row for Section/TagFilter slot signature (heading-slot enabled:false + pills-row with 10 paired-ref children per Focus 6 Sub-option A1)
    - Row for Section/RelatedPosts slot signature (heading-slot enabled:true 'Related posts' + cards-row 3 Card refs)
    - Row for Card title-slot extension (vGH3A layout switch + metadata-caption child insertion per Focus 3 Path A)
    - Row for Primitive/Badge/Outline (if added in Task 6) with literal-to-token binding per OPEN-23-13 dual-track
    - Row for cornerRadius 9999 instance-override convention on TagFilter pills (D-120 default-position; OPEN-23-12 remains open pending Plan 28-01 hacky-or-clean evaluation)

    **Add new section `## Open Flags — Phase 28 (OPEN-28-NN)`:**
    - Populate with any flags raised in Tasks 0-6 (heading-3/-4 .fig escalation outcome if user gate fired; mono-family choice escalation if needed; Phase 27 sidebar Card metadata-caption visual collision risk per T-28-04 mitigation; etc.)
    - If no flags raised: section header + "No Phase 28 OPEN flags at plan 28-00 close. Plans 28-01 + 28-02 + 28-03 may raise OPEN-28-NN at calibration gates."

    **Update existing OPEN rows:**
    - OPEN-23-11: mark RESOLVED (prose-link + prose-list + prose-inline-code shipped per Plan 28-00; bonus prose-code-block added per D-108 going beyond original scope)
    - OPEN-23-10: mark partial (heading-3 + heading-4 shipped; heading-5/-6 remain open — no current consumer per D-106 strict Pitfall 1)
    - OPEN-25-07: status "consumer-ready" (Phase 25 D-50 OPEN waits for first concrete Card consumer; Plans 28-01 + 28-02 + 28-03 are the consumers — flag stays open until Plan 28-01 closes with calibration APPROVE)

    Step 3 — Plan-close sweep:
    - `mcp__pencil__get_variables({})` — verify user-facing count = 107 (or document raw Pencil count if higher due to primitives created)
    - `mcp__pencil__batch_get({ nodeIds: ['<tagFilterId>', '<relatedPostsId>', 'vGH3A', '<badgeOutlineId>'], readDepth: 1 })` — final intactness check on all Plan 28-00 deliverables
    - `mcp__pencil__snapshot_layout({ maxDepth: 0, problemsOnly: true })` — expect `"No layout problems."` at document root (per-frame text-clipping false-positives documented in 28-00-SUMMARY)
    - `mcp__pencil__search_all_unique_properties` scoped to TagFilter + RelatedPosts + Badge/Outline (if added) — confirm zero raw hex/px inside component bodies (literals at descendants override level OR documented in PEN-INVENTORY Variant Evidence per OPEN-23-13 dual-track are acceptable; raw values inside the component frames are NOT)

    Step 4 — Write `28-00-SUMMARY.md` per template (objective + outcome + deliverables + PEN-INVENTORY changes + open flags + next-plan prep + token surface delta + sweep results + acceptance criteria match).
  </action>
  <verify>
    `cat .planning/research/PEN-INVENTORY.md | grep "## Token Extensions (Phase 28)"` returns the new sub-section header; `## Variant Evidence (Phase 28)` and `## Open Flags — Phase 28 (OPEN-28-NN)` both present; OPEN-23-11 marked RESOLVED; OPEN-23-10 marked partial; final sweep all PASS.
  </verify>
  <acceptance_criteria>
    - Pre-flight PASSED.
    - PEN-INVENTORY.md contains new sub-sections: Token Extensions (Phase 28), Variant Evidence (Phase 28), Open Flags — Phase 28.
    - 7 token rows in Token Extensions (heading-3 + heading-4 + mono + inline-code + code-block + prose-link + prose-list).
    - Variant Evidence rows for Section/TagFilter + Section/RelatedPosts + Card title-slot extension + (conditional) Badge/Outline.
    - OPEN-23-11 marked RESOLVED, OPEN-23-10 marked partial, OPEN-25-07 status updated to "consumer-ready".
    - Plan-close sweep results:
      - Token surface 107 user-facing (raw Pencil count documented).
      - `batch_get` on Plan 28-00 deliverables confirms intactness.
      - `snapshot_layout` document root: `"No layout problems."`.
      - `search_all_unique_properties` scoped sweep: zero raw hex/px inside new component bodies (descendants overrides + PEN-INVENTORY-documented literals exempt per OPEN-23-13).
    - 28-00-SUMMARY.md written per template.
  </acceptance_criteria>
  <done>
    Phase 28 foundation complete. 7 prose tokens shipped, 2 Section components built, Card title-slot extended, possible Badge/Outline variant added. PEN-INVENTORY extended. Plan 28-00 closes WITHOUT user gate per D-124 (foundation work agent-deterministic). Plans 28-01 + 28-02 + 28-03 unblocked.
  </done>
</task>

</tasks>

<verification>
- `mcp__pencil__get_editor_state` returns Crito.pen active editor at every pre-flight.
- `mcp__pencil__get_variables({})` returns user-facing count 100 → 107 (or raw Pencil count documented).
- 7 new prose tokens resolve correctly (heading-3 + heading-4 + mono + inline-code + code-block + prose-link + prose-list).
- Section/TagFilter exists inside g9oRa5 with 2 slots + 10 paired-ref pills + sibling note.
- Section/RelatedPosts exists inside g9oRa5 with 2 slots + 3 Card refs + 'Related posts' heading + sibling note.
- Compound/Card title-slot (vGH3A) extended with metadata-caption child per Focus 3 Path A.
- Primitive/Badge/Outline shipped inside avgor (or evaluation outcome documented if not shipped).
- PEN-INVENTORY extended with 3 new sub-sections + OPEN flag updates.
- `snapshot_layout({ maxDepth: 0, problemsOnly: true })` returns `"No layout problems."` at document root.
- 28-00-SUMMARY.md written.
</verification>

<success_criteria>
- 7 new prose tokens shipped (D-105 deliverable).
- Section/TagFilter + Section/RelatedPosts components shipped (D-115 + D-116 deliverables).
- Compound/Card title-slot extended for descendants-override metadata insertion (Focus 3 Path A — unblocks D-111 at instance time in Plans 28-01 + 28-02 + 28-03).
- Badge variant evaluation completed (Focus 7 — Badge/Outline shipped if needed).
- PEN-INVENTORY extended per D-126 (Token Extensions + Variant Evidence + Open Flags sub-sections).
- Zero user-calibration gate at plan close per D-124 + D-86 (foundation plan).
- Plans 28-01 + 28-02 + 28-03 ready to instance Plan 28-00 deliverables.
</success_criteria>

<output>
After completion, create `.planning/phases/28-blog-reconstruction-index-post-tag-page/28-00-SUMMARY.md` per `./.claude/get-shit-done/templates/summary.md` with:
- Token surface delta (100 → 107 user-facing + raw Pencil count)
- 7 new prose token IDs + composite shapes
- Section/TagFilter ID + 10 pill IDs
- Section/RelatedPosts ID + 3 Card ref IDs + heading text ID
- Card title-slot extension: metadata-caption ID + vGH3A layout update
- Badge/Outline ID (if shipped) + variant evaluation outcome
- PEN-INVENTORY sub-section additions list
- Any OPEN-28-NN flags raised
- Crito .fig consult outcome (PASS|FAIL + fallback used)
- Phase 27 sidebar Card metadata-caption collision-risk acceptance per T-28-04
- Plan-close sweep results (token count + batch_get + snapshot_layout + search_all_unique_properties)
- Carry-forward IDs for Plans 28-01 + 28-02 + 28-03 (tagFilterId, relatedPostsId, badgeOutlineId, metadataCaptionId)
</output>
</content>
</invoke>