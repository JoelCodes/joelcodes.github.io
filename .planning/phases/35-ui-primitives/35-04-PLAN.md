---
phase: 35-ui-primitives
plan: 04
type: execute
wave: 3
depends_on: ["35-02", "35-03"]
requirements: [COMP-01, COMP-02]
files_modified:
  - src/pages/dev/primitives.astro
  - tests/accessibility/primitives.spec.ts
autonomous: false
user_setup: []

must_haves:
  truths:
    - "All eight primitives (CTAButton, Eyebrow, Tag, Callout, LinkCard, Breadcrumb, Step, ServiceCard) render on /dev/primitives ordered like the Figma Components page 36:5 (D-12)"
    - "The isolation page builds only in dev — production builds contain no /dev/primitives route (D-14)"
    - "On-dark variants (ghost-on-dark CTAButton, Eyebrow onDark) sit on an ink-colored strip so both light and dark OS screenshots capture them (D-13)"
    - "The temporary axe spec passes with zero violations in both light mode and prefers-color-scheme:dark against /dev/primitives (D-15)"
    - "After the final deletion commit, npm run build produces no reference to primitives in dist/ (D-14 verification)"
    - "Joel approves the Figma 36:5 vs rendered /dev/primitives screenshot comparison (light + dark) before the phase is marked done (ROADMAP success criterion 4)"
  artifacts:
    - path: "src/pages/dev/primitives.astro"
      provides: "DEV-gated isolation page exercising all 8 primitives in Figma 36:5 order — TEMPORARY, deleted in this plan's final task"
      min_lines: 40
      contains: "import.meta.env.PROD"
    - path: "tests/accessibility/primitives.spec.ts"
      provides: "Temporary light+dark axe spec against /dev/primitives — deleted with the page"
      min_lines: 20
      contains: "colorScheme: 'dark'"
  key_links:
    - from: "src/pages/dev/primitives.astro"
      to: "src/components/wl/*.astro (all 8)"
      via: "imports every primitive and renders each variant"
      pattern: "import .* from '../../components/wl/"
    - from: "tests/accessibility/primitives.spec.ts"
      to: "/dev/primitives"
      via: "page.goto + AxeBuilder in light and dark contexts"
      pattern: "/dev/primitives"
    - from: "src/pages/dev/primitives.astro"
      to: "import.meta.env.PROD redirect"
      via: "DEV-gate blocks prod build (blog precedent)"
      pattern: "import.meta.env.PROD"
---

<objective>
Exercise all eight primitives on a temporary DEV-gated `/dev/primitives` isolation page ordered like the Figma Components page `36:5`, verify zero axe violations in both themes, run the Figma-vs-rendered fidelity gate for Joel's approval, then delete the isolation page and its spec and confirm they are absent from the production build.

Purpose: This is the phase's closing gate. The isolation page doubles as the fidelity-comparison artifact (its layout mirrors `36:5` so the side-by-side is mechanical) and as the axe-coverage surface for every primitive at once. Deletion-before-merge (D-14) plus the prod-build grep prevents the dev page from ever shipping — a security requirement flagged for this phase.

Output: a verified primitive library, Joel's fidelity approval, and a clean working tree (isolation page + spec deleted, prod build clean).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-ui-primitives/35-UI-SPEC.md
@.planning/phases/35-ui-primitives/35-PATTERNS.md
@.planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md
@src/pages/blog/index.astro
@tests/accessibility/dark-mode.spec.ts
@tests/accessibility/helpers.ts
@src/layouts/BaseLayout.astro
</context>

<threat_model>
Scope: a DEV-only Astro page + a Playwright spec. The security-relevant concern is the dev page leaking into production.

| ID | Threat | Vector | Mitigation | ASVS |
|----|--------|--------|------------|------|
| T-01 | Dev isolation page ships to production | An accidental early merge, or a missing DEV-gate, exposes `/dev/primitives` (an internal, unpolished surface) on the live site and in the sitemap. | Two-layer defense: (1) `if (import.meta.env.PROD) return Astro.redirect('/')` gate copied from blog precedent so even a pre-deletion merge cannot serve it; (2) the final task DELETES the file and spec, verified by `npm run build && grep -r "primitives" dist/` returning zero (D-14). Both layers are required. | V14.3 |
| T-02 | Stale test referencing a deleted route | The axe spec outlives the page and fails CI against a 404. | The spec is deleted in the SAME final task as the page; the post-deletion `npx playwright test` run must pass with the spec gone. | V14.2 |

No high-severity threats — the page is build-time static and never reaches production if the gate + deletion hold.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build DEV-gated /dev/primitives isolation page + temporary axe spec, verify both themes</name>
  <read_first>
    - src/pages/blog/index.astro (lines 1-10 — the exact `if (import.meta.env.PROD) { return Astro.redirect('/'); }` DEV-gate to copy verbatim; note the relative import depth from src/pages/*)
    - src/layouts/BaseLayout.astro (the layout wrapper + how title is passed)
    - tests/accessibility/dark-mode.spec.ts (lines 1-34 — the exact axe + `browser.newContext({ colorScheme: 'dark' })` + `toHaveClass(/dark/)` + `settleAnimations` pattern to copy-adapt)
    - tests/accessibility/helpers.ts (import `settleAnimations` — do not reimplement)
    - .planning/phases/35-ui-primitives/35-PATTERNS.md (the isolation-page structure template: Figma 36:5 order, light section on paper, ink strip for on-dark variants)
    - .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md (the confirmed Figma 36:5 component order for D-12; the actual component labels to use so copy mirrors Figma, not invented placeholders)
    - all eight src/components/wl/*.astro (their prop signatures — import and exercise each variant correctly)
  </read_first>
  <action>
    Create `src/pages/dev/primitives.astro`. First line of frontmatter: the DEV-gate `if (import.meta.env.PROD) { return Astro.redirect('/'); }` copied from blog/index.astro (threat T-01, D-14). Then import BaseLayout and all eight primitives from `../../components/wl/`.

    Render, inside BaseLayout, every primitive and every variant grouped and ORDERED to match the Figma Components page `36:5` layout (D-12 — use the order confirmed in 35-FIGMA-EXTRACTION.md). Structure:
    - A light-surface `<section>` on `var(--color-wl-paper)`: CTAButton solid / ghost / small (with and without calendar + mail icons), Eyebrow on-light, Tag, Callout, LinkCard, Breadcrumb (a representative items array), Step (a couple of numbered steps, wrapped in `<ol>` at the page level to exercise the li-compatible contract), ServiceCard default + highlight.
    - An ink-colored `<section>` using the non-flippable literal `#12333B` (NOT `bg-wl-ink`, which flips — D-13): CTAButton ghost-on-dark, Eyebrow onDark. This strip lets both light and dark OS screenshots capture the on-dark variants.

    Use the real Figma component labels from the extraction artifact for each instance ("Book a call", etc.) — do not invent placeholder copy (UI-SPEC Copywriting Contract).

    Create `tests/accessibility/primitives.spec.ts` by copy-adapting `dark-mode.spec.ts`: a `test.describe('Primitives isolation page — Phase 35 (TEMPORARY — delete with isolation page)')` with two tests — light mode (`page.goto('/dev/primitives')`, `settleAnimations`, `AxeBuilder().withTags([...wcag...]).analyze()`, expect `violations` `[]`) and dark mode (`browser.newContext({ colorScheme: 'dark' })`, assert `html` has class `/dark/`, then the same axe assertion). Import `settleAnimations` from `./helpers`.

    Run `npm run test:a11y`. If axe reports a contrast or ARIA violation, fix the offending PRIMITIVE (not the page) and re-run — the isolation page is the surface, the components are the code under test.
  </action>
  <verify>
    - `npm run build` succeeds
    - `grep -q "import.meta.env.PROD" src/pages/dev/primitives.astro` (DEV-gate present)
    - `grep -c "import .* from '../../components/wl/" src/pages/dev/primitives.astro` returns 8 (all primitives imported)
    - `grep -q "#12333B" src/pages/dev/primitives.astro` (ink strip uses non-flippable literal, not bg-wl-ink — D-13)
    - `grep -q "colorScheme: 'dark'" tests/accessibility/primitives.spec.ts && grep -q "/dev/primitives" tests/accessibility/primitives.spec.ts`
    - `npm run test:a11y` — the primitives spec passes with zero violations in light and dark
    - `node scripts/check-contrast.mjs` — exits 0
  </verify>
  <acceptance_criteria>
    - `src/pages/dev/primitives.astro` exists, its first frontmatter statement is the `import.meta.env.PROD` redirect gate, and it imports and renders all eight primitives with every variant.
    - On-dark variants (CTAButton ghost-on-dark, Eyebrow onDark) render inside a section whose background is the literal `#12333B`, not `bg-wl-ink`.
    - Step instances are wrapped in a page-level `<ol>` (proving the li-compatible contract works).
    - `tests/accessibility/primitives.spec.ts` runs two axe tests (light + `colorScheme: 'dark'`) against `/dev/primitives` and both pass with `violations` equal to `[]`.
    - `npm run test:a11y` exits 0; `node scripts/check-contrast.mjs` exits 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Fidelity gate — Figma 36:5 vs rendered /dev/primitives (Joel approval)</name>
  <what-built>
    All eight Wavelength primitives (CTAButton 4 variants + icons, Eyebrow on-light/on-dark, Tag, Callout, LinkCard, Breadcrumb, Step, ServiceCard default/highlight) rendered on the `/dev/primitives` isolation page, ordered to mirror the Figma Components page `36:5`, with an ink strip showing the on-dark variants. Light and dark axe suites pass; the contrast gate passes; the SiteHeader CTA was retrofitted to CTAButton (Plan 02).
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (serves at http://localhost:4321).
    2. Open http://localhost:4321/dev/primitives in a browser with LIGHT OS appearance. Capture a screenshot at 1440px width.
    3. Switch OS appearance to DARK (or use browser devtools "Emulate CSS prefers-color-scheme: dark"). Reload and capture a second screenshot at 1440px.
    4. Open the Figma file `1tg8wIPcvOVC5tPZ8pkGO2`, Components page `36:5`, and place the rendered screenshots side-by-side with the Figma primitives block.
    5. Confirm each primitive matches Figma: geometry (padding, radius, gaps), colors (fills, text, borders), and the on-dark variants on the ink strip. Also confirm the SiteHeader "Book a call" CTA on any page looks identical to the Phase 34-approved header (D-02 pixel-neutral).
    6. Note any FLAGGED-GAP items carried from Plan 01 — decide whether they block approval or are acceptable to defer.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to deletion, or describe the specific fidelity deltas to fix (the executor will correct the offending component and re-run the axe + contrast gates before re-presenting).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Delete isolation page + spec, verify absent from production build</name>
  <read_first>
    - src/pages/dev/primitives.astro (the file to delete)
    - tests/accessibility/primitives.spec.ts (the spec to delete — must go together per threat T-02)
  </read_first>
  <action>
    After Joel's approval, delete both `src/pages/dev/primitives.astro` and `tests/accessibility/primitives.spec.ts` (D-14, D-15). If `src/pages/dev/` is now empty, remove the empty directory.

    Run `npm run build` then `grep -r "primitives" dist/` — it must return zero results (threat T-01 verification; the two-layer defense means even the redirect stub is gone once the file is deleted). Run `npx playwright test` — the remaining accessibility suite must pass with the temporary spec gone (threat T-02).

    Do NOT delete the eight `src/components/wl/*.astro` primitives, the SiteHeader retrofit, or the `scripts/check-contrast.mjs` Phase 35 additions — those are the durable phase output. Only the isolation page and its temporary spec die.
  </action>
  <verify>
    - `test ! -f src/pages/dev/primitives.astro` (deleted)
    - `test ! -f tests/accessibility/primitives.spec.ts` (deleted)
    - `npm run build && grep -r "primitives" dist/` returns zero results (D-14, threat T-01)
    - `npx playwright test` passes with the temporary spec removed (threat T-02)
    - `test -f src/components/wl/CTAButton.astro` (durable output survives — sanity check)
  </verify>
  <acceptance_criteria>
    - `src/pages/dev/primitives.astro` and `tests/accessibility/primitives.spec.ts` no longer exist in the working tree.
    - `npm run build` followed by `grep -r "primitives" dist/` returns zero matches.
    - `npx playwright test` exits 0 with no reference to the deleted spec.
    - All eight `src/components/wl/*.astro` files and the SiteHeader retrofit remain intact.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run test:a11y` passes light + dark on /dev/primitives (before deletion)
- `node scripts/check-contrast.mjs` exits 0
- Joel approves the fidelity comparison (Task 2)
- After deletion: `npm run build && grep -r "primitives" dist/` returns zero; `npx playwright test` passes
</verification>

<success_criteria>
- All 8 primitives exercised on /dev/primitives in Figma 36:5 order (D-12), on-dark variants on an ink strip (D-13)
- Zero axe violations in both themes (D-15); contrast gate green
- Joel approves Figma-vs-rendered fidelity comparison (ROADMAP success criterion 4)
- Isolation page + spec deleted; production build contains no /dev/primitives (D-14, threat T-01)
- Durable primitive library + SiteHeader retrofit remain
</success_criteria>

<output>
After completion, create `.planning/phases/35-ui-primitives/35-04-SUMMARY.md` recording: the fidelity-gate outcome (approved / deltas fixed), any FLAGGED-GAP items Joel accepted or deferred, confirmation of the prod-build grep result, and the final durable file list (8 wl/ components + SiteHeader retrofit + check-contrast additions).
</output>
