---
phase: 35-ui-primitives
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
requirements: [COMP-02]
files_modified:
  - src/components/wl/Tag.astro
  - src/components/wl/Callout.astro
  - src/components/wl/LinkCard.astro
  - src/components/wl/Breadcrumb.astro
  - src/components/wl/Step.astro
  - src/components/wl/ServiceCard.astro
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Each supporting primitive (Tag, Callout, LinkCard, Breadcrumb, Step, ServiceCard) exists as an individual Astro component in src/components/wl/ using only --wl-* tokens and the Phase 33 type ramp"
    - "Breadcrumb uses the WAI-ARIA pattern: <nav aria-label='Breadcrumb'> + <ol> + aria-current='page' on the last item"
    - "LinkCard renders as an <a href> with the project-wide focus ring and hover:text-wl-accent"
    - "ServiceCard renders both default and highlight variants via a variant prop (D-01)"
    - "Step renders content compatible with use inside <ol> (does not emit its own <ol>); the number circle is aria-hidden decorative"
    - "Zero references to old neobrutalist token names appear in any of the six new files (D-03, success criterion 2)"
  artifacts:
    - path: "src/components/wl/Tag.astro"
      provides: "COMP-02 Tag primitive — single-slot label pill"
      min_lines: 8
    - path: "src/components/wl/Callout.astro"
      provides: "COMP-02 Callout primitive — slot-driven notice block"
      min_lines: 8
    - path: "src/components/wl/LinkCard.astro"
      provides: "COMP-02 LinkCard — <a href> card with title prop + description slot"
      min_lines: 12
      exports: ["Props: href, title, class"]
    - path: "src/components/wl/Breadcrumb.astro"
      provides: "COMP-02 Breadcrumb — items array, WAI-ARIA nav+ol+aria-current"
      min_lines: 16
      exports: ["Props: items, class"]
    - path: "src/components/wl/Step.astro"
      provides: "COMP-02 Step — number prop + prose slot, li-compatible"
      min_lines: 10
      exports: ["Props: number, class"]
    - path: "src/components/wl/ServiceCard.astro"
      provides: "COMP-02 ServiceCard — default/highlight variants (D-01)"
      min_lines: 12
      exports: ["Props: variant, class"]
  key_links:
    - from: "src/components/wl/*.astro (six primitives)"
      to: "35-FIGMA-EXTRACTION.md"
      via: "fills, text colors, padding, radius, type classes sourced from extraction"
      pattern: "wl-"
    - from: "src/components/wl/Breadcrumb.astro"
      to: "aria-current='page'"
      via: "last item without href renders as non-link current page"
      pattern: "aria-current"
    - from: "src/components/wl/LinkCard.astro"
      to: "focus-visible:outline-wl-accent"
      via: "interactive <a> gets the chrome focus ring"
      pattern: "focus-visible:outline-wl-accent"
---

<objective>
Build the six remaining COMP-02 supporting primitives — Tag, Callout, LinkCard, Breadcrumb, Step, ServiceCard — as individual token-correct Astro components in `src/components/wl/`, each sourcing its geometry and colors from the Plan 01 extraction artifact.

Purpose: These are the composition building blocks Phase 36 content components and Phases 37-39 pages import. Their prop contracts (D-05..D-08) are the API downstream phases build against. They share the WaveMark prop-interface convention and the Phase 33 type ramp, so they are grouped into one plan and split by complexity across two tasks.

Output: six new files in `src/components/wl/`, all axe-ready (verified on the isolation page in Plan 04), all using only `--wl-*` tokens.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-ui-primitives/35-UI-SPEC.md
@.planning/phases/35-ui-primitives/35-PATTERNS.md
@.planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md
@src/components/WaveMark.astro
@src/components/layout/SiteHeader.astro
@src/components/layout/SiteFooter.astro
@src/styles/global.css
</context>

<threat_model>
Scope: six static presentational Astro components. No runtime JS, no forms, no user input, no network.

| ID | Threat | Vector | Mitigation | ASVS |
|----|--------|--------|------------|------|
| T-01 | SVG injection via LinkCard/Breadcrumb icons | If the extraction surfaced an arrow/chevron SVG (LinkCard) or a separator SVG (Breadcrumb), hostile markup could be pasted in. | Copy only viewBox + path geometry from 35-FIGMA-EXTRACTION.md; `aria-hidden="true"`; no `<script>`, `on*=`, `<foreignObject>`, or external href. If no icon was extracted, use a text separator (e.g. `/`) — do not invent an SVG. | V5.3 |
| T-02 | On-dark contrast collapse | If any of these primitives has an on-ink-surface variant, using a flippable token collapses contrast in dark mode. | Only Tag has a possible on-dark case per UI-SPEC; use a non-flippable literal from 117:103 if extracted, else document light-only (D-10). Re-run the contrast gate. | V14 (a11y defense-in-depth) |

No high-severity threats — build-time static markup.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build Tag, Callout, and ServiceCard (presentational, slot/variant-driven)</name>
  <read_first>
    - src/components/WaveMark.astro (the `interface Props { ... class?: string }` + single-line destructure convention, and the `badge ? ... : ...` branch that maps to ServiceCard's `variant === 'highlight'` dispatch)
    - src/components/layout/SiteFooter.astro (body-text-on-a-surface pattern for Callout; footer-local :root non-flippable precedent if a Tag on-dark case exists)
    - .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md (Tag fill/text/padding/radius/type-class; Callout fill/text/padding/radius/border/type-class; ServiceCard default+highlight fills/text/padding/radius and the highlight treatment)
    - .planning/phases/35-ui-primitives/35-UI-SPEC.md (Tag, Callout, ServiceCard visual-contract tables + Accessibility Contract rows for semantic element choice)
    - src/styles/global.css (type ramp classes: .wl-text-small 15px, .wl-text-note 13px, .wl-text-body 16px, .wl-heading-h3 21px — pick per extraction)
  </read_first>
  <action>
    Create three files, each with the WaveMark Props convention and a `class?: string` passthrough. Use ONLY extracted values from 35-FIGMA-EXTRACTION.md; for any FLAGGED-GAP value, add a `FLAGGED-GAP` code comment and record it in the SUMMARY — do not invent.

    `src/components/wl/Tag.astro`: `interface Props { class?: string }`. Render a `<span>` (per UI-SPEC accessibility contract — presentational, no ARIA) with the extracted fill (`bg-wl-*` token or extracted surface), text color, padding, border-radius, and the extracted type class (candidate `.wl-text-small` or `.wl-text-note`). Default `<slot />` for the label. No `dark:` color pairs (tokens flip automatically). If Figma shows a Tag on an ink surface, use a non-flippable literal from 117:103 (threat T-02); otherwise document light-only.

    `src/components/wl/Callout.astro`: `interface Props { class?: string }`. Render a `<div>` (or `<aside role="note">` if extraction indicates an important-notice treatment) with extracted fill, padding, border-radius, and any left-accent border. Body type class candidate `.wl-text-body`. Default `<slot />` for prose.

    `src/components/wl/ServiceCard.astro`: `interface Props { variant?: 'default' | 'highlight'; class?: string }` (D-01), `variant = 'default'` default. Render a `<div>` with a `class:list` that dispatches base styles + `{ 'default-styles': variant === 'default', 'highlight-styles': variant === 'highlight' }` using extracted fills/text/padding/radius and the extracted highlight treatment (border/fill/accent). Heading type-class candidate `.wl-heading-h3`, body `.wl-text-body`. Default `<slot />` for card content.

    All three: zero old-token references, zero `dark:` color overrides on `--wl-*` tokens, no `<script>` tag.
  </action>
  <verify>
    - `npm run build` succeeds
    - `test -f src/components/wl/Tag.astro && test -f src/components/wl/Callout.astro && test -f src/components/wl/ServiceCard.astro`
    - `grep -qE "'default'.*'highlight'|'highlight'.*'default'" src/components/wl/ServiceCard.astro` (variant prop typed)
    - `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow|font-heading" src/components/wl/Tag.astro src/components/wl/Callout.astro src/components/wl/ServiceCard.astro` returns zero results
    - `grep -rE "dark:(bg|text|border)-wl-" src/components/wl/Tag.astro src/components/wl/Callout.astro src/components/wl/ServiceCard.astro` returns zero results (no dark: pairs on flippable tokens)
  </verify>
  <acceptance_criteria>
    - Tag renders `<span>` with default slot; Callout renders `<div>`/`<aside>` with default slot; ServiceCard renders `<div>` with a `variant` prop typed `'default' | 'highlight'` and a class:list branch per variant.
    - Every fill/text/padding/radius value in the three files traces to an EXTRACTED value in 35-FIGMA-EXTRACTION.md, or carries a `FLAGGED-GAP` comment (none invented).
    - No file contains a `dark:`-prefixed `--wl-*` color utility.
    - `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow|font-heading"` across the three files returns zero matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build LinkCard, Breadcrumb, and Step (interactive + a11y-structured)</name>
  <read_first>
    - src/components/layout/SiteHeader.astro (lines 15-19 brand-lockup `<a>` with focus ring for LinkCard; lines 29-55 `<nav aria-label>` + link list for Breadcrumb; the `hover:text-wl-accent` convention on nav links)
    - src/components/WaveMark.astro (number/prop + slot convention for Step)
    - .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md (LinkCard fill/title-color/description-color/padding/radius/hover/arrow-SVG; Breadcrumb separator/type-class/link-color/current-color/gap; Step number-circle size/fill/text + body type class + gap)
    - .planning/phases/35-ui-primitives/35-UI-SPEC.md (LinkCard, Breadcrumb, Step visual-contract tables + Accessibility Contract; the Breadcrumb WAI-ARIA snippet; the Step "li-compatible, does not emit its own <ol>" rule)
    - .planning/phases/35-ui-primitives/35-PATTERNS.md (Breadcrumb `.map` + `aria-current` template; Step number-circle + prose layout template)
    - src/styles/global.css (type ramp: .wl-text-note 13px, .wl-text-small 15px, .wl-text-body 16px, .wl-text-body-large 17px)
  </read_first>
  <action>
    Create three files sourcing all values from 35-FIGMA-EXTRACTION.md; FLAGGED-GAP values get a code comment, not an invented value.

    `src/components/wl/LinkCard.astro`: `interface Props { href: string; title: string; class?: string }`. Render `<a href={href} class:list={['block', 'focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-wl-accent', 'hover:text-wl-accent', className]}>` with the extracted card fill/padding/radius/border. Inside: the title in the extracted title type class + color, then the default `<slot />` for description. If extraction includes an arrow/chevron SVG, bake it inline `aria-hidden="true"` with viewBox+geometry only (threat T-01); if none was extracted, render no icon (do not invent). Hover from extraction, else derive `hover:text-wl-accent` from chrome and flag.

    `src/components/wl/Breadcrumb.astro`: `interface Props { items: Array<{ label: string; href?: string }>; class?: string }`. Render `<nav aria-label="Breadcrumb"><ol>` and map items: last item (or any item without `href`) renders as a non-link `<span aria-current="page">`; other items render `<a href hover:text-wl-accent focus-visible:outline-...>`. Between items render the extracted separator (character or aria-hidden SVG); if no separator was extracted use a text `/` in an `aria-hidden="true"` span (do not invent an SVG — threat T-01). Item type class + link/current colors from extraction.

    `src/components/wl/Step.astro`: `interface Props { number: number; class?: string }`. Render a `<div class:list={['flex ...gap...', className]}>` containing a decorative `<span aria-hidden="true">` number circle (extracted size/fill/text color) and a `<div>` with the extracted body type class wrapping the default `<slot />`. The component must NOT emit its own `<ol>` — it stays `<li>`-compatible so a parent page can wrap steps in `<ol>` (RESEARCH.md Pattern 6).

    All three: zero old-token references, zero `dark:` color pairs on `--wl-*` tokens, no `<script>` tag.
  </action>
  <verify>
    - `npm run build` succeeds
    - `test -f src/components/wl/LinkCard.astro && test -f src/components/wl/Breadcrumb.astro && test -f src/components/wl/Step.astro`
    - `grep -q 'aria-label="Breadcrumb"' src/components/wl/Breadcrumb.astro && grep -q 'aria-current="page"' src/components/wl/Breadcrumb.astro && grep -q "<ol" src/components/wl/Breadcrumb.astro`
    - `grep -q "focus-visible:outline-wl-accent" src/components/wl/LinkCard.astro && grep -q "href" src/components/wl/LinkCard.astro`
    - `grep -qv "<ol" src/components/wl/Step.astro` (Step does not emit its own <ol>)
    - `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow|font-heading" src/components/wl/LinkCard.astro src/components/wl/Breadcrumb.astro src/components/wl/Step.astro` returns zero results
  </verify>
  <acceptance_criteria>
    - LinkCard renders `<a href>` with `href` + `title` required props, the chrome focus ring, and a description `<slot>`; any icon is inline aria-hidden SVG with no script/handler/external-href (threat T-01).
    - Breadcrumb renders `<nav aria-label="Breadcrumb"><ol>...</ol></nav>`, the last/href-less item is `aria-current="page"` and is NOT a link, separators are `aria-hidden`.
    - Step renders a decorative `aria-hidden` number circle + prose slot and emits NO `<ol>` element (li-compatible).
    - Every value traces to an EXTRACTED entry in 35-FIGMA-EXTRACTION.md or carries a `FLAGGED-GAP` comment.
    - `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow|font-heading"` across the three files returns zero matches.
    - `npm run build` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `npm run build` — zero errors after both tasks
- All six files exist in `src/components/wl/`
- `grep -rE "bg-yellow|text-turquoise|shadow-neo|border-neo|--color-yellow|font-heading" src/components/wl/` — zero results across the whole directory (D-03 / success criterion 2)
- `node scripts/check-contrast.mjs` — exits 0 (tag/callout/servicecard pairs from Plan 01)
</verification>

<success_criteria>
- Tag, Callout, LinkCard, Breadcrumb, Step, ServiceCard each exist as individual token-correct components (COMP-02)
- Breadcrumb is WAI-ARIA correct (nav+ol+aria-current); LinkCard is a focus-ringed <a>; Step is li-compatible; ServiceCard has default+highlight variants (D-01)
- Zero old-token references anywhere in src/components/wl/ (D-03)
- All values traced to extraction or explicitly flagged (v1.4 lesson, D-04)
</success_criteria>

<output>
After completion, create `.planning/phases/35-ui-primitives/35-03-SUMMARY.md` recording: each component's chosen semantic element and type class, any FLAGGED-GAP values carried forward, and confirmation the contrast gate still exits 0.
</output>
