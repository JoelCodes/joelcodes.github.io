---
phase: 35-ui-primitives
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [COMP-01, COMP-02]
files_modified:
  - .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md
  - scripts/check-contrast.mjs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Every FIDELITY-GAP value declared in 35-UI-SPEC.md and 35-PATTERNS.md has an extracted Figma value OR an explicit FLAGGED-GAP entry — no value is invented (v1.4 lesson)"
    - "The calendar and mail icon SVGs are captured as exact Figma path data + viewBox, not redrawn from a library (D-06 closed-set)"
    - "Every new text-on-background pair the primitives introduce is present in scripts/check-contrast.mjs PAIRS and the script exits 0"
    - "It is documented which CTAButton variant (solid vs small) the SiteHeader Book-a-call CTA maps to (D-02 pixel-neutral prerequisite)"
    - "On-dark colors (ghost-on-dark, eyebrow onDark) are recorded as non-flippable literals sourced from dark mockup 117:103, never from a flippable --wl-* token (D-10)"
  artifacts:
    - path: ".planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md"
      provides: "Extracted Figma values for every FIDELITY-GAP: CTAButton (solid/ghost/ghost-on-dark/small geometry + colors), calendar/mail icon SVG paths, Eyebrow on-light/on-dark, Tag/Callout/LinkCard/Breadcrumb/Step/ServiceCard fills/text/padding/radius, icon-to-label gap, SiteHeader variant mapping"
      min_lines: 60
      contains: "FLAGGED-GAP"
    - path: "scripts/check-contrast.mjs"
      provides: "PAIRS matrix extended with all Phase 35 text-on-background pairs (on-ink, ghost-on-dark, eyebrow-on-dark, tag, callout, servicecard default/highlight)"
      contains: "PHASE 35"
  key_links:
    - from: "35-FIGMA-EXTRACTION.md"
      to: "scripts/check-contrast.mjs"
      via: "extracted hex values become PAIRS rows"
      pattern: "PHASE 35"
    - from: "figma-desktop MCP"
      to: "35-FIGMA-EXTRACTION.md"
      via: "get_design_context / get_variable_defs on nodes 36:5, 39:31, 39:15, 39:30, 117:103"
      pattern: "36:5|39:31|117:103"
---

<objective>
Extract every FIDELITY-GAP value declared by the UI-SPEC and PATTERNS map from Figma file `1tg8wIPcvOVC5tPZ8pkGO2` BEFORE any component CSS is written, and extend the contrast gate with the resulting pairs. This is the Wave 1 gate: all Wave 2 component work reads its exact geometry, colors, and icon SVGs from the artifact this plan produces.

Purpose: The site was abandoned twice (v1.4) partly because code was authored from best-guess interpretations of the design. This plan enforces the "flag, don't invent" discipline (STATE.md lesson, D-04) — every value is either extracted from Figma with a node ID, or explicitly FLAGGED-GAP for Joel's triage. No component gets a value guessed by eye.

Output: `.planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md` (the value source of truth for Wave 2) and an extended `scripts/check-contrast.mjs` PAIRS matrix that exits 0.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-ui-primitives/35-UI-SPEC.md
@.planning/phases/35-ui-primitives/35-PATTERNS.md
@.planning/phases/35-ui-primitives/35-RESEARCH.md
@src/components/layout/SiteHeader.astro
@src/styles/global.css
</context>

<threat_model>
Scope: build-time Figma extraction + a plain-node contrast script. No runtime code, no user input, no network exposure ships from this plan.

| ID | Threat | Vector | Mitigation | ASVS |
|----|--------|--------|------------|------|
| T-01 | SVG injection via icon extraction | The calendar/mail SVGs are copied from Figma into the codebase as inline SVG (Wave 2). Malformed/hostile markup (`<script>`, `onload=`, external `href`) could enter the closed icon set here. | Record ONLY `viewBox` + `<path d=...>`/`<circle>`/`<rect>` geometry with `stroke`/`fill`/`stroke-width` attributes in the extraction artifact. Explicitly exclude `<script>`, event-handler attributes, `<foreignObject>`, and external `xlink:href`/`href` references. Flag any icon node that contains them instead of copying. | V5.3 |
| T-02 | Supply-chain drift | Temptation to `npm i` an icon library instead of extracting. | RESEARCH.md mandates zero new npm deps; icons are Figma-baked (D-06). This plan adds NO dependency — verify `package.json` unchanged in the SUMMARY. | V14.2 |

No high-severity threats — extraction produces a markdown artifact and a build-time script.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extract all Figma FIDELITY-GAP values into 35-FIGMA-EXTRACTION.md</name>
  <read_first>
    - .planning/phases/35-ui-primitives/35-UI-SPEC.md (every FIDELITY-GAP row — this is the extraction checklist; the "Component Visual Contracts" and "Color" sections enumerate what is unknown)
    - .planning/phases/35-ui-primitives/35-PATTERNS.md (FIDELITY-GAP Summary table at end — 13 gap rows with source node IDs)
    - .planning/phases/35-ui-primitives/35-RESEARCH.md ("Figma Extraction Requirements" section + "Open Questions" 1-4)
    - src/components/layout/SiteHeader.astro (the inline CTA at lines 50-54/64-68 is the known spec for solid/small — record it and confirm which Figma node it maps to)
    - src/styles/global.css (@theme tokens + footer-local :root values — the extraction records which new colors are NON-flippable vs which reuse existing flippable tokens)
  </read_first>
  <action>
    Load the figma-desktop MCP tool schemas via ToolSearch FIRST (get_design_context, get_screenshot, get_variable_defs, get_metadata). The Figma file `1tg8wIPcvOVC5tPZ8pkGO2` must be open in Figma desktop; if a tool call fails because it is not open or a node is unreachable, do NOT invent the value — record it as `FLAGGED-GAP` with the reason and continue (v1.4 lesson, PROJECT.md constraint).

    Create `.planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md`. For every FIDELITY-GAP in the UI-SPEC, extract the exact value from Figma and record it with its source node ID. Cover, at minimum, these nodes and values:

    CTA Button set (nodes `39:31` set, `39:15` Solid, `39:30` Small):
    - Solid padding / border-radius / min-height — VERIFY against SiteHeader's known 9px/17px/10px/44px; note any delta.
    - Small padding / border-radius / dimensions (node `39:30`).
    - Ghost border-width, border-color, padding, border-radius (node `39:31` or Solid sibling).
    - icon-to-label gap (the `gap-[Xpx]` value) for all variants.
    - Which node the SiteHeader Book-a-call CTA maps to (solid `39:15` vs small `39:30`) — resolve RESEARCH.md Open Question 1. Record the answer as `SITEHEADER_CTA_VARIANT`.
    - Whether Solid and Small share the `.wl-cta-label` type class (global.css line ~772 says the class was extracted from node 39:30) or Solid uses `.wl-label-button` (16px). Record `SOLID_TYPE_CLASS` and `SMALL_TYPE_CLASS`.
    - calendar icon: viewBox + path/circle/rect geometry with stroke/fill/stroke-width (find the icon child node via get_design_context on `39:31`).
    - mail icon: same.
    For icons, record ONLY geometry per threat T-01 — no `<script>`, no event handlers, no external hrefs; if present, FLAG the node.

    On-dark values from dark Landing mockup `117:103` (D-10 non-flippable literals):
    - ghost-on-dark text color, border color, border-width, hover treatment.
    - Eyebrow on-dark text color.
    - Whether ink-strip sections visibly change between the light and dark views of the mockup — if unchanged, record "stable — confirmed non-flip" with the node ID.

    Components page `36:5` primitives (extract fill, text color, padding, border-radius, border, and type-class candidate for each; check `117:103` for any on-dark variant):
    - Eyebrow: on-light color (confirm it is `--wl-accent`), gap below eyebrow if specced.
    - Tag: fill, text, padding, radius, border, type class.
    - Callout: fill, text, padding, radius, border/left-accent, type class.
    - LinkCard: fill, title color, description color, padding, radius, border, hover, arrow/chevron SVG if present.
    - Breadcrumb: separator character/SVG, item type class, link color, current-page color, item gap.
    - Step: number-circle size/fill/text, number type class, body type class, gap between circle and prose.
    - ServiceCard: default fill/text/padding/radius, highlight fill and the highlight treatment (border? fill? accent icon?), heading + body type-class candidates.

    D-04 flag rule: if the Components page `36:5` contains primitives BEYOND the COMP-01/02 roster (extra icon sets, dividers, unlisted variants), list them under a "Beyond-roster — flag for roadmap triage" heading. Do NOT build them.

    Structure the artifact with one section per component, a table of `property | extracted value | source node ID | notes`, and a top summary table listing every value as `EXTRACTED` or `FLAGGED-GAP`. Every on-dark color must be labelled "non-flippable literal — source 117:103".
  </action>
  <verify>
    - `test -f .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md` succeeds
    - `grep -c "source node\|117:103\|36:5\|39:15\|39:30\|39:31" .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md` returns a non-zero count (node IDs are cited)
    - `grep -q "SITEHEADER_CTA_VARIANT" .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md` (Open Question 1 resolved)
    - `grep -qi "non-flippable" .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md` (D-10 discipline recorded)
    - `git diff --exit-code package.json` (zero new npm dependencies — threat T-02)
  </verify>
  <acceptance_criteria>
    - The artifact contains one section per component: CTAButton, Eyebrow, Tag, Callout, LinkCard, Breadcrumb, Step, ServiceCard, plus an Icons section for calendar + mail SVG geometry.
    - Every value is tagged EXTRACTED (with a Figma node ID) or FLAGGED-GAP (with a reason); the string `FLAGGED-GAP` appears at least once OR the artifact explicitly states "no gaps — all values extracted".
    - `SITEHEADER_CTA_VARIANT` names solid or small; `SMALL_TYPE_CLASS` and `SOLID_TYPE_CLASS` are recorded.
    - calendar and mail icon entries contain a `viewBox` and path/geometry data and contain no `<script>`, `on*=` handler, or external `href` (threat T-01).
    - All four on-dark values (ghost-on-dark text, ghost-on-dark border, ghost-on-dark hover, eyebrow on-dark) are recorded as non-flippable literals sourced from `117:103`, or documented "stable — confirmed non-flip".
    - `package.json` is byte-identical to its pre-task state.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend check-contrast.mjs PAIRS with all Phase 35 text-on-background pairs</name>
  <read_first>
    - scripts/check-contrast.mjs (the PAIRS array ~lines 131-179 and the constants block ~lines 79-119 — replicate the exact row format `[fg_hex, bg_hex, 'label', threshold, textUse]` and reuse existing constants like L_INK, L_PAPER, L_ACCENT, D_INK, D_PAPER, D_ACCENT; do NOT re-implement the ratio formula)
    - .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md (the values extracted in Task 1 — source of every new hex)
    - .planning/phases/35-ui-primitives/35-PATTERNS.md ("check-contrast.mjs — PAIRS extension" section — the exact Phase 35 constant + PAIRS additions template, including the `L_ON_INK = '#EAF6F3'` / `D_ON_INK = '#12333B'` constants)
    - .planning/phases/35-ui-primitives/35-UI-SPEC.md ("Contrast script extension (D-11)" table — the minimum pair list)
  </read_first>
  <action>
    Add a `// ── PHASE 35 ADDITIONS ──` block to `scripts/check-contrast.mjs` extending the PAIRS array with every new text-on-background pair Phase 35 introduces, using hex values from `35-FIGMA-EXTRACTION.md`. Add these constants near the existing block: `L_ON_INK = '#EAF6F3'`, `D_ON_INK = '#12333B'`, plus any extracted on-dark / tag / callout / servicecard fill constants.

    Add PAIRS rows (threshold 4.5, textUse true) for at minimum:
    - CTAButton solid/small: on-ink text on ink bg, light theme (`L_ON_INK` on `L_INK`) AND dark theme (`D_ON_INK` on `D_INK`).
    - CTAButton ghost: ink text on paper (light + dark) — reuse existing rows if already present, add a clarifying label if not.
    - CTAButton ghost-on-dark: extracted foreground on ink surface (non-flippable — single row, does not flip).
    - Eyebrow on-light: accent on paper (light + dark).
    - Eyebrow on-dark: extracted foreground on ink surface (non-flippable).
    - Tag: extracted text on extracted fill.
    - Callout: extracted text on extracted fill.
    - ServiceCard default: text on default fill.
    - ServiceCard highlight: text on highlight fill.

    For any pair whose value is FLAGGED-GAP in the extraction artifact, add the row commented out with a `// FLAGGED-GAP:` note naming the missing value, so the pair is tracked but the script does not fail on an invented value. Every pair with a real extracted value must be active (uncommented).

    Do not modify the ratio-calculation functions or the existing pre-Phase-35 rows.
  </action>
  <verify>
    - `node scripts/check-contrast.mjs` exits 0 (all active TEXT-use pairs pass WCAG AA)
    - `grep -q "PHASE 35" scripts/check-contrast.mjs` (additions block present)
    - `grep -q "on-ink" scripts/check-contrast.mjs` (CTAButton solid pair added)
    - `git diff scripts/check-contrast.mjs | grep -q "^+"` (file was extended)
  </acceptance_criteria>
  <acceptance_criteria>
    - `node scripts/check-contrast.mjs` prints results and exits 0; no active TEXT-use pair reports FAIL.
    - A `PHASE 35` block exists in the PAIRS array with active rows for CTAButton solid on-ink (light + dark), Eyebrow on-light, and every component whose fill/text was EXTRACTED in Task 1.
    - Every pair whose extracted value is FLAGGED-GAP appears as a commented `// FLAGGED-GAP:` row (tracked, not invented, not failing the gate).
    - The existing pre-Phase-35 PAIRS rows and the ratio functions are unchanged (`git diff` shows only additions plus the constants block).
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `test -f .planning/phases/35-ui-primitives/35-FIGMA-EXTRACTION.md` — artifact exists
- `node scripts/check-contrast.mjs` — exits 0
- `git diff --exit-code package.json` — zero new dependencies
- Every FIDELITY-GAP row in 35-UI-SPEC.md has a corresponding EXTRACTED or FLAGGED-GAP entry in the artifact
</verification>

<success_criteria>
- 35-FIGMA-EXTRACTION.md provides an extracted-or-flagged value for every FIDELITY-GAP in the UI-SPEC, each with a Figma node ID
- calendar + mail icon SVG geometry captured (viewBox + paths), free of script/event/external-href (threat T-01)
- SiteHeader CTA variant mapping resolved (D-02 prerequisite)
- On-dark colors recorded as non-flippable literals from 117:103 (D-10)
- check-contrast.mjs extended with all Phase 35 pairs and exits 0
- Zero new npm dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/35-ui-primitives/35-01-SUMMARY.md` recording: the extraction artifact path, every FLAGGED-GAP (so downstream plans and Joel see what could not be extracted), the resolved SITEHEADER_CTA_VARIANT, and the contrast-script pair count added.
</output>
