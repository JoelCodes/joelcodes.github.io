---
phase: 03-portfolio-case-studies
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/pages/portfolio.astro
  - src/data/projects.json
  - src/components/layout/Header.astro
autonomous: true

must_haves:
  truths:
    - "Portfolio grid displays real project card from data"
    - "Portfolio card links to working case study page"
    - "Complete visitor journey from portfolio grid to case study"
  artifacts:
    - path: "src/pages/portfolio.astro"
      provides: "Portfolio grid with data-driven cards"
      contains: "import projects"
  key_links:
    - from: "src/pages/portfolio.astro"
      to: "src/data/projects.json"
      via: "import and map"
      pattern: "projects.map"
    - from: "portfolio card"
      to: "/portfolio/bakery-order-system"
      via: "href link"
      pattern: 'href=.*portfolio/'
---

<objective>
Wire portfolio grid to project data and verify complete flow

Purpose: Connect portfolio page to real project data, completing the portfolio feature
Output: Working portfolio with data-driven grid that links to real case study pages
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-case-studies/03-01-SUMMARY.md
@.planning/phases/03-portfolio-case-studies/03-02-SUMMARY.md
@src/pages/portfolio.astro
@src/data/projects.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update portfolio page to use project data</name>
  <files>src/pages/portfolio.astro</files>
  <action>
Update portfolio.astro to render cards from projects.json instead of hardcoded placeholders.

1. Add import at top of frontmatter:
   ```
   import projects from '../data/projects.json';
   ```

2. Replace hardcoded card HTML with map over projects:
   - Keep the same card structure and classes
   - Use project.slug for href: `/portfolio/${project.slug}`
   - Use project.title for card title
   - Use project.category for data-category attribute
   - Use project.categoryLabel for display text
   - Use project.thumbnail for image src (with fallback gray bg)

3. Update filter logic to work with actual categories from data:
   - Categories should match: "web-apps", "automation", "ai-dev"
   - The bakery project is "web-apps" category

4. Keep placeholder thumbnail styling for now:
   - Gray background as fallback when image doesn't exist
   - Real images to be added separately

Card structure should remain:
```astro
{projects.map((project) => (
  <a href={`/portfolio/${project.slug}`}
     class="portfolio-card show group ..."
     data-category={project.category}>
    <div class="aspect-[16/9] bg-muted-light dark:bg-muted-dark rounded-lg overflow-hidden">
      <!-- Image with fallback -->
    </div>
    <h3 class="...">{project.title}</h3>
    <span class="...">{project.categoryLabel}</span>
  </a>
))}
```
  </action>
  <verify>
Run `npm run dev` and visit http://localhost:4321/portfolio
- Grid shows card for "Bakery Order Automation"
- Card has correct category "Web Apps"
- Clicking card navigates to /portfolio/bakery-order-system
- Filter "Web Apps" shows the bakery card
- Filter "Automation" or "AI Development" hides the bakery card
- Filter "All Projects" shows the bakery card
  </verify>
  <done>
Portfolio grid renders cards from projects.json data with working links to case study pages
  </done>
</task>

<task type="auto">
  <name>Task 2: Add header navigation link to Portfolio</name>
  <files>src/components/layout/Header.astro</files>
  <action>
Add Portfolio link to the site navigation.

1. Find the nav links section in Header.astro (should have Home, Services, About, Contact links)

2. Add Portfolio link after Home:
   - Desktop nav: Add `<a href="/portfolio" class="...">Portfolio</a>`
   - Mobile nav: Add Portfolio link in mobile menu if separate

3. Follow existing link styling patterns in the header

Link order should be: Home, Portfolio, Services, About, Contact
  </action>
  <verify>
Run `npm run dev`
- Desktop: Portfolio link visible in header nav
- Mobile: Portfolio link visible in mobile menu
- Clicking Portfolio navigates to /portfolio page
  </verify>
  <done>
Portfolio page accessible via main site navigation
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` succeeds without errors
2. Portfolio link appears in navigation (desktop and mobile)
3. Portfolio page shows data-driven card(s)
4. Card links navigate to case study page
5. Complete flow works: Nav -> Portfolio -> Case Study -> Back to Portfolio
6. All filter buttons function correctly
</verification>

<success_criteria>
- Portfolio grid populated from projects.json
- All cards link to working case study pages
- Portfolio accessible from site navigation
- End-to-end portfolio flow works for visitors
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-portfolio-case-studies/03-03-SUMMARY.md`
</output>
