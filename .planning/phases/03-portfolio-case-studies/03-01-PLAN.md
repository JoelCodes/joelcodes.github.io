---
phase: 03-portfolio-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/portfolio.astro
  - src/styles/global.css
autonomous: true

must_haves:
  truths:
    - "Portfolio page shows grid of case study cards"
    - "Filter buttons toggle card visibility by category"
    - "Cards have thumbnail images with proper aspect ratio"
    - "Cards link to individual case study pages"
  artifacts:
    - path: "src/pages/portfolio.astro"
      provides: "Portfolio grid page with filter UI"
      contains: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - path: "src/styles/global.css"
      provides: "Portfolio card animation styles"
      contains: "portfolio-card"
  key_links:
    - from: "src/pages/portfolio.astro"
      to: "/portfolio/[slug]"
      via: "anchor href"
      pattern: 'href="/portfolio/'
---

<objective>
Create portfolio page with filterable grid of case study cards

Purpose: Visitors can browse Joel's work and filter by service type (Web Apps, Automation, AI Development)
Output: Portfolio page at /portfolio with 3-column responsive grid and working filter buttons
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-portfolio-case-studies/03-CONTEXT.md
@.planning/phases/03-portfolio-case-studies/03-RESEARCH.md
@src/layouts/BaseLayout.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio page with grid layout and filter buttons</name>
  <files>src/pages/portfolio.astro</files>
  <action>
Create portfolio page using BaseLayout with:

1. Page header section:
   - H1 title "Portfolio" with Poppins font
   - Subtitle explaining the work showcased

2. Filter buttons row:
   - "All Projects" button (active by default)
   - "Web Apps" button
   - "Automation" button
   - "AI Development" button
   - Use pill/rounded-full style matching design system
   - Active button uses accent-yellow, inactive uses muted-light/dark

3. Portfolio grid:
   - Grid layout: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
   - Include 3 placeholder cards for now (will be replaced when case study content exists):
     - Card 1: "Bakery Order System" (web-apps category)
     - Card 2: "Inventory Automation" (automation category)
     - Card 3: "AI Customer Support" (ai-dev category)

4. Each card structure:
   - Link wrapper to /portfolio/[slug]
   - Thumbnail container with aspect-ratio: 16/9
   - Placeholder gray background for thumbnail (real images later)
   - Card title below thumbnail
   - Category tag small text
   - Data attribute for category: data-category="web-apps|automation|ai-dev"
   - Class "portfolio-card show" for filter system
   - Hover effect: lift (-translate-y-2) and shadow increase

5. Filter JavaScript (inline script):
   - filterSelection(category) function toggles .show class on cards
   - setActiveButton(btn) function updates button styles
   - Filter by data-category attribute match
   - "all" shows all cards
   - Initialize with filterSelection('all') on load

Use semantic HTML: main for page, section for grid area, article for cards.
Follow existing patterns from Hero.astro and Services.astro for spacing (py-16 px-4).
  </action>
  <verify>
Run `npm run dev` and visit http://localhost:4321/portfolio
- Page renders with title and filter buttons
- 3 placeholder cards display in grid
- Filter buttons are visible and styled
  </verify>
  <done>
Portfolio page exists at /portfolio with grid layout, filter buttons, and 3 placeholder cards
  </done>
</task>

<task type="auto">
  <name>Task 2: Add portfolio card animation styles to global CSS</name>
  <files>src/styles/global.css</files>
  <action>
Add portfolio-specific styles to global.css:

1. Portfolio card base state:
```css
.portfolio-card {
  display: none;
  opacity: 0;
}

.portfolio-card.show {
  display: block;
  animation: fadeInScale 300ms ease-in-out forwards;
}

@keyframes fadeInScale {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
```

2. Filter button active state (complement the Tailwind classes):
```css
.filter-btn.active {
  /* Active state handled via Tailwind classes in template */
}
```

Note: Use CSS animations (not transitions) per RESEARCH.md to ensure filtering works on every interaction, not just first click.

Place these styles after the existing utility classes in global.css.
  </action>
  <verify>
Run `npm run dev` and test filtering:
- Click "Web Apps" - only web-apps card visible with fade animation
- Click "All Projects" - all cards fade in
- Click different filters - cards animate each time (not just first click)
  </verify>
  <done>
Portfolio cards animate smoothly when filtering, with fadeInScale animation on show
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` succeeds without errors
2. Portfolio page accessible at /portfolio
3. Grid displays 3 columns on desktop, 2 on tablet, 1 on mobile
4. All 4 filter buttons work correctly
5. Cards animate when filtering (test multiple filter switches)
6. Cards have hover lift effect
7. Card links point to /portfolio/[slug] URLs (will 404 until plan 02)
</verification>

<success_criteria>
- Portfolio page renders with responsive grid layout
- Filter buttons toggle card visibility with animation
- Cards have proper aspect ratio thumbnails and hover effects
- Page follows existing design system (colors, typography, spacing)
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-portfolio-case-studies/03-01-SUMMARY.md`
</output>
