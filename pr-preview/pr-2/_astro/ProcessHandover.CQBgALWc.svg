<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
  <!-- Gift box - bottom (cube front) -->
  <path d="M30 50 L50 40 L70 50 L50 60 Z" fill="currentColor" opacity="0.85"/>
  <!-- Gift box - side -->
  <path d="M30 50 L30 70 L50 80 L50 60 Z" fill="currentColor" opacity="0.7"/>
  <!-- Gift box - top face -->
  <path d="M50 40 L70 50 L70 70 L50 60 Z" fill="currentColor" opacity="1"/>
  <!-- Ribbon vertical -->
  <rect x="48" y="35" width="4" height="50" fill="currentColor" opacity="0.5"/>
  <!-- Ribbon horizontal -->
  <path d="M25 48 L75 58" stroke="currentColor" stroke-width="4" opacity="0.5"/>
  <!-- Bow -->
  <circle cx="50" cy="35" r="6" fill="currentColor" opacity="0.8"/>
</svg>
