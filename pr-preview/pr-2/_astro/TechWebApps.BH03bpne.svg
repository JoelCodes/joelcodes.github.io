<svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
  <!-- Browser window outer frame -->
  <rect x="20" y="25" width="60" height="50" fill="none" stroke="currentColor" stroke-width="3" opacity="0.85"/>
  <!-- Address bar -->
  <rect x="20" y="25" width="60" height="8" fill="currentColor" opacity="1"/>
  <!-- Content block 1 (header) -->
  <rect x="26" y="38" width="48" height="6" fill="currentColor" opacity="0.7"/>
  <!-- Content block 2 (left column) -->
  <rect x="26" y="48" width="20" height="20" fill="currentColor" opacity="0.85"/>
  <!-- Content block 3 (right column) -->
  <rect x="50" y="48" width="20" height="20" fill="currentColor" opacity="0.7"/>
</svg>
